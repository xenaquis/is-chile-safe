/**
 * hreflang.mjs — Validate hreflang correctness and reciprocity.
 * Stub: exits 0. Full assertions added in plan 02-06.
 *
 * When complete this will assert:
 * - Every built HTML page has hreflang="en", hreflang="es", hreflang="x-default"
 * - Each EN page's hreflang="es" URL exists as a built ES page
 * - Each ES page's hreflang="en" URL exists as a built EN page
 * - x-default always points to the EN URL
 */
console.log('hreflang: stub (full assertions added in plan 02-06)');
