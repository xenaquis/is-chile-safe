---
/**
 * PageFooter — wordmark (small) + CEAD source attribution + locale switcher link
 *             + legal page links (EDIT-04, D-13).
 * Zero client:* directives (D-09).
 */
import { EN_STRINGS, ES_STRINGS } from '../config/i18n.ts';

interface Props {
  locale: 'en' | 'es';
  enPath: string;
  esPath: string;
}

const { locale, enPath, esPath } = Astro.props;
const t = locale === 'en' ? EN_STRINGS : ES_STRINGS;

const attribution = locale === 'en'
  ? 'Source: CEAD — Centro de Estudios y Análisis del Delito'
  : 'Fuente: CEAD — Centro de Estudios y Análisis del Delito';

const altLocaleLabel = locale === 'en' ? 'ES' : 'EN';
const altHref = locale === 'en' ? esPath : enPath;
---

<footer class="csm-footer">
  <div class="footer-inner page-content">
    <span class="footer-wordmark">Chile Safety Map</span>
    <span class="footer-attribution">{attribution}</span>
    <a href={altHref} class="footer-lang" lang={locale === 'en' ? 'es' : 'en'}>
      {altLocaleLabel}
    </a>
  </div>
  <!-- Spine nav row — global interconnection: map / communes / rankings / editorial (IA-03) -->
  <nav class="footer-spine page-content" aria-label={locale === 'en' ? 'Site navigation' : 'Navegación del sitio'}>
    {locale === 'en' ? (
      <>
        <a href="/map/">{t.footer_spine_map}</a>
        <a href="/communes/">{t.footer_spine_communes}</a>
        <a href="/rankings/">{t.footer_spine_rankings}</a>
        <a href="/news/">{t.nav_news}</a>
        <a href="/safest-cities-in-chile/">{t.footer_spine_guide}</a>
      </>
    ) : (
      <>
        <a href="/es/mapa/">{t.footer_spine_map}</a>
        <a href="/es/comunas/">{t.footer_spine_communes}</a>
        <a href="/es/rankings/">{t.footer_spine_rankings}</a>
        <a href="/es/noticias/">{t.nav_news}</a>
        <a href="/es/comunas-mas-seguras-chile/">{t.footer_spine_guide}</a>
      </>
    )}
  </nav>
  <!-- Legal links row (D-13) — third nav below spine row -->
  <nav class="footer-legal page-content" aria-label="Legal pages">
    {locale === 'en' ? (
      <>
        <a href="/privacy-policy/">{t.footer_privacy}</a>
        <a href="/terms/">{t.footer_terms}</a>
        <a href="/about/">{t.footer_about}</a>
        <a href="/contact/">{t.footer_contact}</a>
      </>
    ) : (
      <>
        <a href="/es/politica-privacidad/">{t.footer_privacy}</a>
        <a href="/es/terminos/">{t.footer_terms}</a>
        <a href="/es/acerca-de/">{t.footer_about}</a>
        <a href="/es/contacto/">{t.footer_contact}</a>
      </>
    )}
  </nav>
</footer>

<style>
  .csm-footer {
    border-top: 1px solid var(--line);
    background: var(--card);
    padding: var(--lg) 0;
    margin-top: var(--2xl);
  }

  .footer-inner {
    display: flex;
    align-items: center;
    gap: var(--md);
    flex-wrap: wrap;
  }

  .footer-wordmark {
    font-size: var(--text-label);
    font-weight: var(--weight-strong);
    color: var(--ink);
  }

  .footer-attribution {
    font-size: var(--text-label);
    font-weight: var(--weight-body);
    color: var(--muted);
    flex: 1;
  }

  .footer-lang {
    font-size: var(--text-label);
    font-weight: var(--weight-strong);
    color: var(--primary);
    text-decoration: none;
  }

  .footer-lang:hover {
    text-decoration: underline;
  }

  .footer-spine {
    display: flex;
    flex-wrap: wrap;
    gap: var(--md);
    margin-top: var(--sm);
    padding-bottom: var(--sm);
  }

  .footer-spine a {
    font-size: var(--text-label);
    font-weight: var(--weight-strong);
    color: var(--primary);
    text-decoration: none;
  }

  .footer-spine a:hover {
    text-decoration: underline;
  }

  .footer-legal {
    display: flex;
    flex-wrap: wrap;
    gap: var(--md);
    margin-top: var(--sm);
    padding-bottom: var(--sm);
  }

  .footer-legal a {
    font-size: var(--text-label);
    font-weight: var(--weight-strong);
    color: var(--muted);
    text-decoration: none;
  }

  .footer-legal a:hover {
    text-decoration: underline;
  }
</style>
