---
phase: 12-home-ia-redesign-comuna-page-hub-and-spoke
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - site/src/layouts/HomeLayout.astro
  - site/src/pages/index.astro
  - site/src/pages/es/index.astro
autonomous: true
requirements: [IA-01]
must_haves:
  truths:
    - "The EN home (/) and ES home (/es/) render the Hybrid C+B above-the-fold: headline + busca-tu-comuna search hero + two lead ranking tables (lowest + highest reported rate) + map CTA + guide cards"
    - "The long editorial prose is condensed to a shorter block placed below the fold"
    - "Each home page has exactly one H1 and remains fully static/indexable with zero client:* directives"
    - "The search hero is a plain HTML form that GET-submits to the comuna directory carrying ?q="
    - "EN/ES parity: both pages pass enPath=/ and esPath=/es/ so hreflang stays reciprocal"
  artifacts:
    - path: "site/src/layouts/HomeLayout.astro"
      provides: "wide full-bleed layout wrapping BaseLayout (no 720px column)"
      contains: "BaseLayout"
    - path: "site/src/pages/index.astro"
      provides: "rebuilt EN home on Hybrid C+B framing"
      min_lines: 80
    - path: "site/src/pages/es/index.astro"
      provides: "rebuilt ES home parity"
      min_lines: 80
  key_links:
    - from: "site/src/pages/index.astro search hero"
      to: "/communes/?q="
      via: "form action=/communes/ method=get"
      pattern: "action=\"/communes/\""
    - from: "site/src/pages/index.astro lead tables"
      to: "CommuneRankingTable.astro"
      via: "build-time sorted top-N rows"
      pattern: "CommuneRankingTable"
---

<objective>
Rebuild the home (EN `/` + ES `/es/`) on the approved Hybrid C+B framing (spike 001-c): a new wide `HomeLayout`, a "busca tu comuna" search hero that GET-routes to the directory, two side-by-side lead ranking tables (lowest + highest reported rate), the national map as a first-class CTA, guide cards to existing editorial pages, and the existing long prose condensed to a short block below the fold. Single H1, zero JS, EN/ES parity (IA-01).

Purpose: The home today is a narrow 720px wall of editorial text with no rankings, no search, no map CTA. The audit found the site "feels scattered." This is the front door.
Output: 1 new layout + 2 rebuilt page files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-CONTEXT.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md
@.planning/spikes/001-home-hub-framing/001-c-ranking-led.html
@.planning/spikes/001-home-hub-framing/README.md

<interfaces>
<!-- Contracts the executor uses directly. -->

From site/src/layouts/EditorialLayout.astro (the analog to copy for HomeLayout — same wrap pattern, replace the 720px .editorial-prose div with a full-width .home-wide div):
  Props: { lang:'en'|'es'; title:string; description:string; enPath:string; esPath:string; jsonLd?:object|null }
  Wraps BaseLayout with lang/title/description/enPath/esPath/jsonLd; BaseLayout already renders PageHeader + PageFooter — HomeLayout must NOT add them.

From site/src/components/CommuneRankingTable.astro (reuse directly — no prop changes):
  interface RankingRow { name; slug; rate; national_rank; trend:'up'|'down'|'stable'; low_population }
  Props: { rows: RankingRow[]; locale: 'en'|'es' }

From site/src/lib/data.ts:
  loadIndex(): CommuneMeta[]   // CommuneMeta = { cut, name, slug, region_id, population, low_population }
  loadCommune(cut): CommuneData   // has national_rank, trend
  latestCompleteYearRate(commune): number
  loadNationalAverage(): number

Existing EN home prose to condense lives in site/src/pages/index.astro L39-167 (5 sections). Existing guide-link list at L111-122.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HomeLayout.astro (wide full-bleed wrapper)</name>
  <read_first>
    - site/src/layouts/EditorialLayout.astro (the analog — copy the wrap + props + AdSlot/CookieConsent pattern; change only the wrapping div width)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md "Pattern 1: HomeLayout" + "Layout Wrap Pattern" + "BaseLayout Props (hreflang)"
  </read_first>
  <action>
    Create site/src/layouts/HomeLayout.astro mirroring EditorialLayout.astro: same imports (BaseLayout, AdSlot, CookieConsent), same Props interface (lang, title, description, enPath, esPath, jsonLd?), same adsenseEnabled check (import.meta.env.ADSENSE_ENABLED === 'true'). The ONLY deviation: replace the div.editorial-prose (720px) wrapper with div.home-wide that has NO max-width constraint (full viewport width with sensible inner page padding). Keep the default slot, the named ad-content slot, the bottom AdSlot position=bottom, and the conditional CookieConsent. Pass all of lang/title/description/enPath/esPath/jsonLd through to BaseLayout (hreflang depends on enPath/esPath). Do NOT declare html/head and do NOT add PageHeader/PageFooter (BaseLayout owns them).
  </action>
  <verify>
    <automated>cd site && npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - site/src/layouts/HomeLayout.astro exists and imports BaseLayout
    - It does NOT set max-width:720px on its content wrapper
    - It passes enPath and esPath to BaseLayout
    - cd site && npm run check exits 0
  </acceptance_criteria>
  <done>HomeLayout.astro wraps BaseLayout full-width, hreflang props passed through, no header/footer duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild EN home (index.astro) on Hybrid C+B</name>
  <read_first>
    - site/src/pages/index.astro (the file being rebuilt — its prose L39-167 is the SOURCE to condense; its guide links L111-122 are the SOURCE for guide cards; preserve the absolute-label disclaimer at L100-102)
    - .planning/spikes/001-home-hub-framing/001-c-ranking-led.html (above-the-fold layout reference)
    - site/src/components/CommuneRankingTable.astro (reuse for the two lead tables)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md index.astro section + "Editorial Tone Guard"
  </read_first>
  <action>
    Rebuild site/src/pages/index.astro to use HomeLayout (not EditorialLayout), keeping title/description/enPath="/"/esPath="/es/". Above the fold, in order: (1) ONE h1 (keep a keyword H1) + a one-line intro; (2) the busca-tu-comuna hero — a plain form with action="/communes/" method="get" containing input type=search name="q" plus a submit button, zero JS (D-05/D-06); (3) two side-by-side lead ranking tables built at build time: load all non-low-population communes via loadIndex, compute latestCompleteYearRate(loadCommune(cut)) for each, sort ascending, take first 6 as the lowest-reported-rate table and the last 6 (reverse to descending) as the highest-reported-rate table; map each row to { name, slug, rate, national_rank, trend, low_population } and pass to CommuneRankingTable with locale="en"; head each table with an H2 framed as reported CEAD incidence (never absolute safe/dangerous labels — Editorial Tone Guard); (4) a map CTA: a prominent a href="/map/" first-class call to the national interactive map; (5) guide cards: static a links to EXISTING editorial pages only (/chile-crime-map/, /is-chile-safe/, /is-santiago-safe/, /safest-cities-in-chile/, /methodology/) — D-04, no new pages. BELOW the fold: condense the 5 existing prose sections (L39-167) into roughly two paragraphs (CEAD source + how to read rates + the absolute-label disclaimer); preserve key factual claims and CEAD attribution; use H2 for the prose heading (no second H1). Zero client:* anywhere. Hardcode all EN hrefs (never getRelativeLocaleUrl for localized slugs).
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3 && node scripts/validate/structure.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dist/index.html contains exactly one h1 element
    - dist/index.html contains the substring action="/communes/"
    - dist/index.html contains the substring href="/map/"
    - dist/index.html contains a rendered ranking table (commune-ranking-table class present)
    - dist/index.html contains zero astro-island / client hydration markers for the home (no client:* on home)
    - cd site && npm run build exits 0
  </acceptance_criteria>
  <done>EN home rebuilt: 1 H1, search hero form, two lead tables, map CTA, guide cards, condensed prose below fold; build passes; zero JS.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild ES home (es/index.astro) at parity</name>
  <read_first>
    - site/src/pages/es/index.astro (the ES file being rebuilt)
    - site/src/pages/index.astro (the EN version just rebuilt in Task 2 — mirror it exactly, ES-localized)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md "Hardcoded Per-Locale Slug Pattern"
  </read_first>
  <action>
    Rebuild site/src/pages/es/index.astro mirroring the EN home from Task 2, ES-localized: HomeLayout with lang="es", enPath="/", esPath="/es/"; the search hero form action="/es/comunas/" method="get" (hardcoded ES slug); the two lead tables pass locale="es" to CommuneRankingTable; the map CTA a href="/es/mapa/" (hardcoded ES slug); guide cards link to the ES editorial pages (use the ES slugs already used elsewhere on the site — e.g. /es/es-seguro-chile/, /es/metodologia/, /es/mapa/ — match the existing ES editorial routes, do NOT invent slugs; if unsure of an ES editorial slug, link only to ES pages confirmed to exist in dist). ONE h1 in Spanish; condensed prose below the fold in Spanish preserving CEAD attribution + the absolute-label disclaimer. Zero client:*. Every ES slug hardcoded per-locale; never getRelativeLocaleUrl for localized slugs.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3 && node scripts/validate/hreflang.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dist/es/index.html contains exactly one h1 element
    - dist/es/index.html contains the substring action="/es/comunas/"
    - dist/es/index.html contains the substring href="/es/mapa/"
    - dist/es/index.html contains a rendered ranking table (commune-ranking-table class)
    - hreflang.mjs validator passes (reciprocal / and /es/ alternates intact)
    - cd site && npm run build exits 0
  </acceptance_criteria>
  <done>ES home rebuilt at parity with EN, ES slugs hardcoded, single H1, hreflang reciprocal, build + hreflang validator pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home search hero → directory | The form GET-submits a user query as ?q= to the directory page (no server processing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering / Reflected XSS | search hero form on home | mitigate | The hero is a native HTML form with method=get; the home itself does NOT read or echo ?q= into markup (the home has no JS). The query is only carried in the URL to the directory, which handles it safely (see T-12-01 in plan 12-01). No reflection on the home. Severity: low. |
| T-12-SC | Tampering | npm installs | accept | No new packages (RESEARCH audit: none). |
</threat_model>

<verification>
- cd site && npm run build exits 0.
- structure.mjs + hreflang.mjs validators pass.
- dist/index.html and dist/es/index.html each contain exactly one h1, a search-hero form (action=/communes/ and /es/comunas/ respectively), a map CTA, and rendered ranking tables.
- No client:* directives on either home page.
</verification>

<success_criteria>
- Both home pages rebuilt on Hybrid C+B: search hero + two lead tables + map CTA + guide cards above the fold, condensed prose below.
- Exactly one H1 per page; static/indexable; zero JS; EN/ES parity with reciprocal hreflang.
- Lead tables reuse CommuneRankingTable.astro; guide cards link existing editorial pages only.
</success_criteria>

<output>
Create `.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-03-SUMMARY.md` when done.
</output>
