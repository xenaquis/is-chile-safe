---
phase: 27-news-facet-data-model
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - site/src/pages/news.astro
  - site/src/pages/es/noticias.astro
autonomous: true
requirements: [FACET-01, FACET-06, FACET-07]
must_haves:
  truths:
    - "Both /news/ and /es/noticias/ import and call the exact same computeNewsFacets() from newsFacets.ts — no per-locale drift in facet logic"
    - "Both pages' existing existsSync/try-catch graceful-degradation for a missing/malformed current.json is unchanged (empty-state fallback still works)"
    - "No facet artifact is written under site/** and no new derived JSON is committed anywhere"
    - "Full build+validate chain (16/16 validators including facets.mjs) passes in ONE command; @astrojs/check shows zero new errors vs the 4-error ComparatorPairsLinks.astro baseline; pytest suite is re-confirmed at 344 passed / 1 skipped / 1 xfailed (untouched — this phase is TypeScript/Astro only)"
  artifacts:
    - path: "site/src/pages/news.astro"
      provides: "EN news page consuming newsFacets.ts for family/region/window/month data instead of inline monthGroups logic"
      contains: "computeNewsFacets"
    - path: "site/src/pages/es/noticias.astro"
      provides: "ES news page consuming the same newsFacets.ts module"
      contains: "computeNewsFacets"
  key_links:
    - from: "site/src/pages/news.astro"
      to: "site/src/lib/newsFacets.ts"
      via: "import { computeNewsFacets } from '../lib/newsFacets'"
      pattern: "computeNewsFacets\\("
    - from: "site/src/pages/es/noticias.astro"
      to: "site/src/lib/newsFacets.ts"
      via: "import { computeNewsFacets } from '../../lib/newsFacets'"
      pattern: "computeNewsFacets\\("
---

## Planning Decisions

(Carried from 27-01-PLAN.md — same decisions govern this plan's wiring; not re-litigated here.)

- Scope of wiring in this plan is data-layer only: replace each page's inline `monthGroups`/`cutToCommune`(for region)/family-enumeration block with a call into `computeNewsFacets()`. Card rendering markup, `safeUrl()`, and JSON-LD stay untouched — any filter UI, chips, count badges, or URL-state syncing is Phase 28 scope per the scope fence and must NOT be added here, even if it would be a small addition.
- `existsSync`/try-catch ownership of `current.json` parsing stays on each page exactly as today (RESEARCH Pitfall 4) — `computeNewsFacets` never does its own `current.json` I/O; it receives the already-parsed (possibly empty) `incidents` array. `newsFacets.ts` DOES own the archive-directory read (new I/O introduced in 27-01), called from inside `computeNewsFacets` with the `archiveDir` parameter set to the page's local `public/data/incidents/archive` path.
- Validator baseline for this plan's final gate: 16/16 (facets.mjs is now registered per 27-01). `@astrojs/check` baseline is 4 pre-existing errors, all in `ComparatorPairsLinks.astro` — unrelated to this phase; acceptance bar is zero NEW errors, not zero errors overall.

## Objective

Wire both `/news/` and `/es/noticias/` to import and consume the single `computeNewsFacets()` module from Plan 01, eliminating the current copy-paste drift between the two near-identical pages, then run the full build+validate+check+pytest gate to close the phase.

Purpose: Close FACET-01's "no per-locale drift" requirement with a real consumer, and prove FACET-06/07's zero-new-artifact / all-green constraints hold end to end.
Output: `news.astro` and `es/noticias.astro` both refactored to call `computeNewsFacets()`; phase-closing green build/validate/check/pytest run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-news-facet-data-model/27-RESEARCH.md
@.planning/phases/27-news-facet-data-model/27-PATTERNS.md
@.planning/phases/27-news-facet-data-model/27-01-SUMMARY.md

<interfaces>
<!-- From Plan 01's deliverable — use directly, no exploration needed. -->

From site/src/lib/newsFacets.ts (created in 27-01):
```typescript
export interface IncidentLike { cut: string | number; date: string; family?: string; [k: string]: unknown }
export interface FacetBucket { key: string; count: number }
export interface WindowFacet { today: IncidentLike[]; sevenDay: IncidentLike[]; thirtyDay: IncidentLike[] }
export interface MonthBucket { yearMonth: string; count: number }
export interface NewsFacetIndex { byFamily: FacetBucket[]; byRegion: Array<{ regionId: string; count: number }>; byWindow: WindowFacet; byMonth: MonthBucket[] }
export function computeNewsFacets(incidents: IncidentLike[], index: CommuneMeta[], archiveDir?: string): NewsFacetIndex;
```

From site/src/pages/news.astro (existing, lines ~1-104 — the block being replaced):
```typescript
import { existsSync, readFileSync } from 'node:fs';
import path from 'node:path';
const dataPath = path.resolve(process.cwd(), 'public/data/incidents/current.json');
let incidents = [];
let generated = null;
if (existsSync(dataPath)) {
  try {
    const raw = JSON.parse(readFileSync(dataPath, 'utf-8'));
    generated = raw.generated ?? null;
    incidents = raw.incidents ?? [];
  } catch { /* malformed — empty fallback */ }
}
// ...monthGroups reduce-into-Map (lines ~98-104), cutToCommune Map-from-loadIndex (lines ~47-63)
```
This existsSync/try-catch block is UNCHANGED by this plan. Only the code AFTER it (monthGroups + any family-key enumeration) is replaced by:
```typescript
import { computeNewsFacets } from '../lib/newsFacets';
import { loadIndex } from '../lib/data';
const archiveDir = path.resolve(process.cwd(), 'public/data/incidents/archive');
const facets = computeNewsFacets(incidents, loadIndex(), archiveDir);
```
(es/noticias.astro uses relative path `'../../lib/newsFacets'` / `'../../lib/data'` per its one-level-deeper file location.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire news.astro and es/noticias.astro to computeNewsFacets()</name>
  <files>site/src/pages/news.astro, site/src/pages/es/noticias.astro</files>
  <read_first>
    site/src/pages/news.astro (full file, 279 lines — identify exact line range of monthGroups/cutToCommune/family-enumeration block to replace; card-rendering markup at lines ~152-187 stays untouched)
    site/src/pages/es/noticias.astro (full file — confirm near-byte-identical structure per PATTERNS.md, apply the identical replacement)
    site/src/lib/newsFacets.ts (Plan 01's deliverable — exact exports and computeNewsFacets signature)
    site/src/lib/data.ts (loadIndex() import path, CommuneMeta shape)
    .planning/phases/27-news-facet-data-model/27-RESEARCH.md (Pitfall 4 — existsSync/try-catch for current.json must stay page-owned, unchanged)
  </read_first>
  <action>
    In `news.astro`: keep the existing `existsSync(dataPath)`/try-catch block that populates `incidents`/`generated` completely unchanged. Immediately after it, add `import { computeNewsFacets } from '../lib/newsFacets';` (alongside existing imports at the top of frontmatter) and replace the inline `monthGroups` Map-building loop plus any family-key enumeration with a single call: `const archiveDir = path.resolve(process.cwd(), 'public/data/incidents/archive'); const facets = computeNewsFacets(incidents, loadIndex(), archiveDir);`. Update any markup/rendering code that previously read from `monthGroups` to read the equivalent data from `facets.byMonth` (for the archive-month listing, if rendered) — if `monthGroups` was only used to group the CURRENT incidents by month for card display purposes (not archive browsing), preserve that specific per-page grouping behavior by deriving it from `incidents` directly (unchanged), and use `facets` purely for the NEW family/region/window/month facet data this phase adds — do not remove or alter any existing rendered card content, headings, or grouping the page currently displays. Keep `cutToCommune`/`getCommuneInfo`/`safeUrl` exactly as-is (still needed for card rendering, per PATTERNS.md — not part of this extraction).

    Apply the identical change to `es/noticias.astro`, adjusting relative import paths to `'../../lib/newsFacets'` and `'../../lib/data'` (one directory level deeper than `news.astro`). Verify both pages produce structurally identical frontmatter changes (same shape of `facets` variable, same archiveDir construction) — this identical-shape requirement is FACET-01's actual acceptance test.
  </action>
  <acceptance_criteria>
    `grep -c "computeNewsFacets" site/src/pages/news.astro` returns >= 1 and `grep -c "computeNewsFacets" site/src/pages/es/noticias.astro` returns >= 1. `grep -n "existsSync(dataPath)" site/src/pages/news.astro` still matches (the graceful-degradation guard is untouched). `cd site && npx astro build` completes with exit 0 and produces `dist/news/index.html` and `dist/es/noticias/index.html` with non-empty body content (existing incident cards still render — diff the rendered incident count against the pre-change build to confirm no regression). No new file appears under `site/public/**` or `site/dist/**` beyond the normal build output (no facet JSON artifact written — FACET-06).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe\site" && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>Both news.astro and es/noticias.astro import and call computeNewsFacets() identically; existing card rendering and graceful-degradation behavior is unchanged; astro build succeeds and produces both localized news pages with content intact.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-closing gate — chained build+validate+check+pytest, update STATE.md/ROADMAP.md baselines</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/STATE.md (Current Position, Progress Bar, Performance Metrics, Traceability-adjacent sections needing "27 complete" updates)
    .planning/ROADMAP.md (Phase 27 entry — Plans: TBD line, Progress Table row for Phase 27)
    .planning/v2.1-AUTONOMOUS-DIRECTIVE.md (RUN STATUS block — update NEXT ACTION to Phase 28 per the standing protocol)
  </read_first>
  <action>
    Run the full gate in ONE chained command (OneDrive dist/ desync gotcha — never split into separate Bash calls): `cd site && npm run build && npm run validate`. This must show 16/16 validators passing (facets.mjs now included per 27-01). Separately run `cd site && npx astro check` and confirm the error count is exactly 4, all attributable to `ComparatorPairsLinks.astro` (zero new errors introduced by this phase — the acceptance bar per the locked decisions, not the stale "8" figure). Separately re-run the pytest suite (`cd pipeline && python -m pytest`) to reconfirm the untouched baseline of 344 passed / 1 skipped / 1 xfailed (this phase touches zero Python — this run is a confirmation, not an expectation of change). Then update `.planning/STATE.md`: mark Phase 27 complete in the Progress Bar and Current Position, add a `[Phase 27, 2026-07-30]` bullet to Key Decisions recording the 6 Planning Decisions locked in 27-01-PLAN.md (today-anchor semantics, current.json-only facet scope, sort orders, module shape, data-driven family set, facets.mjs as validator #16), and update the "15/15 validators" figures project-wide within STATE.md to "16/16" (validators-passing line in Performance Metrics). Update `.planning/ROADMAP.md`'s Phase 27 entry: replace `**Plans**: TBD` with `**Plans**: 2 plans` and add the plan checklist (`27-01-PLAN.md`, `27-02-PLAN.md`) with brief objectives; update the Progress Table row for Phase 27 to `2/2 | Complete | 2026-07-30`.
  </action>
  <acceptance_criteria>
    Console output of the chained `npm run build && npm run validate` command shows `16/16` (or equivalent all-pass summary line) with `facets.mjs` listed among the passing validators — paste the final summary line. `npx astro check` output shows exactly 4 errors, all in `ComparatorPairsLinks.astro` — paste the error count line. Pytest output shows `344 passed, 1 skipped` and 1 xfailed reference — paste the final summary line (e.g. `344 passed, 1 skipped, 1 xfailed in Ns`). `.planning/STATE.md` and `.planning/ROADMAP.md` are updated per the action above (grep `grep -c "Phase 27" .planning/STATE.md` shows an updated reference to completion).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe\site" && npm run build && npm run validate</automated>
  </verify>
  <done>16/16 validators green, astro check shows the 4-error pre-existing baseline with zero new errors, pytest reconfirms 344/1/1(xfail) untouched, and STATE.md/ROADMAP.md reflect Phase 27 as complete with the locked planning decisions recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `newsFacets.ts` output -> Astro page frontmatter -> rendered static HTML | Facet data (region/family/date derived from already-classified RSS content) flows into server-rendered HTML with no client-side re-interpretation at this phase (Phase 27 ships zero UI/JS for facets) |
| Build process -> `dist/` output | Build-time only; no runtime request path exists for this data at this phase — output is fully static |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Tampering | Astro page frontmatter reading `incidents` (RSS/LLM-derived, indirectly-untrusted content) | accept | Existing `safeUrl()`/HTML-escaping in the unchanged card-rendering markup already handles this; this plan adds zero new rendering of untrusted text (facet keys are internal identifiers like `robos`/region_id numbers, never free-text titles) |
| T-27-06 | Denial of Service | Build-time `computeNewsFacets` call inside page frontmatter | mitigate | Bounded by `current.json`'s existing 30-day retention window (already capped upstream by the pipeline) and per-file archive reads guarded in 27-01 — no unbounded loop introduced by this plan's wiring |
| T-27-07 | Tampering | STATE.md/ROADMAP.md edits in Task 2 | mitigate | Edits are additive/status-update only (Edit tool, not full rewrite) — no destructive removal of prior phase history; verified by reading the file before editing per tool constraints |
</threat_model>

<verification>
Wave-level check: the chained `npm run build && npm run validate` in Task 2 is the integration proof that Plan 01's module and Plan 02's wiring compose correctly — 16/16 validators (including facets.mjs asserting against the real built data) is the single strongest signal that FACET-01 through FACET-07 all hold simultaneously.
</verification>

<success_criteria>
Both news pages consume the identical `computeNewsFacets()` call with no structural drift between locales. Full chained build+validate is 16/16 green. `@astrojs/check` shows zero new errors versus the 4-error baseline. Pytest suite is reconfirmed untouched at 344/1/1(xfail). STATE.md and ROADMAP.md reflect Phase 27 complete.
</success_criteria>

<output>
Create `.planning/phases/27-news-facet-data-model/27-02-SUMMARY.md` when done
</output>
</content>
