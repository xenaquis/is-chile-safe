---
phase: 21-commune-comparator-a-vs-b-seo
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/generate-pairs.mjs
  - data/comparator-pairs.json
  - site/src/lib/comparisonProse.ts
  - site/scripts/validate/avs-b-budget.mjs
  - site/src/config/i18n.ts
  - site/package.json
autonomous: true
requirements: [CMP-03, CMP-04, CMP-05]
must_haves:
  truths:
    - "A deterministic allowlist of all same-region commune pairs (5,031) exists as data/comparator-pairs.json with sorted CUT slugs and an enabled flag"
    - "buildComparisonProse(commA, commB, locale) returns ≥300 non-swappable words of directional EN or ES prose for any same-region pair"
    - "The avs-b-budget validator asserts dist/ total file count < 18,000 and EN/ES compare-page symmetry, exiting 0 on pass and 1 on failure"
    - "All 15 Phase-21 i18n keys exist in both EN_STRINGS and ES_STRINGS"
  artifacts:
    - path: "scripts/generate-pairs.mjs"
      provides: "Same-region pair allowlist generator from data/cead/meta/index.json"
      contains: "comparator-pairs.json"
    - path: "data/comparator-pairs.json"
      provides: "5,031 sorted-CUT pair objects with enabled flags"
      min_lines: 1
    - path: "site/src/lib/comparisonProse.ts"
      provides: "buildComparisonProse() 5-block directional prose engine"
      exports: ["buildComparisonProse", "buildMetaDescription"]
    - path: "site/scripts/validate/avs-b-budget.mjs"
      provides: "Build-time file-budget + pair-symmetry assertion"
      contains: "18000"
    - path: "site/src/config/i18n.ts"
      provides: "15 Phase-21 comparator i18n keys (EN+ES)"
      contains: "cmp_page_h1"
  key_links:
    - from: "scripts/generate-pairs.mjs"
      to: "data/cead/meta/index.json"
      via: "readFileSync at repo-root"
      pattern: "index\\.json"
    - from: "site/src/lib/comparisonProse.ts"
      to: "site/src/lib/data.ts"
      via: "import CommuneData type + loadNationalAverage/loadRegionalAverage"
      pattern: "from './data"
---

<objective>
Build the Wave 0 foundation that makes the A-vs-B page set provably within budget BEFORE any getStaticPaths is written: (1) generate the same-region priority-pair allowlist from REAL commune data, (2) implement the bilingual non-swappable prose engine with a ≥300-word floor, (3) ship the file-budget validator that asserts < 18,000 dist files, and (4) add the 15 comparator i18n keys consumed by every downstream surface.

Purpose: The phase's #1 risk is a page-count explosion (59,685 cartesian pairs × 2 locales blows the Cloudflare 20k limit). This plan computes and ASSERTS the real number (5,031 same-region pairs → 10,062 pages → ~11,944 total files) rather than assuming it. The prose engine is the phase's #2 risk (thin content → SpamBrain deindexing); it must be ground truth before pages are generated against it.

Output: data/comparator-pairs.json, scripts/generate-pairs.mjs, site/src/lib/comparisonProse.ts, site/scripts/validate/avs-b-budget.mjs, i18n keys, and the build wiring that runs pair generation before sync-data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-commune-comparator-a-vs-b-seo/21-RESEARCH.md
@.planning/phases/21-commune-comparator-a-vs-b-seo/21-PATTERNS.md
@.planning/phases/21-commune-comparator-a-vs-b-seo/21-UI-SPEC.md

<interfaces>
<!-- Source-of-truth contracts extracted from the codebase. Use these directly; no exploration needed. -->

From site/src/lib/data.ts (the prose engine consumes these):
- DATA_ROOT = path.resolve(process.cwd(), '..', 'data', 'cead')   // process.cwd() is site/ during build
- export function loadCommune(cut: string): CommuneData            // enriches latestCompleteYear, regionName, cut
- export function loadNationalAverage(): number                    // UNWEIGHTED mean of non-low-pop commune rates
- export function loadRegionalAverage(regionId: string): number    // UNWEIGHTED mean within region
- export function latestCompleteYearRate(commune: CommuneData): number
- export interface CommuneData { cut; name; slug; region_id; national_rank; regional_rank; trend: 'up'|'down'|'stable';
    featured_rates: { propiedad; homicidios; homicidios_count; secuestros }; // each Record<string, number>
    series: SeriesEntry[]; composite_index?: Record<string, CompositeIndexEntry>;
    latestCompleteYear: number; regionName: string }
- interface SeriesEntry { year; rate_per_100k; by_family: Record<string,number>; partial?: boolean }
- interface CompositeIndexEntry { score: number; rank: number; regional_rank: number; level: 1|2|3|4|5; available_metrics: number }

From site/src/lib/proseEngine.ts (EXACT template — mirror its structure):
- type Locale = 'en' | 'es'
- function rankTier(nationalRank, total=346): RankTier   // rank 1 = HIGHEST incidence
- function comparisonBand(signedPct): ComparisonBand
- function dominantFamily(byFamily): [string, string]
- function familyName(key, locale): string               // vida/robos_violentos/propiedad/vif/drogas/armas/incivilidades
- function fmt(n): string                                 // toLocaleString maxFractionDigits 1
- function interpolate(template, vars): string
- export function buildCommuneProse(commune: CommuneData, locale: Locale): string   // targets 500+ words

From data/cead/meta/index.json: array of { cut; name; slug; region_id; population; low_population }

From data/cead/comparator_table.json: array of 346 { id; name; slug; region_id; composite_index: { "2024": { score; rank; regional_rank; level; available_metrics } } }

From site/scripts/sync-data.mjs: copies repo-root data/cead/ -> site/public/data/cead/ on prebuild. NOTE: data/comparator-pairs.json lives OUTSIDE data/cead/, so it is read build-time by getStaticPaths via fs (repo-root), NOT served to the client. The island uses the 346-index prop instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pair-allowlist generator + committed allowlist + build wiring</name>
  <files>scripts/generate-pairs.mjs, data/comparator-pairs.json, site/package.json</files>
  <read_first>
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-PATTERNS.md (generate-pairs.mjs section + sync-data analog)
    - site/scripts/sync-data.mjs (path-anchoring + defensive exit pattern)
    - data/cead/meta/index.json (real region_id distribution — source of truth for pair counts)
    - site/package.json (prebuild chain)
  </read_first>
  <action>
    Create scripts/generate-pairs.mjs at REPO ROOT (sibling of data/, mirroring sync-data.mjs path-anchoring via fileURLToPath(import.meta.url) — NOT process.cwd, since this runs standalone). Read data/cead/meta/index.json; group entries by region_id; for each region emit every unordered pair C(n,2). For each pair, sort the two CUT codes lexicographically and store { cutA, cutB, region_id, enabled } where cutA < cutB (the sort guarantees one canonical page per pair — no A-vs-B/B-vs-A duplication per CMP-03). Set enabled:true for the first 20 pairs (staged Wave-1 batch per CMP-07) and enabled:false for the rest. Write data/comparator-pairs.json. Assert the total pair count === 5031 (real same-region universe from 21-RESEARCH.md) and exit 1 with a clear message if it diverges (guards against silently shipping the wrong allowlist). Also compute and print projected total dist files (1,882 base + enabledPairs×2) so the operator sees the budget headroom. Defensive exit(1) if index.json is missing (mirror sync-data FATAL pattern). Add an npm script "generate-pairs": "node ../scripts/generate-pairs.mjs" to site/package.json and chain it into prebuild BEFORE sync-data: "prebuild": "node ../scripts/generate-pairs.mjs && node scripts/sync-data.mjs" so the allowlist always exists before getStaticPaths runs. Do NOT place comparator-pairs.json under data/cead/ (it must not be synced to public — it is build-time-only).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && node scripts/generate-pairs.mjs && node -e "const p=require('./data/comparator-pairs.json'); if(p.length!==5031)process.exit(1); const enabled=p.filter(x=>x.enabled).length; if(enabled!==20)process.exit(1); const bad=p.find(x=>!(x.cutA<x.cutB)); if(bad)process.exit(1); console.log('OK', p.length, 'pairs,', enabled, 'enabled')"</automated>
  </verify>
  <acceptance_criteria>
    - data/comparator-pairs.json contains exactly 5,031 objects (source assertion vs 21-RESEARCH.md verified universe)
    - Every object has cutA < cutB lexicographically (no duplicate inverse pairs) — assertion in verify command
    - Exactly 20 objects have enabled:true (first staged batch); rest false
    - `node scripts/generate-pairs.mjs` exits 0 and prints projected dist file total
    - site/package.json prebuild runs generate-pairs before sync-data
  </acceptance_criteria>
  <done>Allowlist generated and committed; 5,031 sorted pairs, 20 enabled; build wiring chains generation before sync-data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildComparisonProse() — 5-block directional non-swappable engine</name>
  <files>site/src/lib/comparisonProse.ts</files>
  <read_first>
    - site/src/lib/proseEngine.ts (EXACT template: rankTier, comparisonBand, dominantFamily, familyName, fmt, interpolate, buildCommuneProse structure, EN/ES sentence-bank pattern, D-05 forbidden-language comment)
    - site/src/lib/data.ts (CommuneData, loadNationalAverage, loadRegionalAverage, latestCompleteYearRate)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-RESEARCH.md (Thin-Content Strategy table — the 5 non-swappable blocks)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-UI-SPEC.md (A-vs-B page section order + copywriting invariant)
  </read_first>
  <behavior>
    - buildComparisonProse(commA, commB, 'en') returns ≥300 words (target 400-500) for any same-region pair
    - buildComparisonProse(commA, commB, 'es') returns ≥300 words for the same pair
    - Output contains 5 directional blocks: (1) composite-index score delta with direction (A's score vs B's, points higher/lower, both 2024 ranks); (2) dominant per-family divergence with gap factor and direction; (3) per-commune trend direction narrative; (4) each commune's national rank #X of 346; (5) regional context (same-region → regional-rank comparison); closing CEAD attribution
    - Swapping commA and commB produces DIFFERENT text (directional, not symmetric) — same word count but reordered direction words
    - Output never contains unqualified "safe", "dangerous", "seguro", "peligroso" (forbidden-language invariant); uses "higher/lower reported incidence", "ranked X nationally"
    - buildMetaDescription(commA, commB, locale) returns a 140-155 char string embedding both names + both composite scores + "Source: CEAD"
  </behavior>
  <action>
    Create site/src/lib/comparisonProse.ts mirroring proseEngine.ts. Import CommuneData, loadNationalAverage, loadRegionalAverage, latestCompleteYearRate from './data.ts' and the familyName/fmt/interpolate helpers (copy or adapt verbatim — keep them local to this module). Export buildComparisonProse(commA: CommuneData, commB: CommuneData, locale: 'en'|'es'): string and buildMetaDescription(commA, commB, locale): string. Read composite_index["2024"].score/rank/regional_rank/level from each commune; read by_family at each commune's latestCompleteYear (filter !partial — Pitfall 3); read trend; read national_rank/regional_rank. Build exactly 5 paragraphs per the 21-RESEARCH.md Thin-Content table, each containing at least one DIRECTIONAL datum (signed score delta, gap factor, rank position) so swapping A/B changes meaning. Maintain parallel EN/ES sentence banks like proseEngine (no translation API). Append the CEAD/SPD attribution closing. Include the same D-05 forbidden-language header comment. Do NOT include the word-count throw here — that lives in the [pair].astro getStaticPaths (Plan 03); this module only generates prose. Aim for 400-500 words so the ≥300 floor has margin.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node --input-type=module -e "import('./src/lib/comparisonProse.ts').catch(()=>{}); import('tsx').then(()=>{}).catch(()=>{})" 2>/dev/null; npx tsx -e "import {buildComparisonProse, buildMetaDescription} from './src/lib/comparisonProse.ts'; import {loadCommune} from './src/lib/data.ts'; const a=loadCommune('13110'), b=loadCommune('13119'); const en=buildComparisonProse(a,b,'en'); const es=buildComparisonProse(a,b,'es'); const wc=s=>s.split(/\s+/).filter(Boolean).length; const swap=buildComparisonProse(b,a,'en'); const f=/\b(safe|dangerous|seguro|peligroso)\b/i; if(wc(en)<300||wc(es)<300) process.exit(1); if(en===swap) process.exit(1); if(f.test(en)||f.test(es)) process.exit(1); const md=buildMetaDescription(a,b,'en'); if(md.length<140||md.length>160) process.exit(1); console.log('OK en',wc(en),'es',wc(es),'meta',md.length)"</automated>
  </verify>
  <acceptance_criteria>
    - EN and ES prose each ≥300 words for pair 13110/13119 (behavior assertion in verify)
    - Swapping A/B yields different text (non-swappability assertion)
    - No unqualified forbidden term in EN or ES output (regex assertion)
    - buildMetaDescription returns 140-160 chars
    - Module exports buildComparisonProse and buildMetaDescription
  </acceptance_criteria>
  <done>Prose engine generates ≥300 directional non-swappable bilingual words per pair; meta description sized correctly; forbidden-language clean.</done>
</task>

<task type="auto">
  <name>Task 3: avs-b-budget validator + 15 i18n keys</name>
  <files>site/scripts/validate/avs-b-budget.mjs, site/src/config/i18n.ts</files>
  <read_first>
    - site/scripts/validate/structure.mjs (boilerplate: __dirname, SITE_ROOT, DIST_DIR, graceful dist-missing exit, process.exit pattern)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-PATTERNS.md (avs-b-budget.mjs section — countFiles, SAFE_BOUND, symmetry assertion)
    - site/src/config/i18n.ts (I18nStrings interface + EN_STRINGS/ES_STRINGS structure — append after existing keys)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-UI-SPEC.md (i18n Keys to Add table — all 15 keys with EN+ES values)
  </read_first>
  <action>
    Create site/scripts/validate/avs-b-budget.mjs mirroring structure.mjs boilerplate (__dirname via fileURLToPath, SITE_ROOT = ../.., DIST_DIR = dist). Graceful exit(0) with a message if dist/ is absent. Recursively count ALL files under dist/ (countFiles helper from 21-PATTERNS.md); set SAFE_BOUND=18000; fail with exit(1) and "[CMP-05] File budget exceeded" if totalFiles >= SAFE_BOUND, else print PASS with margin. Add a pair-symmetry assertion: count index.html dirs under dist/compare/ (excluding the landing /compare/index.html itself) vs dist/es/comparar/ (excluding landing) and fail if they differ (guards CMP-03 reciprocity). Exit 1 if any failure, else exit 0. Then extend site/src/config/i18n.ts: add the 15 keys (nav_compare, cmp_page_h1, cmp_page_lead, cmp_input_placeholder, cmp_empty_heading, cmp_add_prompt, cmp_no_match, cmp_max_reached, cmp_avg_column, cmp_remove_aria, cmp_data_error, cmp_homicide_row, cmp_homicide_no_data, cmp_cross_links_heading, avs_b_h1_pattern) to the I18nStrings interface AND to both EN_STRINGS and ES_STRINGS using the EXACT EN/ES values from the 21-UI-SPEC.md i18n table. Keep {name}/{year}/{a}/{b} placeholders intact for runtime interpolation.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node scripts/validate/avs-b-budget.mjs && npx tsx -e "import {EN_STRINGS, ES_STRINGS} from './src/config/i18n.ts'; const keys=['nav_compare','cmp_page_h1','cmp_page_lead','cmp_input_placeholder','cmp_empty_heading','cmp_add_prompt','cmp_no_match','cmp_max_reached','cmp_avg_column','cmp_remove_aria','cmp_data_error','cmp_homicide_row','cmp_homicide_no_data','cmp_cross_links_heading','avs_b_h1_pattern']; for(const k of keys){ if(!EN_STRINGS[k]||!ES_STRINGS[k]){console.error('missing',k);process.exit(1)} } console.log('OK 15 i18n keys EN+ES')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate/avs-b-budget.mjs` exits 0 (dist absent → graceful 0; dist present → budget pass)
    - Validator contains the literal 18000 bound and a [CMP-05] failure message
    - All 15 keys present in both EN_STRINGS and ES_STRINGS (assertion in verify)
    - i18n placeholder tokens {name}/{year}/{a}/{b} preserved verbatim
  </acceptance_criteria>
  <done>Budget validator asserts <18,000 files + EN/ES compare symmetry; 15 bilingual comparator i18n keys added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo JSON → build scripts | generate-pairs.mjs and comparisonProse.ts read ONLY committed repo JSON (index.json, comunas/*.json). No network, no secrets, no user input at build time. |
| build artifact → static HTML | Prose strings are interpolated into Astro/React templates downstream (Plans 02/03) — auto-escaped there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | comparisonProse output rendered to HTML | mitigate | Prose is plain text interpolated via Astro `{expr}` / React children (auto-escaped) downstream; never via set:html. Commune names are from the closed CEAD catalog, not user input. |
| T-21-02 | Repudiation | Generated A-vs-B prose | mitigate | Forbidden-language regex test in Task 2 verify + global forbidden-language.mjs validator (Plan 04) ensures no absolute safety verdict. |
| T-21-03 | DoS (build) | Pair allowlist size | mitigate | Hard-asserted 5,031-pair cap + avs-b-budget.mjs <18,000 file gate prevent page-count explosion. |
| T-21-SC | Tampering | npm/package installs | mitigate | This plan adds ZERO new dependencies (uses tsx for verify only, already present via Astro toolchain). No install task. |
</threat_model>

<verification>
- `node scripts/generate-pairs.mjs` regenerates an identical 5,031-pair allowlist (deterministic).
- `npx tsx` smoke tests for prose engine pass (word count, non-swappability, forbidden-language, meta length).
- `node site/scripts/validate/avs-b-budget.mjs` exits 0.
- All 15 i18n keys resolve in both locales.
</verification>

<success_criteria>
- data/comparator-pairs.json: 5,031 sorted-CUT pairs, 20 enabled.
- buildComparisonProse yields ≥300 non-swappable directional words EN+ES, forbidden-language clean.
- avs-b-budget.mjs asserts <18,000 files and EN/ES compare symmetry.
- 15 comparator i18n keys present in EN_STRINGS and ES_STRINGS.
- Build prebuild chain runs pair generation before sync-data.
</success_criteria>

<output>
Create `.planning/phases/21-commune-comparator-a-vs-b-seo/21-01-SUMMARY.md` when done
</output>
