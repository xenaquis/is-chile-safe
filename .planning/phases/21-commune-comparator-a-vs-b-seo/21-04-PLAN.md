---
phase: 21-commune-comparator-a-vs-b-seo
plan: 04
type: execute
wave: 2
depends_on: ["21-02", "21-03"]
files_modified:
  - site/scripts/validate/all.mjs
autonomous: false
requirements: [CMP-04, CMP-05, CMP-06, CMP-07]
must_haves:
  truths:
    - "avs-b-budget.mjs is registered in the aggregate validator suite (all.mjs) and runs in npm run validate"
    - "The full build + validator suite (14/14) exits 0 with the comparator + A-vs-B pages present"
    - "A random sample of ≥10 enabled A-vs-B pairs is acceptance-checked for prose quality (EN+ES) before deploy"
    - "Total dist file count is confirmed < 18,000 (CMP-05)"
    - "The first ~20-pair batch is published and GSC URL Inspection is verified before any further batch is enabled"
  artifacts:
    - path: "site/scripts/validate/all.mjs"
      provides: "avs-b-budget registered in the validator list"
      contains: "avs-b-budget.mjs"
  key_links:
    - from: "site/scripts/validate/all.mjs"
      to: "site/scripts/validate/avs-b-budget.mjs"
      via: "VALIDATORS array entry"
      pattern: "avs-b-budget"
---

<objective>
Close the phase: register the new budget validator into the aggregate suite, prove the full build + all validators (now 14) exit 0 with the comparator and A-vs-B pages present and under the 18,000-file bound, acceptance-check ≥10 enabled A-vs-B pairs for prose quality in both locales, and gate the staged rollout (publish the first ~20-pair batch, verify GSC URL Inspection before expanding).

Purpose: CMP-04/CMP-05 final assertion, CMP-06 full-suite green, CMP-07 rollout discipline. This is the goal-backward verification that the whole phase holds together.

Output: all.mjs registration + a green full suite + human acceptance + GSC checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-commune-comparator-a-vs-b-seo/21-RESEARCH.md
@.planning/phases/21-commune-comparator-a-vs-b-seo/21-VALIDATION.md

<interfaces>
From site/scripts/validate/all.mjs: a VALIDATORS array (13 entries) iterated with spawnSync, ROLLOUT_ALL=true propagated, summary printed, exit 1 on any failure. Append 'avs-b-budget.mjs' (it requires a prior build; gracefully exits 0 if dist absent).
Full-suite command (memory onedrive-build-artifacts-desync — chain in ONE command): `cd site && npm run build && npm run validate`.
Preview for human review (memory serve-build-for-e2e-review — no preview npm script): `npx astro preview --port 4321 --host`.
Enabled pairs this batch: 20 (set in data/comparator-pairs.json by Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register avs-b-budget in the aggregate validator + full-suite green</name>
  <files>site/scripts/validate/all.mjs</files>
  <read_first>
    - site/scripts/validate/all.mjs (VALIDATORS array + header doc comment + summary block)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-VALIDATION.md (full suite command + sampling rate)
  </read_first>
  <action>
    Append 'avs-b-budget.mjs' to the VALIDATORS array in site/scripts/validate/all.mjs (after figure-registry.mjs) and add it to the header doc comment as validator #14 with a one-line description (file budget < 18,000 + EN/ES compare-page symmetry). Then run the full chained build+validate command in ONE invocation (OneDrive desync guard). All 14 validators must exit 0. If any fails, fix the root cause in the offending plan's surface (do not weaken the assertion).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && npm run validate 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - all.mjs VALIDATORS array contains 'avs-b-budget.mjs' (grep assertion)
    - `npm run build && npm run validate` prints "14/14 validators passed" and exits 0
    - dist total file count < 18,000 (reported by avs-b-budget in the suite output)
  </acceptance_criteria>
  <done>Budget validator registered; full 14-validator suite green on the comparator + A-vs-B build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The first ~20-pair batch of bilingual A-vs-B pages and the comparator, all under the 18,000-file budget with 14/14 validators green. This checkpoint is the CMP-07 prose-quality acceptance sample (≥10 pairs, EN+ES) before deploy.</what-built>
  <how-to-verify>
    1. `cd site && npm run build` then `npx astro preview --port 4321 --host`.
    2. List the 20 enabled pair slugs (from data/comparator-pairs.json where enabled:true). Pick ≥10 at random.
    3. For each sampled pair, open http://localhost:4321/compare/{cutA}-vs-{cutB}/ and the ES /es/comparar/{...}/ and confirm:
       - Prose reads naturally and is SUBSTANTIVELY DIFFERENT per pair (not a swapped template) — directional score deltas, rank positions, trend directions, dominant-family divergence.
       - All 5 uniqueness blocks present (index difference, per-family table incl. homicide sub-row, trend narrative, national rank, regional context).
       - Unique <title> + meta description per page; CEAD attribution present.
       - No absolute "safe/dangerous/seguro/peligroso" verdict.
       - EN and ES are full parity (no English leaking into ES or vice versa).
    4. Confirm the 2 commune cross-links on each A-vs-B page resolve to the right /commune/ pages.
  </how-to-verify>
  <resume-signal>Type "approved" (≥10 pairs read well, EN+ES) or list pairs/blocks needing prose fixes</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The staged-rollout gate (CMP-07): the first ~20-pair batch must be live and GSC-verified before any further batch is enabled. This is a Google Search Console action with no CLI/API in this repo's workflow.</what-built>
  <how-to-verify>
    1. After deploy of this batch (via the normal master push → Cloudflare deploy hook), open Google Search Console for ischilesafe.com.
    2. Run URL Inspection on at least 3 of the enabled A-vs-B pages (one EN, one ES, one cross-region if any) and at least 2 commune pages carrying the new compare cross-links.
    3. Confirm each is "URL is on Google" or "URL can be indexed" (no "Excluded — Duplicate" / "Discovered – not indexed" red flags that would indicate thin-content rejection).
    4. Only after this confirmation should a future cycle flip more pairs to enabled:true in data/comparator-pairs.json (out of scope this phase per Deferred Ideas).
  </how-to-verify>
  <resume-signal>Type "indexable confirmed" (with the inspected URLs) or describe GSC issues to address before batch expansion</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build artifact → Cloudflare deploy | Static dist/ pushed via existing deploy-on-code hook; no new secrets introduced this phase. |
| published pages → Google index | GSC URL Inspection is a read-only verification; no write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Repudiation (SEO deindex) | First A-vs-B batch | mitigate | Staged ~20-pair rollout + GSC URL-Inspection gate before expansion; ≥10-pair human prose acceptance; build-time thin-content + forbidden-language gates. |
| T-21-11 | DoS (build/Cloudflare) | Total file count | mitigate | avs-b-budget.mjs registered in all.mjs asserts < 18,000 on every validate run. |
| T-21-SC | Tampering | npm installs | mitigate | Zero new dependencies across the phase. No install task. |
</threat_model>

<verification>
- `npm run build && npm run validate` → 14/14 green, dist < 18,000 files.
- ≥10 enabled A-vs-B pairs human-acceptance-checked (EN+ES) for non-swappable prose quality.
- GSC URL Inspection confirms the first batch + sampled commune pages are indexable.
</verification>

<success_criteria>
- avs-b-budget registered; full suite 14/14 green (CMP-04, CMP-05, CMP-06).
- ≥10-pair prose acceptance sample passes before deploy (CMP-07).
- First ~20-pair batch GSC-verified before any expansion (CMP-07).
</success_criteria>

<output>
Create `.planning/phases/21-commune-comparator-a-vs-b-seo/21-04-SUMMARY.md` when done
</output>
