---
phase: 18-composite-crime-index
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/map/ResultPanel.tsx
autonomous: true
gap_closure: true
requirements: [CI-05]
must_haves:
  truths:
    - "Clicking any commune in the default map view (year=2025, mode=composite) renders the composite section: integer 0-100 score, 5-band label, national + regional rank, and the always-visible bilingual caveat"
  artifacts:
    - path: "site/src/components/map/ResultPanel.tsx"
      provides: "COMPOSITE_YEAR-keyed lookup of data.composite_index so the composite block no longer returns null at year=2025"
      contains: "COMPOSITE_YEAR"
  key_links:
    - from: "site/src/components/map/ResultPanel.tsx"
      to: "commune JSON composite_index['2024']"
      via: "data.composite_index?.[String(COMPOSITE_YEAR)]"
      pattern: "composite_index\\?\\.\\[String\\(COMPOSITE_YEAR\\)\\]"
---

<objective>
Close Gap 1 (CI-05): the ResultPanel composite section is invisible in the default map view because it reads composite data under the wrong year key.

Purpose: ResultPanel.tsx:262 reads `data.composite_index?.[String(year)]`, but `year` is initialised to 2025 (MapIsland.tsx:96) while commune JSON stores composite data only under key "2024". The composite block guard returns null, so clicking any commune in the default view shows no score, no band, no rank — the core UI delivery of CI-05 is invisible.

Output: ResultPanel composite block reads a fixed `COMPOSITE_YEAR = 2024` constant (mirroring commune/[slug].astro:221), so the score/band/rank/caveat render on commune click regardless of the active rate-series year.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-composite-crime-index/18-VERIFICATION.md
@.planning/phases/18-composite-crime-index/18-CONTEXT.md

<interfaces>
From site/src/pages/commune/[slug].astro:220-241 (the CORRECT pattern to mirror):
- `const COMPOSITE_YEAR = 2024;`
- `const ci = data.composite_index?.[String(COMPOSITE_YEAR)];`
- `if (!ci) return null;`
- ci shape: { score:number, level:1|2|3|4|5, rank:number, regional_rank:number }

From site/src/components/map/ResultPanel.tsx:260-296 (the BROKEN block to fix):
- Block guarded by `mode === 'composite'`
- Line 262: `const ci = data.composite_index?.[String(year)];` ← reads `year` (2025 at mount), must read COMPOSITE_YEAR (2024)
- Uses ci.level, ci.score, ci.rank, ci.regional_rank, BAND_EN/BAND_ES, EN_STRINGS/ES_STRINGS ci_* keys, TOTAL_COMMUNES — all already wired; ONLY the year key is wrong
- `year` remains correct for the rate-series stat card (lines 298-309) — do NOT change those usages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key the composite block on COMPOSITE_YEAR=2024 instead of the active series year</name>
  <read_first>
    - site/src/components/map/ResultPanel.tsx (the file being modified; the composite block at lines 260-296 and the rate stat card at 298-309)
    - site/src/pages/commune/[slug].astro lines 220-241 (the correct COMPOSITE_YEAR=2024 pattern to mirror)
    - .planning/phases/18-composite-crime-index/18-VERIFICATION.md Gap 1 (the decided fix)
  </read_first>
  <action>
    In ResultPanel.tsx, introduce a module-level (or component-scope, mirroring commune/[slug].astro) constant `const COMPOSITE_YEAR = 2024;`. Change the composite-block lookup at line 262 from `data.composite_index?.[String(year)]` to `data.composite_index?.[String(COMPOSITE_YEAR)]`. Do NOT touch the `year`-based rate stat card (lines 298-309) or any other `year` usage — `year` stays the active rate-series year everywhere else. Do NOT alter the `mode === 'composite'` guard, the band/rank/caveat rendering, or the choropleth coloring (which already works from the payload `ci` field, not from this panel). Leave all EN_STRINGS/ES_STRINGS, BAND_EN/BAND_ES, and TOTAL_COMMUNES usages unchanged.
  </action>
  <verify>
    <automated>cd site && npx astro check 2>&1 | Select-String -Pattern "ResultPanel" -Quiet; if (Select-String -Path src/components/map/ResultPanel.tsx -Pattern "String\(COMPOSITE_YEAR\)") { exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/components/map/ResultPanel.tsx` contains `COMPOSITE_YEAR = 2024` and the composite block reads `data.composite_index?.[String(COMPOSITE_YEAR)]` (grep-assertable, not `String(year)`).
    - No remaining `composite_index?.[String(year)]` occurrence in the file.
    - The rate stat card still interpolates the active `year` (lines ~303/304 unchanged).
    - `npx astro check` reports no new errors attributable to ResultPanel.tsx.
    - Behavior: clicking a commune in the default view (year=2025, mode=composite) renders the composite section with score + band label + national/regional rank + always-visible bilingual caveat (the block no longer returns null).
  </acceptance_criteria>
  <done>
    Composite block keyed on COMPOSITE_YEAR=2024; renders on commune click in default view; astro check green; choropleth untouched.
  </done>
</task>

</tasks>

<verification>
- `npx astro check` exits clean (no new ResultPanel errors).
- Grep: `String(COMPOSITE_YEAR)` present; `String(year)` absent from the composite block.
- Manual/observable: default-view commune click shows the composite section (score, band, ranks, caveat).
</verification>

<success_criteria>
CI-05 popup/ResultPanel requirement met in the default view: composite score, 5-band label, national + regional rank, and bilingual caveat render on commune click without changing the active year toggle.
</success_criteria>

<output>
Create `.planning/phases/18-composite-crime-index/18-06-SUMMARY.md` when done.
</output>
