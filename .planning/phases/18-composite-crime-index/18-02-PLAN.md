---
phase: 18-composite-crime-index
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - pipeline/build_map_payload.py
  - pipeline/scrape_cead.py
  - pipeline/tests/test_scrape_cead.py
  - pipeline/tests/test_workflow_order.py
  - .github/workflows/cead-scraper.yml
  - data/cead/map-payload.json
autonomous: false
requirements: [CI-01, CI-10]
user_setup: []

must_haves:
  truths:
    - "build_map_payload() lives in pipeline/build_map_payload.py and is removed entirely from scrape_cead.py (definition and call)"
    - "Each commune entry in map-payload.json carries a ci field (level 1-5) read from composite_index[2024].level, null/omitted when absent"
    - "map-payload.json stays under the 30 KB budget after adding ci"
    - "The GitHub Actions workflow runs three steps in order: scrape CEAD -> build composite index -> build map payload"
    - "If the composite-index step fails, the CEAD data commit still runs (D-11 graceful degrade): the index step is continue-on-error so a non-zero index never blocks the data commit"
    - "test_scrape_cead.py imports build_map_payload from pipeline.build_map_payload and still passes"
  artifacts:
    - path: "pipeline/build_map_payload.py"
      provides: "Extracted payload builder; adds ci field; runs 30 KB assertion"
      contains: "def build_map_payload"
    - path: ".github/workflows/cead-scraper.yml"
      provides: "Three-step pipeline order; index step continue-on-error so CEAD commit is never blocked (D-11)"
      contains: "build_composite_index.py"
  key_links:
    - from: "pipeline/build_map_payload.py"
      to: "data/cead/comunas/{CUT}.json"
      via: "reads composite_index[str(REFERENCE_YEAR)].level for ci field"
      pattern: "composite_index"
    - from: ".github/workflows/cead-scraper.yml"
      to: "pipeline/build_composite_index.py"
      via: "workflow step between scrape and payload, marked continue-on-error so its failure does not block the data commit (D-11)"
      pattern: "build_composite_index.py"
---

<objective>
Extract build_map_payload() from scrape_cead.py into its own isolated pipeline entrypoint, enrich each map-payload commune entry with the ci field (composite level 1-5), move the 30 KB size assertion, remove the old embedded definition + call, and wire the three-step GitHub Actions order (scrape -> index -> payload) with the index/payload steps marked continue-on-error so a failed index never blocks the CEAD data commit (D-11 graceful degrade).

Purpose: D-11 pipeline isolation — if the index step fails the CEAD scraper still refreshes data; the payload builder runs after the index enriches commune files so the map gets the ci field. This plan is the schema-migration seam (Pitfalls 4 and 5) and must be human-reviewed.
Output: pipeline/build_map_payload.py; trimmed scrape_cead.py; updated workflow; regenerated map-payload.json carrying ci.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-composite-crime-index/18-RESEARCH.md
@.planning/phases/18-composite-crime-index/18-PATTERNS.md
@.planning/phases/18-composite-crime-index/18-01-SUMMARY.md

<interfaces>
<!-- Verified contracts — do not re-explore. -->

Extraction source: pipeline/scrape_cead.py
  - build_map_payload() defined lines 102-204 (move VERBATIM)
  - called at line 326 inside _write_all_outputs()
  - 30 KB size assertion lines 328-331; payload writes lines 346-359
  - Imports the function needs (no circular deps):
      from pipeline.cead.normalizer import compute_level
      from pipeline.shared.schema import FAMILY_KEYS
      from pipeline.shared.atomic_write import atomic_write_json

ci field encoding (A3, LOCKED): ci = composite_index[str(REFERENCE_YEAR)].level (integer 1-5),
  NOT the raw 0-100 score. null/omit when commune has no composite for the year.

Pitfall 4: ci adds ~7 B x 346 ~= 2.4 KB; payload is near 30 KB -> omit ci when null to stay under budget.
Pitfall 5: REMOVE build_map_payload() definition + its call + the size assertion + payload writes from scrape_cead.py
  so the old embedded version cannot run.
Consumer #6: pipeline/tests/test_scrape_cead.py must change import to
  from pipeline.build_map_payload import build_map_payload and still pass.

REFERENCE_YEAR = 2024 (from pipeline.composite_config).

D-11 failure semantics (LOCKED RESOLUTION):
  Step order: "Run CEAD scraper" (writes commune files) -> "Build composite index" -> "Build map payload" -> "Commit data if changed".
  For "CEAD data still refreshes if the index fails" to actually hold, the index step (and the dependent payload step)
  MUST NOT block the data commit. Decision: mark BOTH the "Build composite index" and "Build map payload" steps
  continue-on-error: true. The scrape step already wrote commune/region/national files before they run, so when the
  index fails the workflow continues to "Commit data if changed" and the refreshed CEAD data is still committed.
  (Chosen continue-on-error on the index/payload steps over if: always() on the commit step: it keeps the commit
  step's existing condition untouched and localizes the graceful-degrade to exactly the two new fallible steps.)

Workflow analog: .github/workflows/cead-scraper.yml "Run CEAD scraper" step lines 33-34;
  insert two steps before "Commit data if changed"; keep git-add/commit-only-if-changed/deploy-hook gate unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract build_map_payload.py, add ci field, move 30 KB assertion, strip scrape_cead.py</name>
  <files>pipeline/build_map_payload.py, pipeline/scrape_cead.py, pipeline/tests/test_scrape_cead.py</files>
  <read_first>
    - pipeline/scrape_cead.py (build_map_payload() lines 102-204; _write_all_outputs() call line 326; size assertion lines 328-331; payload writes lines 346-359; module head 20-46)
    - pipeline/tests/test_scrape_cead.py (current import of build_map_payload + payload assertions)
    - .planning/phases/18-composite-crime-index/18-PATTERNS.md (build_map_payload.py section lines 133-153)
    - .planning/phases/18-composite-crime-index/18-RESEARCH.md (Pipeline Extraction lines 498-527; Pitfalls 4 and 5 lines 423-433)
    - data/cead/comunas/13101.json (composite_index[str(2024)].level shape written by plan 01)
  </read_first>
  <action>
    Create pipeline/build_map_payload.py: move build_map_payload() VERBATIM from scrape_cead.py:102-204 with its existing imports (compute_level, FAMILY_KEYS, atomic_write_json) plus the _get_data_dir env-override pattern. Two changes on extraction:
    (1) For each commune entry add ci: read composite_index[str(REFERENCE_YEAR)].level (1-5) from the enriched commune file; set ci to null OR omit the key entirely when the commune has no composite for the year (A3: encode LEVEL not raw score; omit-when-null to protect the 30 KB budget per Pitfall 4).
    (2) Move the 30 KB size assertion (len(serialized) >= 30720 -> raise ValueError) into this file after building the payload; keep separators=(",",":") compact serialization. Write map-payload.json + all per-year payloads via atomic_write_json(compact=True). Add __main__ guard.
    Then strip scrape_cead.py (Pitfall 5): REMOVE the build_map_payload() definition (102-204), its call inside _write_all_outputs() (326), the size assertion (328-331), and the map-payload writes (346-359). Leave commune/region/national/meta writes intact. The scraper must still run and write its own outputs without producing map-payload.
    Update pipeline/tests/test_scrape_cead.py: change the import to from pipeline.build_map_payload import build_map_payload; keep payload assertions passing (move any map-payload-specific test to target the new module).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && python -m pytest pipeline/tests/test_scrape_cead.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'def build_map_payload' pipeline/scrape_cead.py == 0 (definition removed)
    - grep -c 'build_map_payload' pipeline/scrape_cead.py == 0 (call removed)
    - python -c "from pipeline.build_map_payload import build_map_payload" exits 0
    - python pipeline/build_composite_index.py && python pipeline/build_map_payload.py exits 0
    - map-payload.json has a ci field on scored communes (level 1-5)
    - map-payload.json serialized bytes < 30720 (assertion in build_map_payload.py did not raise)
    - python -m pytest pipeline/tests/test_scrape_cead.py -x GREEN
  </acceptance_criteria>
  <done>build_map_payload.py is the sole owner of the payload build; scrape_cead.py no longer defines or calls it; ci field present (level 1-5, omitted when null); 30 KB assertion lives in the new file; test_scrape_cead.py green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire three-step GitHub Actions order (index continue-on-error, D-11) + ordering test</name>
  <files>.github/workflows/cead-scraper.yml, pipeline/tests/test_workflow_order.py</files>
  <read_first>
    - .github/workflows/cead-scraper.yml (full file; "Run CEAD scraper" step lines 33-34; "Commit data if changed" + deploy-hook gate lines 36-52)
    - .planning/phases/18-composite-crime-index/18-PATTERNS.md (cead-scraper.yml section lines 271-283)
  </read_first>
  <action>
    Insert two steps in .github/workflows/cead-scraper.yml between "Run CEAD scraper" and "Commit data if changed", in this exact order (D-11): a "Build composite index" step running python pipeline/build_composite_index.py, then a "Build map payload" step running python pipeline/build_map_payload.py.
    Mark BOTH new steps with continue-on-error: true (D-11 failure semantics). This is what makes "CEAD data still refreshes if the index fails" true: the scrape step has already written commune/region/national files, so when the index (or payload) step fails, continue-on-error lets the workflow proceed to "Commit data if changed" and commit the refreshed CEAD data instead of aborting the job. Leave the existing "Commit data if changed" condition + git add data/ + commit-only-if-changed + deploy-hook gate unchanged (do NOT add if: always() there — the continue-on-error on the two new steps already guarantees the commit step runs). Document this decision in the SUMMARY.
    Create pipeline/tests/test_workflow_order.py: parse the workflow YAML, collect step run commands, assert a step running build_composite_index.py exists, a step running build_map_payload.py exists, the composite-index step index is less than the map-payload step index, both precede the "Commit data if changed" step, AND both the composite-index and map-payload steps carry continue-on-error: true.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && python -m pytest pipeline/tests/test_workflow_order.py -x</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest pipeline/tests/test_workflow_order.py -x GREEN
    - .github/workflows/cead-scraper.yml contains a step running python pipeline/build_composite_index.py
    - .github/workflows/cead-scraper.yml contains a step running python pipeline/build_map_payload.py
    - The build_composite_index.py step appears BEFORE the build_map_payload.py step
    - Both new steps appear BEFORE the existing "Commit data if changed" step
    - Both new steps carry continue-on-error: true (so a failed index does not block the CEAD data commit — D-11)
  </acceptance_criteria>
  <done>Workflow runs scrape -> build_composite_index -> build_map_payload -> commit, in that order; the index/payload steps are continue-on-error so a failed index still lets the CEAD data commit (D-11); deploy-hook gate untouched; ordering test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enriched commune JSON -> map-payload.json | Pipeline-internal read; payload size budget protects the client bundle |
| pipeline output -> Cloudflare deploy (data commit) | Workflow gate must not ship a stale/oversized payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | map-payload.json size budget (ci inflation) | mitigate | 30 KB assertion moved into build_map_payload.py raises before write; ci omitted when null (Pitfall 4) keeps payload under budget |
| T-18-06 | Integrity | dual build_map_payload definitions (old embedded version runs) | mitigate | Pitfall 5 removes the definition + call from scrape_cead.py; grep gate asserts zero occurrences; test_scrape_cead.py imports from the new module |
| T-18-07 | Denial of Service (silent stale data) | workflow step ordering / index failure | mitigate | Index + payload steps are continue-on-error (D-11): a failed index does not abort the job; the CEAD data already written by the scrape step still commits, so a transient index failure degrades gracefully to a fresh-CEAD/stale-index state rather than blocking the whole refresh; the step still shows as failed in the Actions log for visibility |
</threat_model>

<verification>
- python -m pytest pipeline/tests/test_scrape_cead.py pipeline/tests/test_workflow_order.py -x green
- python pipeline/scrape_cead.py && python pipeline/build_composite_index.py && python pipeline/build_map_payload.py exits 0
- map-payload.json carries ci on scored communes and stays < 30 KB
- workflow step order asserted scrape -> index -> payload -> commit; index/payload steps continue-on-error (D-11)
</verification>

<success_criteria>
- build_map_payload extracted; scrape_cead.py no longer owns it (CI-01 isolation)
- ci field (level 1-5) present in map-payload (CI-10 additive migration seam)
- three-step workflow order wired with continue-on-error on index/payload so CEAD commit is never blocked (D-11)
- no payload-builder regression (test_scrape_cead.py green)
</success_criteria>

<output>
Create `.planning/phases/18-composite-crime-index/18-02-SUMMARY.md` when done.
</output>
