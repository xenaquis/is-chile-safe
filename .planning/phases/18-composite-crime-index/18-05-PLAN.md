---
phase: 18-composite-crime-index
plan: 05
type: execute
wave: 5
depends_on: [18-03, 18-04]
files_modified:
  - site/scripts/validate/forbidden-language.mjs
  - site/scripts/validate/figure-registry.mjs
  - data/SOURCES.md
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
autonomous: false
requirements: [CI-08, CI-09, CI-10]
user_setup: []

must_haves:
  truths:
    - "The forbidden-language validator flags most dangerous / mas peligrosa / mas peligroso / el mas seguro when unqualified by reported / reportado"
    - "The forbidden-language validator allows qualified phrasings (according to reported crime / segun delitos reportados)"
    - "The figure-registry validator registers F13 (SPD VHC homicide, the M1 index input) and F14 (SII) with zero orphans"
    - "data/SOURCES.md documents the composite formula, the locked weight vector, the normalization method, the SPD VHC switch (M1), the SII cap, and reference year 2024"
    - "EN and ES methodology pages document the formula, normalization, SPD switch, and SII caveat"
    - "npm run validate exits 0 on the full build (all consumers non-breaking, CI-10)"
  artifacts:
    - path: "site/scripts/validate/forbidden-language.mjs"
      provides: "Extended FORBIDDEN_TERMS + ALLOW_LIST"
      contains: "most dangerous"
    - path: "site/scripts/validate/figure-registry.mjs"
      provides: "F13 + F14 registered; exclusion comment removed"
      contains: "F13"
    - path: "data/SOURCES.md"
      provides: "Composite methodology + weight vector + SII/SPD documentation"
      contains: "spd_homicide_rate"
  key_links:
    - from: "site/scripts/validate/figure-registry.mjs"
      to: "data/SOURCES.md"
      via: "F13/F14 token substrings must exist verbatim in SOURCES.md"
      pattern: "F13"
    - from: "site/scripts/validate/forbidden-language.mjs"
      to: "dist/ HTML"
      via: "scans built pages for unqualified superlatives"
      pattern: "FORBIDDEN_TERMS"
---

<objective>
Close the editorial-guard and documentation requirements: extend the forbidden-language validator (new unqualified-superlative terms + qualified allow-list entries), register F13 (SPD VHC homicide, the M1 index input) and F14 (SII) in the figure-registry, document the composite methodology in data/SOURCES.md and both methodology pages, and run the full validator suite to prove the additive schema migration is non-breaking across all consumers.

Purpose: CI-09 (forbidden-language extension), CI-08 (methodology pages + figure-registry zero-orphan), CI-10 (schema migration verified non-breaking; @astrojs/check + full validator suite green). This is the gate that proves no absolute verdict is emitted anywhere and every figure is sourced. All M-numbering in docs and the figure registry follows C-01: homicide is M1 (spd_homicide_rate); there is no "M5 homicide".
Output: extended validators; documented SOURCES.md + methodology pages; green npm run validate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-composite-crime-index/18-PATTERNS.md
@.planning/phases/18-composite-crime-index/18-RESEARCH.md
@.planning/phases/18-composite-crime-index/18-03-SUMMARY.md
@.planning/phases/18-composite-crime-index/18-04-SUMMARY.md

<interfaces>
M-numbering (C-01, LOCKED): homicide = M1 (spd_homicide_rate). Older RESEARCH prose that calls homicide "M5"
  or labels F13 "(index M5)" is STALE — use M1 everywhere in SOURCES.md, methodology, and the figure registry.

forbidden-language.mjs (analog FORBIDDEN_TERMS lines 35-57 + ALLOW_LIST lines 63-66; normalizeAccents NFD already strips mas<-mas):
  Add to FORBIDDEN_TERMS (the safest and la mas segura already present):
    most dangerous, mas peligrosa, mas peligroso, el mas seguro
  Add to ALLOW_LIST (qualified usage, D-09):
    /according to reported (crime|incidents)/i
    /segun (delitos|incidentes) reportados/i

figure-registry.mjs (analog F1-F12 entries lines 56-179; F10 token-group shape lines 136-144):
  Remove the F13/F14 exclusion comment (lines 16-17, 54, 162-163) and the F1-F12,F15 hardcoded header/message strings (line ~184).
  Add F13 (SPD VHC homicide, M1 index input): tokens [[SPD, homicidio],[Subsecretaria de Prevencion, homicidio]];
  Add F14 (SII exposure): tokens [[SII, trabajadores],[SII, exposicion]].
  Tokens must be substrings actually present in data/SOURCES.md AFTER it is updated.
  Use ACCENT-FREE spellings in BOTH the registry tokens and SOURCES.md so the substring check matches exactly:
    "Subsecretaria de Prevencion" (no accents), "exposicion" (no accent). Do NOT write "Subsecretaría" / "exposición".

data/SOURCES.md (analog existing ## CEAD / ## INE section headings) must document:
  1. composite formula (weighted sum of 7 winsorized metrics)
  2. the LOCKED weight vector verbatim (C-02): spd_homicide_rate 0.30, cead_robos_rate 0.20, cead_vida_rate 0.15,
     cead_propiedad_rate 0.12, cead_vif_rate 0.10, cead_drogas_rate 0.08, cead_armas_rate 0.05
  3. normalization = winsorized min-max, limits [0.01, 0.01]
  4. SPD VHC switch: M1 (spd_homicide_rate) uses SPD for the index; CEAD grupo-101 featured_rates.homicidios preserved for display
  5. SII exposure caveat (HQ-domicile; cap 5.0)
  6. reference year 2024
  The literal substrings the figure tokens need MUST appear verbatim (accent-free):
    "SPD" + "homicidio"; "Subsecretaria de Prevencion" + "homicidio"; "SII" + "trabajadores"; "SII" + "exposicion".

methodology pages (EN site/src/pages/methodology.astro + ES site/src/pages/es/metodologia.astro):
  document formula, chosen normalization method, SPD homicide switch (M1), SII exposure caveat; every new figure registered.

Consumers to verify non-breaking (D-12, all 6): ChoroplethLayer.ts, ResultPanel.tsx, commune/[slug].astro,
  es/comuna/[slug].astro, figure-registry.mjs, pipeline tests. @astrojs/check + full validator suite green.
all.mjs chains the 13/14 validators; npm run validate runs the suite against dist/.

OneDrive desync (memory): chain build + validate in ONE command.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend forbidden-language validator (terms + allow-list)</name>
  <files>site/scripts/validate/forbidden-language.mjs</files>
  <read_first>
    - site/scripts/validate/forbidden-language.mjs (FORBIDDEN_TERMS lines 35-57; ALLOW_LIST lines 63-66; normalizeAccents NFD)
    - .planning/phases/18-composite-crime-index/18-PATTERNS.md (forbidden-language.mjs section lines 235-250)
    - .planning/phases/18-composite-crime-index/18-RESEARCH.md (Forbidden-Language Validator Extension lines 550-582)
  </read_first>
  <action>
    Add to FORBIDDEN_TERMS (the safest and la mas segura already present): most dangerous, mas peligrosa, mas peligroso, el mas seguro. The normalizeAccents NFD pass already maps mas peligrosa to match mas peligrosa. Add to ALLOW_LIST two qualified-usage patterns so reported/reportado framing is permitted: /according to reported (crime|incidents)/i and /segun (delitos|incidentes) reportados/i (D-09). Do not change the scan target (still dist/ HTML).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node scripts/validate/forbidden-language.mjs</automated>
  </verify>
  <acceptance_criteria>
    - forbidden-language.mjs FORBIDDEN_TERMS contains most dangerous, mas peligrosa, mas peligroso, el mas seguro
    - ALLOW_LIST contains the two qualified-usage regexes
    - node scripts/validate/forbidden-language.mjs exits 0 against the current dist/
  </acceptance_criteria>
  <done>Validator flags the four unqualified superlatives and permits reported/reportado-qualified phrasings; passes on current build.</done>
</task>

<task type="auto">
  <name>Task 2: Document SOURCES.md (M1 homicide) + register F13/F14 in figure-registry</name>
  <files>data/SOURCES.md, site/scripts/validate/figure-registry.mjs</files>
  <read_first>
    - data/SOURCES.md (existing ## CEAD / ## INE / ## SPD / ## SII section structure)
    - site/scripts/validate/figure-registry.mjs (F1-F12 entries lines 56-179; F10 shape lines 136-144; F13/F14 exclusion comment lines 16-17,54,162-163; header/message strings line ~184)
    - .planning/phases/18-composite-crime-index/18-PATTERNS.md (figure-registry.mjs section lines 254-267)
    - .planning/phases/18-composite-crime-index/18-RESEARCH.md (Figure Registry Extension lines 586-620 — NOTE: treat the "(index M5)" label there as stale; homicide is M1 per C-01)
  </read_first>
  <action>
    Update data/SOURCES.md to document (verbatim where locked): the composite formula (weighted sum of 7 winsorized metrics), the C-02 weight vector exactly (spd_homicide_rate 0.30, cead_robos_rate 0.20, cead_vida_rate 0.15, cead_propiedad_rate 0.12, cead_vif_rate 0.10, cead_drogas_rate 0.08, cead_armas_rate 0.05), the winsorized min-max normalization with limits [0.01, 0.01], the SPD VHC homicide switch (M1 = spd_homicide_rate uses SPD; CEAD grupo-101 featured_rates.homicidios preserved), the SII exposure caveat (HQ-domicile, cap 5.0), and reference year 2024. Label homicide as M1 (NOT M5). Ensure the accent-free substrings the figure tokens need appear literally: "SPD" + "homicidio"; "Subsecretaria de Prevencion" + "homicidio"; "SII" + "trabajadores"; "SII" + "exposicion".
    In figure-registry.mjs remove the F13/F14 exclusion comment and the hardcoded F1-F12,F15 header/message strings; add F13 (SPD VHC homicide, the M1 index input — tokens [[SPD, homicidio],[Subsecretaria de Prevencion, homicidio]]) and F14 (SII exposure — tokens [[SII, trabajadores],[SII, exposicion]]) following the F10 token-group shape. Any F13 description text must say M1, not M5. The token substrings must match SOURCES.md verbatim (accent-free).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node scripts/validate/figure-registry.mjs</automated>
  </verify>
  <acceptance_criteria>
    - data/SOURCES.md contains the verbatim C-02 weight vector and the strings winsorized, limits [0.01, 0.01], 2024, cap 5.0
    - data/SOURCES.md labels homicide as M1 (no "M5" homicide reference)
    - `grep -q "Subsecretaria de Prevencion" data/SOURCES.md` succeeds (exact accent-free spelling)
    - `grep -q "exposicion" data/SOURCES.md` succeeds (exact accent-free spelling)
    - `grep -q "trabajadores" data/SOURCES.md` and `grep -q "homicidio" data/SOURCES.md` succeed
    - figure-registry.mjs contains F13 and F14 entries; F13/F14 exclusion comment removed; F13 description says M1 (not M5)
    - node scripts/validate/figure-registry.mjs exits 0 (zero orphans, F13/F14 tokens resolve in SOURCES.md)
  </acceptance_criteria>
  <done>SOURCES.md documents the full composite methodology with the locked vector and M1 homicide labeling; the exact accent-free token substrings are present; F13/F14 registered with M1 description; figure-registry zero-orphan passes.</done>
</task>

<task type="auto">
  <name>Task 3: Methodology pages (EN + ES) + full non-breaking validator gate</name>
  <files>site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro</files>
  <read_first>
    - site/src/pages/methodology.astro (existing methodology section structure; how figures/sources are cited)
    - site/src/pages/es/metodologia.astro (ES mirror)
    - data/SOURCES.md (updated in Task 2 — mirror the documented facts, incl. M1 homicide labeling)
    - .planning/phases/18-composite-crime-index/18-RESEARCH.md (Consumer Enumeration lines 531-546; CI-10 schema migration)
  </read_first>
  <action>
    Add a composite-index methodology section to EN methodology.astro and ES metodologia.astro documenting: the composite formula (weighted sum of 7 winsorized metrics), the chosen normalization method (winsorized min-max, limits [0.01, 0.01]), the SPD VHC homicide switch (M1 index input uses SPD; CEAD grupo-101 preserved for the per-family display), and the SII exposure caveat (cap 5.0, HQ-domicile limitation), reference year 2024. Use M1 labeling for homicide (not M5). Cite F13 (SPD) and F14 (SII) so the figure-registry resolves. Mirror EN content in ES (translated); hardcode ES internal links per the localized-slug pitfall. Never use absolute-verdict language.
    Then run the full migration gate: chain build + validate in ONE command (OneDrive desync). This proves all 6 consumers (ChoroplethLayer, ResultPanel, both commune pages, figure-registry, pipeline tests) are non-breaking and @astrojs/check + the validator suite are green (CI-10).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - methodology.astro and es/metodologia.astro document formula, normalization method, SPD switch (M1), SII caveat (CI-08)
    - figure-registry zero-orphan passes (F13/F14 cited on methodology pages)
    - cd site && npm run build && npm run validate exits 0 (CI-10 — all consumers non-breaking, @astrojs/check + full suite green)
    - forbidden-language validator green on the rebuilt dist/ (CI-09 end-to-end)
  </acceptance_criteria>
  <done>Both methodology pages document the composite methodology (M1 homicide) with figures cited; full build + 13/14 validators + @astrojs/check green; schema migration proven non-breaking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built dist/ HTML -> public | Editorial/legal: no absolute safety verdict may ship; every figure must be sourced |
| SOURCES.md tokens -> figure-registry | Documentation completeness gate (zero-orphan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13 | Reputation/Legal | unqualified superlative verdict in any built page | mitigate | forbidden-language.mjs adds most dangerous / mas peligrosa / mas peligroso / el mas seguro; ALLOW_LIST permits only reported/reportado-qualified usage; runs in npm run validate against dist/ |
| T-18-14 | Repudiation (unsourced figure) | F13/F14 displayed without a registered source | mitigate | figure-registry F13/F14 tokens must resolve in SOURCES.md; accent-free grep gate (Subsecretaria de Prevencion / exposicion) asserts the substrings exist; zero-orphan gate fails the build otherwise |
| T-18-15 | Integrity (schema migration breaks a consumer silently) | featured_rates -> 7-metric additive migration | mitigate | npm run build + full validator suite + @astrojs/check exercise all 6 consumers; additive-only fields (D-12) keep featured_rates intact |
</threat_model>

<verification>
- node scripts/validate/forbidden-language.mjs exits 0 (CI-09)
- node scripts/validate/figure-registry.mjs exits 0 zero-orphan (CI-08)
- grep -q "Subsecretaria de Prevencion" data/SOURCES.md && grep -q "exposicion" data/SOURCES.md succeed (token/doc no-drift)
- cd site && npm run build && npm run validate exits 0 (CI-10, all consumers non-breaking)
- SOURCES.md + both methodology pages document formula, normalization, SPD switch (M1), SII caveat
</verification>

<success_criteria>
- Forbidden-language validator extended and green (CI-09)
- Methodology pages + SOURCES.md document the composite with M1 homicide labeling; figure-registry zero-orphan (CI-08)
- F13/F14 token substrings present verbatim (accent-free) in SOURCES.md — registry and doc cannot drift
- Schema migration non-breaking; @astrojs/check + full validator suite green (CI-10)
</success_criteria>

<output>
Create `.planning/phases/18-composite-crime-index/18-05-SUMMARY.md` when done.
</output>
