---
phase: 18-composite-crime-index
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/composite_index.py
  - pipeline/tests/test_composite.py
autonomous: true
gap_closure: true
requirements: [CI-02]
must_haves:
  truths:
    - "normalize_metric winsorizes only the non-NaN values, so NaN communes coexisting with an extreme outlier cannot distort the winsorize clip point; the clip point is derived from real-data maxima"
  artifacts:
    - path: "pipeline/composite_index.py"
      provides: "NaN-masked winsorization: winsorize computed on arr[mask] then scattered back, NaN positions untouched; misleading masked-array comment corrected"
      contains: "mask"
    - path: "pipeline/tests/test_composite.py"
      provides: "Test combining a NaN commune with a large outlier asserting the clip point comes from real data, not NaN"
      contains: "nan"
  key_links:
    - from: "pipeline/composite_index.py normalize_metric"
      to: "scipy winsorize"
      via: "winsorize applied to arr[mask] (non-NaN only) then scattered to original positions"
      pattern: "mask|isnan"
---

<objective>
Close Gap 3 (CI-02): normalize_metric winsorizes a plain ndarray containing NaN, so NaN (which sorts to the high end) can corrupt the upper clip point when NaN communes coexist with extreme outliers.

Purpose: composite_index.py:38 calls `winsorize(arr, ...)` on a plain ndarray that still contains NaN. scipy.stats.mstats.winsorize sorts NaN to the high end, so the upper-1% clip point can be derived from NaN positions rather than real-data maxima. The line ~37 comment "scipy masked array handles this" is false — no masked array is ever created. The NaN re-injection at line 49 restores output NaN but does NOT fix the clip computation. No existing test exercises NaN + outlier together (the current NaN test at lines 208-213 uses a clean series with no outlier).

Output: winsorization runs on the non-NaN subset only and is scattered back into original positions; NaN positions stay NaN; the misleading comment is removed/corrected; a new test combines a NaN commune with a large outlier and asserts the clip point is derived from the real-data maximum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-composite-crime-index/18-VERIFICATION.md
@.planning/phases/18-composite-crime-index/18-CONTEXT.md

<interfaces>
From pipeline/composite_index.py:22-51 (normalize_metric — the function to fix):
- Input: pd.Series of float (rate per 100k); NaN = missing commune.
- Output: pd.Series in [0,100] with NaN preserved at the same index.
- D-01 locked: `winsorize(limits=[0.01,0.01])` (WINSORIZE_LIMITS from composite_config) then min-max to [0,100].
- Current broken lines: 35 (`arr = np.array(series, dtype=float)`), 37 (false comment), 38 (`winsorize(arr,...)`), 40-46 (min-max via nanmin/nanmax), 49 (NaN re-injection).
- Spread guard (existing, must still pass): test_normalize_spread asserts 25th pctile > 10% of max.

WR-01 (non-blocking warning) — composite_index.py:142-145: dead arithmetic
`score = (weighted_sum / total_weight) * 100.0 / 100.0 * 100.0` immediately overwritten by line 146 `score = weighted_sum / total_weight`. Fold a trivial one-line removal of the dead line 143 (+ the now-stale comment 144-145) into Task 1 ONLY if removing it leaves line 146 as the sole, correct assignment. Otherwise leave untouched.

From pipeline/tests/test_composite.py:183-213 (test_normalize_spread — add the new NaN+outlier test alongside, do not delete the spread guard).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mask NaN before winsorizing in normalize_metric (and remove the false comment)</name>
  <read_first>
    - pipeline/composite_index.py lines 22-51 (normalize_metric) and 142-148 (WR-01 dead arithmetic)
    - pipeline/composite_config.py (WINSORIZE_LIMITS value)
    - .planning/phases/18-composite-crime-index/18-VERIFICATION.md Gap 3 (decided fix)
    - .planning/phases/18-composite-crime-index/18-CONTEXT.md D-01 (winsorized min-max locked)
  </read_first>
  <action>
    In normalize_metric: build a boolean `mask = ~np.isnan(arr)`. Initialise the winsorized array as a NaN-filled copy (`arr_w = np.full_like(arr, np.nan)`), compute `winsorize(arr[mask], limits=list(WINSORIZE_LIMITS))` on the non-NaN subset only, and scatter the winsorized values back into `arr_w[mask]`. Then compute min-max scaling from `np.nanmin(arr_w)`/`np.nanmax(arr_w)` exactly as before; NaN positions remain NaN through scaling. Keep the existing `hi == lo` zero-fill guard and the final NaN re-injection (now redundant but harmless — keep or drop, but NaN positions MUST stay NaN). Guard the empty/all-NaN case: if `mask.sum() == 0`, return an all-NaN Series of the original length/index. Remove the false line ~37 comment "scipy masked array handles this" and replace it with an accurate note that NaN is masked out before winsorize so clip points come only from real data. WR-01: if trivially safe, delete the dead line 143 and its stale comment (144-145), leaving line 146 `score = weighted_sum / total_weight` as the sole assignment; otherwise leave that block untouched. Do not change METRIC_WEIGHTS, SII logic, or any other function.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe"; python -m pytest pipeline/tests/test_composite.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - composite_index.py normalize_metric computes winsorize on `arr[mask]` (non-NaN only) and scatters results back; grep-assertable presence of `mask`/`isnan` masking before winsorize.
    - The false "scipy masked array handles this" comment is gone; replaced by an accurate description.
    - NaN inputs still produce NaN outputs at the same index positions.
    - Existing test_normalize_spread still passes (spread guard intact).
    - All of pipeline/tests/test_composite.py passes (`pytest ... -x` exits 0).
    - If WR-01 was touched: composite_index.py has no dead `* 100.0 / 100.0 * 100.0` line and compute_composite still returns the correct `weighted_sum / total_weight` score.
  </acceptance_criteria>
  <done>normalize_metric masks NaN before winsorize; comment corrected; full test_composite.py suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Add a NaN+outlier winsorization correctness test</name>
  <read_first>
    - pipeline/tests/test_composite.py lines 183-213 (existing test_normalize_spread + single-NaN check)
    - pipeline/composite_index.py normalize_metric (post-fix, from Task 1)
  </read_first>
  <action>
    Add a new test function (e.g. test_normalize_nan_outlier_clip) in pipeline/tests/test_composite.py that constructs a series containing BOTH at least one NaN commune AND a large outlier (e.g. a clean ascending series of ~345 values with one value set to a Sierra-Gorda-scale outlier such as 43000.0 and another distinct index set to NaN). Run normalize_metric and assert: (1) the NaN index stays NaN in the output; (2) the non-NaN max of the output is finite and equals 100.0 (clip point derived from real data, not from NaN); (3) the spread guard still holds (25th pctile of non-NaN output > 10% of the non-NaN max). The assertions must prove the upper clip point is derived from real-data values, not NaN positions. Do not modify or remove the existing test_normalize_spread.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe"; python -m pytest pipeline/tests/test_composite.py -k "nan" -q</automated>
  </verify>
  <acceptance_criteria>
    - A new test exercises a series with at least one NaN commune AND a large outlier in the same series.
    - The test asserts the NaN position stays NaN, the non-NaN output max is finite (==100.0), and the spread guard holds — proving the clip point comes from real data.
    - `pytest pipeline/tests/test_composite.py -k nan` passes (exits 0).
    - Full suite `pytest pipeline/tests/test_composite.py` still passes.
  </acceptance_criteria>
  <done>NaN+outlier test added and passing; clip-point-from-real-data behavior locked by an automated assertion.</done>
</task>

</tasks>

<verification>
- `python -m pytest pipeline/tests/test_composite.py -x -q` exits 0 (full suite, including the new NaN+outlier test and the unchanged spread guard).
- Grep: normalize_metric masks NaN (`isnan`/`mask`) before `winsorize`; no surviving "scipy masked array handles this" comment.
- composite_config / SII logic / other functions unchanged.
</verification>

<success_criteria>
CI-02 satisfied: normalization prevents NaN from distorting winsorize clip points; the correctness is proven by an automated NaN+outlier test, and the existing distribution-spread guard still passes.
</success_criteria>

<output>
Create `.planning/phases/18-composite-crime-index/18-08-SUMMARY.md` when done.
</output>
