---
phase: 18-composite-crime-index
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - data/SOURCES.md
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
autonomous: true
gap_closure: true
requirements: [CI-04, CI-08]
must_haves:
  truths:
    - "The published SII exposure-cap description in SOURCES.md and both methodology pages (EN + ES) accurately states that the denominator FALLS BACK TO the INE resident population when the SII/INE ratio exceeds 5.0 — matching locked decision D-03 and composite_index.py:84"
  artifacts:
    - path: "data/SOURCES.md"
      provides: "Corrected SII cap description (INE fallback, not 5.0x INE)"
      contains: "INE"
    - path: "site/src/pages/methodology.astro"
      provides: "Corrected EN SII exposure caveat (F14)"
    - path: "site/src/pages/es/metodologia.astro"
      provides: "Corrected ES SII exposure caveat (F14)"
  key_links:
    - from: "data/SOURCES.md + methodology pages"
      to: "pipeline/composite_index.py:84 (returns INE on cap)"
      via: "documented behavior now matches code (INE fallback)"
      pattern: "falls back|fall back|recurre|sustituye|reemplaza"
---

<objective>
Close Gap 2 (CI-04, CI-08): the docs describe the SII exposure cap incorrectly. The CODE IS CORRECT per locked decision D-03; only the documentation is wrong.

Purpose: D-03 in 18-CONTEXT.md states "when sii_workers/ine_population > 5.0, fall back to INE population as the denominator." composite_index.py:84 correctly `return ine, True` on cap. But SOURCES.md:104 says "value clamped to cap 5.0 before normalization", methodology.astro:417 says "capped at 5.0 × the commune's INE resident population", and es/metodologia.astro:434 says "se limita a 5,0 veces la población residente INE" — all three contradict D-03 and the code. For Providencia (ratio 5.186) the false docs imply denominator 581,010 while the code uses 116,202 (raw INE).

Output: All three documents accurately state the cap = falls back to the INE resident population when the SII/INE ratio exceeds 5.0. composite_index.py is NOT touched. EN and ES stay in sync; figure-registry F14 tokens stay accent-free; editorial constraint (no absolute verdict) intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-composite-crime-index/18-VERIFICATION.md
@.planning/phases/18-composite-crime-index/18-CONTEXT.md

<interfaces>
Locked semantics (D-03, 18-CONTEXT.md): when `sii_workers / ine_population > 5.0`, the denominator FALLS BACK to the INE resident population. The 5.0 ratio is a TRIGGER for fallback, NOT a multiplier applied to INE.

Source of truth (pipeline/composite_index.py:79-85):
- `if ratio > SII_CAP_RATIO: return ine, True` — returns raw INE population on cap.

Current WRONG doc strings to correct:
- data/SOURCES.md:104 — "`sii_workers / ine_population > 5.0` → value clamped to `cap 5.0` before normalization."
- site/src/pages/methodology.astro:416-417 — "the metric is capped at 5.0 × the commune's INE resident population before normalization."
- site/src/pages/es/metodologia.astro:433-434 — "la métrica se limita a 5,0 veces la población residente INE de la comuna antes de la normalización."

Figure-registry F14 tokens that MUST remain present and accent-free (SOURCES.md:112-113): "SII" + "trabajadores", "SII" + "exposicion".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the SII cap description in SOURCES.md to INE-fallback semantics</name>
  <read_first>
    - data/SOURCES.md (lines ~98-113: SII exposure caveat M7 + figure-registry F14 tokens)
    - .planning/phases/18-composite-crime-index/18-CONTEXT.md D-03/D-04 (locked fallback semantics)
    - pipeline/composite_index.py lines 79-85 (source of truth: returns ine on cap)
  </read_first>
  <action>
    In data/SOURCES.md replace the line ~104 cap clause so it states the rule accurately: when the ratio `sii_workers / ine_population` exceeds 5.0, the metric FALLS BACK to the INE resident-population denominator (the 5.0 is a fallback trigger threshold, not a multiplier). Remove the misleading "value clamped to `cap 5.0`" / "5.0 × INE" phrasing. Keep the surrounding M7 caveat text (HQ-domicile distortion, available_metrics decrement) intact. Do NOT alter the figure-registry F14 tokens at lines ~112-113 ("SII"+"trabajadores", "SII"+"exposicion") and keep all SII/figure tokens accent-free.
  </action>
  <verify>
    <automated>if ((Select-String -Path data/SOURCES.md -Pattern "5\.0 . INE|5\.0.*INE.*resident|clamped to .cap 5\.0|cap 5\.0. before normalization").Count -eq 0 -and (Select-String -Path data/SOURCES.md -Pattern "falls back|fall back").Count -ge 1) { exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - data/SOURCES.md no longer contains a "5.0 × INE" / "clamped to cap 5.0 before normalization" assertion.
    - data/SOURCES.md states the denominator falls back to the INE resident population when the SII/INE ratio exceeds 5.0.
    - F14 tokens "SII"+"trabajadores" and "SII"+"exposicion" still present and accent-free.
    - No absolute safety-verdict language introduced.
  </acceptance_criteria>
  <done>SOURCES.md SII cap description matches D-03 and composite_index.py:84; F14 tokens intact.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the EN + ES methodology SII exposure caveat (F14) and keep them in sync</name>
  <read_first>
    - site/src/pages/methodology.astro lines ~410-419 (EN SII exposure caveat F14)
    - site/src/pages/es/metodologia.astro lines ~427-436 (ES SII exposure caveat F14)
    - data/SOURCES.md corrected SII paragraph from Task 1 (keep all three in agreement)
    - pipeline/composite_index.py lines 79-85 (source of truth)
  </read_first>
  <action>
    In methodology.astro (EN) replace the clause "the metric is capped at 5.0 × the commune's INE resident population before normalization" with accurate wording: when the SII/INE worker-to-resident ratio exceeds 5.0, the metric falls back to (uses) the INE resident population as the denominator. Mirror the same correction in es/metodologia.astro (ES): replace "se limita a 5,0 veces la población residente INE de la comuna antes de la normalización" with wording stating that cuando la razón trabajadores SII / población residente INE supera 5,0, la métrica recurre a (utiliza) la población residente INE como denominador. Keep the existing "available metrics decremented by one" / "decrementado en uno" sentence. Keep the EN and ES paragraphs semantically identical (in-sync, D-07 bilingual parity). Note the existing typo `<\strong>` at methodology.astro:413 may be left as-is unless it falls inside your edited span; do not introduce new markup errors. Do NOT change figure-registry F14 headings or tokens, and introduce no absolute safety-verdict phrasing.
  </action>
  <verify>
    <automated>if ((Select-String -Path site/src/pages/methodology.astro -Pattern "5\.0 . the commune|capped at 5\.0 .").Count -eq 0 -and (Select-String -Path site/src/pages/es/metodologia.astro -Pattern "5,0 veces").Count -eq 0) { cd site; npx astro check 2>&1 | Select-String -Pattern "metodologia|methodology" -Quiet; exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - methodology.astro no longer asserts "5.0 × INE" / "capped at 5.0 ×"; it states INE-fallback-on-ratio-exceeds-5.0.
    - es/metodologia.astro no longer asserts "5,0 veces la población residente INE"; it states the same INE fallback in Spanish.
    - EN and ES caveats are semantically equivalent (bilingual parity).
    - `npx astro check` reports no new errors for either methodology page.
    - No absolute safety-verdict language; F14 tokens/headings unchanged.
  </acceptance_criteria>
  <done>Both methodology pages describe the SII cap as INE fallback, in sync, matching the code and SOURCES.md; astro check green.</done>
</task>

</tasks>

<verification>
- Grep across all three files confirms no surviving "5.0 × INE" / "5,0 veces" / "clamped to cap 5.0" assertion.
- All three state INE fallback when ratio > 5.0.
- `npx astro check` clean; figure-registry F14 tokens accent-free and present (validator unchanged behavior).
- `npm run validate` (forbidden-language) still green — no verdict language introduced.
- composite_index.py untouched.
</verification>

<success_criteria>
CI-04 (documented cap matches implementation) and CI-08 (accurate methodology pages) satisfied: code and docs now agree that the SII denominator falls back to INE resident population when the worker/resident ratio exceeds 5.0.
</success_criteria>

<output>
Create `.planning/phases/18-composite-crime-index/18-07-SUMMARY.md` when done.
</output>
