---
phase: 18-composite-crime-index
plan: 04
type: execute
wave: 4
depends_on: [18-02, 18-03]
files_modified:
  - site/src/components/map/ResultPanel.tsx
  - site/src/lib/data.ts
  - site/src/pages/commune/[slug].astro
  - site/src/pages/es/comuna/[slug].astro
  - site/src/components/map/map.css
  - site/src/config/i18n.ts
autonomous: true
requirements: [CI-05, CI-06]
user_setup: []

must_haves:
  truths:
    - "The ResultPanel popup shows the integer 0-100 composite score, its band label, and the national + regional rank when in index mode"
    - "Each commune page (EN and ES) shows the integer composite score, band label, national rank, and regional rank"
    - "Every index-displaying surface renders an always-visible (never collapsed) bilingual caveat block"
    - "Band labels render EN Very Low/Low/Moderate/High/Very High and ES Muy Bajo/Bajo/Moderado/Alto/Muy Alto"
    - "The score is rendered as Math.round(score) integer, never a decimal, never with a /100 suffix"
    - "When composite_index is absent the whole section is omitted (no N/A placeholder)"
  artifacts:
    - path: "site/src/components/map/ResultPanel.tsx"
      provides: "Composite index JSX block + caveat block; extended CommuneData interface"
      contains: "composite_index"
    - path: "site/src/pages/commune/[slug].astro"
      provides: "EN commune-page composite section + caveat"
      contains: "ci-caveat"
    - path: "site/src/pages/es/comuna/[slug].astro"
      provides: "ES commune-page composite section + caveat"
      contains: "ci-caveat"
  key_links:
    - from: "site/src/components/map/ResultPanel.tsx"
      to: "composite_index[String(year)]"
      via: "reads score/rank/regional_rank/level from commune data"
      pattern: "composite_index\\[String"
    - from: "site/src/pages/commune/[slug].astro"
      to: "site/src/lib/data.ts loadCommune"
      via: "additive composite_index field rides along"
      pattern: "loadCommune"
---

<objective>
Surface the composite index in every display surface: extend ResultPanel.tsx (CommuneData interface + composite score JSX block + mandatory caveat), add the composite section + caveat to both commune Astro pages (EN and ES), and add the CSS classes. All score reads use String(year) keys; the score renders as a rounded integer with a 5-band label and national + regional rank; the caveat is always visible.

Purpose: CI-05 (integer 0-100 + 5 bands + national/regional rank in popup, panel, commune pages) and CI-06 (mandatory bilingual caveat, no absolute verdict). This is the editorial-risk surface — the caveat is the strongest defense against the index reading as a verdict (D-07).
Output: extended ResultPanel.tsx; composite sections in both commune pages; .ci-rank / .ci-caveat / .composite-index-section CSS; i18n band + caveat strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-composite-crime-index/18-PATTERNS.md
@.planning/phases/18-composite-crime-index/18-UI-SPEC.md
@.planning/phases/18-composite-crime-index/18-02-SUMMARY.md

<interfaces>
ResultPanel.tsx (analog homicide section lines 318-351 — IIFE, String(year) keying, !== undefined null-vs-zero guard, bilingual ternaries, panel-section wrapper; TOTAL_COMMUNES=346 const exists line 36):
  Extend CommuneData (lines 52-71) additively (D-12):
    composite_index?: Record<string, { score: number; rank: number; regional_rank: number; level: 1|2|3|4|5; available_metrics: number }>;
    spd_homicide_rate?: Record<string, number>;
    sii_exposure_index?: Record<string, number | null>;
  New JSX section between stat-card (sec 3) and mini-stats (sec 4). Render Math.round(ci.score) (NEVER toFixed);
    band via BAND_EN/BAND_ES arrays (D-08); national #rank + regional_rank (C-03/CI-05); always-visible caveat (D-07).

UI-SPEC (locked):
  - Placement: new .panel-section.composite-index-section after .panel-badge-row, before existing .stat-card.
  - Score .stat-big 28px/700; band .stat-unit 14px/700 muted; rank .ci-rank 14px/400 muted.
  - Caveat .ci-caveat: card bg, 1px var(--line) border, 3px var(--primary) left border, 8px/16px padding, 14px/400 muted; never inside <details>.
  - Bands EN: Very Low/Low/Moderate/High/Very High; ES: Muy Bajo/Bajo/Moderado/Alto/Muy Alto.
  - Rank format EN: #138 of 346 nationally · #12 in {region}; ES: #138 de 346 a nivel nacional · #12 en {region}.
  - Caveat EN: Composite of reported crimes (CEAD/SPD), adjusted for SII economic activity. Does not reflect unreported crime. Reference year: 2024.
  - Caveat ES: Compuesto de delitos reportados al sistema policial (CEAD/SPD), ajustado por actividad economica SII. No refleja delitos no denunciados. Datos de referencia: 2024.
  - Empty state: section omitted entirely (no N/A).
  - New CSS in map.css only: .composite-index-section, .ci-rank, .ci-caveat.

Commune pages (analog: commune/[slug].astro head 26-86 for data loading; MethodologyCaveat.astro import line 25 + its <aside class="caveat"> bilingual pattern lines 13-41):
  - loadCommune(cut) (data.ts:167) already returns full commune JSON; composite_index rides along additively (D-12).
  - Add loadComparatorTable() to data.ts mirroring loadIndex() (data.ts:132-138, cached readFileSync+JSON.parse) only if needed at build time.
  - Strings go through EN_STRINGS/ES_STRINGS in site/src/config/i18n.ts — do NOT inline raw HTML text (forbidden-language validator scans dist/).
  - i18n localized-slug (memory): hardcode /es/comuna/... cross-links; do not rely on getRelativeLocaleUrl for ES slugs.

Pitfall 3: composite_index keys are strings after JSON round-trip — always String(year) (TS) / str(year).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResultPanel composite block + caveat + i18n strings</name>
  <files>site/src/components/map/ResultPanel.tsx, site/src/components/map/map.css, site/src/config/i18n.ts</files>
  <read_first>
    - site/src/components/map/ResultPanel.tsx (CommuneData interface lines 52-71; homicide IIFE section lines 318-351; TOTAL_COMMUNES line 36)
    - .planning/phases/18-composite-crime-index/18-PATTERNS.md (ResultPanel.tsx section lines 201-215)
    - .planning/phases/18-composite-crime-index/18-UI-SPEC.md (Composite Index Stat Card; Mandatory Caveat Block sections)
    - site/src/config/i18n.ts (EN_STRINGS/ES_STRINGS shape)
  </read_first>
  <behavior>
    - When composite_index[String(year)] exists, the panel renders the integer score, band label, national #rank, and regional_rank
    - The caveat block text is always present (not behind <details>) in EN and ES
    - When composite_index is absent the section is not rendered (no N/A)
  </behavior>
  <action>
    Extend CommuneData additively (D-12) with composite_index?, spd_homicide_rate?, sii_exposure_index? (shapes in interfaces). Add a new .panel-section.composite-index-section between the level-chip row and the existing stat-card, using the homicide IIFE pattern: read ci = data.composite_index?.[String(year)]; guard absence (omit section). Render Math.round(ci.score) in .stat-big (NEVER toFixed, no /100 suffix); band label from BAND_EN/BAND_ES[ci.level-1] (D-08) in .stat-unit; .ci-rank showing #rank of 346 nationally and #regional_rank in region (lang-aware). Add the always-visible .ci-caveat block with the exact locked EN/ES caveat copy from UI-SPEC — route the caveat + band + rank strings through EN_STRINGS/ES_STRINGS in i18n.ts (do not inline raw text; the forbidden-language validator scans dist/). Add .composite-index-section, .ci-rank, .ci-caveat to map.css per UI-SPEC styling.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - ResultPanel.tsx CommuneData declares composite_index, spd_homicide_rate, sii_exposure_index (optional)
    - ResultPanel.tsx renders Math.round(ci.score) and references composite_index[String( ... )]
    - grep -c 'toFixed' on the composite block == 0 (integer only)
    - .ci-caveat and .composite-index-section present in map.css
    - caveat strings live in i18n.ts (not inline in JSX literal HTML)
    - npx astro check passes
  </acceptance_criteria>
  <done>ResultPanel shows integer score + band + national/regional rank + always-visible bilingual caveat; absent-data omits the section; TS clean.</done>
</task>

<task type="auto">
  <name>Task 2: Commune pages (EN + ES) composite section + caveat</name>
  <files>site/src/pages/commune/[slug].astro, site/src/pages/es/comuna/[slug].astro, site/src/lib/data.ts</files>
  <read_first>
    - site/src/pages/commune/[slug].astro (head 26-86 data loading; MethodologyCaveat import line 25; StatCard/LevelChip usage)
    - site/src/pages/es/comuna/[slug].astro (ES mirror structure)
    - site/src/lib/data.ts (loadCommune line 167; loadIndex cache pattern lines 132-138)
    - site/src/components/MethodologyCaveat.astro (aside.caveat bilingual locale-prop pattern lines 13-41)
    - .planning/phases/18-composite-crime-index/18-PATTERNS.md (commune page sections lines 219-231)
    - .planning/phases/18-composite-crime-index/18-UI-SPEC.md (Commune Page Index Section)
  </read_first>
  <action>
    EN commune/[slug].astro: loadCommune(cut) already returns the additive composite_index (D-12). Below the existing Incidence Rate block and above the family breakdown bars, render a static <section class="panel-section composite-index-section"> with the integer score (.stat-big = composite_index[String(REFERENCE_YEAR)].score rounded), band label (.stat-unit), and .ci-rank showing #national_rank of 346 nationally and #regional_rank in {region_name}. Below it render an always-visible .ci-caveat (clone the MethodologyCaveat aside pattern but unconditional, never <details>) with the locked EN caveat copy via EN_STRINGS. Omit the whole section when composite_index is absent (no N/A).
    ES comuna/[slug].astro: mirror exactly with ES band labels (Muy Bajo/Bajo/Moderado/Alto/Muy Alto) and the locked ES caveat via ES_STRINGS; hardcode /es/comuna/ cross-links (i18n localized-slug pitfall). Add loadComparatorTable() to data.ts mirroring loadIndex only if a build-time comparator read is needed; otherwise rely on loadCommune.
    Use String(year) keying (Pitfall 3). Do not introduce forbidden absolute-verdict language.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx astro check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - site/src/pages/commune/[slug].astro contains composite-index-section and ci-caveat
    - site/src/pages/es/comuna/[slug].astro contains composite-index-section and ci-caveat with ES band labels
    - Both pages render national rank AND regional rank (CI-05)
    - npm run build succeeds; a sampled built commune HTML contains the caveat text
    - no /100 suffix and no toFixed on the score in either page
  </acceptance_criteria>
  <done>EN + ES commune pages display integer score + band + national/regional rank with an always-visible bilingual caveat; build green; no absolute-verdict language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commune JSON -> rendered HTML | Editorial/legal boundary — a single score implies a verdict through rank + color alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Reputation/Legal | absolute safety verdict in commune/popup HTML | mitigate | Mandatory always-visible bilingual caveat (D-07) on every surface; caveat strings frame the index as composite of reported crime; no safe/dangerous superlatives; forbidden-language validator (Plan 05) scans dist/ |
| T-18-11 | Spoofing (false precision) | float score display | mitigate | Math.round(score) integer only; no toFixed; no /100 suffix (UI-SPEC forbidden-language list) |
| T-18-12 | Information disclosure (misleading rank without context) | rank shown without caveat | mitigate | Rank always rendered adjacent to the always-visible caveat in the same .composite-index-section |
</threat_model>

<verification>
- npx astro check passes (CI-05 TS types)
- npm run build succeeds; built EN + ES commune HTML contain the caveat text and integer score
- ResultPanel renders score + band + national/regional rank + caveat in index mode
</verification>

<success_criteria>
- Integer 0-100 score + 5 bands + national/regional rank in popup, panel, and both commune pages (CI-05)
- Always-visible bilingual caveat on every index-displaying surface (CI-06)
- No absolute verdict; integer-only score; absent-data omits the section
</success_criteria>

<output>
Create `.planning/phases/18-composite-crime-index/18-04-SUMMARY.md` when done.
</output>
