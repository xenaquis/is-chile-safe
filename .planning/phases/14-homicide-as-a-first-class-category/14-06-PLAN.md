---
phase: 14-homicide-as-a-first-class-category
plan: 06
type: execute
wave: 5
depends_on: ["14-05"]
files_modified:
  - site/src/components/map/ResultPanel.tsx
autonomous: false
requirements: [HOM-02]
must_haves:
  truths:
    - "The commune detail panel shows a separate homicide figure — rate per 100k + absolute case count + CEAD year (D-06/D-07) — distinct from the aggregated vida family bar"
    - "A zero/no-data homicide year renders as an explicit, non-alarmist 'no reported cases — CEAD {year}' (never blank, never 'dangerous/safe')"
    - "The homicide section cites CEAD subgroup 101 + year, in both EN and ES"
    - "PanelFamilyBars (the 7 family bars) is unchanged — no regression to family rendering (HOM-02)"
    - "The full build + map validator passes after the UI changes (chained build+validate per OneDrive note)"
  artifacts:
    - path: "site/src/components/map/ResultPanel.tsx"
      provides: "Homicide breakdown section reading featured_rates.homicidios + homicidios_count"
      contains: "homicidios_count"
  key_links:
    - from: "site/src/components/map/ResultPanel.tsx homicide section"
      to: "featured_rates.homicidios[year] / homicidios_count[year]"
      via: "per-commune JSON fetched on panel open"
      pattern: "featured_rates"
---

<objective>
Add the separate homicide figure to the commune detail panel (HOM-02, D-06/D-07):
rate per 100k + absolute case count + CEAD year, distinct from the aggregated vida
family bar, with an explicit non-alarmist zero/no-data state, bilingual and
CEAD-cited — and verify the whole homicide feature end-to-end in a real browser.

Purpose: Homicide is rare; many small comunas have 0 or very few cases where the
rate is volatile and easily misread. Showing the raw count + year alongside the rate
is the most honest framing and satisfies the editorial rule (never "dangerous/safe";
always cite CEAD + year). The 7 family bars must not regress.
Output: The panel homicide section + a passing build + a human-verified map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md
@.planning/phases/14-homicide-as-a-first-class-category/14-RESEARCH.md
@.planning/phases/14-homicide-as-a-first-class-category/14-05-SUMMARY.md

<interfaces>
<!-- From site/src/components/map/ResultPanel.tsx -->
interface CommuneData { id; name; series; last_updated?; incidents?; ... }   # lines 52-65; featured_rates NOT yet typed here
# panel fetches /data/cead/comunas/${cut}.json on open (lines 107-126)
# rate display pattern lines 165-198; PanelFamilyBars render ~line 309 (byFamily 7-element array)
# i18n pattern: {lang === 'es' ? 'ES' : 'EN'}  (e.g. line 134)
# CSS classes available: panel-section, stat-mid, stat-sub, official-row; var(--muted)

<!-- Data contract from 14-02 (per-commune JSON) -->
# featured_rates.homicidios: {year: rate}     (0 stored explicitly; absent year = no data — Pitfall 5)
# featured_rates.homicidios_count: {year: count}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the homicide breakdown section to ResultPanel</name>
  <files>site/src/components/map/ResultPanel.tsx</files>
  <read_first>
    - site/src/components/map/ResultPanel.tsx — CommuneData interface (lines 52-65), fetch pattern (lines 107-126), rate display pattern (lines 165-198), PanelFamilyBars render (~line 309), i18n inline pattern (line 134)
    - .planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md §"ResultPanel.tsx — Add homicide figure section" (lines 486-559) — the exact section + null-vs-0 guard + regression note
    - .planning/phases/14-homicide-as-a-first-class-category/14-RESEARCH.md §"ResultPanel.tsx (D-06/D-07)" — placement (after family bars, before incidents) + zero-state copy
    - CLAUDE.md — editorial: never "peligroso/seguro"; cite CEAD + year; explicit non-alarmist 0 state; i18n parity
  </read_first>
  <action>
    In site/src/components/map/ResultPanel.tsx, extend the local CommuneData interface
    so featured_rates is typed with homicidios?: Record<string,number> and
    homicidios_count?: Record<string,number> (plus propiedad/secuestros for completeness).
    Add a homicide breakdown section AFTER the family bars (after the PanelFamilyBars
    render ~line 309), before the incidents section. Read homRate =
    featured_rates.homicidios?.[String(year)] and count =
    featured_rates.homicidios_count?.[String(year)] using `!== undefined` presence
    checks (NOT truthiness — a confirmed 0 must not read as "no data", Pitfall 5).
    When data is present: render the rate ("X por 100.000 hab." / "X per 100,000 inhab.")
    and, when count is present, the absolute count ("(Y casos)" / "(Y cases)" with
    singular/plural). When the year is absent (no data): render an explicit non-alarmist
    "Sin casos reportados — CEAD {year}" / "No reported cases — CEAD {year}". Always
    append a CEAD source line citing subgroup 101 Delitos contra la Vida / Life Crimes +
    year. Use existing panel-section/stat-mid/stat-sub/official-row classes and the
    inline {lang === 'es' ? ... : ...} i18n pattern. Do NOT touch PanelFamilyBars or the
    by_family 7-element array — the homicide section is purely additive (HOM-02
    no-regression).
  </action>
  <verify>
    <automated>cd site && npx tsc --noEmit 2>&1 | grep -i "ResultPanel" && exit 1 || node -e "const f=require('fs').readFileSync('site/src/components/map/ResultPanel.tsx','utf8'); if(!/homicidios_count/.test(f)||!(/Sin casos reportados/.test(f)&&/No reported cases/.test(f))) process.exit(1); console.log('homicide panel section present EN+ES')"</automated>
  </verify>
  <acceptance_criteria>
    - site/src/components/map/ResultPanel.tsx renders a homicide section with rate + count + CEAD year, after the family bars and before incidents
    - Zero/no-data renders explicit non-alarmist "Sin casos reportados — CEAD {year}" / "No reported cases — CEAD {year}" (both locales present)
    - Presence is checked via `!== undefined` (confirmed 0 shows as 0, not "no data")
    - PanelFamilyBars and the by_family array are untouched
    - `cd site && npx tsc --noEmit` reports no error in ResultPanel.tsx
  </acceptance_criteria>
  <done>The panel shows an honest, CEAD-cited, bilingual homicide figure without regressing the family bars.</done>
</task>

<task type="auto">
  <name>Task 2: Full chained build + validate (forbidden-language + map checks green)</name>
  <files>site/src/components/map/ResultPanel.tsx</files>
  <read_first>
    - site/package.json — build, prebuild (sync-data), validate scripts
    - site/scripts/validate/forbidden-language.mjs — the "never peligroso/seguro" editorial gate
    - .planning/STATE.md — OneDrive dist/ desync: chain build + validate in ONE command
  </read_first>
  <action>
    Run a single chained command that mirrors data (sync-data prebuild), builds the
    site, and runs the validators — at minimum the map validator (Check 8/9 from 14-03)
    and the forbidden-language validator (ensures no "peligroso/seguro" copy slipped
    into the homicide strings). Because the repo lives in OneDrive, chain build +
    validate in one invocation so dist/ does not desync between processes. Fix any
    type error, validator failure, or forbidden-language hit before completing.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -5 && node scripts/validate/map.mjs && node scripts/validate/forbidden-language.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd site && npm run build` completes without error
    - `node scripts/validate/map.mjs` passes (including Check 8/9)
    - `node scripts/validate/forbidden-language.mjs` passes (no "peligroso/seguro" in new homicide copy)
  </acceptance_criteria>
  <done>The full homicide feature builds and passes the editorial + map validators in one chained run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the homicide layer + panel end-to-end in a browser</name>
  <files>none — verification only (no files modified)</files>
  <action>
    Human-verification checkpoint. The executor starts the dev server and pauses for
    the user to confirm the homicide chip, the independent choropleth, and the panel
    figure (including the zero-state) render correctly in a real browser, with no
    family-bar regression and no console errors. See what-built / how-to-verify.
  </action>
  <what-built>
    The homicide feature end-to-end: an 8th 'Homicidios'/'Homicide' chip with the
    featured accent-dot, an independent homicide choropleth on its own scale, and a
    commune-panel homicide figure (rate + count + CEAD year, with an explicit
    non-alarmist no-data state). The 7 family bars are unchanged.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd site && npm run dev` (predev runs sync-data; map data served from public/).
    2. Open http://localhost:4321/map/ (EN) and http://localhost:4321/es/mapa/ (ES).
    3. Confirm an 8th chip labeled 'Homicide' (EN) / 'Homicidios' (ES) with the featured accent-dot; hover shows the honest "subgroup 101 within Life Crimes / subgrupo 101 Delitos contra la Vida" tooltip — NOT implying an 8th sibling family.
    4. Click the homicide chip: the choropleth recolors on its OWN scale (variation visible across comunas, not collapsed to one color); zero-homicide comunas are lightest/visually distinct. Change the year — the homicide layer persists (does not revert to the family/level scale).
    5. Click a high-population commune (e.g. Santiago): the panel shows a homicide figure with rate per 100k + absolute count + CEAD year. The 7 family bars are still present (no regression).
    6. Click a small/zero-homicide commune: the panel shows an explicit non-alarmist "no reported cases — CEAD {year}" / "Sin casos reportados — CEAD {year}", never blank, never "dangerous/safe".
    7. Confirm zero new browser console errors on both map pages.
  </how-to-verify>
  <verify>
    <human-check>User confirms the homicide chip, independent choropleth, panel figure (incl. zero-state), bilingual copy, and no family-bar regression all check out with no console errors.</human-check>
  </verify>
  <resume-signal>Type "approved" if the homicide chip, independent choropleth, panel figure (incl. zero-state), bilingual copy, and no-regression all check out — or describe the issues.</resume-signal>
  <done>The full homicide feature is human-verified in a real browser across both locales, with no family-bar regression and no console errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-commune JSON → panel render | Panel reads CEAD-derived values fetched same-origin; values pre-clamped at pipeline time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering / Information disclosure | ResultPanel rendering homicide rate/count from JSON | mitigate | Values are integers clamped in the pipeline (T-14-03/04); rendered as text via React (auto-escaped); `!== undefined` guard prevents misreading 0 as "no data" — honest editorial framing preserved |
| T-14-14 | Repudiation (editorial/legal) | homicide copy could imply "dangerous/safe" | mitigate | forbidden-language validator (Task 2) gates the build; copy cites CEAD + year and uses reported-incidence framing per editorial policy; explicit non-alarmist zero state |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages — uses existing panel components/classes; no install task |
</threat_model>

<verification>
- `cd site && npx tsc --noEmit` passes for ResultPanel.tsx
- `npm run build` + map.mjs + forbidden-language.mjs all green
- Human-verify: homicide chip + independent choropleth + panel figure (incl. zero-state) + no family-bar regression + no console errors
</verification>

<success_criteria>
- Panel shows separate homicide rate + count + CEAD year, distinct from vida (HOM-02)
- Explicit non-alarmist zero/no-data state (D-07), bilingual, CEAD-cited
- No regression to PanelFamilyBars (HOM-02)
- Full build + editorial + map validators green; browser-verified end-to-end
</success_criteria>

<output>
Create `.planning/phases/14-homicide-as-a-first-class-category/14-06-SUMMARY.md` when done.
</output>
