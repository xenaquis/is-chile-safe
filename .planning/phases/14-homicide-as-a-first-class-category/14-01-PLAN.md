---
phase: 14-homicide-as-a-first-class-category
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pipeline/experiments/test_subgroup101.py
  - pipeline/tests/test_scrape_cead.py
  - pipeline/tests/fixtures/cead_subgroup101_rate_sample.html
  - pipeline/tests/fixtures/cead_subgroup101_count_sample.html
autonomous: false
requirements: [HOM-01]
must_haves:
  truths:
    - "The exact CEAD POST param that selects homicide subgroup 101 is confirmed against the live endpoint (subgrupo[]=101 or the documented fallback grupo[])"
    - "It is confirmed whether rate (medida=2) and count (medida=1) require one or two separate requests"
    - "A captured live HTML response for subgroup 101 exists as a test fixture so downstream pipeline tests run offline"
    - "A pytest scaffold for the homicide accumulator exists (RED — expected to fail until 14-02 implements the loop)"
  artifacts:
    - path: "pipeline/experiments/test_subgroup101.py"
      provides: "Live runtime experiment confirming the subgroup-101 POST param + medida behavior"
    - path: "pipeline/tests/test_scrape_cead.py"
      provides: "Nyquist test scaffold for homicide accumulator + featured_rates.homicidios population"
    - path: "pipeline/tests/fixtures/cead_subgroup101_rate_sample.html"
      provides: "Captured live CEAD rate response (medida=2, subgroup 101) for offline parser tests"
  key_links:
    - from: "pipeline/experiments/test_subgroup101.py"
      to: "pipeline/cead/client.py::make_cead_session / _DATA_ENDPOINT"
      via: "import + session.post"
      pattern: "make_cead_session|_DATA_ENDPOINT"
---

<objective>
Confirm — against the LIVE CEAD endpoint — the exact POST parameter that selects
homicide subgroup 101 within the `vida` family, and whether rate (`medida=2`)
and absolute count (`medida=1`) come back in one response or need two requests.
This is the hard prerequisite for the entire pipeline half (D-01, D-03): the
`subgrupo[]=101` param is INFERRED from `como scrap.txt`, NOT confirmed live.
Also lay down the pytest scaffold (RED) that 14-02 will turn GREEN.

Purpose: De-risk the single highest-uncertainty assumption (RESEARCH Assumption
A1/A2, confidence LOW) before any fetch-loop code is written. If `subgrupo[]`
is silently ignored or rejected, every downstream pipeline task would produce
garbage (the full `vida` aggregate mislabeled as homicide).
Output: A deleted-after-use experiment script that printed its confirmation, two
captured HTML fixtures, and a failing test scaffold ready for implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-homicide-as-a-first-class-category/14-CONTEXT.md
@.planning/phases/14-homicide-as-a-first-class-category/14-RESEARCH.md
@.planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md

<interfaces>
<!-- From pipeline/cead/client.py — the executor uses these directly. -->
make_cead_session() -> requests.Session   # warmed session with Referer/Origin headers
_DATA_ENDPOINT  # POST target: get_estadisticas_delictuales.php
fetch_communes_batch(session, cut_codes, year, familia_id) -> str  # latin-1 decoded HTML

<!-- From pipeline/scrape_cead.py — the parser the experiment must validate against. -->
parse_cead_table(html) -> list[dict]   # each row: {"name": str, "values": {year_str: val_str}}
_match_cut(name, catalog) -> str | None
parse_cead_float(s) -> float | None    # maps '', '-', 'S/I', 'N/A' -> None

<!-- Existing test infra (from pipeline/tests/) -->
<!-- pytest run from repo root: python -m pytest pipeline/tests/ -->
<!-- conftest.py FIXTURES_DIR = pipeline/tests/fixtures; sample_cead_html fixture pattern -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Live runtime experiment — confirm subgroup-101 POST param + medida behavior</name>
  <files>pipeline/experiments/test_subgroup101.py</files>
  <read_first>
    - .planning/phases/14-homicide-as-a-first-class-category/14-RESEARCH.md §"CEAD Subgroup-101 Scrape Mechanics" (the test_subgroup101.py template, lines ~140-187) and §"Common Pitfalls" Pitfall 1 + Pitfall 2
    - pipeline/cead/client.py — make_cead_session, _DATA_ENDPOINT, fetch_communes_batch (lines 82-129) for the exact POST payload shape and latin-1 decode
    - pipeline/scrape_cead.py — parse_cead_table + _match_cut + parse_cead_float (row accumulation pattern, PATTERNS.md lines 698-707)
    - como scrap.txt — the CEAD param list (medida, familia[], grupo[], subgrupo[])
  </read_first>
  <action>
    Create pipeline/experiments/test_subgroup101.py mirroring the RESEARCH template.
    It must build a session via make_cead_session(), POST to _DATA_ENDPOINT twice for
    year 2024 with familia[]="1", subgrupo[]="101", seleccion="1", tipoVal="1,2",
    descarga="true", comuna[]=["13101","13102"], region[]="13" — once with medida="2"
    (rate) and once with medida="1" (count). Write each raw response to
    pipeline/cache/test_subgroup101_rate.html and _count.html. Run it, then feed
    each response through parse_cead_table to confirm: (a) the response has commune
    rows (non-zero formato_4 rows); (b) medida="2" returns plausible homicide RATES
    (single-to-low-double digits per 100k, < 200 even for high-crime comunas);
    (c) medida="1" returns plausible absolute COUNTS (small integers, < 1000);
    (d) the two responses differ numerically (count != rate). If subgrupo[]=101
    returns 0 rows OR values identical to a familia[]=1 (full vida aggregate)
    request, the param is ignored — RETRY with grupo[]="101" per the documented
    fallback (RESEARCH Pitfall 1), and record which param worked.
    Record the CONFIRMED facts in the plan SUMMARY: (1) the exact param key that
    selects subgroup 101 (`subgrupo[]` vs `grupo[]`), (2) confirmation that rate and
    count need two separate requests (medida is single-valued per request), (3) the
    zero-commune marker observed (`"0,0"` vs `"-"` — settles RESEARCH Assumption A3).
    This task requires a live network call to the CEAD government server that the
    executor must run and observe — it cannot be auto-verified offline. Reuse the
    existing courtesy rhythm: no tight loop, a single pair of requests.
  </action>
  <verify>
    <automated>MISSING — live network confirmation; verified by human observation of printed STATUS/SIZE/sample-values output. The captured fixtures (Task 2) are the durable artifact.</automated>
    <human-check>Executor runs `python pipeline/experiments/test_subgroup101.py`; both POSTs return HTTP 200 with commune rows; rate values are plausible homicide rates and count values are small integers; the working param (subgrupo[] or grupo[]) and the zero-marker are recorded in the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - pipeline/experiments/test_subgroup101.py exists and runs without exception
    - Both responses (rate + count) contain parseable commune rows for 13101/13102
    - medida=2 values are plausible rates (< 200/100k); medida=1 values are small integer counts
    - SUMMARY records: the confirmed param key, one-vs-two request finding, and the observed zero-marker (`"0,0"` vs `"-"`)
  </acceptance_criteria>
  <resume-signal>Type "confirmed: subgrupo[]" (or "confirmed: grupo[]") plus the zero-marker observed, or describe the failure so the fallback can be re-planned.</resume-signal>
  <done>The live experiment confirms which POST param selects subgroup 101 and that rate+count are two requests; findings recorded for 14-02 to implement against.</done>
</task>

<task type="auto">
  <name>Task 2: Capture live responses as offline test fixtures</name>
  <files>pipeline/tests/fixtures/cead_subgroup101_rate_sample.html, pipeline/tests/fixtures/cead_subgroup101_count_sample.html</files>
  <read_first>
    - pipeline/tests/conftest.py — FIXTURES_DIR pattern + sample_cead_html fixture (lines 1-25)
    - pipeline/tests/fixtures/ — existing captured CEAD HTML fixtures for naming/format reference
    - pipeline/cache/test_subgroup101_rate.html + _count.html — the raw responses captured in Task 1
  </read_first>
  <action>
    Copy the two raw responses captured in Task 1 from pipeline/cache/ into
    pipeline/tests/fixtures/ as cead_subgroup101_rate_sample.html and
    cead_subgroup101_count_sample.html (preserve latin-1 bytes verbatim — do not
    re-encode). These give downstream pipeline tests a real, offline subgroup-101
    response to parse without hitting CEAD. If the live capture is large, trim to a
    handful of commune rows but keep the surrounding HTML table structure intact
    (parse_cead_table must still find formato_4 rows).
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; import sys; [sys.exit(1) for f in ['cead_subgroup101_rate_sample.html','cead_subgroup101_count_sample.html'] if not (Path('pipeline/tests/fixtures')/f).exists()]; print('fixtures present')"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/tests/fixtures/cead_subgroup101_rate_sample.html exists and is non-empty
    - pipeline/tests/fixtures/cead_subgroup101_count_sample.html exists and is non-empty
    - Running parse_cead_table over each fixture yields at least one commune row
  </acceptance_criteria>
  <done>Two offline fixtures exist so 14-02's pipeline tests parse real subgroup-101 HTML without a live call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing test scaffold for homicide accumulator (Nyquist RED)</name>
  <files>pipeline/tests/test_scrape_cead.py</files>
  <read_first>
    - pipeline/tests/test_output.py — the full file (build_map_payload tests, _make_minimal_records helper, 30KB guard at lines 66-77, entry-schema test at 145-164) — the exact analog for the new tests
    - .planning/phases/14-homicide-as-a-first-class-category/14-RESEARCH.md §"Validation Architecture" → "Phase Requirements → Test Map" + "Wave 0 Requirements"
    - pipeline/cead/normalizer.py — attach_featured (lines 213-233) — what featured_rates.homicidios should contain after population
  </read_first>
  <behavior>
    - Test: build_map_payload entries expose a `homicide_rate` key (int or None) — currently absent, so RED
    - Test: build_map_payload entries expose a `homicide_count` key (int or None) — currently absent, so RED
    - Test: a 346-entry payload WITH homicide fields still serializes to < 30720 bytes (extends the existing budget guard)
    - Test: after the homicide accumulator runs over the captured fixture, featured_rates.homicidios for a sampled commune is non-empty (parses the fixture via parse_cead_table) — RED until 14-02 wires the loop
  </behavior>
  <action>
    Create pipeline/tests/test_scrape_cead.py. Reuse the _make_minimal_records
    helper shape from test_output.py but extend records so the homicide path can be
    exercised. Add tests asserting: build_map_payload entries contain `homicide_rate`
    and `homicide_count` keys; a 346-entry payload carrying both homicide ints stays
    under 30720 bytes (mirror test_map_payload_346_entries_under_30kb); and a parser
    test that loads cead_subgroup101_rate_sample.html via parse_cead_table and asserts
    plausible rate magnitudes. These tests MUST FAIL now (the keys don't exist yet) —
    that is the intended RED state; do NOT implement production code here. Mark any
    test that depends on the not-yet-written accumulator with a clear assertion so the
    failure message reads as "homicide_rate key missing", not an import error.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_scrape_cead.py -q 2>&1 | grep -E "failed|error" || echo "EXPECTED-RED"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/tests/test_scrape_cead.py exists with at least 3 tests covering homicide_rate, homicide_count, and the 30KB budget
    - The new tests FAIL (RED) because production code is not yet written — failure messages reference the missing homicide keys, not import/collection errors
    - `python -m pytest pipeline/tests/ -q` still collects (no syntax/import breakage of the suite)
  </acceptance_criteria>
  <done>A RED Nyquist scaffold exists; 14-02 implementation will turn it GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CEAD gov endpoint → pipeline | Untrusted external HTML/bytes cross into the Python process during the live experiment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering / Information disclosure | CEAD HTML response parsed by parse_cead_table | mitigate | Decode latin-1 explicitly (no charset guessing); the experiment only reads/prints values, never evals; fixtures are trimmed static HTML committed for offline tests |
| T-14-02 | Denial of Service (to CEAD, courtesy) | Live POST to gov server | mitigate | Single pair of requests in the experiment; no loop; reuse warmed session — no hammering of the government endpoint |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced (RESEARCH §Standard Stack: requests/bs4/tenacity already installed) — no install task, legitimacy gate N/A |
</threat_model>

<verification>
- `python pipeline/experiments/test_subgroup101.py` returns HTTP 200 twice with parseable rows
- Two fixtures present under pipeline/tests/fixtures/
- `python -m pytest pipeline/tests/test_scrape_cead.py` is RED (expected) with homicide-key failures
- `python -m pytest pipeline/tests/ -q` collects without import errors
</verification>

<success_criteria>
- The subgroup-101 POST param is CONFIRMED live (subgrupo[] or grupo[]) and recorded
- Rate-vs-count request count (one vs two) confirmed
- Zero-commune marker (`"0,0"` vs `"-"`) observed and recorded (settles A3)
- Offline fixtures + RED test scaffold in place for 14-02
</success_criteria>

<output>
Create `.planning/phases/14-homicide-as-a-first-class-category/14-01-SUMMARY.md` when done.
CRITICAL: record the confirmed POST param, one-vs-two request finding, and the zero-marker — 14-02 depends on these facts.
</output>
