---
phase: 23-enusc-communal-victimization-layer
plan: 04
type: execute
wave: 4
depends_on: ["23-03"]
files_modified:
  - data/SOURCES.md
  - site/scripts/validate/figure-registry.mjs
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
autonomous: true
requirements: [VL-05]
must_haves:
  truths:
    - "F16 is registered in figure-registry.mjs and its tokens are present in data/SOURCES.md so figure-registry zero-orphan passes"
    - "data/SOURCES.md has a SEPARATE '## INE ENUSC SAE' section distinct from the existing F11 ENUSC underreporting block"
    - "EN + ES methodology pages document the ENUSC SAE VHDV indicator (source, SAE method, experimental status, 136-comuna coverage limit, victimization != reported-crime distinction)"
    - "The composite index and all Phase-18 outputs are byte-unchanged (additive-only) — confirmed by a verification across the full suite"
  artifacts:
    - path: "site/scripts/validate/figure-registry.mjs"
      provides: "F16 entry + F1-F16 banner"
      contains: "F16"
    - path: "data/SOURCES.md"
      provides: "## INE ENUSC SAE communal VHDV section (F16 tokens)"
      contains: "INE ENUSC SAE"
    - path: "site/src/pages/methodology.astro"
      provides: "EN H2 Household Victimization Indicator (ENUSC 2024 SAE)"
      contains: "ENUSC 2024 SAE"
    - path: "site/src/pages/es/metodologia.astro"
      provides: "ES H2 Indicador de Victimización en Hogares (ENUSC 2024 SAE)"
      contains: "ENUSC 2024 SAE"
  key_links:
    - from: "site/scripts/validate/figure-registry.mjs"
      to: "data/SOURCES.md"
      via: "F16 token presence check"
      pattern: "## INE ENUSC SAE"
---

<objective>
Close the phase: register figure F16, add the distinct ENUSC SAE section to SOURCES.md, document the indicator on the EN + ES methodology pages, and run the full suite to prove zero-orphan passes and Phase-18 outputs are byte-unchanged (additive-only).

Purpose: VL-05 (F-series zero-orphan + SOURCES.md + methodology documentation; Phase-18 outputs byte-unchanged).
Output: figure-registry F16, SOURCES.md section, methodology EN/ES sections, green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-enusc-communal-victimization-layer/23-RESEARCH.md
@.planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md
@.planning/phases/23-enusc-communal-victimization-layer/23-02-SUMMARY.md
@.planning/phases/23-enusc-communal-victimization-layer/23-03-SUMMARY.md

<interfaces>
<!-- figure-registry.mjs F15 entry (lines ~178-191) is the analog. F16 is the confirmed next free ID. -->
<!-- F16 tokens (PATTERNS.md): [['## INE ENUSC SAE','VHDV'], ['ENUSC','Victimizacion en Hogares','SAE']]
     -> requires a matching '## INE ENUSC SAE' section in SOURCES.md. -->
<!-- console banner line ~197 currently says "F1-F15" -> update to "F1-F16". -->

<!-- SOURCES.md already has '## ENUSC — underreporting / cifra negra' (F11, line ~292) — DO NOT merge.
     Add a SEPARATE '## INE ENUSC SAE — communal VHDV victimization (experimental)' section
     (full content in PATTERNS.md "data/SOURCES.md" block). -->

<!-- methodology.astro / es/metodologia.astro each already have an ENUSC (F11) section.
     Add a SEPARATE H2 (EN: "Household Victimization Indicator (ENUSC 2024 SAE)";
     ES: "Indicador de Victimización en Hogares (ENUSC 2024 SAE)") — markup in PATTERNS.md. -->

<!-- Phase-18 byte-unchanged scope: composite_index, featured_rates, series, national_rank,
     regional_rank, trend, spd_homicide_rate, sii_exposure_index in data/cead/comunas/*.json. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register F16 + add distinct INE ENUSC SAE section to SOURCES.md</name>
  <read_first>
    - site/scripts/validate/figure-registry.mjs (FIGURE_REGISTRY array, F15 entry ~178-191, banner ~197)
    - data/SOURCES.md (existing ## ENUSC underreporting block ~292-316; token/section format)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (sections "figure-registry.mjs" and "data/SOURCES.md")
  </read_first>
  <action>Append an F16 entry to the FIGURE_REGISTRY array in site/scripts/validate/figure-registry.mjs after F15: id 'F16', description "ENUSC SAE VHDV household victimization rate (experimental, 2024)", tokens [['## INE ENUSC SAE','VHDV'], ['ENUSC','Victimizacion en Hogares','SAE']], note distinguishing it from F11. Update the console banner from "F1-F15" to "F1-F16". Add a SEPARATE new section to data/SOURCES.md titled "## INE ENUSC SAE — communal VHDV victimization (experimental)" copied from the PATTERNS.md block (full name, publisher INE, landing page, file URL with sfvrsn caveat, measure semantics = proportion 0-1 NOT per-100k, INE experimental status, vintage 2024, sha256 ad9503826fd21590eca7cf37629872df09f3115ad0c6d80dfe5cedcb63c3d4ba, Used-by chain, licence, and an explicit "Distinct from" line referencing the F11 underreporting section). Do NOT modify the existing ## ENUSC underreporting block.</action>
  <verify>
    <automated>node site/scripts/validate/figure-registry.mjs && echo REGISTRY_OK</automated>
  </verify>
  <done>figure-registry.mjs has F16 with the two token groups; the banner reads F1-F16; SOURCES.md has the distinct ## INE ENUSC SAE section containing VHDV; the validator exits 0 (zero-orphan, REGISTRY_OK printed).</done>
</task>

<task type="auto">
  <name>Task 2: Document the indicator on EN + ES methodology pages</name>
  <read_first>
    - site/src/pages/methodology.astro (existing ENUSC F11 section; H2 structure)
    - site/src/pages/es/metodologia.astro (existing ENUSC F11 section; H2 structure)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section "methodology.astro + es/metodologia.astro" — EN/ES H2 markup)
  </read_first>
  <action>Add a SEPARATE H2 section to each methodology page after the existing ENUSC (F11) section. EN (methodology.astro): id "enusc-vhdv", heading "Household Victimization Indicator (ENUSC 2024 SAE)", a paragraph explaining the VHDV proportion-of-households measure, the SAE method, the INE experimental "etapa inicial de madurez" status, the 136-of-346 coverage limitation, the explicit statement that it is shown as an additive module and is NOT merged with the CEAD per-100k crime index (distinct measurement: survey victimization vs police denuncias), and an attributed INE source link. ES (es/metodologia.astro): the mirrored Spanish section per PATTERNS.md. Use no absolute safe/dangerous verdict wording (forbidden-language must still exit 0). Keep the existing F11 ENUSC sections untouched.</action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/forbidden-language.mjs && node -e "const fs=require('fs');for(const f of ['dist/methodology/index.html','dist/es/metodologia/index.html']){const h=fs.readFileSync(f,'utf8');if(!h.includes('ENUSC 2024 SAE')){console.error('missing in',f);process.exit(1);}}console.log('METHODOLOGY_OK');"</automated>
  </verify>
  <done>Both methodology pages render the new ENUSC 2024 SAE H2; built dist/ pages contain "ENUSC 2024 SAE"; forbidden-language exits 0 (METHODOLOGY_OK printed).</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate + Phase-18 byte-unchanged verification</name>
  <read_first>
    - site/scripts/validate/all.mjs (the 13-validator suite invocation)
    - .planning/phases/23-enusc-communal-victimization-layer/23-VALIDATION.md (full-suite command; per-requirement test map)
    - pipeline/tests/test_build_enusc_enrichment.py (test_phase18_byte_unchanged — the byte-unchanged proof)
  </read_first>
  <action>Run the phase gate in single chained commands (OneDrive desync — never split build from validate). First the pipeline tests: `cd pipeline && python -m pytest tests/ -q`. Then the site gate: `cd site && npm run build && npm run validate`. Confirm all pytest pass (including test_phase18_byte_unchanged and the ENUSC coverage/graceful tests) and all 13+ Node validators pass (figure-registry zero-orphan with F16, forbidden-language exit 0, commune.mjs enusc presence). This task adds no new source files; it is the VL-05 byte-unchanged + zero-orphan acceptance gate. If any Phase-18 commune field changed, the enrichment from plan 23-02 violated additive-only — fail and report rather than patch around it.</action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/ -q && cd ../site && npm run build && npm run validate && echo PHASE23_GATE_GREEN</automated>
  </verify>
  <done>Full pytest suite green (incl. test_phase18_byte_unchanged), full 13+ Node validator suite green (figure-registry F16 zero-orphan, forbidden-language 0, commune enusc presence); PHASE23_GATE_GREEN printed; Phase-18 outputs confirmed byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/registry edits -> validator suite | Registry + SOURCES tokens must stay in sync; methodology copy must avoid verdict language |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Tampering | figure-registry orphan (F16 token drift) | mitigate | figure-registry.mjs zero-orphan validator gates F16 against the SOURCES.md section in the same commit |
| T-23-11 | Repudiation | methodology copy implying a safety verdict | mitigate | forbidden-language validator run over built methodology pages; copy attributes INE + experimental status |
| T-23-12 | Tampering | silent Phase-18 output regression | mitigate | pytest test_phase18_byte_unchanged + full suite are the phase gate; any Phase-18 field change fails the gate loudly |

No high-severity threats remain after mitigation: documentation-only edits gated by the existing zero-orphan, forbidden-language, and byte-unchanged validators.
</threat_model>

<verification>
- `node site/scripts/validate/figure-registry.mjs` exits 0 (F16 zero-orphan)
- `cd pipeline && python -m pytest tests/ -q && cd ../site && npm run build && npm run validate` all green
- dist/ methodology EN + ES contain "ENUSC 2024 SAE"
- Phase-18 commune fields byte-unchanged (test_phase18_byte_unchanged green)
</verification>

<success_criteria>
- VL-05: F16 registered (zero-orphan), SOURCES.md distinct section + EN/ES methodology documentation present, and the composite index + all Phase-18 outputs verified byte-unchanged via the full gate.
</success_criteria>

<output>
Create `.planning/phases/23-enusc-communal-victimization-layer/23-04-SUMMARY.md` when done.
</output>
