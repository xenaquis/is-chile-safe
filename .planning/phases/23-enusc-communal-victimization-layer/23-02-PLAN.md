---
phase: 23-enusc-communal-victimization-layer
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - pipeline/build_enusc_enrichment.py
  - pipeline/tests/test_build_enusc_enrichment.py
  - .github/workflows/cead-scraper.yml
autonomous: true
requirements: [VL-01, VL-02, VL-05]
must_haves:
  truths:
    - "build_enusc_enrichment.py adds exactly one key (enusc_vhdv) to each of the 136 covered comuna JSON files and touches no other key"
    - "If data/snapshots/enusc_vhdv.json is absent, the enrichment exits 0 (graceful degrade) so the CEAD pipeline is never broken"
    - "A build-time assertion fails loudly unless exactly 136 CUTs resolve to existing comuna files"
    - "All Phase-18 fields (composite_index, featured_rates, series, national_rank, regional_rank, trend, spd_homicide_rate, sii_exposure_index) are byte-unchanged after enrichment"
    - "The CEAD GitHub Actions workflow runs the ENUSC fetch + enrich steps after the CEAD scraper with continue-on-error: true"
  artifacts:
    - path: "pipeline/build_enusc_enrichment.py"
      provides: "additive enrichment of comunas/{CUT}.json with enusc_vhdv; coverage + overwrite-refusal guards"
      contains: "EXPECTED_COVERAGE"
    - path: "pipeline/tests/test_build_enusc_enrichment.py"
      provides: "pytest: graceful-degrade, coverage assertion, additive-only, phase18 byte-unchanged, idempotent/overwrite-refusal"
    - path: ".github/workflows/cead-scraper.yml"
      provides: "ENUSC fetch + enrich steps inserted between CEAD scraper and composite index, continue-on-error"
      contains: "fetch_enusc_vhdv.py"
  key_links:
    - from: "pipeline/build_enusc_enrichment.py"
      to: "data/cead/comunas/{CUT}.json"
      via: "read, add enusc_vhdv only, atomic_write_json"
      pattern: "atomic_write_json"
    - from: ".github/workflows/cead-scraper.yml"
      to: "pipeline/build_enusc_enrichment.py"
      via: "continue-on-error workflow step after CEAD scraper"
      pattern: "continue-on-error"
---

<objective>
Build the additive enrichment step that writes the enusc_vhdv key into the 136 covered comuna JSONs, with a hard byte-unchanged guarantee on all Phase-18 outputs, and wire both ENUSC steps into the CEAD GitHub Actions workflow with graceful failure.

Purpose: VL-01 (graceful failure / isolated step), VL-02 (build-time coverage assertion == 136), VL-05 (Phase-18 outputs byte-unchanged — additive-only is the hard invariant).
Output: pipeline/build_enusc_enrichment.py, its pytest, cead-scraper.yml wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-enusc-communal-victimization-layer/23-RESEARCH.md
@.planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md
@.planning/phases/23-enusc-communal-victimization-layer/23-01-SUMMARY.md

<interfaces>
<!-- enusc_vhdv.json shape (produced by plan 23-01): -->
<!-- { source, source_url, vintage:"2024", fetched, caveat,
       records: [ { cut, name, vhdv_rate, ci_lower, ci_upper, tipo_estimacion, cv } ] } -->

<!-- enrichment loop pattern: build_composite_index.py lines ~345-388 (additive setdefault then atomic_write). -->
<!-- For ENUSC use a flat key (not year-keyed):
     commune["enusc_vhdv"] = {vhdv_rate, ci_lower, ci_upper, tipo_estimacion, cv, vintage:2024}. -->

<!-- Reusable: pipeline.shared.atomic_write.atomic_write_json(path, data) -->
<!-- EXPECTED_COVERAGE = 136 -->
<!-- Phase-18 fields that MUST stay byte-unchanged: composite_index, featured_rates, series,
     national_rank, regional_rank, trend, spd_homicide_rate, sii_exposure_index. -->

<!-- cead-scraper.yml current order (lines ~34-43): Run CEAD scraper -> Build composite index (continue-on-error)
     -> Build map payload (continue-on-error) -> commit -> deploy. Insert ENUSC steps between scraper and composite. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing pytest for enrichment (graceful, coverage, byte-unchanged, idempotent) (RED)</name>
  <read_first>
    - pipeline/tests/test_composite.py (commune-fixture set, additive-enrichment assertion idiom, tmp_path)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section "test_build_enusc_enrichment.py" — required test cases)
  </read_first>
  <behavior>
    - test_missing_snapshot_graceful: no enusc_vhdv.json present; main() raises SystemExit with code 0 (use pytest.raises(SystemExit), assert e.value.code == 0); no comuna files modified.
    - test_coverage_assertion: snapshot with 136 records, all CUTs exist as temp comunas/{cut}.json; after run resolved == 136 and each enriched file has enusc_vhdv with vintage 2024.
    - test_additive_only: a temp commune JSON pre-populated with composite_index + featured_rates; after enrichment those two values are deep-equal to the originals and the enusc_vhdv key was added.
    - test_phase18_byte_unchanged: 10-commune sample; capture json.dumps(sort_keys) of composite_index/featured_rates/series before; after enrichment assert identical strings.
    - test_duplicate_write_refuses_overwrite: run enrichment twice with identical snapshot -> no error (idempotent); a third run with a mutated vhdv_rate for one CUT -> RuntimeError containing "refusing overwrite".
  </behavior>
  <action>Create pipeline/tests/test_build_enusc_enrichment.py mirroring test_composite.py fixtures. Build a temp data root with a comunas/ dir of minimal commune JSONs (include composite_index + featured_rates keys so byte-unchanged is meaningful) and a data/snapshots/enusc_vhdv.json fixture. Monkeypatch the module REPO_ROOT / data_dir / snapshot-path constants to the temp tree (monkeypatch.setattr on the imported module). Cover the five behavior cases. Tests MUST fail now because build_enusc_enrichment.py does not exist (RED).</action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_build_enusc_enrichment.py -x -q 2>&1 | grep -E "rror|no tests ran"</automated>
  </verify>
  <done>test_build_enusc_enrichment.py exists with the five named cases; running it errors on import (RED), proving the tests target the not-yet-written enrichment script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement build_enusc_enrichment.py (additive, coverage guard, overwrite-refusal) (GREEN)</name>
  <read_first>
    - pipeline/build_composite_index.py (lines ~345-388 additive enrichment loop; REPO_ROOT + atomic_write usage)
    - pipeline/shared/atomic_write.py (atomic_write_json)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section "build_enusc_enrichment.py" — verbatim pattern blocks)
  </read_first>
  <action>Create pipeline/build_enusc_enrichment.py. Constants: REPO_ROOT (parent.parent of file), EXPECTED_COVERAGE = 136, data_dir = data/cead, snapshot_path = data/snapshots/enusc_vhdv.json. Graceful degrade: if snapshot_path absent, log.warning and sys.exit(0). Load snapshot, build enusc_by_cut = {r["cut"]: r}. For each cut: commune_path = data_dir/comunas/{cut}.json; if absent log.warning and continue. Read commune JSON; if commune already has enusc_vhdv and it differs from the new value, raise RuntimeError("... refusing overwrite ..."); otherwise set commune["enusc_vhdv"] = {vhdv_rate, ci_lower, ci_upper, tipo_estimacion, cv, vintage:2024} (touch NO other key — additive only) and atomic_write_json. Count resolved. After the loop, if resolved != EXPECTED_COVERAGE raise RuntimeError("Coverage assertion failed ..."). Log enriched count. Do NOT import or modify build_composite_index; do NOT touch any Phase-18 field; do NOT merge vhdv_rate into the composite.</action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_build_enusc_enrichment.py -x -q</automated>
  </verify>
  <done>All five enrichment tests pass (GREEN). Graceful exit on missing snapshot, coverage==136 assertion, additive-only + byte-unchanged Phase-18 fields, idempotent re-run, overwrite-refusal on divergent value.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ENUSC fetch + enrich into cead-scraper.yml (continue-on-error, graceful)</name>
  <read_first>
    - .github/workflows/cead-scraper.yml (current step order; the two existing continue-on-error steps lines ~37-43)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section ".github/workflows/cead-scraper.yml" — exact step YAML)
  </read_first>
  <action>Edit .github/workflows/cead-scraper.yml. Insert two steps between "Run CEAD scraper" and "Build composite index": (1) "Build ENUSC victimization snapshot" with continue-on-error: true and env CACHE_ONLY set to the string '1', running `python pipeline/snapshots/fetch_enusc_vhdv.py`; (2) "Enrich commune JSONs with ENUSC VHDV" with continue-on-error: true running `python pipeline/build_enusc_enrichment.py`. CACHE_ONLY='1' ensures the cron NEVER attempts a network download (no stable URL); if pipeline/cache/enusc_vhdv.xlsx is absent the fetch fails, continue-on-error absorbs it, and CEAD + composite index continue unaffected (VL-01). Do not alter the existing steps, the commit-if-changed step, or the deploy step.</action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('.github/workflows/cead-scraper.yml').read_text(encoding='utf-8'); assert 'fetch_enusc_vhdv.py' in t and 'build_enusc_enrichment.py' in t; k=t.index('scrape_cead.py'); i=t.index('fetch_enusc_vhdv.py'); j=t.index('build_composite_index.py'); assert k<i<j,(k,i,j); assert 'CACHE_ONLY' in t; print('order OK')"</automated>
  </verify>
  <done>cead-scraper.yml has both ENUSC steps positioned after scrape_cead.py and before build_composite_index.py, each with continue-on-error: true; the fetch step sets CACHE_ONLY='1'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot JSON -> commune JSON enrichment | Parsed-then-committed values merged into build-consumed per-commune files |
| CI step -> Phase-18 locked outputs | Enrichment writes the same files Phase-18 produced; risk of clobbering locked data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Tampering | comunas/{CUT}.json Phase-18 fields | mitigate | enrichment adds only enusc_vhdv; pytest test_phase18_byte_unchanged + test_additive_only gate every write; overwrite-refusal raises on any divergent pre-existing enusc_vhdv |
| T-23-05 | Denial of Service | CEAD pipeline broken by ENUSC failure | mitigate | both steps continue-on-error: true; missing snapshot -> sys.exit(0); coverage failure stays isolated to the ENUSC step |
| T-23-06 | Tampering | GitHub Actions step injection | accept | YAML edited adds two trusted local python invocations only; no new secrets, no third-party action added |

No high-severity threats remain after mitigation: the only write target is the additive enusc_vhdv key, guarded by byte-unchanged tests and overwrite refusal; failures are contained by continue-on-error.
</threat_model>

<verification>
- `cd pipeline && python -m pytest tests/test_build_enusc_enrichment.py -x -q` green
- `cd pipeline && python build_enusc_enrichment.py` (with real snapshot present) enriches 136 files; re-run is idempotent
- cead-scraper.yml: ENUSC steps ordered after scraper, before composite, both continue-on-error
</verification>

<success_criteria>
- VL-01: enrichment is isolated and degrades gracefully (exit 0) when the snapshot is absent; CI never breaks CEAD.
- VL-02: coverage assertion enforces exactly 136 resolved CUTs.
- VL-05 (additive half): Phase-18 outputs byte-unchanged after enrichment, proven by pytest.
</success_criteria>

<output>
Create `.planning/phases/23-enusc-communal-victimization-layer/23-02-SUMMARY.md` when done.
</output>
