---
phase: 26-event-clustering-spike
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - pipeline/scripts/build_golden_set.py
  - .planning/phases/26-event-clustering-spike/26-golden-set-draft.json
  - pipeline/tests/fixtures/clustering_golden_set.json
  - pipeline/tests/test_clustering.py
  - .planning/phases/26-event-clustering-spike/26-CALL-LOG.md
autonomous: true
requirements: [CLUS-01]
user_setup: []

must_haves:
  truths:
    - "The golden set contains between 60 and 100 hand-labeled pairs"
    - "The golden set includes the comuna 2101 / 2026-07-01 bucket (9 articles, 3 distinct events) with correct merge/no_merge sub-groupings"
    - "The golden set includes the comuna 4102 / 2026-07-01 Tongoy bucket (8 articles) with the outlier article treated as a presumed distinct event, not pre-declared reporting noise"
    - "The golden set includes a hand-constructed cross-bucket typo/homophone negative sub-set that bypasses (cut,date) bucketing by construction"
    - "Every pair carries a category field distinguishing same_date_diff_crime, multi_outlet_positive, typo_homophone_negative, and sampled pairs, and each of the three CLUS-01 adversarial classes has at least one represented pair"
    - "Every labeled pair has a cached_verdict populated from a real, live OpenRouter call — not synthesized — and no verdict with source != 'model' is ever persisted as cached_verdict"
    - "Every merge-labeled pair has been confirmed by an independent blind second pass that never sees cached_verdict or the first pass's label"
    - "Total live LLM calls made while building the golden set are logged in a cumulative, append-only ledger and stay well under the 2,000-call phase budget"
  artifacts:
    - path: "pipeline/tests/fixtures/clustering_golden_set.json"
      provides: "60-100 hand-labeled pairs, each with incident_a_id, incident_b_id, label, category, label_basis, label_confidence, note, cached_verdict, plus a meta block"
      min_lines: 200
    - path: "pipeline/scripts/build_golden_set.py"
      provides: "sampling + labeling-draft + live-verdict-population CLI used to produce the fixture (--dry-run/--fill-verdicts/--out)"
      min_lines: 80
    - path: ".planning/phases/26-event-clustering-spike/26-CALL-LOG.md"
      provides: "append-only cumulative ledger of OpenRouter calls for the whole phase, seeded with the 3 spike-ping calls"
  key_links:
    - from: "pipeline/scripts/build_golden_set.py"
      to: "pipeline/news/clustering.py"
      via: "bucket_incidents() / prefilter_candidates() / adjudicate_pair()"
      pattern: "from pipeline\\.news\\.clustering import"
    - from: "pipeline/tests/fixtures/clustering_golden_set.json"
      to: "data/incidents/current.json"
      via: "incident_a_id / incident_b_id trace back to real incident IDs"
      pattern: "incident_._id"
---

<objective>
Build the CLUS-01 golden set: 60-100 hand-labeled article pairs drawn from `data/incidents/current.json`, hard-coding the two mandatory adversarial buckets (comuna 2101 Antofagasta, comuna 4102 Tongoy — both dated 2026-07-01), a deterministic supplemental sample of same-`(cut,date)` buckets, and a hand-constructed cross-bucket typo/homophone negative sub-set. Each pair is stamped with a real, live-called `cached_verdict` from `pipeline.news.clustering.adjudicate_pair()` so `test_clustering.py` can later run fully offline against real model output.

Purpose: Produce the evaluation fixture the GO/NO-GO precision computation (Wave 3) depends on — this is the one wave in the phase that legitimately makes live OpenRouter calls, and it must do so within a logged, budget-aware, resumable run whose ground-truth labels are independently confirmed.
Output: `pipeline/tests/fixtures/clustering_golden_set.json` (final, committed fixture) + `pipeline/scripts/build_golden_set.py` (reusable sampling/build CLI) + a cumulative call-count ledger for the phase's LLM budget.

**Bucket source rationale (W5):** `current.json` is the sole source for bucket sampling and for both mandatory buckets — the archive (`26-RESEARCH.md`) only covers 2026-04 and 2026-06, neither of which contains the mandatory 2026-07-01 buckets, so `current.json` is both necessary and sufficient. The hand-constructed typo/homophone negative sub-set (Task 1) may draw comuna names from either source since it does not depend on a specific incident date.

**Threshold freeze (Fable decision, locked):** `_PREFILTER_THRESHOLD = 45.0` from `26-01-PLAN.md` is pre-registered and FROZEN for this plan — no task in this plan may tune it, even though the mandatory buckets measure at 100% pass-through (see 26-01's docstring). Tuning after any precision signal is observed would reintroduce the circularity the golden-set gate exists to prevent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.1-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/26-event-clustering-spike/26-RESEARCH.md
@.planning/phases/26-event-clustering-spike/26-PATTERNS.md
@.planning/phases/26-event-clustering-spike/26-00-SPIKE-PING.md
@pipeline/news/clustering.py
@pipeline/news/store.py
@pipeline/tests/conftest.py
@data/incidents/current.json
</context>

<interfaces>
<!-- Functions from 26-01-PLAN.md's clustering.py that this plan consumes directly. -->

```python
# pipeline/news/clustering.py (built in 26-01)
def bucket_incidents(incidents: list[dict]) -> dict[tuple[str, str], list[dict]]: ...
def prefilter_candidates(bucket: list[dict], threshold: float = 45.0) -> list[tuple[dict, dict]]: ...
def adjudicate_pair(incident_a: dict, incident_b: dict) -> ClusterVerdict: ...
# ClusterVerdict: same_event, confidence, facts, rationale, source ("model"|"failsafe_api"|"failsafe_parse")
```

```python
# pipeline/news/store.py:88-99 — read-only JSON loading helper to reuse for current.json/archive
def _load_incidents_list(path: pathlib.Path) -> list[dict]: ...  # handles missing file, malformed JSON, {"incidents": [...]} wrapper
```

No analog for `build_golden_set.py` exists in `26-PATTERNS.md` — compose it from `store.py`'s `_load_incidents_list`-equivalent read-only JSON loading, `clustering.py`'s `bucket_incidents`/`prefilter_candidates` (do not reimplement bucketing), and the argparse conventions already used across other files in `pipeline/scripts/*.py`.

**`FIXTURES_DIR` note:** `pipeline/tests/conftest.py`'s `FIXTURES_DIR` constant is module-local and NOT importable by other test files — `test_feeds.py:21`, `test_parser.py:10`, and `test_scrape_cead.py:21` each define their own `FIXTURES_DIR = Path(__file__).parent / "fixtures"` locally rather than importing conftest's. `pipeline/tests/test_clustering.py` must do the same: define `FIXTURES_DIR` at its own module level, do not attempt to import it from `conftest.py`.

Confirmed real data (verified 2026-07-29 against `data/incidents/current.json`, do not re-derive — use directly):

**Comuna 2101, 2026-07-01 — 9 articles, 3 distinct events (hard adversarial negative bucket):**
- Event A "copamiento preventivo" (1 article, no pair partner in this bucket -> only appears as a no_merge partner against B/C members): `bfe084c29a65e701`
- Event B "condena homicidio pareja, 12 años" (merge group, 3 articles): `0b69f11f785bc0c5`, `f8af960bd49a010d`, `717c7f1c77418734`
- Event C "operativo vehículos robados" (merge group, candidate members): `0f1e3e9cdc5dd364`, `dc0f9cdcce2ded3b`, `041424e5985d8607` — PLUS two articles requiring closer scrutiny per the relabeling rule below: `16439f8dc78173a6` (an aggregate article, "Dos operativos … 6 detenidos") and `b01de8947aa555f6` (a later procedural-stage article, "formalizados por receptación"). Do not pre-assume these two merge with the arrest-day operativo articles — apply the same-case standard from the Tongoy relabeling instruction (Task 2) to each.

**Comuna 4102, 2026-07-01 (Tongoy) — 8 articles, presumptively one event with one outlier requiring verification:**
- Seven articles report "8 años y medio" / "homicidio frustrado": `9b1e83ae50fd427e`, `282067156cf95860`, `60a9e3a03c773ec4`, `585fb5b5b019e25e`, `e499c44be683263c`, `59dca37a17b66fa6`, `c3ec78c8f3e3be83`
- One outlier reports "19 años" / "homicidio" (consummated, not frustrated): `6eed24180c36dbec`
- **Do not label the outlier as the same event by default.** A 10.5-year sentence delta plus a consummated-vs-frustrated charge difference is the signature of a DIFFERENT case, not reporting noise. `6eed24180c36dbec` is presumed a DISTINCT event (`no_merge` against the other seven) unless the source articles (fetch/read the `url` field, or the R2-archived full text where the URL is unfetchable — Google News URLs are JS pages and cannot be fetched directly) affirmatively prove one case.

**Typo/homophone confusable comuna pairs (verified against `data/ine/poblacion_comunal.json`, use directly for the Task 1 cross-bucket negative sub-set — real comunas, real CUTs, lexically confusable names):**
`Los Andes` (5301) / `Los Angeles` (8301); `Quilaco` (8308) / `Quilleco` (8309); `San Carlos` (16301) / `San Pablo` (10307); `Colina` (13301) / `Molina` (7304); `Caldera` (3102) / `Calera` (5502); `Chanco` (7202) / `Lanco` (14103); `Cochrane` (11301) / `Colchane` (1403); `Pemuco` (16105) / `Penco` (8107); `Peumo` (6112) / `Pirque` (13202); `Talca` (7101) / `Taltal` (2104); `Paillaco` (14107) / `Placilla` (6308); `Nancagua` (6305) / `Rancagua` (6101).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sample supplemental buckets + construct typo/homophone negatives + generate draft candidate pairs</name>
  <files>pipeline/scripts/build_golden_set.py, .planning/phases/26-event-clustering-spike/26-golden-set-draft.json</files>
  <read_first>
    pipeline/news/store.py (lines 88-99, `_load_incidents_list`)
    pipeline/news/clustering.py (from 26-01: `bucket_incidents`, `prefilter_candidates` signatures)
  </read_first>
  <action>
    Create `pipeline/scripts/build_golden_set.py` as a read-only CLI script (never writes to `data/`) with an explicit argparse contract: `--dry-run` (sample + report counts, write nothing to disk), default with no flag (write the draft to disk), `--fill-verdicts` (reserved for Task 3's live-call mode — this task must NOT implement live calling, only accept and no-op-reject the flag with a clear "not yet implemented in this invocation" message if passed, since Task 3 owns that code path), `--out PATH` (override the default draft output path). The `<verify>` command below must match this exact `--dry-run` contract.
    Script responsibilities:
    1. Load `data/incidents/current.json` via a local reimplementation or import of `store._load_incidents_list`-equivalent logic (read-only; do not call `merge_and_write`).
    2. Group incidents into `(cut, date)` buckets by importing and calling `pipeline.news.clustering.bucket_incidents(incidents)` — do NOT reimplement `dedup.py`'s bucketing logic inline; this is the CLUS-02 single-source-of-truth requirement from `26-01-PLAN.md`.
    3. Exclude the two mandatory buckets `("2101","2026-07-01")` and `("4102","2026-07-01")` from supplemental sampling (they are hard-coded separately, per the `<interfaces>` block above).
    4. Deterministic supplemental sampling: build the set of eligible buckets (size 2-5, excluding the two mandatory buckets), sort by `(cut, date)` string order, then walk the sorted list adding WHOLE buckets while `total_pairs + C(size,2) <= 100` (where `total_pairs` starts at 36 + 28 = 64, the pair count of the two mandatory buckets), stopping as soon as the next whole bucket would exceed 100. Record the final `selected_buckets` (list of `(cut,date)` tuples) and `total_pairs` for later inclusion in the fixture `meta`. Never drop individual pairs to hit the 60-100 target — only whole buckets, and only by not including a bucket that would push the running total over 100 (never by post-hoc trimming after seeing labels).
    5. For every selected bucket (including the two mandatory ones), call `prefilter_candidates(bucket)` from `pipeline.news.clustering` — but for the two mandatory buckets specifically, emit ALL intra-bucket pairs regardless of the prefilter threshold result, tagging each with `prefilter_score` (the raw `token_set_ratio`) and `proposed: true|false` (`true` if the pair would have passed `_PREFILTER_THRESHOLD`, `false` otherwise). For supplemental buckets, only emit pairs that pass the prefilter (this is the real production candidate-generation path — 26-RESEARCH.md Pitfall 2: never hand-pick pairs to make the eventual precision number look good), still recording `prefilter_score` and `proposed: true` on each. `proposed: false` pairs are recorded but are never sent to the LLM in Task 3 and never enter the precision numerator/denominator in Wave 3 — a `proposed: false` pair whose eventual label is `merge` is counted as an FN of type `fn_prefiltered` (documented for Wave 3 to consume, not computed here).
    6. Construct the hand-built cross-bucket typo/homophone negative sub-set: pick 8-12 pairs from the confusable comuna list in `<interfaces>` above. For each pair, synthesize two minimal incident dicts with deterministic IDs (`f"typo-{cut}-01"` / `f"typo-{cut}-02"` style, clearly namespaced so they can never collide with real sha256 incident IDs), each with a `title_es` describing a plausible but clearly DIFFERENT crime/date in each of the two confusable comunas (e.g. "Robo con violencia registrado en Los Andes" vs "Detienen a sospechoso de hurto en Los Angeles") and the corresponding real `cut` value from the confusable-pair list. These pairs deliberately come from DIFFERENT `(cut,date)` buckets — document inline in the script (comment + a field on each pair, `bypasses_bucket: true`) that this is intentional: class (b) of CLUS-01's three adversarial classes (same crime reported with a misspelled/homophonous comuna) can never occur inside the strict `(cut,date)` bucketing used elsewhere in this script, so it must be constructed directly. Tag every pair in this sub-set `category: "typo_homophone_negative"`.
    7. Tag every pair generated in steps 5-6 with a `category` field: pairs from the two mandatory buckets get a provisional value reflecting bucket membership (finalized once labels are known in Task 2); supplemental sampled pairs get `category: "sampled"`; typo/homophone sub-set pairs get `category: "typo_homophone_negative"` per step 6.
    8. Write a draft JSON to `.planning/phases/26-event-clustering-spike/26-golden-set-draft.json` (a phase-local working artifact — NEVER under `pipeline/tests/fixtures/`, which is reserved for the final committed fixture only) with one entry per candidate pair (including `proposed: false` pairs and the typo/homophone sub-set): `pair_id` (`f"{cut}-{date}-{a_id[:6]}-{b_id[:6]}"` for bucketed pairs, `f"typo-{a_cut}-{b_cut}"` for the typo/homophone sub-set), `incident_a_id`, `incident_b_id`, `title_a`, `title_b` (for human/Fable review), `prefilter_score`, `proposed`, `category`, `bypasses_bucket` (bool, default false), `label: null` (to be filled in Task 2), no `cached_verdict` yet.
    9. Print a summary to stdout: total buckets sampled, `selected_buckets`, total candidate pairs generated (proposed + not-proposed + typo/homophone), expected range check against 60-100 for the proposed+typo/homophone subset (log a warning, not a failure, if outside range).
  </action>
  <verify>
    <automated>python pipeline/scripts/build_golden_set.py --dry-run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The script never imports or calls `merge_and_write`, `atomic_write_json`, or anything that writes under `data/`.
    - The script imports `bucket_incidents` from `pipeline.news.clustering` — grep confirms no inline `(cut,date)` grouping loop duplicated in `build_golden_set.py`.
    - `.planning/phases/26-event-clustering-spike/26-golden-set-draft.json` is written under the phase directory only, never under `pipeline/tests/fixtures/`.
    - The draft contains at least one pair with `category: "typo_homophone_negative"` and `bypasses_bucket: true`, drawn from a real confusable comuna pair with a real CUT.
    - The draft's proposed+typo/homophone pair count falls in a range Task 2/3 can finalize into 60-100 without dropping individual pairs.
  </acceptance_criteria>
  <done>Draft candidate-pair fixture generated deterministically from real current.json data using the actual bucket_incidents()/prefilter_candidates() functions, plus a hand-constructed typo/homophone negative sub-set, ready for ground-truth labeling.</done>
</task>

<task type="auto">
  <name>Task 2: Label ground truth (offline) + mandatory blind second pass on merge labels</name>
  <files>.planning/phases/26-event-clustering-spike/26-golden-set-draft.json</files>
  <read_first>
    .planning/phases/26-event-clustering-spike/26-golden-set-draft.json (produced by Task 1)
  </read_first>
  <action>
    For the two mandatory buckets, assign `label` as follows (this REVISES the original all-merge assumption for both buckets — do not pre-declare either bucket's outlier pairs as noise):
    - Comuna 2101: pairs with both members inside Event B = `merge`. Pairs with both members inside `{0f1e3e9cdc5dd364, dc0f9cdcce2ded3b, 041424e5985d8607}` (the arrest-day operativo core) = `merge`. For `16439f8dc78173a6` (aggregate "dos operativos") and `b01de8947aa555f6` (later formalization stage) against the operativo core: read the source articles (`url` field, or R2-archived full text if the URL is a JS-rendered Google News page) and assign `merge` ONLY if the articles affirmatively describe the identical arrest/case; default to `no_merge` if ambiguous. Any pair spanning Event A vs B, A vs C, or B vs C = `no_merge`.
    - Comuna 4102 (Tongoy): the 7 pairs among `{9b1e83ae50fd427e, 282067156cf95860, 60a9e3a03c773ec4, 585fb5b5b019e25e, e499c44be683263c, 59dca37a17b66fa6, c3ec78c8f3e3be83}` = `merge`. The 7 pairs spanning `6eed24180c36dbec` against each of those seven = `no_merge` UNLESS the source articles affirmatively prove one case (read `url`/R2 archive) — the default, absent proof, is `no_merge` (a 10.5-year sentence delta + consummated-vs-frustrated is presumed a case difference, not reporting noise).
    For every sampled supplemental pair in the draft, read `title_a`/`title_b` and assign `label: "merge"` only when both headlines plainly describe the same real-world incident (same crime type, same approximate circumstances) — otherwise `label: "no_merge"`. If the total labeled-pair count falls outside 60-100 after Task 1's deterministic bucket selection, adjust by re-running Task 1's bucket-accumulation stopping point (add/remove whole buckets from the tail of the deterministic order) — never trim or cherry-pick individual pair labels to hit the count.
    For the typo/homophone negative sub-set constructed in Task 1: every pair gets `label: "no_merge"` (they are constructed to be lexically confusable but factually distinct incidents by design).
    Every pair must carry `label_basis` — the concrete discriminator used to reach the label (`"fecha"`, `"lugar"`, `"actores"`, `"sentencia"`, `"etapa_procesal"`, or `"cut_mismatch"` for the typo/homophone sub-set) — so a human can re-check the label without re-reading the corpus. Retain `title_a`/`title_b`/`outlet_a`/`outlet_b`/`url_a`/`url_b` on EVERY pair in the final fixture — do not strip these fields even for pairs that feel unambiguous; a label that cannot be audited from the fixture alone is not a label.
    Any `merge` label that rests on headline text alone (i.e. the labeler did not consult the source URL/R2 archive) must be marked `label_confidence: "low"` and will be excluded from the precision denominator in Wave 3 (reported separately there, not decided here) — the labeler should consult the source article for every pair it intends to label `merge`; only when the URL is genuinely unfetchable and no R2 archive text exists should a headline-only `merge` label with `label_confidence: "low"` be accepted.
    **Mandatory blind second pass (Fable decision, LOCKED — not optional):** for every pair currently labeled `merge`, spawn a separate subagent via the Agent tool that receives ONLY `title_a`, `title_b`, `outlet_a`, `outlet_b`, `url_a`, `url_b` for that pair — explicitly never `cached_verdict` (it does not exist yet at this stage), never the first pass's `label`, `label_basis`, or `note`. The subagent must independently judge merge/no_merge. If the second pass agrees `merge`, keep the label and add `second_pass_confirmed: true`. If the second pass says the pair is distinct, change the pair's `label` to `"no_merge"` and record `note: "downgraded by blind second pass"`. If the second pass is unsure/cannot decide, set `excluded: "label_disputed"` on the pair (it leaves the precision denominator and is reported separately in Wave 3) rather than forcing a label. Pairs labeled `no_merge` in the first pass require NO second pass — mislabeling in that direction cannot manufacture a false TP, so it is not the risk this step defends against.
    Update `category` on every pair now that labels are final: mandatory-bucket pairs with `label == "no_merge"` -> `category: "same_date_diff_crime"`; mandatory-bucket pairs with `label == "merge"` -> `category: "multi_outlet_positive"`; typo/homophone sub-set keeps `category: "typo_homophone_negative"`; supplemental sampled pairs keep `category: "sampled"`.
    Write the updated draft back to `.planning/phases/26-event-clustering-spike/26-golden-set-draft.json` (still phase-local, not yet the final fixture — Task 3 finalizes it after live verdicts are filled).
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('.planning/phases/26-event-clustering-spike/26-golden-set-draft.json',encoding='utf-8')); pairs=d['pairs']; assert all(p.get('label') in ('merge','no_merge') or p.get('excluded') for p in pairs); merges=[p for p in pairs if p.get('label')=='merge']; assert all(p.get('second_pass_confirmed') or str(p.get('note','')).startswith('downgraded') for p in merges), 'unconfirmed merge label found'; print('labels OK:', len(pairs), 'pairs,', len(merges), 'merges confirmed')"</automated>
  </verify>
  <acceptance_criteria>
    - Every pair has a `label` in `{"merge","no_merge"}` or is marked `excluded: "label_disputed"`.
    - `6eed24180c36dbec`'s 7 cross-pairs against the rest of the Tongoy bucket are `no_merge` unless a `note` field cites an affirmative source confirmation (not "reporting noise").
    - Every `merge`-labeled pair carries either `second_pass_confirmed: true` or was downgraded/excluded by the blind second pass — no unconfirmed `merge` label survives.
    - Every pair carries `label_basis`, `category`, and retains `title_a`/`title_b`/`outlet_a`/`outlet_b`/`url_a`/`url_b`.
  </acceptance_criteria>
  <done>Ground truth labeled per the revised (non-circular) Tongoy/2101 standard, every merge label independently confirmed by a blind second-pass agent, category and label_basis recorded on every pair.</done>
</task>

<task type="auto">
  <name>Task 3: Live verdict fill (resumable, budget-ledgered) + finalize committed fixture</name>
  <files>pipeline/tests/fixtures/clustering_golden_set.json, pipeline/tests/test_clustering.py, .planning/phases/26-event-clustering-spike/26-CALL-LOG.md</files>
  <read_first>
    .planning/phases/26-event-clustering-spike/26-golden-set-draft.json (final labeled draft from Task 2)
    pipeline/news/clustering.py (`adjudicate_pair`, from 26-01)
    pipeline/tests/test_feeds.py (line 21 — local `FIXTURES_DIR` convention to mirror, per the `<interfaces>` note above; do NOT import conftest.py's)
    .planning/phases/26-event-clustering-spike/26-CALL-LOG.md (if it already exists from a prior partial run — this task must resume from it, not overwrite it)
  </read_first>
  <action>
    Implement the `--fill-verdicts` mode of `pipeline/scripts/build_golden_set.py` (the flag Task 1 stubbed out). Before any live call:
    1. **Preflight.** Load `.env` the way `pipeline/scrape_news.py:162-165` does (try/except around `python-dotenv`'s `load_dotenv`). Assert `os.environ.get("OPENROUTER_API_KEY")` is a non-empty string and not the literal `"placeholder"` (remember the project's own gotcha: unset GitHub secrets surface as empty strings, not missing keys — check emptiness explicitly, not just presence). Make exactly ONE probe call to `adjudicate_pair` against a trivial known pair; if the returned verdict's `source != "model"`, abort the whole task immediately with a loud error — do not proceed to the fill loop.
    2. **Budget ledger.** Read `.planning/phases/26-event-clustering-spike/26-CALL-LOG.md` as an append-only ledger (create it if absent, seeded with a first row recording the 3 calls already spent by `26-00-SPIKE-PING.md`, cumulative=3). Each row: `timestamp, script, calls_this_run, cumulative_total`. Before starting the fill loop, compute the projected call count (`len(pairs needing a verdict)` — see idempotency below) and REFUSE to start if `last_cumulative_total + projected > 2000`, appending a row explaining the abort ("NO-GO-by-cost") per the hard budget rule in `.planning/v2.1-AUTONOMOUS-DIRECTIVE.md`.
    3. **Fill loop — resumable and idempotent.** For every pair in the labeled draft: skip any pair that already has `cached_verdict` with `cached_verdict.source == "model"` (idempotent restart — re-invocation never re-spends calls on already-filled pairs). For every remaining pair, call `adjudicate_pair(incident_a, incident_b)` using the real incidents looked up by ID from `current.json` (or the synthesized dicts for the typo/homophone sub-set, which already carry `title_es`). **Never persist a verdict whose `source != "model"`**: if `adjudicate_pair` returns a fail-safe verdict, leave that pair's `cached_verdict: null` and append its `pair_id` to a `failed_pairs` list in the draft — the fixture is not considered final while `failed_pairs` is non-empty. On `RateLimitError` surfacing as a fail-safe result, sleep briefly and retry once (mirror `classifier.py`'s hand-rolled single retry — do not add `tenacity`, per 26-RESEARCH.md). Write the draft back to disk after every pair (or every 10 pairs) so progress survives an interruption. Abort the loop after 3 consecutive non-model outcomes rather than grinding through repeated failures, leaving whatever was completed as a valid resumable checkpoint.
    4. After the loop completes with `failed_pairs` empty, append a final row to `26-CALL-LOG.md`: total pairs this run, live calls made this run, new cumulative total, timestamp, model used (`ibm-granite/granite-4.1-8b`), `total_possible_in_bucket_pairs` (the count of ALL intra-bucket pairs across every bucket touched — mandatory + supplemental — including `proposed: false` pairs, so Wave 3 can compute an honest reduction ratio), and explicit confirmation the cumulative total is far under the 2,000-call phase budget.
    5. **Fixture `meta` block.** Write into the final fixture's top-level `meta`: `model`, `base_url`, `system_prompt_sha256` (sha256 of `clustering.SYSTEM_PROMPT`), `max_tokens`, `temperature`, `mergeable_rule` (the literal string `"same_event and confidence=='high'"`), `generated` (ISO timestamp), `calls` (this run's call count), `selected_buckets` (from Task 1), `total_pairs`.
    6. Write the final `pipeline/tests/fixtures/clustering_golden_set.json` — top-level `{"meta": {...}, "pairs": [...]}`, each pair with `pair_id`, `incident_a_id`, `incident_b_id`, `title_a`, `title_b`, `outlet_a`, `outlet_b`, `url_a`, `url_b`, `label`, `category`, `label_basis`, `label_confidence` (omit or `"high"` when source-verified, `"low"` when headline-only), `second_pass_confirmed` (bool, where applicable), `excluded` (omit unless disputed), `note`, `prefilter_score`, `proposed`, `bypasses_bucket`, `cached_verdict` (the full `ClusterVerdict.model_dump()`, always `source == "model"`). Delete `.planning/phases/26-event-clustering-spike/26-golden-set-draft.json` (or leave it — it is outside `pipeline/tests/fixtures/` so it cannot be confused with the final fixture, but the committed final artifact must be the `clustering_golden_set.json` file, not the draft).
    Add `test_golden_set_shape` to `pipeline/tests/test_clustering.py`: define `FIXTURES_DIR` locally at module level (per the `<interfaces>` note — do not import conftest's); assert total pair count is between 60 and 100 inclusive (pairs with `excluded` set do not count toward this range); assert every non-excluded pair has non-null `label` and a `cached_verdict` with `source == "model"`; assert the fixture contains at least one pair from each of the two mandatory ID lists in the `<interfaces>` block (i.e., the mandatory buckets are actually represented); assert at least one pair exists in EACH of the three CLUS-01 adversarial categories (`category in {"same_date_diff_crime", "multi_outlet_positive", "typo_homophone_negative"}` each represented at least once, in addition to any `"sampled"` pairs).
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_clustering.py::test_golden_set_shape -x</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/tests/fixtures/clustering_golden_set.json` exists, is valid JSON, and `len(data["pairs"])` (excluding disputed-excluded pairs) is between 60 and 100.
    - Every non-excluded pair's `cached_verdict` is a dict with keys `same_event`, `confidence`, `facts`, `rationale`, `source`, and `source == "model"` for every one (no fail-safe verdict ever persisted as `cached_verdict`).
    - `.planning/phases/26-event-clustering-spike/26-CALL-LOG.md` exists, is append-only (multiple rows if resumed), and its latest cumulative total is strictly less than 2,000; it includes a `total_possible_in_bucket_pairs` figure.
    - `pipeline/tests/fixtures/clustering_golden_set.json`'s `meta` block contains `model`, `system_prompt_sha256`, `max_tokens`, `temperature`, `mergeable_rule`, `selected_buckets`, `total_pairs`.
    - `test_golden_set_shape` asserts representation of all three CLUS-01 adversarial categories.
    - No file under `pipeline/tests/fixtures/clustering_golden_set_draft.json` exists anywhere (the draft lives only under the phase directory, per Task 1).
  </acceptance_criteria>
  <done>Final golden-set fixture committed with real, source=="model" live-called verdicts for 60-100 hand-labeled pairs, including both mandatory hard buckets (revised labeling) and the typo/homophone negative sub-set; call budget logged cumulatively and confirmed under the phase cap; fill loop proven resumable/idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `build_golden_set.py` -> `data/incidents/current.json` | Script reads production incident data; must never write to it |
| `build_golden_set.py`/labeling loop -> OpenRouter | Live API calls consume the phase's shared, budget-capped LLM allowance |
| First-pass labeler -> blind second-pass agent | The second pass must never see the first pass's label or the fixture's `cached_verdict`, or its independence is void |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-05 | Tampering | `pipeline/scripts/build_golden_set.py` accidentally calling a write path | mitigate | Script never imports `merge_and_write`/`atomic_write_json`; acceptance criteria explicitly grep for their absence; all output confined to `pipeline/tests/fixtures/` (final fixture only) and the phase directory (draft) |
| T-26-06 | Denial of Service (cost) | Live-call loop over 60-100 pairs, potentially re-run multiple times | mitigate | Cumulative, append-only `26-CALL-LOG.md` ledger read at startup; hard refuse-to-start guard before 2,000; idempotent skip of already-filled pairs makes re-runs cheap; expected actual usage (60-100 calls) is two orders of magnitude under budget |
| T-26-07 | Information Disclosure | `26-CALL-LOG.md` / fixture JSON | accept | Neither file contains `OPENROUTER_API_KEY` or any secret material — only pair IDs, labels, and model verdict JSON (already public-domain news headlines); `grep -riE "sk-or-|OPENROUTER_API_KEY\s*=" pipeline/ .planning/phases/26-event-clustering-spike/` stays clean per 26-01's phase-wide guard |
| T-26-13 | Repudiation | Fail-safe verdict laundered into `cached_verdict` as if real | mitigate | Fill loop structurally refuses to persist any verdict with `source != "model"`; preflight probe call aborts the whole task before any pair is processed if the API is unreachable — the vacuous-precision failure mode (PM-03) cannot occur downstream of this plan |
| T-26-14 | Repudiation | Ground-truth label circularity (a false merge laundered into a TP) | mitigate | Tongoy/2101 outlier pairs default to `no_merge` absent affirmative source proof; every `merge` label requires an independent blind second-pass agent confirmation with no access to the first pass's label or any cached verdict |
</threat_model>

<verification>
`python -m pytest pipeline/tests/test_clustering.py -q` (full file, including the new `test_golden_set_shape`) plus a manual `git status` / `git diff --stat -- data/` check confirming zero changes under `data/`.
</verification>

<success_criteria>
- `pipeline/tests/fixtures/clustering_golden_set.json` committed with 60-100 labeled pairs, both mandatory hard buckets present with the revised (non-circular) sub-grouping, the typo/homophone negative sub-set present, every pair carrying a real `source=="model"` `cached_verdict`.
- Every `merge` label independently confirmed by a blind second pass.
- Call budget logged cumulatively (across resumed runs) and under 2,000.
- `data/` directory byte-unchanged (read-only compliance verified).
</success_criteria>

<output>
Create `.planning/phases/26-event-clustering-spike/26-02-SUMMARY.md` when done
</output>
</output>
