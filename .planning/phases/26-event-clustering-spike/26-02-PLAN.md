---
phase: 26-event-clustering-spike
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - pipeline/scripts/build_golden_set.py
  - pipeline/tests/fixtures/clustering_golden_set.json
  - pipeline/tests/test_clustering.py
  - .planning/phases/26-event-clustering-spike/26-CALL-LOG.md
autonomous: true
requirements: [CLUS-01]
user_setup: []

must_haves:
  truths:
    - "The golden set contains between 60 and 100 hand-labeled pairs"
    - "The golden set includes the comuna 2101 / 2026-07-01 bucket (9 articles, 3 distinct events) with correct merge/no_merge sub-groupings"
    - "The golden set includes the comuna 4102 / 2026-07-01 Tongoy bucket (8 articles, one probable event with noisy sentence-length figures) labeled as a hard positive"
    - "Every labeled pair has a cached_verdict populated from a real, live OpenRouter call — not synthesized"
    - "Total live LLM calls made while building the golden set are logged and stay well under the 2,000-call phase budget"
  artifacts:
    - path: "pipeline/tests/fixtures/clustering_golden_set.json"
      provides: "60-100 hand-labeled pairs, each with incident_a_id, incident_b_id, label, note, cached_verdict"
      min_lines: 200
    - path: "pipeline/scripts/build_golden_set.py"
      provides: "sampling + labeling-draft + live-verdict-population CLI used to produce the fixture"
      min_lines: 60
    - path: ".planning/phases/26-event-clustering-spike/26-CALL-LOG.md"
      provides: "logged OpenRouter call count for this plan's live run"
  key_links:
    - from: "pipeline/scripts/build_golden_set.py"
      to: "pipeline/news/clustering.py"
      via: "prefilter_candidates() / adjudicate_pair()"
      pattern: "from pipeline\\.news\\.clustering import"
    - from: "pipeline/tests/fixtures/clustering_golden_set.json"
      to: "data/incidents/current.json"
      via: "incident_a_id / incident_b_id trace back to real incident IDs"
      pattern: "incident_._id"
---

<objective>
Build the CLUS-01 golden set: 60-100 hand-labeled article pairs drawn from `data/incidents/current.json`, hard-coding the two mandatory adversarial buckets (comuna 2101 Antofagasta, comuna 4102 Tongoy — both dated 2026-07-01) plus supplemental buckets sampled from the 257 same-`(cut,date)` multi-article buckets confirmed present in `26-00-SPIKE-PING.md`. Each pair is stamped with a real, live-called `cached_verdict` from `pipeline.news.clustering.adjudicate_pair()` so `test_clustering.py` can later run fully offline against real model output.

Purpose: Produce the evaluation fixture the GO/NO-GO precision computation (Wave 3) depends on — this is the one wave in the phase that legitimately makes live OpenRouter calls, and it must do so within a logged, budget-aware run.
Output: `pipeline/tests/fixtures/clustering_golden_set.json` (final, committed fixture) + `pipeline/scripts/build_golden_set.py` (reusable sampling/build CLI) + a call-count log for the phase's cumulative LLM budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.1-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/26-event-clustering-spike/26-RESEARCH.md
@.planning/phases/26-event-clustering-spike/26-PATTERNS.md
@.planning/phases/26-event-clustering-spike/26-00-SPIKE-PING.md
@pipeline/news/clustering.py
@pipeline/news/store.py
@pipeline/tests/conftest.py
@data/incidents/current.json
</context>

<interfaces>
<!-- Functions from 26-01-PLAN.md's clustering.py that this plan consumes directly. -->

```python
# pipeline/news/clustering.py (built in 26-01)
def prefilter_candidates(bucket: list[dict], threshold: float = 45.0) -> list[tuple[dict, dict]]: ...
def adjudicate_pair(incident_a: dict, incident_b: dict) -> ClusterVerdict: ...  # ClusterVerdict: same_event, confidence, facts, rationale
```

```python
# pipeline/news/store.py:88-99 — read-only JSON loading helper to reuse for current.json/archive
def _load_incidents_list(path: pathlib.Path) -> list[dict]: ...  # handles missing file, malformed JSON, {"incidents": [...]} wrapper
```

Confirmed real data (verified 2026-07-29 against `data/incidents/current.json`, do not re-derive — use directly):

**Comuna 2101, 2026-07-01 — 9 articles, 3 distinct events (hard adversarial negative bucket):**
- Event A "copamiento preventivo" (1 article, no pair partner in this bucket -> only appears as a no_merge partner against B/C members): `bfe084c29a65e701`
- Event B "condena homicidio pareja, 12 años" (merge group, 3 articles): `0b69f11f785bc0c5`, `f8af960bd49a010d`, `717c7f1c77418734`
- Event C "operativo vehículos robados" (merge group, 5 articles): `0f1e3e9cdc5dd364`, `dc0f9cdcce2ded3b`, `041424e5985d8607`, `16439f8dc78173a6`, `b01de8947aa555f6`

**Comuna 4102, 2026-07-01 (Tongoy) — 8 articles, probable single event, noisy sentence figures (hard positive bucket):**
All 8 describe the same shooting/homicide-attempt event (man shot mother, baby, and dog in Tongoy) with inconsistent reported sentence lengths across outlets ("8 años y medio" vs "19 años" vs "12 años" variants — treat this as reporting noise, not evidence of distinct events): `9b1e83ae50fd427e`, `6eed24180c36dbec`, `282067156cf95860`, `60a9e3a03c773ec4`, `585fb5b5b019e25e`, `e499c44be683263c`, `59dca37a17b66fa6`, `c3ec78c8f3e3be83`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sample supplemental buckets + generate draft candidate pairs</name>
  <files>pipeline/scripts/build_golden_set.py</files>
  <read_first>
    pipeline/news/store.py (lines 88-99, `_load_incidents_list`)
    pipeline/news/clustering.py (from 26-01: `prefilter_candidates` signature)
    pipeline/news/dedup.py (lines 85-104, `(cut, date)` bucketing structure to mirror for grouping current.json)
  </read_first>
  <action>
    Create `pipeline/scripts/build_golden_set.py` as a read-only CLI script (never writes to `data/`). It must:
    1. Load `data/incidents/current.json` via a local reimplementation or import of `store._load_incidents_list`-equivalent logic (read-only; do not call `merge_and_write`).
    2. Group incidents into `(cut, date)` buckets (mirroring `dedup.py:85-104`'s structure).
    3. Exclude the two mandatory buckets `("2101","2026-07-01")` and `("4102","2026-07-01")` from sampling (they are hard-coded separately, per the `<interfaces>` block above).
    4. Deterministically select 15-20 additional buckets with between 2 and 5 articles each (sort remaining multi-article buckets by `(cut, date)` string order and take a fixed-stride sample — e.g. every Nth bucket — so the selection is reproducible across runs, not random).
    5. For every selected bucket (including the two mandatory ones), call `prefilter_candidates(bucket)` from `pipeline.news.clustering` to produce the candidate pair list — this is the SAME pre-filter that will run in production, so the golden set reflects real candidate-generation behavior, not an idealized one (26-RESEARCH.md Pitfall 2: never hand-pick pairs to make the eventual precision number look good).
    6. Write a draft JSON to `pipeline/tests/fixtures/clustering_golden_set_draft.json` with one entry per candidate pair: `pair_id` (`f"{cut}-{date}-{a_id[:6]}-{b_id[:6]}"`), `incident_a_id`, `incident_b_id`, `title_a`, `title_b` (for human/Fable review), `label: null` (to be filled in Task 2), no `cached_verdict` yet.
    7. Print a summary to stdout: total buckets sampled, total candidate pairs generated, expected range check against 60-100 (log a warning, not a failure, if outside range — Task 2 will add/trim by adjusting bucket count).
  </action>
  <verify>
    <automated>python pipeline/scripts/build_golden_set.py --dry-run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The script never imports or calls `merge_and_write`, `atomic_write_json`, or anything that writes under `data/`.
    - `pipeline/tests/fixtures/clustering_golden_set_draft.json` is written under `pipeline/tests/fixtures/` only.
    - The draft file's candidate-pair count (summed across mandatory + sampled buckets) is reported in stdout and falls in a range that Task 2 can trim/pad into the final 60-100.
  </acceptance_criteria>
  <done>Draft candidate-pair fixture generated deterministically from real current.json data using the actual prefilter_candidates() function, ready for ground-truth labeling.</done>
</task>

<task type="auto">
  <name>Task 2: Label ground truth, run live adjudication, finalize fixture</name>
  <files>pipeline/tests/fixtures/clustering_golden_set.json, pipeline/tests/test_clustering.py, .planning/phases/26-event-clustering-spike/26-CALL-LOG.md</files>
  <read_first>
    pipeline/tests/fixtures/clustering_golden_set_draft.json (produced by Task 1)
    pipeline/news/clustering.py (`adjudicate_pair`, from 26-01)
    pipeline/tests/conftest.py (lines 9, 12-21 — `FIXTURES_DIR` convention)
  </read_first>
  <action>
    For the two mandatory buckets, assign `label` using the exact groupings given in the `<interfaces>` block above: any pair with both members inside Event B = `merge`; any pair with both members inside Event C = `merge`; any pair spanning Event A/B, A/C, or B/C = `no_merge`; every pair within the Tongoy 4102 bucket = `merge` (single event, noisy details).
    For every sampled supplemental pair in the draft, read `title_a`/`title_b` and assign `label: "merge"` only when both headlines plainly describe the same real-world incident (same crime type, same approximate circumstances) — otherwise `label: "no_merge"`. Trim or re-run Task 1's sampling stride if the total labeled-pair count falls outside 60-100 (adjust bucket count, not individual labels, to hit the range — do not cherry-pick labels to hit a target count, per 26-RESEARCH.md Pitfall 2).
    Remove `title_a`/`title_b` from the pairs that carry no labeling ambiguity if desired, but keep `note` (a one-line human-readable justification) for every pair, especially the two mandatory buckets.
    Write a `pipeline/scripts/run_golden_set_calls.py`-style loop (may live inline in `build_golden_set.py` behind a `--fill-verdicts` flag, or as an ad-hoc one-time invocation — either is acceptable) that calls `adjudicate_pair(incident_a, incident_b)` from `pipeline.news.clustering` exactly once per labeled pair, using the real incidents looked up by ID from `current.json`, and writes the returned `ClusterVerdict` (as a dict) into that pair's `cached_verdict` field.
    Maintain a running call counter during this loop; after completion, write `.planning/phases/26-event-clustering-spike/26-CALL-LOG.md` recording: total pairs, total live calls made, timestamp, model used (`ibm-granite/granite-4.1-8b`), and confirmation the count is far under the 2,000-call phase budget. If mid-run the counter would exceed 2,000, STOP immediately and record a partial/NO-GO-by-cost note in the same file per the hard budget rule in `.planning/v2.1-AUTONOMOUS-DIRECTIVE.md`.
    Write the final `pipeline/tests/fixtures/clustering_golden_set.json` (delete/rename over the `_draft` file — do not leave the draft committed alongside the final fixture) matching the format from `26-RESEARCH.md`'s "Golden-Set Construction Mechanics" section: top-level `{"pairs": [...]}`, each pair with `pair_id`, `incident_a_id`, `incident_b_id`, `label`, `note`, `cached_verdict`.
    Add `test_golden_set_shape` to `pipeline/tests/test_clustering.py`: asserts total pair count is between 60 and 100 inclusive; asserts every pair has non-null `label` and non-null `cached_verdict`; asserts the fixture contains at least one pair with both `incident_a_id`/`incident_b_id` drawn from each of the two mandatory ID lists in the `<interfaces>` block (i.e., the mandatory buckets are actually represented, not accidentally dropped).
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_clustering.py::test_golden_set_shape -x</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/tests/fixtures/clustering_golden_set.json` exists, is valid JSON, and `len(data["pairs"])` is between 60 and 100.
    - Every pair's `cached_verdict` is a dict with keys `same_event`, `confidence`, `facts`, `rationale` (i.e., a real `ClusterVerdict.model_dump()`, not a placeholder).
    - `.planning/phases/26-event-clustering-spike/26-CALL-LOG.md` exists and states a call count strictly less than 2,000.
    - No file under `pipeline/tests/fixtures/clustering_golden_set_draft.json` remains committed once the final fixture is written (draft is cleaned up or gitignored).
  </acceptance_criteria>
  <done>Final golden-set fixture committed with real live-called verdicts for 60-100 hand-labeled pairs, including both mandatory hard buckets; call budget logged and confirmed under the phase cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `build_golden_set.py` -> `data/incidents/current.json` | Script reads production incident data; must never write to it |
| `build_golden_set.py`/labeling loop -> OpenRouter | Live API calls consume the phase's shared, budget-capped LLM allowance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-05 | Tampering | `pipeline/scripts/build_golden_set.py` accidentally calling a write path | mitigate | Script never imports `merge_and_write`/`atomic_write_json`; acceptance criteria explicitly grep for their absence; all output confined to `pipeline/tests/fixtures/` and the phase directory |
| T-26-06 | Denial of Service (cost) | Live-call loop over 60-100 pairs | mitigate | Running call counter with a hard stop before 2,000; call count logged to `26-CALL-LOG.md` for audit; expected actual usage (60-100 calls) is two orders of magnitude under budget |
| T-26-07 | Information Disclosure | `26-CALL-LOG.md` / fixture JSON | accept | Neither file contains `OPENROUTER_API_KEY` or any secret material — only pair IDs, labels, and model verdict JSON (already public-domain news headlines) |
</threat_model>

<verification>
`python -m pytest pipeline/tests/test_clustering.py -q` (full file, including the new `test_golden_set_shape`) plus a manual `git status` / `git diff --stat -- data/` check confirming zero changes under `data/`.
</verification>

<success_criteria>
- `pipeline/tests/fixtures/clustering_golden_set.json` committed with 60-100 labeled pairs, both mandatory hard buckets present and correctly sub-grouped, every pair carrying a real `cached_verdict`.
- Call budget logged and under 2,000.
- `data/` directory byte-unchanged (read-only compliance verified).
</success_criteria>

<output>
Create `.planning/phases/26-event-clustering-spike/26-02-SUMMARY.md` when done
</output>
