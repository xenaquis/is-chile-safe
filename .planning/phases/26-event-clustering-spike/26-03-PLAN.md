---
phase: 26-event-clustering-spike
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - pipeline/scripts/run_clustering_spike.py
  - pipeline/tests/test_clustering.py
  - .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md
autonomous: true
requirements: [CLUS-07, CLUS-08]

must_haves:
  truths:
    - "Precision and recall are computed strictly at the pair level (TP/FP/FN over golden-set pairs), never at the cluster level"
    - "The full pairwise decision matrix (every candidate pair's verdict, not just merged ones) is present in the spike's logged output"
    - "The precision check runs as a pytest regression using only the cached_verdict fixture data, with zero live network calls"
    - "The spike report states measured precision, recall, and call cost, and leaves the GO/NO-GO line for the Fable orchestrator to confirm against the locked 100%/zero-FP gate"
  artifacts:
    - path: "pipeline/scripts/run_clustering_spike.py"
      provides: "loads golden set, computes clusters via assemble_clusters, computes pair-level TP/FP/FN/precision/recall, writes the pairwise decision matrix"
      min_lines: 80
    - path: ".planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md"
      provides: "measured precision/recall/cost, full pairwise matrix reference, GO/NO-GO line pending Fable confirmation"
      min_lines: 40
  key_links:
    - from: "pipeline/scripts/run_clustering_spike.py"
      to: "pipeline/tests/fixtures/clustering_golden_set.json"
      via: "reads cached_verdict per pair, does not call adjudicate_pair live"
      pattern: "cached_verdict"
    - from: "pipeline/tests/test_clustering.py"
      to: "pipeline/tests/fixtures/clustering_golden_set.json"
      via: "test_golden_set_precision_100pct reads the fixture directly, offline"
      pattern: "test_golden_set_precision_100pct"
---

<objective>
Compute the GO/NO-GO evidence: run the full clustering pipeline (mergeable-state rule -> union-find assembly -> oversized-cluster flag) against the golden-set fixture's `cached_verdict` data, produce pair-level precision/recall/cost metrics against the locked 100%-precision/zero-false-merge gate, log the full pairwise decision matrix, and encode the precision check as an offline pytest regression (CLUS-08) so a future model swap cannot silently regress clustering quality.

Purpose: Turn the golden set built in Wave 2 into the measured evidence Wave 4 needs to decide whether `IncidentRecord` gains `cluster_id`/`is_primary`.
Output: `pipeline/scripts/run_clustering_spike.py` (report-generating CLI) + `pipeline/tests/test_clustering.py::test_golden_set_precision_100pct` (offline regression) + `26-SPIKE-REPORT.md` (evidence pack, GO/NO-GO line left for the orchestrator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.1-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/26-event-clustering-spike/26-RESEARCH.md
@.planning/phases/26-event-clustering-spike/26-VALIDATION.md
@pipeline/news/clustering.py
@pipeline/tests/fixtures/clustering_golden_set.json
</context>

<interfaces>
```python
# pipeline/news/clustering.py (from 26-01) — consumed here, no live calls made in this plan
def assemble_clusters(incident_ids: list[str], merge_edges: list[tuple[str, str]], max_size: int = 4) -> tuple[dict, dict]: ...
def cluster_id(member_ids: list[str]) -> str: ...
class ClusterVerdict(BaseModel):  # same_event: bool, confidence: Literal["high","low"], facts: dict, rationale: str
```

Golden-set fixture shape (from 26-02): `{"pairs": [{"pair_id", "incident_a_id", "incident_b_id", "label" ("merge"|"no_merge"), "note", "cached_verdict": {"same_event", "confidence", "facts", "rationale"}}]}`.

Locked mergeable-state rule (26-RESEARCH.md Anti-Patterns, must be applied here — do not accept `confidence: "low"` merges): an edge is drawn between two incidents ONLY when `cached_verdict.same_event is True AND cached_verdict.confidence == "high"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: run_clustering_spike.py — pair-level metrics + pairwise decision matrix + report</name>
  <files>pipeline/scripts/run_clustering_spike.py, .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md</files>
  <read_first>
    pipeline/tests/fixtures/clustering_golden_set.json (final fixture from 26-02)
    pipeline/news/clustering.py (assemble_clusters, cluster_id, ClusterVerdict — from 26-01)
    .planning/phases/26-event-clustering-spike/26-RESEARCH.md (section "Precision/Recall Metric Definition" — exact TP/FP/FN formula, pair-level only)
  </read_first>
  <action>
    Create `pipeline/scripts/run_clustering_spike.py` as a read-only CLI. It must:
    1. Load `pipeline/tests/fixtures/clustering_golden_set.json`.
    2. For each pair, apply the mergeable-state rule from `<interfaces>` (`same_event is True AND confidence == "high"`) to its `cached_verdict` to determine whether the pipeline would draw a merge edge for that pair.
    3. Compute, at the PAIR level (never cluster level, per 26-RESEARCH.md Pitfall 1): TP = golden `label=="merge"` AND pipeline drew an edge; FP = golden `label=="no_merge"` AND pipeline drew an edge (**this must be exactly 0 for GO**); FN = golden `label=="merge"` AND pipeline did NOT draw an edge. Precision = TP/(TP+FP); Recall = TP/(TP+FN).
    4. Build the full incident-ID universe from all `incident_a_id`/`incident_b_id` values across the fixture, and the merge-edge list from step 2, then call `assemble_clusters()` to produce `(clusters, flagged)` — assert no cluster with a false-merge edge silently escapes review by cross-checking `flagged` against any FP-containing component.
    5. Compute `cluster_id()` for every resulting cluster (both `clusters` and `flagged`), demonstrating the deterministic-ID path end to end on real golden-set data.
    6. Write the FULL pairwise decision matrix (every pair: `pair_id`, golden `label`, `cached_verdict.same_event`, `cached_verdict.confidence`, whether an edge was drawn, TP/FP/FN/TN classification) to `.planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md` as a Markdown table (CLUS-06's "full pairwise decision matrix per cluster is present in the spike's logged output" requirement).
    7. Append to the same report: precision, recall, TP/FP/FN counts, total pairs, the reduction-ratio evidence from the pre-filter (total possible pairs in sampled buckets vs. pairs actually adjudicated — read from `26-CALL-LOG.md`'s pair count), actual OpenRouter call count (from `26-CALL-LOG.md`), and cluster-size-cap compliance (list any flagged >4-member components).
    8. End the report with an explicit line: `**GO/NO-GO verdict: [PENDING — Fable orchestrator confirms against locked gate: 100% precision, 0 false merges]**` plus the raw numbers needed to confirm it (precision value, FP count) so confirmation is a one-line read, not a recomputation.
  </action>
  <verify>
    <automated>python pipeline/scripts/run_clustering_spike.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `26-SPIKE-REPORT.md` contains a Markdown table with one row per golden-set pair (60-100 rows), not a per-cluster summary only.
    - The report states precision as `TP / (TP + FP)` with the literal computed value, and separately states the FP count as an integer (not just "0%" or a percentage rounding that could hide FP=1 in a large N).
    - No cluster in `clusters` (the non-flagged output) contains more than 4 members — cross-checked against `flagged`.
    - The report's final line contains the literal string `GO/NO-GO verdict: [PENDING` (case-sensitive) so downstream tooling/orchestrator review can grep for it.
  </acceptance_criteria>
  <done>run_clustering_spike.py produces a complete, pair-level-precise evidence report with the full decision matrix, ready for Fable confirmation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline pytest regression for golden-set precision (CLUS-08)</name>
  <files>pipeline/tests/test_clustering.py</files>
  <read_first>
    pipeline/scripts/run_clustering_spike.py (Task 1 of this plan — reuse its metric-computation logic, do not duplicate it inline in the test)
    pipeline/tests/fixtures/clustering_golden_set.json
  </read_first>
  <behavior>
    - Test 1 (`test_golden_set_precision_100pct`): loads the golden-set fixture, applies the same mergeable-state rule and pair-level TP/FP/FN computation as `run_clustering_spike.py` (import/reuse its function rather than reimplementing), and asserts `FP == 0` and `precision == 1.0` given the CURRENT cached verdicts. If this assertion fails, the test failure message must print the FP count and the offending `pair_id`s so a future model-swap regression is immediately actionable.
    - Test 2 (whole-file network isolation): the entire `pipeline/tests/test_clustering.py` file makes zero live network calls — verify by asserting (or by structural guarantee, since `test_golden_set_precision_100pct` only reads the fixture's `cached_verdict` field and never calls `adjudicate_pair`) that this test does not patch or invoke `client.chat.completions.create` at all.
  </behavior>
  <action>
    Refactor `pipeline/scripts/run_clustering_spike.py`'s metric computation (mergeable-state rule + TP/FP/FN/precision/recall) into an importable function, e.g. `compute_pairwise_metrics(pairs: list[dict]) -> dict` (module-level, no CLI/argparse coupling), if it is not already structured that way from Task 1 — the CLI's `main()` should call this function, and the test should import and call the same function directly. This avoids duplicating the precision formula between the report generator and the regression test.
    Add `test_golden_set_precision_100pct` to `pipeline/tests/test_clustering.py`: `from pipeline.scripts.run_clustering_spike import compute_pairwise_metrics`; load the fixture via `FIXTURES_DIR / "clustering_golden_set.json"` (conftest.py convention); call `compute_pairwise_metrics(fixture["pairs"])`; assert `result["fp"] == 0` and `result["precision"] == 1.0`, with an assertion message listing FP pair_ids if it fails.
    Confirm (by code inspection, not a runtime assertion) that this test performs no `import openai` usage and no `patch("pipeline.news.clustering.client")` — it is a pure offline computation over already-cached JSON data, structurally incapable of a live call.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_clustering.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_golden_set_precision_100pct` imports `compute_pairwise_metrics` from `pipeline.scripts.run_clustering_spike` rather than reimplementing the formula.
    - Running `pytest pipeline/tests/test_clustering.py -q` with network disabled (or `OPENROUTER_API_KEY` unset) still passes — this test path never constructs an outbound request.
    - If the golden-set fixture currently yields FP > 0, this task's acceptance is NOT met by silently loosening the assertion — the failing test result itself is the correct, honest signal for Wave 4's GO/NO-GO decision; do not modify the assertion to force a pass.
  </acceptance_criteria>
  <done>test_golden_set_precision_100pct exists, is fully offline, and its pass/fail state is the authoritative CLUS-08 regression signal for future model swaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `run_clustering_spike.py` / test -> `clustering_golden_set.json` | Both consumers trust the fixture's `cached_verdict` field as ground truth for offline computation — no live LLM call is made from either |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-08 | Repudiation | GO/NO-GO computation | mitigate | Full pairwise decision matrix logged in `26-SPIKE-REPORT.md` (every pair, not just merged ones) — any future dispute about the precision number is auditable against the raw table, not just a summary statistic |
| T-26-09 | Tampering | Precision formula duplicated between report script and test | mitigate | `compute_pairwise_metrics` implemented once, imported by both the CLI report and the pytest regression — a single source of truth prevents the two from silently diverging |
</threat_model>

<verification>
`python -m pytest pipeline/tests/test_clustering.py -q` (offline, full file) and `python -m pytest pipeline/tests -q` (full ~301+ suite green). Manually confirm `26-SPIKE-REPORT.md`'s pairwise table row count matches the golden-set fixture's pair count.
</verification>

<success_criteria>
- Pair-level precision/recall/cost computed and reported with the full decision matrix.
- `test_golden_set_precision_100pct` is offline, reuses the same metric function as the report, and its true/false result is the honest CLUS-08 signal (not adjusted to force a pass).
- `26-SPIKE-REPORT.md` ends with a clearly greppable pending GO/NO-GO line plus the raw precision/FP numbers needed to confirm it.
</success_criteria>

<output>
Create `.planning/phases/26-event-clustering-spike/26-03-SUMMARY.md` when done
</output>
