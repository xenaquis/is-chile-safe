---
phase: 26-event-clustering-spike
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - pipeline/scripts/run_clustering_spike.py
  - pipeline/tests/test_clustering.py
  - pipeline/tests/conftest.py
  - .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md
autonomous: true
requirements: [CLUS-06, CLUS-07, CLUS-08]

must_haves:
  truths:
    - "Precision and recall are computed strictly at the pair level (TP/FP/FN over golden-set pairs), never at the cluster level"
    - "The full pairwise decision matrix (every candidate pair's verdict, not just merged ones) is present in the spike's logged output"
    - "A run with zero successful model calls (tp+fp==0) can never read as precision 1.0 — precision is None in that case and the gate test cannot pass"
    - "The precision-gate check runs as a pytest regression using only the cached_verdict fixture data, with zero live network calls, AND a separate always-green baseline-regression test exists so a NO-GO does not leave the suite permanently red"
    - "A cached-verdict fixture becomes stale and loudly fails its own regression test if the clustering model/prompt/config changes"
    - "The spike report states measured precision, recall, call cost, confidence distribution, and the frozen pre-filter's honest reduction ratio, and leaves the GO/NO-GO line for the Fable orchestrator to confirm against the locked 100%/zero-FP gate"
  artifacts:
    - path: "pipeline/scripts/run_clustering_spike.py"
      provides: "loads golden set, computes clusters via assemble_clusters, computes pair-level TP/FP/FN/TN/precision/recall/n_failsafe via compute_pairwise_metrics(), writes the pairwise decision matrix"
      min_lines: 90
    - path: ".planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md"
      provides: "measured precision/recall/cost/confidence-distribution, full pairwise matrix reference, GO/NO-GO line pending Fable confirmation"
      min_lines: 50
  key_links:
    - from: "pipeline/scripts/run_clustering_spike.py"
      to: "pipeline/tests/fixtures/clustering_golden_set.json"
      via: "reads cached_verdict per pair, does not call adjudicate_pair live"
      pattern: "cached_verdict"
    - from: "pipeline/tests/test_clustering.py"
      to: "pipeline/tests/fixtures/clustering_golden_set.json"
      via: "test_golden_set_meets_go_gate reads the fixture directly, offline"
      pattern: "test_golden_set_meets_go_gate"
---

<objective>
Compute the GO/NO-GO evidence: run the full clustering pipeline (mergeable-state rule -> union-find assembly -> oversized-cluster flag) against the golden-set fixture's `cached_verdict` data, produce pair-level precision/recall/cost metrics against the locked 100%-precision/zero-false-merge gate with an honest zero-denominator guard, log the full pairwise decision matrix, and encode the precision check as TWO offline pytest regressions (an always-green baseline-drift sentinel and the GO/NO-GO gate itself) plus a model/prompt-staleness sentinel — so a future model swap or fixture drift cannot silently regress clustering quality, and a NO-GO does not leave the suite permanently red.

Purpose: Turn the golden set built in Wave 2 into the measured evidence Wave 4 needs to decide whether `IncidentRecord` gains `cluster_id`/`is_primary`.
Output: `pipeline/scripts/run_clustering_spike.py` (report-generating CLI, exposes `compute_pairwise_metrics`) + `pipeline/tests/test_clustering.py::{test_golden_set_metrics_match_recorded_baseline, test_golden_set_meets_go_gate, test_cached_verdicts_match_current_model_config, test_golden_set_live_reeval}` + `pipeline/tests/conftest.py` (registers the `live_llm` marker) + `26-SPIKE-REPORT.md` (evidence pack, GO/NO-GO line left for the orchestrator).

**Threshold freeze reminder:** `_PREFILTER_THRESHOLD = 45.0` remains frozen (see `26-01-PLAN.md`/`26-02-PLAN.md`) — this plan reports on it, it never adjusts it, even though the honest reduction ratio on real data is expected to be near 0%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.1-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/26-event-clustering-spike/26-RESEARCH.md
@.planning/phases/26-event-clustering-spike/26-VALIDATION.md
@pipeline/news/clustering.py
@pipeline/tests/fixtures/clustering_golden_set.json
@pipeline/tests/conftest.py
</context>

<interfaces>
```python
# pipeline/news/clustering.py (from 26-01) — consumed here, no live calls made by default in this plan
def assemble_clusters(incident_ids: list[str], merge_edges: list[tuple[str, str]], max_size: int = 4) -> tuple[dict, dict]: ...
def cluster_id(member_ids: list[str]) -> str: ...
def adjudicate_pair(incident_a: dict, incident_b: dict) -> ClusterVerdict: ...  # only used by the opt-in live_llm test
class ClusterVerdict(BaseModel):  # same_event: bool, confidence: Literal["high","low"], facts: dict, rationale: str, source: Literal["model","failsafe_api","failsafe_parse"]
MODEL: str
SYSTEM_PROMPT: str
```

Golden-set fixture shape (from 26-02): `{"meta": {"model", "base_url", "system_prompt_sha256", "max_tokens", "temperature", "mergeable_rule", "generated", "calls", "selected_buckets", "total_pairs"}, "pairs": [{"pair_id", "incident_a_id", "incident_b_id", "title_a", "title_b", "label" ("merge"|"no_merge"), "category", "label_basis", "label_confidence", "second_pass_confirmed", "excluded", "note", "prefilter_score", "proposed", "bypasses_bucket", "cached_verdict": {"same_event", "confidence", "facts", "rationale", "source"}}]}`.

Locked mergeable-state rule (26-RESEARCH.md Anti-Patterns, must be applied here — do not accept `confidence: "low"` merges): an edge is drawn between two incidents ONLY when `cached_verdict.same_event is True AND cached_verdict.confidence == "high"`.

`pipeline/tests/conftest.py` currently registers no pytest markers — `pytest_configure(config)` must be added (or extended if it already exists) to call `config.addinivalue_line("markers", "live_llm: makes real OpenRouter calls, skipped by default")` so `@pytest.mark.live_llm` never emits an "unknown mark" warning.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: run_clustering_spike.py — compute_pairwise_metrics + pairwise decision matrix + report</name>
  <files>pipeline/scripts/run_clustering_spike.py, .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md</files>
  <read_first>
    pipeline/tests/fixtures/clustering_golden_set.json (final fixture from 26-02, including `meta` and `category`/`proposed` fields)
    pipeline/news/clustering.py (assemble_clusters, cluster_id, ClusterVerdict — from 26-01)
    .planning/phases/26-event-clustering-spike/26-CALL-LOG.md (from 26-02 — read `total_possible_in_bucket_pairs` for the reduction-ratio evidence)
    .planning/phases/26-event-clustering-spike/26-RESEARCH.md (section "Precision/Recall Metric Definition" — exact TP/FP/FN formula, pair-level only)
  </read_first>
  <action>
    Create `pipeline/scripts/run_clustering_spike.py` as a read-only CLI, structured so its metric logic lives in an importable, CLI-independent function (Task 2 of this plan reuses it directly — do not write it as CLI-coupled code that a test would have to shell out to). It must:
    1. Load `pipeline/tests/fixtures/clustering_golden_set.json`.
    2. Define `def compute_pairwise_metrics(pairs: list[dict]) -> dict` (module-level, no argparse coupling) that, for every non-excluded pair, applies the mergeable-state rule from `<interfaces>` (`same_event is True AND confidence == "high"`) to its `cached_verdict` to determine whether the pipeline would draw a merge edge for that pair, then computes at the PAIR level (never cluster level, per 26-RESEARCH.md Pitfall 1): `tp` = golden `label=="merge"` AND pipeline drew an edge; `fp` = golden `label=="no_merge"` AND pipeline drew an edge (**this must be exactly 0 for GO**); `fn` = golden `label=="merge"` AND pipeline did NOT draw an edge, split into `fn_llm_rejected` (the pair was adjudicated by the LLM but the mergeable-state rule rejected it) and `fn_prefiltered` (the pair's `proposed == False`, i.e. it never reached the LLM — count these using the `proposed` field recorded in 26-02, not a fresh recomputation); `tn` = golden `label=="no_merge"` AND pipeline did not draw an edge; `n_failsafe` = count of pairs whose `cached_verdict.source != "model"` (should be 0 given 26-02's guarantees — report it anyway as a defense-in-depth check). Return a dict with EXACTLY these keys: `tp, fp, fn, fn_prefiltered, fn_llm_rejected, tn, precision, recall, n_pairs, n_failsafe`. **`precision = tp / (tp + fp)` UNLESS `tp + fp == 0`, in which case `precision = None`** — a run that merges nothing is not a passing run, it is an unmeasured run, and must never read as `1.0`. `recall = tp / (tp + fn)` if `tp + fn > 0` else `None`.
    3. Build the full incident-ID universe from all `incident_a_id`/`incident_b_id` values across the fixture's non-excluded pairs, and the merge-edge list from step 2's mergeable-state rule, then call `assemble_clusters()` to produce `(clusters, flagged)` — assert no cluster with a false-merge edge silently escapes review by cross-checking `flagged` against any FP-containing component.
    4. Compute `cluster_id()` for every resulting cluster (both `clusters` and `flagged`), demonstrating the deterministic-ID path end to end on real golden-set data.
    5. Write the FULL pairwise decision matrix (every non-excluded pair: `pair_id`, `category`, golden `label`, `cached_verdict.same_event`, `cached_verdict.confidence`, whether an edge was drawn, TP/FP/FN/TN classification) to `.planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md` as a Markdown table (CLUS-06's "full pairwise decision matrix per cluster is present in the spike's logged output" requirement). Also add a short "Excluded / disputed pairs" sub-table for any `excluded: "label_disputed"` pairs, reported separately and never folded into precision/recall.
    6. Append to the same report, as separate labeled integers (never only as a rounded percentage, since a percentage can hide `fp==1` in a large N): TP, FP, FN (with the `fn_prefiltered`/`fn_llm_rejected` split), TN, precision (or the literal string `None (zero denominator)` if unmeasured), recall, `n_pairs`, `n_failsafe`. Report negatives in set, negatives actually adjudicated by the LLM, positives adjudicated, and pairs filtered pre-LLM as separate integers, plus the line `negatives_adjudicated / negatives = N/M` directly above the verdict line. Report the honest reduction ratio: `total_possible_in_bucket_pairs` (from `26-CALL-LOG.md`) vs. pairs actually proposed by the pre-filter — state plainly that on the two mandatory buckets the expected reduction is ≈0% (min observed `token_set_ratio` = 48.7 against a frozen threshold of 45.0) and that the real cost bound in this phase is `(cut,date)` bucketing itself, noting the largest observed bucket sizes and their pair counts if any bucket in the sample is large (e.g. a 26-article bucket -> 325 possible pairs, a 16-article bucket -> 120).
    7. Report the confidence distribution across the golden set (`high: N, low: M`) and the count of pairs that reached the fail-safe path (`n_failsafe`); if `low == 0`, state explicitly: "the `confidence == 'high'` condition rejected 0 pairs on this golden set; measured precision rests entirely on `same_event`."
    8. Add the scope sentence: "measured on same-`(cut,date)` buckets only; the ±1-day window and cross-day procedural follow-ups are untested and out of scope for this verdict." Also state that `_PREFILTER_THRESHOLD = 45.0` was pre-registered before any precision measurement and was NOT tuned during this run.
    9. Add the line `merge-labels independently confirmed: N/N (blind second pass)` reading `second_pass_confirmed`/downgrade counts from the fixture.
    10. End the report with an explicit line: `**GO/NO-GO verdict: [PENDING — Fable orchestrator confirms against locked gate: 100% precision, 0 false merges, tp > 0, n_failsafe == 0]**` plus the raw numbers needed to confirm it (precision value or `None`, FP count, TP count, n_failsafe count) so confirmation is a one-line read, not a recomputation.
  </action>
  <verify>
    <automated>python pipeline/scripts/run_clustering_spike.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `26-SPIKE-REPORT.md` contains a Markdown table with one row per non-excluded golden-set pair (60-100 rows), not a per-cluster summary only.
    - The report states precision as the literal computed value or `None (zero denominator)`, and separately states TP and FP as integers.
    - No cluster in `clusters` (the non-flagged output) contains more than 4 members — cross-checked against `flagged`.
    - The report includes the confidence distribution, the `fn_prefiltered`/`fn_llm_rejected` split, the honest reduction-ratio statement, the same-date-only scope sentence, and the `merge-labels independently confirmed: N/N` line.
    - The report's final line contains the literal string `GO/NO-GO verdict: [PENDING` (case-sensitive) so downstream tooling/orchestrator review can grep for it.
  </acceptance_criteria>
  <done>run_clustering_spike.py produces a complete, pair-level-precise evidence report with the full decision matrix and an honest zero-denominator-safe metric function, ready for Fable confirmation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline pytest regressions — baseline sentinel, GO/NO-GO gate, model-staleness guard, opt-in live re-eval (CLUS-08)</name>
  <files>pipeline/tests/test_clustering.py, pipeline/tests/conftest.py</files>
  <read_first>
    pipeline/scripts/run_clustering_spike.py (Task 1 of this plan — reuse `compute_pairwise_metrics`, do not duplicate it inline in the test)
    pipeline/tests/fixtures/clustering_golden_set.json
    pipeline/tests/conftest.py (current marker registration state, if any)
  </read_first>
  <behavior>
    - Test 1 (`test_golden_set_metrics_match_recorded_baseline`): computes `compute_pairwise_metrics(fixture["pairs"])` and asserts the result equals a `baseline` block written into the fixture `meta` by this task (`meta.baseline = {tp, fp, fn, fn_prefiltered, fn_llm_rejected, tn, precision, recall, n_pairs, n_failsafe}`, recorded once when the fixture is finalized). This test is ALWAYS GREEN by construction — it is the true regression sentinel: any future fixture edit or metric-code change that silently shifts the numbers breaks it loudly.
    - Test 2 (`test_golden_set_meets_go_gate`): asserts `fp == 0 and tp > 0 and n_failsafe == 0` using the CURRENT cached verdicts (via `compute_pairwise_metrics`). This is the actual GO/NO-GO signal. On GO it passes; on NO-GO Wave 4 (`26-04-PLAN.md`) adds a `strict=True` xfail marker to this specific test — this plan does NOT add that marker (the marker's content depends on the measured outcome, which is not known until this test is first run).
    - Test 3 (`test_cached_verdicts_match_current_model_config`): imports `MODEL`, `SYSTEM_PROMPT` from `pipeline.news.clustering`, recomputes `hashlib.sha256(SYSTEM_PROMPT.encode()).hexdigest()`, and asserts equality with `fixture["meta"]["model"]` / `fixture["meta"]["system_prompt_sha256"]` / `max_tokens` / `temperature` / `mergeable_rule`. On failure, the assertion message must read: "clustering model/prompt changed — the cached golden-set verdicts are stale; re-run `pipeline/scripts/build_golden_set.py --fill-verdicts` and re-confirm the GO gate before shipping."
    - Test 4 (`test_golden_set_live_reeval`, live, skipped by default): decorated `@pytest.mark.live_llm` and `@pytest.mark.skipif(not os.environ.get("CLUSTERING_LIVE_EVAL"), reason="opt-in live re-evaluation")`; re-adjudicates every fixture pair with a live `adjudicate_pair` call and recomputes metrics, asserting the live result matches `meta.baseline` (or logging a drift warning — pick whichever the test framework in this repo favors; do not let this test run in the default suite).
    - Test 5 (whole-file network isolation, unchanged from prior plan intent): the entire `pipeline/tests/test_clustering.py` file makes zero live network calls by default (Test 4 is the sole exception, gated behind both the marker and the env var).
  </behavior>
  <action>
    Refactor/confirm `pipeline/scripts/run_clustering_spike.py`'s `compute_pairwise_metrics` is importable with no CLI/argparse coupling (Task 1 should already satisfy this — verify, do not re-derive the formula here).
    In `pipeline/tests/conftest.py`, add or extend `def pytest_configure(config): config.addinivalue_line("markers", "live_llm: makes real OpenRouter calls, skipped by default")`.
    Add all four tests from `<behavior>` to `pipeline/tests/test_clustering.py`. `test_golden_set_metrics_match_recorded_baseline` and `test_golden_set_meets_go_gate` both `from pipeline.scripts.run_clustering_spike import compute_pairwise_metrics`; load the fixture via a LOCAL `FIXTURES_DIR` constant defined in this test file (per `26-02-PLAN.md`'s `<interfaces>` note — never import conftest's).
    Before writing the tests, compute and persist `meta.baseline` into `pipeline/tests/fixtures/clustering_golden_set.json` by running `compute_pairwise_metrics` once against the current fixture and writing the result dict into `meta.baseline` — this is what makes Test 1 always-green from that point forward.
    Confirm (by code inspection, not a runtime assertion) that `test_golden_set_metrics_match_recorded_baseline`, `test_golden_set_meets_go_gate`, and `test_cached_verdicts_match_current_model_config` perform no `import openai` usage and no `patch("pipeline.news.clustering.client")` — they are pure offline computations over already-cached JSON data, structurally incapable of a live call. Only `test_golden_set_live_reeval` touches `adjudicate_pair`, and it is gated behind both `@pytest.mark.live_llm` and the `CLUSTERING_LIVE_EVAL` env var.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_clustering.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_golden_set_metrics_match_recorded_baseline` and `test_golden_set_meets_go_gate` both import `compute_pairwise_metrics` from `pipeline.scripts.run_clustering_spike` rather than reimplementing the formula.
    - Running `pytest pipeline/tests/test_clustering.py -q` with network disabled (or `OPENROUTER_API_KEY` unset) still passes for every test except the opt-in `test_golden_set_live_reeval` (which is skipped by default, so the suite is still green).
    - `pytest.ini`/`conftest.py` registers the `live_llm` marker — running the suite produces no "unknown mark" warning.
    - If the golden-set fixture currently yields `fp > 0` (or `tp == 0`), `test_golden_set_meets_go_gate`'s failing result is the correct, honest signal for Wave 4's GO/NO-GO decision — this task must NOT modify that assertion to force a pass; `test_golden_set_metrics_match_recorded_baseline` stays green regardless (it only checks internal consistency against the recorded baseline, not the gate).
  </acceptance_criteria>
  <done>Four regression tests exist: an always-green baseline sentinel, the honest GO/NO-GO gate (never weakened to force a pass), a model/prompt-staleness guard, and an opt-in live re-evaluation gated off by default; all offline tests pass with network disabled; the live_llm marker is registered so no warning appears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `run_clustering_spike.py` / tests -> `clustering_golden_set.json` | Consumers trust the fixture's `cached_verdict` field as ground truth for offline computation — no live LLM call is made from any test except the opt-in `test_golden_set_live_reeval` |
| Wave 3 gate result -> Wave 4 schema decision | The exact `tp/fp/n_failsafe` numbers computed here are the sole basis Wave 4 is permitted to act on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-08 | Repudiation | GO/NO-GO computation | mitigate | Full pairwise decision matrix logged in `26-SPIKE-REPORT.md` (every non-excluded pair, not just merged ones) — any future dispute about the precision number is auditable against the raw table, not just a summary statistic |
| T-26-09 | Tampering | Precision formula duplicated between report script and tests | mitigate | `compute_pairwise_metrics` implemented once in `run_clustering_spike.py`, imported by both the CLI report and both offline pytest regressions — a single source of truth prevents divergence |
| T-26-15 | Repudiation | A red gate test being silently "fixed" instead of investigated | mitigate | `test_golden_set_metrics_match_recorded_baseline` (always green, internal-consistency only) is kept structurally separate from `test_golden_set_meets_go_gate` (the honest, possibly-red signal); the two can never be confused because they assert different things against different sources of truth |
| T-26-16 | Tampering | Stale cached verdicts surviving an undetected model/prompt swap | mitigate | `test_cached_verdicts_match_current_model_config` recomputes the prompt hash and compares against `meta` on every run; failure message tells the reader exactly how to regenerate and re-confirm |
</threat_model>

<verification>
`python -m pytest pipeline/tests/test_clustering.py -q` (offline, full file, `live_llm` tests skipped) and `python -m pytest pipeline/tests -q` (full ~301+ suite green). Manually confirm `26-SPIKE-REPORT.md`'s pairwise table row count matches the golden-set fixture's non-excluded pair count.
</verification>

<success_criteria>
- Pair-level precision/recall/cost/confidence-distribution computed and reported with the full decision matrix and an honest zero-denominator guard.
- `test_golden_set_meets_go_gate` is offline, reuses the same metric function as the report, and its true/false result is the honest CLUS-08 signal (never adjusted to force a pass); `test_golden_set_metrics_match_recorded_baseline` is always green and cannot be confused with it.
- `test_cached_verdicts_match_current_model_config` makes a future model swap loud instead of silent.
- `26-SPIKE-REPORT.md` ends with a clearly greppable pending GO/NO-GO line plus the raw precision/FP/TP/n_failsafe numbers needed to confirm it.
</success_criteria>

<output>
Create `.planning/phases/26-event-clustering-spike/26-03-SUMMARY.md` when done
</output>
</output>
