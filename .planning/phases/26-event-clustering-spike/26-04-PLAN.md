---
phase: 26-event-clustering-spike
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - pipeline/news/schema.py
  - pipeline/tests/test_schema_incidents.py
  - pipeline/tests/test_clustering.py
  - .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md
  - .planning/STATE.md
autonomous: true
requirements: [CLUS-09]

must_haves:
  truths:
    - "The GO/NO-GO verdict actually applied matches the measured tp/fp/n_failsafe from 26-03's compute_pairwise_metrics — never asserted independently of the evidence"
    - "A collection/import/runtime error in the gate test is treated as a STATE.md blocker, never auto-read as NO-GO"
    - "On GO, IncidentRecord validates existing current.json/archive records unchanged (cluster_id defaults to None, is_primary defaults to False)"
    - "On NO-GO, no schema field is added, the gate test is quarantined with a strict xfail (never weakened), and the finding is documented plainly"
    - "The is_primary tie-break rule is explicitly deferred to Phase 28 in the report and in STATE.md, not silently left undocumented"
    - "The full pipeline/tests suite (~301+ tests) exits 0 in either verdict branch — on NO-GO this is achieved via a strict(True) xfail quarantine of the gate test, never by weakening its assertion — and the frontend 14-validator suite stays green"
  artifacts:
    - path: ".planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md"
      provides: "final CONFIRMED GO/NO-GO verdict line, schema-change status, is_primary deferral note, backward-compat consumer sweep"
      contains: "GO/NO-GO verdict:"
  key_links:
    - from: "pipeline/news/schema.py"
      to: "pipeline/tests/test_schema_incidents.py"
      via: "test_incident_record_cluster_fields_optional backward-compat test"
      pattern: "cluster_id"
---

<objective>
Close the phase: read the measured `tp/fp/n_failsafe` evidence produced by `26-03-PLAN.md`'s `compute_pairwise_metrics` (via `test_golden_set_meets_go_gate`), apply the GO/NO-GO decision it dictates (never independently, and never confusing a collection/import error with a NO-GO), conditionally ship `IncidentRecord.cluster_id`/`is_primary` (GO only, backward-compatible), quarantine the gate test with a strict xfail on NO-GO so the suite stays green without weakening the evidence, finalize `26-SPIKE-REPORT.md` with the confirmed verdict, and run the full regression suite to close the phase clean.

Purpose: Turn Wave 3's measured evidence into the phase's committed outcome — either a shipped, backward-compatible schema addition or a documented NO-GO with zero schema churn and an honest, auditable green suite.
Output: `pipeline/news/schema.py` (conditionally modified) + `pipeline/tests/test_schema_incidents.py` (conditionally extended) + `pipeline/tests/test_clustering.py` (conditionally gains a strict xfail marker on NO-GO) + finalized `26-SPIKE-REPORT.md` with a confirmed (not pending) GO/NO-GO line + `.planning/STATE.md` (Phase 28 prerequisite note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.1-AUTONOMOUS-DIRECTIVE.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-event-clustering-spike/26-RESEARCH.md
@.planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md
@pipeline/news/schema.py
@pipeline/tests/test_schema_incidents.py
</context>

<interfaces>
From pipeline/news/schema.py:48-61 (existing IncidentRecord — exact field-addition template):
```python
class IncidentRecord(BaseModel):
    ...
    slug: str | None = None  # resolved commune slug (NEWS-03); optional for back-compat
    # ADD (Phase 26, ON GO ONLY):
    cluster_id: str | None = None
    is_primary: bool = False  # default False everywhere in this phase — Phase 28 owns the real
                                # tie-break rule (earliest date? highest confidence?); no incident
                                # is ever marked True by Phase 26 code.
```

From pipeline/tests/test_schema_incidents.py:221-226 (existing backward-compat test — exact template for CLUS-09):
```python
def test_incident_record_slug_optional():
    """An IncidentRecord without slug still validates (back-compat with existing current.json)."""
    data = _valid_incident()
    data.pop("slug", None)
    record = IncidentRecord.model_validate(data)
    assert record.slug is None
```
Also required companion edit if GO: `test_incidents_file_valid_round_trip` (lines 53-64) asserts an explicit `set(incident.keys())` — this must gain `"cluster_id"` and `"is_primary"` or it breaks on the schema change.

`pipeline.scripts.run_clustering_spike.compute_pairwise_metrics(pairs) -> dict` (from 26-03) is the sole authoritative source for `tp`, `fp`, `n_failsafe`. `pipeline/tests/test_clustering.py::test_golden_set_meets_go_gate` (from 26-03) is the test whose pass/fail state IS the verdict — an import/collection error in this test is NOT the same thing as the test failing its `fp == 0` assertion.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the measured GO/NO-GO decision to IncidentRecord (and quarantine the gate on NO-GO)</name>
  <files>pipeline/news/schema.py, pipeline/tests/test_schema_incidents.py, pipeline/tests/test_clustering.py</files>
  <read_first>
    .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md (from 26-03 — read the measured precision/FP/TP/n_failsafe numbers, still marked PENDING)
    pipeline/news/schema.py (lines 48-92, full IncidentRecord + field_validators)
    pipeline/tests/test_schema_incidents.py (lines 22-36 `_valid_incident` helper, 53-64 round-trip test, 221-226 slug backward-compat template)
    pipeline/tests/test_clustering.py (the `test_golden_set_meets_go_gate` test from 26-03)
  </read_first>
  <behavior>
    - Test 1 (GO branch only — write regardless, it is a no-op assertion on NO-GO since the fields won't exist to pop): `test_incident_record_cluster_fields_optional` — an `IncidentRecord` built from `_valid_incident()` with `cluster_id`/`is_primary` popped from the dict still validates, with `record.cluster_id is None` and `record.is_primary is False`.
    - Test 2: `test_incidents_file_valid_round_trip` (existing test, extended if GO) — the expected key set for a serialized incident includes `cluster_id` and `is_primary` alongside all existing fields.
  </behavior>
  <action>
    First, re-run `python -m pytest pipeline/tests/test_clustering.py::test_golden_set_meets_go_gate -q` to read the current, authoritative result (do not trust a stale number from an earlier read — this is the live gate). Interpret the outcome using this decision rule, exactly:
    - **Collection error / import error / any error NOT the `assert fp == 0 and tp > 0 and n_failsafe == 0` line itself (e.g. `ImportError`, missing fixture, `KeyError`, unexpected exception):** this is a BLOCKER, not a verdict. Record it under `## Blockers` in `.planning/STATE.md`, attempt a fix (max 2 cycles per `.planning/v2.1-AUTONOMOUS-DIRECTIVE.md`), and re-run. Do NOT publish a GO or NO-GO verdict from an error state.
    - **Test PASSES** (`fp == 0 and tp > 0 and n_failsafe == 0`, i.e. `compute_pairwise_metrics` reports `precision == 1.0` with a non-empty numerator) — this is GO:
      Add `cluster_id: str | None = None` and `is_primary: bool = False` to `IncidentRecord` in `pipeline/news/schema.py`, exactly per the `<interfaces>` template above, including the inline comment stating Phase 28 owns the real `is_primary` tie-break logic and no Phase-26 code ever sets it `True`. Do not add a `@field_validator` for either field (no existing precedent requires one — `slug` has none either, per `26-PATTERNS.md`).
      Add `test_incident_record_cluster_fields_optional` to `pipeline/tests/test_schema_incidents.py` per the exact template above.
      Locate and extend `test_incidents_file_valid_round_trip`'s expected-keys assertion to include `"cluster_id"` and `"is_primary"` — this is a REQUIRED companion edit, not optional (26-PATTERNS.md explicitly flags this test breaks otherwise).
      Run `python -m pytest pipeline/tests/test_schema_incidents.py -q` and confirm every existing test in that file (not just the new one) still passes, proving `data/incidents/current.json`'s existing records validate unchanged under the new optional fields.
    - **Test FAILS on its own gate assertion** (`fp > 0`, or `tp == 0`/`precision is None`, or `n_failsafe > 0`) — this is NO-GO:
      Make NO changes to `pipeline/news/schema.py` or `pipeline/tests/test_schema_incidents.py` at all. Do not add the fields "just in case" or with a different default — CLUS-09 is explicit that on NO-GO no schema change ships. Copy the specific FP `pair_id`s (from `compute_pairwise_metrics`'s per-pair breakdown / the `26-SPIKE-REPORT.md` decision matrix) into a note for Task 2's report update, so the NO-GO is pair-by-pair auditable.
      **Quarantine, do not weaken:** in `pipeline/tests/test_clustering.py`, add `@pytest.mark.xfail(reason="Phase 26 NO-GO: measured FP=<n> on <YYYY-MM-DD>; see 26-SPIKE-REPORT.md", strict=True)` directly above `test_golden_set_meets_go_gate`. `strict=True` means: the test's current failure keeps the suite green (an xfail'd failure is not a suite failure), but the DAY this test would start passing, the xfail itself fails loudly (`XPASS` under `strict=True` is a hard failure) — forcing a human/Fable re-decision rather than silently reopening the gate. Do not touch the test's assertion body itself; only the marker changes. `test_golden_set_metrics_match_recorded_baseline` (from 26-03, unrelated to the gate) is untouched and stays green either way — this is what allows "full pytest suite exits 0" to be literally true on NO-GO without ever loosening the honest gate assertion.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_schema_incidents.py pipeline/tests/test_clustering.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The decision applied in this task is byte-traceable to the live re-run of `test_golden_set_meets_go_gate` performed at the start of this task, not to a prior assumption.
    - On GO: `IncidentRecord.model_validate(<any existing current.json incident dict>)` succeeds with `cluster_id is None` and `is_primary is False` for every existing record (no existing incident has these keys yet).
    - On NO-GO: `git diff --stat -- pipeline/news/schema.py pipeline/tests/test_schema_incidents.py` shows zero changes; `test_golden_set_meets_go_gate` carries `@pytest.mark.xfail(strict=True, ...)` and its underlying assertion is byte-identical to 26-03's version (no loosening); `python -m pytest pipeline/tests/test_clustering.py -q` exits 0 (the xfail'd failure does not fail the suite).
    - On a blocker (import/collection error): no GO or NO-GO verdict is recorded anywhere in this task's output; `.planning/STATE.md` gains a `## Blockers` entry instead.
  </acceptance_criteria>
  <done>Schema change applied if and only if the measured golden-set evidence supports GO; NO-GO path leaves schema untouched, quarantines the gate test with a strict xfail (suite stays green, evidence stays honest), decision traceable to the live test result; collection errors are never mistaken for a verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize spike report + full regression close-out</name>
  <files>.planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md, .planning/STATE.md</files>
  <read_first>
    .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md (from 26-03, currently ends with the PENDING line)
    .planning/v2.1-AUTONOMOUS-DIRECTIVE.md (deferred-live list conventions, hard safety rules)
  </read_first>
  <action>
    Re-run `python pipeline/scripts/run_clustering_spike.py` first so the ENTIRE report body (pairwise matrix + metrics + confidence distribution + reduction-ratio evidence) is machine-regenerated from the current fixture in this same session — then edit ONLY the final verdict line and the two new sections below. Do not hand-copy or re-derive any precision/FP/TP/recall number from memory; every number in the finalized report must equal what `run_clustering_spike.py` printed in this session (diff-checkable, not remembered). Add an explicit acceptance criterion check: `diff` the regenerated report body against the previous PENDING version's numeric fields — they must match exactly (the report body itself is stable; only the final verdict line and the two new sections are new).
    Replace the `26-SPIKE-REPORT.md` line `**GO/NO-GO verdict: [PENDING — Fable orchestrator confirms against locked gate: 100% precision, 0 false merges, tp > 0, n_failsafe == 0]**` with the CONFIRMED verdict (`GO` or `NO-GO`), the exact measured precision value (or `None (zero denominator)`), the exact FP/TP/n_failsafe counts, and the exact recall value — all copied from the regenerated report / Task 1's live test result, not recomputed independently. On NO-GO, additionally list the specific FP `pair_id`s inline (from Task 1) so the verdict is pair-by-pair auditable, and state the xfail quarantine mechanism in one sentence: "the suite is green via `@pytest.mark.xfail(strict=True)` on `test_golden_set_meets_go_gate`, not via a weakened assertion; the day this test passes, the xfail itself fails and forces re-confirmation."
    Add a section "Schema change status" stating either: (GO) "`IncidentRecord.cluster_id`/`is_primary` added, optional-with-default, backward-compatible — verified against `pipeline/tests/test_schema_incidents.py`. `store.py:merge_and_write` writes raw dicts (not `model_dump()`), so this addition changes no existing output byte and produces no `data/` diff. It also means **nothing populates `cluster_id` yet**: a producer step (a clustering run invoked after `dedup.deduplicate` inside `scrape_news.py`, writing `cluster_id`/`is_primary` into `build_incident`'s dict) is required before Phase 28 can render clusters — recorded as a Phase 28 prerequisite below. `is_primary` tie-break rule is explicitly deferred to Phase 28; Phase 26 never sets it `True`." or (NO-GO) "No schema change shipped. `data/`/`pipeline/news/schema.py` byte-unchanged."
    Add a section "Backward-compat consumer sweep" (GO or NO-GO) recording, verbatim in substance: `pipeline/tests/test_schema_incidents.py:59` key-set assertion (edited on GO, untouched on NO-GO); `pipeline/tests/test_scrape_news.py:346` uses a subset check (`required_fields - set(...)`), safe either way; `pipeline/archive_r2.py:245-251` uses `csv.DictWriter(..., extrasaction="ignore")` with `inc.get(f, "")` — on GO, the new fields are deliberately absent from `incidents.csv` (no CSV schema change); `incidents.jsonl`/`corpus-state.json` are built from raw dicts and are unaffected either way; `site/src/components/map/IncidentPinLayer.ts:42-54` is a hand-kept TS mirror that is deliberately NOT edited in this pipeline-only phase — mirroring is deferred to Phase 28 as dead code otherwise.
    Add a section "LLM call budget" totaling all calls made across the phase (26-00 ping: 3 calls + 26-02 golden-set build: N calls, read from the final cumulative total in `26-CALL-LOG.md`) and confirming the total is under 2,000.
    On GO, add to `.planning/STATE.md`: a note under an appropriate section (e.g. `## Notes` or `## Pending`) stating the `cluster_id`/`is_primary` producer step is a Phase 28 prerequisite, with the exact wording from the "Schema change status" section above (or a faithful paraphrase pointing back to `26-SPIKE-REPORT.md`).
    Run the full backend regression: `python -m pytest pipeline/tests -q` and record the pass/xfail count in the report (expect ~301+ green on GO; ~300+ green plus 1 strict-xfail on NO-GO — zero red either way).
    Run the OneDrive-safe chained frontend check to confirm this phase's Python-only changes did not regress the frontend (they should not have touched `site/**` at all — this is a negative-control check): `cd site && npm run build && npm run validate` (single chained command per the OneDrive convention) and record 14/14 validators green.
    Confirm via `git status`/`git diff --stat -- data/` that `data/` remains byte-unchanged for the entire phase (all four plans combined) and record that confirmation in the report's closing line.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests -q && cd site && npm run build && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - `26-SPIKE-REPORT.md` no longer contains the string `PENDING` anywhere.
    - The report's LLM call budget total is a specific integer under 2,000, not an estimate.
    - Full pytest suite exits 0 (via a strict xfail quarantine on NO-GO, never via a loosened assertion); frontend `npm run validate` exits 0 (14/14).
    - `git diff --stat -- data/` produces no output (data/ untouched across the whole phase).
    - The report's numeric fields (precision, TP, FP, recall, confidence distribution) match exactly what `run_clustering_spike.py` printed in this session.
  </acceptance_criteria>
  <done>26-SPIKE-REPORT.md finalized with a confirmed GO/NO-GO verdict (traceable to the live regenerated report, never hand-copied), backward-compat sweep and schema-change status recorded, STATE.md carries the Phase 28 producer-step prerequisite on GO; full backend + frontend suites green; data/ confirmed read-only for the entire phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Wave 3 test result -> Wave 4 schema decision | The GO/NO-GO decision must derive strictly from the live-re-run test outcome, not from a cached belief carried over from planning, and a collection error must never be mistaken for a verdict |
| `pipeline/news/schema.py` -> all existing IncidentRecord consumers (Python + TypeScript mirror) | Adding fields must not break `current.json`/archive validation for any existing record |
| NO-GO gate test -> future phases' green-suite convention | A strict-xfail quarantine must keep the suite green without ever letting a future phase "fix" the underlying assertion instead of re-confirming the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-10 | Tampering (of evidence) | GO/NO-GO decision process | mitigate | Decision is bound to a live re-run of `test_golden_set_meets_go_gate` at the start of Task 1, explicitly forbidding a stale/assumed result and explicitly distinguishing a collection error (blocker) from a gate failure (NO-GO); acceptance criteria require byte-traceability |
| T-26-11 | Elevation of Privilege (schema drift) | `IncidentRecord` optional-field addition | mitigate | Fields are optional-with-default (`None`/`False`), no `@field_validator` added beyond existing precedent, full `test_schema_incidents.py` suite re-run to prove zero backward-compat breakage before considering the task done |
| T-26-17 | Repudiation | NO-GO verdict silently "fixed" by loosening the gate assertion in a later phase | mitigate | `@pytest.mark.xfail(strict=True)` quarantines the failure without touching the assertion; `strict=True` makes an unexpected pass a hard failure, forcing an explicit re-decision instead of a silent flip |
</threat_model>

<verification>
`python -m pytest pipeline/tests -q` (full suite) + `cd site && npm run build && npm run validate` (14/14 validators, chained single command per OneDrive convention) both exit 0. `git diff --stat -- data/` empty.
</verification>

<success_criteria>
- GO/NO-GO verdict confirmed in `26-SPIKE-REPORT.md`, traceable to the live, regenerated `run_clustering_spike.py` output and the live `test_golden_set_meets_go_gate` result — never hand-copied or recomputed independently.
- Schema change present if and only if GO; absent and untouched if NO-GO; on NO-GO the gate test is quarantined via strict xfail, never weakened.
- `is_primary` tie-break deferral to Phase 28 explicitly documented in both the report and STATE.md.
- Full pytest (~301+) and 14/14 frontend validators green; `data/` confirmed byte-unchanged for the whole phase.
</success_criteria>

<output>
Create `.planning/phases/26-event-clustering-spike/26-04-SUMMARY.md` when done
</output>
</output>
