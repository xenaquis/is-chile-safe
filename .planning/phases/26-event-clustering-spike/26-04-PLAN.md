---
phase: 26-event-clustering-spike
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - pipeline/news/schema.py
  - pipeline/tests/test_schema_incidents.py
  - .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md
autonomous: true
requirements: [CLUS-09]

must_haves:
  truths:
    - "The GO/NO-GO verdict actually applied matches the measured FP count from 26-03's precision computation — never asserted independently of the evidence"
    - "On GO, IncidentRecord validates existing current.json/archive records unchanged (cluster_id defaults to None, is_primary defaults to False)"
    - "On NO-GO, no schema field is added and the finding is documented plainly"
    - "The is_primary tie-break rule is explicitly deferred to Phase 28 in the report, not silently left undocumented"
    - "The full pipeline/tests suite (~301+ tests) and the frontend 14-validator suite both stay green after this plan"
  artifacts:
    - path: ".planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md"
      provides: "final GO/NO-GO verdict line, schema-change status, is_primary deferral note"
      contains: "GO/NO-GO verdict:"
  key_links:
    - from: "pipeline/news/schema.py"
      to: "pipeline/tests/test_schema_incidents.py"
      via: "test_incident_record_cluster_fields_optional backward-compat test"
      pattern: "cluster_id"
---

<objective>
Close the phase: read the measured precision/FP evidence produced by `26-03-PLAN.md`'s `run_clustering_spike.py` and `test_golden_set_precision_100pct`, apply the GO/NO-GO decision it dictates (never independently), conditionally ship `IncidentRecord.cluster_id`/`is_primary` (GO only, backward-compatible), finalize `26-SPIKE-REPORT.md` with the confirmed verdict, and run the full regression suite to close the phase clean.

Purpose: Turn Wave 3's measured evidence into the phase's committed outcome — either a shipped, backward-compatible schema addition or a documented NO-GO with zero schema churn.
Output: `pipeline/news/schema.py` (conditionally modified) + `pipeline/tests/test_schema_incidents.py` (conditionally extended) + finalized `26-SPIKE-REPORT.md` with a confirmed (not pending) GO/NO-GO line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.1-AUTONOMOUS-DIRECTIVE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-event-clustering-spike/26-RESEARCH.md
@.planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md
@pipeline/news/schema.py
@pipeline/tests/test_schema_incidents.py
</context>

<interfaces>
From pipeline/news/schema.py:48-61 (existing IncidentRecord — exact field-addition template):
```python
class IncidentRecord(BaseModel):
    ...
    slug: str | None = None  # resolved commune slug (NEWS-03); optional for back-compat
    # ADD (Phase 26, ON GO ONLY):
    cluster_id: str | None = None
    is_primary: bool = False  # default False everywhere in this phase — Phase 28 owns the real
                                # tie-break rule (earliest date? highest confidence?); no incident
                                # is ever marked True by Phase 26 code.
```

From pipeline/tests/test_schema_incidents.py:221-226 (existing backward-compat test — exact template for CLUS-09):
```python
def test_incident_record_slug_optional():
    """An IncidentRecord without slug still validates (back-compat with existing current.json)."""
    data = _valid_incident()
    data.pop("slug", None)
    record = IncidentRecord.model_validate(data)
    assert record.slug is None
```
Also required companion edit if GO: `test_incidents_file_valid_round_trip` (lines 53-64) asserts an explicit `set(incident.keys())` — this must gain `"cluster_id"` and `"is_primary"` or it breaks on the schema change.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the measured GO/NO-GO decision to IncidentRecord</name>
  <files>pipeline/news/schema.py, pipeline/tests/test_schema_incidents.py</files>
  <read_first>
    .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md (from 26-03 — read the measured precision/FP numbers)
    pipeline/news/schema.py (lines 48-92, full IncidentRecord + field_validators)
    pipeline/tests/test_schema_incidents.py (lines 22-36 `_valid_incident` helper, 53-64 round-trip test, 221-226 slug backward-compat template)
  </read_first>
  <behavior>
    - Test 1 (GO branch only — write regardless, it is a no-op assertion on NO-GO since the fields won't exist to pop): `test_incident_record_cluster_fields_optional` — an `IncidentRecord` built from `_valid_incident()` with `cluster_id`/`is_primary` popped from the dict still validates, with `record.cluster_id is None` and `record.is_primary is False`.
    - Test 2: `test_incidents_file_valid_round_trip` (existing test, extended if GO) — the expected key set for a serialized incident includes `cluster_id` and `is_primary` alongside all existing fields.
  </behavior>
  <action>
    First, re-run `python -m pytest pipeline/tests/test_clustering.py::test_golden_set_precision_100pct -q` to read the current, authoritative FP/precision result (do not trust a stale number from an earlier read — this is the live gate).
    IF the test PASSES (precision == 1.0, FP == 0) — this is GO:
      Add `cluster_id: str | None = None` and `is_primary: bool = False` to `IncidentRecord` in `pipeline/news/schema.py`, exactly per the `<interfaces>` template above, including the inline comment stating Phase 28 owns the real `is_primary` tie-break logic and no Phase-26 code ever sets it `True`. Do not add a `@field_validator` for either field (no existing precedent requires one — `slug` has none either, per `26-PATTERNS.md`).
      Add `test_incident_record_cluster_fields_optional` to `pipeline/tests/test_schema_incidents.py` per the exact template above.
      Locate and extend `test_incidents_file_valid_round_trip`'s expected-keys assertion to include `"cluster_id"` and `"is_primary"` — this is a REQUIRED companion edit, not optional (26-PATTERNS.md explicitly flags this test breaks otherwise).
      Run `python -m pytest pipeline/tests/test_schema_incidents.py -q` and confirm every existing test in that file (not just the new one) still passes, proving `data/incidents/current.json`'s existing records validate unchanged under the new optional fields.
    IF the test FAILS (FP > 0) — this is NO-GO:
      Make NO changes to `pipeline/news/schema.py` or `pipeline/tests/test_schema_incidents.py` at all. Do not add the fields "just in case" or with a different default — CLUS-09 is explicit that on NO-GO no schema change ships.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_schema_incidents.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The decision applied in this task is byte-traceable to the live re-run of `test_golden_set_precision_100pct` performed at the start of this task, not to a prior assumption.
    - On GO: `IncidentRecord.model_validate(<any existing current.json incident dict>)` succeeds with `cluster_id is None` and `is_primary is False` for every existing record (no existing incident has these keys yet).
    - On NO-GO: `git diff --stat -- pipeline/news/schema.py pipeline/tests/test_schema_incidents.py` shows zero changes.
  </acceptance_criteria>
  <done>Schema change applied if and only if the measured golden-set evidence supports GO; NO-GO path leaves schema untouched with the decision traceable to the live test result.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize spike report + full regression close-out</name>
  <files>.planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md</files>
  <read_first>
    .planning/phases/26-event-clustering-spike/26-SPIKE-REPORT.md (from 26-03, currently ends with the PENDING line)
    .planning/v2.1-AUTONOMOUS-DIRECTIVE.md (deferred-live list conventions, hard safety rules)
  </read_first>
  <action>
    Replace the `26-SPIKE-REPORT.md` line `**GO/NO-GO verdict: [PENDING — Fable orchestrator confirms against locked gate: 100% precision, 0 false merges]**` with the CONFIRMED verdict (`GO` or `NO-GO`), the exact measured precision value, the exact FP count, and the exact recall value — all copied from the live test result read in Task 1, not recomputed independently.
    Add a section "Schema change status" stating either: (GO) "`IncidentRecord.cluster_id`/`is_primary` added, optional-with-default, backward-compatible — verified against `pipeline/tests/test_schema_incidents.py`. `is_primary` tie-break rule is explicitly deferred to Phase 28 (STATE.md research flag); Phase 26 never sets it `True`." or (NO-GO) "No schema change shipped. `data/`/`pipeline/news/schema.py` byte-unchanged."
    Add a section "LLM call budget" totaling all calls made across the phase (26-00 ping: 3 calls + 26-02 golden-set build: N calls from `26-CALL-LOG.md`) and confirming the total is under 2,000.
    Run the full backend regression: `python -m pytest pipeline/tests -q` and record the pass count in the report (expect ~301+ green, zero red).
    Run the OneDrive-safe chained frontend check to confirm this phase's Python-only changes did not regress the frontend (they should not have touched `site/**` at all — this is a negative-control check): `cd site && npm run build && npm run validate` (single chained command per the OneDrive convention) and record 14/14 validators green.
    Confirm via `git status`/`git diff --stat -- data/` that `data/` remains byte-unchanged for the entire phase (all four plans combined) and record that confirmation in the report's closing line.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests -q && cd site && npm run build && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - `26-SPIKE-REPORT.md` no longer contains the string `PENDING` anywhere.
    - The report's LLM call budget total is a specific integer under 2,000, not an estimate.
    - Full pytest suite exits 0; frontend `npm run validate` exits 0 (14/14).
    - `git diff --stat -- data/` produces no output (data/ untouched across the whole phase).
  </acceptance_criteria>
  <done>26-SPIKE-REPORT.md finalized with a confirmed GO/NO-GO verdict and full evidence trail; full backend + frontend suites green; data/ confirmed read-only for the entire phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Wave 3 test result -> Wave 4 schema decision | The GO/NO-GO decision must derive strictly from the live-re-run test outcome, not from a cached belief carried over from planning |
| `pipeline/news/schema.py` -> all existing IncidentRecord consumers (Python + TypeScript mirror) | Adding fields must not break `current.json`/archive validation for any existing record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-10 | Tampering (of evidence) | GO/NO-GO decision process | mitigate | Decision is bound to a live re-run of `test_golden_set_precision_100pct` at the start of Task 1, explicitly forbidding a stale/assumed result; acceptance criteria require byte-traceability |
| T-26-11 | Elevation of Privilege (schema drift) | `IncidentRecord` optional-field addition | mitigate | Fields are optional-with-default (`None`/`False`), no `@field_validator` added beyond existing precedent, full `test_schema_incidents.py` suite re-run to prove zero backward-compat breakage before considering the task done |
</threat_model>

<verification>
`python -m pytest pipeline/tests -q` (full suite) + `cd site && npm run build && npm run validate` (14/14 validators, chained single command per OneDrive convention) both exit 0. `git diff --stat -- data/` empty.
</verification>

<success_criteria>
- GO/NO-GO verdict confirmed in `26-SPIKE-REPORT.md`, traceable to the live precision test result.
- Schema change present if and only if GO; absent and untouched if NO-GO.
- `is_primary` tie-break deferral to Phase 28 explicitly documented.
- Full pytest (~301+) and 14/14 frontend validators green; `data/` confirmed byte-unchanged for the whole phase.
</success_criteria>

<output>
Create `.planning/phases/26-event-clustering-spike/26-04-SUMMARY.md` when done
</output>
