---
phase: 15-crime-type-seo-ranking-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/scripts/validate/seo.mjs
  - site/src/lib/familyDefs.ts
autonomous: true
requirements: [CSEO-01, CSEO-02]

must_haves:
  truths:
    - "seo.mjs samples the new /crime-ranking/ and /es/ranking-delito/ pages so OG + ItemList assertions fire on them"
    - "Each of the 8 crime types has a bilingual methodology note that lists which CEAD delitos it covers, frames figures as reported incidence (denuncias), and cites CEAD"
  artifacts:
    - path: "site/scripts/validate/seo.mjs"
      provides: "First-subdir sample entries for crime-ranking (EN) + ranking-delito (ES)"
      contains: "crime-ranking"
    - path: "site/src/lib/familyDefs.ts"
      provides: "RANKING_METHODOLOGY_EN + RANKING_METHODOLOGY_ES records keyed by family key (incl. homicidios)"
      exports: ["RANKING_METHODOLOGY_EN", "RANKING_METHODOLOGY_ES"]
  key_links:
    - from: "site/scripts/validate/seo.mjs"
      to: "dist/crime-ranking/* + dist/es/ranking-delito/*"
      via: "firstSubdir heuristic appended to SAMPLES array"
      pattern: "crime-ranking"
    - from: "site/src/lib/familyDefs.ts"
      to: "crime-ranking page methodology <p>"
      via: "RANKING_METHODOLOGY_EN/ES imported by the [crime].astro pages in Plan 02"
      pattern: "RANKING_METHODOLOGY_(EN|ES)"
---

<objective>
Land the Wave 0 foundation that the new crime-ranking pages depend on: (1) extend the `seo.mjs` validator SAMPLES array so its OG + ItemList JSON-LD assertions actually fire on the new `/crime-ranking/` (EN) and `/es/ranking-delito/` (ES) namespaces — without this, CSEO-02's structured-data guarantees are never verified on the new pages; (2) add bilingual methodology-note copy (8 crime types: homicidios + 7 families) to `familyDefs.ts`, honoring the CONTEXT decision that each page is titled by what the data actually measures and carries an honest methodology note listing which CEAD delitos the aggregate includes.

Purpose: These two files are pure foundation with no dependency on the pages existing yet (the seo.mjs sample is guarded by `firstSubdir ?` so it is skipped, not failed, on pre-Phase-15 builds). Building them first means Plan 02's pages are verified by SEO assertions the moment they exist.

Output: Modified `site/scripts/validate/seo.mjs` and `site/src/lib/familyDefs.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-CONTEXT.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-RESEARCH.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-PATTERNS.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend seo.mjs SAMPLES with crime-ranking first-subdir heuristic</name>
  <files>site/scripts/validate/seo.mjs</files>
  <read_first>
    - site/scripts/validate/seo.mjs (lines 160-200 — the dir declarations, firstSubdir calls, and SAMPLES array; note the EXISTING crime entries at lines 169-170, 176-177, 187-188 are the exact template to mirror)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-PATTERNS.md (section "site/scripts/validate/seo.mjs" — exact insertion snippets with the firstSubdir pattern)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-RESEARCH.md (Pitfall 6 — why this must be its own change)
  </read_first>
  <action>
    In site/scripts/validate/seo.mjs, mirror the existing crime-dir sampling pattern for the two new Phase 15 namespaces. After the existing `enCrimeDir`/`esCrimeDir` declarations (currently lines 169-170) add `enCrimeRankingDir = path.join(DIST_DIR, 'crime-ranking')` and `esCrimeRankingDir = path.join(DIST_DIR, 'es', 'ranking-delito')`. After the existing `firstCrimeEn`/`firstCrimeEs` calls (currently lines 176-177) add `firstCrimeRankingEn = firstSubdir(enCrimeRankingDir)` and `firstCrimeRankingEs = firstSubdir(esCrimeRankingDir)`. In the SAMPLES array, after the existing crime entries (currently lines 187-188), append two guarded spread entries exactly like the existing crime ones: EN entry `[path.join(enCrimeRankingDir, firstCrimeRankingEn, 'index.html'), 'crime-ranking/<first> (EN)', false]` guarded by `firstCrimeRankingEn ?`, and ES entry `[path.join(esCrimeRankingDir, firstCrimeRankingEs, 'index.html'), 'ranking-delito/<first> (ES)', true]` guarded by `firstCrimeRankingEs ?`. The third tuple element is `expectEsUrl` — false for EN, true for ES, matching the existing rows. Do NOT change firstSubdir, checkOg, or the JSON-LD assertion loops — they consume SAMPLES generically. Use the Edit tool; do not rewrite the file.
  </action>
  <verify>
    <automated>cd site && node -e "const s=require('fs').readFileSync('scripts/validate/seo.mjs','utf8'); const f=s.split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'); if(!/crime-ranking/.test(f)) {console.error('FAIL: crime-ranking dir not added'); process.exit(1);} if(!/ranking-delito/.test(f)) {console.error('FAIL: ranking-delito dir not added'); process.exit(1);} if((f.match(/firstCrimeRanking(En|Es)/g)||[]).length < 4){console.error('FAIL: firstCrimeRanking* not wired in both declaration and SAMPLES'); process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/validate/seo.mjs` (implicitly via the require above) parses without syntax error.
    - The non-comment body of seo.mjs contains both `crime-ranking` and `ranking-delito` literals.
    - `firstCrimeRankingEn` and `firstCrimeRankingEs` each appear in BOTH a declaration line and a SAMPLES spread (>= 4 total occurrences), proving the sample entries are wired, not just the dir vars.
    - No edits to firstSubdir, checkOg, or the assertion loops.
  </acceptance_criteria>
  <done>seo.mjs samples crime-ranking (EN) + ranking-delito (ES); guarded so pre-Phase-15 builds skip rather than fail.</done>
</task>

<task type="auto">
  <name>Task 2: Add bilingual RANKING_METHODOLOGY entries to familyDefs.ts</name>
  <files>site/src/lib/familyDefs.ts</files>
  <read_first>
    - site/src/lib/familyDefs.ts (full file — confirm existing FAMILY_LABELS_EN/ES, FAMILY_DEFS_EN/ES, FAMILY_ORDER structure and that `homicidios` key already exists in those records; match the `Record<string, string>` export style verbatim)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-PATTERNS.md (section "site/src/lib/familyDefs.ts" — the EXACT RANKING_METHODOLOGY_EN/ES copy strings, already validator-checked)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-RESEARCH.md (section 5 "Methodological Note" — validator-compliance rationale; section 2 mapping table — which delitos each family covers; note hurtos→propiedad and manejo-ebriedad→incivilidades must be labeled as the full family, not the sub-type)
    - site/scripts/validate/forbidden-language.mjs (FORBIDDEN_TERMS — confirm no proposed string contains a blocked term)
    - site/src/lib/jsonld.ts (FORBIDDEN/assertSafeName guard — methodology body is NOT passed to buildItemList, but confirm the copy avoids 'safe'/'dangerous' anyway for tone)
  </read_first>
  <action>
    Append two new exported records to site/src/lib/familyDefs.ts after the existing FAMILY_DEFS_ES block: `RANKING_METHODOLOGY_EN: Record<string, string>` and `RANKING_METHODOLOGY_ES: Record<string, string>`, each keyed by the 8 family keys `homicidios, robos_violentos, drogas, propiedad, vif, armas, incivilidades, vida`. Use the exact copy strings provided in 15-PATTERNS.md (the "site/src/lib/familyDefs.ts" section) — they are already verified against forbidden-language.mjs and jsonld.ts guards. Each note must: (a) name the underlying CEAD delitos in that family aggregate; (b) state figures are reported incidence / denuncias, not convictions; (c) cite "CEAD — Ministry of Interior and Public Security" (EN) / "CEAD — Ministerio del Interior y Seguridad Pública" (ES). For `homicidios` specifically the note must reference CEAD subgroup 101 within the Life Crimes family (this is the homicide-data deviation — its rate is read from `featured_rates.homicidios`, NOT `by_family`, but the methodology TEXT lives here alongside the families). For `propiedad` the note must say the aggregate INCLUDES hurtos and robos (honest broad label per CONTEXT — never relabel it "hurtos only"); for `incivilidades` similarly say it is the full family aggregate. Do not modify FAMILY_LABELS_*, FAMILY_DEFS_*, or FAMILY_ORDER. Use Edit to append; do not rewrite the file.
  </action>
  <verify>
    <automated>cd site && npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "familyDefs" || true) && node --input-type=module -e "import('./src/lib/familyDefs.ts').then(m=>{const KEYS=['homicidios','robos_violentos','drogas','propiedad','vif','armas','incivilidades','vida'];for(const r of ['RANKING_METHODOLOGY_EN','RANKING_METHODOLOGY_ES']){if(!m[r]){console.error('FAIL missing '+r);process.exit(1);}for(const k of KEYS){const v=m[r][k];if(!v||v.length<60){console.error('FAIL '+r+'.'+k+' missing/too short');process.exit(1);}if(!/CEAD/.test(v)){console.error('FAIL '+r+'.'+k+' does not cite CEAD');process.exit(1);}}}console.log('PASS');}).catch(e=>{console.error(e);process.exit(1);})" 2>/dev/null || echo "NOTE: if ts import fails under node, rely on the build in Plan 02 Task 3 to typecheck; the grep above confirms no familyDefs tsc error"</automated>
  </verify>
  <acceptance_criteria>
    - `RANKING_METHODOLOGY_EN` and `RANKING_METHODOLOGY_ES` are exported and each contains all 8 family keys.
    - Every value cites "CEAD", states reported-incidence/denuncias framing, and names the included delitos.
    - The `homicidios` value references subgroup 101 / Life Crimes family.
    - `propiedad` and `incivilidades` values explicitly describe the FULL family aggregate (no sub-type-only relabeling).
    - No string contains a forbidden-language term ('safe', 'dangerous', 'peligros*', 'la mas segura', etc.) — confirmed against forbidden-language.mjs FORBIDDEN_TERMS.
    - `tsc --noEmit` reports no error originating in familyDefs.ts.
  </acceptance_criteria>
  <done>familyDefs.ts exports bilingual methodology notes for all 8 crime types, validator-clean, ready for Plan 02 pages to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CEAD JSON → build-time read | The only data crossing into the build; already validated by the pipeline + build validators. No user input, no network, no auth at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Methodology copy in familyDefs.ts (editorial-guard bypass) | mitigate | Copy strings are validator-checked against forbidden-language.mjs FORBIDDEN_TERMS in Task 2 acceptance; the build-time forbidden-language.mjs scan over dist/ HTML (Plan 02) re-asserts on rendered output. |
| T-15-02 | Information disclosure | seo.mjs sample paths | accept | Validator reads only local dist/ HTML; no secrets, no PII, no network. Static-page generation — minimal surface. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: none). No install tasks → no slopcheck checkpoint required. |

No HIGH threats: this phase is static-page generation from committed local JSON with no user input, no auth, and no runtime network. The relevant low-severity items are build-time editorial-guard integrity (T-15-01) which is covered by existing validators.
</threat_model>

<verification>
- `cd site && node -e "..."` SAMPLES guard check (Task 1) passes.
- `RANKING_METHODOLOGY_EN/ES` present, all 8 keys, CEAD-cited (Task 2).
- No forbidden-language term in any new methodology string.
- These changes are inert until Plan 02 creates the pages — pre-existing build/validate must remain green: `cd site && npm run build && npm run validate` (run once at end of this plan to confirm no regression to the existing crime pages).
</verification>

<success_criteria>
- seo.mjs will sample the new namespaces once Plan 02 ships the pages (guarded, no false-fail before then).
- familyDefs.ts exposes honest, CEAD-citing, reported-incidence methodology copy for all 8 crime types.
- Existing build + full validator suite still green (no regression).
</success_criteria>

<output>
Create `.planning/phases/15-crime-type-seo-ranking-pages/15-01-SUMMARY.md` when done.
</output>
