---
phase: 37-dependency-security-hygiene
plan: 04
type: execute
wave: 4
depends_on: [37-03]
files_modified:
  - pipeline/requirements.txt (via PR merges)
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-02]
executor: sonnet (merges + gate + #18 close); orchestrator (poll the next scheduled news-pipeline run)

must_haves:
  truths:
    - "The pipeline patch PRs #26 python-dotenv, #31 scipy range, #35 pydantic and #36 lxml are each merged through 37-merge-pr.sh, or closed with a written reason. Merges are serialized, because all four edit pipeline/requirements.txt."
    - "#37 (openai major) is NOT touched here; it belongs to 37-05 (DEPS-03)."
    - "#18 (typescript 5.9.3→7.0.2, major) is closed with a written reason citing its failing checks on its current head. It is merged only if its current head is fully green AND the local gate (astro check 0 errors) passes after merge."
    - "After the last merge, the next SCHEDULED news-pipeline run on the new master succeeds (its Health gate step green), which proves the runtime pipeline deps on the Actions runner."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
      provides: "merged/closed lines, the #18 reason, and the scheduled-run id + conclusion"
  key_links:
    - from: pipeline/requirements.txt
      to: news-pipeline.yml pip install
      via: "next scheduled run"
      pattern: "news-pipeline"
---

<objective>
DEPS-02 (pipeline backlog + the broken TS major): land the four pipeline patch bumps and resolve #18 with a written reason. The pipeline merges do not change the built site (deploy-on-code watches site/** and data paths only), so the proof is the local pytest gate plus one live scheduled news run.

Purpose: #18 (47 days old at planning) and #26 (33 days) are the oldest items against the "none older than 14 days" bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
@.planning/phases/37-dependency-security-hygiene/37-03-SUMMARY.md

<interfaces>
- The tools are the same as in 37-02.
- Measured 2026-09-24:
  - #26 python-dotenv 1.2.2→1.2.3; #31 scipy `>=1.18.0,<2` → `>=1.18.1,<2`; #35 pydantic 2.13.4→2.13.5; #36 lxml 6.1.1→6.1.3. All are CI-green at their heads.
  - #18 typescript 5.9.3→7.0.2 (site/package.json:35 `"typescript": "^5.9.3"`, dev); "Frontend build + validate" = FAILURE and "Cloudflare Pages" = FAILURE on its head.
- news-pipeline.yml runs on cron '0 */6 * * *' (35-02 evidence), with pip install -r pipeline/requirements.txt in-job. The heartbeat/health-gate semantics are G-04/G-09.
- lxml is used by the CEAD scraper, which runs LOCALLY only (memory cead-scraper-quarterly-local). Its live check is Phase 38 HYG-06 (on/after 2026-10-01), and pytest covers the parser offline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialized merge of #26, #31, #35, #36 + live scheduled-run proof</name>
  <files>pipeline/requirements.txt (via merges), .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-03-SUMMARY.md must exist. Otherwise STOP.

For N in 26, 31, 35, 36, in order, apply the per-PR procedure of 37-02 Task 1 (auto-closed check, --check-only, `@dependabot rebase` with a 45-min wait, the close-with-reason path, merge, pull --rebase, 37-gate.sh green, the revert path, push).

Do not merge #37 here. `requirements.txt` changes rebase #37, and 37-05 re-reads it.

After the last merge, the orchestrator records master SHA S, then polls `gh run list --workflow news-pipeline.yml --event schedule --json databaseId,headSha,status,conclusion,createdAt` every 10 min for up to 7 h until the first scheduled run created after the push of S completes. Require conclusion success. Log `scheduled news run <id> on <sha7>: success`.
- If it fails, read its log (`gh run view <id> --log-failed`). If the failure is the pip install or an import error from a merged package, `git revert` that merge (gate, push), log it, and wait for the next scheduled run.
- If it is an unrelated upstream/API failure, log the diagnosis and wait for the next run, up to 2 runs. Then pause (pause point 5) if still red.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && for N in 26 31 35 36; do s=$(gh pr view $N --json state -q .state); echo "#$N $s"; [ "$s" != OPEN ] || exit 1; grep -qE "^(merged|closed|auto-closed|reverted) #$N[ :]" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || exit 1; done && test "$(gh pr view 37 --json state -q .state)" = OPEN && grep -qE "^scheduled news run [0-9]+ on [0-9a-f]{7}: success" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md && git diff --quiet origin/master -- pipeline/requirements.txt</automated>
    Expected: the four PRs are not OPEN and are logged; #37 is still OPEN; a scheduled news run on the new master succeeded; the local requirements.txt equals origin/master.
  </verify>
  <done>The pipeline patch bumps are on master and proven by a live scheduled run.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve #18 (typescript 7 major) with a written reason</name>
  <files>.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
1. Run `37-merge-pr.sh --check-only 18` and log the exit code and failing check names.
2. If it exits 4 or 3, write the reason file:
   "Closing: typescript 5.9.3 → 7.0.2 is a major bump, and on this PR's head the checks '<failing names>' fail (<run URL>). Adopting TypeScript 7 needs its own verified change (astro check / @astrojs/check compatibility across the site), which is outside the v2.2 dependency-hygiene scope (Phase 37). Per the weekly triage policy (DEPLOYMENT.md § Dependency triage policy), majors are adopted in a dedicated change, not by merging a red Dependabot PR. Dependabot will re-propose the next TypeScript release."
   Then run `37-close-pr.sh 18 <file>`.
3. Only if --check-only exits 0 (all green on its current head): run the normal merge procedure. The gate must show astro check with 0 errors (37-gate.sh already asserts rc 0). A red gate means `git revert` plus the same close reason with "reverted" wording.
4. Commit `docs(37): pipeline PRs + #18 triaged` and push (G-47).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && s=$(gh pr view 18 --json state -q .state) && echo "#18 $s" && [ "$s" != OPEN ] && grep -qE "^(merged|closed|reverted) #18[ :]" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md && { [ "$s" = MERGED ] || gh pr view 18 --json comments -q '.comments[-1].body' | grep -q "outside the v2.2 dependency-hygiene scope"; }</automated>
    Expected: #18 is not OPEN. If it was closed, its last comment carries the written reason.
  </verify>
  <done>#18 is resolved with a written, evidenced reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PyPI → Actions runner | news-pipeline installs requirements.txt on every run |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10 | Denial of service | news collection breaks on a dep bump | mitigate | Local pytest gate per merge, a live scheduled-run proof, and a revert path |
| T-37-11 | Repudiation | #18 closed silently | mitigate | The reason comment cites the failing checks and the run URL, and the verify reads the comment back |
| T-37-SC | Tampering | pip installs | accept | Patch bumps of already-pinned packages proposed by Dependabot; no new package |
</threat_model>

<verification>
- #26/#31/#35/#36/#18 are not OPEN and are logged; #37 is still OPEN.
- A scheduled news-pipeline run on the new master succeeded.
</verification>

<success_criteria>
Pipeline patch backlog cleared without breaking live collection; oldest PR (#18) resolved with a reason.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-04-SUMMARY.md` when done.
</output>
