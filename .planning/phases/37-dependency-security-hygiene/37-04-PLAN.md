---
phase: 37-dependency-security-hygiene
plan: 04
type: execute
wave: 4
depends_on: [37-03]
files_modified:
  - pipeline/requirements.txt (via PR merges)
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-02]
executor: sonnet (merges + gate + #18 close); orchestrator (poll the next scheduled news-pipeline run)

must_haves:
  truths:
    - "The pipeline patch PRs #26 python-dotenv, #31 scipy range, #35 pydantic and #36 lxml are each merged through 37-merge-pr.sh, or closed with a written reason. Merges are serialized, because all four edit pipeline/requirements.txt. A superseded PR is followed to its successor #M (R-07)."
    - "The open Dependabot openai 2→3.x PR (#37 or its successor, identified by the title text 'openai from 2.53.0', R-07) is NOT touched here; it belongs to 37-05 (DEPS-03)."
    - "#18 (typescript 5.9.3→7.0.2, major) is closed with the G-55 owner-reserved reason ('major upgrade reserved for owner review; reopen or @dependabot recreate when ready'), citing its current check state. It is never merged in the run, even if green, and no `@dependabot ignore` command is posted."
    - "After the last merge, the next SCHEDULED news-pipeline run on the new master succeeds (its Health gate step green, with attempted >= 1, or 'classifier path not exercised' logged), and the next scheduled r2-archive run succeeds (it installs the same requirements.txt and uses lxml via trafilatura). A failure is attributed to a merge only if its traceback names a bumped package (R-13)."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
      provides: "merged/closed lines, the #18 reason, and the scheduled news and r2-archive run ids + conclusions"
  key_links:
    - from: pipeline/requirements.txt
      to: news-pipeline.yml pip install
      via: "next scheduled run"
      pattern: "news-pipeline"
---

<objective>
DEPS-02 (pipeline backlog + the broken TS major): land the four pipeline patch bumps and resolve #18 with a written reason. The pipeline merges do not change the built site (deploy-on-code watches site/** and data paths only), so the proof is the local pytest gate plus one live scheduled news run.

Purpose: #18 (47 days old at planning) and #26 (33 days) are the oldest items against the "none older than 14 days" bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
@.planning/phases/37-dependency-security-hygiene/37-03-SUMMARY.md

<interfaces>
- The tools are the same as in 37-02.
- Measured 2026-09-24:
  - #26 python-dotenv 1.2.2→1.2.3; #31 scipy `>=1.18.0,<2` → `>=1.18.1,<2`; #35 pydantic 2.13.4→2.13.5; #36 lxml 6.1.1→6.1.3. All are CI-green at their heads.
  - #18 typescript 5.9.3→7.0.2 (site/package.json:35 `"typescript": "^5.9.3"`, dev); "Frontend build + validate" = FAILURE and "Cloudflare Pages" = FAILURE on its head.
- news-pipeline.yml runs on cron '0 */6 * * *' (35-02 evidence), with pip install -r pipeline/requirements.txt in-job. The heartbeat/health-gate semantics are G-04/G-09.
- lxml is used by the CEAD scraper, which runs LOCALLY only (memory cead-scraper-quarterly-local), AND by trafilatura, which pipeline/news/fulltext.py:22 imports. r2-archive.yml (cron '30 5 * * *') installs pipeline/requirements.txt (:50) and runs pipeline/archive_r2.py (:58), which uses fulltext.py. The first live lxml run on CEAD is Phase 38 HYG-06 (on/after 2026-10-01); pytest covers the parser offline.
- scipy is used only by pipeline/composite_index.py (and its test), which no cron runs.
- The news run log carries `Health gate OK: attempted=<n> responded=<m> ...` (measured 2026-09-24T21:07Z: attempted=38 responded=38).
- The openai PR is found by title, not number: `gh pr list --author app/dependabot --state open --search 'openai in:title' --json number,title`, filtered to titles containing "openai from 2.53.0". Measured #37 at 3.7.0; PyPI latest 3.19.2; the pip queue sits at open-pull-requests-limit 5 (#26/#31/#35/#36/#37), so freeing slots here will likely let Dependabot supersede #37 on its next Saturday run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialized merge of #26, #31, #35, #36 + live scheduled-run proof</name>
  <files>pipeline/requirements.txt (via merges), .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-03-SUMMARY.md must exist. Otherwise STOP.

For N in 26, 31, 35, 36, in order, apply the per-PR procedure of 37-02 Task 1 (auto-closed/superseded check with successor triage, --check-only with its exit-code branches (3/6 rebase, 5 poll and never close, 7 G-55 close, 4 classify before any close), merge, `git pull --rebase --autostash origin master`, 37-gate.sh green, the G-54 revert rule, push). These merges touch only pipeline/, which deploy-on-code does not watch, so the per-merge served-route check of 37-02 step 3 (G-61) does not apply.

Do not merge the openai PR (#37 or its successor) here. `requirements.txt` changes rebase it, and 37-05 re-reads it by title.

After the last merge, the orchestrator records master SHA S, then polls `gh run list --workflow news-pipeline.yml --event schedule --json databaseId,headSha,status,conclusion,createdAt` every 10 min for up to 7 h until the first scheduled run created after the push of S completes. Require conclusion success. Log `scheduled news run <id> on <sha7>: success attempted=<n>`, reading attempted from the `Health gate OK: attempted=` log line; if attempted is 0, also log `classifier path not exercised` (no new candidates).
- Also poll `gh run list --workflow r2-archive.yml --event schedule --json databaseId,headSha,status,conclusion,createdAt` for the first scheduled run created after the push of S (daily 05:30 UTC, so up to ~24 h), require success, and log `scheduled r2-archive run <id> on <sha7>: success`.
- If either fails, read its log (`gh run view <id> --log-failed`). Attribute it to a merge only if the traceback names a bumped package (python-dotenv, scipy, pydantic, lxml); then `git revert` that merge (gate, push), log it, and wait for the next scheduled run. If the traceback names an unpinned package (boto3, trafilatura, …), log `third-party drift: <pkg>` and do not revert.
- If it is an unrelated upstream/API failure, log the diagnosis and wait for the next run, up to 2 runs. Then pause (pause point 5) if still red.
- Log that #31 (scipy) and #36 (lxml on CEAD) have no live proof in this phase: scipy runs in no cron, and the first live lxml run on CEAD is Phase 38 HYG-06.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && for N in 26 31 35 36; do s=$(gh pr view $N --json state -q .state); echo "#$N $s"; [ "$s" != OPEN ] || exit 1; grep -qE "^(merged|closed|auto-closed|reverted|superseded) #$N[ :]" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || exit 1; M=$(grep -oE "^superseded #$N -> #[0-9]+" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md | grep -oE "[0-9]+$" | tail -1); [ -z "$M" ] || [ "$(gh pr view $M --json state -q .state)" != OPEN ] || exit 1; done && test "$(gh pr list --author app/dependabot --state open --search 'openai in:title' --json number,title -q '[.[]|select(.title|contains("openai from 2.53.0"))]|length')" -ge 1 && grep -qE "^scheduled news run [0-9]+ on [0-9a-f]{7}: success" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md && grep -qE "^scheduled r2-archive run [0-9]+ on [0-9a-f]{7}: success" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md && git diff --quiet origin/master -- pipeline/requirements.txt</automated>
    Expected: the four PRs are not OPEN and are logged; an open Dependabot PR titled "openai from 2.53.0 …" still exists (#37 or its successor, R-07); a scheduled news run and a scheduled r2-archive run on the new master succeeded; the local requirements.txt equals origin/master.
  </verify>
  <done>The pipeline patch bumps are on master and proven by live scheduled news and r2-archive runs; the untested paths (scipy, lxml on CEAD) are logged.</done>
</task>

<task type="auto">
  <name>Task 2: Close #18 (typescript 7 major) with the owner-reserved reason</name>
  <files>.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
1. Run `37-merge-pr.sh --check-only 18` and log the exit code (expected 7 = major, G-55) and the current check conclusions from `gh pr view 18 --json statusCheckRollup` (measured 2026-09-24: "Frontend build + validate" and "Cloudflare Pages" FAILURE).
2. Whatever the exit code and even if every check is green, write the reason file:
   "Closing: typescript 5.9.3 → 7.0.2 is a major upgrade, reserved for owner review; reopen or @dependabot recreate when ready. Current head checks: <names and conclusions> (<run URL>). Adopting TypeScript 7 needs its own verified change (astro check / @astrojs/check compatibility across the site), which is outside the v2.2 dependency-hygiene scope (Phase 37). Per the weekly triage policy (the DEPS-04 policy, being added to DEPLOYMENT.md in Phase 37), majors are adopted by the owner in a dedicated change (G-55). Dependabot will re-propose the next TypeScript release."
   Then run `37-close-pr.sh 18 <file>`. Do not merge #18, and do not post any `@dependabot ignore` command.
3. If Dependabot has already closed #18 as superseded by a newer TypeScript PR #M, log `superseded #18 -> #M` and close #M with the same reason (its versions in place of 5.9.3 → 7.0.2).
4. List #18 (or #M) for the deferred-live "majors reserved for owner" line in 37-07.
5. Commit `docs(37): pipeline PRs + #18 triaged`, `git pull --rebase --autostash origin master`, and push (G-47).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && L=.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md && s=$(gh pr view 18 --json state -q .state) && echo "#18 $s" && [ "$s" = CLOSED ] && grep -qE "^(closed|superseded) #18[ :]" $L && { M=$(grep -oE "^superseded #18 -> #[0-9]+" $L | grep -oE "[0-9]+$" | tail -1); T=${M:-18}; [ "$(gh pr view $T --json state -q .state)" = CLOSED ] && gh pr view $T --json comments -q '[.comments[]|select(.author.login!="dependabot")|.body]|join("\n")' | grep -q "major upgrade, reserved for owner review"; }</automated>
    Expected: #18 is CLOSED, never MERGED. The PR closed by us (#18, or its logged successor #M) is CLOSED and a non-Dependabot comment carries the G-55 owner-reserved reason. Dependabot always answers after a close ("OK, I won't notify you again about this release…", measured on #12 and #20), so the last comment is never ours (BF-03).
  </verify>
  <done>#18 is closed with the owner-reserved major reason, citing its current checks, and listed for deferred-live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PyPI → Actions runner | news-pipeline installs requirements.txt on every run |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10 | Denial of service | news collection breaks on a dep bump | mitigate | Local pytest gate per merge, live scheduled news + r2-archive proofs, and a revert path limited to tracebacks naming a bumped package |
| T-37-11 | Repudiation | #18 closed silently | mitigate | The reason comment cites the failing checks and the run URL, and the verify reads the comment back |
| T-37-SC | Tampering | pip installs | accept | Patch bumps of already-pinned packages proposed by Dependabot; no new package |
</threat_model>

<verification>
- #26/#31/#35/#36/#18 are not OPEN and are logged (#18 CLOSED with the G-55 reason); the openai 2→3.x PR (#37 or its successor) is still OPEN.
- A scheduled news-pipeline run and a scheduled r2-archive run on the new master succeeded.
</verification>

<success_criteria>
Pipeline patch backlog cleared without breaking live collection; oldest PR (#18) resolved with a reason.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-04-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

- **R-07:** Task 1 verify no longer pins `#37 = OPEN`; it requires an open Dependabot PR whose title contains "openai from 2.53.0" (`gh pr list … --search 'openai in:title'`). Twins: must_haves truth 2, interfaces, action text, Expected, verification. Successors of superseded pipeline PRs are triaged and checked in the verify.
- **R-13:** Task 1 also polls the next scheduled r2-archive run (success required, logged), logs attempted from the health-gate line (or "classifier path not exercised"), attributes a failure to a merge only if the traceback names a bumped package (unpinned → "third-party drift", no revert), and logs that #31 (scipy) and #36 (lxml on CEAD) have no live proof (first live lxml on CEAD = Phase 38 HYG-06). Interfaces line on lxml corrected (trafilatura in fulltext.py:22 via r2-archive). Twins: must_haves truth 4, artifact, verify, done, threat T-37-10, verification.
- **Decision (a) (G-55) / R-08:** Task 2 always closes #18 with the owner-reserved major reason, even if green; no `@dependabot ignore`; a superseding TypeScript PR gets the same close; verify requires CLOSED and the new reason text. Twins: must_haves truth 3, task name, done.
- **R-03 / R-05 / R-09 (G-53/G-54):** Task 1 inherits the 37-02 exit-code branches, the stage-based revert rule and `--autostash`; Task 2 step 5 uses `--autostash`.

## Revision R2 (gate R1)

- **BF-03:** the Task 2 verify reads every non-Dependabot comment (`select(.author.login!="dependabot")`) instead of `.comments[-1]`, because Dependabot always replies after a close (re-measured 2026-09-25 on #12: xenaquis 18:34:11Z, then dependabot 18:34:14Z). No other plan's verify reads a close reason. Twin: Expected.
- **BF-05 twin:** Task 1 states that the per-merge served-route check does not apply (pipeline-only merges do not deploy).
- **Non-blocker applied:** NB-13(d), #18's close reason cites the DEPS-04 policy 'being added to DEPLOYMENT.md in Phase 37'.
