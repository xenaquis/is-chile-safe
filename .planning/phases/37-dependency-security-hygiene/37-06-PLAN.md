---
phase: 37-dependency-security-hygiene
plan: 06
type: execute
wave: 6
depends_on: [37-05]
files_modified:
  - .github/workflows/codeql.yml
  - pipeline/tests/test_workflow_guards.py
  - pipeline/tests/test_codeql_workflow.py
  - DEPLOYMENT.md
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-04]
executor: opus (CI security: new workflow, permissions, SHA pins)

must_haves:
  truths:
    - "CodeQL is enabled via ADVANCED setup: a committed .github/workflows/codeql.yml. Repo Settings / the code-scanning default-setup API are NOT touched (G-46)."
    - "codeql.yml passes all four workflow gates: lint-workflows.sh (actionlint + shellcheck), check-sha-pins.sh (every uses: is a 40-hex SHA with a `# vN.N.N` comment), zizmor 1.10.0 --persona=regular run with GH_TOKEN (online audits, as CI runs it), and check-secret-hygiene.sh."
    - "Least privilege: top-level `permissions: {}`; the job grants only `contents: read` and `security-events: write`; checkout has `persist-credentials: false`; there are no `run:` steps and no secrets."
    - "After push, the CodeQL push run for the ci(37) commit's SHA (selected by headSha, not --limit 1) completes success for both javascript-typescript and python, and code-scanning analyses exist for both categories. Open alert counts by severity are recorded, not gated. The coverage line states that .astro components are not assumed analyzed, verified from the first run log."
    - "DEPLOYMENT.md records the weekly manual Dependabot triage policy (no auto-merge; majors reserved for the owner, G-55; the repo owner runs the weekly triage from the Monday after the run ends, G-56; the gate as a literal command chain) and the CodeQL decision, and the stale DEP-01 Known-Gaps entry (astro 6.4.6, PRs #5/#7/#12 'left open') is marked resolved."
    - "One dated GitHub issue 'Dependabot weekly triage — first check <date>' exists with the policy checklist (G-56). No repo settings are touched."
  artifacts:
    - path: .github/workflows/codeql.yml
      provides: "CodeQL advanced setup, push/PR on master + weekly schedule, build-mode none"
    - path: pipeline/tests/test_codeql_workflow.py
      provides: "structure pins: triggers, permissions, languages, no run steps, SHA-pinned uses"
    - path: DEPLOYMENT.md
      provides: "§10 'Dependency triage policy (DEPS-04)' (owner, majors, literal gate chain, issue number) and 'CodeQL decision (DEPS-04)' (with the coverage line)"
  key_links:
    - from: pipeline/tests/test_workflow_guards.py _MUST_HAVE_FALSE
      to: .github/workflows/codeql.yml
      via: "\"codeql.yml\": 1"
      pattern: "\"codeql.yml\": 1"
---

<objective>
DEPS-04: turn on CodeQL without touching repo settings, and write the standing Dependabot triage policy and the CodeQL decision into DEPLOYMENT.md, which is where the repo keeps its ops runbook (§10 Operational Notes; there is no SECURITY.md or docs/, measured).

Purpose: the directive's owner default says "Weekly manual triage policy documented. No auto-merge. Enable CodeQL (free on public repos)." The hard rules forbid changing repo settings. G-46 reconciles the two: a committed workflow is a code change, and default setup would be a settings change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-RESEARCH.md
@.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
@.github/workflows/ci.yml
@.github/scripts/check-sha-pins.sh
@DEPLOYMENT.md

<interfaces>
Measured 2026-09-24:
- The repo is PUBLIC, so CodeQL advanced setup is free. `code-scanning/alerts` returned 404 "no analysis found" (research), meaning no default setup is active; advanced SARIF uploads are refused when default setup is on.
- The checkout pin in use is `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1` (ci.yml:19). Reuse it.
- github/codeql-action tags: `git ls-remote --tags https://github.com/github/codeql-action` lists v4.38.x (annotated, peeled `^{}`). Resolve at execution: `gh api repos/github/codeql-action/releases/latest --jq .tag_name`, then the PEELED commit SHA for that tag (the `^{}` line). init, analyze (and upload, if used) must share the same SHA.
- ci.yml:95-98 runs zizmor as `pipx run zizmor==1.10.0 --persona=regular .github/workflows/`, but only on pull_request. A direct push to master does not run ci.yml, so zizmor must run locally: create the venv `python -m venv "$TEMP/venv-zizmor"`, then `pip install zizmor==1.10.0` into it (the same package and version CI already uses), then `GH_TOKEN=$(gh auth token) zizmor --persona=regular .github/workflows/`. CI passes GH_TOKEN (ci.yml:95-98), which enables zizmor's online audits; without it the local run is offline and weaker than CI (R-12). codeql.yml enters master by direct push, so CI first sees it on the next PR.
- codeql.yml runs on every push to master with `cancel-in-progress`, so `gh run list … --limit 1` after a later docs push reads the docs commit's run, still in progress (R-12). Select runs by headSha.
- site/src holds 84 .astro files (10 of them use set:html), against 41 .ts and 17 .tsx (premortem; .astro and set:html counts re-measured 2026-09-25). Whether CodeQL's JS/TS extractor reads .astro is not verified offline (R-14).
- Dependabot opens version PRs on Saturdays around 14:53Z (measured: #18 08-08, #26/#27/#28 08-22, #30/#31/#33 08-29, #35/#36/#37 09-05), so a Monday triage sees the weekend wave.
- pipeline/tests/test_workflow_guards.py:520-527: `_MUST_HAVE_FALSE = {"ci.yml": 3, "deploy-on-code.yml": 1, "heartbeat.yml": 1, "r2-archive.yml": 1, "deploy-manual.yml": 1}`. The docstring (:505-517) says "six non-pushing checkouts".
- test_workflow_guards.py:659 requires every `run: python` step to follow setup-python + pip install in the same job. codeql.yml has no run steps, so this is trivially satisfied.
- dependabot.yml already covers github-actions (weekly, grouped), pip /pipeline and npm /site (weekly, open-pull-requests-limit 5 each), so the policy cadence matches Dependabot's schedule.
- DEPLOYMENT.md:361-379: "Known Gaps … DEP-01 (2026-08-05) — astro stays at 6.4.6 … Dependabot PRs #5, #7 and #12 are left OPEN deliberately". This is stale: astro was 7.1.6 since #21, and is ≥ 7.2.8 after 37-01. §10 Operational Notes starts at :237.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: codeql.yml (advanced setup) + structure pins</name>
  <files>.github/workflows/codeql.yml, pipeline/tests/test_codeql_workflow.py, pipeline/tests/test_workflow_guards.py</files>
  <behavior>
    - test_codeql_workflow.py (yaml via pytest.importorskip, the pattern of test_news_health_gate.py) asserts:
      - on.push.branches == ['master'], on.pull_request.branches == ['master'], on.schedule has exactly 1 cron;
      - top-level permissions == {};
      - the single job's permissions == {'contents': 'read', 'security-events': 'write'};
      - strategy.matrix.language == ['javascript-typescript', 'python'] and fail-fast is false;
      - every step has `uses` and no `run`;
      - every `uses` matches `^[\w.-]+/[\w./-]+@[0-9a-f]{40}$`, and the raw line carries `# v\d+\.\d+\.\d+`;
      - the init and analyze SHAs are equal;
      - the checkout step's with.persist-credentials is False;
      - the init step's with has build-mode == 'none' and languages == '${{ matrix.language }}';
      - the analyze step's with.category == '/language:${{ matrix.language }}'.
    - Negative control: a copy of the YAML text with permissions changed to `write-all` fails the permissions assertion, tested via a helper that takes text.
    - test_workflow_guards.py: `_MUST_HAVE_FALSE` gains `"codeql.yml": 1`, and the docstring count says eight (ci.yml 3 + deploy-on-code, heartbeat, r2-archive, deploy-manual 1 each + codeql.yml 1; the current "six" is already stale); the existing tests stay green.
  </behavior>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-05-SUMMARY.md must exist. Otherwise STOP. Re-confirm read-only that `gh repo view --json visibility` is PUBLIC; if it is not, STOP (CodeQL is not free) and report.

Write .github/workflows/codeql.yml per G-46:
- name CodeQL.
- on: push branches [master]; pull_request branches [master]; schedule cron '23 6 * * 1', a Monday off-minute that lines up with the weekly triage.
- top-level `permissions: {}`.
- `concurrency: { group: codeql-${{ github.ref }}, cancel-in-progress: true }`.
- job `analyze`: name "CodeQL (${{ matrix.language }})", runs-on ubuntu-latest, timeout-minutes 30, job permissions `contents: read` and `security-events: write`, strategy fail-fast false with matrix.language [javascript-typescript, python].
- steps:
  1. checkout at the ci.yml SHA with `persist-credentials: false`;
  2. github/codeql-action/init@<peeled SHA> # <tag>, with languages `${{ matrix.language }}`, build-mode `none`, and `config: |` containing paths-ignore for site/dist, site/public/data, data, pipeline/cache, .tools and node_modules;
  3. github/codeql-action/analyze@<same SHA> # <tag>, with category `/language:${{ matrix.language }}`.
- A header comment cites DEPS-04/G-46: "advanced setup chosen because default setup is a repo-settings change; do not enable default setup while this file exists (GitHub rejects advanced uploads then)."
- The file must not contain `${{` inside any `run:` (it has none) and must contain no secrets.

Write test_codeql_workflow.py per the behavior block, and edit `_MUST_HAVE_FALSE` and its docstring.

Create the zizmor venv the verify uses: `python -m venv "$TEMP/venv-zizmor"`, then `"$TEMP/venv-zizmor/Scripts/python" -m pip install zizmor==1.10.0` (the package and version ci.yml:98 runs).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_codeql_workflow.py pipeline/tests/test_workflow_guards.py -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-sha-pins.sh && bash .github/scripts/check-secret-hygiene.sh && GH_TOKEN=$(gh auth token) "$TEMP/venv-zizmor/Scripts/zizmor" --persona=regular .github/workflows/</automated>
    Expected: all commands exit 0. zizmor, run with GH_TOKEN so its online audits match CI, reports no findings at persona regular. The pytest negative control (write-all) fails as designed inside its own test.
  </verify>
  <done>codeql.yml passes all four workflow gates, the structure pins are green, and the persist-credentials guard covers the new checkout.</done>
</task>

<task type="auto">
  <name>Task 2: DEPLOYMENT.md policy + CodeQL decision; gate, push, first CodeQL run</name>
  <files>DEPLOYMENT.md, .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
Edit DEPLOYMENT.md (preserve its line endings; check with `file DEPLOYMENT.md` first):

1. Under the DEP-01 Known-Gaps entry (:363-379), keep the history and append one paragraph: "**Resolved (v2.2 Phase 37, <date>):** astro is at <ver> (≥ 7.2.8, closes GHSA-26w7-cxv4-gfx2 critical and GHSA-376h-93r7-7g6f) via PR #39 (<sha7>). PRs #5/#7/#12 are long closed. The open-PR reminder role is replaced by the triage policy below."

2. Add a new §10 subsection `### Dependency triage policy (DEPS-04, v2.2 Phase 37)` with these rules:
   - Owner (G-56): the repository owner (xenaquis) runs the weekly triage, starting on the Monday after the v2.2 autonomous run ends (<first-check date>). The dated issue #<n> "Dependabot weekly triage — first check <date>" carries this checklist.
   - Cadence: weekly and manual, on Monday after Dependabot's weekly run (dependabot.yml: npm /site, pip /pipeline, github-actions, weekly, limit 5 each; the version PRs arrive on Saturdays around 14:53 UTC). No auto-merge: never `gh pr merge --auto`, no auto-merge workflow, no auto-approve bot.
   - Targets: ≤ 3 open Dependabot PRs and none older than 14 days, measured after triaging the Saturday wave; `npm audit --omit=dev` in site/ has 0 critical; a critical alert is handled the same day, alone, before anything else.
   - Order: security PRs first (framework/runtime alone), then patch/minor one at a time (a shared lockfile means serialize), then majors (owner only).
   - Merge criteria: every PR check SUCCESS on the head being merged, and the PR's base not older than the last site/, pipeline/ or .github/ change on master (otherwise comment `@dependabot rebase` and wait for the new checks); a PENDING check is waited for, never treated as a failure. Merge via `gh pr merge N --squash --match-head-commit <head>` (squash is the repo convention). Then `git pull --rebase --autostash origin master` and the full local gate as ONE command (OneDrive): `cd site && npm ci && npm run build && npm run validate && npx vitest run && npx astro check && cd .. && python -m pip install -q -r pipeline/requirements.txt && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-sha-pins.sh && bash .github/scripts/check-secret-hygiene.sh`. If the site changed, confirm the served build after the deploy: `data-build-now` on /news/ and /es/noticias/ must be ≥ the deploy-on-code run's createdAt, then curl the routes (G-51). If the fingerprint does not advance, check the Cloudflare Pages build log in the dashboard.
   - Revert rule (G-54): revert a merge only when a build, vitest, astro check, pytest or structural validator result that was green turns red. A red freshness/news-freshness validator is a news-pipeline incident, not a dependency failure; an install error under OneDrive is retried, not reverted.
   - Majors (G-55): reserved for the owner. An agent (the autonomous run or any scripted triage) merges only patch/minor and security-fix PRs; a major PR is closed with the reason "major upgrade reserved for owner review; reopen or @dependabot recreate when ready" and listed for the owner. No `@dependabot ignore` commands. The owner adopts a major in its own change; this matters most for CLAUDE.md-pinned stack items (astro, @astrojs/react, React, Leaflet, react-leaflet, TypeScript, Node) and the LLM client (openai). openai majors additionally need the DEPS-03 eval reproduction (identical commune/family/parse/null counts on golden_set_v2, or the same-day control rule G-50).
   - Closing: always with a written reason comment first (superseded / red CI with run URL / out-of-scope major). Never delete branches. Never force-push. Rollback = `git revert <merge sha>`.
   - Pipeline deps: after a merge touching pipeline/requirements.txt, the next scheduled news-pipeline run must succeed.
   - Record: one line per merge/close in the phase or ops log.

3. Add `### CodeQL decision (DEPS-04, G-46)`, stating:
   - CodeQL runs via ADVANCED setup (.github/workflows/codeql.yml: push/PR to master + weekly Monday, javascript-typescript + python, build-mode none, least privilege, SHA-pinned).
   - Default setup was not used because enabling it is a repository-settings change, which the autonomous run may not make.
   - If the owner later prefers default setup, delete codeql.yml in the same change, because GitHub rejects advanced-setup uploads while default setup is on.
   - Findings are triaged in the same weekly pass. The first-run alert counts by severity go here: <counts>.
   - Coverage (R-14): "Coverage: python + .js/.ts/.tsx/.mjs; .astro components (84, 10 with set:html) are not assumed analyzed; verified from the first run log: <files extracted / .astro present yes|no>". Read it from the javascript-typescript job log of the first run (`gh run view <id> --log`, searching for the extractor's file counts and any `.astro` path). The set:html review stays manual.

Then:
- Run 37-gate.sh (green) and commit `ci(37): CodeQL advanced setup + dependency triage policy (DEPS-04)`.
- `git pull --rebase --autostash origin master` and `git push` (G-47). Record C = `git log -1 --format=%H --grep='^ci(37): CodeQL' origin/master` (the SHA after the rebase).
- The orchestrator polls `gh run list --workflow codeql.yml --branch master --event push --limit 30 --json databaseId,headSha,status,conclusion` and selects the run with headSha == C (R-12), until it completes, capped at 30 min; the conclusion must be success. A cancelled run for C is accepted only if a later run on a descendant of C (`git merge-base --is-ancestor C <sha>`) is success. Do not push anything else before this run completes. Log `codeql_first_run: <id> sha=<C7> conclusion=<c>` in 37-TRIAGE-LOG.md.
- Then `gh api "repos/xenaquis/is-chile-safe/code-scanning/analyses?per_page=20"` must include the categories `/language:python` and `/language:javascript-typescript` for the pushed SHA.
- Record the open alert counts by severity: `gh api "repos/xenaquis/is-chile-safe/code-scanning/alerts?state=open&per_page=100"`. If that call returns 403 (the token scope measured in research), use the analyses' `results_count` and say so. Fill the <counts> placeholder and the Coverage line from the run log.
- Create the triage issue (G-56, decision (b); issue creation is allowed, repo settings are not touched): <date> = the first Monday (UTC) strictly after the projected run end = max(this plan's execution date, 2026-10-01, the earliest HYG-06 date in Phase 38, directive § Phase 38). Write the body in the scratchpad: the "Dependency triage policy" bullets verbatim, the literal gate chain, the DEPS-02 measurement command `gh pr list --author app/dependabot --state open --limit 50 --json number,createdAt`, and one line stating how <date> was computed and that the final run report retitles the issue (`gh issue edit`) if the run ends later. Then `gh issue create --title "Dependabot weekly triage — first check <date>" --body-file <file>`. No label is required. Fill <first-check date> and #<n> in the Owner bullet.
- Commit `docs(37): CodeQL first-run counts + triage owner`, `git pull --rebase --autostash origin master`, and push.
- Any CodeQL alert with security_severity critical or high becomes a deferred-live line in 37-07 (it is not fixed in this phase unless it is a one-line workflow fix).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && grep -n "^### Dependency triage policy (DEPS-04" DEPLOYMENT.md && grep -n "^### CodeQL decision (DEPS-04, G-46)" DEPLOYMENT.md && grep -q "No auto-merge" DEPLOYMENT.md && grep -q "Resolved (v2.2 Phase 37" DEPLOYMENT.md && ! grep -q "<counts>" DEPLOYMENT.md && ! grep -q "<first-check date>" DEPLOYMENT.md && ! grep -q "see the phase-37 gate" DEPLOYMENT.md && grep -q "major upgrade reserved for owner review" DEPLOYMENT.md && grep -q "Coverage: python" DEPLOYMENT.md && test "$(gh issue list --state open --search 'Dependabot weekly triage in:title' --json number --jq length)" -ge 1 && grep -qE "^codeql_first_run: [0-9]+ sha=[0-9a-f]{7}" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md && C=$(git log -1 --format=%H --grep='^ci(37): CodeQL' origin/master) && gh run list --workflow codeql.yml --branch master --event push --limit 30 --json headSha,status,conclusion | python -c "import json,sys,subprocess;R=json.load(sys.stdin);C=sys.argv[1];r=[x for x in R if x['headSha']==C];print(r[:1]);d=lambda s:subprocess.call(['git','merge-base','--is-ancestor',C,s])==0;assert r and r[0]['status']=='completed' and (r[0]['conclusion']=='success' or (r[0]['conclusion']=='cancelled' and any(x['conclusion']=='success' and x['headSha']!=C and d(x['headSha']) for x in R)))" "$C" && gh api "repos/xenaquis/is-chile-safe/code-scanning/analyses?per_page=20" --jq '[.[].category]|unique|join(",")' | grep -q "/language:python" && gh api "repos/xenaquis/is-chile-safe/code-scanning/analyses?per_page=20" --jq '[.[].category]|unique|join(",")' | grep -q "/language:javascript-typescript" && git fetch -q origin && ! git status -sb | head -1 | grep -q ahead</automated>
    Expected: both sections exist, the stale entry is marked resolved, and the counts, the Coverage line, the owner date and issue number are filled in; the policy spells out the gate chain and the owner-reserved majors. The triage issue is open. The CodeQL push run for the ci(37) SHA (selected by headSha, not the newest run) succeeded, analyses exist for both languages, and nothing is left unpushed.
  </verify>
  <done>CodeQL is live without any settings change, the policy (owner, majors, gate chain) and decision (with coverage) are in DEPLOYMENT.md, and the dated triage issue exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| third-party action (github/codeql-action) → runner | runs with a security-events write token |
| workflow permissions | token scope granted to the new job |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-16 | Tampering | codeql-action supply chain | mitigate | Full 40-hex SHA pins (check-sha-pins.sh), peeled from the release tag; Dependabot github-actions keeps them current |
| T-37-17 | Elevation of privilege | job token | mitigate | Top-level permissions {} plus job contents:read/security-events:write only; persist-credentials false (pytest-pinned); zizmor regular clean |
| T-37-18 | Information disclosure | secrets in the workflow | mitigate | There are no secrets or run steps; check-secret-hygiene is in the gate |
| T-37-19 | Repudiation | settings change by the agent | mitigate | Advanced setup is a committed file only; the default-setup API is never called (G-46) |
| T-37-SC | Tampering | pip install zizmor==1.10.0 (local venv) | accept | The same package and version CI already runs (ci.yml:98); lint-only, in a scratch venv |
</threat_model>

<verification>
- The pytest pins, lint-workflows, check-sha-pins, zizmor and secret-hygiene all pass.
- The CodeQL push run for the ci(37) SHA succeeded, with analyses for both languages; zizmor ran with GH_TOKEN.
- DEPLOYMENT.md has the policy (owner, majors, literal gate chain) and the decision sections (with the coverage line), and the stale entry is resolved.
- The dated issue "Dependabot weekly triage — first check <date>" is open.
</verification>

<success_criteria>
DEPS-04 is met: the triage policy and the CodeQL decision are recorded in DEPLOYMENT.md, and CodeQL actually runs.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-06-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

- **R-08 + decision (b) (G-56):** Task 2 step 2 replaces "see the phase-37 gate" with the literal command chain, adds the Owner bullet (repo owner, from the Monday after the run ends, issue #<n>) and the Saturday cadence note; Task 2 creates one dated issue "Dependabot weekly triage — first check <date>" with the checklist (date rule: first Monday after max(execution date, 2026-10-01); retitled by the final report if the run ends later). Twins: must_haves truths 5-6, artifact, verify (issue + no placeholder + no "see the phase-37 gate"), done, verification.
- **Decision (a) (G-55):** the Majors policy bullet now reserves every major for the owner, gives the close text, and forbids `@dependabot ignore`.
- **R-01 / R-02 / R-03 / R-05 twins:** the policy's merge criteria add the stale-base rebase, "pending is waited for", the served-build fingerprint (G-51) and the G-54 revert rule.
- **R-12:** Task 2 selects the CodeQL run by headSha == C (the ci(37) commit), waits before any other push, accepts a cancelled run only with a successful descendant, logs `codeql_first_run:`; Task 1 runs zizmor with `GH_TOKEN=$(gh auth token)`. Twins: must_haves truths 2-3, interfaces, Task 1 verify, Task 2 verify, files_modified (+37-TRIAGE-LOG.md).
- **R-14:** the CodeQL decision gets the Coverage line (.astro 84 files, 10 with set:html, not assumed analyzed), read from the first run log; verify greps it.
- **R-09:** `git pull --rebase --autostash origin master` in Task 2.

## Revision R2 (gate R1)

- **Non-blockers applied:** NB-13(a), the docstring count is 'eight', not 'seven' (ci.yml 3 + four single-checkout files + codeql.yml; re-measured test_workflow_guards.py:520-526); NB-13(c), the zizmor venv creation moved into the Task 1 action.
