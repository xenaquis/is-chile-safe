---
phase: 37-dependency-security-hygiene
plan: 05
type: execute
wave: 5
depends_on: [37-04]
files_modified:
  - pipeline/requirements.txt (only via PR #37 merge, only on MERGE decision)
  - .planning/phases/37-dependency-security-hygiene/37-DEPS03.md
  - .planning/phases/37-dependency-security-hygiene/eval/spend-ledger-37-05.json
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-03]
executor: opus (pipeline LLM client dependency, decision record); orchestrator (eval runs, scheduled-run polling)

must_haves:
  truths:
    - "The DEPS-03 comparison is declared before any spend (G-48). Run B = the current master classifier.py with the openai version on PR #37's head, installed in a scratch venv, running eval_classifier.py on the default golden_set_v2 (44 labelled + 3 null) with the baseline's provider/model/reasoning variant. MERGE iff B.commune_correct, B.family_correct, B.parse_errors and B.null_correct are EXACTLY equal to 36-DEPS03-BASELINE.json, AND B.empty_content_count == B.finish_length_count == B.api_errors == 0, AND B.status == COMPLETE, AND pytest pipeline under the same venv exits 0, AND PR #37's checks are all SUCCESS on its current head. Anything else means CLOSE with a written reason. served_by_counts is reported only, never compared."
    - "The comparison is valid only if `git hash-object pipeline/news/classifier.py` at HEAD == baseline.classifier_blob. If they differ, STOP with no spend and report."
    - "Spend: expected ≈ USD 0.01 per run (G-16: 0.010017 / 47 items). Plan cap USD 0.05, enforced with --spend-cap on each run; at most 2 eval runs (B, then control A only on mismatch)."
    - "On mismatch, one control run A (the same HEAD, the pinned openai 2.53.0 env) is made so the close reason can say whether the delta is the library or provider variance. A never turns a mismatch into a merge."
    - "On MERGE, the next scheduled news-pipeline run on the merged master succeeds."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-DEPS03.md
      provides: "declared rule, pytest result under openai 3.x, B (and A) metrics vs the baseline, decision MERGE|CLOSE, spend"
  key_links:
    - from: 37-DEPS03.md decision
      to: .planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json
      via: "exact integer equality on 4 metrics (G-41, G-48)"
      pattern: "^\\*\\*Decision\\*\\*: (MERGE|CLOSE)"
---

<objective>
DEPS-03: decide PR #37 (openai 2.53.0 → 3.7.0, major) on evidence. Merge only if the NREC-01 eval reproduces the Phase-36 baseline metrics exactly under openai 3.x; otherwise close it with a written reason that carries the numbers.

Purpose: openai is the client for the live classifier (classifier.py:63 imports), clustering.py:45-46 and eval_classifier.py:41. PR #37's CI failed "Pipeline pytest" at open (2026-09-05, before the Phase-34 rewrite), so it must be re-triaged fresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-RESEARCH.md
@.planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json
@pipeline/experiments/eval_classifier.py

<interfaces>
- eval_classifier.py CLI (measured with --help on 2026-09-24): `--provider {openrouter,deepseek} --model MODEL --reasoning {auto,none,enabled_false,effort_none,thinking_disabled} --golden GOLDEN --out-dir OUT_DIR --spend-ledger SPEND_LEDGER --spend-cap SPEND_CAP [--probe] [--decide]`.
  - It writes `<out-dir>/run-<slug>.json` (:1034) with the keys commune_correct, family_correct, parse_errors, null_correct, empty_content_count, finish_length_count, api_errors, transient, served_by_counts, spend_usd and status (:585-617).
  - It loads pipeline/.env and then the root .env (:953-954); the root .env exists.
  - Do NOT pass --decide: it rewrites the Phase-34 results files.
- 36-DEPS03-BASELINE.json (G-41, produced by 36-06 Task 3) has the keys commune_correct, family_correct, parse_errors, null_correct, served_by_counts and classifier_blob, plus the model, variant and run file name. The Phase-34 winner for context is deepseek/deepseek-v4.1-flash on openrouter with reasoning enabled_false (G-16). Use the values stored in the baseline file, not these.
- The PR #37 head version is read at execution: `gh pr diff 37 -- pipeline/requirements.txt`, the `+openai==` line. Measured 3.7.0 on 2026-09-24; Dependabot may have rebased it to a later 3.x.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preconditions, declared rule, and pytest under openai 3.x (no spend)</name>
  <files>.planning/phases/37-dependency-security-hygiene/37-DEPS03.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-04-SUMMARY.md must exist. Then:
- 36-DEPS03-BASELINE.json must exist with the 6 keys above; if it does not, STOP: "DEPS-03 blocked: no G-41 baseline".
- `git hash-object pipeline/news/classifier.py` must equal baseline.classifier_blob; if it does not, STOP with no spend: "classifier.py changed since the baseline".
- `gh pr view 37 --json state` must be OPEN.

1. Refresh the PR: if #37 is CONFLICTING or older than master's last requirements.txt change (37-04 merged four PRs into that file), comment `@dependabot rebase` and wait up to 45 min for its checks to complete on the new head. Log the head SHA and each check conclusion.
2. Write 37-DEPS03.md with the rule from must_haves verbatim (G-48), the baseline values, the openai target version V, and the spend cap, before any eval call.
3. Create the scratch venv: `python -m venv "$SCR/venv-openai3"`, then install `pip install -r pipeline/requirements.txt "openai==V"` into it (the second argument overrides the pin). Log `pip show openai` from the venv. openai is an existing, already-pinned package from PyPI; this only moves it to the version Dependabot proposes.
4. Run `"$SCR/venv-openai3/Scripts/python" -m pytest pipeline -q -p no:cacheprovider` (PYTHONIOENCODING=utf-8). Record the exit code and, on failure, the failing test ids and the first error line.
5. If pytest is red, record Decision CLOSE (reason: "openai V breaks N pipeline tests: <ids>; first error <line>; migration needs its own change touching classifier.py/clustering.py error handling (pipeline kernel), outside Phase-37 scope"). Skip Task 2's eval (zero spend) and go to Task 2 step 4.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && test "$(grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md)" -eq 1 && python -c "import json,subprocess;b=json.load(open('.planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json',encoding='utf-8'));h=subprocess.check_output(['git','hash-object','pipeline/news/classifier.py'],text=True).strip();print(b['commune_correct'],b['family_correct'],b['parse_errors'],b['null_correct'],h==b['classifier_blob']);assert h==b['classifier_blob']" && grep -q "^## Declared rule (G-48)" .planning/phases/37-dependency-security-hygiene/37-DEPS03.md && grep -qE "^pytest_openai3: exit [0-9]+" .planning/phases/37-dependency-security-hygiene/37-DEPS03.md</automated>
    Expected: the baseline is readable, the blob matches, the rule is declared, and the pytest result is recorded.
  </verify>
  <done>The rule is fixed before spend, and the library compatibility is known from tests.</done>
</task>

<task type="auto">
  <name>Task 2: Eval B (and control A on mismatch), decide, act</name>
  <files>.planning/phases/37-dependency-security-hygiene/37-DEPS03.md, .planning/phases/37-dependency-security-hygiene/eval/spend-ledger-37-05.json, .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md, pipeline/requirements.txt (via merge)</files>
  <action>
Run only if pytest_openai3 exit 0.

1. Run B with the venv python: `pipeline/experiments/eval_classifier.py --provider <baseline.provider or openrouter> --model <baseline.model> --reasoning <baseline variant> --out-dir "$SCR/deps03-B" --spend-ledger .planning/phases/37-dependency-security-hygiene/eval/spend-ledger-37-05.json --spend-cap 0.05`. Copy run-*.json to the phase eval/ dir as run-B.json.
2. Compare per the rule. If everything is equal, Decision MERGE. Otherwise run control A with the system python (openai 2.53.0 pinned) and the same arguments, `--out-dir "$SCR/deps03-A"`, the same ledger and the same cap, where the ledger total must stay ≤ 0.05. Then Decision CLOSE.
3. Write into 37-DEPS03.md:
   - a metric table (baseline / B / A);
   - `**Decision**: MERGE` or `**Decision**: CLOSE`;
   - spend_usd from the ledger;
   - on CLOSE, a variance note: "A == baseline" means the delta is attributable to openai 3.x; "A != baseline" means provider variance, and a strict-identity reproduction is not achievable on this date.
4. Act:
   - MERGE: `37-merge-pr.sh 37` (its checks must be SUCCESS on the head; if they are not, CLOSE instead, citing the check). Then `git pull --rebase` and 37-gate.sh green (system python now reinstalls openai V via the gate's pip install), and push. The orchestrator then polls for the first scheduled news-pipeline run after the push (≤ 7 h), which must be success; if it is red and the log shows an openai error, `git revert` the merge, gate, push, and record "reverted".
   - CLOSE: write the reason file: "DEPS-03 (v2.2 Phase 37): openai <V> not merged. Rule declared before the run: identical commune/family/parse/null counts vs 36-DEPS03-BASELINE.json on golden_set_v2 (G-41/G-48). Result: <baseline → B (→ A)> <or the pytest failure>. <variance note>. Revisit as a dedicated migration; Dependabot will re-propose." Then run `37-close-pr.sh 37 <file>`.
5. Record the spend in the directive's RUN STATUS "LLM spend" line (G-48). Commit `docs(37): DEPS-03 decision` and push.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && D=.planning/phases/37-dependency-security-hygiene && grep -qE "^\*\*Decision\*\*: (MERGE|CLOSE)" $D/37-DEPS03.md && s=$(gh pr view 37 --json state -q .state) && echo "#37 $s" && [ "$s" != OPEN ] && { grep -q "^\*\*Decision\*\*: MERGE" $D/37-DEPS03.md && [ "$s" = MERGED ] || { grep -q "^\*\*Decision\*\*: CLOSE" $D/37-DEPS03.md && [ "$s" = CLOSED ]; }; } && { [ ! -f $D/eval/spend-ledger-37-05.json ] || python -c "import json;d=json.load(open('$D/eval/spend-ledger-37-05.json'));print('spend',d['total_usd']);assert d['total_usd']<=0.05"; } && { [ "$s" != MERGED ] || python -c "import json;b=json.load(open('.planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json',encoding='utf-8'));r=json.load(open('$D/eval/run-B.json',encoding='utf-8'));k=('commune_correct','family_correct','parse_errors','null_correct');assert all(r[x]==b[x] for x in k) and r['empty_content_count']==r['finish_length_count']==r['api_errors']==0 and r['status']=='COMPLETE'"; }</automated>
    Expected: the Decision line matches the PR state (MERGE→MERGED, CLOSE→CLOSED), spend ≤ USD 0.05, and a MERGE is only possible with exact metric equality. On MERGE, 37-gate.sh is green and the scheduled-run success is logged.
  </verify>
  <done>#37 is resolved per the pre-declared rule, with numbers in the record and spend within the cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| openai SDK → OpenRouter / DeepSeek | the live classifier's transport and error typing |
| eval → external LLM spend | metered API |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-12 | Denial of service | classifier error-handling drift under openai 3.x (G-03/G-09 typed outcomes) | mitigate | pytest under a 3.x venv before any spend; exact-metric merge rule; live scheduled run; revert path |
| T-37-13 | Information disclosure | API keys in eval logs | mitigate | Keys come from .env via load_dotenv only; no key is echoed; check-secret-hygiene is in the gate |
| T-37-14 | Tampering | baseline mismatch hiding a prompt change | mitigate | Assert classifier_blob == HEAD blob before spend |
| T-37-15 | Denial of service (cost) | runaway eval | mitigate | --spend-cap 0.05 per run with a shared ledger; ≤ 2 runs |
| T-37-SC | Tampering | pip install openai==V in the venv | accept | An existing dependency at the Dependabot-proposed version, isolated in a scratch venv |
</threat_model>

<verification>
- 37-DEPS03.md has the declared rule, the metrics and the Decision; #37's state matches the Decision.
- Ledger total ≤ USD 0.05, and the RUN STATUS spend is updated.
</verification>

<success_criteria>
DEPS-03 is met in either branch: merged with identical metrics, or closed with a written, numeric reason.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-05-SUMMARY.md` when done.
</output>
