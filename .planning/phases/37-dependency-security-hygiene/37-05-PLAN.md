---
phase: 37-dependency-security-hygiene
plan: 05
type: execute
wave: 5
depends_on: [37-04]
files_modified:
  - pipeline/requirements.txt (only via the openai PR merge (#37 or its successor), only on MERGE decision)
  - .planning/phases/37-dependency-security-hygiene/37-DEPS03.md
  - .planning/phases/37-dependency-security-hygiene/eval/spend-ledger-37-05.json
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-03]
executor: opus (pipeline LLM client dependency, decision record); orchestrator (eval runs, scheduled-run polling)

must_haves:
  truths:
    - "The DEPS-03 comparison is declared before any spend (G-48, G-50, G-57). N = the open Dependabot PR whose title contains 'openai from 2.53.0' (#37 or its successor, R-07), resolved at Task 1 start and logged as `deps03_pr: #N V=<v>`; V = the `+openai==` version in N's diff. Run B = the current master classifier.py with openai V in a scratch venv installed from a copy of requirements.txt whose openai line is replaced by openai==V, running eval_classifier.py on the default golden_set_v2 (44 labelled + 3 null) with the baseline's provider/model/reasoning variant. Decision table (G-50), the four counts being commune_correct, family_correct, parse_errors and null_correct: B == baseline → MERGE; A == baseline and B != baseline → CLOSE; A != baseline and B == A → MERGE; anything else → CLOSE. Every MERGE also needs empty_content_count == finish_length_count == api_errors == 0 and status == COMPLETE in each run compared, pytest pipeline exit 0 under the same venv, and N's checks all SUCCESS on its current head. served_by_counts is reported only, never compared."
    - "Before any spend (Task 2 step 0, R-11), `git hash-object pipeline/news/classifier.py` at HEAD must equal baseline.classifier_blob; if they differ, STOP with no spend and report. The zero-spend CLOSE path (pytest does not pass under openai V) does not use the baseline and is not blocked by a blob mismatch."
    - "Spend: expected ≈ USD 0.01 per run (G-16: 0.010017 / 47 items). Plan cap USD 0.05 on a shared ledger, enforced with --spend-cap on each run; at most 2 runs of each of B and A (G-50)."
    - "Control A (same HEAD, pinned openai 2.53.0, same day, same provider) runs only when B != baseline. A == baseline attributes the delta to the library (CLOSE); A != baseline means provider drift, and B is then compared to A (MERGE iff B == A with errors 0 in both)."
    - "A failed venv install or import is an instrument error: STOP and report, never CLOSE. The CLOSE reason names the measured cause: a collection failure (openai 3.x depends on httpx2, not httpx) is not reported as a production-kernel break (R-04)."
    - "On MERGE, the next scheduled news-pipeline run on the merged master succeeds."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-DEPS03.md
      provides: "declared rule, pytest result under openai 3.x, B (and A) metrics vs the baseline, decision MERGE|CLOSE, spend"
  key_links:
    - from: 37-DEPS03.md decision
      to: .planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json
      via: "exact integer equality on 4 metrics vs the baseline, or vs the same-day control A when A drifted (G-41, G-48, G-50)"
      pattern: "^\\*\\*Decision\\*\\*: (MERGE|CLOSE)"
---

<objective>
DEPS-03: decide the open Dependabot openai 2→3.x PR (#37 at 3.7.0 on 2026-09-24, or its successor) on evidence. Merge only per the G-50 table: the NREC-01 eval reproduces the Phase-36 baseline metrics exactly under openai 3.x, or matches a same-day control A when the provider itself drifted. Otherwise close it with a written reason that carries the numbers or the measured failure.

Purpose: openai is the client for the live classifier (classifier.py:63 imports), clustering.py:45-46 and eval_classifier.py:41. PR #37's CI failed "Pipeline pytest" at open (2026-09-05, before the Phase-34 rewrite). The failed run 33973132996 shows the measured cause: openai 3.7.0 installs httpx2/httpcore2 instead of httpx, and collection stops at `import httpx`. It must be re-triaged fresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-RESEARCH.md
@.planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json
@pipeline/experiments/eval_classifier.py

<interfaces>
- eval_classifier.py CLI (measured with --help on 2026-09-24): `--provider {openrouter,deepseek} --model MODEL --reasoning {auto,none,enabled_false,effort_none,thinking_disabled} --golden GOLDEN --out-dir OUT_DIR --spend-ledger SPEND_LEDGER --spend-cap SPEND_CAP [--probe] [--decide]`.
  - It writes `<out-dir>/run-<slug>.json` (:1034) with the keys commune_correct, family_correct, parse_errors, null_correct, empty_content_count, finish_length_count, api_errors, transient, served_by_counts, spend_usd and status (:585-617).
  - It loads pipeline/.env and then the root .env (:953-954); the root .env exists.
  - Do NOT pass --decide: it rewrites the Phase-34 results files.
- 36-DEPS03-BASELINE.json (G-41, produced by 36-06 Task 3) has the keys commune_correct, family_correct, parse_errors, null_correct, served_by_counts and classifier_blob, plus the model, variant and run file name. The Phase-34 winner for context is deepseek/deepseek-v4.1-flash on openrouter with reasoning enabled_false (G-16). Use the values stored in the baseline file, not these.
- The PR is resolved at execution by title, not number (R-07): `gh pr list --author app/dependabot --state open --search 'openai in:title' --json number,title`, filtered to titles containing "openai from 2.53.0" → N. V = the `+openai==` line of `gh pr diff N`. Measured #37 at 3.7.0 on 2026-09-24; PyPI latest is 3.19.2 and the pip queue sits at open-pull-requests-limit 5, so Dependabot is likely to supersede #37 with another number once 37-04 frees slots (history: #22 3.0.0 → #25 3.3.0 → #32 3.3.1 → #37 3.7.0).
- pipeline/requirements.txt:12 pins `openai==2.53.0` (pytest==9.* at :10). `pip install -r requirements.txt "openai==V"` fails with ResolutionImpossible (rc=1, premortem). Installing from a copy with the openai line replaced resolves (dry-run 2026-09-25, openai 3.7.0: openai 3.7.0 + httpx2 2.13.1 + httpcore2 2.13.1, no httpx).
- Five test modules import httpx: test_classifier.py:13, test_classifier_router.py:19, test_clustering.py:17, test_news_health_gate.py:26 and test_scrape_news.py:798 (function-level import).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preconditions, declared rule, and pytest under openai 3.x (no spend)</name>
  <files>.planning/phases/37-dependency-security-hygiene/37-DEPS03.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-04-SUMMARY.md must exist. Then:
- 36-DEPS03-BASELINE.json must exist with the 6 keys above; if it does not, STOP: "DEPS-03 blocked: no G-41 baseline". Only its existence is required here; the classifier_blob check moved to Task 2 step 0, before any spend (R-11).
- Resolve N (interfaces block). If no open Dependabot PR has a title containing "openai from 2.53.0", STOP: "DEPS-03 blocked: no open openai 2→3.x PR" and report. Read V from `gh pr diff N` and write `deps03_pr: #N V=<V>` into 37-DEPS03.md.

1. Refresh the PR: run `37-merge-pr.sh --check-only --allow-major N`. On exit 3 or 6 (CONFLICTING, or a stale base after 37-04 merged four PRs into requirements.txt), comment `@dependabot rebase` and wait up to 45 min, polling every 2 min, for its checks to complete on the new head (exit 5 keeps polling). Log the head SHA and each check conclusion. If the rebase changes V, update the `deps03_pr:` line.
2. Write 37-DEPS03.md with the rule from must_haves verbatim under `## Declared rule (G-48/G-50)`, including the G-50 decision table, the baseline values, N, the openai target version V, and the spend cap, before any eval call.
3. Create the scratch venv (R-04): `python -m venv "$SCR/venv-openai3"`; `sed 's/^openai==.*/openai==V/' pipeline/requirements.txt > "$SCR/req-openai3.txt"`; then `"$SCR/venv-openai3/Scripts/python" -m pip install -r "$SCR/req-openai3.txt"`. Do NOT pass `"openai==V"` as a second argument: pip rejects two conflicting pins (ResolutionImpossible). openai is an existing, already-pinned package from PyPI; this only moves it to the version Dependabot proposes. Before step 4, assert in that venv that `pip check` exits 0, that `pip show openai` reports Version == V, and that `python -m pytest --version` exits 0. Then write `pip_openai: V=<V> pip_check=0`. If the install, `pip check` or an import fails, that is an instrument error: STOP and report, never CLOSE.
4. Run `"$SCR/venv-openai3/Scripts/python" -m pytest pipeline -q -p no:cacheprovider` (PYTHONIOENCODING=utf-8). Record `pytest_openai3: exit <n> collection_errors=<k> failed=<f>`, keeping collection errors (exit 2 / "Interrupted: N errors during collection") separate from test failures, and write the first error line verbatim (expected from run 33973132996: `ModuleNotFoundError: No module named 'httpx'`).
5. If pytest does not exit 0, record Decision CLOSE with the reason that matches what was measured:
   - Collection errors on `import httpx`: "under openai V the suite does not collect: openai 3.x depends on httpx2, not httpx, and <k> test modules import httpx (<module ids>); the production kernel was not exercised; migration = port test fixtures to httpx2 + re-verify typed errors/failover".
   - Collected tests that fail: "openai V fails <f> pipeline tests: <ids>; first error <line>". Name classifier.py/clustering.py only if the failing tests exercise them.
   Skip Task 2's eval (zero spend) and go to Task 2 step 4.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && test "$(grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md)" -eq 1 && D=.planning/phases/37-dependency-security-hygiene && python -c "import json,subprocess,re;b=json.load(open('.planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json',encoding='utf-8'));assert all(k in b for k in ('commune_correct','family_correct','parse_errors','null_correct','served_by_counts','classifier_blob'));t=open('.planning/phases/37-dependency-security-hygiene/37-DEPS03.md',encoding='utf-8').read();h=subprocess.check_output(['git','hash-object','pipeline/news/classifier.py'],text=True).strip();ok=re.search('^pytest_openai3: exit 0 ',t,re.M);print(b['commune_correct'],b['family_correct'],b['parse_errors'],b['null_correct'],'blob_match',h==b['classifier_blob'],'pytest_ok',bool(ok));assert (not ok) or h==b['classifier_blob']" && grep -q "^## Declared rule (G-48" $D/37-DEPS03.md && grep -qE "^deps03_pr: #[0-9]+ V=[0-9.]+" $D/37-DEPS03.md && grep -qE "^pip_openai: V=[0-9.]+ pip_check=0" $D/37-DEPS03.md && grep -qE "^pytest_openai3: exit [0-9]+ collection_errors=[0-9]+ failed=[0-9]+" $D/37-DEPS03.md</automated>
    Expected: the baseline is readable with its 6 keys; the blob is asserted only when pytest passed under openai V (the zero-spend CLOSE path is not blocked by a blob mismatch, R-11); the rule is declared; N and V are logged; the venv is proven sound (`pip_openai: V=… pip_check=0`, so a CLOSE can never come from a broken instrument); and the pytest result is recorded with collection errors separate from failures.
  </verify>
  <done>The rule is fixed before spend, and the library compatibility is known from tests.</done>
</task>

<task type="auto">
  <name>Task 2: Eval B (and control A on mismatch), decide, act</name>
  <files>.planning/phases/37-dependency-security-hygiene/37-DEPS03.md, .planning/phases/37-dependency-security-hygiene/eval/spend-ledger-37-05.json, .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md, pipeline/requirements.txt (via merge)</files>
  <action>
Run only if pytest_openai3 exit 0.

0. Before any spend (R-11): `git hash-object pipeline/news/classifier.py` must equal baseline.classifier_blob; if it does not, STOP with no spend: "classifier.py changed since the baseline", and report.
1. Run B with the venv python: `pipeline/experiments/eval_classifier.py --provider <baseline.provider or openrouter> --model <baseline.model> --reasoning <baseline variant> --out-dir "$SCR/deps03-B" --spend-ledger .planning/phases/37-dependency-security-hygiene/eval/spend-ledger-37-05.json --spend-cap 0.05`. Copy run-*.json to the phase eval/ dir as run-B.json.
2. Decide with the G-50 table (R-10), on the four counts, with errors 0 and status COMPLETE in each run compared:
   - B == baseline → Decision MERGE.
   - Otherwise run control A with the system python (openai 2.53.0 pinned) and the same arguments, `--out-dir "$SCR/deps03-A"`, the same ledger and the same cap; copy its run-*.json to the phase eval/ dir as run-A.json. Then:
     - A == baseline and B != baseline → Decision CLOSE (library drift).
     - A != baseline and B == A → Decision MERGE (provider drift; openai 3.x neutral).
     - anything else → Decision CLOSE.
   - A run that ends with status != COMPLETE may be repeated once (at most 2 runs of each of B and A); the ledger total must stay ≤ 0.05.
3. Write into 37-DEPS03.md:
   - a metric table (baseline / B / A);
   - the table branch that applied;
   - `**Decision**: MERGE` or `**Decision**: CLOSE`;
   - spend_usd from the ledger;
   - a variance note: "A == baseline" means the delta is attributable to openai 3.x; "A != baseline" means provider variance, and the decision compares B to A.
4. Act:
   - MERGE: `37-merge-pr.sh --allow-major N` (the only major merged in the run, G-55; its checks must be SUCCESS on the head and its base not stale; if not, CLOSE instead, citing the check). Then `git pull --rebase --autostash origin master` and 37-gate.sh green (system python now reinstalls openai V via the gate's pip install; G-54 revert rule), and push. The orchestrator then polls for the first scheduled news-pipeline run after the push (≤ 7 h), which must be success; if it is red and the traceback names openai/httpx2, `git revert` the merge, gate, push, and record "reverted".
   - CLOSE: write the reason file: "DEPS-03 (v2.2 Phase 37): openai <V> not merged. Rule declared before the run: identical commune/family/parse/null counts vs 36-DEPS03-BASELINE.json on golden_set_v2, or vs a same-day control when the provider drifted (G-41/G-48/G-50). Result: <baseline → B (→ A)> <or the Task 1 step 5 reason>. <variance note>. Revisit as a dedicated migration; Dependabot will re-propose." Then run `37-close-pr.sh N <file>`.
5. Record the spend in the directive's RUN STATUS "LLM spend" line (G-48). Commit `docs(37): DEPS-03 decision`, then `git pull --rebase --autostash origin master` and push (R-09).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && D=.planning/phases/37-dependency-security-hygiene && grep -qE "^\*\*Decision\*\*: (MERGE|CLOSE)" $D/37-DEPS03.md && N=$(grep -oE "^deps03_pr: #[0-9]+" $D/37-DEPS03.md | grep -oE "[0-9]+$") && s=$(gh pr view $N --json state -q .state) && echo "#$N $s" && [ "$s" != OPEN ] && { grep -q "^\*\*Decision\*\*: MERGE" $D/37-DEPS03.md && [ "$s" = MERGED ] || { grep -q "^\*\*Decision\*\*: CLOSE" $D/37-DEPS03.md && [ "$s" = CLOSED ]; }; } && { [ ! -f $D/eval/spend-ledger-37-05.json ] || python -c "import json;d=json.load(open('$D/eval/spend-ledger-37-05.json'));print('spend',d['total_usd']);assert d['total_usd']<=0.05"; } && { [ "$s" != MERGED ] || python -c "import json,os;D='$D';b=json.load(open('.planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json',encoding='utf-8'));k=('commune_correct','family_correct','parse_errors','null_correct');ok=lambda r:r['empty_content_count']==r['finish_length_count']==r['api_errors']==0 and r['status']=='COMPLETE';eq=lambda x,y:all(x[i]==y[i] for i in k);B=json.load(open(D+'/eval/run-B.json',encoding='utf-8'));pa=D+'/eval/run-A.json';A=json.load(open(pa,encoding='utf-8')) if os.path.exists(pa) else None;assert ok(B) and (eq(B,b) or (A is not None and ok(A) and not eq(A,b) and eq(B,A)))"; }</automated>
    Expected: the Decision line matches the state of the PR recorded in `deps03_pr:` (MERGE→MERGED, CLOSE→CLOSED), spend ≤ USD 0.05, and a MERGE is only accepted per the G-50 table: B == baseline, or A != baseline and B == A (read from eval/run-A.json), with errors 0 and COMPLETE. On MERGE, 37-gate.sh is green and the scheduled-run success is logged.
  </verify>
  <done>The openai PR (#37 or its successor) is resolved per the pre-declared G-50 table, with numbers (or the measured failure) in the record and spend within the cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| openai SDK → OpenRouter / DeepSeek | the live classifier's transport and error typing |
| eval → external LLM spend | metered API |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-12 | Denial of service | classifier error-handling drift under openai 3.x (G-03/G-09 typed outcomes) | mitigate | pytest under a 3.x venv before any spend; exact-metric merge rule; live scheduled run; revert path |
| T-37-13 | Information disclosure | API keys in eval logs | mitigate | Keys come from .env via load_dotenv only; no key is echoed; check-secret-hygiene is in the gate |
| T-37-14 | Tampering | baseline mismatch hiding a prompt change | mitigate | Assert classifier_blob == HEAD blob before spend (Task 2 step 0) |
| T-37-15 | Denial of service (cost) | runaway eval | mitigate | --spend-cap 0.05 per run with a shared ledger; ≤ 2 runs of each of B and A |
| T-37-SC | Tampering | pip install openai==V in the venv | accept | An existing dependency at the Dependabot-proposed version, installed from a sed-edited copy of requirements.txt into a scratch venv; `pip check` must pass |
</threat_model>

<verification>
- 37-DEPS03.md has the declared rule (G-48/G-50 table), `deps03_pr:`, `pip_openai: … pip_check=0`, the metrics or the measured failure, and the Decision; that PR's state matches the Decision.
- Ledger total ≤ USD 0.05, and the RUN STATUS spend is updated.
</verification>

<success_criteria>
DEPS-03 is met in either branch: merged under the G-50 table (B == baseline, or B == A when A drifted), or closed with a written reason carrying the numbers or the measured cause.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-05-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

- **R-04 (G-57):** Task 1 step 3 installs from `$SCR/req-openai3.txt` (sed-replaced openai line) instead of two conflicting pins; asserts `pip check` 0, Version == V, `pytest --version` 0 and logs `pip_openai: V=<v> pip_check=0`; an install/import failure is an instrument error (STOP, never CLOSE). Step 4 records collection errors separately from failures, with the first error line verbatim. Step 5 CLOSE reason names the measured cause (httpx → httpx2; kernel not exercised). Re-measured 2026-09-25: pip dry-run from the sed copy resolves openai 3.7.0 + httpx2/httpcore2 2.13.1 and no httpx; 5 test modules import httpx. Twins: must_haves truth 5 (new), interfaces, verify, threat T-37-SC, verification, success_criteria.
- **R-07 (G-57):** N resolved by title "openai from 2.53.0" (#37 or successor), logged as `deps03_pr: #N V=<v>`; V read from `gh pr diff N` (the old `gh pr diff 37 -- <path>` form is not a valid gh invocation). Twins: files_modified, truth 1, objective, interfaces, precondition, Task 2 act + verify, done, verification.
- **R-10 (G-50):** must_haves truths 1/3/4, Task 2 step 2 and the MERGED verify now follow the three-branch G-50 table (B == baseline → MERGE; A == baseline ≠ B → CLOSE; A ≠ baseline and B == A → MERGE; else CLOSE), ≤ 2 runs of each, verify reads eval/run-A.json. key_links via updated.
- **R-11:** the classifier_blob check moved from Task 1 step 0 to Task 2 step 0 (before spend); Task 1 verify asserts the blob only when `pytest_openai3: exit 0`. Twins: truth 2, threat T-37-14.
- **Decision (a) (G-55):** the MERGE branch uses `37-merge-pr.sh --allow-major N`, the only major merged in the run.
- **R-09 / R-05:** `--autostash` and the G-54 revert rule on the MERGE branch.

## Revision R2 (gate R1)

- **R-09 twin (previous edit not applied):** Task 2 step 5's docs push uses `git pull --rebase --autostash origin master`.
