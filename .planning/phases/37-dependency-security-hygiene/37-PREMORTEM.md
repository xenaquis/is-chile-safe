---
phase: 37
artifact: premortem
plans_evaluated: 37-01..37-07 @ 8a7eff6
method: 3 opus perspectives (técnica, metodológica, integración) + fresh opus arbiter (xhigh)
risks: 17
subagent_tokens: 715570
---

# Phase 37 — Premortem

## Arbiter summary

Arbitraje del premortem de la Fase 37 sobre 8a7eff6. Leí completos los 7 planes (37-01..37-07), G-41 y G-46..G-50 y las reglas duras de la directiva, REQUIREMENTS DEPS-01..04, ROADMAP § Phase 37, la sección de merge de 37-RESEARCH y la línea base de 36-06.

Contra el repo y el estado vivo medí, siempre en modo lectura:
- deploy-on-code.yml, ci.yml, dependabot.yml, freshness.mjs y all.mjs.
- Los 17 PRs de Dependabot abiertos con su baseRefOid y los cerrados.
- El log del pytest que falló en #37 (run 33973132996). openai 3.7.0 instala httpx2/httpcore2 en lugar de httpx, y la colección se corta en `import httpx`.
- `pip install --dry-run` con dos pines en conflicto, que da ResolutionImpossible (rc=1).
- `npm audit fix --dry-run`, que crashea con "Cannot read properties of null (reading 'edgesOut')".
- `npm outdated`: 9 paquetes, entre ellos @astrojs/react 7.0.0, vitest 5.0.1 y astro 7.3.5.
- `pip index versions openai`: la última es 3.19.2, pero #37 sigue en 3.7.0. La cola de Dependabot está congelada en el límite de 5.
- El createdAt de los PRs de versión: todos son sábados a las ~14:53Z.
- En prod, /news/: `data-build-now` sigue a la corrida de deploy-on-code con unos 40 s de diferencia. origin/master = 8a7eff6, un commit de docs sin corrida de deploy-on-code.

No escribí en el working tree ni en data/, no hice push y no llamé a ningún LLM. Dos salvedades: hice `git fetch` (solo actualiza refs remotas en .git) y dejé un r.txt en el scratchpad más un log en npm-cache.

Lo que funciona bien: la mecánica de merge (--match-head-commit, autor Dependabot, sin --auto/--admin, revert en vez de force), la serialización por lockfile y el cierre con motivo escrito.

Falla la instrumentación. Hay un P0: 37-served-routes.sh miente en las dos direcciones.
- Cuando el sistema está sano, falla por construcción: 37-02, 37-03 y 37-07 lo corren sobre commits de docs que nunca disparan deploy-on-code.
- Cuando pasa, no prueba nada: el éxito de deploy-on-code solo significa que el hook respondió 2xx, y el build viejo de Cloudflare sigue dando 200.
- Ya existe una huella barata que resuelve las dos cosas (`data-build-now`) y ningún plan la usa.

Hay 7 P1:
- R-02: los checks del PR son de una base vieja (#39 está 144 commits y 39 archivos de site/ atrás) y prod despliega antes del gate.
- R-03: el exit 4 mezcla PENDING/UNKNOWN con FAILURE, así que cierra PRs de seguridad sanos.
- R-04: el venv de DEPS-03 no se puede instalar, el verify acepta cualquier exit y el motivo del CLOSE atribuye mal la causa (httpx→httpx2, no el kernel).
- R-05: un freshness a 48 h o un EPERM de npm ci en OneDrive disparan reverts y cierres de fixes de seguridad.
- R-06: `npm audit fix` crashea hoy, y G-49 no autoriza ninguna alternativa (nanoid, alta, fixable).
- R-07: #37 está fijado por número y la cola congelada lo va a reemplazar en cuanto 37-04 la vacíe.
- R-08: DEPS-02 es una foto tomada antes de la ola del sábado y la política no tiene responsable.

Hay 6 P2 a mitigar, todos con ediciones baratas:
- R-09: `git pull --rebase` con el log sin commitear.
- R-10: G-50 contra 37-05.
- R-11: el precondicional de blob bloquea el CLOSE de gasto cero.
- R-12: la carrera de CodeQL y zizmor sin token.
- R-13: r2-archive, scipy y lxml quedan sin prueba en vivo.
- R-14: CodeQL no cubre .astro.

Se aceptan con razón R-15 (el toolchain local no es el de CI), R-16 (la rama MERGE de openai 3.x es inalcanzable en la ruta por defecto) y R-17 (G-47 sin firma del owner, delegado por G-35).

Hipótesis del planner que quedan falsadas:
- "El segundo argumento pisa el pin" (37-05:73).
- "npm audit fix es el fix compatible" (37-03:57).
- "lxml solo lo usa CEAD" (37-04:53).
- "Dependabot corre el lunes" (I-06).

Descarto M-12: cerrar un PR de versión ignora esa release, no el major. También descarto sub-afirmaciones de I-06, M-08, T-07(b) e I-07.

Mi pronóstico sin ediciones: la fase se traba en el verify de 37-02 (served-routes sobre un SHA de docs) y en 37-03 Task 2 (nanoid). O cierra con un 'prod verificado' que no prueba el build y un DEPS-02 que deja de ser cierto al sábado siguiente. Las ediciones de R-01, R-03, R-05 y R-06 cambian G-47 y G-49, así que hay que registrarlas como G-51+ antes de ejecutar.

## Final risks

| id | sev | decision | origin | plans | scenario | plan edit / acceptance |
|---|---|---|---|---|---|---|
| R-01 | P0 | MITIGAR | T-01 + M-03 + I-01 (SHA sin corrida) + T-02 + M-04 + I-03 (PASS vacuo) + T-11 (cola de CF, dedup) | 37-01 (Task 1 script + verify, Task 2 paso 5), 37-02 (acción :76, verify :79), 37-03 (Task 2 paso 4, verify), 37-07 (must_have :24, paso 2) | 37-served-routes.sh exige una corrida de deploy-on-code con headSha == SHA (37-01-PLAN.md:109). Pero 37-02 (acción :76, verify :79), 37-03 (Task 2 :94 y verify :98) y 37-07 (:70) lo invocan sobre origin/master después de pushear commits docs(37). Esos commits solo tocan .planning/, y deploy-on-code.yml:27-39 solo escucha site/**, su propio yml, current.json, archive/** y data/cead/**. Los commits de datos del bot usan GITHUB_TOKEN y [skip ci] (:33-36). Medido hoy: origin/master = 8a7eff6 (docs) no tiene corrida de deploy-on-code, y la última corrida es 396eef9. Resultado: FAIL por construcción con prod sano, y el ejecutor improvisa el SHA. En la otra dirección, deploy-on-code.yml:72-73 es solo un `curl -X POST` al hook. Si el build de Cloudflare falla, prod sigue sirviendo el build anterior: 8 rutas 200, 404 OK, el MapIsland viejo 200 y 'Latest incident: ' presente (medido: 1 match). El script da PASS y 37-07 firma 'prod serves the merged build' (37-01:24, :70) sin evidencia. Con 1 build concurrente en el plan free, los rebuilds de preview que Dependabot dispara tras cada merge de lockfile pueden encolar el build de prod más allá del tope de 15 min. Ya existe una huella de build que el plan no usa: news.astro:79/:284 y es/noticias.astro:78/:283 emiten data-build-now. Medido: prod 2026-09-25T00:14:49Z contra la corrida de deploy-on-code de 00:14:10Z. | 37-01 Task 1, nuevo contrato de 37-served-routes.sh SHA:<br>(a) Resolver D = `git log -1 --format=%H SHA -- site data/incidents/current.json data/incidents/archive data/cead .github/workflows/deploy-on-code.yml` y exigir la corrida de deploy-on-code con headSha == D en completed/success. Imprimir `deployable=<D>`.<br>(b) Tomar T0 = createdAt de esa corrida. Leer `data-build-now` de /news/ y de /es/noticias/ (con Cache-Control: no-cache) y exigir que sea ≥ T0. Mantener las 8 rutas, la 404, el MapIsland 200 y el grep 'Latest incident: '. Imprimir `built_at=<ts>`.<br>(c) Tope de 30 min, no 15.<br>(d) Si pasado el tope data-build-now < T0 (hook OK y build sin ver): NO revertir. Pausa (pause point 5) con pregunta al owner sobre el log de build en el dashboard de CF. Solo se revierte si la huella es nueva Y fallan rutas.<br><br>Verify de 37-01 Task 1, dos controles negativos:<br>- El script sobre 8a7eff6 debe imprimir deployable=<último SHA de site> y no fallar por 'no run'.<br>- Con FAKE_T0=now+1h debe dar FAIL por huella vieja.<br><br>37-01 Task 2 paso 5: aplicar la regla (d) en lugar de 'fails after 15 min → git revert'.<br><br>37-02/37-03/37-07: mantener la llamada sobre origin/master, que ahora el script resuelve a D.<br><br>37-07 must_have :24 y paso 2: registrar en 37-VERIFICATION `served_routes: PASS deployable=<D> built_at=<ts>`.<br><br>Registrar el nuevo contrato como G-51 antes de ejecutar. |
| R-02 | P1 | MITIGAR | T-03 + I-02 + M-14 (dedup) | 37-01 (script, Task 2 paso 1), 37-02 paso 2, heredado por 37-03/37-04/37-07 | 37-merge-pr.sh solo exige SUCCESS en el head (37-01:96). No exige que esos checks se hayan corrido contra el master actual, y master no tiene protección: `branches/master/protection` da 404 'Branch not protected'. Medido: la base de #39 es 5f48134 (2026-09-09), 144 commits y 39 archivos de site/ (+3784 líneas) detrás de origin/master, y sigue MERGEABLE. Todas las bases de los PRs abiertos son anteriores a las Fases 35/36. El lockfile no cambió desde ninguna base, así que el riesgo está en el código de site/ nuevo con la dependencia nueva y en los hunks de lockfile que mergean limpio sin rebase. El orden además está invertido: `gh pr merge` es un push con token de usuario, dispara deploy-on-code y prod recibe el árbol combinado ANTES del gate local (37-01:128-130; 37-02:72-73). ci.yml:3-5 no corre en push. Si el gate posterior sale rojo, prod ya sirvió un árbol que nadie construyó, o el plan revierte el fix de seguridad. | 37-01 Task 1, en 37-merge-pr.sh:<br>- Nuevo exit 6 'stale base' si `git diff --quiet <baseRefOid> origin/master -- site pipeline .github` falla, es decir, si hubo cambios de código desde la base con la que corrió el CI. Aplica también en --check-only.<br>- En 37-02 paso 2 (heredado por 37-03/04/07): ante exit 6, comentar `@dependabot rebase` y esperar hasta 45 min a que los checks del head nuevo terminen COMPLETED (la misma ruta que el exit 3). Así CI Linux y el preview de CF construyen el árbol real antes del merge.<br>- 37-01 Task 2 paso 1: #39 entra por esta ruta (medido: 39 archivos de site/ cambiados).<br>- Opcional, si el rebase no llega en 45 min: correr 37-gate.sh en un worktree del scratchpad (fuera de OneDrive), con origin/master + el head del PR fusionado localmente, ANTES de `gh pr merge`, y registrarlo en el log. |
| R-03 | P1 | MITIGAR | M-05 + I-04 (dedup) + hallazgo propio (mergeable UNKNOWN, checks de CodeQL en PRs tras 37-06) | 37-01 (script + verify), 37-02 paso 2, heredado por 37-03/37-04/37-07 | 37-01:96 da exit 4 tanto con una conclusión FAILURE como con 'a status other than COMPLETED'. 37-01:95 solo trata CONFLICTING (exit 3), y un mergeable UNKNOWN (GitHub lo recalcula después de cada push a master) cae en el camino de rechazo sin código definido. 37-02:71 manda cerrar ante exit 4 'naming the failing check'. Después de cada merge de lockfile, Dependabot rebasea los PRs de site en conflicto, y sus checks (CI + CF, y desde 37-06 también CodeQL con pull_request, 37-06:94) quedan IN_PROGRESS un buen rato. El ejecutor cierra un PR de seguridad sano (#42/#43/#41/#38) con un comentario público falso. El texto de 37-02:71 promete 'Dependabot re-proposes the fix on its next weekly run', una hipótesis sin falsar: las actualizaciones de seguridad no siguen el calendario semanal. La alerta queda abierta y cae en 37-03 Task 2, cuyo fallback crashea (R-06). | 37-01 Task 1, en 37-merge-pr.sh:<br>- exit 5 = PENDING: algún check con status ≠ COMPLETED, falta un check requerido o mergeable == UNKNOWN.<br>- exit 4 solo con una conclusión en {FAILURE, CANCELLED, TIMED_OUT, ACTION_REQUIRED, STARTUP_FAILURE} sobre el head actual, imprimiendo el detailsUrl.<br>- Agregar la opción `--rollup-json FILE` para test y, en el verify de 37-01, un control negativo: un fixture con un check IN_PROGRESS debe salir 5, nunca 4.<br><br>37-02 paso 2 (heredado):<br>- exit 5 → sondear cada 2 min hasta 45 min.<br>- exit 4 → leer `gh run view <id> --log-failed` y clasificar según R-05 antes de cerrar. Si la causa es freshness o instalación, no cerrar: `@dependabot rebase` una vez.<br>- Reemplazar 'Dependabot re-proposes the fix on its next weekly run' por 'the alert stays open; 37-03 Task 2 handles the residual'.<br>- En 37-07, contar y marcar como hallazgo todo PR de seguridad cerrado sin merge. |
| R-04 | P1 | MITIGAR | T-04 + M-02 (dedup), con evidencia propia | 37-05 Task 1 (pasos 3-5, verify), 37-07 (texto deferred-live '#37 closed') | 37-05:73 instala `pip install -r pipeline/requirements.txt "openai==V"` suponiendo que 'the second argument overrides the pin'. Falso: con requirements.txt:12 `openai==2.53.0`, pip aborta. Medido: dry-run con pines en conflicto → 'ERROR: ResolutionImpossible', rc=1. El venv queda vacío y el pytest de :74 falla por 'No module named pytest'. Entonces :75 registra 'Decision CLOSE: openai V breaks N pipeline tests', y el verify (:78, grep `pytest_openai3: exit [0-9]+`) acepta cualquier código, así que es un gate vacuo. Aunque el ejecutor arregle el venv, la causa real medida es otra. El log de la corrida 33973132996 muestra que openai 3.7.0 instala httpx2/httpcore2 en lugar de httpx, y la colección se interrumpe en test_clustering.py con 'No module named httpx'. Hay 5 módulos de test que importan httpx: test_classifier.py:13, test_classifier_router.py:19, test_clustering.py:17, test_news_health_gate.py:26 y test_scrape_news.py:798. El motivo que prescribe :75 ('classifier.py/clustering.py error handling, pipeline kernel') es falso, se publica en el PR y orienta mal el ítem deferred-live. | 37-05 Task 1 paso 3: copiar requirements.txt a $SCR/req-openai3.txt con la línea de openai reemplazada (`sed 's/^openai==.*/openai==V/'`) e instalar desde esa copia. Antes del paso 4, afirmar que `pip check` sale 0, que `pip show openai` da Version == V y que `python -m pytest --version` sale 0, y registrar `pip_openai: V=<v> pip_check=0`. Si falla la instalación o un import, es un error del instrumento: STOP y reporte, nunca CLOSE.<br><br>Paso 4: registrar por separado los errores de colección (exit 2 / 'Interrupted') y las fallas de tests, con la primera línea de error verbatim.<br><br>Paso 5, nueva plantilla de motivo: 'under openai V the suite does not collect: openai 3.x depends on httpx2, not httpx, and N test modules import httpx (<ids>); the production kernel was not exercised; migration = port test fixtures to httpx2 + re-verify typed errors/failover'.<br><br>Verify: agregar `grep -qE '^pip_openai: V=[0-9.]+ pip_check=0'`. |
| R-05 | P1 | MITIGAR | T-08 + M-06 + I-05 + M-13 (dedup) | 37-01 (script, Task 2 paso 5), 37-02 pasos 2-3, heredado por 37-03/37-04/37-05 (rama MERGE)/37-07 | 37-gate.sh corre `npm ci` y `npm run validate` (37-01:104). validate incluye freshness.mjs (all.mjs:56), que compara Date.now() (freshness.mjs:52) contra un tope de 48 h. El check de PR 'Frontend build + validate' corre el mismo all.mjs (ci.yml:37-41, y ci.yml:80-83 admite que freshness puede ponerse rojo solo por la edad de los datos). Si las noticias se estancan más de 48 h durante la fase, cosa posible en esta milestone (la directiva:83 dice que freshness rojo es una señal real), cada gate posterior a un merge sale rojo y 37-02:73 manda `git revert` del merge sano, incluido #39 (RCE). Además el revert tampoco pone verde el gate. Los PRs rebaseados salen con CI rojo y se cierran (R-03). `npm ci` borra y recrea node_modules dentro de OneDrive: un EPERM o EBUSY por un .node bloqueado o por un `astro preview` abierto da rojo de infraestructura, y termina igual en revert. Estado medido hoy: freshness está sano (last_new 2026-09-24T21:06Z), así que el escenario depende de un outage. | 37-01 Task 1, en 37-gate.sh:<br>- Emitir un resultado etiquetado por etapa (INSTALL, BUILD, VALIDATE:<nombre>, VITEST, ASTRO, PYTEST, LINT) en lugar de un solo &&. Mantener la cadena en un solo proceso por OneDrive.<br><br>37-02 paso 3 (heredado por 37-01 paso 5, 37-03, 37-04 y 37-07), nueva regla de revert:<br>- Revertir solo si una etapa BUILD, VITEST, ASTRO, PYTEST o de validador estructural que estaba verde en el gate previo (o en el snapshot) pasa a rojo después del merge.<br>- INSTALL rojo → cerrar procesos que bloqueen archivos, reintentar una vez y, si persiste, pausar sin revertir.<br>- freshness o news-freshness rojos → detener la cola de merges y registrar el incidente de noticias (ruta de la Fase 34 / pause point 6), sin excluir el validador (directiva:83). No revertir ni cerrar PRs.<br><br>Opcional: correr el gate en un clon dentro de $SCR, fuera de OneDrive. |
| R-06 | P1 | MITIGAR | T-05, verificado de nuevo | 37-03 Task 2 (paso 2, verify), 37-07 (deferred-live) | G-49 (directiva:146) permite los advisories residuales solo con `npm audit fix` sin --force. Medido hoy en site/: `npm audit fix --dry-run --json` → 'Cannot read properties of null (reading edgesOut)', rc=1. nanoid 3.3.17 (package-lock.json:4857-4858; el rango del padre es ^3.3.16, :5115) es alta, runtime según --omit=dev, con fixAvailable true y sin PR (37-03:57). Si el crash persiste después de los merges (no verificado, porque el árbol cambia), el verify de 37-03 (:98) exige 'no fixable high left' y falla, y G-49 no ofrece otra ruta: gap plan y la fase no cierra. Si corre, es el único lockfile de la fase generado en Windows y pusheado directo a master sin CI Linux (ci.yml:3-5). Con R-01 sin mitigar, tampoco se vería si CF no lo construye. | 37-03 Task 2 paso 2:<br>- Correr primero `npm audit fix --dry-run` y registrar el rc.<br>- Si crashea, fallback solo-lockfile dentro del rango: `npm update <pkg> --package-lock-only`. Si eso también crashea, `npm install <pkg>@<patched> --package-lock-only` solo cuando el paquete sea transitivo y la versión caiga dentro del rango del padre. Mismo requisito de diff: solo site/package-lock.json.<br>- Gate verde + R-01 con la huella de build obligatoria antes de dar por hecho el residual.<br>- Si todo crashea: registrar `residual <pkg> <GHSA> fixable-but-tooling-crash (arborist edgesOut)`, enviarlo a deferred-live y hacer que el verify acepte los residuales con esa marca en el log.<br><br>Registrar G-51, que enmienda G-49 en la herramienta y no en la política, antes de ejecutar. Nota de exposición: nanoid entra en el build vía postcss en un sitio estático. |
| R-07 | P1 | MITIGAR | T-07(a) + M-07 (dedup), con evidencia propia | 37-04 verify, 37-05 (precondición, interfaces, verify), 37-02 paso 1, 37-03, G-48 | Los planes identifican el PR de openai por número: 37-04 verify :73 exige `#37 = OPEN`, 37-05 :69 lo pone como precondición, :56 y G-48 también. Medido: la última versión de openai en PyPI es 3.19.2 y #37 sigue en 3.7.0. No hubo PRs de versión desde el 09-05, con pip en 5 abiertos (#26/#31/#35/#36/#37) = open-pull-requests-limit 5 (dependabot.yml:13-17): la cola está congelada. El historial muestra reemplazos sucesivos: #22 3.0.0 → #25 3.3.0 → #32 3.3.1 ('Superseded by #37'). 37-04 libera 4 cupos. El siguiente sábado (~14:53Z) lo más probable es que Dependabot cierre #37 'Superseded' y abra 3.19.x con otro número. Entonces el verify de 37-04 falla, 37-05 hace STOP y DEPS-03 no se decide. Lo mismo vale para los PRs de seguridad reemplazados en 37-02/03, que solo se registran como auto-closed y dejan al sucesor sin triage. | 37-04 verify: reemplazar `test "$(gh pr view 37 ...)" = OPEN` por 'existe un PR abierto de Dependabot cuyo título contiene "openai from 2.53.0"' (`gh pr list --author app/dependabot --state open --search 'openai in:title' --json number,title`).<br><br>37-05:<br>- En precondición, interfaces y verify, cambiar 'PR #37' por 'el PR abierto de Dependabot openai 2→3.x (#37 o su sucesor)', resolviendo N al inicio y registrándolo.<br>- V se lee del diff de ese N.<br>- Actualizar el texto de G-48 en el mismo sentido (orquestador).<br><br>37-02 paso 1 y 37-03: si un PR fue 'Superseded by #M', triar #M en el mismo plan con el mismo procedimiento. |
| R-08 | P1 | MITIGAR | M-08 + I-06 + T-07(b) + M-16 (dedup; se corrigió el día) | 37-07 (paso 1, deferred-live), 37-01 (script), 37-06 Task 2 | DEPS-02 se mide una sola vez en 37-07 (:69, verify :83). Medido: los PRs de versión se crean los sábados a ~14:52-14:53Z (#18 08-08, #26/#27/#28 08-22, #30/#31/#33 08-29, #35/#36/#37 09-05). npm (#18/#27/#28/#30/#33) y pip están en el límite de 5. `npm outdated` ya muestra @astrojs/react 7.0.0 (major de un peer fijado), vitest 5.0.1 (major), astro 7.3.5, react/react-dom/@types 19.3.0 y @astrojs/sitemap 3.7.4 en espera. Si 37-07 mide antes del primer sábado posterior al vaciado, cierra en verde y días después hay 8-10 PRs abiertos. Al 2026-10-20 el requisito tildado es falso. La política de 37-06 (:124 'weekly and manual') no tiene responsable ni disparador después de la corrida, y remite a 'the phase-37 gate' (:126), que vive en un directorio de fase que se archiva. 37-07:69 dice 'a red major is closed', pero la política de DEPLOYMENT.md (37-06:127) también cubre los majors verdes de ítems fijados. Esa redacción deja espacio a mergear @astrojs/react 7 si sale verde. | 37-07 Task 1 paso 1:<br>- Medir DEPS-02 solo después de la primera corrida de Dependabot del sábado (≥15:30Z) posterior al último merge de 37-02..37-05, y triar esa ola.<br>- Reemplazar 'a red major is closed with a reason' por 'majors follow the DEPLOYMENT.md policy: CLAUDE.md-pinned items and openai are closed with a reason even if green'.<br>- Registrar el conteo de cierres por tipo de motivo.<br><br>37-01, en 37-merge-pr.sh: exit 7 'major' si el título es 'from X to Y' con un major distinto, salvo `--allow-major`.<br><br>37-06 Task 2 paso 2: reemplazar 'see the phase-37 gate' por la cadena literal de comandos y nombrar al responsable del triage semanal como decisión del owner.<br><br>37-07 Task 2: agregar a deferred-live 'triage semanal: responsable <owner>, desde <fecha>' con un recordatorio fechado (issue, igual que FRESH-04). |
| R-09 | P2 | MITIGAR | T-06 + I-10 (dedup) | 37-01..37-05, 37-07 | 37-merge-pr.sh agrega una línea a 37-TRIAGE-LOG.md (37-01:98), que ya está trackeado desde el commit de Task 1. El paso siguiente es `git pull --rebase origin master` (37-01:129; 37-02:72). La config medida solo tiene pull.rebase=false (gitconfig del sistema) y no hay rebase.autoStash, así que git rechaza el comando: 'cannot pull with rebase: You have unstaged changes'. Pasa en el primer merge. Si el ejecutor improvisa con stash o commit, se arriesga a perder las líneas `^merged #N` que leen los verify. | En 37-01 Task 2 paso 3, 37-02 pasos 3-4, 37-03, 37-04, 37-05 (rama MERGE) y 37-07: usar `git pull --rebase --autostash origin master`. Agregar `git diff --quiet -- . ':!.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md' \|\| exit 8` como precondición de árbol limpio en 37-merge-pr.sh. |
| R-10 | P2 | MITIGAR | M-01 + I-07 + T-09 (dedup; se bajó la severidad) | 37-05 | 37-05:21 ('A never turns a mismatch into a merge'), :91 ('run control A ... Then Decision CLOSE') y el verify :103 (MERGED exige run-B == línea base) contradicen G-50 (directiva:147): B==A≠línea base → MERGE, hasta 2 corridas de cada uno. La directiva manda. Hoy la rama es inalcanzable, porque pytest no llega a colectar con openai 3.x (R-04). Pero si alguien la alcanza, el ejecutor tiene dos reglas incompatibles y el verify rechaza un MERGE autorizado. | 37-05, en must_haves :18/:21, Task 2 paso 2 y verify: tabla de tres ramas según G-50.<br>- B==línea base → MERGE.<br>- A==línea base y B≠línea base → CLOSE.<br>- A≠línea base y B==A (errores 0) → MERGE.<br>- Cualquier otro caso → CLOSE.<br><br>El verify de MERGED acepta (B==línea base) o (A≠línea base y B==A), leyendo eval/run-A.json. Límite: ≤2 corridas de cada uno, con el ledger compartido ≤0.05. La tabla se declara textual en 37-DEPS03.md antes de gastar. |
| R-11 | P2 | MITIGAR | hallazgo propio del árbitro | 37-05 | 37-05:68 exige que el blob de `git hash-object pipeline/news/classifier.py` coincida con baseline.classifier_blob y, si no, hace STOP de todo DEPS-03 antes del pytest (:74). La ruta CLOSE de gasto cero, que es la probable según R-04, no usa la línea base. Un hotfix a classifier.py entre el ship de 36-09 y 37-05 (la milestone ya tuvo hotfixes) bloquearía DEPS-03 sin necesidad y la fase no cerraría. | Mover el chequeo de classifier_blob de 37-05 Task 1 paso 0 a Task 2, antes del paso 1 (antes de cualquier gasto). En Task 1 solo exigir que exista la línea base. En el verify de Task 1, que el assert de blob sea condicional a `pytest_openai3: exit 0`. |
| R-12 | P2 | MITIGAR | M-11 + T-10 (dedup) | 37-06 (Task 1 verify, Task 2), 37-07 paso 3 | codeql.yml corre en cada push a master, sin paths (37-06:94), con cancel-in-progress (:96). 37-06 Task 2 pushea el commit ci(37), espera, pushea 'docs(37): CodeQL first-run counts' (:143) y el verify lee `--limit 1` (:147), que es la corrida del commit de docs, todavía in_progress. Resultado: FAIL con el sistema sano. 37-07 (:71) repite el patrón. Además, el zizmor local (:61, :108) corre sin GH_TOKEN, es decir offline, mientras CI lo corre con GH_TOKEN (ci.yml:95-98) y con las auditorías online. codeql.yml entra por push directo y CI recién lo ve en el siguiente PR, lo que puede poner rojo 'Lint' en todos los PRs nuevos y provocar cierres en cascada en 37-07. | 37-06 Task 2 y verify: seleccionar la corrida con headSha == SHA del commit ci(37) (`gh run list --workflow codeql.yml --json headSha,status,conclusion` filtrado) y esperar a que termine. Aceptar una corrida cancelada solo si hay una success sobre un descendiente.<br><br>37-07 paso 3: tomar la última corrida COMPLETED y esperar las in_progress.<br><br>37-06 Task 1: correr `GH_TOKEN=$(gh auth token) zizmor --persona=regular .github/workflows/` para que el local cubra las auditorías online que corre CI. |
| R-13 | P2 | MITIGAR | I-09 + M-15 (dedup) | 37-04 Task 1 | La prueba en vivo de 37-04 (:68) es solo la siguiente corrida programada de news-pipeline. r2-archive.yml (cron diario, :5) instala el mismo requirements.txt (:50) y corre archive_r2.py (:58), que usa fulltext.py con trafilatura (:22), consumidor de lxml. 37-04:53 supone que lxml solo lo usa CEAD, una hipótesis falsada. scipy solo se usa en pipeline/composite_index.py. Si una corrida de news no tiene candidatos nuevos, no ejercita el clasificador. Y una falla por un paquete sin pin (boto3, trafilatura) se atribuiría a los merges y dispararía un revert equivocado. | 37-04 Task 1:<br>- Después del último merge, sondear también la siguiente corrida de r2-archive.yml y exigir success.<br>- Exigir que el resumen de la corrida de news muestre ≥1 ítem clasificado. Si no hay ninguno, registrar 'classifier path not exercised'.<br>- Registrar que #31 (scipy) y #36 (lxml/CEAD) no tienen prueba en vivo en la fase, y que la primera corrida en vivo de lxml sobre CEAD es HYG-06 de la Fase 38.<br>- Atribuir una falla a un merge solo si el traceback nombra el paquete bumpeado. Si nombra un paquete sin pin, registrar deriva de terceros y no revertir. |
| R-14 | P2 | MITIGAR | I-11 (PLAUSIBLE: el extractor no se pudo verificar offline) | 37-06 Task 2, 37-07 Task 2 | La decisión de CodeQL (37-06:132-136) se va a leer como cobertura del sitio, pero site/src tiene 84 .astro frente a 41 .ts y 17 .tsx (medido), y 10 archivos .astro usan set:html, que es la superficie real de XSS con titulares verbatim. No verifiqué que el extractor JS/TS de CodeQL procese .astro, y es probable que no. Si es así, CodeQL nace inerte sobre la parte más riesgosa y el registro en DEPLOYMENT.md sobreafirma. | 37-06 Task 2 paso 3: agregar la línea 'Coverage: python + .js/.ts/.tsx/.mjs; .astro components (84, 10 with set:html) are not assumed analyzed; verified from the first run log: <files extracted / .astro present yes\|no>'. Leerlo del log de la primera corrida (`gh run view --log`). 37-07: agregar un deferred-live 'set:html review is manual'. |
| R-15 | P2 | ACEPTAR | T-12 | 37-01 (script), 37-07 | El gate local corre con node v22.23.2 y npm 10.9.8 (medido), contra .nvmrc 22.12.0 que usan CI (ci.yml:31) y CF, y con python miniconda 3.13 contra 3.12 en CI y en el cron. Además hace `pip install -r` sobre el Python base. Un verde local no es evidencia de Linux. | Se acepta. La evidencia Linux sale de los checks del PR sobre una base actual (R-02: rebase ante base vieja) y de la huella de build de prod (R-01), no del gate local. Única edición: 37-gate.sh imprime `node --version; npm --version; python --version` en cada línea de gate del TRIAGE-LOG, y 37-VERIFICATION no presenta el gate local como prueba de prod. |
| R-16 | P2 | ACEPTAR | I-08 + M-09 + M-10 (dedup) | 37-07 (texto deferred-live) | En la rama MERGE de openai 3.x, la prueba es la igualdad de 4 conteos con n=47 más una corrida programada. Ninguna de las dos ejercita failover, breaker, _is_retryable tipado, resp.provider ni usage.cost. Además, la extracción del subconjunto v2 de la línea base (36-06:151) no especifica su procedimiento. | Se acepta. En la ruta por defecto la rama MERGE es inalcanzable: con openai 3.x la suite no colecta (httpx→httpx2, medido en el run 33973132996), y 37-05:75 cierra antes de cualquier gasto, así que el eval y la línea base no se usan en esta fase. Para no perder el hallazgo, el deferred-live de 37-07 ('#37 closed: openai 3.x migration') debe listar lo que exige esa migración: portar los fixtures a httpx2; hacer un dispatch de news-pipeline.yml con force_backup=true (como en G-25); comprobar que resp.provider y usage.cost sobreviven; y recalcular el subconjunto v2 de la línea base desde per_item con la semántica de run_full antes de comparar. |
| R-17 | P2 | ACEPTAR | I-12 | 37-01 (RUN STATUS), 37-07 (reporte) | 37-RESEARCH.md:184-192 pedía la firma del owner por PR o por lote antes de que el agente mergeara PRs preexistentes, y cada merge de site/ despliega a prod. G-47 (directiva:144) lo resolvió el orquestador. Si el owner entendía 'weekly manual triage' (directiva:71) como triage humano, va a cuestionar unos 15 merges públicos. | Se acepta. G-35 registra la delegación del owner del 2026-09-24 en el orquestador para las preguntas abiertas del planner; los merges son reversibles con git revert, y G-47 prohíbe --auto y los bots. Edición mínima: antes de 37-01, anotar en RUN STATUS la lista de PRs que el agente va a mergear y reportarla al owner en el cierre. Se incluye además en decisiones_para_owner como ratificación. |

## Discarded

- M-12: La premisa es falsa para PRs de versión. Cerrar un PR de Dependabot ignora esa release, no el major, y Dependabot propone la versión siguiente. El historial lo confirma: openai 3.x se volvió a proponer como #22, #25, #32 y #37. Los textos 'Dependabot will re-propose the next release' de 37-04:85 y 37-05:99 son correctos. La parte válida (nadie dispara el triage ni congela majors) se conserva en R-08 y en decisiones_para_owner.
- I-06 (sub-afirmación del día): Dice que Dependabot corre los lunes (10-05, 12, 19). Lo medido es otra cosa: todos los PRs de versión se crearon un sábado a ~14:53Z (08-08, 08-22, 08-29, 09-05). El resto de I-06 se integró en R-08 con el día corregido.
- M-08 (sub-afirmación 'la política dice Monday pero Dependabot corre sábado'): 37-06:124 dice 'on Monday after Dependabot's weekly run'. Hacer el triage el lunes después de una corrida de sábado es coherente, así que no hay defecto. El resto de M-08 (foto antes de la ola, cola congelada) se integró en R-08.
- T-07(b): Queda cubierto en buena parte: 37-07:69 manda aplicar la política de DEPLOYMENT.md, y esa política (37-06:127) exige un cambio propio para los majors de ítems fijados en CLAUDE.md y de openai, estén verdes o no. Lo que falta (la redacción 'red major' y la defensa en profundidad en el script) se integró en R-08. Un major verde de vitest pasa por el gate completo, que corre vitest.
- I-07 (sub-afirmación sobre el precondicional de 37-01): El grep `G-4[6-9]` de 37-01:81 imprime 4 con G-50 presente o ausente, así que no bloquea ni omite nada. El conflicto real con G-50 queda en R-10.
- I-01 (severidad P0 por sí solo) / T-11 (riesgo independiente): No se descartan los hechos, solo se fusionan. El desajuste de SHA y el PASS vacuo son el mismo instrumento y se tratan juntos como P0 en R-01. La cola de 1 build concurrente de CF solo importa en combinación con la huella de build y quedó dentro de R-01 (tope de 30 min y no revertir si la huella no avanza). No encontré evidencia de que la fase agote la cuota de 500 builds al mes; su visibilidad queda como decisión del owner.

## Owner decisions surfaced

- Quién hace el triage semanal de Dependabot cuando termine la corrida autónoma, desde qué fecha y con qué recordatorio (por ejemplo, un issue fechado). Sin responsable, DEPS-02 deja de ser cierto en uno o dos sábados. Los PRs de versión se crean los sábados a ~14:53Z y hay una ola en cola: @astrojs/react 7, vitest 5, astro 7.3.5, react 19.3 y openai 3.19.2.
- Si se congelan majors con `@dependabot ignore this major version` (openai 3.x, TypeScript 7, @astrojs/react 7, vitest 5). Reduce el ruido semanal pero detiene esas propuestas de forma indefinida. Default dentro de la corrida: no ignorar, solo cerrar con motivo.
- Cómo diagnosticar un build de producción de Cloudflare que falla. Hay dos opciones: que el owner mire el dashboard, o que el owner cree un token de CF de solo lectura como secret (el agente no puede tocar secretos ni settings). Aplica cuando la huella data-build-now no avanza después del hook (R-01 regla d), e incluye mirar el uso de builds contra el límite free de 500/mes con 1 concurrente.
- Ratificar G-47. El agente va a mergear unos 15 PRs de Dependabot, cada uno desplegado a prod, cuando 37-RESEARCH.md:184-192 recomendaba la firma del owner por PR o por lote. Está delegado por G-35. Queda la opción de reservar los majors para el owner.
- Cuándo se hace la migración a openai 3.x como cambio propio (fixtures httpx→httpx2, errores tipados, failover con force_backup, línea base recalculada), o si el cliente LLM de producción se queda en 2.x por ahora.

## Raw perspective counts

- PERSPECTIVA I (integración y producto). Revisé los 7 planes contra el código y el estado vivo, con lecturas de gh/npm y ninguna escritura: deploy-on-code.yml, ci.yml, news-pipeline.yml, r2-archive.yml, dependabot.yml, los validadores de site/ y la directiva (G-46..G-50). También leí la protección de la rama master (404, no está protegida), el PR #39 y su run de CI, y la salida de npm outdated.

**Mecanismo central que falla.** La fase trata "checks SUCCESS en el head" y "37-served-routes.sh PASS" como prueba de que lo que se sirve en prod está verificado. Ninguno de los dos instrumentos mide eso:
- El CI de #39 corrió el 2026-09-10 contra una base que está 144 commits y 39 archivos de site/ detrás de master.
- Cada merge despliega a prod antes del gate local.
- El chequeo de prod miente en las dos direcciones. Da PASS aunque Cloudflare siga sirviendo el build viejo. Da FAIL por construcción cuando el SHA pedido es un commit de docs o de datos del bot, y esos son justamente los SHAs que 37-02, 37-03 y 37-07 le pasan.

**Hipótesis del premortem que falsé.**
- FRESH-04 no se contamina. D cuenta solo el paso de deploy de news-pipeline (35-07-PLAN.md:151-152), y la ventana de 7 días [2026-09-25, 2026-10-02] termina antes de que la 37 pueda arrancar (necesita la 36 cerrada).
- Hoy HYG-06 no choca, porque la regla de orden estricto lo corre después de la 37. Dos salvedades: si la 37 se estanca, arrastra a la 38, y el scrape local de CEAD será la primera corrida en vivo del parser con lxml 6.1.3 (solo lo cubre el pytest offline).
- Los pushes del cron usan push-with-rebase (news-pipeline.yml:94), así que un merge humano no hace perder datos.
- El token de gh tiene los scopes workflow y repo (security_events incluido), así que no bloquea merges de workflows ni la API de code scanning.

**Qué necesita al owner y ningún plan lo dice:**
- Quién hace el triage semanal después de la corrida.
- Acceso al dashboard de Cloudflare: no hay token de CF en .env, y un build fallido en CF solo se puede ver ahí.
- El research recomendaba que el owner aprobara cada merge (37-RESEARCH.md:184-192). G-47 lo resolvió sin el owner, así que conviene declararlo en el reporte final.: 12
- PERSPECTIVA M (metodológica), sobre los planes 37-01..37-07 en HEAD 8a7eff6. Leí los 7 planes, la directiva (G-16, G-41, G-45..G-50), REQUIREMENTS DEPS-01..04, ROADMAP § Phase 37, 36-06/36-02 (cómo nace la línea base), eval_classifier.py, classifier.py, deploy-on-code.yml, ci.yml, dependabot.yml, los validadores de freshness y test_classifier_router.py. Medí en solo lectura con gh (PRs abiertos y cerrados de Dependabot, sus comentarios, fechas y día de la semana) y con un `pip --dry-run` en %TEMP%. No hubo escrituras, LLM ni push.

Veredicto: casi todos los instrumentos de la fase fallan en alguna de las dos direcciones.
- **(a) Fallan con el sistema sano:**
  - El chequeo de rutas servidas se ancla a un SHA que nunca dispara deploy.
  - El gate local y el CI de los PRs incluyen freshness, una señal ajena a las dependencias, y un gate rojo dispara un revert.
  - El exit 4 del script de merge mezcla checks PENDING con FAILED, así que cierra PRs de seguridad sanos que Dependabot está rebaseando.
  - El venv de DEPS-03 no se puede instalar (ResolutionImpossible, medido), lo que produce un CLOSE atribuido falsamente a openai 3.x.
  - La verificación de CodeQL compite con su propio push.
- **(b) Pasan con el sistema roto:**
  - "deploy-on-code success" solo significa que el POST al hook devolvió 2xx. Cloudflare sigue sirviendo el build anterior, así que las 8 rutas, el MapIsland y "Latest incident" pasan igual. Existe `data-build-now` y el plan no lo usa.
  - El umbral DEPS-02 es una foto. Se cumple trivialmente cerrando PRs con cualquier motivo, o midiendo antes del sábado ~14:53Z en que se libera la cola que hoy está tapada por open-pull-requests-limit 5 (npm y pip tienen hoy exactamente 5 PRs de versión abiertos cada uno).
  - La igualdad exacta con n=47 contra un proveedor con 4 endpoints no tiene poder: prueba determinismo del proveedor, no la librería. Las rutas de producción que openai 3.x podría romper (retry tipado, respaldo DeepSeek, `resp.provider` y `usage.cost`) no se ejercitan: el eval solo usa OpenRouter y los fakes del test son SimpleNamespace.

Además, el plan 37-05 contradice a G-50: dice que A nunca convierte un desajuste en MERGE, y su verify exige B == línea base. Los planes están atados a números de PR que Dependabot reemplaza cada semana (openai #22→#25→#32→#37). Y los cierres de majors silencian a Dependabot, aunque los textos de cierre prometen que lo re-propondrá: es una decisión de owner que no se declara como tal.: 16
- T — Técnica. Leí los 7 planes, 37-RESEARCH, los G-41/G-46..G-50 de la directiva, ci.yml, deploy-on-code.yml, dependabot.yml, news-pipeline.yml, check-sha-pins.sh, lint-workflows.sh, site/package.json, package-lock.json, requirements.txt, test_workflow_guards.py y eval_classifier.py. Además medí cuatro cosas en modo solo lectura: (a) pip dry-run con dos pines exactos en conflicto; (b) `npm audit fix --dry-run` sobre el árbol actual; (c) el statusCheckRollup de los PRs #26/#27/#37/#39 y el log del pytest que falló en #37; (d) los advisories de astro vía gh api. No escribí nada en el repo ni en data/ y no llamé a ningún LLM. Conclusión: la mecánica de merge es sólida en lo que controla (--match-head-commit, el autor tiene que ser Dependabot, sin --auto/--admin, revert en vez de force). Pero cuatro instrumentos mienten, y en las dos direcciones. (1) 37-served-routes.sh sale en rojo por construcción en todo SHA que no dispara deploy-on-code, y cuando sale en verde no puede probar que Cloudflare construyó el SHA nuevo. (2) Los checks "SUCCESS" de los PRs tienen entre 15 y 33 días y se midieron contra una base previa a las Fases 35/36. Además el merge ocurre antes del gate local y se despliega solo a prod. (3) El venv de DEPS-03 no se puede instalar tal como está escrito: pip rechaza los dos pines de openai. El pytest que falle después produciría un motivo de cierre inventado. La causa real de la falla de #37 es otra, está verificada y es que openai 3.x ya no trae httpx. (4) `npm audit fix` se cae hoy con un TypeError sobre este árbol, y G-49 no autoriza ninguna alternativa. A eso se suman dos roturas deterministas del flujo: `git pull --rebase` con 37-TRIAGE-LOG.md modificado en el working tree, y #37 fijado por número cuando pip ya está en el límite de 5 PRs. Mi pronóstico: la fase se traba en el gate de cierre (served-routes rojo, residual de nanoid, DEPS-03 bloqueado). O cierra en verde con un 'prod verificado' que no prueba nada. Qué rompe cada merge, uno por uno: astro 7.2.8 pide Node >=22.12.0 (npm view), y con el pin de CF en 22.12.0 lo cumple. También trae sharp ^0.35.4 como optionalDependency, así que es probable que #41 quede sin efecto. Con sharp 0.35.x en Windows y OneDrive, el riesgo es el EPERM en `npm ci`, no la versión. El lockfile ya tiene las entradas opcionales de linux/win/darwin para sharp, rollup y esbuild. Los checks de Actions no cuestan nada porque el repo es público. codeql.yml pasa check-sha-pins (el piso de 14 es solo un aviso). Lo frágil está en otro lado: zizmor corre local sin token y el workflow va directo a master.: 12
