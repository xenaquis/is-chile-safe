---
phase: 37-dependency-security-hygiene
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - site/package.json (via PR merges)
  - site/package-lock.json (via PR merges)
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-02, DEPS-01]
executor: sonnet (merges + gate); orchestrator (deploy polling + served-route curl)

must_haves:
  truths:
    - "The four site security PRs, #42 js-yaml, #43 devalue, #41 sharp and #38 svgo, are each either merged through 37-merge-pr.sh (checks SUCCESS on the merged head) or closed through 37-close-pr.sh with a written reason. None is silently left or silently closed."
    - "Merges are serialized: one merge, then `git pull --rebase`, then 37-gate.sh green, then push, before the next PR is touched, because all four PRs share site/package-lock.json."
    - "After the batch, `npm audit --omit=dev` reports 0 critical, and no advisory whose package was bumped by a merged PR in this plan is still listed."
    - "Prod serves the last merge: 37-served-routes.sh passes for master HEAD."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
      provides: "one merged/closed line per PR, gate wall time, and the npm audit counts before and after"
  key_links:
    - from: each merge
      to: 37-gate.sh
      via: "run after git pull --rebase, before the next merge"
      pattern: "merged #4[123] |merged #38 "
---

<objective>
DEPS-02 (security half of the site backlog): land the runtime-dependency security bumps that Dependabot already opened. Together they close alerts #28 js-yaml (high), #29 devalue (medium), #27 sharp (high), and #19/#20 svgo (medium/high).

Purpose: these are runtime packages in the prod build (npm audit --omit=dev lists all four). Doing them before the non-security patches keeps any lockfile-conflict churn on the security path short.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-RESEARCH.md
@.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
@.planning/phases/37-dependency-security-hygiene/37-01-SUMMARY.md

<interfaces>
- Merge tool: `bash .planning/phases/37-dependency-security-hygiene/37-merge-pr.sh [--check-only] N`. Exit 0 means merged; 3 means CONFLICTING and needs a rebase; 4 means a check is not SUCCESS.
- Close tool: `bash .../37-close-pr.sh N REASON_FILE`, where REASON_FILE holds >= 40 chars and is written in the scratchpad.
- Gate: `bash .../37-gate.sh`, run in the background, exit 0 required.
- Prod: `bash .../37-served-routes.sh <sha>`.
- Measured 2026-09-24: #42 js-yaml 4.3.1→4.3.2 (GHSA-2883-xcg3-v3hh), #43 devalue 5.9.0→5.9.2 (GHSA-9rgm-9g3h-6x36), #41 sharp 0.35.3→0.35.4 (GHSA-rgj7-g3m4-5g8c) and #38 svgo 4.0.2→4.1.0 (GHSA-w27v-7q3p-w38r, GHSA-4vpr-x523-8j87). All four were CI-green at their pre-#39 heads.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialized merge of #42, #43, #41, #38 with a per-merge gate</name>
  <files>site/package.json, site/package-lock.json (via merges), .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-01-SUMMARY.md must exist and record #39 merged. Otherwise STOP.

Log the `npm audit --omit=dev` counts ("before").

For N in 42, 43, 41, 38, in order:
1. Read `gh pr view N --json state`. If it is CLOSED or MERGED because Dependabot closed it after #39 (for example "no longer needed" or "superseded"), log `auto-closed #N by Dependabot: <its last comment's first line>` and continue with the next N.
2. Run `37-merge-pr.sh --check-only N`.
   - On exit 3: comment `@dependabot rebase` on the PR and wait up to 45 min, polling every 2 min, for a new head with all checks COMPLETED.
   - On exit 4, or when the checks on the rebased head are still not green: do NOT merge. Write a reason naming the failing check and its run URL, then run `37-close-pr.sh N`. Dependabot re-proposes the fix on its next weekly run, and 37-07 re-measures.
3. Run `37-merge-pr.sh N`, then `git pull --rebase origin master`, then 37-gate.sh in the background (exit 0).
   - If the gate is red: `git revert --no-edit <merge sha>`, gate again (green), push, and log `reverted #N: <first failing line>`. Then continue. A revert is a normal commit, not a history rewrite.
4. Push any local docs commits (`git pull --rebase && git push`) before the next N (G-47).

After the batch, the orchestrator runs 37-served-routes.sh on master HEAD's SHA until PASS (15 min cap), then logs the audit counts ("after") and the per-merge gate wall times. Commit `docs(37): site security PRs triaged` and push.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && MERGED=""; for N in 42 43 41 38; do s=$(gh pr view $N --json state -q .state); echo "#$N $s"; [ "$s" = MERGED ] && ! grep -q "^reverted #$N" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md && MERGED="$MERGED $N"; [ "$s" != OPEN ] || exit 1; grep -qE "^(merged|closed|auto-closed|reverted) #$N[ :]" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || exit 1; done && export MERGED && cd site && npm audit --omit=dev --json > "$TEMP/37-02-audit.json"; python -c "import json,os;d=json.load(open(os.path.join(os.environ['TEMP'],'37-02-audit.json')));v=d['vulnerabilities'];print(d['metadata']['vulnerabilities']);assert d['metadata']['vulnerabilities']['critical']==0;M=os.environ['MERGED'].split();pk={'42':'js-yaml','43':'devalue','41':'sharp','38':'svgo'};bad=[pk[n] for n in M if pk[n] in v];print('merged',M,'still listed',bad);assert not bad" && cd .. && bash .planning/phases/37-dependency-security-hygiene/37-served-routes.sh "$(git rev-parse origin/master)"</automated>
    Expected: none of #42/#43/#41/#38 is OPEN, each has a log line, critical == 0, none of the four packages is still in npm audit, and served routes PASS. The "still listed" assert only covers PRs that were MERGED. A package whose PR was closed with a reason is left to 37-03 Task 2 (residual audit fix).
  </verify>
  <done>The site security PRs are resolved one by one, each with a green gate, and prod serves the result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Dependabot PR → master → prod | lockfile changes deploy automatically via deploy-on-code |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering | lockfile merge order | mitigate | Merges are serialized with a gate between them, and --match-head-commit pins each merge to its checked head |
| T-37-06 | Denial of service | broken build after a bump | mitigate | Per-merge gate; `git revert` rollback; served-route check at the end |
| T-37-07 | Repudiation | a PR closed without a reason | mitigate | Closes happen only through 37-close-pr.sh (comment first) |
| T-37-SC | Tampering | npm installs | accept | Dependabot bumps of existing packages only; `npm ci` from the merged lockfile |
</threat_model>

<verification>
- #42/#43/#41/#38 are not OPEN, and each has a log line.
- npm audit --omit=dev critical == 0.
- 37-gate.sh is green after every merge; served routes PASS on master HEAD.
</verification>

<success_criteria>
The runtime security bumps are on master and prod, and every decision is written down.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-02-SUMMARY.md` when done.
</output>
