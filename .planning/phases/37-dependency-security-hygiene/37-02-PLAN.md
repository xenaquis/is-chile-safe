---
phase: 37-dependency-security-hygiene
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - site/package.json (via PR merges)
  - site/package-lock.json (via PR merges)
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-02, DEPS-01]
executor: sonnet (merges + gate); orchestrator (deploy polling + served-route curl)

must_haves:
  truths:
    - "The four site security PRs, #42 js-yaml, #43 devalue, #41 sharp and #38 svgo, are each either merged through 37-merge-pr.sh (checks SUCCESS on the merged head, base not stale) or closed through 37-close-pr.sh with a written reason. None is silently left or silently closed. A PR whose checks are PENDING (exit 5), or red only for freshness/install causes, is never closed (G-53/G-54). A PR that Dependabot supersedes is followed to its successor #M in this plan (R-07)."
    - "Merges are serialized: one merge, then `git pull --rebase --autostash`, then 37-gate.sh green, then (every PR here touches site/) `37-served-routes.sh <merge sha>` PASS logged as `served_routes: PASS deployable=<D> built_at=<ts> (#N)` (directive hard rule 'after every deploy, curl prod and verify'; G-61), then push, before the next PR is touched, because all four PRs share site/package-lock.json."
    - "After the batch, `npm audit --omit=dev` reports 0 critical, and no advisory whose package was bumped by a merged PR in this plan is still listed."
    - "Prod serves every merge (per-merge line above) and the last one: 37-served-routes.sh on origin/master resolves the deployable SHA D and passes with a fresh build fingerprint (G-51); the log carries `served_routes: PASS deployable=<D> built_at=<ts>`."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
      provides: "one merged/closed line per PR, gate wall time, and the npm audit counts before and after"
  key_links:
    - from: each merge
      to: 37-gate.sh
      via: "run after git pull --rebase --autostash, then the per-merge served-route check, before the next merge"
      pattern: "merged #4[123] |merged #38 "
---

<objective>
DEPS-02 (security half of the site backlog): land the runtime-dependency security bumps that Dependabot already opened. Together they close alerts #28 js-yaml (high), #29 devalue (medium), #27 sharp (high), and #19/#20 svgo (medium/high).

Purpose: these are runtime packages in the prod build (npm audit --omit=dev lists all four). Doing them before the non-security patches keeps any lockfile-conflict churn on the security path short.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-RESEARCH.md
@.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
@.planning/phases/37-dependency-security-hygiene/37-01-SUMMARY.md

<interfaces>
- Merge tool: `bash .planning/phases/37-dependency-security-hygiene/37-merge-pr.sh [--check-only] [--stale-ok GATE_LOG] N`. Exit codes (G-53): 0 merged; 3 CONFLICTING; 4 a check FAILED (conclusion FAILURE/CANCELLED/TIMED_OUT/ACTION_REQUIRED/STARTUP_FAILURE); 5 PENDING (a check not COMPLETED, a required check missing, or mergeable UNKNOWN); 6 stale base (site/pipeline/.github changed since the PR's base); 7 major (reserved for the owner, G-55); 8 dirty tree; 9 not eligible.
- Close tool: `bash .../37-close-pr.sh N REASON_FILE`, where REASON_FILE holds >= 40 chars and is written in the scratchpad.
- Gate: `bash .../37-gate.sh`, run in the background, exit 0 required. It prints `STAGE <name> rc=<n>` per stage (INSTALL, BUILD, VALIDATE, VALIDATE:parse, VALIDATE:<name>, VITEST, ASTRO, PYTEST, LINT), a `TOOLCHAIN` line and a final `GATE rc=` line (G-54, G-61).
- Prod: `bash .../37-served-routes.sh <sha>`. It resolves the deployable SHA D; exit 0 PASS, 1 route failure, 10 stale build, 11 deploy run missing/failed, 12 pending (G-51).
- Measured 2026-09-24: #42 js-yaml 4.3.1→4.3.2 (GHSA-2883-xcg3-v3hh), #43 devalue 5.9.0→5.9.2 (GHSA-9rgm-9g3h-6x36), #41 sharp 0.35.3→0.35.4 (GHSA-rgj7-g3m4-5g8c) and #38 svgo 4.0.2→4.1.0 (GHSA-w27v-7q3p-w38r, GHSA-4vpr-x523-8j87). All four were CI-green at their pre-#39 heads, but on bases older than Phases 35/36, so each is expected to exit 6 (stale base) and go through `@dependabot rebase` first (R-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialized merge of #42, #43, #41, #38 with a per-merge gate</name>
  <files>site/package.json, site/package-lock.json (via merges), .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-01-SUMMARY.md must exist and record #39 merged. Otherwise STOP.

Log the `npm audit --omit=dev` counts ("before").

For N in 42, 43, 41, 38, in order:
1. Read `gh pr view N --json state`. If it is CLOSED or MERGED because Dependabot closed it after #39 (for example "no longer needed" or "superseded"), log `auto-closed #N by Dependabot: <its last comment's first line>`. If that comment says "Superseded by #M", log `superseded #N -> #M` and triage #M in this plan with this same procedure (R-07). Then continue with the next N.
2. Run `37-merge-pr.sh --check-only N` and branch on the exit code (G-53):
   - 3 or 6: comment `@dependabot rebase` on the PR and re-run --check-only every 2 min for up to 45 min until it exits 0; exit 5 keeps polling. Optional: the scratch-worktree gate with `--stale-ok` (37-01 Task 2 step 1).
   - 5 (PENDING): poll every 2 min for up to 45 min. If it is still 5, log `pending #N`, move to the next N, and revisit it once after the batch (another 45 min). If it is still pending then, STOP and report; never close a PR for being pending.
   - 7 (major): close it with the G-55 reason "major upgrade reserved for owner review; reopen or @dependabot recreate when ready" and list it for deferred-live.
   - 4 (FAILED): read `gh run view <id> --log-failed` for each failing check and classify the cause by the G-54 stages. If the cause is VALIDATE:freshness / VALIDATE:news-freshness (news data age, not the bump), do NOT close: stop the merge queue and record the news incident (G-54, as in step 3). If it is install/infrastructure, do NOT close: comment `@dependabot rebase` once and re-classify on the new head (exit 5 keeps polling). Only a cause attributable to the bump (BUILD, VITEST, ASTRO, PYTEST or a structural validator) means: do NOT merge; write a reason naming the failing check, its run URL and the classified cause, then run `37-close-pr.sh N`. The alert stays open; 37-03 Task 2 handles the residual, and 37-07 re-measures.
3. Run `37-merge-pr.sh N`, then `git pull --rebase --autostash origin master` (R-09), then 37-gate.sh in the background (exit 0). If the gate is red, apply the G-54 revert rule by stage:
   - Revert (`git revert --no-edit <merge sha>`, gate again, push, log `reverted #N: <first red STAGE line>`) only if a BUILD, VITEST, ASTRO, PYTEST or structural VALIDATE:<name> stage that was rc=0 in the previous gate (or the 37-01 baseline gate, `## Baseline gate` in the log, G-61) is now red. A revert is a normal commit, not a history rewrite.
   - INSTALL red (EPERM/EBUSY under OneDrive): close processes that lock files under site/node_modules (a running `astro preview`, editors), retry the gate once; if it is still red, pause (pause point 5) without reverting.
   - VALIDATE:freshness or VALIDATE:news-freshness red: stop the merge queue and record the news incident (Phase-34 path / pause point 6). The validator stays in the gate (directive: a red freshness is a real signal). No revert, no PR close.
   - LINT red: not attributable to a dependency bump; stop and report.
   - Per-merge served routes (BF-05, G-61; directive hard rule "after every deploy, curl prod and verify"): once the gate is green and the merge touched site/ (`git diff --name-only <merge sha>^ <merge sha> -- site` is non-empty, true for every PR in this plan), the orchestrator runs `37-served-routes.sh <merge sha>` every 60 s, capped at 30 min, BEFORE the next PR is touched, and logs `served_routes: PASS deployable=<D> built_at=<ts> (#N)`. At the cap, rule (d) of G-51 applies to THIS merge: exit 10/11 → no revert, pause point 5 naming #N and <merge sha7>; exit 1 on a fresh fingerprint → `git revert --no-edit <merge sha>` (gate, `git pull --rebase --autostash origin master`, push) and log `reverted #N: served_routes exit 1 <first failure>`.
4. Push any local docs commits (`git pull --rebase --autostash origin master && git push`) before the next N (G-47).

After the batch (in addition to the per-merge checks), the orchestrator runs 37-served-routes.sh on origin/master every 60 s until PASS (30-min cap, rule (d) of G-51 at the cap: no revert on a stale fingerprint, pause point 5 with the Cloudflare-log owner question), and logs `served_routes: PASS deployable=<D> built_at=<ts>`. Then it logs the audit counts ("after") and the per-merge gate wall times. Commit `docs(37): site security PRs triaged`, then `git pull --rebase --autostash origin master` and push (R-09).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && MERGED=""; for N in 42 43 41 38; do s=$(gh pr view $N --json state -q .state); echo "#$N $s"; [ "$s" = MERGED ] && ! grep -q "^reverted #$N" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md && MERGED="$MERGED $N"; case " $MERGED " in *" $N "*) grep -qE "^served_routes: PASS deployable=[0-9a-f]{7,40} built_at=[^ ]+ \(#$N\)" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || exit 1;; esac; [ "$s" != OPEN ] || exit 1; grep -qE "^(merged|closed|auto-closed|reverted|superseded) #$N[ :]" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || exit 1; M=$(grep -oE "^superseded #$N -> #[0-9]+" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md | grep -oE "[0-9]+$" | tail -1); [ -z "$M" ] || [ "$(gh pr view $M --json state -q .state)" != OPEN ] || exit 1; done && export MERGED && cd site && npm audit --omit=dev --json > "$TEMP/37-02-audit.json"; python -c "import json,os;d=json.load(open(os.path.join(os.environ['TEMP'],'37-02-audit.json')));v=d['vulnerabilities'];print(d['metadata']['vulnerabilities']);assert d['metadata']['vulnerabilities']['critical']==0;M=os.environ['MERGED'].split();pk={'42':'js-yaml','43':'devalue','41':'sharp','38':'svgo'};bad=[pk[n] for n in M if pk[n] in v];print('merged',M,'still listed',bad);assert not bad" && cd .. && bash .planning/phases/37-dependency-security-hygiene/37-served-routes.sh "$(git rev-parse origin/master)" && grep -qE "^served_routes: PASS deployable=[0-9a-f]{7,40} built_at=" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</automated>
    Expected: none of #42/#43/#41/#38 (nor any logged successor #M) is OPEN, each has a log line, each MERGED (not reverted) PR has its per-merge `served_routes: PASS deployable=<D> built_at=<ts> (#N)` line (BF-05, G-61), critical == 0, none of the four packages is still in npm audit, and served routes PASS on origin/master (the script resolves the deployable SHA D, so a docs-only HEAD does not fail by construction) with a fresh build fingerprint logged. The "still listed" assert only covers PRs that were MERGED. A package whose PR was closed with a reason is left to 37-03 Task 2 (residual audit fix).
  </verify>
  <done>The site security PRs are resolved one by one, each with a green gate and a per-merge served-route PASS, and prod serves the result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Dependabot PR → master → prod | lockfile changes deploy automatically via deploy-on-code |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering | lockfile merge order | mitigate | Merges are serialized with a gate between them, and --match-head-commit pins each merge to its checked head |
| T-37-06 | Denial of service | broken build after a bump | mitigate | Per-merge stage-labelled gate; `git revert` only on a bump-attributable stage regression (G-54); served-route check with the build fingerprint after every site/ merge and at the end (G-51, G-61) |
| T-37-07 | Repudiation | a PR closed without a reason | mitigate | Closes happen only through 37-close-pr.sh (comment first) |
| T-37-SC | Tampering | npm installs | accept | Dependabot bumps of existing packages only; `npm ci` from the merged lockfile |
</threat_model>

<verification>
- #42/#43/#41/#38 are not OPEN, and each has a log line.
- npm audit --omit=dev critical == 0.
- 37-gate.sh is green after every merge (or a G-54 decision is logged); every site/ merge has its per-merge `served_routes: PASS deployable=<D> built_at=<ts> (#N)` line, logged before the next PR (G-61); served routes PASS on origin/master with a fresh build fingerprint (`served_routes: PASS deployable=<D> built_at=<ts>`).
- No security PR was closed while PENDING or for a freshness/install cause.
</verification>

<success_criteria>
The runtime security bumps are on master and prod, and every decision is written down.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-02-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

- **R-01 (G-51):** the served-route call stays on origin/master (the script now resolves the deployable SHA D); 30-min cap and rule (d); verify greps the `served_routes: PASS deployable= built_at=` line. Twins: must_haves truth 4, interfaces, threat T-37-06, verification.
- **R-02 (G-53):** step 2 treats exit 6 (stale base) like exit 3: `@dependabot rebase`, 45-min wait, optional `--stale-ok` worktree gate. Interfaces note that all four bases predate Phases 35/36.
- **R-03 (G-53):** step 2 branches on exit 5 (poll; never close for pending) vs exit 4 (read `--log-failed`, classify by stage, rebase once on freshness/install causes). "Dependabot re-proposes the fix on its next weekly run" replaced by "the alert stays open; 37-03 Task 2 handles the residual". Twin: must_haves truth 1, verification.
- **R-05 (G-54):** step 3 revert rule by stage (bump-attributable stage regression only; INSTALL retry then pause; freshness red stops the queue with no revert/close; LINT stop).
- **R-07:** step 1 follows "Superseded by #M" to the successor; verify checks any logged successor is not OPEN.
- **R-08 / decision (a) (G-55):** exit 7 → close with the owner-reserved major reason.
- **R-09:** `git pull --rebase --autostash origin master` in steps 3-4 and in must_haves/key_links.

## Revision R2 (gate R1)

- **BF-05 (G-61):** step 3 adds the per-merge `37-served-routes.sh <merge sha>` (60 s, 30-min cap, rule (d) on THIS merge) before the next PR, logged `served_routes: PASS deployable=<D> built_at=<ts> (#N)`; a revert names that merge. The end-of-batch check stays. Twins: must_haves truths 2 and 4, key_links, verify (one `(#N)` line per MERGED, not reverted, PR), Expected, done, threat T-37-06, verification.
- **BF-02 twin (R-05/G-54):** step 3's reference is now 'the 37-01 baseline gate' (was the non-existent 'snapshot gate'); interfaces list the VALIDATE, VALIDATE:parse and TOOLCHAIN lines.
- **R-09 twin:** the final docs push uses `git pull --rebase --autostash origin master`.
- **Non-blocker applied:** NB-06, the exit-4 freshness cause now stops the queue (G-54) instead of 'treat as exit 5'; an install cause gets one rebase and a re-classification.
