---
phase: 37-dependency-security-hygiene
plan: 07
type: execute
wave: 7
depends_on: [37-06]
files_modified:
  - .planning/phases/37-dependency-security-hygiene/37-VERIFICATION.md
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
autonomous: true
requirements: [DEPS-01, DEPS-02, DEPS-03, DEPS-04]
executor: orchestrator (re-measure, residual triage, close gate); fresh opus validator (verification)

must_haves:
  truths:
    - "DEPS-01 is measured at close: site/package.json astro ≥ 7.2.8; Dependabot alert #22 (GHSA-26w7-cxv4-gfx2) has state fixed; `npm audit --omit=dev` critical == 0."
    - "DEPS-02 is measured at close (UTC now), only after the first Saturday Dependabot run (≥ 15:30Z) that follows the last merge of 37-02..37-05, with that wave triaged (G-58): open Dependabot PRs ≤ 3 AND max(now − createdAt) ≤ 14 days. Every PR that was open at the 37-01 snapshot is MERGED or CLOSED, and each close carries a reason comment. Majors in the wave (e.g. vitest 5, @astrojs/react 7) are closed with the G-55 owner-reserved reason even if green, never merged. Closes are counted by reason type."
    - "DEPS-03: 37-DEPS03.md has a Decision that matches the state of the PR recorded in its `deps03_pr:` line (#37 or its successor)."
    - "DEPS-04: DEPLOYMENT.md has both DEPS-04 sections, and the latest COMPLETED CodeQL run on master is success (in-progress runs are awaited, not read)."
    - "The close gate runs at ONE HEAD: 37-gate.sh green (every STAGE rc=0) with counts ≥ the Phase-36 close baseline (directive RUN STATUS) + 37-06's new tests. 37-VERIFICATION.md records `served_routes: PASS deployable=<D> built_at=<ts>` for origin/master at that HEAD (G-51)."
    - "Every security PR closed without merge during the phase is counted and flagged as a finding in 37-VERIFICATION.md (R-03)."
    - "A fresh opus validator signs 37-VERIFICATION.md against gh/npm/prod evidence, not against the SUMMARYs."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-VERIFICATION.md
      provides: "`DEPS-0N: PASS|FAIL` lines with evidence, close_sha, gate counts, `served_routes: PASS deployable= built_at=`, the Saturday wave triaged, closes by reason, security PRs closed unmerged, validator verdict"
  key_links:
    - from: 37-VERIFICATION.md
      to: ROADMAP `- [x] **Phase 37`
      via: "only when all four DEPS lines are PASS"
      pattern: "^DEPS-0[1-4]: PASS"
---

<objective>
Close Phase 37: re-measure every DEPS criterion live, triage any PR that Dependabot opened during the phase so DEPS-02 holds at close, run the close gate, get an independent verification, and update the run's records.

Purpose: the backlog is a moving target. Dependabot's weekly runs can open up to 5 PRs per ecosystem while the phase executes, and the queue has been frozen at the limit, so emptying it releases a wave on the next Saturday run. DEPS-02 is only true if it is measured at close, after that wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
@.planning/phases/37-dependency-security-hygiene/37-DEPS03.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
- The tools are 37-merge-pr.sh [--check-only], 37-close-pr.sh, 37-gate.sh and 37-served-routes.sh (37-01).
- The DEPS-02 measurement command is `gh pr list --author app/dependabot --state open --limit 50 --json number,createdAt`.
- Alert state: `gh api repos/xenaquis/is-chile-safe/dependabot/alerts/22 --jq .state` (measured open 2026-09-24).
- Dependabot creates version PRs on Saturdays around 14:52-14:53Z (measured: #18 08-08, #26/#27/#28 08-22, #30/#31/#33 08-29, #35/#36/#37 09-05). npm and pip each sat at open-pull-requests-limit 5. Waiting in the queue (npm outdated, premortem): @astrojs/react 7.0.0 (major of a pinned peer), vitest 5.0.1 (major), astro 7.3.5, react/react-dom/@types 19.3.0, @astrojs/sitemap 3.7.4; openai 3.19.2 on PyPI.
- Majors are reserved for the owner (G-55); the weekly triage owner and the dated issue come from 37-06 (G-56).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure, triage new PRs, close gate, verification</name>
  <files>.planning/phases/37-dependency-security-hygiene/37-VERIFICATION.md, .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-06-SUMMARY.md must exist. Otherwise STOP.

1. Wait for the Saturday wave (G-58): find the last merge of 37-02..37-05 in 37-TRIAGE-LOG.md; the measurement may start only after the first Saturday ≥ 15:30Z that follows it. Then list the PRs Dependabot opened in that run and triage them, together with any PR > 14 days, with the same per-PR procedure as 37-02 Task 1 (exit-code branches, `--autostash`, G-54 revert rule; merge when every check is SUCCESS on a current base and the gate is green; otherwise close with a written reason). Apply the DEPLOYMENT.md policy for majors: every major (e.g. vitest 5, @astrojs/react 7, TypeScript 7) is closed with the G-55 reason "major upgrade reserved for owner review; reopen or @dependabot recreate when ready" even if green; CLAUDE.md-pinned items and openai included (openai 2→3.x only through DEPS-03). No `@dependabot ignore` commands. Then measure DEPS-02. Run at most 2 passes. If it still does not hold, record DEPS-02: FAIL with the list. The phase does not close; this goes to a gap plan.
2. Run the close gate once at HEAD H (37-gate.sh in the background; record the wall time, counts and STAGE lines). Run 37-served-routes.sh on origin/master H every 60 s until PASS (30-min cap; rule (d) of G-51 at the cap: no revert on a stale fingerprint, pause point 5 with the Cloudflare-log owner question).
3. Measure DEPS-01: the astro version, alert #22 state == "fixed", and the npm audit metadata. Measure DEPS-03: the 37-DEPS03.md Decision against the state of the PR in its `deps03_pr:` line. Measure DEPS-04: the two DEPLOYMENT.md headings and the latest COMPLETED codeql.yml master run (wait for in_progress runs, capped at 30 min, R-12).
4. Write 37-VERIFICATION.md with:
   - `close_sha: <H>`;
   - `gate: pytest <p>/<s>/<x> vitest <n> validators <k>/<k> astro-check 0 errors lint 0 sha-pins 0 secret-hygiene 0`;
   - `served_routes: PASS deployable=<D> built_at=<ts>` (the local gate is not presented as evidence of prod);
   - `dependabot_wave: <Saturday YYYY-MM-DD> triaged`;
   - `closes_by_reason: superseded=<a> major-owner=<b> red-ci=<c> other=<d>`;
   - `security_prs_closed_unmerged: <n> <list>`, each one flagged as a finding (R-03);
   - `DEPS-01: PASS|FAIL — <evidence>` through `DEPS-04: ...`;
   - `open_dependabot_prs: <n> max_age_days: <d>`;
   - residual alerts by severity (Dependabot + CodeQL), each one routed to the deferred-live list;
   - spend.
5. Spawn a FRESH opus validator with only this plan, 37-VERIFICATION.md and read-only gh/npm/curl access. It re-runs the DEPS measurements itself and appends `validator: PASS|FAIL — <notes>`.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && V=.planning/phases/37-dependency-security-hygiene/37-VERIFICATION.md && grep -qE "^close_sha: [0-9a-f]{7,40}" $V && test "$(grep -cE '^DEPS-0[1-4]: (PASS|FAIL)' $V)" -eq 4 && grep -qE "^validator: (PASS|FAIL)" $V && grep -qE "^served_routes: PASS deployable=[0-9a-f]{7,40} built_at=" $V && grep -qE "^closes_by_reason: " $V && grep -qE "^security_prs_closed_unmerged: [0-9]+" $V && python -c "import re,datetime;t=open('$V',encoding='utf-8').read();d=re.search('^dependabot_wave: ([0-9-]{10}) triaged',t,re.M).group(1);print('wave',d);assert datetime.date.fromisoformat(d).weekday()==5" && gh pr list --author app/dependabot --state open --limit 50 --json number,createdAt | python -c "import json,sys,datetime as d;p=json.load(sys.stdin);n=d.datetime.now(d.timezone.utc);a=[(n-d.datetime.fromisoformat(x['createdAt'].replace('Z','+00:00'))).days for x in p];print('open',len(p),'max_age',max(a) if a else 0);assert len(p)<=3 and (not a or max(a)<=14)" && test "$(gh api repos/xenaquis/is-chile-safe/dependabot/alerts/22 --jq .state)" = fixed</automated>
    Expected: 4 DEPS lines, a validator line, the served-routes line with deployable/built_at, the closes-by-reason and security-closes lines, a triaged Saturday wave, ≤ 3 open PRs with none > 14 days (measured after that wave), alert #22 fixed.
  </verify>
  <done>Every DEPS criterion is re-measured live and independently verified.</done>
</task>

<task type="auto">
  <name>Task 2: Records + push</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/v2.2-AUTONOMOUS-DIRECTIVE.md</files>
  <action>
Only if all four DEPS lines are PASS and the validator is PASS. Otherwise write a gap summary into RUN STATUS and stop; do not tick anything.

- REQUIREMENTS.md: tick DEPS-01..04 and set the traceability rows (:106-109) to Complete.
- ROADMAP.md: `- [x] **Phase 37` in the phase list, and tick the plan checkboxes under § Phase 37. Preserve CRLF: edit with python, reading and writing the bytes exactly.
- STATE.md: Current Position → Phase 37 complete, Phase 38 next.
- Directive: update RUN STATUS (Phases row, Git, LLM spend including the DEPS-03 spend, NEXT ACTION → Phase 38). Append deferred-live lines for:
  - residual alerts (major-only fixes, `fixable-but-tooling-crash` residuals from 37-03, CodeQL high/critical);
  - "#<N> closed: openai 3.x migration" if DEPS-03 was CLOSE (N from `deps03_pr:`), with the measured cause;
  - "#18 closed: TypeScript 7 adoption" and every other major closed for the owner (G-55), listed by PR number, package and versions;
  - "Dependabot weekly triage: owner xenaquis, from <first-check date>, issue #<n> (G-56); retitle the issue if the run ends after that date";
  - "set:html review is manual: CodeQL is not assumed to analyze .astro (R-14)";
  - any pause point 5 raised on the Cloudflare build fingerprint (G-51), with the run id and the owner question.
- Commit `docs(37): close phase 37 (DEPS-01..04)`, then `git pull --rebase --autostash origin master` and `git push` (never --force).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && test "$(grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md)" -eq 1 && test "$(grep -cE '^- \[x\] \*\*DEPS-0[1-4]\*\*' .planning/REQUIREMENTS.md)" -eq 4 && file .planning/ROADMAP.md | grep -q CRLF && git fetch -q origin && ! git status -sb | head -1 | grep -q ahead</automated>
    Expected: Phase 37 is ticked, the 4 DEPS requirements are ticked, ROADMAP is still CRLF, and everything is pushed.
  </verify>
  <done>Phase 37 is closed on the record and pushed. Phase 38 is next.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Dependabot → open PR set during the phase | new PRs can arrive at any time |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-20 | Repudiation | self-graded close | mitigate | A fresh opus validator re-measures from gh/npm/prod |
| T-37-21 | Tampering | late PRs merged without gates | mitigate | The same scripts and per-merge gate; majors never merged (G-55); at most 2 passes; otherwise FAIL and a gap plan |
| T-37-SC | Tampering | installs | accept | Only via the Dependabot PR procedure |
</threat_model>

<verification>
- 37-VERIFICATION.md has 4 DEPS lines PASS, a validator PASS, and close_sha.
- ≤ 3 open Dependabot PRs, none > 14 days, measured after the first Saturday wave that follows the last merge (G-58); alert #22 fixed.
- `served_routes: PASS deployable=<D> built_at=<ts>` recorded; security PRs closed unmerged counted and flagged.
- Records are updated and pushed.
</verification>

<success_criteria>
Phase 37's four success criteria are true at close and independently verified.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-07-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

- **R-08 (G-58) + decision (a) (G-55):** Task 1 step 1 waits for the first Saturday Dependabot run (≥ 15:30Z) after the last merge of 37-02..37-05 and triages that wave before measuring DEPS-02; "a red major is closed" replaced by "every major is closed with the owner-reserved reason even if green"; closes counted by reason type. Task 2 adds deferred-live lines for the majors closed for the owner and for the weekly-triage owner/issue (decision (b), G-56). Twins: must_haves truth 2, objective purpose, interfaces, verify (Saturday wave date, closes_by_reason), threat T-37-21, verification.
- **R-01 (G-51):** must_haves truth 5 and step 4 record `served_routes: PASS deployable=<D> built_at=<ts>`; step 2 uses the 30-min cap and rule (d); verify greps the line; Task 2 adds a deferred-live line for any Cloudflare-build pause.
- **R-03:** security PRs closed without merge are counted and flagged (`security_prs_closed_unmerged:`), new truth + verify.
- **R-07:** DEPS-03 is measured against the PR in `deps03_pr:` (#37 or successor); the deferred-live line uses that number.
- **R-12:** DEPS-04 reads the latest COMPLETED CodeQL run and waits for in-progress runs.
- **R-06 / R-14 twins:** deferred-live lines for `fixable-but-tooling-crash` residuals and "set:html review is manual".
- **R-09:** `--autostash` in Task 2 and in the inherited per-PR procedure.
