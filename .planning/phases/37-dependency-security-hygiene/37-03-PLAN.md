---
phase: 37-dependency-security-hygiene
plan: 03
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - site/package.json (via PR merges)
  - site/package-lock.json (via PR merges and, conditionally, npm audit fix)
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-02, DEPS-01]
executor: sonnet (merges + gate); orchestrator (deploy polling + served-route curl)

must_haves:
  truths:
    - "#27 @astrojs/react, #40 @vitest/mocker+vitest, #28 vitest, #30 @types/react-dom and #34 fast-uri are each merged through 37-merge-pr.sh, or closed through 37-close-pr.sh with a written reason."
    - "#28 is closed as superseded, and not merged, when #40's vitest target is >= 4.1.11."
    - "@astrojs/react stays on major 6 and React stays on 19 (CLAUDE.md version table)."
    - "After Task 2, `npm audit --omit=dev` reports 0 critical and 0 high, except advisories whose only fix is a semver-major bump. Those are listed by GHSA id in the triage log and go to deferred-live."
    - "Prod serves master HEAD: 37-served-routes.sh passes."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
      provides: "merged/closed lines, the #40 vitest version, and residual advisories with their fix type"
  key_links:
    - from: npm audit fix (conditional)
      to: site/package-lock.json
      via: "semver-compatible fix only (never --force)"
      pattern: "npm audit fix"
---

<objective>
DEPS-02 (non-security site backlog) plus the residual transitive advisories. Land the remaining site patch/minor PRs, then clear any runtime advisory that no PR covers (measured: nanoid alert #14, which has no Dependabot PR) with a semver-compatible `npm audit fix`.

Purpose: DEPS-02 needs ≤ 3 open PRs, none older than 14 days (#27/#28 are 33 days old at planning). The phase goal is to clear the alert backlog, not only the critical one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
@.planning/phases/37-dependency-security-hygiene/37-02-SUMMARY.md
@CLAUDE.md

<interfaces>
- The tools are the same as in 37-02 (37-merge-pr.sh [--check-only], 37-close-pr.sh, 37-gate.sh, 37-served-routes.sh).
- Measured 2026-09-24:
  - #27 @astrojs/react 6.0.2→6.0.4 (runtime);
  - #40 "@vitest/mocker and vitest" (dev; closes alerts #25/#26, GHSA-82fw-gwwq-j7x9, fixed in 4.1.11);
  - #28 vitest 4.1.10→4.1.11 (dev);
  - #30 @types/react-dom 19.2.4→19.2.5 (dev);
  - #34 fast-uri 3.1.5→3.1.7 (dev; closes alerts #15-18, fixed in 3.1.6).
- nanoid alert #14 (GHSA-2v37-7h3g-55p8, high, runtime, patched 3.3.18) has no PR. npm audit says "fix available via `npm audit fix`", which is a non-force, semver-compatible fix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialized merge of #27, #40, #30, #34; #28 superseded check</name>
  <files>site/package.json, site/package-lock.json (via merges), .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-02-SUMMARY.md must exist. Otherwise STOP.

For N in 27, 40, 30, 34, in order, apply exactly the per-PR procedure of 37-02 Task 1: auto-closed check, --check-only, `@dependabot rebase` with a 45-min wait, the close-with-reason path on red checks, merge, `git pull --rebase`, 37-gate.sh green, the revert path on a red gate, and push.

Extra check for #27: after the merge, `node -p "require('./site/package.json').dependencies['@astrojs/react']"` must start with ^6 or 6, and `react` must stay ^19. If a rebased head changed either major, do not merge it; close it with the reason "major change to a CLAUDE.md-pinned peer; needs its own phase".

#28: read #40's vitest target from `gh pr view 40 --json title,body`, or from site/package.json after its merge. If it is >= 4.1.11 and #28 is still OPEN, close #28 with the reason "Superseded by #40 (vitest → <ver>, merged <sha7>), which already includes 4.1.11 and the GHSA-82fw-gwwq-j7x9 fix. Closed under the weekly triage policy (DEPLOYMENT.md § Dependency triage policy)." Otherwise run the normal merge procedure for #28.

Commit `docs(37): site patch PRs triaged` and push.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && for N in 27 40 28 30 34; do s=$(gh pr view $N --json state -q .state); echo "#$N $s"; [ "$s" != OPEN ] || exit 1; grep -qE "^(merged|closed|auto-closed|reverted) #$N[ :]" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || exit 1; done && node -e "const p=require('./site/package.json');const r=p.dependencies['@astrojs/react'],x=p.dependencies.react;console.log(r,x);process.exit(/^\^?6\./.test(r)&&/^\^?19\./.test(x)?0:1)" && test "$(gh pr view 28 --json state -q .state)" != MERGED -o "$(gh pr view 40 --json state -q .state)" != MERGED</automated>
    Expected: all five PRs are not OPEN and each has a log line; @astrojs/react is 6.x and react 19.x; #28 and #40 are not both MERGED (the superseded rule). 37-gate.sh exit 0 after each merge (background logs).
  </verify>
  <done>The site patch backlog is resolved, with the peer pins intact.</done>
</task>

<task type="auto">
  <name>Task 2: Residual transitive advisories (semver-compatible npm audit fix) + served routes</name>
  <files>site/package-lock.json, .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
1. In site/, run `npm audit --omit=dev --json` and log each remaining advisory: package, severity, GHSA, and fixAvailable (true, false, or {isSemVerMajor}).
2. If any critical or high advisory has fixAvailable === true (non-major), run `npm audit fix`. NEVER use `--force`, because that permits majors.
   - Require `git diff --stat` to show ONLY site/package-lock.json, with site/package.json unchanged; otherwise `git checkout -- site/` and log "audit fix touched package.json; skipped".
   - Run 37-gate.sh (green), commit `build(deps): npm audit fix for residual transitive advisories (DEPS-01/02)`, then `git pull --rebase` and push (G-47).
   - On a red gate, `git checkout -- site/`, log it, and do not commit.
3. Log each advisory that is still open with the reason it remains, for example "only fix is semver-major <pkg>@<ver>". These become deferred-live lines in 37-07.
4. The orchestrator runs 37-served-routes.sh on origin/master HEAD until PASS (15 min cap).
5. Commit the log (`docs(37): residual advisories`) and push.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm audit --omit=dev --json > "$TEMP/37-03-audit.json"; python -c "import json,os;d=json.load(open(os.path.join(os.environ['TEMP'],'37-03-audit.json')));m=d['metadata']['vulnerabilities'];print(m);assert m['critical']==0;bad=[k for k,v in d['vulnerabilities'].items() if v['severity'] in ('high','critical') and v.get('fixAvailable') is True];print('fixable high/critical left',bad);assert not bad" && cd .. && git diff --quiet origin/master -- site/package.json site/package-lock.json && bash .planning/phases/37-dependency-security-hygiene/37-served-routes.sh "$(git rev-parse origin/master)"</automated>
    Expected: critical == 0, no high/critical advisory with a non-major fix is left, and the local lockfile equals origin/master (pushed). Served routes PASS.
  </verify>
  <done>npm audit --omit=dev shows no critical and no fixable high advisory, any major-only residuals are logged, and prod is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | `npm audit fix` resolves new transitive versions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | Tampering | npm audit fix | mitigate | Non-force only (no majors); the diff must be package-lock.json only; the full gate runs before commit |
| T-37-09 | Denial of service | peer drift (React/@astrojs/react) | mitigate | Major pins asserted after #27 |
| T-37-SC | Tampering | transitive installs | accept | The versions are patch/minor of packages already in the tree and are resolved by the audit advisory database, with no new top-level package |
</threat_model>

<verification>
- #27/#40/#28/#30/#34 are not OPEN, and each has a log line.
- npm audit: 0 critical, and no fixable high left.
- Served routes PASS on origin/master.
</verification>

<success_criteria>
The site backlog is empty of the measured PRs, and the runtime audit is clean apart from major-only residuals, which are documented.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-03-SUMMARY.md` when done.
</output>
