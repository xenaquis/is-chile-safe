---
phase: 37-dependency-security-hygiene
plan: 03
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - site/package.json (via PR merges)
  - site/package-lock.json (via PR merges and, conditionally, npm audit fix or the G-52 lockfile-only fallback)
  - .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
autonomous: true
requirements: [DEPS-02, DEPS-01]
executor: sonnet (merges + gate); orchestrator (deploy polling + served-route curl)

must_haves:
  truths:
    - "#27 @astrojs/react, #40 @vitest/mocker+vitest, #28 vitest, #30 @types/react-dom and #34 fast-uri are each merged through 37-merge-pr.sh, or closed through 37-close-pr.sh with a written reason (a major is closed with the G-55 owner-reserved reason, never merged). Each merge is followed by its per-merge served-route PASS before the next PR (G-61). A PR that Dependabot supersedes is followed to its successor #M (R-07)."
    - "#28 is closed as superseded, and not merged, when #40's vitest target is >= 4.1.11."
    - "@astrojs/react stays on major 6 and React stays on 19 (CLAUDE.md version table)."
    - "After Task 2, `npm audit --omit=dev` reports 0 critical and 0 high, except advisories whose only fix is a semver-major bump, or that are logged `fixable-but-tooling-crash` because every G-52 tool path crashed or was rejected. Those are listed by GHSA id in the triage log and go to deferred-live."
    - "Prod serves master HEAD: 37-served-routes.sh on origin/master passes with a fresh build fingerprint (G-51), logged as `served_routes: PASS deployable=<D> built_at=<ts>`; a residual fix pushed from Windows counts as done only after that."
  artifacts:
    - path: .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
      provides: "merged/closed lines, the #40 vitest version, and residual advisories with their fix type"
  key_links:
    - from: npm audit fix (conditional)
      to: site/package-lock.json
      via: "semver-compatible fix only (never --force); on a crash, the G-52 fallback `npm update <pkg> --package-lock-only`; the diff under site/ must be lockfile-only (G-60)"
      pattern: "npm audit fix"
---

<objective>
DEPS-02 (non-security site backlog) plus the residual transitive advisories. Land the remaining site patch/minor PRs, then clear any runtime advisory that no PR covers (measured: nanoid alert #14, which has no Dependabot PR) with a semver-compatible `npm audit fix`, or with the G-52 lockfile-only fallback when `npm audit fix` crashes (measured today).

Purpose: DEPS-02 needs ≤ 3 open PRs, none older than 14 days (#27/#28 are 33 days old at planning). The phase goal is to clear the alert backlog, not only the critical one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md
@.planning/phases/37-dependency-security-hygiene/37-02-SUMMARY.md
@CLAUDE.md

<interfaces>
- The tools are the same as in 37-02 (37-merge-pr.sh [--check-only], 37-close-pr.sh, 37-gate.sh, 37-served-routes.sh).
- Measured 2026-09-24:
  - #27 @astrojs/react 6.0.2→6.0.4 (runtime);
  - #40 "@vitest/mocker and vitest" (dev; closes alerts #25/#26, GHSA-82fw-gwwq-j7x9, fixed in 4.1.11);
  - #28 vitest 4.1.10→4.1.11 (dev);
  - #30 @types/react-dom 19.2.4→19.2.5 (dev);
  - #34 fast-uri 3.1.5→3.1.7 (dev; closes alerts #15-18, fixed in 3.1.6).
- nanoid alert #14 (GHSA-2v37-7h3g-55p8, high, runtime, patched 3.3.18) has no PR. npm audit says "fix available via `npm audit fix`", which is a non-force, semver-compatible fix.
- BUT `npm audit fix --dry-run` crashes on this tree: "Cannot read properties of null (reading 'edgesOut')", rc=1 (premortem, and re-measured 2026-09-25 on a scratch copy of site/package*.json with `--package-lock-only`). nanoid is 3.3.17 at package-lock.json:4857-4858 (a single `node_modules/nanoid` entry); its parent range is `^3.3.16` (:5115); 3.3.18 and 3.3.19 are published. `npm update nanoid --package-lock-only --dry-run` printed "up to date" (inconclusive under dry-run). nanoid reaches the static build via postcss. The tree changes after 37-02/37-03 merges, so re-measure at Task 2.
- Tool exit codes and the per-PR procedure are those of 37-02 Task 1 (G-53 exit codes, G-54 revert rule, `--autostash`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialized merge of #27, #40, #30, #34; #28 superseded check</name>
  <files>site/package.json, site/package-lock.json (via merges), .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
Step 0, the precondition. `grep -c '^- \[x\] \*\*Phase 36' .planning/ROADMAP.md` must print 1, and 37-02-SUMMARY.md must exist. Otherwise STOP.

For N in 27, 40, 30, 34, in order, apply exactly the per-PR procedure of 37-02 Task 1: auto-closed/superseded check (a successor #M is triaged here, R-07), --check-only with its exit-code branches (3/6 rebase, 5 poll and never close, 7 G-55 close, 4 classify before any close), merge, `git pull --rebase --autostash origin master`, 37-gate.sh green, the G-54 revert rule on a red gate, the per-merge `37-served-routes.sh <merge sha>` PASS logged as `served_routes: PASS deployable=<D> built_at=<ts> (#N)` before the next PR (every PR here touches site/; BF-05, G-61), and push.

Extra check for #27: after the merge, `node -p "require('./site/package.json').dependencies['@astrojs/react']"` must start with ^6 or 6, and `react` must stay ^19. If a rebased head changed either major, do not merge it (37-merge-pr.sh already exits 7); close it with the G-55 reason "major upgrade reserved for owner review; reopen or @dependabot recreate when ready (major change to a CLAUDE.md-pinned peer)". If this check fails after a merge, revert that merge.

#28: read #40's vitest target from `gh pr view 40 --json title,body`, or from site/package.json after its merge. If it is >= 4.1.11 and #28 is still OPEN, close #28 with the reason "Superseded by #40 (vitest → <ver>, merged <sha7>), which already includes 4.1.11 and the GHSA-82fw-gwwq-j7x9 fix. Closed under the weekly triage policy (the DEPS-04 policy, being added to DEPLOYMENT.md in Phase 37)." Otherwise run the normal merge procedure for #28.

Commit `docs(37): site patch PRs triaged`, then `git pull --rebase --autostash origin master` and push (R-09).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && for N in 27 40 28 30 34; do s=$(gh pr view $N --json state -q .state); echo "#$N $s"; [ "$s" != OPEN ] || exit 1; grep -qE "^(merged|closed|auto-closed|reverted|superseded) #$N[ :]" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || exit 1; M=$(grep -oE "^superseded #$N -> #[0-9]+" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md | grep -oE "[0-9]+$" | tail -1); [ -z "$M" ] || [ "$(gh pr view $M --json state -q .state)" != OPEN ] || exit 1; [ "$s" != MERGED ] || grep -q "^reverted #$N" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || grep -qE "^served_routes: PASS deployable=[0-9a-f]{7,40} built_at=[^ ]+ \(#$N\)" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md || exit 1; done && node -e "const p=require('./site/package.json');const r=p.dependencies['@astrojs/react'],x=p.dependencies.react;console.log(r,x);process.exit(/^\^?6\./.test(r)&&/^\^?19\./.test(x)?0:1)" && test "$(gh pr view 28 --json state -q .state)" != MERGED -o "$(gh pr view 40 --json state -q .state)" != MERGED</automated>
    Expected: all five PRs (and any logged successor) are not OPEN and each has a log line; each MERGED, not reverted, PR has its per-merge `served_routes: PASS … (#N)` line (G-61); @astrojs/react is 6.x and react 19.x; #28 and #40 are not both MERGED (the superseded rule). 37-gate.sh exit 0 after each merge (background logs).
  </verify>
  <done>The site patch backlog is resolved, with the peer pins intact.</done>
</task>

<task type="auto">
  <name>Task 2: Residual transitive advisories (semver-compatible npm audit fix) + served routes</name>
  <files>site/package-lock.json, .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</files>
  <action>
1. In site/, run `npm audit --omit=dev --json` and log each remaining advisory: package, severity, GHSA, and fixAvailable (true, false, or {isSemVerMajor}).
2. If any critical or high advisory has fixAvailable === true (non-major) (G-52, amends G-49 on the tool, not the policy):
   a. Run `npm audit fix --dry-run` first and log `audit_fix_dry_run rc=<n>` with its first error line.
   b. rc 0 → run `npm audit fix`. NEVER use `--force`, because that permits majors.
   c. rc ≠ 0 (measured today: the arborist 'edgesOut' crash) → lockfile-only fallback inside the declared range: `npm update <pkg> --package-lock-only` for each fixable package. If that also crashes or leaves the lockfile unchanged, and the package is transitive with the patched version inside its parent's range (nanoid: `^3.3.16` admits 3.3.18), try `npm install <pkg>@<patched> --package-lock-only`. If that writes <pkg> into site/package.json, the diff rule below rejects it.
   d. The same diff rule applies to every path (G-60, amends G-52): `git diff --name-only -- site/` must print exactly `site/package-lock.json`. Files outside site/ (this triage log under .planning/, modified by steps 1 and 2a) are not part of the rule. Otherwise `git checkout -- site/` and log "<tool> changed <the files actually listed>; skipped".
   e. Run 37-gate.sh (green, every STAGE rc=0), commit `build(deps): npm audit fix for residual transitive advisories (DEPS-01/02)` (name the tool actually used in the body), then `git pull --rebase --autostash origin master` and push (G-47). This is the only lockfile of the phase generated on Windows and pushed to master without Linux CI (ci.yml runs on PRs only), so the residual counts as fixed only after 37-served-routes.sh passes with a fresh build fingerprint (G-51).
   f. On a red gate, `git checkout -- site/`, log it, and do not commit.
   g. If every path crashes or is rejected, log `residual <pkg> <GHSA> fixable-but-tooling-crash (arborist edgesOut)` and route it to deferred-live (37-07). Exposure note for that line: nanoid enters the build via postcss on a static site.
3. Log each advisory that is still open with the reason it remains, for example "only fix is semver-major <pkg>@<ver>" or the `fixable-but-tooling-crash` line. These become deferred-live lines in 37-07.
4. The orchestrator runs 37-served-routes.sh on origin/master every 60 s until PASS (30-min cap; rule (d) of G-51 at the cap) and logs `served_routes: PASS deployable=<D> built_at=<ts>`.
5. Commit the log (`docs(37): residual advisories`), `git pull --rebase --autostash origin master`, and push.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm audit --omit=dev --json > "$TEMP/37-03-audit.json"; python -c "import json,os,re;d=json.load(open(os.path.join(os.environ['TEMP'],'37-03-audit.json')));L=open('../.planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md',encoding='utf-8').read();m=d['metadata']['vulnerabilities'];print(m);assert m['critical']==0;bad=[k for k,v in d['vulnerabilities'].items() if v['severity'] in ('high','critical') and v.get('fixAvailable') is True and not re.search('^residual '+re.escape(k)+' [^ ]+ fixable-but-tooling-crash',L,re.M)];print('fixable high/critical left (unmarked)',bad);assert not bad;R=[m.start() for m in re.finditer('^residual [^ ]+ [^ ]+ fixable-but-tooling-crash',L,re.M)];assert all(re.search('^audit_fix_dry_run rc=[1-9]',L[:p],re.M) for p in R),'tooling-crash residual without a failed dry-run'" && cd .. && git diff --quiet origin/master -- site/package.json site/package-lock.json && bash .planning/phases/37-dependency-security-hygiene/37-served-routes.sh "$(git rev-parse origin/master)" && grep -qE "^served_routes: PASS deployable=[0-9a-f]{7,40} built_at=" .planning/phases/37-dependency-security-hygiene/37-TRIAGE-LOG.md</automated>
    Expected: critical == 0; no high/critical advisory with a non-major fix is left unless the log marks it `fixable-but-tooling-crash` (G-52), and each such line comes after an `audit_fix_dry_run rc=<non-zero>` line (G-60); the local lockfile equals origin/master (pushed). Served routes PASS on origin/master with a fresh build fingerprint logged.
  </verify>
  <done>npm audit --omit=dev shows no critical and no fixable high advisory (other than logged `fixable-but-tooling-crash` residuals), any major-only residuals are logged, and prod is verified by the build fingerprint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | `npm audit fix` resolves new transitive versions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | Tampering | npm audit fix / G-52 fallback | mitigate | Non-force only (no majors); dry-run first; fallback stays inside the parent's declared range; the diff under site/ must be package-lock.json only (`git diff --name-only -- site/`, G-60); the full gate runs before commit; served build fingerprint after push |
| T-37-09 | Denial of service | peer drift (React/@astrojs/react) | mitigate | Major pins asserted after #27 |
| T-37-SC | Tampering | transitive installs | accept | The versions are patch/minor of packages already in the tree and are resolved by the audit advisory database, with no new top-level package |
</threat_model>

<verification>
- #27/#40/#28/#30/#34 are not OPEN, and each has a log line.
- npm audit: 0 critical, and no fixable high left unless logged `fixable-but-tooling-crash` (G-52).
- Served routes PASS on origin/master with a fresh build fingerprint (G-51).
</verification>

<success_criteria>
The site backlog is empty of the measured PRs, and the runtime audit is clean apart from major-only and tooling-crash residuals, which are documented and routed to deferred-live.
</success_criteria>

<output>
Create `.planning/phases/37-dependency-security-hygiene/37-03-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

- **R-06 (G-52):** Task 2 step 2 rewritten: `npm audit fix --dry-run` first (rc logged); on a crash, `npm update <pkg> --package-lock-only`, then `npm install <pkg>@<patched> --package-lock-only` only for a transitive package inside its parent's range; same lockfile-only diff rule; gate + served build fingerprint before the residual counts; otherwise `residual <pkg> <GHSA> fixable-but-tooling-crash (arborist edgesOut)` → deferred-live, and the verify accepts only residuals carrying that mark. Re-measured 2026-09-25 in a scratch copy: the dry-run still crashes (rc=1, edgesOut); `npm update nanoid --package-lock-only --dry-run` printed "up to date" (inconclusive). Note: `npm install <pkg>@<v>` normally saves to package.json, so that sub-path is expected to be rejected by the diff rule. Twins: frontmatter files_modified, must_haves truth 4, key_links, objective, interfaces, verify, done, threat T-37-08, verification, success_criteria.
- **R-01 (G-51):** step 4 30-min cap + rule (d), verify greps the `served_routes:` log line; must_haves truth 5.
- **R-03 / R-05 / R-09 (G-53/G-54):** Task 1 inherits the 37-02 exit-code branches, the stage-based revert rule and `--autostash`.
- **R-07:** Task 1 follows successors; verify checks logged successors.
- **Decision (a) (G-55):** #27's major-drift close reason is now the owner-reserved major text; must_haves truth 1.

## Revision R2 (gate R1)

- **BF-04 (G-60):** step 2d now requires `git diff --name-only -- site/` to print exactly `site/package-lock.json` (the triage log under .planning/ may be modified), else `git checkout -- site/` and log '<tool> changed <files>; skipped'. The optional check is applied: the verify requires a preceding `audit_fix_dry_run rc=<non-zero>` line for every `fixable-but-tooling-crash` residual. Twins: key_links, threat T-37-08, Expected.
- **BF-05 twin (G-61):** Task 1 inherits the per-merge served-route PASS; the verify requires a `(#N)` line for every MERGED, not reverted, PR; must_haves truth 1 and Expected updated.
- **R-09 twin:** Task 1's docs push uses `--autostash`.
- **Non-blocker applied:** NB-13(d), #28's close reason cites the DEPS-04 policy 'being added to DEPLOYMENT.md in Phase 37'.
