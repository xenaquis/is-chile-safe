---
phase: 37
artifact: adversarial gate
round: 1
plans_evaluated: 37-01..37-07 @ dede804
verdict: GO-CONDICIONADO
blockers: 6
agents: 5 (haiku, 3 opus lenses high, fresh opus arbiter xhigh) + citation script
subagent_tokens: 1051736
cycle: 1 of 2
---

# Phase 37 — Adversarial Gate, Round 1

## Verdict: GO-CONDICIONADO

Arbitré las tres lentes (A, B y C, todas NO-GO) sobre los 7 planes en dede804. Verifiqué con mis propias lecturas y mediciones de solo lectura: los planes, el premortem, la directiva (G-46..G-59, reglas duras :76-83), all.mjs y figure-registry, test_workflow_guards, requirements.txt, news-pipeline.yml, ci.yml, 35-07 y 36-06. Además hice curl a prod, `gh pr/run/api` (comentarios de #12/#20, corridas dynamic de Dependabot) y dos scratch: pytest para el exit code de colección y npm audit sobre una copia del lockfile. No escribí en el repo, no hice fetch, no hice push y no llamé a ningún LLM. Solo dejé archivos en el scratchpad y en $TEMP.

Veredicto: GO-CONDICIONADO. Ningún defecto confirmado exige re-diseño. Quedan 6 bloqueantes, todos ediciones mecánicas, que hay que aplicar antes de ejecutar:
- BF-01: el control de 37-served-routes.sh debe exigir rc=0. Hoy acepta 1 y 10, y un exit 1 falso autoriza revertir el fix del RCE.
- BF-02: hace falta un gate de línea base antes de #39, porque la referencia que cita G-54 no existe, y el parseo de VALIDATE necesita un conteo y un rc global, porque hoy puede dar verde vacuo.
- BF-03: el verify de #18 lee el último comentario, y Dependabot siempre contesta después del cierre (medido en #12 y #20).
- BF-04: la regla de diff de G-52 sin pathspec rechaza el fix válido de nanoid y lo registra como crash del tooling. Es un defecto que introdujo la propia edición R-06.
- BF-05: prod se verifica por lote, contra la regla 'after every deploy' de la directiva (:77), y el revert queda sin atribución.
- BF-06: faltan las ediciones que condicionaban aceptar R-15, R-16 y R-17. El commit dice 'no edits'.

Las ediciones del premortem R-01..R-14 están aplicadas en el cuerpo y en casi todos sus gemelos. Hay excepciones parciales: R-05 (el snapshot gate), R-08 (el verify de la ola) y R-09 (tres pushes sin --autostash).

De las observaciones del orquestador:
- o1: 37-01 queda fijado a #39 sin sucesor. Falla cerrada, pero conviene la edición NB-03.
- o2: está cubierta y el daño está acotado por el rebase y los checks CI/CF antes del merge. Faltan el chequeo por merge y el caso del lockfile de G-52.
- o3: descartada; las definiciones de D, C y G excluyen a deploy-on-code y a los commits de Dependabot.
- o4: la rama MERGE es inalcanzable (httpx→httpx2, pytest sale 2) y el plan llega a CLOSE sin gasto midiendo, así que no hace falta pre-declararlo.

Hay 13 no-bloqueantes recomendados: NEUTRAL/SKIPPED, el verify de DEPS-02 con un instrumento concreto (las corridas dynamic 'Update'), fixAvailable como objeto, la huella temporal y el congelamiento por la regla (d), y varias fragilidades de scripts y textos. Descarté A-R1-06 porque con los planes actuales no puede ocurrir.

## Final blockers

| id | origin | title | evidence | required edit |
|---|---|---|---|---|
| BF-01 | A-R1-01 | El verify de 37-served-routes.sh no tiene control positivo, y un exit 1 falso termina revirtiendo #39 | Leído: 37-01-PLAN.md:137 (`[ "$rc" -ne 11 ] && [ "$rc" -ne 12 ]`), :134 y :155 (el revert depende de exit 1). Medido por mí con curl GET a las 2026-09-25T03:36Z: las 8 rutas dan 200, /this-route-does-not-exist-37/ da 404, 'Latest incident: ' aparece 1 vez y data-build-now vale 03:16:05.258Z (/news/) y 03:16:04.927Z (/es/noticias/), ambos ≥ T0 = 00:14:10Z, el createdAt de deploy-on-code 36076592181 para 396eef9 (gh run list). | En el verify de 37-01 Task 1, reemplazar `[ "$rc" -ne 11 ] && [ "$rc" -ne 12 ]` por `[ "$rc" -eq 0 ] && echo "$out" \| grep -qE "^PASS deployable=$D built_at="` y actualizar el Expected. Si ese control no da 0 cuando un curl manual muestra prod sano, el ejecutor hace STOP (error del instrumento) y no pasa a Task 2. Opcional: un control de ruta rota vía variable de test (EXTRA_ROUTE que responde 404 y se exige 200) que debe salir 1. |
| BF-02 | A-R1-02, B-R1-07 | 37-gate.sh nunca se ejecuta antes del primer merge, G-54 remite a un 'snapshot gate' que no existe y VALIDATE puede quedar en verde sin haber validado nada | Leído: 37-01-PLAN.md:89-93 (el snapshot no incluye gate), :120 ('(18 lines)'), :125 (exit 0 si todas las etapas dan rc=0), :137 (el verify solo pasa shellcheck sobre 37-gate.sh). 37-02-PLAN.md:75 ('or the 37-01 snapshot gate'). site/scripts/validate/all.mjs:72-73 (stdio 'inherit'), :97 ('VALIDATION SUITE SUMMARY') y :103-106 (resumen). site/scripts/validate/figure-registry.mjs:229 (console.log `  PASS  ${figure.id}: …`). | En 37-01 Task 1, agregar un paso 'Baseline gate': ejecutar 37-gate.sh sobre master antes de cualquier merge (en background, con tee), exigir GATE rc=0, y copiar sus líneas STAGE/GATE en 37-TRIAGE-LOG.md bajo '## Baseline gate'. Si sale rojo, STOP (rojo previo, no atribuible a la Fase 37). El verify de Task 1 hace grep de '^## Baseline gate' y de '^GATE rc=0 '.<br>En la spec de 37-gate.sh:<br>(a) parsear las líneas VALIDATE solo después de 'VALIDATION SUITE SUMMARY';<br>(b) exigir exactamente 18, y si no, imprimir `STAGE VALIDATE:parse rc=1`;<br>(c) agregar `STAGE VALIDATE rc=<exit de npm run validate>` a la conjunción del GATE.<br>En 37-02:75, cambiar '(or the 37-01 snapshot gate)' por '(or the 37-01 baseline gate)'. |
| BF-03 | B-R1-01 | El verify de 37-04 Task 2 lee `.comments[-1]` y falla por construcción, porque Dependabot responde después del cierre | Leído: 37-04-PLAN.md:97 (`.comments[-1].body \| grep -q "major upgrade, reserved for owner review"`). Medido por mí con `gh pr view 12/20 --json comments`: en #12, xenaquis 18:34:11Z y luego dependabot 18:34:14Z 'OK, I won't notify you again about this release…'; en #20, xenaquis 18:34:19Z y luego dependabot 18:34:23Z. 37-01-PLAN.md:113: 37-close-pr.sh primero comenta y después cierra. | En el verify de 37-04:97, reemplazar la lectura por `gh pr view $T --json comments -q '[.comments[]\|select(.author.login!="dependabot")\|.body]\|join("\n")' \| grep -q "major upgrade, reserved for owner review"` y ajustar el Expected ('un comentario no-Dependabot lleva el motivo'). Aplicar la misma lectura a cualquier otro verify que lea el motivo de un cierre. |
| BF-04 | B-R1-02 | La regla de diff de 37-03 (paso 2d) rechaza por construcción el fix correcto, porque el triage log está modificado | Leído: 37-03-PLAN.md:90 (el paso 1 escribe en el log), :92 (2a escribe en el log), :95 (2d: '`git diff --stat` must show ONLY site/package-lock.json … otherwise `git checkout -- site/`'), :98 (2g) y :104 (el verify acepta `^residual … fixable-but-tooling-crash`). En ~/.claude/get-shit-done/workflows/execute-plan.md:277 está el 'Task Commit Protocol' (commit por tarea). | En 37-03:95, exigir que `git diff --name-only -- site/` imprima exactamente `site/package-lock.json` (el log bajo .planning/ puede estar modificado). Si no, `git checkout -- site/` y registrar '<tool> changed <archivos reales>; skipped'. Aclarar el gemelo en el texto de G-52 ('diff under site/ must be lockfile-only'). Opcional: que el verify exija, antes de cada línea `residual … fixable-but-tooling-crash`, una línea `audit_fix_dry_run rc=` distinta de 0. |
| BF-05 | C-R1-01 | Prod se verifica al final del lote y no después de cada deploy, contra la regla dura de la directiva, y el revert de la regla (d) queda sin un merge concreto que revertir | Leído: v2.2-AUTONOMOUS-DIRECTIVE.md:77 ('After every deploy, curl prod and verify'), :144 (G-47, que no la enmienda) y :148 (G-51). 37-02-PLAN.md:67-81 (el procedimiento por PR, pasos 1-4, no tiene served-routes; :81 dice 'After the batch'). 37-03-PLAN.md:71 (lo hereda) y :100. 37-07-PLAN.md:72-73. 37-01-PLAN.md:134 ('revert <merge sha7>'). | En 37-02 Task 1 paso 3, que heredan 37-03 Task 1 y la ola de 37-07: después de cada merge que toque site/ y de su gate verde, el orquestador ejecuta `37-served-routes.sh <merge sha>` (cada 60 s, tope de 30 min, regla (d)) ANTES de tocar el siguiente PR. Se registra `served_routes: PASS deployable=<D> built_at=<ts> (#N)` por merge, y un revert por la regla (d) nombra ese merge. El verify de 37-02 exige una línea así por cada PR de site/ MERGED. El chequeo al final del lote se mantiene. La alternativa (peor) es registrar una G-60 que declare la desviación de :77, con la razón y una regla de atribución para el revert por lote. |
| BF-06 | A-R1-10, B-R1-04, C-R1-02 | No se aplicaron las ediciones que eran condición para aceptar R-15, R-16 y R-17 | Leído: 37-PREMORTEM.md:83 ('Única edición: 37-gate.sh imprime node --version; npm --version; python --version'), :84 ('debe listar … httpx2 … force_backup=true … resp.provider y usage.cost … recalcular') y :85 ('Edición mínima: antes de 37-01, anotar en RUN STATUS la lista de PRs'). También `git log -1 2d62e52` ('R-15..R-17 accepted (no edits)'), 37-01-PLAN.md:125 (GATE sin versiones), 37-07-PLAN.md:106 y 37-05-PLAN.md:78. Mi grep de 'node --version\|TOOLCHAIN\|force_backup\|usage\.cost\|resp\.provider\|ratif' sobre 37-0*-PLAN.md dio 0 coincidencias. | (1) En la spec de 37-gate.sh, imprimir `TOOLCHAIN node=<v> npm=<v> python=<v>` antes de la línea GATE y copiarla al log.<br>(2) En 37-07 Task 2, el deferred-live '#<N> closed: openai 3.x migration' lista literalmente los cuatro requisitos de R-16.<br>(3) En 37-01 Task 1 paso 1, escribir en RUN STATUS la lista de PRs candidatos a merge. En 37-07 Task 2 y en el reporte final, agregar el ítem para el owner 'Ratificar G-47 (merges con la cuenta del owner)' con esa lista y los PRs efectivamente mergeados.<br>(4) Agregar R-15/R-16/R-17 a las secciones 'Revision R1' de 37-01 y 37-07. |

## Conditions

1. BF-01: En el verify de 37-01 Task 1, reemplazar `[ "$rc" -ne 11 ] && [ "$rc" -ne 12 ]` por `[ "$rc" -eq 0 ] && echo "$out" | grep -qE "^PASS deployable=$D built_at="` y actualizar el Expected. Si ese control no da 0 cuando un curl manual muestra prod sano, el ejecutor hace STOP (error del instrumento) y no pasa a Task 2. Opcional: un control de ruta rota vía variable de test (EXTRA_ROUTE que responde 404 y se exige 200) que debe salir 1.
2. BF-02: En 37-01 Task 1, agregar un paso 'Baseline gate': ejecutar 37-gate.sh sobre master antes de cualquier merge (en background, con tee), exigir GATE rc=0, y copiar sus líneas STAGE/GATE en 37-TRIAGE-LOG.md bajo '## Baseline gate'. Si sale rojo, STOP (rojo previo, no atribuible a la Fase 37). El verify de Task 1 hace grep de '^## Baseline gate' y de '^GATE rc=0 '. En la spec de 37-gate.sh: (a) parsear las líneas VALIDATE solo después de 'VALIDATION SUITE SUMMARY'; (b) exigir exactamente 18, y si no, imprimir `STAGE VALIDATE:parse rc=1`; (c) agregar `STAGE VALIDATE rc=<exit de npm run validate>` a la conjunción del GATE. En 37-02:75, cambiar '(or the 37-01 snapshot gate)' por '(or the 37-01 baseline gate)'.
3. BF-03: En el verify de 37-04:97, reemplazar la lectura por `gh pr view $T --json comments -q '[.comments[]|select(.author.login!="dependabot")|.body]|join("\n")' | grep -q "major upgrade, reserved for owner review"` y ajustar el Expected ('un comentario no-Dependabot lleva el motivo'). Aplicar la misma lectura a cualquier otro verify que lea el motivo de un cierre.
4. BF-04: En 37-03:95, exigir que `git diff --name-only -- site/` imprima exactamente `site/package-lock.json` (el log bajo .planning/ puede estar modificado). Si no, `git checkout -- site/` y registrar '<tool> changed <archivos reales>; skipped'. Aclarar el gemelo en el texto de G-52 ('diff under site/ must be lockfile-only'). Opcional: que el verify exija, antes de cada línea `residual … fixable-but-tooling-crash`, una línea `audit_fix_dry_run rc=` distinta de 0.
5. BF-05: En 37-02 Task 1 paso 3, que heredan 37-03 Task 1 y la ola de 37-07: después de cada merge que toque site/ y de su gate verde, el orquestador ejecuta `37-served-routes.sh <merge sha>` (cada 60 s, tope de 30 min, regla (d)) ANTES de tocar el siguiente PR. Se registra `served_routes: PASS deployable=<D> built_at=<ts> (#N)` por merge, y un revert por la regla (d) nombra ese merge. El verify de 37-02 exige una línea así por cada PR de site/ MERGED. El chequeo al final del lote se mantiene. La alternativa (peor) es registrar una G-60 que declare la desviación de :77, con la razón y una regla de atribución para el revert por lote.
6. BF-06: (1) En la spec de 37-gate.sh, imprimir `TOOLCHAIN node=<v> npm=<v> python=<v>` antes de la línea GATE y copiarla al log. (2) En 37-07 Task 2, el deferred-live '#<N> closed: openai 3.x migration' lista literalmente los cuatro requisitos de R-16. (3) En 37-01 Task 1 paso 1, escribir en RUN STATUS la lista de PRs candidatos a merge. En 37-07 Task 2 y en el reporte final, agregar el ítem para el owner 'Ratificar G-47 (merges con la cuenta del owner)' con esa lista y los PRs efectivamente mergeados. (4) Agregar R-15/R-16/R-17 a las secciones 'Revision R1' de 37-01 y 37-07.

## Non-blockers

| id | title | detail |
|---|---|---|
| NB-01 | Los gemelos se contradicen sobre NEUTRAL, SKIPPED y STALE (A-R1-07, B-R1-05, C-R1-08d) | El artifact (37-01:28), la truth (:22) y G-47 (directiva :144) exigen 'SUCCESS'. Pero las reglas 6 y 7 de la acción (:107-108) dejan mergear un check COMPLETED con NEUTRAL o SKIPPED, y según el artifact ese caso sería un 5 perpetuo. Hoy ci.yml no tiene `if:`, así que no hay SKIPPED. Después de 37-06 aparecen checks de CodeQL en los PRs. Edición: un mapeo explícito. NEUTRAL o SKIPPED se aceptan solo en checks que no sean de los 4 requeridos, y se registran. STALE da 5. Agregar un fixture NEUTRAL. |
| NB-02 | El verify de DEPS-02 (G-58) no ata la ola al último merge, redondea la edad hacia abajo y no tiene evidencia de que la corrida del sábado ocurrió (A-R1-03, C-R1-05, E3) | 37-07:89 solo comprueba weekday()==5 y usa `.days`, que es un floor: un PR de 14,9 días pasa. Tampoco verifica la truth 2 (los PRs del snapshot en estado no-OPEN). Instrumento medido: `gh api 'repos/xenaquis/is-chile-safe/actions/runs?event=dynamic'` lista 'pip in /pipeline - Update' y 'npm_and_yarn in /site - Update' (por ejemplo, 2026-09-05T14:52:00Z). Con la cola en el límite, los sábados 09-12 y 09-19 solo corrió github_actions. Edición: exigir esas corridas dynamic para /site y /pipeline de ese sábado, completed y success, antes de medir; esperar hasta que aparezcan, con tope el domingo 00:00Z, y registrar. En el verify: `total_seconds() <= 14*86400`, wave_date > la fecha del último `merged #` de 37-02..37-05, y ningún número del snapshot en OPEN. |
| NB-03 | 37-01 fija #39 y #33 por número, sin ruta al sucesor (o1, hallazgo del árbitro) | 37-01:22, :147-157 y :160 fijan los PRs por número, y DEPS-01 dice literalmente 'PR #39 merged'. Si un advisory nuevo de astro hace que Dependabot reemplace #39, el script sale 9 y el plan hace STOP. Edición: resolver el PR de seguridad de astro por título ('Bump astro from 7.1.6 to'), seguir 'Superseded by #M', exigir un destino ≥ 7.2.8 y ≥ la primera versión parchada de cada advisory de astro abierto (gh api advisories), y registrar una G-NN que lea DEPS-01 como '#39 o su sucesor'. |
| NB-04 | El chequeo de 37-03 ignora fixAvailable cuando es un objeto no-major (A-R1-04) | 37-03:104 usa `is True`. Lo medí en una copia scratch del lockfile: astro critical isDirect=True fixAvailable={"name":"astro","version":"7.3.5","isSemVerMajor":false}. Un advisory futuro sobre un pin exacto pasaría sin registrarse. Edición: tratar `isinstance(fa,dict) and not fa['isSemVerMajor']` como un residual `fix-needs-direct-pin-change`, que va a deferred-live y que el verify acepta con esa marca. |
| NB-05 | La huella es temporal, G-52 deja un lockfile sin build Linux previo, y la regla (d) congela también los deploys de noticias (A-R1-05, C-R1-03) | Confirmé el mecanismo. news-pipeline.yml:117-119 hace POST al mismo CF_HOOK, y la huella de prod de 03:16:05Z sale de la corrida de news 36089397586, no de deploy-on-code. La ventana de un PASS falso es de ~30-40 s por deploy de noticias, con probabilidad baja. 37-03:96 admite que el lockfile generado en Windows llega a master sin CI Linux ni preview. Si Cloudflare no lo construye, la regla (d) pausa sin revertir y los builds de noticias también fallan. Ediciones:<br>(a) G-52 además exige que el diff de package-lock.json cambie solo las claves `node_modules/<pkg>` objetivo y no elimine entradas (un one-liner de python sobre `packages`).<br>(b) Documentar en 37-VERIFICATION que la huella es temporal.<br>(c) Opcional: en el commit de G-52, si al tope tampoco avanza la huella del siguiente deploy de noticias, revertir (es reversible) y después preguntar al owner. |
| NB-06 | La rama freshness del exit 4 en 37-02 contradice a G-54 (A-R1-11) | 37-02:73 manda un rebase y 'treat it as exit 5'. Pero el script sigue dando 4, así que la rama queda indefinida y termina en STOP después de horas de sondeo. G-54 (:151) y 37-02:77 dicen 'stop the queue'. Edición: si el exit 4 es por freshness o news-freshness, detener la cola y registrar el incidente de noticias. Si es por instalación, un rebase y reclasificar. |
| NB-07 | Fragilidades de los scripts (B-R1-08 a/b/c, A-R1-12, A-R1-08) | (a) Si `gh pr merge --match-head-commit` falla, el script sale con 1, un código fuera del contrato G-53 (37-01:109). Edición: mapearlo a 5 ('head moved, re-check').<br>(b) --stale-ok exige base == el SHA actual de origin/master (:106), y los commits de datos del bot lo mueven. Edición: aceptarlo si `git diff --quiet <base del log> origin/master -- site pipeline .github scripts .nvmrc`.<br>(c) El grep del verify `--(auto\|admin\|delete-branch)` (:137) también coincide con comentarios. Edición: limitarlo a `gh pr (merge\|close).*--(auto\|admin\|delete-branch)`.<br>(d) La etapa 6 omite scripts/ y .nvmrc, que son entradas del build (site/package.json:10 prebuild `../scripts/generate-pairs.mjs`; ci.yml:31 node-version-file .nvmrc). Agregarlos en la etapa 6 y en la política. |
| NB-08 | DEPS-03: defectos latentes en la rama MERGE y línea base exigida en la ruta de gasto cero (B-R1-03, A-R1-09, B-R1-09) | (a) El protocolo de GSD commitea por tarea (execute-plan.md:277), así que 37-DEPS03.md queda trackeado después de Task 1. Task 2 paso 3 lo modifica y el paso 4 (`37-merge-pr.sh --allow-major`) sale 8, que se convierte en CLOSE (37-05:104-111; la exclusión de 37-01:102 cubre solo el triage log). Edición: commitear 37-DEPS03.md antes del paso 4 o excluir el directorio de la fase; los exits 5 y 8 son STOP, no CLOSE.<br>(b) El control A usa el python del sistema (:99) y B un venv. Edición: armar A en `$SCR/venv-openai2` desde requirements.txt sin modificar, y exigir exit 0 ahí antes de un CLOSE por 'tests that fail'.<br>(c) 37-05:70 y el verify :83 exigen la línea base sin condición. Pero 36-06 solo la emite después de un PASS (36-06-PLAN.md:26/:151), y si FID-02 falla ofrece al owner 'accept the best iteration' (:145), así que la 36 puede cerrar sin línea base. Edición: condicionar su existencia y el assert de claves a `pytest_openai3: exit 0`. |
| NB-09 | Sucesor de openai 3.19.x en la ola de 37-07 (B-R1-06) | 37-07:72 y G-55 (:152) no dejan claro si un PR nuevo 'openai from 2.53.0' que llegue después del CLOSE de DEPS-03 se cierra con G-55 o se re-evalúa. Cerrar un PR ignora solo esa release (#12/#20), así que casi seguro vuelve a aparecer en la ola. Edición: cerrarlo con el motivo G-55, citando el deps03_pr y la causa medida (httpx→httpx2), sin venv ni eval nuevos. |
| NB-10 | El chequeo servido no mira ES ni el render del mapa (C-R1-04) | El script solo busca 'Latest incident: ' en /news/ (37-01:131). Medí que /es/noticias/ contiene 'Último incidente: 25 de septiembre de 2026', así que el control ES es barato. El mapa es client:only: un 200 del chunk no prueba que renderice. Edición: agregar el grep ES y un deferred-live 'render del mapa no verificado tras #27/React minor' (BrowserOS no está disponible). |
| NB-11 | CLAUDE.md queda con un piso de astro obsoleto (C-R1-07) | CLAUDE.md:34 dice 'Astro 7.1.x … Do NOT drop back below v7.0.9', y ningún plan lo edita. Después de 37-01 el piso de seguridad es 7.2.8 (GHSA-26w7). Edición: agregar un deferred-live o una línea para la Fase 38 que suba el piso a ≥ 7.2.8, o editarlo en 37-06. |
| NB-12 | El issue de G-56 no notifica y la Fase 38 queda sin triage (C-R1-06) | `gh api user` = xenaquis: el issue lo crea el mismo owner, sin notificación ni assignee. Entre el cierre de la 37 y el primer lunes nadie hace triage, y el retitulado no actualiza la fecha en DEPLOYMENT.md. Edición: declarar en el reporte final que el issue es pasivo, decir quién cubre las alertas críticas durante la 38, y hacer que el retitulado también corrija DEPLOYMENT.md. |
| NB-13 | Arreglos menores en gemelos y textos (A-R1-13, C-R1-08 a-c, hallazgo del árbitro) | (a) 37-06:92 manda poner 'seven' en el docstring, pero son 8 checkouts: ci.yml tiene 3 y hay 5 archivos con 1 (test_workflow_guards.py:506/:511/:520-526). Debe decir 'eight'.<br>(b) 37-02:81, 37-03:77 y 37-05:113 pushean sin `git pull --rebase --autostash`, gemelos de R-09.<br>(c) La creación del venv de zizmor está solo en las interfaces (37-06:63): moverla a la acción de Task 1.<br>(d) Hallazgo del árbitro: los comentarios públicos de cierre de 37-01:157 (#33), 37-03:75 (#28) y 37-04:90 (#18) citan 'DEPLOYMENT.md § Dependency triage policy', que recién crea 37-06. Hoy DEPLOYMENT.md solo tiene §10 en :237 y DEP-01 en :363. Edición: citar 'DEPS-04 policy (being added to DEPLOYMENT.md in Phase 37)'. |

## Discarded

- A-R1-06 Si D se pushea junto con un commit posterior, la corrida con headSha == D no aparece: Con los planes actuales no se puede dar. Cada commit que toca rutas de deploy se pushea solo:
- En 37-01:155 y en 37-02:75, el revert va seguido de gate y push.
- En 37-03:96, el commit build(deps) va seguido de pull y push, y Task 1 ya había pusheado en :77.
- `gh pr merge` genera un push de un solo commit.
En todos los casos D es el head del push. Lo verifiqué leyendo esos pasos. Queda como riesgo solo si un plan futuro mezcla pushes, y la edición A-R1-06 sería entonces la defensa.
- B-R1-08(d)/A-R1-16(;) El `;` después de npm audit en los verify enmascara fallas previas: No es un defecto: el `;` es necesario porque npm audit sale con código distinto de 0 cuando hay vulnerabilidades. En 37-01 Task 2, si la cadena se corta antes de `cd site`, el grep sobre `../.planning/...` apunta a un path que no existe desde la raíz del repo y la cadena falla cerrada. En 37-02, `exit 1` dentro del for termina el shell. Lo verifiqué por lectura de 37-01:160 y 37-02:84.
- B-R1-14 (DESACTUALIZADA mecánica) 37-06:67 parafrasea _MUST_HAVE_FALSE sin los comentarios en línea: Es cosmético: las claves y los conteos coinciden con test_workflow_guards.py:520-526, que leí. El único error real es el 'seven' del docstring (debería decir 'eight'), y va en NB-13.

## Orchestrator observations

| obs | status | detail |
|---|---|---|
| o1: ¿los planes siguen valiendo si la 36 cierra tarde (≥ 2026-10-10)? | CONFIRMADA-POR-ARBITRO | Los planes 37-02..37-05 siguen a los sucesores: 'Superseded by #M', el PR de openai por título y el sucesor de #18. En cambio, 37-01 fija #39 y #33 por número y no tiene rama para un sucesor (37-01:22, :147-157, :160), y DEPS-01 dice literalmente 'PR #39 merged'. Si antes de ejecutar se publica un advisory de astro con parche > 7.2.8, 37-01 hace STOP: falla cerrada, pero traba la fase (NB-03).<br>Medido:<br>- Los PRs de versión de astro se reemplazan semana a semana: #23 7.2.1 → #29 7.2.4 → #33 7.2.7.<br>- Los PRs de seguridad pueden aparecer cualquier día. #39 nació el 2026-09-10T00:41Z de la corrida dinámica 'npm_and_yarn in /site for astro - Update'.<br>- Las colas npm y pip siguen en el límite de 5: los sábados 09-12 y 09-19 solo corrió github_actions. Por eso los números de los PRs de versión no cambian hasta que se liberen cupos.<br>Las versiones no invalidan nada: el piso astro ≥ 7.2.8 sigue siendo correcto aunque exista 7.3.5, y la ola de 37-07 lo recoge. La fecha de G-56 se ajusta sola: max(fecha de ejecución, 2026-10-01). |
| o2: cada merge a site/ despliega antes del gate local. ¿Se acota el daño y se distingue un build de CF fallido de un sitio sano? | CUBIERTA-POR-LENTE | Lo cubren C-R1-01, A-R1-05 y C-R1-03, y el árbitro lo confirmó.<br>Lo que acota el daño:<br>- El exit 6 fuerza `@dependabot rebase`, así que el CI Linux (build + all.mjs + pytest + lint, ci.yml:37-41) y el preview de Cloudflare construyen el árbol real antes del merge. vitest y astro check solo corren en el gate local.<br>- Los merges son serializados.<br>- Hay revert según G-54.<br>- La huella data-build-now (G-51) separa 'hook OK sin build' de 'build servido'.<br>Las brechas:<br>- La verificación por lote (BF-05).<br>- La huella es temporal: news-pipeline.yml:117-119 dispara el mismo CF_HOOK. Medido: la huella de prod de 03:16:05Z sale de la corrida de news 36089397586, sin deploy-on-code desde las 00:14:10Z. Hay un PASS falso posible en una ventana de ~30-40 s (~0,1-0,2 % por merge).<br>- El lockfile de G-52 llega a master sin CI Linux ni preview, y la regla (d) congela también los deploys de noticias mientras se espera al owner (NB-05). |
| o3: ¿los deploys de la Fase 37 contaminan D, C o G de FRESH-04? | DESCARTADA | Nadie lo trató, así que lo verifiqué yo:<br>- D cuenta solo las corridas de news-pipeline cuyo paso 'Trigger Cloudflare Pages deploy' salió success (35-07-PLAN.md:151-152). Las corridas de deploy-on-code no entran.<br>- C y G cuentan solo commits con autor github-actions que tocan current.json o archive (35-07-PLAN.md:153-155).<br>- Los squash merges de Dependabot tienen como autor dependabot[bot] (99c0e46) o xenaquis (8920b4c), y tocan site/ o pipeline/.<br>- H (heartbeat) se reporta aparte.<br>Aunque las ventanas se solapen (el check de 7 días cierra ~2026-10-02), no hay contaminación. Los pushes de la 37 no chocan con el push-with-rebase del bot porque tocan archivos disjuntos. |
| o4: con openai 3.x → httpx2, ¿se puede llegar a la rama MERGE de DEPS-03, o habría que declarar el CLOSE desde ya? | CUBIERTA-POR-LENTE | Lo cubren A-R1-14, B-R1-12 y C-R1-11, y el árbitro lo confirmó. A MERGE no se puede llegar mientras 4 módulos importen httpx a nivel de módulo: test_classifier.py:13, test_classifier_router.py:19, test_clustering.py:17 y test_news_health_gate.py:26 (grep). Además, ningún otro requisito de pipeline/requirements.txt trae httpx (lo leí). Medí en un scratch que pytest aborta en la colección con exit 2 ('Interrupted: 1 error during collection', sin correr tests).<br>El plan ya llega a CLOSE midiendo, con gasto cero. Eso es mejor que declararlo antes, porque G-57 exige la causa medida. Solo faltan dos ajustes: que el STOP por falta de línea base sea condicional en la ruta de gasto cero (NB-08), y que el sucesor de openai 3.19.x en la ola de 37-07 tenga una regla explícita (NB-09). |

## Closures verified

- R-01/G-51: el contrato (a)-(d) está en 37-01:126-134 y en sus gemelos 37-02:81/:84, 37-03:100/:104, 37-07:73/:78/:89 y en la directiva :148. Medido hoy en prod: 8×200, 404 y data-build-now ≥ T0, así que el control positivo exigido en BF-01 es alcanzable.
- R-02/R-03/R-08/R-09 en 37-merge-pr.sh (37-01:99-108): los exits 3-9 son disjuntos, los fixtures pending/failure/major cubren 5, 4 y 7, y la exclusión de árbol limpio está en :102. Los 17 PRs abiertos y sus rollups coinciden con las interfaces (gh pr list, medido): #18 tiene Frontend y CF en FAILURE, #37 tiene pytest en FAILURE y el resto está en SUCCESS.
- R-04/R-07/R-10/R-11 en 37-05: venv armado desde la copia hecha con sed (:75), `pip_openai:` en el verify (:83), la tabla G-50 exacta en el verify de MERGED (:116), el assert del blob condicionado a `pytest_openai3: exit 0` (:83) y deps03_pr por título (:71). El título de #37, 'build(deps): Bump openai from 2.53.0 to 3.7.0', coincide con el filtro.
- R-12/R-14 en 37-06: la corrida se elige por headSha == C (:149), zizmor corre con GH_TOKEN (:113) y la línea Coverage está en :144. El venv de zizmor (E2) falla cerrado si falta (:113), así que no produce un gate vacío.
- R-13 en 37-04:71-75: sondeo de r2-archive, atribución por traceback y registro de lo que queda sin prueba en vivo.
- E1 resuelto: en un scratch, pytest con un import faltante sale con rc=2 'Interrupted' antes de correr cualquier test, y 37-05:76 registra collection_errors y failed por separado.
- La precondición cuenta 13 entradas G-46..G-58 (directiva :143-155), y la regex excluye G-59.
- El comportamiento de Dependabot al cerrar un PR confirma el descarte de M-12: responde 'won't notify you again about this release' (#12 y #20). La consecuencia está en BF-03.
- npm audit en una copia scratch del lockfile da 1 crítica (astro, fixAvailable como objeto no-major a 7.3.5) y 4 altas más 1 moderada con fixAvailable true, igual que en las interfaces.

## Previous edits not applied

- R-15 (ACEPTAR con 'única edición'): 37-gate.sh debe imprimir node/npm/python en la línea de gate. No está aplicada (37-01:115-125). Solo se aplicó la otra mitad, en 37-07:78.
- R-16 (ACEPTAR con edición): el deferred-live de openai en 37-07:106 no lista los requisitos de la migración: portar a httpx2, force_backup=true, resp.provider/usage.cost y recalcular la línea base v2.
- R-17 (ACEPTAR con 'edición mínima'): falta la lista en RUN STATUS de los PRs a mergear antes de 37-01 y el pedido al owner de ratificar G-47. Ningún plan lo hace.
- R-05/G-54 (gemelo): 37-02:75 remite a un '37-01 snapshot gate' que el snapshot de 37-01 (:89-93) no produce.
- R-08/G-58 (gemelo del verify, parcial): 37-07:89 solo afirma que la ola cae sábado. No afirma que sea posterior al último merge ni que la corrida de Dependabot haya ocurrido, y la edad usa el floor `.days`.
- R-09 (gemelos, parcial): hay pushes sin `git pull --rebase --autostash` en 37-02:81, 37-03:77 y 37-05:113.

## Measurements reserved to the orchestrator

- Al iniciar 37-01: medir de nuevo el estado de #39 y #33 y los advisories de astro (`gh api 'advisories?affects=astro'`), para decidir si aplica NB-03.
- Después de BF-02: registrar en el log las líneas STAGE del gate de línea base y confirmar que hay exactamente 18 VALIDATE con rc=0 antes del merge de #39.
- En 37-07: confirmar con `gh api 'repos/xenaquis/is-chile-safe/actions/runs?event=dynamic'` que el sábado posterior al último merge aparecen las corridas 'npm_and_yarn in /site - Update' y 'pip in /pipeline - Update' (NB-02).
- Después de 37-06: observar la conclusión de los checks de CodeQL en los PRs de Dependabot (si alguna es NEUTRAL o SKIPPED) antes de aplicar la regla 7 (NB-01).
- En 37-03 Task 2: si se usa la ruta G-52, revisar en el diff de package-lock.json que solo cambien las claves node_modules/<pkg> objetivo y que no se elimine ninguna entrada (NB-05). Solo se puede medir en ejecución.

## Lens verdicts

- C: producto, ruta servida y coherencia con el pedido del owner (ronda 1, dede804): NO-GO (11 findings)
- A — mediciones, estadística e instrumentos (ronda 1, dede804). Revisé 37-served-routes.sh (G-51), 37-merge-pr.sh (G-53),: NO-GO (16 findings)
- B — ejecutabilidad de gates, tareas e invariantes (ronda 1, planes 37-01..37-07 @ dede804): NO-GO (14 findings)

## Condition closure log (append)

