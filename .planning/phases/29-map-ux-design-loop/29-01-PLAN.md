---
phase: 29-map-ux-design-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/sketches/029-map-controls/MANIFEST.md
  - .planning/sketches/029-map-controls/score.mjs
  - .planning/sketches/029-map-controls/iter-1/desktop.html
  - .planning/sketches/029-map-controls/iter-1/mobile-375.html
  - .planning/sketches/029-map-controls/iter-1/desktop.png
  - .planning/sketches/029-map-controls/iter-1/mobile-375.png
  - .planning/sketches/029-map-controls/iter-1/SCORE.md
autonomous: true
requirements: [MAPUX-01, MAPUX-02, MAPUX-03]

must_haves:
  truths:
    - "The baseline discoverability failure (507px desktop / 991px mobile overflow, news toggle offscreen) is cited from the already-captured 29-BASELINE.md, not re-measured from scratch"
    - "A first, genuine redesign iteration exists as a throwaway static-HTML sketch that a human can open directly in a browser"
    - "Iteration 1's news-layer toggle is provably always visible and provably outside the Leaflet pane z-index conflict band"
    - "Iteration 1 is scored against the falsifiable rubric (evaluate_script), not by eye, at both desktop and 375px widths"
  artifacts:
    - path: ".planning/sketches/029-map-controls/score.mjs"
      provides: "Reusable, falsifiable scoring function (scoreControlShell) shared by every iteration in the loop"
    - path: ".planning/sketches/029-map-controls/iter-1/desktop.html"
      provides: "Throwaway self-contained redesign of the map control shell, desktop viewport"
    - path: ".planning/sketches/029-map-controls/iter-1/mobile-375.html"
      provides: "Throwaway self-contained redesign of the map control shell, 375px viewport"
    - path: ".planning/sketches/029-map-controls/iter-1/SCORE.md"
      provides: "Raw evaluate_script JSON results for iteration 1, both viewports, plus the Tab-walk keyboard-trap result"
    - path: ".planning/sketches/029-map-controls/MANIFEST.md"
      provides: "Running iteration table (pass/fail per rubric criterion) that the final design spec cites"
  key_links:
    - from: ".planning/sketches/029-map-controls/iter-1/desktop.html"
      to: ".planning/sketches/029-map-controls/score.mjs"
      via: "evaluate_script injecting scoreControlShell() against the opened file:// page"
      pattern: "scoreControlShell"
    - from: ".planning/sketches/029-map-controls/iter-1/SCORE.md"
      to: ".planning/sketches/029-map-controls/MANIFEST.md"
      via: "iteration 1's pass/fail row, traceable back to the raw JSON"
      pattern: "iter-1"
---

<objective>
Run the first genuine design-loop iteration (MAPUX-02) of Phase 29's map control-shell redesign, scored against the falsifiable five-point rubric (MAPUX-03), building on the already-captured baseline evidence (MAPUX-01, cited not re-derived).

**Every task in this plan is executed INLINE by the Fable orchestrator in the same BrowserOS-connected session — never dispatched to a `gsd-executor` subagent.** `gsd-executor`'s tool registry is Read/Write/Edit/Bash/Grep/Glob only; it has no Agent tool and no BrowserOS MCP access, so any step requiring a screenshot, `evaluate_script`, or a live click-through is structurally impossible for it to perform (Operating Lesson 1, directive line 100). A plan step routed to `gsd-executor` here would become a silently-skipped gate, not a completed task.

This phase ships **no map code**. All "apply" steps below write throwaway static HTML under `.planning/sketches/029-map-controls/`, never `site/src/components/map/*`.

Purpose: Produce iteration 1's complete evidence set (sketch HTML + screenshots + falsifiable score) so iteration 2 (Plan 29-02) has a real prior state to improve on and compare against.
Output: `.planning/sketches/029-map-controls/score.mjs`, `iter-1/` (desktop.html, mobile-375.html, desktop.png, mobile-375.png, SCORE.md), and the first `MANIFEST.md` row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-map-ux-design-loop/29-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-BASELINE.md
@site/src/components/map/map.css
@site/src/components/map/MapTopbar.tsx
@site/src/components/map/FiltersRow.tsx
@site/src/components/map/Legend.tsx
</context>

<interfaces>
<!-- Current control-shell markup/CSS the sketch must literally reproduce (copy-pasted values, not imported code) -->
<!-- so iteration 1 starts from the REAL current layout, per 29-RESEARCH.md's "Recommended Project Structure" note. -->

From site/src/components/map/map.css (verified in 29-RESEARCH.md, read again this session for exact values):
- `.map-topbar { z-index: 800; }` (map.css:32)
- `.mode-toggle { position: absolute; z-index: 801; }` fixed top-right, non-scrolling precedent (map.css:104-111)
- `.filters-row { overflow-x: auto; scrollbar-width: none; }` + hidden `::-webkit-scrollbar` — this 13-line block (map.css:47-59) is the root cause; iteration 1 must NOT reproduce it unmodified
- `.legend { z-index: 700; bottom: 32px; left: 16px; }` (map.css:128-131) — opposite corner from a bottom-right FAB
- `.chip { height: 44px; }` (map.css:68) — existing touch-target baseline already passes at 44px; preserve it

Leaflet z-index bands (verified in 29-RESEARCH.md from the installed package, `site/node_modules/leaflet/dist/leaflet.css`):
- `.leaflet-popup-pane` = 700 (leaflet.css:114) — anything below 700 risks being drawn under a popup
- `.leaflet-control` = 800, `.leaflet-top`/`.leaflet-bottom` = 1000 (leaflet.css:134, 141)
- Rule for this sketch: any always-visible, non-modal control (the news toggle, a filter FAB) must be styled `z-index: 800` or higher.

From 29-RESEARCH.md's Code Examples section (lines 216-299): the full `scoreControlShell()` function body — extract verbatim into `score.mjs`, do not retype from memory or paraphrase.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Save the falsifiable rubric script and initialize the sketch directory</name>
  <files>.planning/sketches/029-map-controls/score.mjs, .planning/sketches/029-map-controls/MANIFEST.md</files>
  <action>
Executed inline by the orchestrator (file creation, no BrowserOS needed for this task).

Create `.planning/sketches/029-map-controls/score.mjs`. Extract the `scoreControlShell` function body verbatim from `29-RESEARCH.md`'s "Code Examples" section (the fenced block starting `function scoreControlShell() {` through its closing `scoreControlShell();` call, lines 216-299 of that file) and wrap it as a named, exported function plus a helper that returns its serialized JSON source as a string (BrowserOS `evaluate_script` takes a script body, not an import) — e.g. `export const scoreControlShellSource = scoreControlShell.toString();` alongside `export function scoreControlShell() { ... }` for any Node-side reuse. Do not alter the function's logic; this is a copy-paste-and-wrap operation, not a rewrite.

Create `.planning/sketches/029-map-controls/MANIFEST.md` with: a one-paragraph statement of the design direction (cite the baseline root cause from `29-BASELINE.md` §1-2: the flat `.filters-row` scroll overflow with zero affordance, and the CrimeMapping finding in `29-BASELINE.md` §3 about persistent labeled entry points per dimension), an empty iteration table with columns `Iteration | newsToggleDiscoverable | filterOverflowsContainer | touchTargetViolations | keyboardTrapFree | zIndexViolations | Notes`, and a `Baseline (iteration 0)` row pre-filled directly from `29-BASELINE.md`'s already-captured numbers (507px/991px overflow, `.ev-chip` offscreen, 0 touch-target violations, focus-trap PASS by construction per the folded-in Phase 28 pass, no z-index data captured at baseline since the current shell was never scored by this rubric — mark that cell "not measured at baseline").
  </action>
  <verify>
    <automated>test -f ".planning/sketches/029-map-controls/score.mjs" && grep -c "scoreControlShell" ".planning/sketches/029-map-controls/score.mjs"</automated>
  </verify>
  <done>score.mjs exists, contains a real (non-stub) scoreControlShell implementation matching 29-RESEARCH.md's five numbered criteria; MANIFEST.md exists with the baseline row filled from 29-BASELINE.md's actual numbers, not placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Build and screenshot iteration 1's redesigned control shell</name>
  <files>.planning/sketches/029-map-controls/iter-1/desktop.html, .planning/sketches/029-map-controls/iter-1/mobile-375.html, .planning/sketches/029-map-controls/iter-1/desktop.png, .planning/sketches/029-map-controls/iter-1/mobile-375.png</files>
  <action>
Executed inline by the orchestrator via BrowserOS MCP (`navigate`, `take_snapshot`/screenshot, `evaluate_script`) — this task cannot be delegated to `gsd-executor`.

Redesign target for iteration 1 (primary bug fix, not yet informed by the competitor click-through — that citation is reserved for iteration 2 per MAPUX-04's "informs at least one design iteration" wording): apply Pattern 2 from `29-RESEARCH.md` (always-visible news toggle, styled like `.mode-toggle` — `position: fixed`, `z-index: 801`, top-right or a non-colliding corner, never inside the scrollable chip row) and Pitfall 3's remedy (the filter chip row keeps horizontal scroll but gains a visible "there's more" affordance — a CSS `mask-image` fade on the trailing edge, restored `scrollbar-width: auto` or a chevron icon — so overflow is no longer silent). Build `desktop.html` self-contained (inline `<style>`, no external CSS/JS files, no build step) reproducing the current topbar/filter markup structure (chip row, year select, mode toggle, legend) with these two fixes applied. Build `mobile-375.html` as the same redesign at 375px width, using a `<dialog>`-based bottom-sheet FAB for the filter panel per Pattern 1 (real `showModal()` call in an inline `<script>`, not just the declarative `open` attribute — Pitfall 2) to also address the mobile 991px overflow.

Open `iter-1/desktop.html` via `file://` in BrowserOS at desktop width and screenshot to `iter-1/desktop.png`. Open `iter-1/mobile-375.html` inside a 375px-wide iframe (per the directive's mandated mobile-emulation method — BrowserOS has no native viewport resize) and screenshot to `iter-1/mobile-375.png`.
  </action>
  <verify>
    <automated>test -f ".planning/sketches/029-map-controls/iter-1/desktop.html" && test -f ".planning/sketches/029-map-controls/iter-1/mobile-375.html" && test -f ".planning/sketches/029-map-controls/iter-1/desktop.png" && test -f ".planning/sketches/029-map-controls/iter-1/mobile-375.png" && grep -c "showModal" ".planning/sketches/029-map-controls/iter-1/mobile-375.html"</automated>
  </verify>
  <done>Both throwaway sketch HTML files and both PNG screenshots exist under iter-1/; mobile-375.html contains a real showModal() call (not just a declarative open attribute); the desktop sketch's news-toggle equivalent is styled at z-index >= 800 outside the scrollable row (spot-checked by grep for the chip-row container class not wrapping the toggle element).</done>
</task>

<task type="auto">
  <name>Task 3: Score iteration 1 against the falsifiable rubric and record the delta</name>
  <files>.planning/sketches/029-map-controls/iter-1/SCORE.md, .planning/sketches/029-map-controls/MANIFEST.md</files>
  <action>
Executed inline by the orchestrator via BrowserOS `evaluate_script` and the keyboard-press tool — cannot be delegated to `gsd-executor`.

Against the opened `iter-1/desktop.html` page, inject and run `scoreControlShell()` (from `score.mjs`) via `evaluate_script`; record the raw JSON result. Repeat against `iter-1/mobile-375.html` inside the 375px iframe. For criterion 4 (no keyboard trap), drive `focusables.length + 2` real Tab keypresses via BrowserOS's keyboard tool on both pages, then a Shift+Tab from the first element, asserting `document.activeElement` per the procedure in `29-RESEARCH.md`'s Accessibility section (confinement inside an open `<dialog>` on the mobile sheet is a PASS, not a trap — do not misscore it as one).

Write `iter-1/SCORE.md` containing: both raw JSON blobs verbatim (desktop + mobile), the Tab-walk pass/fail result for both, and a one-paragraph delta note explaining what changed from the baseline and which specific baseline defect it targets (cite `29-BASELINE.md`'s exact overflow numbers, not "felt better" language — Pitfall 1). Append iteration 1's row to `MANIFEST.md`'s iteration table using the actual JSON values (not summaries).
  </action>
  <verify>
    <automated>test -f ".planning/sketches/029-map-controls/iter-1/SCORE.md" && grep -c "newsToggleDiscoverable" ".planning/sketches/029-map-controls/iter-1/SCORE.md" && grep -c "iter-1" ".planning/sketches/029-map-controls/MANIFEST.md"</automated>
  </verify>
  <done>SCORE.md contains real JSON output (both viewports) from an executed evaluate_script call, not hand-typed approximations; the Tab-walk result is stated as pass/fail with the dialog-confinement caveat applied correctly; MANIFEST.md's iteration table has a filled iter-1 row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None — no new trust boundary | Phase 29 produces static, never-deployed throwaway HTML and a documentation artifact; nothing here is served to a real user or processes untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-01 | Tampering | `.planning/sketches/029-map-controls/*.html` | accept | Throwaway static files, never deployed, never referenced from any served route; no production attack surface |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (design-only, per 29-RESEARCH.md's Package Legitimacy Audit — not applicable) |
</threat_model>

<verification>
- All three tasks' artifacts exist under `.planning/sketches/029-map-controls/` as listed in `files_modified`.
- `MANIFEST.md` has both a baseline row (from `29-BASELINE.md`) and an iter-1 row (from this plan's Task 3), both populated with real measured numbers.
- `iter-1/mobile-375.html` uses `showModal()`, not a declarative-only `<dialog open>`.
</verification>

<success_criteria>
Iteration 1 is a complete, falsifiable, screenshot-backed design cycle distinct from the baseline capture, ready for Plan 29-02 to build iteration 2 against.
</success_criteria>

<output>
Create `.planning/phases/29-map-ux-design-loop/29-01-SUMMARY.md` when done
</output>
