---
phase: 29-map-ux-design-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/sketches/029-map-controls/MANIFEST.md
  - .planning/sketches/029-map-controls/STRESS.md
  - .planning/sketches/029-map-controls/score.mjs
  - .planning/sketches/029-map-controls/check-iteration.mjs
  - .planning/sketches/029-map-controls/check-astro.mjs
  - .planning/sketches/029-map-controls/mobile-frame.html
  - .planning/sketches/029-map-controls/iter-1/desktop.html
  - .planning/sketches/029-map-controls/iter-1/mobile-375.html
  - .planning/sketches/029-map-controls/iter-1/desktop.png
  - .planning/sketches/029-map-controls/iter-1/mobile-375.png
  - .planning/sketches/029-map-controls/iter-1/SCORE.md
autonomous: true
requirements: [MAPUX-01, MAPUX-02, MAPUX-03]

must_haves:
  truths:
    - "The baseline discoverability failure (507px desktop / 991px mobile overflow, news toggle offscreen) is cited from the already-captured 29-BASELINE.md, not re-measured"
    - "The stress conditions every iteration is scored under are FROZEN before iteration 1 is built, so they cannot be reverse-engineered from the design (F-02 precedent)"
    - "Iteration 1's news-layer toggle is proven topmost at its own centre point (elementFromPoint) against a Leaflet pane stand-in carrying the real leaflet.css z-index values, and carries an explicit numeric z-index >= 800, never `auto`"
    - "Iteration 1 is scored against the falsifiable rubric at desktop, 375px, 320px, 481px and 639px — with every score produced by an executed evaluate_script call, never hand-typed"
  artifacts:
    - path: ".planning/sketches/029-map-controls/score.mjs"
      provides: "Reusable falsifiable scoring function + verdict() thresholds, shared by every iteration AND reusable unchanged against the real /map/ in Phase 30"
    - path: ".planning/sketches/029-map-controls/STRESS.md"
      provides: "Five pre-registered, frozen adversarial conditions every iteration is re-scored under"
    - path: ".planning/sketches/029-map-controls/check-iteration.mjs"
      provides: "Machine gate that exits non-zero on a missing measurement, a NOT_MEASURED_FAIL, or an iteration structurally identical to its predecessor (theater detection)"
    - path: ".planning/sketches/029-map-controls/iter-1/SCORE.md"
      provides: "Raw evaluate_script JSON per viewport per stress condition, flushed as produced"
    - path: ".planning/sketches/029-map-controls/MANIFEST.md"
      provides: "Running criterion x condition matrix that the final design spec cites"
  key_links:
    - from: ".planning/sketches/029-map-controls/iter-1/SCORE.md"
      to: ".planning/sketches/029-map-controls/score.mjs"
      via: "evaluate_script injecting (scoreControlShell)() against the HTTP-served page"
      pattern: "scoreControlShell"
    - from: ".planning/sketches/029-map-controls/iter-1/SCORE.md"
      to: ".planning/sketches/029-map-controls/MANIFEST.md"
      via: "iteration 1's criterion x condition row, traceable to the raw JSON"
      pattern: "iter-1"
---

**Fable review: APPROVED 2026-07-30** — after an Opus plan-check (6 blockers / 7 warnings) and an Opus premortem (17 failure modes / 21 amendments), all applied INLINE by Fable rather than in a third Sonnet round (F-22/F-26/F-34 precedent). Two blockers were found by execution, not by reading: the `file://` iframe returns `contentDocument: null` so every mobile score would have come back empty, and `save_screenshot` cannot write into a repo path containing spaces. Both Opus passes independently found the first; only the premortem found that F-39's mandated Opus review was missing from the plans entirely. Every gate shape above was run against the real tree at approval time, and each new assertion was proven to exit non-zero against a constructed counterexample.

<objective>
Run the first genuine design-loop iteration (MAPUX-02) of the map control-shell redesign, scored against a falsifiable rubric (MAPUX-03) under pre-registered adversarial conditions, building on the already-captured baseline (MAPUX-01, cited not re-derived).

**ORCHESTRATOR: do not invoke `/gsd:execute-phase` for this phase. Execute these tasks directly in this session.** Every task runs INLINE in the Fable orchestrator's BrowserOS-connected session. `gsd-executor`'s registry is Read/Write/Edit/Bash/Grep/Glob only — no Agent tool, no BrowserOS — so any screenshot, `evaluate_script` or live click-through is structurally impossible for it (Operating Lesson 1). `type: execute` + `autonomous: true` is the shape the dispatcher would normally hand to `gsd-executor`; prose alone does not change dispatcher behavior, hence this explicit instruction.

This phase ships **no map code**. All "apply" steps write throwaway static HTML under `.planning/sketches/029-map-controls/`, never `site/src/components/map/*`.

Output: the measurement harness, the frozen stress conditions, and iteration 1's complete evidence set, so iteration 2 (Plan 29-02) has a real prior state to improve on and be compared against.
</objective>

<browseros_resume_protocol>
Applies to every browser step in this phase. The MCP connection already dropped once this session and `save_screenshot` has intermittently timed out.

1. **The harness is committed, not improvised**: `score.mjs`, `mobile-frame.html` and the HTTP server command are on disk before any measurement, so any measurement is re-runnable from a cold session.
2. **Flush as you go.** Every `evaluate_script` result is appended to `iter-N/SCORE.md` **immediately**, carrying its `capturedAt`, `pageUrl`, `viewportWidthPx` and stress-condition name, before the next call is made. Never buffer blobs to write at the end of a task.
3. On any MCP error or timeout: retry once; re-check `http://127.0.0.1:9200/mcp`; restart the tab and re-run **only** the missing cells (named in `SCORE.md` by their absence).
4. If BrowserOS is unrecoverable the iteration closes **PARTIAL**: missing cells read `NOT_MEASURED_FAIL`, `check-iteration.mjs` exits non-zero, the iteration does **not** count toward MAPUX-02's >=2, and Phase 29 is marked BLOCKED per directive line 59 with Phase 30 skipped. A phase is never closed on a partial evidence pack.
5. Screenshots land in `C:\Users\Carlo\bos-shots\p29\` first, so a drop after the shot but before the `cp` costs one file copy, not one iteration.
</browseros_resume_protocol>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-map-ux-design-loop/29-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-BASELINE.md
@.planning/phases/29-map-ux-design-loop/29-PREMORTEM.md
</context>

<interfaces>
<!-- COMPLETE real inventory of everything competing for map-edge space. The first draft listed
     five facts and omitted every one that would actually break the design (premortem F7/A12).
     Values re-read from site/src/components/map/map.css this session. -->

| Real element | Position / z-index | Why the sketch must model it |
|---|---|---|
| `.map-topbar` | z 800 (map.css:32) | the shell being redesigned |
| `.mode-toggle` | absolute, z 801 (map.css:104-111) — **resets to `position: static` at <=480px (map.css:240-251)** | cited as the always-visible-toggle precedent, **but that precedent does not hold at 375px**. A sketch pinning a fixed z-801 toggle at mobile designs something the real CSS actively contradicts. |
| `.zoom-ctl` | absolute, right:16px, bottom:120px, z 700, shown >=640px (map.css:257-275) | **bottom-right is already occupied on desktop.** 29-RESEARCH.md Open Question 2 recommends a bottom-right FAB and checks for collision with `.legend` at bottom-**left** — the wrong corner. |
| `.result-panel` | fixed right, 380px wide, z 700 desktop; `left:0;right:0;height:40vh` bottom sheet <=640px (map.css:450-480) | the right 380px is not free canvas on desktop, and the bottom 40vh is a sheet on mobile. A bottom-right FAB collides with both. |
| `.legend` | bottom-left, z 700 (map.css:127-131); `.legend-mobile` `<details>` <=480px (map.css:210-224) | a second `<details>`/`<dialog>` surface at 375px interacts with this one |
| `.partial-year-badge` | z 800, top-centre (map.css:900-905) | ties with `.map-topbar` at 800 — resolved by DOM order |
| `.search-dropdown` | z 900 (map.css:364-369) | real ladder has five occupied tiers, not two |
| `.map-error-overlay` | z 900 (map.css:407-410) | " |
| `.toast` | z 1000 (map.css:803-808) | " |
| `.filters-row` | `overflow-x:auto; scrollbar-width:none` + hidden `::-webkit-scrollbar` (map.css:47-59) | **the root cause.** Iteration 1 must NOT reproduce this unmodified. |
| `.chip` | height 44px (map.css:68) | touch-target baseline already passes; preserve it |

Leaflet z-index bands, read from `site/node_modules/leaflet/dist/leaflet.css` (verified by execution this session):
`.leaflet-tile-pane` 200 · `.leaflet-overlay-pane` 400 · `.leaflet-shadow-pane` 500 · `.leaflet-marker-pane` 600 · `.leaflet-tooltip-pane` 650 · `.leaflet-popup-pane` 700 · `.leaflet-control` 800 · `.leaflet-top`/`.leaflet-bottom` 1000.

Rule: any always-visible non-modal control needs an **explicit numeric** `z-index` >= 800. `z-index: auto` is a VIOLATION — its paint order is DOM-order dependent and will not survive contact with the real 970-line `map.css`. A modal `<dialog>` opened with `showModal()` renders in the top layer and needs no z-index at all.

The real React island is `MapIsland.tsx` (541 lines, `client:only="react"`) — the shell renders **nothing** server-side.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the measurement harness</name>
  <files>.planning/sketches/029-map-controls/score.mjs, .planning/sketches/029-map-controls/check-iteration.mjs, .planning/sketches/029-map-controls/check-astro.mjs, .planning/sketches/029-map-controls/mobile-frame.html, .planning/sketches/029-map-controls/MANIFEST.md</files>
  <action>
Executed inline by the orchestrator. File creation only; no BrowserOS needed.

**`score.mjs`** — export `scoreControlShell()` (a self-contained function whose `.toString()` is injected via `evaluate_script` as `(${src})()`; the plain definition returns nothing when injected, so the invocation wrapper is mandatory) plus `export function verdict(r)`. Base it on 29-RESEARCH.md's rubric with these corrections, each of which fixes a criterion that could not fail:

- **Criterion 1 (news toggle discoverable)**: rect fully inside the viewport AND `document.elementFromPoint(centre)` is the toggle or a descendant. Report `newsToggleMissing: true` when the selector finds nothing — absence must be a FAIL, never a missing key.
- **Criterion 2 (filters discoverable)**: the first draft was true-by-construction — `[style*="overflow"]` matches only *inline* styles (both sketches use `<style>` blocks, so it never matches) and for a FAB `closest()` returns null, making `scrollWidth === clientWidth` and the criterion `false` for free. Replace with the same viewport+topmost test as criterion 1 for the entry point, PLUS a whole-document scan: every element whose `scrollWidth - clientWidth > 8` and whose computed `overflow-x` is `auto|scroll|hidden` is recorded in `hiddenOverflowElements` with its overflow in px. `filterOverflowsContainer = hiddenOverflowElements.length > 0`. This cannot be defeated by renaming a class.
- **Criterion 3 (touch targets)**: every interactive element's rect < 44px in either axis is a violation; count them. Defined at 375px and 320px.
- **Criterion 4 (keyboard)**: settled **by construction per F-41's adopted standard** — focusable count, count of `tabindex="-1"`, and a scan of the sketch's own inline `<script>` text for `keydown`/`keyup`/`preventDefault`. Zero interceptors + zero negative tabindexes = PASS. One call instead of thirty round-trips, and it is the stronger evidence.
- **Criterion 5 (occlusion / pane conflict)**: `z-index: auto` on a non-modal control counts as a VIOLATION (the first draft's `Number('auto') || 0` scored an unstyled control as compliant). If no `.leaflet-container`/`.leaflet-map-pane` stand-in is present, set `criterion5: 'NOT_MEASURED_FAIL'` — **fail closed, loudly**; the first draft left the key absent entirely, so it would have read as blank in every row forever. Also emit `mapWidthRatio` and `controlCoveragePct` (union area of `[data-role]` control boxes intersecting the map rect, as a % of that rect).

Every returned object must carry `capturedAt` (ISO), `pageUrl`, `viewportWidthPx`, and `stressCondition`.

`verdict(r)` applies the thresholds pre-registered in STRESS.md and returns one boolean per criterion. Note the deliberate asymmetry recorded in F-43: `mapWidthRatio` is **measured and reported but NOT gating**, because the full-bleed question is ruled out of Phase 30's scope.

**`check-iteration.mjs`** — takes an iteration number N and exits non-zero unless: (1) every JSON blob in `iter-N/SCORE.md` `JSON.parse`s and carries `newsToggleDiscoverable`, `filterOverflowsContainer`, `touchTargetViolations`, `criterion5`, `capturedAt`, `pageUrl`, `viewportWidthPx`, `stressCondition`; (2) no blob has `criterion5 === 'NOT_MEASURED_FAIL'` and every condition named in `STRESS.md` appears; (3) for N >= 2, the count of `data-role="entry-point"` elements in `iter-N/desktop.html` differs from `iter-(N-1)/desktop.html` **or** at least one criterion x condition cell moved FAIL -> PASS — otherwise exit 1 with `ITERATION N IS NOT A DISTINCT ITERATION`; (4) `iter-N/SCORE.md` contains at least one literal numeric value also present in `iter-(N-1)/SCORE.md`, proving the delta note compared rather than paraphrased. Per F-36 this is a real `.mjs` file — never regex inside a shell-quoted `node -e`.

**`check-astro.mjs`** — reads a captured astro-check log path, finds the summary line by **string** scanning (not regex, per F-36), exits 0 only if the error count is exactly 4 and warnings 0, else exits 1 printing the delta.

**`mobile-frame.html`** — the committed 375px iframe wrapper (`?src=` parameterized), so the mobile harness is an artifact rather than an ad-hoc DOM injection that dies with the tab (premortem F9).

**`MANIFEST.md`** — design direction paragraph citing 29-BASELINE.md §1-2 (the flat `.filters-row` overflow with its scrollbar suppressed) and §3 (CrimeMapping's persistent labeled per-dimension entry points); an iteration x criterion x stress-condition matrix; and a pre-filled `Baseline (iteration 0)` row from 29-BASELINE.md's real numbers — 507px desktop / 991px mobile overflow, `.ev-chip` offscreen, 0 touch-target violations, keyboard PASS by construction, and **z-index data that IS available**: `.map-topbar` 800, `.legend` **700 — colliding with `.leaflet-popup-pane` at 700**, control coverage 24.7% of the map area (18.4% topbar + 6.3% legend), `mapWidthRatio` 812/1296 = 0.63. Do NOT mark these "not measured at baseline"; they were measured this session and they are what proves the rubric can return FAIL against a real input rather than only against prototypes built to pass.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- site/)" && node -e "import('./.planning/sketches/029-map-controls/score.mjs').then(m=>{if(typeof m.scoreControlShell!=='function'||typeof m.verdict!=='function'){console.error('FAIL: score.mjs must export scoreControlShell and verdict');process.exit(1)};const s=m.scoreControlShell.toString();for(const k of ['NOT_MEASURED_FAIL','elementFromPoint','hiddenOverflowElements','controlCoveragePct','capturedAt']){if(!s.includes(k)){console.error('FAIL: scoreControlShell missing '+k);process.exit(1)}};console.log('score.mjs OK')})" && node .planning/sketches/029-map-controls/check-astro.mjs --selftest && test -f .planning/sketches/029-map-controls/mobile-frame.html && grep -q "Baseline (iteration 0)" .planning/sketches/029-map-controls/MANIFEST.md</automated>
  </verify>
  <done>score.mjs exports both functions and its rubric body contains the fail-closed sentinel, the topmost test, the whole-document overflow scan, the coverage metric and the provenance field; check-astro.mjs self-test passes; mobile-frame.html and MANIFEST.md exist with the baseline row carrying the real measured z-index and coverage numbers; `site/` is untouched.</done>
</task>

<task type="auto">
  <name>Task 1b: Pre-register and FREEZE the stress conditions — before any sketch is built</name>
  <files>.planning/sketches/029-map-controls/STRESS.md</files>
  <action>
Executed inline by the orchestrator. **This task MUST complete before Task 2 begins.** The conditions are frozen here exactly as F-02 froze the rapidfuzz threshold before any precision number was observed: choosing them after seeing a score is the circularity this phase's acceptance gate exists to prevent.

The problem this solves (premortem F4): iteration 1's build instructions *are* the rubric criteria, so it scores 5/5 by construction; iteration 2 then has no failing target, and a cosmetic recolour with the word "CrimeMapping" typed into the delta note would pass every gate. Pre-registered adversarial conditions give iteration 1 a real chance of failing something and give iteration 2 a falsifiable target that is not the orchestrator's own opinion.

Write the five conditions, and do not amend them afterwards:
1. **Content growth** — a 9th crime-family chip and a 2nd year-select injected via `evaluate_script` before scoring (29-RESEARCH.md Pitfall 3's own stated warning sign, which the rubric never implemented).
2. **Longest real label** — every filter label replaced by the longest string it can actually carry: ES `Robos con violencia o intimidación`, and the EN equivalent. Both locales ship; a shell that only fits EN is broken.
3. **Narrow viewport** — 320px in addition to 375px. 375 is not the floor.
4. **Enlarged text** — `document.documentElement.style.fontSize = '24px'` (WCAG 1.4.4, 150%) before scoring at 375px.
5. **Real breakpoint boundaries** — also score at **481px and 639px**, the bands `map.css:192/210/240/271` actually switch on, and the band Phase 25's P0 tablet-overflow defect lived in. A design validated only at "desktop" and 375px has never been tested at either boundary it meets in production.

Also record the pre-registered numeric thresholds (F-43): `controlCoveragePct <= 25%` at 375px with all panels closed; `touchTargetViolations === 0` at 375px and 320px; `zIndexViolations.length === 0`; `filterOverflowsContainer === false`. `mapWidthRatio` is **recorded but explicitly NOT gating** — the full-bleed question is out of scope per F-43.

State the anti-theater rule explicitly: **a row with no FAIL anywhere in it, and no prior row to improve on, is evidence the harness is too weak — not that the design is perfect.** If iteration 1 passes all five conditions on the first try, record that in `iter-1/SCORE.md` and add a sixth, harder condition before iteration 2 rather than declaring the loop converged.
  </action>
  <verify>
    <automated>test -f .planning/sketches/029-map-controls/STRESS.md && node -e "const s=require('fs').readFileSync('.planning/sketches/029-map-controls/STRESS.md','utf8');const need=['320','375','481','639','24px','controlCoveragePct','NOT gating'];for(const k of need){if(!s.includes(k)){console.error('FAIL: STRESS.md missing '+k);process.exit(1)}};console.log('STRESS.md OK')" && test -z "$(ls .planning/sketches/029-map-controls/iter-1 2>/dev/null)"</automated>
  </verify>
  <done>STRESS.md exists, names all five conditions with their viewport widths and the numeric thresholds, states the anti-theater rule, and — proven by the final clause of the gate — was written while `iter-1/` was still empty, so the conditions cannot have been reverse-engineered from the design.</done>
</task>

<task type="auto">
  <name>Task 2: Build and screenshot iteration 1</name>
  <files>.planning/sketches/029-map-controls/iter-1/desktop.html, .planning/sketches/029-map-controls/iter-1/mobile-375.html, .planning/sketches/029-map-controls/iter-1/desktop.png, .planning/sketches/029-map-controls/iter-1/mobile-375.png</files>
  <action>
Executed inline by the orchestrator via BrowserOS — cannot be delegated.

**Redesign target for iteration 1** (the primary bug fix; the competitor-informed restructure is reserved for iteration 2 per MAPUX-04's "informs at least one design iteration"): an always-visible news-layer toggle that is never inside the scrollable chip row, carrying an explicit numeric `z-index` >= 800; and the chip row's silent overflow given a real affordance (trailing mask-image fade, restored scrollbar, or a chevron) so `map.css:47-59`'s suppressed scrollbar is no longer the root cause.

**Both sketches MUST include a Leaflet stacking stand-in**, or criterion 5 cannot fire at all: a `div.leaflet-container` sized to the sketch's map area containing the real pane divs with the real z-index values copied verbatim from `leaflet.css` (200/400/500/600/650/700/800/1000), plus one absolutely-positioned dummy inside `.leaflet-popup-pane` positioned to overlap the news toggle's coordinates — so criterion 1's `elementFromPoint` topmost check becomes a real test of stacking rather than a formality.

**And the full competing-furniture inventory** from `<interfaces>`, as inert positioned stand-ins with their real classes and z-index values: `.zoom-ctl` (bottom-right, desktop-only >=640), `.result-panel` (right 380px desktop / bottom 40vh <=640), `.legend` + `.legend-mobile`, `.partial-year-badge`, `.search-dropdown`, `.toast`, and `.mode-toggle` **including its `position:static` reset at <=480**. A toggle or FAB placement colliding with any of these is a FAIL. If iteration 1's bottom-right FAB collides with `.zoom-ctl` — which it will, since 29-RESEARCH.md Open Question 2 checked the wrong corner — say so in `SCORE.md` and move it. That is exactly the kind of real failure this loop exists to surface.

Every control the sketch introduces must carry `data-role="news-toggle"`, `data-role="filter-entry"` or `data-role="entry-point"`, or the rubric will not score it — and these same hooks are what let `score.mjs` run unchanged against the real `/map/` as Phase 30's regression instrument.

Build `desktop.html` and `mobile-375.html` self-contained (inline `<style>`, no build step). The mobile variant uses a `<dialog>` bottom sheet opened with a real `showModal()` call — native top-layer rendering gives modal focus containment, ESC and backdrop for free, satisfying MAPSH-02's zero-new-JS-dependency constraint and the WCAG keyboard gate at once.

**Serving and screenshots:**
- Serve over HTTP so the 375px iframe is same-origin — a `file://` parent is an opaque origin and `iframe.contentDocument` is `null`, which would make every mobile measurement come back empty. **This was verified by execution at approval time: `file://` returned `BLOCKED: contentDocument null`; over HTTP the same probe returned `innerWidth: 375` with `matchMedia('(max-width:480px)')` matching.** Background: `python -m http.server 4399 --directory .planning/sketches/029-map-controls` (port 4399 avoids astro preview on 4321; do not reuse the preview server, whose EN pages bounce an es-locale browser to production per F-40).
- Before scoring, assert same-origin access: `evaluate_script` returning `document.querySelector('iframe').contentDocument !== null` must be `true`. If false or throwing, the mobile score is `NOT_MEASURED_FAIL` — never silently omitted.
- `save_screenshot` cannot write into the repo (its absolute path contains spaces — the constraint that forced 29-BASELINE.md's shots to `C:\Users\Carlo\bos-shots\p29\`). Capture to `C:\Users\Carlo\bos-shots\p29\iter-1-desktop.png` / `iter-1-mobile375.png`, then `cp` into the repo. The `test -f` gates below check the **repo** copy, so a forgotten `cp` fails rather than passing silently.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- site/)" && node -e "const fs=require('fs');const d=fs.readFileSync('.planning/sketches/029-map-controls/iter-1/desktop.html','utf8');const m=fs.readFileSync('.planning/sketches/029-map-controls/iter-1/mobile-375.html','utf8');const fail=s=>{console.error('FAIL: '+s);process.exit(1)};for(const k of ['leaflet-popup-pane','leaflet-container','data-role=\"news-toggle\"','data-role=\"filter-entry\"','zoom-ctl','result-panel','legend']){if(!d.includes(k))fail('desktop.html missing '+k)};if(!m.includes('showModal'))fail('mobile-375.html has no showModal call');console.log('iter-1 sketches OK')" && test -f .planning/sketches/029-map-controls/iter-1/desktop.png && test -f .planning/sketches/029-map-controls/iter-1/mobile-375.png</automated>
  </verify>
  <done>Both sketches exist with the Leaflet pane stand-in, the competing-furniture stand-ins and the `data-role` hooks; the mobile sketch calls `showModal()`; both PNGs are in the repo (not merely in bos-shots); `site/` untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Score iteration 1 under every frozen stress condition</name>
  <files>.planning/sketches/029-map-controls/iter-1/SCORE.md, .planning/sketches/029-map-controls/MANIFEST.md</files>
  <action>
Executed inline by the orchestrator via BrowserOS `evaluate_script` — cannot be delegated.

Inject `(${scoreControlShell.toString()})()` against the HTTP-served `iter-1/desktop.html`, and against `iter-1/mobile-375.html` inside `mobile-frame.html`'s same-origin iframe, **once per stress condition from STRESS.md** (baseline/375/320/481/639, plus content-growth, longest-label and 24px-text mutations applied via `evaluate_script` before scoring). Per the resume protocol, append each raw JSON blob to `SCORE.md` **immediately** as it is produced, with its `capturedAt`, `pageUrl`, `viewportWidthPx` and `stressCondition`.

Criterion 4 is settled by construction per F-41 (focusable count + negative-tabindex count + inline-script scan for key interceptors) — not by a 30-round-trip Tab walk, which is also structurally impossible inside the iframe, where the parent's `document.activeElement` stays pinned to the `<iframe>` element. **Then** perform exactly one real interaction, on the desktop page only: click the filter entry point to fire `showModal()`, assert `document.querySelector('dialog').matches(':modal') === true` — this is the check that actually distinguishes a real `showModal()` from a declarative `<dialog open>`, which `grep -c "showModal"` cannot — press `Escape`, and assert focus returned to the invoking element. Record all three results in `SCORE.md`.

Write the delta note citing 29-BASELINE.md's exact numbers (507px/991px), never "feels better". Append iteration 1's row to `MANIFEST.md`'s matrix using actual values. If iteration 1 passes all five conditions on the first try, apply STRESS.md's anti-theater rule: record that fact and add a sixth, harder condition before iteration 2.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- site/)" && node .planning/sketches/029-map-controls/check-iteration.mjs 1 && grep -q "iter-1" .planning/sketches/029-map-controls/MANIFEST.md && grep -q ":modal" .planning/sketches/029-map-controls/iter-1/SCORE.md</automated>
  </verify>
  <done>check-iteration.mjs passes for iteration 1 — meaning every blob parses, carries its provenance fields, covers every frozen condition, and none reads NOT_MEASURED_FAIL; the `:modal` assertion result is recorded; MANIFEST.md's matrix has a populated iter-1 row; `site/` untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None — no new trust boundary | Static, never-deployed throwaway HTML plus documentation; nothing is served to a real user or processes untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-01 | Tampering | `.planning/sketches/029-map-controls/*.html` | accept | Throwaway static files, never deployed, never referenced from any served route |
| T-29-04 | Tampering | local HTTP server on 4399 | accept | Bound to a loopback-served static directory of throwaway files; stopped at phase close |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `test -z "$(git status --porcelain -- site/)"` is the FIRST assertion of every task. If it fails, the phase has violated its own hard gate (ROADMAP.md:98/:364): revert with `git checkout -- site/`, record the attempted change in STATE.md `## Blockers`, and re-run. Never carry it into Phase 30 as a fait accompli.
- STRESS.md was written while `iter-1/` was empty (asserted in Task 1b's gate).
- Every score in SCORE.md came from an executed `evaluate_script` call, proven by `check-iteration.mjs`'s parse + provenance-field check rather than by a `grep` for a typed word.
- No `<automated>` block above launders an exit code: no `| tail`, no `| head`, no `; echo`, no `|| echo`, no `|| true`, and no `;` terminating an `&&` chain.
</verification>

<success_criteria>
Iteration 1 is a complete, falsifiable, screenshot-backed design cycle distinct from the baseline, measured under conditions frozen before it was designed, ready for Plan 29-02 to build iteration 2 against.
</success_criteria>

<output>
Create `.planning/phases/29-map-ux-design-loop/29-01-SUMMARY.md` when done
</output>
