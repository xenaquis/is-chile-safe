---
phase: 29-map-ux-design-loop
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - .planning/sketches/029-map-controls/iter-2/desktop.html
  - .planning/sketches/029-map-controls/iter-2/mobile-375.html
  - .planning/sketches/029-map-controls/iter-2/desktop.png
  - .planning/sketches/029-map-controls/iter-2/mobile-375.png
  - .planning/sketches/029-map-controls/iter-2/SCORE.md
  - .planning/sketches/029-map-controls/MANIFEST.md
  - .planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
  - .planning/STATE.md
autonomous: true
requirements: [MAPUX-02, MAPUX-03, MAPUX-04, MAPUX-05]

must_haves:
  truths:
    - "Iteration 2 is a genuine second screenshot->redesign->apply->re-screenshot->score cycle, distinct from iteration 1, and its redesign rationale explicitly cites a specific competitor finding from 29-BASELINE.md §3"
    - "Iteration 2 is scored against the same falsifiable rubric as iteration 1, at both viewports, so the two iterations are comparable"
    - "The design loop terminates in a written, accepted design spec that Phase 30 can implement against without re-deriving decisions"
    - "MAPUX-05's acceptance gate is recorded as a Fable-proxy decision with human review explicitly deferred, not waived"
  artifacts:
    - path: ".planning/sketches/029-map-controls/iter-2/SCORE.md"
      provides: "Raw evaluate_script JSON results for iteration 2, both viewports, plus the Tab-walk result"
    - path: ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md"
      provides: "The accepted deliverable: winning control-shell design, rubric comparison table across baseline/iter-1/iter-2, and the MAPSH-01..07 requirement-to-decision mapping Phase 30 implements against"
    - path: ".planning/STATE.md"
      provides: "A new Fable Decision (F-NN) recording the MAPUX-05 acceptance verdict and the deferred (not waived) human-review status, plus an updated deferred-live list entry"
  key_links:
    - from: ".planning/sketches/029-map-controls/iter-2/SCORE.md"
      to: ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md"
      via: "the design spec's rubric-comparison table cites iter-2's exact JSON values, not a paraphrase"
      pattern: "iter-2"
    - from: ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md"
      to: ".planning/STATE.md"
      via: "the Fable Decision row references the design spec path as the accepted artifact"
      pattern: "29-DESIGN-SPEC"
---

<objective>
Complete the map-control-shell design loop: run iteration 2 (informed by the already-captured CrimeMapping/CityProtect competitor findings, per MAPUX-04), then write the accepted design spec and record the Fable-proxy acceptance gate (MAPUX-05) that unblocks Phase 30.

**Every task in this plan is executed INLINE by the Fable orchestrator in the same BrowserOS-connected session — never dispatched to a `gsd-executor` subagent**, for the same structural reason as Plan 29-01 (Operating Lesson 1, directive line 100): `gsd-executor` has no Agent tool and no BrowserOS MCP access.

This phase ships **no map code**. Iteration 2's "apply" step writes throwaway static HTML under `.planning/sketches/029-map-controls/iter-2/`, never `site/src/components/map/*`. The design spec is documentation Phase 30 implements against in a later phase.

Purpose: Close out MAPUX-02 (≥2 iterations — this is the second), MAPUX-04 (competitor click-through cited in a redesign), and MAPUX-05 (accepted spec + acceptance gate), so Phase 30 can start.
Output: `.planning/sketches/029-map-controls/iter-2/*`, updated `MANIFEST.md`, `.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md`, and a new Fable Decision + deferred-live entry in `.planning/STATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-map-ux-design-loop/29-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-BASELINE.md
@.planning/phases/29-map-ux-design-loop/29-01-SUMMARY.md
@.planning/sketches/029-map-controls/MANIFEST.md
@.planning/sketches/029-map-controls/iter-1/SCORE.md
</context>

<interfaces>
<!-- Competitor findings this iteration MUST cite (from 29-BASELINE.md §3, already captured — do not re-crawl) -->

CrimeMapping.com (CityProtect family), five transferable findings already recorded:
1. Filters live in a permanent, labeled left rail (grouped by dimension: WHAT/WHERE/WHEN), not a flattened single row — nothing is ever offscreen.
2. The map is full-bleed edge-to-edge (vs. this project's 812/1296px boxed map).
3. Current filter state is always legible without opening a panel (persistent record-count chip + explicit active date-range text).
4. Empty filter results are announced in-place with a banner, never a silent blank map.
5. Search is prominent and wide, top-left — already matches this project's `SearchBox` placement.

Iteration 2's redesign MUST attack finding 1 specifically (the single flat scrollable row is the root cause per `29-BASELINE.md` §3's stated "Design consequence"): group the four dimensions (time / crime family / homicide layer / news layer) into separately-labeled, always-visible entry points rather than one undifferentiated scroll row, per Pattern 2 and the anti-pattern warning in `29-RESEARCH.md` ("hiding the news toggle inside the new filter dialog" is explicitly banned — the news toggle stays its own persistent control, separate from the grouped filter entry points).

MAPSH requirement text this spec must map decisions against (from `.planning/REQUIREMENTS.md`): MAPSH-01 (always-visible labeled news toggle), MAPSH-02 (native `<details>`/`<dialog>` filter panel, bottom-sheet/FAB on mobile, zero new JS deps), MAPSH-03 (keyboard-operable, no focus traps, touch targets >=44px, no z-index conflicts), MAPSH-05 (changes confined to `MapTopbar.tsx` and sibling components; `ChoroplethLayer.ts`/`IncidentPinLayer.ts`/`LowZoomDotLayer.ts` untouched; no declarative react-leaflet layer component), MAPSH-07 (shared vocabulary with news facets via data, not a code dependency). MAPSH-04 (`?region=` deep link) and MAPSH-06 (regression verification) are explicitly Phase 30's job, not this spec's — 29-RESEARCH.md's Open Question 3 already rules this out of scope; state that exclusion explicitly in the spec.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build, screenshot, and score iteration 2 (competitor-informed)</name>
  <files>.planning/sketches/029-map-controls/iter-2/desktop.html, .planning/sketches/029-map-controls/iter-2/mobile-375.html, .planning/sketches/029-map-controls/iter-2/desktop.png, .planning/sketches/029-map-controls/iter-2/mobile-375.png, .planning/sketches/029-map-controls/iter-2/SCORE.md, .planning/sketches/029-map-controls/MANIFEST.md</files>
  <action>
Executed inline by the orchestrator via BrowserOS MCP — cannot be delegated to `gsd-executor`.

Read `iter-1/SCORE.md` first: any rubric criterion iteration 1 failed (news toggle discoverability, filter overflow, touch targets, keyboard trap, z-index violations) is a mandatory target for iteration 2, in addition to the competitor-informed structural change.

Redesign target for iteration 2: restructure the flat `.filters-row` into grouped, separately-labeled, always-visible entry points (e.g., a compact top-bar cluster of distinct labeled buttons — "Year", "Crime type", "News layer" — each opening its own small `<details>` popover or, for the news toggle specifically, remaining a standalone always-active button per the anti-pattern warning) — citing CrimeMapping finding 1 explicitly in this iteration's delta note. Carry forward whatever iteration 1 already fixed (persistent news toggle at z-index >= 800, mobile `<dialog>` bottom sheet with real `showModal()`) rather than regressing it. Build `iter-2/desktop.html` and `iter-2/mobile-375.html` self-contained (inline `<style>`, no build step), reusing iteration 1's file structure as the base.

Open both files via BrowserOS (`file://`, desktop width; 375px iframe for mobile) and screenshot to `iter-2/desktop.png` / `iter-2/mobile-375.png`.

Run `scoreControlShell()` (from `.planning/sketches/029-map-controls/score.mjs`) via `evaluate_script` against both pages, plus the Tab-walk keyboard-trap procedure (same method as Plan 29-01 Task 3, including the open-`<dialog>`-confinement-is-a-pass caveat). Write `iter-2/SCORE.md` with both raw JSON blobs, the Tab-walk result, and a delta note that (a) names the specific competitor finding cited and (b) names the specific iter-1 rubric failure(s), if any, this iteration fixes — citing `iter-1/SCORE.md`'s actual values, not a summary. Append iteration 2's row to `MANIFEST.md`'s iteration table.
  </action>
  <verify>
    <automated>test -f ".planning/sketches/029-map-controls/iter-2/SCORE.md" && test -f ".planning/sketches/029-map-controls/iter-2/desktop.png" && test -f ".planning/sketches/029-map-controls/iter-2/mobile-375.png" && grep -c "newsToggleDiscoverable" ".planning/sketches/029-map-controls/iter-2/SCORE.md" && grep -ci "crimemapping" ".planning/sketches/029-map-controls/iter-2/SCORE.md"</automated>
  </verify>
  <done>iter-2's sketch HTML, screenshots and SCORE.md all exist; SCORE.md's delta note names a specific CrimeMapping finding (grep confirms the word appears, case-insensitive) and cites iter-1's actual prior values; MANIFEST.md's table has both iter-1 and iter-2 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Write the accepted design spec</name>
  <files>.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md</files>
  <action>
Executed inline by the orchestrator (document synthesis; no further BrowserOS calls needed beyond what Task 1 already captured).

Write `29-DESIGN-SPEC.md` consolidating: (1) the baseline problem statement (cite `29-BASELINE.md`'s exact 507px/991px overflow numbers), (2) a comparison table across baseline / iter-1 / iter-2 using the actual rubric values from `MANIFEST.md` (not re-summarized prose), (3) the winning design — state explicitly whether iter-2 is adopted wholesale or iter-1's news-toggle/dialog mechanics are combined with iter-2's grouped-entry-point structure (per `29-RESEARCH.md`'s Open Question 1 default: React-rendered elements in `MapTopbar.tsx`, not a Leaflet `L.control()`), (4) a per-MAPSH-requirement decision table (MAPSH-01, 02, 03, 05, 07 each get one row: "decision" + "source iteration/finding"; MAPSH-04 and MAPSH-06 rows state explicitly "out of scope for this spec, Phase 30's responsibility" per `29-RESEARCH.md` Open Question 3), (5) explicit reaffirmation of the locked constraints Phase 30 must not violate (no new shipped JS dependencies, `client:only="react"` on the map island never `client:load`, `ChoroplethLayer.ts`/`IncidentPinLayer.ts`/`LowZoomDotLayer.ts` untouched, no declarative react-leaflet layer component), (6) links to every evidence artifact (`iter-1/`, `iter-2/`, `competitor-clickthrough` findings already in `29-BASELINE.md` §3, `MANIFEST.md`).
  </action>
  <verify>
    <automated>test -f ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md" && grep -c "MAPSH-01" ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md" && grep -c "MAPSH-02" ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md" && grep -c "iter-2" ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md"</automated>
  </verify>
  <done>29-DESIGN-SPEC.md exists, has a filled MAPSH-01..07 decision table (with MAPSH-04/06 explicitly marked out of scope), a baseline/iter-1/iter-2 comparison table using real rubric numbers, and links to every evidence artifact under .planning/sketches/029-map-controls/.</done>
</task>

<task type="auto">
  <name>Task 3: Record the Fable-proxy acceptance gate and close the phase</name>
  <files>.planning/STATE.md</files>
  <action>
Executed inline by the orchestrator — this IS the acceptance act (MAPUX-05), not a delegated review.

Append a new row `F-39` (next available number after F-38; confirm the current max F-NN in `.planning/STATE.md` § Fable Decisions before numbering, since F-37 was a correction and rows are not always monotonic in table order) recording: the design spec path (`.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md`) as ACCEPTED against MAPUX-03's five concrete criteria (cite the iter-2 rubric result showing the criteria that pass); explicitly state "human review of this acceptance is DEFERRED, not waived" per the directive's gate amendment 1 and the existing deferred-live list item 5; state that Phase 30 is now unblocked to start per ROADMAP.md's hard-gate wording. Update the existing "Deferred-live list" item about Phase 29's human review (currently "pending, that phase has not run yet") to reflect that the evidence pack now exists and is awaiting the user's read, with the exact path to review (`29-DESIGN-SPEC.md` plus `.planning/sketches/029-map-controls/`).

Then run the phase-close gate, executed exactly as written below (already verified to exit 0 against the current tree with 4 expected astro-check errors in `ComparatorPairsLinks.astro:28`, 16/16 validators, and pytest 344 passed/1 skipped/1 xfailed) — do not "fix" the 4 astro-check errors or the 1 xfail, both are pre-existing and deliberate per F-34/F-08.
  </action>
  <verify>
    <automated>cd site && npx astro check; npm run build && npm test && npm run validate && cd .. && python -m pytest -q</automated>
  </verify>
  <done>STATE.md has a new Fable Decision recording MAPUX-05's Fable-proxy acceptance with human review explicitly deferred (not waived); the deferred-live list is updated with the evidence-pack path; the phase-close gate exits 0 with 16/16 validators and pytest 344 passed/1 skipped/1 xfailed (or the F-19 freshness exclusion branch is invoked and recorded per its documented preconditions, never silently).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None — no new trust boundary | Throwaway static HTML + documentation artifacts only; `29-DESIGN-SPEC.md` is read by a future Phase 30 planner, not executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-02 | Tampering | `.planning/sketches/029-map-controls/iter-2/*.html` | accept | Throwaway static files, never deployed |
| T-29-03 | Repudiation | Fable-proxy acceptance of MAPUX-05 | mitigate | Acceptance is recorded as a numbered, dated, rationale-bearing Fable Decision row in STATE.md (auditable), and explicitly labeled deferred human review rather than a silent waiver |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `iter-2/` has the same five-artifact shape as `iter-1/` (desktop.html, mobile-375.html, desktop.png, mobile-375.png, SCORE.md).
- `29-DESIGN-SPEC.md` exists and maps every in-scope MAPSH requirement to a decision with a cited source.
- STATE.md's new Fable Decision explicitly uses the words "deferred" for human review, never "waived" or "skipped".
- Phase-close gate command exits 0 against the real tree (already verified once by the planner before this plan was written; re-run at execution time is mandatory, not optional, since the tree changes between planning and execution).
</verification>

<success_criteria>
MAPUX-01 through MAPUX-05 are all satisfied with cited, falsifiable evidence; Phase 30 is unblocked per ROADMAP.md's hard-gate wording; the phase closes with a green gate and an honestly-recorded deferred (not waived) human-review status.
</success_criteria>

<output>
Create `.planning/phases/29-map-ux-design-loop/29-02-SUMMARY.md` when done
</output>
