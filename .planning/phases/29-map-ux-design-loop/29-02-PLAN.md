---
phase: 29-map-ux-design-loop
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - .planning/sketches/029-map-controls/iter-2/desktop.html
  - .planning/sketches/029-map-controls/iter-2/mobile-375.html
  - .planning/sketches/029-map-controls/iter-2/desktop.png
  - .planning/sketches/029-map-controls/iter-2/mobile-375.png
  - .planning/sketches/029-map-controls/iter-2/SCORE.md
  - .planning/sketches/029-map-controls/check-spec.mjs
  - .planning/sketches/029-map-controls/MANIFEST.md
  - .planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
  - .planning/phases/29-map-ux-design-loop/29-UI-CHECK.md
  - .planning/STATE.md
autonomous: true
requirements: [MAPUX-02, MAPUX-03, MAPUX-04, MAPUX-05]

must_haves:
  truths:
    - "Iteration 2 is a genuine second redesign cycle, proven distinct from iteration 1 by check-iteration.mjs (structural difference or a FAIL->PASS cell), not by prose"
    - "Iteration 2's redesign rationale cites a specific competitor finding from 29-BASELINE.md §3"
    - "The design spec pins implementable values — z-index numbers, the four real breakpoint bands, element semantics, focus order, ARIA, EN+ES label strings, data-role hooks — so Phase 30 implements rather than re-designs"
    - "MAPUX-05 acceptance is issued only AFTER an independent Opus review of the evidence pack, discharging F-39 and breaking the F-06/F-11/F-23 self-review circularity"
  artifacts:
    - path: ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md"
      provides: "The accepted deliverable: winning design, the nine pinned sections, and the criterion x condition matrix Phase 30 must reproduce in real code"
    - path: ".planning/phases/29-map-ux-design-loop/29-UI-CHECK.md"
      provides: "Independent Opus verdict on the evidence pack, blind to the orchestrator's rationale — the gate that makes the acceptance non-circular"
    - path: ".planning/STATE.md"
      provides: "Fable Decision F-42 recording the MAPUX-05 acceptance with human review explicitly deferred (not waived), citing the Opus verdict"
  key_links:
    - from: ".planning/sketches/029-map-controls/iter-2/SCORE.md"
      to: ".planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md"
      via: "the spec's matrix cites iter-2's exact JSON values, not a paraphrase"
      pattern: "iter-2"
    - from: ".planning/phases/29-map-ux-design-loop/29-UI-CHECK.md"
      to: ".planning/STATE.md"
      via: "the F-42 row cites the Opus verdict as the basis for acceptance"
      pattern: "29-UI-CHECK"
---

**Fable review: APPROVED 2026-07-30** — same revision cycle as 29-01. The structural change here is Task 2b: an independent Opus review of the evidence pack now sits between writing the spec and accepting it, discharging F-39 (which the first draft dropped) and breaking the self-review circularity that F-06/F-11/F-23 exist to prevent. The phase-close gate's astro-check assertion was restored (the first draft kept F-34's deliberate `;` but deleted the assertion that makes it safe, leaving 4 errors and 400 errors indistinguishable) and verified by mutation: exit 0 at the real 4/0 baseline, exit 1 against a synthetic 40-error log. The `site/` chain is now a subshell — verified that a mid-chain failure both propagates and leaves the persistent cwd intact.

<objective>
Close the design loop: run iteration 2 (informed by the already-captured CrimeMapping findings, MAPUX-04), write the accepted design spec, obtain an independent Opus review of it, and only then record the Fable-proxy acceptance (MAPUX-05) that unblocks Phase 30.

**ORCHESTRATOR: do not invoke `/gsd:execute-phase` for this phase. Execute these tasks directly in this session.** Same structural reason as Plan 29-01 (Operating Lesson 1): `gsd-executor` has no Agent tool and no BrowserOS access. The BrowserOS resume protocol in `29-01-PLAN.md` applies unchanged to every browser step below.

This phase ships **no map code**. Iteration 2 writes throwaway HTML under `.planning/sketches/029-map-controls/iter-2/`, never `site/src/components/map/*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-map-ux-design-loop/29-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-BASELINE.md
@.planning/phases/29-map-ux-design-loop/29-PREMORTEM.md
@.planning/sketches/029-map-controls/STRESS.md
@.planning/sketches/029-map-controls/MANIFEST.md
@.planning/sketches/029-map-controls/iter-1/SCORE.md
</context>

<interfaces>
Competitor findings this iteration must cite (29-BASELINE.md §3, already captured live — do not re-crawl; police.uk was behind a Cloudflare human-verification challenge that was NOT circumvented, and MAPUX-04 permits "police.uk **or** CityProtect"):
1. Filters live in a permanent, labeled rail grouped by dimension (WHAT/WHERE/WHEN) — nothing is ever offscreen.
2. The map is full-bleed edge-to-edge (vs this project's 812/1296px boxed map).
3. Current filter state is always legible without opening a panel (persistent record count + explicit active range).
4. Empty results are announced in place, never a silent blank map.
5. Search is prominent and wide, top-left — already matches this project's `SearchBox`.

**Iteration 2 must attack finding 1**, the root cause per 29-BASELINE.md §3's stated design consequence: one flat scrollable row is carrying five dimensions (mode, year, crime family, homicide layer, news layer). Group them into separately-labeled, always-visible entry points. The news toggle **stays its own persistent control** — hiding it inside the new filter dialog is explicitly banned, since that would re-create the discoverability bug the phase exists to fix.

MAPSH requirements the spec maps decisions against: MAPSH-01 (always-visible labeled news toggle), MAPSH-02 (native `<details>`/`<dialog>` + CSS, bottom-sheet/FAB on mobile, zero new JS deps), MAPSH-03 (keyboard-operable, no focus traps, >=44px targets, no z-index conflicts), MAPSH-05 (changes confined to `MapTopbar.tsx` and sibling control components; `ChoroplethLayer.ts`/`IncidentPinLayer.ts`/`LowZoomDotLayer.ts` untouched; no declarative react-leaflet layer component), MAPSH-07 (shared vocabulary with news facets via data, not a code dependency). MAPSH-04 (`?region=`) and MAPSH-06 (regression verification) are Phase 30's job — the spec states that exclusion explicitly.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build, screenshot and score iteration 2 (competitor-informed)</name>
  <files>.planning/sketches/029-map-controls/iter-2/desktop.html, .planning/sketches/029-map-controls/iter-2/mobile-375.html, .planning/sketches/029-map-controls/iter-2/desktop.png, .planning/sketches/029-map-controls/iter-2/mobile-375.png, .planning/sketches/029-map-controls/iter-2/SCORE.md, .planning/sketches/029-map-controls/MANIFEST.md</files>
  <action>
Executed inline by the orchestrator via BrowserOS — cannot be delegated.

**Read `iter-1/SCORE.md` first.** Every criterion x stress-condition cell that iteration 1 failed is a mandatory target for iteration 2, in addition to the competitor-informed restructure. If iteration 1 failed nothing, STRESS.md's anti-theater rule has already added a sixth harder condition — iteration 2 is scored under it too.

**Redesign target**: restructure the flat row into grouped, separately-labeled, always-visible entry points (each `data-role="entry-point"`, each opening its own `<details>` popover), with the news toggle remaining a standalone always-visible control at explicit `z-index >= 800`. Carry forward what iteration 1 fixed rather than regressing it. Reuse iteration 1's file structure, the Leaflet pane stand-in and the full competing-furniture inventory — a design scored without the stand-ins is not comparable to iteration 1.

Serve, screenshot and score exactly as Plan 29-01 Task 2/3 specify: HTTP on 4399 (never `file://` — opaque origin, `contentDocument` null, verified by execution), same-origin assertion before any mobile score, screenshots to `C:\Users\Carlo\bos-shots\p29\iter-2-*.png` then `cp` into the repo, one run per frozen stress condition, blobs flushed to `SCORE.md` as produced, criterion 4 by construction plus the single `:modal` interaction.

The delta note must (a) name the specific CrimeMapping finding cited and (b) name the specific iter-1 cell(s) this iteration changes, citing `iter-1/SCORE.md`'s actual values. Append iteration 2's row to `MANIFEST.md`.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- site/)" && test -f .planning/sketches/029-map-controls/iter-2/desktop.png && test -f .planning/sketches/029-map-controls/iter-2/mobile-375.png && node .planning/sketches/029-map-controls/check-iteration.mjs 2 && grep -qi "crimemapping" .planning/sketches/029-map-controls/iter-2/SCORE.md</automated>
  </verify>
  <done>check-iteration.mjs passes for iteration 2 — which, per its rule 3, means iteration 2 is structurally distinct from iteration 1 or moved a cell FAIL->PASS, so a recolour cannot pass; both screenshots are in the repo; the delta note cites CrimeMapping and iter-1's real values; `site/` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write the accepted design spec</name>
  <files>.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md, .planning/sketches/029-map-controls/check-spec.mjs</files>
  <action>
Executed inline by the orchestrator; no further BrowserOS calls needed.

Phase 30 is a declared regression-risk phase working against protected Leaflet layer files. It must **implement**, not decide. So the spec pins values, not intentions. Write these nine sections, each as a table or explicit list:

1. **Exact z-index for every new/changed control**, placed in the real ladder (700 legend/result-panel/zoom-ctl · 800 topbar/partial-year-badge · 801 mode-toggle · 900 search-dropdown/error-overlay · 1000 toast · Leaflet 400–1000). Rule: a modal `<dialog>` uses the top layer and needs **no** z-index; every non-modal control needs an explicit number, never `auto`.
2. **The four real breakpoint bands** — `<480`, `480–639`, `640–1023`, `>=1024` (`map.css:192/210/240/271/463`), **not** "desktop and 375px" — stating what the shell is in each, and what becomes of `.mode-toggle`'s `position:static` reset at `<=480`.
3. **Exact element semantics per control**: `<button aria-pressed>` vs `<input type="checkbox">` vs `<details>/<summary>` vs `<dialog>`. This determines React state shape and MAPSH-02 compliance and cannot be inferred from a screenshot.
4. **Focus order** as an explicit DOM-order list, plus what happens on `<dialog>` close (focus returns to the invoking control).
5. **ARIA**: `aria-label`/`aria-pressed` on the news toggle; `aria-expanded` + `aria-controls` per entry point; `aria-labelledby` on the dialog; and whether a live region announces filter results — CrimeMapping findings 3 and 4 argue yes, and `/news/` already ships the precedent (`#news-result-count`, `aria-live="polite"`, verified in F-41).
6. **Exact EN and ES label strings with their i18n keys**, plus the longest-string layout consequence from STRESS.md condition 2.
7. **The `data-role` hooks Phase 30 must ship into the real DOM** (`news-toggle`, `entry-point`, `filter-entry`) — so `score.mjs` runs **unchanged** against the real `/map/` as Phase 30's MAPSH-06 regression instrument. Without this the rubric dies with the sketch and Phase 30 has no acceptance instrument.
8. **Explicit non-decisions**: MAPSH-04 `?region=` out of scope; and the Phase 28 N1 trap — `/news/`'s `?region=` is **CSV multi-value** while the map's is **single-value focus**, so Phase 30 must NOT reuse `parseFilterParams`. The spec must therefore state, per filter dimension it introduces, whether it is single- or multi-select, and flag the divergence.
9. **The criterion x condition matrix the accepted design achieved**, so Phase 30 knows the bar to reproduce in real code, not just the shape to build.

Plus: the baseline problem statement citing 507px/991px; a comparison across baseline/iter-1/iter-2 using MANIFEST.md's real values; and links to every evidence artifact.

**Two rulings to state explicitly in the spec:**
- **Full-bleed is OUT of scope (F-43).** CrimeMapping finding 2 and the 812/1296 = 0.63 `mapWidthRatio` are recorded as findings; `mapWidthRatio` is measured but **not gating**. Achieving full-bleed means changing `.map-stage`/`.map-canvas` in `map.css`, which is in direct tension with MAPSH-05's confinement of Phase 30 to `MapTopbar.tsx` and siblings. The phase's stated failure is discoverability, not framing; widening a declared regression-risk phase for a secondary finding is a bad trade.
- **Phase 30 driving warning (F-40).** Until F-40's cross-origin redirect is fixed, Phase 30 must always drive `http://127.0.0.1:4321/es/mapa/`, **never** `/map/` — the EN page bounces an es-locale browser to production, so the next agent to open it would silently measure production instead of its own build. Add "fix F-40's one-line redirect" as an explicit Phase 30 Wave-0 task, since that fix touches `site/` and is therefore barred from this phase by its own hard gate.
- **Loop-history note (premortem F16, accepted as-is):** iteration 1's "before" is the measured baseline rather than a prior sketch. The chain baseline -> iter-1 -> iter-2 contains two complete redesign->re-measure transitions, which is what MAPUX-02's ">=2 full iterations" protects. State this reading so the returning human need not wonder whether a cycle was skipped.

Write `check-spec.mjs` (a real `.mjs`, per F-36) asserting each of the nine sections is present by heading, that the z-index table carries a numeric value per control rather than prose, and that the tokens `MAPSH-01..07`, `iter-1`, `iter-2`, `507` and `991` all appear. Prove it fails on a spec with the z-index column blank before relying on it.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- site/)" && node .planning/sketches/029-map-controls/check-spec.mjs .planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md</automated>
  </verify>
  <done>29-DESIGN-SPEC.md has all nine pinned sections with numeric z-index values, the four real breakpoint bands, the MAPSH decision table with 04/06 marked out of scope, both explicit rulings (F-43 full-bleed, F-40 driving warning) and the loop-history note; check-spec.mjs was demonstrated to fail on a blanked z-index column before being trusted.</done>
</task>

<task type="auto">
  <name>Task 2b: Independent Opus review of the evidence pack — blind to the orchestrator's rationale</name>
  <files>.planning/phases/29-map-ux-design-loop/29-UI-CHECK.md</files>
  <action>
Executed inline by the orchestrator **spawning** an Opus `gsd-ui-checker` (`model="opus"` per F-18 routing). **This task discharges F-39 and must complete before Task 3 records acceptance — acceptance must not precede the review.**

Why it exists: without it, the same agent writes the rubric, builds both iterations, scores them, writes the spec and accepts it. That is precisely the circularity F-06 (blind second pass), F-11 (split the dispatch so reviewer != author) and F-23 (escalate, never let the producing model review itself) exist to prevent. The directive's gate amendment 1 replaces *human* acceptance with Fable acceptance; it does not license *unreviewed* acceptance. And F-39 already bound this phase to an Opus check that the first draft of these plans silently dropped.

The agent is given ONLY: `29-BASELINE.md`, `STRESS.md`, `MANIFEST.md`, `iter-1/SCORE.md`, `iter-2/SCORE.md`, the four PNGs, `29-DESIGN-SPEC.md`, and the MAPUX-01..05 + MAPSH-01..07 requirement text. It is **not** given the plans, the summaries, or any orchestrator commentary — it must reconstruct the argument from the evidence exactly as the returning human will.

It answers with BLOCK/FLAG/PASS per question, in `29-UI-CHECK.md`:
1. Is iteration 2 a genuinely distinct design from iteration 1, judged from the two HTML files and the two screenshots — or a restyle? Name the structural difference, or state there is none.
2. Which rubric criteria are falsifiable against these sketches, and which are true by construction? Name each explicitly.
3. Does the spec contain everything Phase 30 needs to implement without re-designing, checked against the real `map.css` and `MapTopbar.tsx`? Name every missing decision.
4. Is there any decision in the spec the evidence does not support?

A **BLOCK** on question 1 or 3 must be resolved (another iteration, or spec amendments) before Task 3. Max 2 cycles, then Fable decides inline and records the unresolved BLOCK verbatim in the F-42 row and in STATE.md `## Blockers` — never silently. If Opus is unavailable (`529`), apply F-23: do **not** downgrade to Sonnet; run the pass inline as Fable against the blind evidence set only, record that fact in F-42, and add "independent Opus re-review of 29-UI-CHECK.md" to the deferred-live list.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-map-ux-design-loop/29-UI-CHECK.md && node -e "const s=require('fs').readFileSync('.planning/phases/29-map-ux-design-loop/29-UI-CHECK.md','utf8');const q=(s.match(/BLOCK|FLAG|PASS/g)||[]).length;if(q<4){console.error('FAIL: expected a BLOCK/FLAG/PASS verdict for each of the 4 questions, found '+q);process.exit(1)};console.log('UI-CHECK has '+q+' verdicts')"</automated>
  </verify>
  <done>29-UI-CHECK.md exists with a verdict per question; any BLOCK on Q1 or Q3 is either resolved or recorded verbatim in F-42 and STATE.md Blockers.</done>
</task>

<task type="auto">
  <name>Task 3: Record the acceptance gate and close the phase</name>
  <files>.planning/STATE.md</files>
  <action>
Executed inline by the orchestrator — this is the acceptance act (MAPUX-05), now resting on Task 2b's independent review rather than on self-review.

Append a new row **`F-42`** to STATE.md § Fable Decisions. **F-39, F-40 and F-41 already exist** — all three written earlier this session for this phase — so the original "next after F-38" was false when written; derive the number by grepping `^| F-` and taking the numeric max, never by reading the last row (the table is not sorted). The row records: `29-DESIGN-SPEC.md` ACCEPTED against MAPUX-03's five criteria, citing the iter-2 matrix; the `29-UI-CHECK.md` verdict, stating that acceptance is **Fable-proxy on independently-reviewed evidence**, not Fable-only; that **human review is DEFERRED, not waived** (directive gate amendment 1); and that Phase 30 is unblocked per ROADMAP.md's hard-gate wording.

Update deferred-live item 5 from "pending, that phase has not run yet" to the evidence-pack paths the user should read: `29-DESIGN-SPEC.md`, `29-UI-CHECK.md`, and `.planning/sketches/029-map-controls/`.

Then run the phase-close gate below. Note its shape: the whole `site/` chain runs in a **subshell**, because the Bash tool's cwd persists between calls and a mid-chain failure would otherwise leave the shell inside `site/`, silently breaking the next command's relative paths. The single `;` after `npx astro check` is deliberate and load-bearing (F-34): astro-check exits non-zero at the expected 4-error baseline, and `check-astro.mjs` downstream is what makes discarding that status safe. **Never remove the assertion while keeping the `;`** — that combination is what turned this gate decorative in the first draft, where 4 errors and 400 errors were indistinguishable. Do not "fix" the 4 astro errors (`ComparatorPairsLinks.astro:28`) or the 1 xfail (Phase 26's deliberate quarantine, F-08).
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- site/)" && (cd site && npx astro check > .astro-check.log 2>&1; node ../.planning/sketches/029-map-controls/check-astro.mjs .astro-check.log && rm -f .astro-check.log && npm run build && npm test && npm run validate) && python -m pytest -q</automated>
  </verify>
  <done>STATE.md carries F-42 recording MAPUX-05's acceptance with human review explicitly deferred (not waived) and citing 29-UI-CHECK.md; the deferred-live list points at the evidence pack; the close gate exits 0 with astro check at exactly 4/0, 16/16 validators, vitest 37, pytest 344 passed / 1 skipped / 1 xfailed. If `freshness` goes red, apply F-19's branch out of band and record it in STATE.md `## Blockers` — never encode that exclusion in the gate command (F-34).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None — no new trust boundary | Throwaway static HTML plus documentation; `29-DESIGN-SPEC.md` is read by a future Phase 30 planner, not executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-02 | Tampering | `.planning/sketches/029-map-controls/iter-2/*.html` | accept | Throwaway static files, never deployed |
| T-29-03 | Repudiation | Fable-proxy acceptance of MAPUX-05 | mitigate | Acceptance rests on an independent Opus verdict (Task 2b) and is recorded as a numbered, dated, rationale-bearing decision row citing that verdict; human review explicitly deferred, never silently waived |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `test -z "$(git status --porcelain -- site/)"` is the FIRST assertion of every task — the enforcement of the phase's no-map-code hard gate, which both first-draft plans stated in prose and neither checked. On failure: `git checkout -- site/`, record in STATE.md `## Blockers`, re-run.
- Task 2b precedes Task 3 in execution order. Acceptance after review, never before.
- No `<automated>` block launders an exit code: no `| tail`, `| head`, `; echo`, `|| echo`, `|| true`. The one `;` in Task 3 sits immediately after `npx astro check` and is immediately followed by the assertion that makes it safe (F-34); the `site/` chain is wrapped in a subshell so a failure cannot poison the persistent cwd.
</verification>

<success_criteria>
MAPUX-01..05 all satisfied with cited, falsifiable evidence; the acceptance is independently reviewed rather than self-issued; Phase 30 is unblocked per ROADMAP.md's hard gate; the phase closes green with human review honestly recorded as deferred.
</success_criteria>

<output>
Create `.planning/phases/29-map-ux-design-loop/29-02-SUMMARY.md` when done
</output>
