---
phase: 28-news-visualizer-ui
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - site/scripts/validate/facets.mjs
autonomous: true
requirements: [NEWSUI-01, NEWSUI-02, NEWSUI-04, NEWSUI-05, NEWSUI-06, NEWSUI-07]

must_haves:
  truths:
    - "facets.mjs (still validator #16 — extended, not replaced, so no hardcoded validator count anywhere needs correcting) proves, against the real built dist/ output, every falsifiable claim in 28-VALIDATION.md's Wave 0 table"
    - "The F-27 bundling assumption (Astro's Vite pipeline bundles a bare <script>'s ES import) is proven by build, not asserted in prose — both dist news pages are shown to reference a chunk containing newsFilterLogic's code"
    - "The full phase-close gate (build + vitest + validate, chained in one command, no exit-code laundering) is green, astro check stays at the 4-error baseline, and the Python pytest suite is unaffected"
  artifacts:
    - path: "site/scripts/validate/facets.mjs"
      provides: "new assertions 11-17: anchorDate correctness, card-count==incident-count, i18n key parity, empty-state node presence, no-island, no-cluster-markup, F-27 bundled-import proof — each both-locales, source+dist where applicable"
  key_links:
    - from: "facets.mjs new assertions"
      to: "dist/news/index.html and dist/es/noticias/index.html"
      via: "existsSync + readFileSync against real build output, per-locale iteration (RR-1 pattern)"
      pattern: "DIST_PAGES|SERIALIZATION_SITES"
---

<objective>
Close out Phase 28 with the falsifiable build-time proof VALIDATION.md's Wave 0 table requires: extend `facets.mjs` (stay at validator #16 — do not add a sibling file and trigger the F-21 stale-count hazard) with assertions for `anchorDate` correctness, the NEWSUI-02 static-superset card count, i18n key parity, the empty-state node, the no-island / no-cluster-markup guards, and — the plan's single highest-risk item — proof that Astro actually bundles `newsFilterLogic.ts`'s import into both pages' shipped JS (F-27's load-bearing assumption). Then run and confirm the full chained phase-close gate.

Purpose: convert every prose claim in Plans 28-01/02 into a build-time-falsifiable check, per the plan-quality requirement that no assertion ships without a demonstrated RED counterexample.
Output: `facets.mjs` extended with 7 new numbered assertions; a green, chained `npm run build && npm test && npm run validate` phase-close gate; `npm run check` confirmed at the 4-error/0-warning baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-news-visualizer-ui/28-UI-SPEC.md
@.planning/phases/28-news-visualizer-ui/28-VALIDATION.md
@.planning/phases/28-news-visualizer-ui/28-01-SUMMARY.md
@.planning/phases/28-news-visualizer-ui/28-02-SUMMARY.md
@site/scripts/validate/facets.mjs
@site/src/pages/news.astro
@site/src/pages/es/noticias.astro
</context>

<interfaces>
<!-- facets.mjs's own established idioms (Plan 28-02's SUMMARY confirms exact final markup this plan reads). -->
- `fail(reason)` — logs `FAIL facets: ${reason}` and `process.exit(1)`. Every new assertion uses this, never a bare `throw`.
- `SERIALIZATION_SITES` (source-level, both `.astro` files) and `DIST_PAGES` (built HTML, both locales) arrays — the existing two-locale-iteration pattern (assertion 7, RR-1). Every new assertion that touches page content MUST iterate both entries, never check only EN (the exact defect RR-1 fixed).
- Existing regex idiom for extracting the `#news-facets` node: `distHtml.match(/<script type="application\/json" id="news-facets">([\s\S]*?)<\/script>/)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend facets.mjs with assertions 11-16 (anchorDate, card-count, i18n parity, empty-state, no-island, no-cluster-markup)</name>
  <files>site/scripts/validate/facets.mjs</files>
  <read_first>
    site/scripts/validate/facets.mjs (full file — assertion 7's SERIALIZATION_SITES/DIST_PAGES pattern at lines 244-332 is the exact template; the "assertion N — ${locale} ..." message convention; the `fail()` helper)
    .planning/phases/28-news-visualizer-ui/28-VALIDATION.md § Wave 0 Requirements falsifiability table (the exact counterexample each assertion must catch)
    site/src/pages/news.astro and site/src/pages/es/noticias.astro (post-28-02 state, via their SUMMARY — exact rendered markup: filter-bar IDs, `#news-empty` structure, `data-*` attribute names, `class="news-card"` opening-tag shape)
    site/src/config/i18n.ts (post-28-01 state — the 15 `news_*` keys and their exact EN_STRINGS/ES_STRINGS locations)
  </read_first>
  <action>
    Add six new numbered assertion blocks (11-16) after the existing assertion 10, each following assertion 7's per-locale iteration template exactly:
    (11) **anchorDate correctness** — independently recompute the max `date` string across raw `incidents` (same lexicographic-max logic already used for assertion 8's `newestDate`, do not duplicate a third time — reuse the existing `newestDate` local already computed above assertion 8, or hoist that computation earlier if assertion ordering requires it) and assert both dist pages' parsed `#news-facets.anchorDate` equals it exactly. Falsifiable counterexample (state in a comment, mirroring assertion 7's style): construct a tmp-fixture current.json whose max incident date is moved back one day and confirm this assertion — run via `FACETS_CURRENT_JSON=<tmp>` — fails; this file need not execute that fixture run itself (that is a manual RED/GREEN demonstration performed once during this task, per the plan-quality rule), but the assertion logic itself must be capable of catching it.
    (12) **card-count == incident-count** — for each dist page, count occurrences of the literal `class="news-card"` opening-tag pattern (regex `/<article class="news-card"/g` or equivalent — NOT a bare substring count, which would also match attribute text per VALIDATION.md's explicit warning) and assert it equals `incidents.length`. This directly proves NEWSUI-02 (full static superset, filtering never removes server-rendered content).
    (13) **i18n key parity** — read `site/src/config/i18n.ts` source, extract the `EN_STRINGS` and `ES_STRINGS` object bodies (string-split or balanced-brace extraction, matching this validator's existing style of parsing TS source with regex/string ops rather than importing the module — consistent with assertion 3's `schema.py` parsing approach), and assert every key matching `/^\s*news_[a-z_]+:/m` in one block also appears in the other. Fail with the specific missing key name.
    (14) **empty-state node presence** — for each dist page, assert the HTML contains an element with `id="news-empty"` and the `hidden` attribute present on it in the initial (unfiltered) render, and that it is NOT an `<h1>`/`<h2>` tag (regex-check the tag name immediately preceding `id="news-empty"` is not `h1`/`h2`).
    (15) **no-island** — for each dist page, assert the HTML does NOT contain the string `astro-island` (Astro's hydration marker) — proves NEWSUI-06's "no measurable... hydration regression" structurally, not just by inspection.
    (16) **no cluster markup** — for each dist page AND each `.astro` source file, assert the HTML/source does not contain a source-count badge class or string tied to `news_cluster_source_count` being rendered (e.g. no element referencing that key in either page's template body — the key may exist in `i18n.ts` per Plan 28-01's reserved-key decision, but must never be read by either page's Astro template). Distinguish "key exists in i18n.ts" (expected, assertion 13 requires it) from "key is consumed in news.astro/noticias.astro markup" (forbidden) — grep the two `.astro` sources specifically, not `i18n.ts`.
    Every new assertion message starts `assertion N — ${locale} ...` matching the existing numbering convention. Do not scan this validator's own source for any of these patterns (self-scan tautology, per F-24's lesson).
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/facets.mjs</automated>
  </verify>
  <done>facets.mjs exits 0 against the real build; each of the six new assertions was manually broken once (edit the corresponding source/dist condition or point FACETS_CURRENT_JSON at a mutated tmp fixture), confirmed to produce a non-zero exit with the expected `FAIL facets: assertion N` message, then restored and reconfirmed exit 0 — before moving to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: F-27 bundling proof (assertion 17) + full chained phase-close gate</name>
  <files>site/scripts/validate/facets.mjs</files>
  <read_first>
    site/src/lib/newsFilterLogic.ts (Plan 28-01 — pick one unique, unlikely-to-collide identifier from this module, e.g. the `WINDOW_7D_WIDTH_DAYS` constant name or a distinctive function name, to search for in built JS chunks)
    site/dist/_astro/ directory structure after a build (RankingTableEnhancer.astro's bundled chunk is the existing proof-of-concept this codebase already ships, per 28-RESEARCH.md Q7/28-PATTERNS.md — locate its chunk naming convention as the template, e.g. `*.astro_astro_type_script_index_0_lang.*.js`)
    .planning/STATE.md Fable Decision F-27 (the exact assumption this assertion exists to prove, and what "fails loudly, never falls back silently" means operationally for a validator)
  </read_first>
  <action>
    Add assertion 17: after a real build, walk `site/dist/_astro/` for `.js` files (reuse the existing `walk()` helper's shape from assertion 10, or a small local equivalent) and assert AT LEAST ONE chunk's contents include a distinctive identifier from `newsFilterLogic.ts` (e.g. the literal string `WINDOW_7D_WIDTH_DAYS` or `parseFilterParams`) — proving Astro's Vite pipeline actually bundled the module rather than the import silently no-op'ing or erroring pre-build. Then, for each of the two dist news pages, assert the page's HTML references a `<script type="module" src="/_astro/...">` (or equivalent Astro-emitted script tag) pointing at a chunk — cross-reference that at least one such referenced chunk is among the chunks found to contain the distinctive identifier (a page that ships an unrelated script tag while the filter chunk exists elsewhere would NOT satisfy this — the assertion must tie the specific page to the specific chunk, not just prove the chunk exists somewhere in dist/). Falsifiable counterexample to state in the comment and demonstrate once: temporarily rename the import path in `news.astro` to something invalid, rebuild, confirm this assertion fails with a clear message (or the build itself fails first — either is an acceptable RED, but confirm which one actually happens and document it in the comment) — then restore and reconfirm GREEN. Per F-27, if this assertion cannot be made to pass with a genuine, unmodified `import` statement (i.e., the bundling assumption is actually false), STOP and report — do not silently rewrite Plan 28-02 to duplicate the module inline; that decision belongs to the orchestrator, not this task.
    After assertion 17 is green, run the full chained phase-close gate as ONE command (OneDrive rule) and confirm every step's real exit code — never route through `| tail`/`| head`/`; echo`/`|| echo`.
  </action>
  <verify>
    <automated>cd site && npm run build && npm test && npm run validate && npm run check 2>&1 | grep -E "^[0-9]+ error" | grep -qE "^4 error"</automated>
  </verify>
  <done>All 17 facets.mjs assertions pass against a real build; assertion 17 concretely ties each dist news page to a built chunk containing newsFilterLogic.ts's code (F-27 proven by build, not asserted); `npm test` (vitest, including newsFilterLogic.test.ts + newsFacets.test.ts) exits 0; `npm run validate` (all validators) exits 0; `npm run check` reports exactly 4 errors (unchanged pre-existing baseline in scripts/validate/*.mjs, 0 warnings).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time `dist/` artifacts → validator process | Trusted (produced by the same build this validator gates), but the validator must not trust CLAIMS about the build (e.g. "the import was added") without reading the actual bundled output — this is exactly what assertion 17 exists to close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Tampering | A future edit silently reverts `escapeForInlineScript` on the now-larger `facetsProjection` (including `anchorDate`) | mitigate | Already covered by existing assertion 7, unmodified by this plan — `anchorDate` folds into the same guarded object, no new escaping call site was introduced (verified in Plan 28-02) |
| T-28-09 | Tampering / Repudiation | A future edit copy-pastes `newsFilterLogic.ts`'s logic inline instead of importing it, silently reintroducing the twin-drift risk F-27 forbids | mitigate | Assertion 17 fails if the shipped page's script chunk does not contain the module's distinctive identifier — a copy-paste that doesn't import would not produce that exact chunk reference, catching the regression at build-validation time, not at code review time |
| T-28-10 | Denial of Service (build) | A malformed/huge `dist/_astro/` walk in assertion 17 on a much larger future build | accept | Bounded by the existing `SKIP_DIRS` pattern (node_modules/.astro/dist already excluded from the public walk in assertion 10); `_astro/` output is Astro-generated and bounded by the site's own page count (834 pages today, budget-asserted elsewhere) |
</threat_model>

<verification>
`cd site && npm run build && npm test && npm run validate` (chained, ONE command per the OneDrive rule) exits 0. `npm run check` reports exactly 4 errors / 0 warnings. No `<automated>` block in this plan pipes a gate command through `| tail`, `| head`, `; echo`, or `|| echo` (self-audited: the Task 2 verify block's `grep` reads a specific line from `npm run check`'s own output for a diagnostic match, it does not launder the exit code of `npm run build && npm test && npm run validate`, which precede it in the `&&` chain and must each exit 0 for the chain to reach the `check` step at all).
</verification>

<success_criteria>
- `facets.mjs` (still registered as validator #16 in `all.mjs` — unchanged registration, no new sibling file) passes 17 assertions against the real Phase 28 build.
- Assertion 17 concretely proves the F-27 bundling assumption against real `dist/_astro/` chunk contents, not prose.
- Every new assertion was manually demonstrated RED then restored GREEN (Operating Lesson 11).
- Full phase-close gate green: `npm run build && npm test && npm run validate`, `npm run check` at the unchanged 4-error/0-warning baseline.
- Python `pytest` suite is unaffected (no Python files touched in Phase 28) — confirmed unchanged at 344 passed / 1 skipped / 1 xfailed by inspection of git status (no `pipeline/**` or `data/**` diffs from this phase).
</success_criteria>

<output>
Create `.planning/phases/28-news-visualizer-ui/28-03-SUMMARY.md` when done
</output>
