---
phase: 36-classification-fidelity-attribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/lib/incidentHeadline.ts
  - site/src/lib/incidentHeadline.test.ts
  - site/src/pages/news.astro
  - site/src/pages/es/noticias.astro
  - site/src/components/CommuneNewsSection.astro
  - site/src/components/home/HomeNewsPulse.astro
  - site/src/components/map/IncidentPinLayer.ts
  - site/src/components/map/IncidentsList.tsx
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
autonomous: true
requirements: [FID-01]
executor: sonnet (display / i18n)

must_haves:
  truths:
    - "ES surfaces render title_src when it is present (the verbatim outlet headline). Legacy rows without title_src render title_es with a small 'Titular generado automáticamente' note (G-28 as proposed)."
    - "Every EN surface that shows title_en carries 'Machine-translated headline' (owner default, directive 'EN headline disclosure'). When title_en === title_src (the 36-04 kinship-guard fallback), EN shows the Spanish original with 'Original Spanish headline (not translated)'."
    - "One pure helper (site/src/lib/incidentHeadline.ts) decides text and label for all 6 surfaces: news.astro, es/noticias.astro, CommuneNewsSection.astro, HomeNewsPulse.astro, IncidentPinLayer.ts, IncidentsList.tsx. No surface re-implements the rule."
    - "The /news/ and /es/noticias/ intro paragraphs and the methodology pages (EN/ES) state the headline policy: Spanish verbatim from the outlet, English machine-translated and labelled."
    - "No new innerHTML/set:html path. Titles stay escaped (Astro text, escHtml, React text, textContent)."
  artifacts:
    - path: site/src/lib/incidentHeadline.ts
      provides: "headlineFor(incident, locale) → {text, kind}; HEADLINE_LABELS[locale][kind]"
      exports: ["headlineFor", "headlineLabel"]
  key_links:
    - from: site/src/pages/news.astro
      to: site/src/lib/incidentHeadline.ts
      via: "import { headlineFor, headlineLabel }"
      pattern: "incidentHeadline"
    - from: site/src/components/map/IncidentPinLayer.ts
      to: site/src/lib/incidentHeadline.ts
      via: "escHtml(headlineFor(incident, lang).text)"
      pattern: "headlineFor"
---

<objective>
FID-01 display side: show the outlet's verbatim headline on Spanish surfaces and a labelled machine translation on English surfaces, and disclose the policy.

This is build-time static HTML for pages. The two map islands and the home pulse enhancer are the existing client paths. The plan reads the optional `title_src` field introduced by 36-01. It is safe to ship before new rows exist: every current row is legacy.

Purpose:
- V-06: headlines are LLM rewrites presented as the outlet's, and the pages only say "classified by our automated pipeline" (news.astro:281).
- Owner decisions: verbatim headline policy; "Machine-translated headline" label on EN cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-classification-fidelity-attribution/36-RESEARCH.md
@.planning/phases/35-honest-freshness-signals/35-04-SUMMARY.md
@site/src/components/CommuneNewsSection.astro
@site/src/components/map/IncidentPinLayer.ts

<interfaces>
Evidence at HEAD 9e3abaf. Line numbers were re-read after Phase 35; the research file:line refs are stale.
- news.astro:
  - :55-57 Incident interface (title_es/title_en);
  - :277-283 intro paragraph ("…classified by our automated pipeline. Attribution is always provided…");
  - :447-454 card: `<h3 class="news-title">` holds the fam-dot, `<a href={safeUrl(incident.url)}>{incident.title_en}</a>` (:451-453) and news-sr.
  - facets.mjs constraints: no `display` rule on any `.news-card` selector; no literal {n}/{date}; toLocaleDateString with month needs timeZone 'UTC'.
- es/noticias.astro: :54-56 interface; :276-282 intro ("…se clasifican mediante pipeline automatizado…"); :450-452 card link text `{incident.title_es}`.
- CommuneNewsSection.astro:
  - :24-25 interface;
  - :95 hasForbiddenTerm;
  - :110 `safe = all.filter(inc => !hasForbiddenTerm(inc.title_es) && !hasForbiddenTerm(inc.title_en))`;
  - :170 `title = locale === 'es' ? incident.title_es : incident.title_en`;
  - :179-181 cnews-meta (outlet · date). Zero client JS (D-09).
- HomeNewsPulse.astro: :110 client Incident interface; :243-245 `title.textContent = locale === 'en' ? i.title_en || i.title_es : i.title_es`; :247-249 meta textContent. It is a processed Astro <script>: imports are allowed, {expr} interpolation is not (memory "Astro <script> no expr interpolation").
- IncidentPinLayer.ts: :28-40 Incident interface; :118 `const title = escHtml(lang === 'es' ? incident.title_es : incident.title_en)`; popup labels are inline lang ternaries (:123-125).
- IncidentsList.tsx: :8-12 interface; :37-39 `{lang === 'es' ? e.title_es : e.title_en}`.
- methodology.astro:262-271 is the "News-only crime category: sexuales" section. es/metodologia.astro:269-275 is its twin ("Categoría exclusiva de noticias: sexuales").
- The 36-01 contract: title_src?: string (verbatim), via_url?: string. For new rows title_es === title_src.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: incidentHeadline.ts pure helper + vitest</name>
  <files>site/src/lib/incidentHeadline.ts, site/src/lib/incidentHeadline.test.ts</files>
  <behavior>
    - es + title_src "Detienen a yerno de X" → {text: "Detienen a yerno de X", kind: "verbatim"}; headlineLabel(kind, 'es') === null.
    - es + no title_src → {text: title_es, kind: "legacy_generated"}; label es 'Titular generado automáticamente'.
    - en + title_src + title_en different → {text: title_en, kind: "machine_translated"}; label en 'Machine-translated headline'.
    - en + title_en === title_src (guard fallback) → {text: title_src, kind: "original_spanish"}; label en 'Original Spanish headline (not translated)'.
    - en + legacy row (no title_src) → {text: title_en || title_es, kind: "machine_translated"}.
    - Whitespace-only title_src counts as absent. Empty title_en falls back to title_src, then title_es.
    - HEADLINE_LABELS has both locales for every kind except verbatim: es machine_translated 'Titular traducido automáticamente', es original_spanish 'Titular original en español', en legacy_generated 'Automatically generated headline'.
  </behavior>
  <action>
Create the helper with the types `HeadlineKind = 'verbatim' | 'machine_translated' | 'original_spanish' | 'legacy_generated'` and a minimal input interface `{ title_src?: string | null; title_es: string; title_en: string }`, so all 6 surfaces can pass their own Incident shape.

Write the vitest from the behavior list.

The copy is decided here. It is neutral, attributes the outlet implicitly, and carries no safety verdict.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run src/lib/incidentHeadline.test.ts</automated>
    Expected: pass. Negative control: make es prefer title_es over title_src, and the verbatim test must FAIL. Revert.
  </verify>
  <done>A single tested rule for headline text and label per locale.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the helper into all 6 surfaces + forbidden-term filter on title_src</name>
  <files>site/src/pages/news.astro, site/src/pages/es/noticias.astro, site/src/components/CommuneNewsSection.astro, site/src/components/home/HomeNewsPulse.astro, site/src/components/map/IncidentPinLayer.ts, site/src/components/map/IncidentsList.tsx</files>
  <action>
Step 0: the Phase-35-closed precondition (see 36-01 Step 0). Execution mode: this is the only wave-1 plan that builds site/ (G-21(1) precedent), so run it alone.

In each surface, add `title_src?: string` (and `via_url?: string`) to its local Incident interface. Then replace the locale ternary with `headlineFor(incident, locale)`:
- news.astro:451-453 and es/noticias.astro:450-452: the link text becomes `{h.text}`. When headlineLabel(h.kind, locale) is non-null, add `<span class="news-headline-note">{label}</span>` inside h3.news-title after the `</a>` and before news-sr. Style it with font-size var(--text-label), a muted colour and margin-left. Put NO `display` property on any selector containing `.news-card` (facets.mjs constraint).
- CommuneNewsSection.astro:
  - at :110, filter on title_src as well: `!hasForbiddenTerm(inc.title_src ?? '')`;
  - at :170, use the helper;
  - append the label inside `.cnews-meta` as ` · {label}` when non-null.
- HomeNewsPulse.astro: in the processed <script>, import headlineFor/headlineLabel from '../../lib/incidentHeadline'. Set title.textContent = h.text. When the label is non-null, append a `<span class="pulse-note">` with textContent = label into the meta element. Add a :global(.pulse-note) style (LPX-1: client-created nodes). No {expr} inside <script>.
- IncidentPinLayer.ts:118: `const h = headlineFor(incident, lang)`, title = escHtml(h.text). When the label is non-null, add `<span class="pin-headline-note">${escHtml(label)}</span>` after the title in the popup markup.
- IncidentsList.tsx:37-39: render h.text, plus the label in a `<span className="event-note">` when non-null (React text).

Do not change safeUrl/safeHref, outlet or date rendering, or any facet or filter data-* attribute.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && echo "EN cards: $(grep -o 'class="news-card"' dist/news/index.html | wc -l) EN notes: $(grep -o 'class="news-headline-note"' dist/news/index.html | wc -l)" && echo "ES cards: $(grep -o 'class="news-card"' dist/es/noticias/index.html | wc -l) ES notes: $(grep -o 'class="news-headline-note"' dist/es/noticias/index.html | wc -l)" && [ "$(grep -o 'class="news-card"' dist/news/index.html | wc -l)" = "$(grep -o 'Machine-translated headline' dist/news/index.html | wc -l)" ] && ! grep -rn "set:html\|innerHTML" src/lib/incidentHeadline.ts && node scripts/validate/facets.mjs && node scripts/validate/forbidden-language.mjs && node scripts/validate/news-freshness.mjs && node scripts/validate/map.mjs && node scripts/validate/hreflang.mjs && npx vitest run && { npx astro check > .astro-check.log 2>&1; rc=$?; tail -3 .astro-check.log; [ $rc -eq 0 ]; }</automated>
    Expected, as ONE chained command (OneDrive):
    - With the current all-legacy data, the EN card count equals the "Machine-translated headline" count. The ES note count equals the ES card count, because every row is legacy and gets 'Titular generado automáticamente'. Record both numbers.
    - facets, forbidden-language, news-freshness, map and hreflang PASS. vitest is ≥ 184 + the new tests. astro check reports 0 errors.
    - The title_src rendering path is proven by Task 1's vitest and by the 36-09 live check once new rows exist.
  </verify>
  <done>All 6 surfaces use one rule. EN always carries a disclosure label, and ES legacy rows are labelled as generated.</done>
</task>

<task type="auto">
  <name>Task 3: Headline-policy disclosure on news intros + methodology (EN/ES)</name>
  <files>site/src/pages/news.astro, site/src/pages/es/noticias.astro, site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro</files>
  <action>
Append one sentence to each news intro paragraph:
- news.astro:277-283, after "classified by our automated pipeline.": "Spanish headlines are reproduced verbatim from each outlet; English headlines are machine translations and are labelled as such."
- es/noticias.astro:276-282: "Los titulares en español se reproducen textualmente desde cada medio; los titulares en inglés son traducciones automáticas y se indican como tales."

After the sexuales section, add a section with `id="news-headlines"` / `id="titulares-noticias"` to methodology.astro (:262-271) and es/metodologia.astro (:269-275):
- H2 "News headlines" / "Titulares de noticias".
- One paragraph:
  - EN: "Spanish headlines on the news layer are the outlet's own headline, reproduced verbatim. English headlines are machine translations of that headline and carry a 'Machine-translated headline' label. When an automatic check finds that a translation changes a family-relationship term, the original Spanish headline is shown instead. Items collected before this policy were given a headline written by our classifier; they are labelled 'automatically generated' and leave the news layer within 30 days."
  - ES: "Los titulares en español de la capa de noticias son el titular del propio medio, reproducido textualmente. Los titulares en inglés son traducciones automáticas de ese titular y llevan la etiqueta 'Machine-translated headline'. Cuando una verificación automática detecta que una traducción cambia un término de parentesco, se muestra en su lugar el titular original en español. Los ítems recolectados antes de esta política tienen un titular redactado por nuestro clasificador; se indican como 'generado automáticamente' y salen de la capa de noticias en un plazo de 30 días."
  - No absolute safety wording.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && grep -c 'id="news-headlines"' dist/methodology/index.html && grep -c 'id="titulares-noticias"' dist/es/metodologia/index.html && grep -c 'machine translations and are labelled' dist/news/index.html && grep -c 'traducciones automáticas y se indican' dist/es/noticias/index.html && node scripts/validate/forbidden-language.mjs && node scripts/validate/hreflang.mjs && node scripts/validate/cead-vintage.mjs && node scripts/validate/structure.mjs</automated>
    Expected: each grep prints 1, and the validators PASS.
  </verify>
  <done>The headline policy is disclosed where visitors read news, and in the methodology, in both locales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| current.json (third-party headlines) → static HTML / client popups | verbatim outlet text is now rendered as-is |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07 | Tampering / XSS | title_src rendering | mitigate | Only escaping paths are used: Astro text, escHtml (IncidentPinLayer), React text, textContent. A grep guard in verify rejects set:html/innerHTML in the helper. |
| T-36-08 | Repudiation / editorial | verbatim headline with a forbidden term | mitigate | CommuneNewsSection also filters title_src. The pipeline editorial filter (36-04, G-29 as proposed) rejects such items before storage. forbidden-language.mjs gates dist. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
From site/, as one command: `npm run build && npm run validate; npx vitest run`. Read each rc. The 18 validators PASS (freshness reflects live data, and red is real), vitest is ≥ 184 + new, and astro check reports 0 errors.
</verification>

<success_criteria>
ES shows verbatim headlines, or a labelled legacy headline. EN shows a labelled machine translation, or the labelled Spanish original. The policy is disclosed in both locales.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-03-SUMMARY.md` when done.
</output>
