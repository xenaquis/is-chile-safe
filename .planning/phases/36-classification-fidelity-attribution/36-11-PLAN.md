---
phase: 36-classification-fidelity-attribution
plan: 11
type: execute
wave: 7
depends_on: ["36-09", "36-10"]
files_modified:
  - .planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
autonomous: true
requirements: [FID-01, FID-02, FID-03, FID-04, FID-05, FID-06, FID-07]
executor: orchestrator (close gate, push, deploy polling, docs); fresh opus = served-route validator

must_haves:
  truths:
    - "Close gate at ONE HEAD (numbers declared):
      - pytest ≥ 642 passed / 1 skipped / 2 xfailed + every Phase-36 new test, exit 0;
      - vitest ≥ 184 + new, rc 0;
      - validators 19/19 PASS (18 + news-dedup #19), or 18/18 recorded as '18/18 (#19 not registered: PRUNE_EXISTING=False, G-30)' when 36-05/36-09 set PRUNE_EXISTING=False (premortem R-04); freshness is a real signal and is never excluded;
      - astro check 0 errors; lint-workflows exit 0; check-secret-hygiene exit 0;
      - check_hotfix_regression.py --data exit 0."
    - "Served-route verification on prod after the final deploy (curl, fresh opus validator):
      - FID-07: the 8 t59 ids and 63fb0c3ef0adca19 are absent from the served current.json; none of the 9 dropped URLs appears in /news/ or /es/noticias/; 514872e1d06db7f2's exact strings are present while it is in the window (else in archive/2026-09.json);
      - FID-05: the dedup validator gives 0 drops over the served current.json (under PRUNE_EXISTING=False: the forward-only `--only-new-since 36-S0-IDS.txt` check gives 0 drops and FID-05 is PARTIAL, forward-only, G-30, without blocking the other FIDs; premortem R-04). It is reported as 'rule-idempotent: 0 drops under the locked 0.82 (cut,date) rule (regression validator)', never as 'no duplicate cards' (premortem R-11);
      - FID-01: EN card count == 'Machine-translated headline' + 'Original Spanish headline' + 'Automatically generated headline' labels, and the 'Automatically generated headline' count == served rows without title_src (G-36, premortem R-01); ES cards for rows with title_src show the exact title_src text (10 random rows, HTML-unescaped match);
      - FID-04 (G-37): PASS iff (a) the live decode rate recorded in 36-LIVE-EVIDENCE.md (Σ attempts ≥ 20) is ≥ live_decode_floor of 36-GNEWS-DECODE.md AND (b) the news.google.com share over live-added ids — ids absent from S0 (36-S0-IDS.txt) AND not in 36-BACKFILL-IDS.txt — is ≤ 43% (n ≥ 20) on the served file; (a) failing because Google blocks runner decoding → PARTIAL (G-35(6)); live_decode_floor `n/a (decoder ineffective)` → PARTIAL 'decoder ineffective' (G-44). The backfill's Google share is reported separately and never gated (G-31, premortem R-02)."
    - "Every FID requirement is marked from recorded evidence only: PASS → [x]; Partial/FAILED → left [ ] with the measured numbers and a deferred-live entry. PLAUSIBLE never signs."
  artifacts:
    - path: .planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md
      provides: "gate numbers, served-route evidence, per-FID verdict with evidence pointers"
  key_links:
    - from: 36-VERIFICATION.md
      to: 36-EVAL-RESULTS.md / 36-FID03-AUDIT.md / 36-LIVE-EVIDENCE.md / 36-BACKFILL-AUDIT.md / 36-DEDUP-REVIEW.md / 36-GNEWS-DECODE.md
      via: "per-FID evidence table"
      pattern: "FID-0"
---

<objective>
Close Phase 36. Re-measure the full gate at one HEAD, verify every FID on the served routes, re-run the FID-07 regression after all pipeline and backfill changes, update the planning docs and the directive from evidence, and push.

Purpose: the directive's close-gate rule is ≥ baseline + new tests, with freshness red being a real signal. The validation role checks the served route, not only the harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/36-classification-fidelity-attribution/36-LIVE-EVIDENCE.md
@.planning/phases/36-classification-fidelity-attribution/36-EVAL-RESULTS.md
@.planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md
@.planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md
@.planning/phases/36-classification-fidelity-attribution/36-GNEWS-DECODE.md

<interfaces>
- Baseline (directive RUN STATUS, Phase-35 close): pytest 642/1/2, vitest 184, validators 18/18, astro check 0 err, lint 0, secret-hygiene 0.
- Directive hard rules: push only after a green gate plus `git pull --rebase`, never --force. deploy-manual.yml dispatch is authorized after a green gate when served data/pages changed. Curl prod after every deploy.
- Evidence files from 36-02..36-10, plus 36-S0-IDS.txt (36-09 Task 2) and 36-BACKFILL-IDS.txt (36-10 Task 3; absent on NO-PUBLICATION = empty set). G-28..G-35 are already in the directive, and G-36..G-42 (premortem R1: G-36 EN legacy label, G-37 FID-04 decode floor, G-38 contested v2 items, G-39 golden composition shortfall, G-40 36-10 timing, G-41 DEPS-03 baseline, G-42 FID-03 population and two-pass audit) are appended by the orchestrator with the R1 revision, and G-43..G-45 (gate R1: G-43 decode with a requests.Session, G-44 decode-floor lower bound, G-45 36-08 cap USD 0.90) with the R2 revision. G-33 (FID-03 outcome + Granite-era NO) is filled by 36-08.
- REQUIREMENTS.md FID lines :38-44 and traceability :99-105. ROADMAP.md Phase 36 detail :157-169 and list :121. The directive RUN STATUS table :8-18, decision log, and deferred-live :125-139.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close gate at one HEAD + FID-07 on data</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md</files>
  <action>
1. `git pull --rebase`.
2. At one HEAD (record the SHA):
   - `PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider; echo rc=$?`;
   - from site/, ONE command: `npm run build && npm run validate; echo validate=$?; npx vitest run; echo vitest=$?; npx astro check > .astro-check.log 2>&1; echo check=$?; tail -3 .astro-check.log`;
   - `bash .github/scripts/lint-workflows.sh; echo lint=$?`; `bash .github/scripts/check-secret-hygiene.sh; echo hyg=$?`;
   - `python pipeline/check_hotfix_regression.py --data data/incidents; echo fid07=$?`;
   - `{ if grep -q '^PRUNE_EXISTING: bool = False' pipeline/news/store.py; then python pipeline/validate_news_dedup.py data/incidents/current.json --only-new-since .planning/phases/36-classification-fidelity-attribution/36-S0-IDS.txt; else python pipeline/validate_news_dedup.py data/incidents/current.json; fi; }; echo dedup=$?` (forward-only under PRUNE_EXISTING=False, premortem R-04).
   Record validators as `validators: 19/19`, or as `validators: 18/18 (#19 not registered: PRUNE_EXISTING=False, G-30)` when #19 was never registered under the False branch.
3. Record the wall time and every number against the declared thresholds. A red freshness holds the phase (G-20(9) precedent: >24 h red → a Phase-34 gap plan).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && grep -nE "^close_sha: [0-9a-f]{7,40}" .planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md && grep -nE "^validators: (19/19|18/18 \(#19 not registered: PRUNE_EXISTING=False, G-30\))" .planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider && python pipeline/check_hotfix_regression.py --data data/incidents && { if grep -q '^PRUNE_EXISTING: bool = False' pipeline/news/store.py; then python pipeline/validate_news_dedup.py data/incidents/current.json --only-new-since .planning/phases/36-classification-fidelity-attribution/36-S0-IDS.txt; else python pipeline/validate_news_dedup.py data/incidents/current.json; fi; }</automated>
    Expected: all exit 0, and the recorded numbers meet the thresholds. Under PRUNE_EXISTING=False the 18/18 line and the forward-only dedup are accepted and FID-05 is PARTIAL; the other FIDs close on their own evidence.
  </verify>
  <done>The close gate is green at one SHA, with FID-07 and FID-05 re-checked after the backfill.</done>
</task>

<task type="auto">
  <name>Task 2: Push + deploy + served-route verification (fresh opus validator)</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md</files>
  <action>
1. If any file under pipeline/ or site/src changed since the 36-09 pre-push GO (`git diff --stat <ship_sha>..HEAD -- pipeline site/src`), a fresh-opus reviewer gives a new GO on that diff first (gate R1 NB-11). `git push` (never --force). If site/ changed since the last deploy, confirm the deploy-on-code run succeeded. Otherwise dispatch deploy-manual.yml if served data or pages changed. Wait ≥ 2 min.
2. Curl prod: /news/, /es/noticias/, /data/incidents/current.json, /methodology/ and /es/metodologia/, plus one commune page that has news (EN and ES).
3. Run the must_haves served-route checks:
   - check_hotfix_regression.py --html with --current = the served file;
   - validate_news_dedup.py over the served current.json (with `--only-new-since 36-S0-IDS.txt` under PRUNE_EXISTING=False, premortem R-04);
   - label counts: /news/ card count == MT + 'Original Spanish headline' + 'Automatically generated headline', and AG == served rows without title_src (G-36); 10 random title_src rows matched in /es/noticias/ (HTML-unescape the page text); record in the FID-01 row the known exception 514872e1d06db7f2 (a t59 manual retitle without title_src, so it carries 'Automatically generated headline') (gate R1 NB-13);
   - FID-04 (G-37, premortem R-02/R-03): (a) the live decode rate and Σ attempts from 36-LIVE-EVIDENCE.md vs live_decode_floor; (b) the news.google.com share over served rows whose id is absent from 36-S0-IDS.txt AND not in 36-BACKFILL-IDS.txt (n ≥ 20). Report the Google share of the backfill ids separately as `backfill_google_share: x/y (not gated, G-31)`.
4. A fresh opus validator re-derives each verdict from the curled artifacts and the evidence files (not from summaries) and signs PASS/FAIL per FID. The FID-01 row cites q4 = n_changes/n from 36-FID03-AUDIT and 36-BACKFILL-AUDIT (NB-17); the FID-02 row cites the effective G-39 quotas, the suicide n, per-category rates only with n ≥ 3 (NB-18), and the line 'FID-02 is in-sample by construction; out-of-sample evidence = 36-FID03-AUDIT and 36-BACKFILL-AUDIT' (premortem R-22); each gate verdict cites its Wilson 95 % interval and served_by_counts from the evidence files (premortem R-21).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && curl -s https://ischilesafe.com/news/ -o "$TEMP/36c-news.html" && curl -s https://ischilesafe.com/es/noticias/ -o "$TEMP/36c-noticias.html" && curl -s https://ischilesafe.com/data/incidents/current.json -o "$TEMP/36c-current.json" && python pipeline/check_hotfix_regression.py --html "$TEMP/36c-news.html" "$TEMP/36c-noticias.html" --current "$TEMP/36c-current.json" && { if grep -q '^PRUNE_EXISTING: bool = False' pipeline/news/store.py; then python pipeline/validate_news_dedup.py "$TEMP/36c-current.json" --only-new-since .planning/phases/36-classification-fidelity-attribution/36-S0-IDS.txt; else python pipeline/validate_news_dedup.py "$TEMP/36c-current.json"; fi; } && N=$(grep -o 'class="news-card"' "$TEMP/36c-news.html" | wc -l) && MT=$(grep -o 'Machine-translated headline' "$TEMP/36c-news.html" | wc -l) && OS=$(grep -o 'Original Spanish headline' "$TEMP/36c-news.html" | wc -l) && AG=$(grep -o 'Automatically generated headline' "$TEMP/36c-news.html" | wc -l) && LEG=$(python -c "import json,sys; print(sum(1 for i in json.load(open(sys.argv[1],encoding='utf-8'))['incidents'] if not (i.get('title_src') or '').strip()))" "$TEMP/36c-current.json") && echo "cards=$N MT=$MT OS=$OS AG=$AG legacy_rows=$LEG" && [ "$N" -eq "$((MT+OS+AG))" ] && [ "$AG" -eq "$LEG" ] && [ "$(grep -cE '^FID-0[1-7]: (PASS|FAIL|PARTIAL)' .planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md)" -eq 7 ] && grep -nE "^backfill_google_share: " .planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md && ! grep -n "no duplicate cards" .planning/phases/36-classification-fidelity-attribution/36-VERIFICATION.md</automated>
    Expected: the FID-07 and dedup checks exit 0 on prod (dedup forward-only under PRUNE_EXISTING=False), the EN card count equals MT + OS + AG and AG equals the served rows without title_src (NB-15), there are exactly 7 FID verdict lines, the backfill Google share is reported (ungated; "0/0" on NO-PUBLICATION), and the verification never claims "no duplicate cards".
  </verify>
  <done>Every FID is verified on the served routes after the final deploy.</done>
</task>

<task type="auto">
  <name>Task 3: Docs from evidence + directive update + final push</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/v2.2-AUTONOMOUS-DIRECTIVE.md</files>
  <action>
- REQUIREMENTS.md: mark [x] only the FIDs whose 36-VERIFICATION verdict is PASS, and update traceability (:99-105) with the Complete / Partial (reason) status.
- FID-05 wording (premortem R-11), in 36-VERIFICATION.md, the REQUIREMENTS.md traceability/status note and STATE.md: "rule-idempotent: 0 drops under the locked 0.82 (cut,date) rule (regression validator)". Never write "no duplicate cards"; the informational near-duplicate counts of 36-LIVE-EVIDENCE.md (near_dup_pairs_info) are cited next to it.
- ROADMAP.md: mark Phase 36 [x] in the list only if all 7 FIDs pass, or with the explicit Partial note used for Phase 34. Set **Plans** to "11 plans (36-01..36-11) — complete <date>".
- STATE.md: current position.
- Directive:
  - RUN STATUS: next action, phases, git SHA, spend (USD 0.53 + the Phase-36 ledgers);
  - G-28..G-35, G-36..G-42 and G-43..G-45 are already recorded; add outcome numbers where they apply and fill the reserved G-33 (FID-03 outcome + Granite-era NO) if 36-08 has not;
  - deferred-live: if FID-02/FID-03 passed, reword "Live precision (G-18)" (do not remove it) as 'fixed forward from <ship date>; cards dated 2026-09-23..<ship date> keep the old-prompt rate and age out by <ship date + 30 d>' (gate R1 NB-16); update "Backfill not published" per the 36-10 decision, with the in-window / archived counts and the date the last backfill row leaves /news/ (2026-10-23 for rows dated 2026-09-22) (gate R1 o3);
  - deferred-live, Phase 38 (gate R1 NB-19): "R2 APA citations of legacy rows use the LLM title_es (archive_r2.py:98-106, :240-246)";
  - deferred-live FRESH-04 7-day evidence (premortem I-09 discard condition): mark which news runs are post-36-ship (headSha contains ship_sha); the first post-ship run prunes and counts in C and D, it is not a no-op; keep "Granite-era re-classification"; remove "19 OTHER_DEFECT live items → Phase 36 input" with a note pointing to where they were used (golden v3 categories; 18 age out of the window by 2026-10-05; aef6f3680c904f4c, dated 2026-09-23, is not edited, per the data-mutation rule);
  - deferred-live / Phase-37 hand-off, add (premortem R-18, G-41): "DEPS-03 compares against 36-DEPS03-BASELINE.json (prompt at ship SHA), not 34-AB-RESULTS.json".
- Final report: list the 36-09/36-10 pushes and the backfill publication as 'decided by the orchestrator, reviewable by the owner' (premortem R-23).
- Memory: update the v2.2-milestone entry.
- Commit `docs(36): close phase — verification + traceability`, then `git pull --rebase`, `git push`.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && grep -n "G-34" .planning/v2.2-AUTONOMOUS-DIRECTIVE.md && grep -n "36-DEPS03-BASELINE.json" .planning/v2.2-AUTONOMOUS-DIRECTIVE.md && ! grep -n "no duplicate cards" .planning/STATE.md && grep -n "Phase 36" .planning/ROADMAP.md | head -3 && git fetch -q origin && git status -sb | head -1 && ! git status -sb | head -1 | grep -q ahead</automated>
    Expected: G-34 is logged, the DEPS-03 baseline hand-off is in the directive, STATE.md does not claim "no duplicate cards", ROADMAP is updated, and the branch is in sync with origin (no "ahead").
  </verify>
  <done>Phase 36 is closed from evidence, pushed, and the directive, requirements and roadmap reflect measured outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local master → prod | final release of the phase |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-33 | Repudiation | overstating completion | mitigate | Verdicts come from recorded evidence plus a fresh-opus served-route re-derivation. Partial stays unchecked with numbers. |
| T-36-34 | Tampering | push race with the news cron | mitigate | `git pull --rebase` before every push, never --force. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
The gate numbers are ≥ declared, the served-route checks pass, 7 FID verdict lines are present, and the docs are synced and pushed.
</verification>

<success_criteria>
Phase 36 closes at ≥ baseline + new tests, with every FID verified on prod from evidence and the hotfixed cards intact after the pipeline and backfill changes.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-11-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-01 | must_haves FID-01 served check: three-label sum + AG == served rows without title_src (G-36); Task 2 step 3 label counts |
| R-02 | must_haves FID-04: live-added = absent from 36-S0-IDS.txt AND not in 36-BACKFILL-IDS.txt; backfill Google share reported, ungated; interfaces; Task 2 step 3 + verify (`backfill_google_share:` line) |
| R-03 | must_haves FID-04 (G-37 (a) decode floor + (b) share); context (+36-GNEWS-DECODE.md); Task 2 step 3 |
| R-04 | must_haves validators (19/19 or the 18/18 line) and FID-05 forward-only PARTIAL; Task 1 step 2 dedup branch + validators line; Task 1 verify (regex accepts the 18/18 line, dedup branches on PRUNE_EXISTING); Task 2 step 3 + verify (served dedup branch) |
| R-11 | must_haves FID-05 wording; Task 3 FID-05 wording in VERIFICATION / REQUIREMENTS / STATE; Task 2 and Task 3 verify reject "no duplicate cards" |
| R-18 | Task 3 deferred-live / Phase-37 hand-off line for DEPS-03 (G-41); verify greps it |

Also updated (twin of the new knobs): interfaces and Task 3 now say G-28..G-35 and G-36..G-41 are already recorded, instead of "append G-28..G-34".

## Revision R2 (gate R1)

| id | change |
|---|---|
| G-44 twin (R-03 lower bound) | must_haves FID-04: floor n/a → PARTIAL 'decoder ineffective' |
| NB-12 | interfaces and Task 3 name G-36..G-42 (G-42 was omitted) plus G-43..G-45; Task 2 verify requires exactly 7 FID lines (`-eq 7`); Task 3 verify fails when the branch is ahead |
| NB-15 (36-11 part) | Task 2 verify asserts EN cards == MT + OS + AG and AG == served rows without title_src (block reused from 36-09 Task 2 verify); Expected. Not applied: an assertion on the commune page (no expected value given) |
| NB-13 | Task 2 step 3: 514872e1d06db7f2 recorded as the known FID-01 exception |
| NB-17 / NB-18 | Task 2 step 4: FID-01 cites q4; FID-02 cites effective G-39 quotas, suicide n, rates only with n ≥ 3 |
| R-21 / R-22 (prev. edits) | Task 2 step 4: Wilson 95 % interval + served_by_counts cited; FID-02 in-sample line |
| NB-11 | Task 2 step 1: new fresh-opus GO when pipeline/ or site/src changed after the 36-09 GO |
| NB-16 | Task 3: 'Live precision (G-18)' reworded, not removed |
| o3 | Task 3: in-window / archived counts and last-row exit date (2026-10-23) in the backfill entry |
| NB-19 | Task 3: Phase-38 deferred-live line for legacy R2 APA citations |
| I-09 (prev. edit) | Task 3: FRESH-04 7-day evidence marks post-36-ship runs |
| R-23 (prev. edit) | Task 3: final report lists orchestrator-decided pushes/backfill as reviewable by the owner |
