---
phase: 36-classification-fidelity-attribution
plan: 05
type: execute
wave: 3
depends_on: ["36-01", "36-04"]
files_modified:
  - pipeline/news/dedup.py
  - pipeline/tests/test_dedup.py
  - pipeline/news/store.py
  - pipeline/tests/test_store.py
  - pipeline/validate_news_dedup.py
  - pipeline/tests/test_validate_news_dedup.py
  - site/scripts/validate/news-dedup.mjs
  - .planning/phases/36-classification-fidelity-attribution/36-DEDUP-REVIEW.md
autonomous: true
requirements: [FID-05, FID-04]
executor: opus (dedup kernel + store.merge_and_write contract); fresh opus = drop-list reviewer (Task 3)

must_haves:
  truths:
    - "merge_and_write applies the SAME deterministic rule (dedup.py: canonical URL, then (cut, date) bucket, SequenceMatcher ratio ≥ 0.82 on normalized title_es) over existing current.json + new incidents, in stored order: the first-seen item wins. No LLM and no new threshold (D-10; v2.1 lock)."
    - "After one merge, applying dedup.deduplicate over the written current.json yields 0 drops (FID-05). New items that are near-duplicates of an existing archive-month item are not added. Existing archive entries are never pruned."
    - "URL identity covers both fields. An item whose url OR via_url canonicalizes to an already-kept url or via_url is a duplicate (FID-04 'dedup keys on both')."
    - "The prune of EXISTING current.json rows ships only if a fresh-opus review of the pre-ship drop list finds 0 false merges and 514872e1d06db7f2 is not in it (G-30 as proposed). Otherwise PRUNE_EXISTING = False (forward-only), FID-05 is Partial and an owner question is written. The forward-only branch is checkable before the legacy pairs age out: `validate_news_dedup.py FILE --only-new-since IDS_FILE` counts only pairs where at least one row is not in IDS_FILE (36-09/36-10/36-11 use it with 36-S0-IDS.txt; premortem R-04)."
    - "validate_news_dedup.py (stdlib + pipeline.news.dedup only) and site/scripts/validate/news-dedup.mjs exist and are tested. The .mjs is NOT registered in all.mjs until 36-09 has observed a post-ship live run at 0 drops."
    - "FRESH-04 byte-identity still holds: a no-op run over a duplicate-free current.json writes nothing. No test_fresh04_* assert changes; only the bump002 fixture title of test_fresh04_new_incident_rewrites_and_bumps changes (gate R1 BF-06)."
  artifacts:
    - path: pipeline/news/dedup.py
      provides: "prune_near_duplicates(items) -> (kept, dropped_pairs); filter_new_against(existing, new) -> (kept_new, dropped_pairs); via_url alias"
    - path: pipeline/validate_news_dedup.py
      provides: "CLI: drops=N, exit 1 if N>0; --list; --only-new-since IDS_FILE (forward-only count, R-04)"
    - path: site/scripts/validate/news-dedup.mjs
      provides: "validator #19 (registered in 36-09)"
  key_links:
    - from: pipeline/news/store.py merge_and_write
      to: pipeline/news/dedup.py prune_near_duplicates
      via: "after _merge_by_id, before partition"
      pattern: "prune_near_duplicates"
    - from: site/scripts/validate/news-dedup.mjs
      to: pipeline/validate_news_dedup.py
      via: "spawnSync python3|python over site/public/data/incidents/current.json"
      pattern: "validate_news_dedup.py"
---

<objective>
FID-05: make the existing deterministic 0.82 (cut, date) dedup rule run against existing incidents, so duplicate cards stop accumulating across runs, and enforce "0 drops over current.json" with a validator. It also delivers the FID-04 dedup half: url and via_url are one identity.

Purpose:
- V-11: scrape_news.py:563 dedups only the run's own batch, and store._merge_by_id merges by id only (store.py:119-127).
- Measured 2026-09-24 (current.json generated 2026-09-24T21:06:59Z): dedup.deduplicate over current.json drops 64 of 798 (baseline in V-11: 73/807). 58 of those are Granite-era and 6 are dated ≥ 2026-09-23.
- 514872e1d06db7f2 (the t59-retitled card) is alone in its (9102, 2026-09-03) bucket and is not dropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/36-classification-fidelity-attribution/36-RESEARCH.md
@pipeline/news/dedup.py
@pipeline/news/store.py
@pipeline/tests/test_dedup.py

<interfaces>
Evidence at HEAD 9e3abaf:
- dedup.py:
  - :21 `_THRESHOLD = 0.82`; :29-37 `_canonical_url` (strips utm_*); :40-49 normalize_title; :52-57 are_duplicates;
  - :65-106 deduplicate(incidents): phase 1 canonical URL (:76-83), phase 2 bucket (cut, date) on normalize_title(title_es) (:87-104), keep-first. Pure stdlib, no LLM (D-10).
- store.py merge_and_write (36-01/36-04 may shift lines; re-grep):
  - :182-185 load existing and previous_payload; :191 `combined = _merge_by_id(existing, new_incidents)`;
  - :194-206 partition; :214-237 archive loop (existing_archive / merged_archive, skip when no new id);
  - :240 new_in_window_count; :263-272 no-op guard.
- backfill_classifier_outage.py:697 `survivors = dedup.deduplicate(existing + fresh)` (existing first). It stays valid.
- Title field: title_es. Since 36-04, title_es == title_src (verbatim) for new rows, and legacy rows carry the LLM headline. Pitfall 5 (research): the threshold's behaviour on verbatim titles is unverified, so Task 3 re-measures on the data that exists at execution time.
- CI: ci.yml runs only on pull_request/workflow_dispatch (ci.yml:3-5), and validators run at local close gates. The Cloudflare build is `npm run build` without validate (site/package.json:11). An unregistered validator therefore cannot break a deploy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dedup.py prune/filter APIs + via_url alias; store.merge_and_write cross-run dedup</name>
  <files>pipeline/news/dedup.py, pipeline/tests/test_dedup.py, pipeline/news/store.py, pipeline/tests/test_store.py</files>
  <behavior>
    - prune_near_duplicates([A, B_near_dup_of_A, C]) → kept [A, C], dropped [(B, A.id, ratio)]. It is order-stable, and deduplicate(x) == prune_near_duplicates(x)[0] for every existing test input. The existing test_dedup.py tests pass unedited.
    - URL alias: A{url: P}, B{url: G, via_url: P} → B dropped (reason "url"). A{url: G}, B{url: P, via_url: G} → B dropped.
    - filter_new_against(existing=[A, A2_dup_of_A], new=[N_dup_of_A, M]) → kept_new [M]. The existing list is never modified.
    - merge_and_write, with current.json holding [A, B_dup_of_A] and new []: the written incidents are [A]. last_new_incident_at is carried, not bumped, and the return value is 0.
    - merge_and_write with new [N_dup_of_existing_A]: N is not written, the file stays byte-identical (no-op guard) when nothing else changed, and last_new_incident_at is not bumped.
    - An aged new item that duplicates an archive-month item is not added to the archive. Existing archive duplicates are untouched.
    - PRUNE_EXISTING=False (monkeypatched): the existing [A, B] pair stays and only new items are filtered.
    - Exactly one existing test_store.py test changes its fixture (gate R1 BF-06): test_fresh04_new_incident_rewrites_and_bumps (test_store.py:509-526). Its bump002 incident gets a title_es clearly different from bump001's (SequenceMatcher ratio on normalize_title < 0.82; the shared _make_incident fixture at :21-33 yields 'Incidente bump001' / 'Incidente bump002', same cut and date, which the cross-run prune now drops), with the comment 'FID-05 36-05: cross-run dedup'. Its asserts are not touched. Every other existing test_store.py test (including the FRESH-04 byte-identity ones) passes unedited.
    - New test: a cross-run near-duplicate of an existing current.json row → merge_and_write returns 0, last_new_incident_at is not updated, and current.json is byte-identical (gate R1 BF-06).
  </behavior>
  <action>
dedup.py:
- Refactor the body of deduplicate() into `prune_near_duplicates(items) -> tuple[list[dict], list[tuple[dict, str, str]]]`, where each dropped entry is (item, kept_id, reason = "url" | f"title:{ratio:.3f}").
- Make deduplicate() a thin wrapper (behaviour identical).
- Phase 1 URL identity: the key set = {canonical(url)} ∪ {canonical(via_url)} if via_url. An item is a duplicate when its key set intersects the seen keys; on keep, add all of its keys.
- Add `filter_new_against(existing, new)`: it seeds the bucket titles and URL keys from existing (never dropping existing), then filters new.
- Threshold and normalization are unchanged. Docstring: "FID-05: cross-run, deterministic; no LLM clustering (v2.1 lock)".

store.py:
- Add the module constant `PRUNE_EXISTING: bool = True` (G-30 as proposed; Task 3 may flip it).
- In merge_and_write, after `combined = _merge_by_id(existing, new_incidents)`:
  - if PRUNE_EXISTING: `combined, dropped = dedup.prune_near_duplicates(combined)`;
  - else: `kept_new, dropped = dedup.filter_new_against(existing, [i for i in combined if i["id"] not in existing_ids])` and rebuild combined = existing + kept_new.
  - Log `dedup cross-run: dropped N (existing M, new K)` at INFO, with the dropped ids at DEBUG.
- In the archive loop, replace `_merge_by_id(existing_archive, aged_incidents)` input with `filter_new_against(existing_archive, [a for a in aged_incidents if a["id"] not in {e["id"] for e in existing_archive}])[0]`, so existing archive rows stay untouched.
- The partition, the G-05 last_new rule and the no-op guard stay as they are. The new_in_window_count semantics naturally exclude dropped new items.
- Do NOT change the implementation (threshold, normalization, prune order or the no-op guard) to keep test_fresh04_new_incident_rewrites_and_bumps passing with its old fixture; only its bump002 title changes (gate R1 BF-06).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_dedup.py pipeline/tests/test_store.py pipeline/tests/test_backfill_classifier_outage.py pipeline/tests/test_scrape_news.py -q -p no:cacheprovider</automated>
    Expected: exit 0. Negative control: skip the prune call, and the "[A, B] → [A]" test must FAIL. Revert.
  </verify>
  <done>Cross-run dedup runs inside merge_and_write for every writer (live, backfill). It is deterministic, first-seen wins, and FRESH-04 is intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validate_news_dedup.py + news-dedup.mjs (validator #19, not yet registered)</name>
  <files>pipeline/validate_news_dedup.py, pipeline/tests/test_validate_news_dedup.py, site/scripts/validate/news-dedup.mjs</files>
  <behavior>
    - `python pipeline/validate_news_dedup.py FILE` on a duplicate-free fixture prints "news-dedup: PASS — 0 drops over N incidents" and exits 0.
    - A fixture with one title near-duplicate pair prints "drops=1" and one "DROP <id> ~ KEEP <id> (title:0.9xx)" line, and exits 1. A missing or malformed file prints ::error:: and exits 2.
    - `--list` prints every pair even on PASS (none).
    - `--only-new-since IDS_FILE` (premortem R-04; IDS_FILE = one incident id per line, blank lines and `#` comments ignored): only pairs where at least one of (DROP, KEEP) has an id not in IDS_FILE are counted and printed. Fixture: a legacy pair [A, B] with both ids in IDS_FILE plus a new C with no duplicate → "drops=0", exit 0; the same fixture plus a new D near-duplicate of A → drops=1, exit 1. A missing IDS_FILE → ::error::, exit 2. Without the flag the behaviour is unchanged.
    - The module imports only stdlib + pipeline.news.dedup (so a bare python3 without pipeline deps can run it). Pinned by a subprocess test: after importing the module, `'pydantic' not in sys.modules`.
    - `node site/scripts/validate/news-dedup.mjs` with NEWS_DEDUP_CURRENT_JSON set to a fixture exits the same code as the Python script. Skip the test if node is absent.
  </behavior>
  <action>
validate_news_dedup.py: load the file, call dedup.prune_near_duplicates on data["incidents"], print, and exit. Put the repo root on sys.path as scrape_news.py:60-61 does. With `--only-new-since IDS_FILE`, filter the dropped pairs to those where the dropped id or the kept id is absent from IDS_FILE before counting (the forward-only check that the PRUNE_EXISTING=False branch of 36-09/36-10/36-11 relies on, premortem R-04). Print the mode in the PASS/FAIL line ("forward-only since <N> ids").

news-dedup.mjs:
- Resolve the payload path from env NEWS_DEDUP_CURRENT_JSON, else `site/public/data/incidents/current.json` (the prebuild sync-data copy, same source the pages read; memory "News page build path drift" → resolve from the script dir, not cwd).
- spawnSync `python3`, falling back to `python` on ENOENT, with [repoRoot/pipeline/validate_news_dedup.py, path] and stdio inherit. Exit with the child status. With no interpreter, print a FAIL message and exit 1.
- Header comment: "19. news-dedup.mjs — FID-05 0 drops over current.json (single rule source: pipeline/news/dedup.py; no JS re-implementation)".

Do NOT add it to all.mjs (36-09 Task 3 registers it after the live prune).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_validate_news_dedup.py -q -p no:cacheprovider && python pipeline/validate_news_dedup.py data/incidents/current.json --list > "$TEMP/36-dedup-pre.txt"; echo "rc=$? (expected 1 pre-ship)"; head -3 "$TEMP/36-dedup-pre.txt"; ! grep -n "news-dedup" site/scripts/validate/all.mjs</automated>
    Expected:
    - pytest exits 0.
    - The script over the real current.json exits 1 with drops = the count measured at execution time. Compare it with the 64 measured 2026-09-24 and record it; it may differ because the cron keeps adding rows.
    - news-dedup is not yet in all.mjs.
  </verify>
  <done>The FID-05 validator exists with a single rule source. The pre-ship drop count is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Drop-list precision review (fresh opus) → PRUNE_EXISTING decision</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-DEDUP-REVIEW.md, pipeline/news/store.py</files>
  <action>
Pre-declared rule (G-30 as proposed): the existing-row prune ships iff (a) the fresh-opus reviewer judges 0 of the listed (DROP, KEEP) pairs to be different events (0 false merges, the v2.1 clustering lock standard), and (b) 514872e1d06db7f2 is not a DROP.

1. Generate the list with `python pipeline/validate_news_dedup.py data/incidents/current.json --list` at execution time. For each pair, attach both rows' title_es, title_en, outlet, url, cut and date from current.json.
2. A FRESH opus reviewer (no knowledge of the rule's origin or the threshold) answers per pair {same_event: bool, reason}.
3. Write 36-DEDUP-REVIEW.md with the pair table, the counts and the decision.
4. If the rule fails: set `PRUNE_EXISTING = False` in store.py with a comment citing 36-DEDUP-REVIEW.md. Forward-only dedup still applies. FID-05 becomes Partial, because current.json will not reach 0 drops until the legacy duplicates age out (≤ 30 days; the last current pair, dated 2026-09-24, leaves ~2026-10-24). Write the owner question into the SUMMARY for the directive's deferred-live list. The downstream plans already carry this branch (premortem R-04): 36-09 evaluates FID-05_live with `--only-new-since 36-S0-IDS.txt` and does not register #19; 36-10 gates on validators 18/18 plus the same forward-only check; 36-11 accepts `validators: 18/18 (#19 not registered: PRUNE_EXISTING=False, G-30)`. FID-05 stays PARTIAL without blocking the other FIDs.
5. If the rule passes, leave True.

In both cases, record the expected post-ship first-run drop count for 36-09's live check.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && test -s .planning/phases/36-classification-fidelity-attribution/36-DEDUP-REVIEW.md && grep -E "^PRUNE_EXISTING: bool = (True|False)" pipeline/news/store.py && grep -c "514872e1d06db7f2" .planning/phases/36-classification-fidelity-attribution/36-DEDUP-REVIEW.md && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider; rc=$?; echo "pytest rc=$rc"; [ $rc -eq 0 ]</automated>
    Expected: the review file exists and states the 514872e1d06db7f2 check. PRUNE_EXISTING matches the recorded decision. The full pytest rc is 0, at ≥ 642/1/2 + new.
  </verify>
  <done>The one-time public removal of duplicate cards is backed by a pre-declared 0-false-merge review, or it is disabled with an owner question.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pipeline run → published current.json | cards removed or not added by an automatic rule |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-12 | Tampering / integrity | false merge hides a distinct incident | mitigate | The existing rule and threshold are reused (no new heuristic). A 0-false-merge review is required before the existing-row prune ships, with a PRUNE_EXISTING kill switch. Removals are logged per run, and git history keeps every removed row. |
| T-36-13 | Repudiation | FID-07 hotfixed card displaced by a wrong-kinship sibling | mitigate | 514872e1d06db7f2 must not be a DROP (Task 3). The 36-09 FID-07 script re-checks after the ship. |
| T-36-14 | Elevation | validator spawning python | accept | It runs a repo-local script with fixed arguments, only in local gates and PR CI, never in deploy. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
- The full pytest run is green (exit code).
- The pre-ship drop count and the review decision are recorded.
- The validator is unregistered (grep).
</verification>

<success_criteria>
Deterministic cross-run dedup lives inside merge_and_write, with url/via_url identity. The validator is ready, and the existing-row prune is gated by a recorded precision review.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-05-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-04 | must_haves truth (forward-only check); artifact provides (+--only-new-since); Task 2 behavior (--only-new-since fixture tests) + action (filter pairs by IDS_FILE); Task 3 step 4 (names the downstream False branch and the ~2026-10-24 age-out) |

Twins fixed: 36-09 (must_haves FID-05 + #19, Task 2 step 5 writes 36-S0-IDS.txt, Task 3 steps + verify), 36-10 (must_haves, Task 3 steps 2/4 + verify), 36-11 (must_haves validators/FID-05, Task 1/2 commands + verify). R-12 (ship-time re-review of the drop list) lives in 36-09 Task 2 step 3b.

## Revision R2 (gate R1)

| id | change |
|---|---|
| BF-06 | Task 1 behavior: 'every existing test_store.py test passes unedited' replaced by the single declared fixture change (bump002 title distinct, ratio < 0.82, comment 'FID-05 36-05: cross-run dedup', asserts untouched; read test_store.py:18-33 and :509-526, store.py:191) + new test (cross-run near-duplicate → return 0, last_new unchanged, byte-identical current.json); Task 1 action forbids changing the implementation to dodge it; must_haves FRESH-04 truth notes the fixture-only change |
| NB-08 | Task 3 verify: the chain's exit code now fails the verify |
