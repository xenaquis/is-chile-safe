---
phase: 36-classification-fidelity-attribution
plan: 08
type: execute
wave: 4
depends_on: ["36-06"]
files_modified:
  - pipeline/experiments/fid03_audit.py
  - pipeline/tests/test_fid03_audit.py
  - .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md
autonomous: true
requirements: [FID-03]
executor: sonnet (audit tooling, Task 1); orchestrator = selection + classification run (Task 2); fresh opus = blind reviewer (Task 3)

must_haves:
  truths:
    - "FID-03 population (G-32 as proposed): every candidate row with stored inputs (data/incidents/rejected/*.json) whose parsed first_seen falls in the 14 days before the audit start A, minus golden_set_v3 source_ids, minus event-siblings of v3 items (rows whose SequenceMatcher ratio over dedup.normalize_title(title) is ≥ 0.60 against any v3 headline; the excluded list is recorded). A = now; if the would-publish pool is < 50, A moves back 1 day at a time (the 14-day window is kept) until ≥ 50, and the final A is recorded (premortem R-09). This is replayed through the PRODUCTION classify path (build_router_from_env → preflight → router.classify, via backfill.run_classify) with the Phase-36 prompt, and written to a cache OUTSIDE data/."
    - "Sample = 50 'would-publish' items (OK ∧ commune resolvable ∧ centroid found ∧ not editorial_filter), drawn with random.Random(3608) from the replayed pool."
    - "Pass (declared before classification): agreement = q1 (a crime incident in Chile) ∧ q3 (family correct) ≥ 43/50 (ceil(0.85·50); owner-default FID-03 threshold ≥ 85%). 'vida is no longer a catch-all' passes iff, among sampled items predicted vida, the count whose correct family is vida is ≥ ceil(0.85·n_vida). The vida share of the would-publish pool is reported with references 51% (V-07 pre-Granite) and 51.4% (G-18), not gated."
    - "The audit is two-pass (premortem R-10): pass 1 shows the reviewer only title, description, url (plus the decoded publisher_url for news.google.com rows) and outlet, and fixes q1 plus the reviewer's own commune and family; pass 2 shows the prediction for q2 and q4. q3 = pass-1 family == predicted family. Every verdict carries basis = source | headline_only, and agreement is also reported per basis (gate unchanged)."
    - "Granite-era re-classification (2026-07-27..09-04) = NO in this milestone (owner default, directive 'Re-classify Granite-era published incidents'), recorded as a G-NN and kept on the deferred-live list."
    - "Spend ≤ plan cap USD 0.90 (G-45, gate R1 BF-07; estimate: pool ≈ 1,150 rows × $0.00025 ≈ $0.29 per classify pass; a failed first audit re-classifies the pool into a NEW cache, ≈ $0.58 in total plus back-off)."
  artifacts:
    - path: pipeline/experiments/fid03_audit.py
      provides: "--select / --classify / --sample / --score"
    - path: .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md
      provides: "final A, pool size and epoch composition, sibling exclusions, outcome split, 50 two-pass verdicts, agreement (overall and by basis), vida precision, decision"
  key_links:
    - from: pipeline/experiments/fid03_audit.py --classify
      to: pipeline/backfill_classifier_outage.py run_classify / SpendMeter
      via: "function-local import (R-12: classifier imported after env load)"
      pattern: "run_classify"
---

<objective>
FID-03: a 50-item hand audit of a fresh 2-week sample under the Phase-36 classifier. It must meet the ≥ 85% family-agreement default and show that vida is no longer a catch-all. This is the out-of-sample check on 36-06's prompt: the golden-set source ids are excluded.

Purpose:
- V-07: vida rose to 68% in the Granite era.
- G-18: the live G-16 model on the old prompt scored q1 40/50 and q3 38/50, with 7 of 10 non-crimes labelled vida.
- Orchestrator correction in 36-RESEARCH.md:368: vida must be measured on a post-change sample, not on current.json as a whole.
- Why a replay rather than live incidents: accepted live incidents store no description, only the URL and LLM titles (schema.py:48-61). Only rejected/ keeps classifier inputs (scrape_news.py:113-122). The pool is therefore the stored candidate stream. It omits the items live accepted after 2026-09-23 (no stored input), which biases the sample toward harder, previously rejected or unjudged items. That makes the bias conservative. Stated in the report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/36-classification-fidelity-attribution/36-06-SUMMARY.md
@.planning/phases/36-classification-fidelity-attribution/36-GOLDEN-V3.md
@.planning/phases/34-news-classification-restore/34-BACKFILL-AUDIT.md
@pipeline/backfill_classifier_outage.py

<interfaces>
Evidence at HEAD 9e3abaf:
- backfill_classifier_outage.py:
  - :136 parse_dt; :212 `_assert_outside(path, data_dir, flag)`; :312 load_cache;
  - :356-500 SpendMeter (OpenRouter /key usage delta + estimates; `cap`, `would_exceed`, `charge`, `persist`);
  - :505-566 run_classify(rows, cache_path, router, meter, limit=None). It needs rows {id, title, description}. Since 36-04 it passes strip_html(description). Exit codes: EXIT_CAP 3, EXIT_EXHAUSTED 4.
  - :808 `_ok_items(rows, cache)`; :994-1041 main loads the .env files before any classifier import (R-12).
- rejected/*.json rows: {id, url, outlet, date, title, description, rejection_stage, first_seen}. 2026-09-24: 4,427 rows; stages classifier_none 4,170 (includes the G-18 2,122), low_confidence 113, resolver_fail 90, commune_null 54; 114 rows with first_seen ≥ 2026-09-23T03:20 (live model: low_confidence 113, commune_null 1).
- The G-11 verdict format (34-BACKFILL-AUDIT.md "Per-item verdicts"): q1 crime incident in Chile, q2 commune as in source, q3 family correct, q4 headline adds facts, plus correct family and reason.
- Would-publish mirrors scrape_news `_answered`: resolve_cut → get_centroid → editorial_filter (36-04 fidelity.forbidden_term on source_headline/title_en).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fid03_audit.py (select / classify / sample / score)</name>
  <files>pipeline/experiments/fid03_audit.py, pipeline/tests/test_fid03_audit.py</files>
  <behavior>
    - --select --as-of 2026-10-01T00:00:00Z --window-days 14 --exclude-ids-file F --out-rows R: rows from every rejected/YYYY-MM.json covering the window, with parse_dt(first_seen) in [as_of−14d, as_of), deduped by id (first wins), excluding the ids in F. It prints counts by stage. On a fixture it gives the exact expected ids, and a boundary row at exactly as_of−14d is included while one at as_of is excluded.
    - --select … --exclude-siblings-file T (premortem R-09; T = one v3 headline per line): a row whose difflib.SequenceMatcher ratio over pipeline.news.dedup.normalize_title(title) is ≥ 0.60 against any line of T is excluded and listed (id, matched headline, ratio) in the output. Fixture: "Formalizan a imputado por robo en Temuco" is excluded against "Formalizan a imputado por robo en Temuco - La Tercera"; an unrelated title is kept. --select also prints the pool split by epoch: classifier_none (outage) vs low_confidence / commune_null / resolver_fail (live model).
    - --classify refuses a cache path under data/ (reuses _assert_outside) and calls backfill.run_classify with a SpendMeter capped by --spend-cap. It is not unit-tested against the network: the test patches build_router_from_env with a fake router.
    - --sample 50 --seed 3608 --cache C --rows R --out S picks from would-publish items only (fixture: 3 OK-resolvable, 1 OK-unresolvable, 1 not_crime → the pool is 3). It is deterministic for a fixed seed and writes {id, url, outlet, title, description, predicted_family, commune_name, cut, title_src, title_en, title_en_fallback} — title_src = feeds.source_headline(title, outlet) and title_en/title_en_fallback = fidelity.guard_title_en(title_src, output title_en, outlet), the same fields 36-04 gives run_review/--audit-sample (premortem R-16), never the classifier's title_es. It exits 5 if the pool is < 50 (report, do not sample around it).
    - --sample … --blind-out B (premortem R-10) additionally writes the pass-1 file with ONLY {id, title, description, url, publisher_url, outlet}. publisher_url: for news.google.com urls, pipeline.news.gnews_decoder.decode_gnews_url(url, session=S, timeout=10) with S = one requests.Session() whose User-Agent header = pipeline.news.fulltext.USER_AGENT (exists today, never raises; ingest_url from 36-07 is not required). The session is mandatory: without it the decoder returns None after the old-format attempt (gnews_decoder.py:223-225), and 0/2,212 Google URLs in rejected/ decode offline (gate R1 BF-03, G-43), accepted only if http(s) and host ≠ news.google.com, else null; 1.5 s between decode calls; no LLM. Test with the decoder patched: a Google row gets the patched publisher URL, a failed decode gives null, a non-Google row gets its own url, and the patched decoder received a non-None session (BF-03). --blind-out prints `publisher_url_decoded: d/n_google`.
    - --score --verdicts V: agreement = count(q1 ∧ q3), with q3 = (pass-1 family == predicted_family); vida_n = count(predicted_family == vida); vida_ok = count(predicted vida ∧ pass-1 family vida); pass = agreement ≥ 43 ∧ vida_ok ≥ ceil(0.85·vida_n). It also reports agreement per basis (source | headline_only). Output is JSON + markdown. Fixture: 43/50 with vida 10/11 → pass; 42/50 → fail; vida 8/10 → fail (needs 9); a verdict with basis headline_only is counted in its own basis row.
  </behavior>
  <action>
Implement from the behavior list. Constants: FID03_MIN_AGREE = 43 (ceil(0.85·50), owner default); VIDA_MIN_RATE = 0.85; SAMPLE_N = 50; SEED = 3608.

Keep all classifier/pipeline imports function-local (R-12, backfill main :994 pattern). Load pipeline/.env then .env before building the router.

No writes under data/.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_fid03_audit.py -q -p no:cacheprovider</automated>
    Expected: exit 0. Negative control: set FID03_MIN_AGREE = 42, and the 42/50 → fail test must FAIL. Revert.
  </verify>
  <done>A deterministic, offline-tested audit tool that replays through the production path.</done>
</task>

<task type="auto">
  <name>Task 2: Select + replay-classify the 14-day pool (orchestrator)</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md</files>
  <action>
1. A = now (UTC), recorded (premortem R-09: A is declared as now, not a planned date). Build, in the scratchpad, the exclude file = the golden_set_v3 source_ids (from 36-GOLDEN-V3.md) and the siblings file = every v3 headline (one per line).
2. Run --select with --exclude-ids-file and --exclude-siblings-file. Record the pool size N, the stage split and the list of excluded siblings.
3. Spend precheck: estimate = N × $0.00025. Pass `--spend-cap min(0.90, 5.00 − phase-36 spend so far)` (G-45). If the estimate exceeds the cap, pause point 4.
4. Run --classify with the cache and ledger in the scratchpad. On exit 4 (router exhausted), re-invoke later; never sample after an exit 4 (G-18 precedent). On exit 3 (cap), report and pause.
5. Record the outcome split (ok / not_crime / parse_error / api_error), the would-publish count, the would-publish family distribution, the vida share (next to the execution-time share of current.json — 2026-09-24: 549/798 overall, 52/104 new era — not the stale 71.4 % of 36-RESEARCH.md; premortem R-21), and the spend. Record `classifier_blob_iter1: <git hash-object pipeline/news/classifier.py>`. Also record the pool composition by epoch as one line `pool_by_epoch: outage=<classifier_none n> live=<low_confidence+commune_null+resolver_fail n>` (premortem R-09).
6. Back-off (premortem R-09): if the would-publish count is < 50 (the condition on which --sample exits 5), move A back by 1 day (window stays 14 days), re-run --select and --classify (the cache is reused, so only new rows cost; same spend cap), and repeat until would-publish ≥ 50. Record `as_of_final: <A>` and every step.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && grep -nE "^pool: [0-9]+" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md && grep -nE "^spend_usd: 0\.[0-8][0-9]*" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md && grep -nE "^as_of_final: 2026-" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md && grep -nE "^pool_by_epoch: outage=[0-9]+ live=[0-9]+" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md && git diff --quiet -- data/</automated>
    Expected: the pool, spend, final-A and epoch lines are present, spend < 0.90 (G-45), and data/ is unchanged.
  </verify>
  <done>The fresh 2-week stream has been replayed through the Phase-36 classifier, with measured outcomes and spend.</done>
</task>

<task type="auto">
  <name>Task 3: Blind 50-item audit (fresh opus) + mechanical decision + G-NN</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md</files>
  <action>
1. --sample 50 --seed 3608.
2. A FRESH opus reviewer, not told the thresholds, audits in two passes (premortem R-10):
   - Pass 1 (from the --blind-out file only): title, description, url, publisher_url (decoded locally for news.google.com rows; the Google link itself is a JS page that cannot be fetched) and outlet. It answers q1 (a crime incident in Chile) and its own commune and family under the CEAD taxonomy of pipeline/cache/cead_page.html plus the news-only sexuales family, with a reason and basis = source (it read the publisher article) or headline_only (it could not).
   - Pass 2 (after pass 1 is written and frozen): it sees the model's predicted commune and family plus title_src and the guarded title_en, and answers q2 (commune consistent with source) and q4 (title_en adds or changes facts).
   - q3 is computed, not asked: pass-1 family == predicted family.
   The reviewer may fetch the publisher URL, one at a time, with ≥ 1.5 s between fetches.
3. Run --score. Decision rule (declared):
   - pass → FID-03 PASS.
   - fail → at most ONE extra category-level prompt iteration through 36-06 Task 3, which counts toward its 3-iteration cap. Re-run FID-02 and this audit with seed 3609 on the same pool minus the first sample. The re-audit re-classifies that pool into a NEW cache `$SCR/fid03-cache-iter{k}.jsonl`, never the first one: run_classify skips every id already final in its cache (backfill_classifier_outage.py:522-523, read 2026-09-24), so reusing it would measure the old prompt. Record `classifier_blob_iter{k}: <git hash-object pipeline/news/classifier.py>` in 36-FID03-AUDIT.md and assert it equals the blob of the prompt that 36-09 will ship. This second pass fits the plan cap USD 0.90 (G-45; pass `--spend-cap` = 0.90 − the first pass's spend, bounded by 5.00 − phase-36 spend). If the extra iteration passes, 36-DEPS03-BASELINE.json is re-extracted from that winning iteration (36-06 Task 3, NB-02) (gate R1 BF-07).
   - still fail → FID-03 = FAILED/Partial with numbers. 36-09 may still ship if FID-02 passed, because it improves on the live G-18 q1 40/50. Write the owner question to deferred-live.
4. Write the per-item verdict table (with basis), counts, agreement per basis as one line `agreement_by_basis: source=<a>/<n> headline_only=<b>/<m>`, the line `publisher_url_decoded: d/n_google` from --blind-out (BF-03), vida precision, vida share and the decision into 36-FID03-AUDIT.md. Informational, never gated: `sample_by_epoch: outage=<n> live=<m>` and `agreement_by_epoch: outage=<a>/<n> live=<b>/<m>` (gate R1 NB-01), and the Wilson 95 % interval of the agreement next to the decision (premortem R-21).
5. Record G-33 (FID-03 outcome + Granite-era re-classification NO, the owner default applied) for the directive decision log.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && grep -nE "^agreement: [0-9]+/50" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md && grep -nE "^agreement_by_basis: source=[0-9]+/[0-9]+ headline_only=[0-9]+/[0-9]+" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md && grep -nE "^vida_precision: [0-9]+/[0-9]+" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md && grep -nE "^publisher_url_decoded: [0-9]+/[0-9]+" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md && grep -nE "^\*\*Decision\*\*: (PASS|FAILED)" .planning/phases/36-classification-fidelity-attribution/36-FID03-AUDIT.md</automated>
    Expected: all five lines are present. The decision follows mechanically from the declared numbers.
  </verify>
  <done>FID-03 is decided on a blind (two-pass), out-of-sample (ids and event-siblings excluded), fresh 2-week audit against a threshold declared before classification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local tool → paid API | spend |
| reviewer → publisher sites | outbound fetches |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-22 | Repudiation | audit leakage / bias | mitigate | golden_set_v3 source ids are excluded, the reviewer is blind and fresh, the seed is fixed, and the conservative pool bias is disclosed. |
| T-36-23 | Denial of service (budget) | replay classify | mitigate | SpendMeter cap ≤ 0.90 (G-45), with a phase USD 5 precheck. |
| T-36-24 | Tampering | data/ mutated by the audit | mitigate | The cache must be outside data/ (_assert_outside), and verify runs `git diff --quiet -- data/`. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
- pytest for test_fid03_audit exits 0.
- 36-FID03-AUDIT.md carries the pool, spend, agreement, vida precision and Decision lines.
- data/ is unchanged.
</verification>

<success_criteria>
The FID-03 agreement (≥ 43/50) and the vida-precision rule are measured on a fresh, blind, out-of-sample 2-week audit, and the Granite-era decision is recorded.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-08-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-09 | must_haves population truth (sibling exclusion ≥ 0.60 on dedup.normalize_title, A = now with 1-day back-off, final A recorded); artifact provides; Task 1 behavior (--exclude-siblings-file, epoch split); Task 2 step 1 (A = now, siblings file), step 2, step 5 (`pool_by_epoch`), new step 6 (back-off, `as_of_final`); Task 2 verify; done. Live family distribution of added rows is in 36-09 Task 3. |
| R-10 | must_haves truth (two-pass, basis); Task 1 behavior (--blind-out with publisher_url via gnews_decoder.decode_gnews_url, q3 from pass 1, agreement per basis); Task 3 step 2 (two passes) + step 4 (`agreement_by_basis`); Task 3 verify. Deviation: the premortem named ingest_url.resolve_publisher_url, which 36-07 creates in the same wave 4 without a dependency edge; the plan uses the existing gnews_decoder.decode_gnews_url with the same acceptance rule (http(s), host ≠ news.google.com) so no new dependency or wave change is needed. |
| R-16 | Task 1 behavior --sample fields (title_src, guarded title_en, title_en_fallback; never title_es) |

## Revision R2 (gate R1)

| id | change |
|---|---|
| BF-03 (= R-10 not applied) | Task 1 behavior --blind-out: decode_gnews_url(url, session=requests.Session() with User-Agent = fulltext.USER_AGENT, timeout=10), 1.5 s spacing; test asserts a non-None session reached the patched decoder; `publisher_url_decoded: d/n_google` printed and written (Task 3 step 4) and grepped in Task 3 verify (read gnews_decoder.py:191-232, fulltext.py:37) |
| BF-07 | Task 3 step 3: extra iteration re-classifies into a NEW cache fid03-cache-iter{k}.jsonl (read backfill_classifier_outage.py:522-523), `classifier_blob_iter{k}` recorded and asserted == shipped prompt, DEPS-03 baseline re-extracted on PASS; cap 0.60 → 0.90 in must_haves, Task 2 step 3, Task 2 verify regex + Expected, T-36-23 (G-45) |
| NB-01 | Task 3 step 4: `sample_by_epoch` / `agreement_by_epoch`, informational |
| R-21 (prev. edit) | Task 2 step 5: vida share vs the execution-time current.json share (not the stale 71.4 %); Task 3 step 4: Wilson 95 % interval next to the decision |

The R1 table's R-10 row stays as history; its 'decode without session' reading is superseded by BF-03.
