---
phase: 36-classification-fidelity-attribution
plan: 09
type: execute
wave: 5
depends_on: ["36-01", "36-02", "36-03", "36-04", "36-05", "36-06", "36-07", "36-08"]
files_modified:
  - pipeline/check_hotfix_regression.py
  - pipeline/tests/test_check_hotfix_regression.py
  - site/scripts/validate/all.mjs
  - pipeline/news/store.py
  - .planning/phases/36-classification-fidelity-attribution/36-LIVE-EVIDENCE.md
  - .planning/phases/36-classification-fidelity-attribution/36-S0-IDS.txt
autonomous: true
requirements: [FID-07, FID-01, FID-04, FID-05, FID-06]
executor: sonnet (Task 1 regression script); orchestrator (Tasks 2-3: ship gate, push, deploy polling, live evidence); fresh opus = pre-push reviewer

must_haves:
  truths:
    - "pipeline/check_hotfix_regression.py (FID-07) asserts over data/incidents (current + archive/*.json) that the 8 t59 ids and 63fb0c3ef0adca19 are absent, and that 514872e1d06db7f2 carries exactly title_es 'Detienen a prófugo por violento robo a trabajadores forestales en Carahue' and title_en 'Fugitive arrested over violent robbery of forestry workers in Carahue' (t59 SUMMARY, 'Retitled'). Over served HTML it asserts that none of the 9 dropped URLs appears."
    - "Ship gate at ONE HEAD before the push (numbers declared): pytest ≥ 642 passed / 1 skipped / 2 xfailed + the Phase-36 new tests, exit 0; vitest ≥ 184 + new, rc 0; validators 18/18 PASS (freshness is a real signal); astro check 0 errors; lint-workflows exit 0; check-secret-hygiene exit 0; FID-07 script exit 0; fresh-opus pre-push review GO."
    - "Live evidence (declared): over scheduled news runs after the ship, until ≥ 20 incidents are added by live runs or 8 runs (48 h) pass:
      - every added incident has non-empty title_src, title_es == title_src, and no trailing ' - <outlet>' (FID-01);
      - the dedup validator over data/incidents/current.json gives 0 drops after the first post-ship run (FID-05); if PRUNE_EXISTING=False, `validate_news_dedup.py --only-new-since 36-S0-IDS.txt` gives 0 drops, #19 is not registered and the verdict line is 'FID-05_live: PARTIAL (forward-only, G-30)' (premortem R-04);
      - FID-04 (G-37, amends G-32) PASS iff (a) the live decode rate Σdecoded / Σ(decoded + failed) over the run summaries' gnews_decode, with Σ attempts ≥ 20, is ≥ live_decode_floor of 36-GNEWS-DECODE.md (= min(0.70, residential rate − 0.20)) AND (b) news.google.com share of `url` among added incidents ≤ 43% (the 53% V-10 baseline − 10 pp) with n ≥ 20; (a) failing because Google blocks runner decoding → PARTIAL (G-35(6)) with the rate recorded;
      - 0 '[BioBioChile] Feed parse warning' WARNING lines (FID-06);
      - health gate success, api_errors 0, 0 HTTP 404 (NREC regression)."
    - "news-dedup.mjs is registered in all.mjs as validator #19 only after the live 0-drop observation, and never under PRUNE_EXISTING=False (validators stay 18/18 with the recorded reason, premortem R-04)."
    - "The ship-time drop list (after the last `git pull --rebase`, before the push) is diffed against 36-DEDUP-REVIEW.md, and every new pair passes the same G-30 fresh-opus review before the push (premortem R-12)."
    - "On prod, every EN card carries exactly one disclosure label: count(news-card) == 'Machine-translated headline' + 'Original Spanish headline' + 'Automatically generated headline', and the 'Automatically generated headline' count == served rows without title_src (G-36, premortem R-01)."
    - "Informational, never gated (premortem R-09/R-11/R-13): family distribution and vida share of the added rows vs 50% (new era, measured 52/104) and 51.4% (G-18); near-duplicate pairs outside the 0.82 (cut, date) rule; title_en_fallbacks/accepted vs an informational ≤ 5%."
  artifacts:
    - path: pipeline/check_hotfix_regression.py
      provides: "FID-07 regression check (data + served HTML)"
    - path: .planning/phases/36-classification-fidelity-attribution/36-LIVE-EVIDENCE.md
      provides: "ship gate numbers, deploy run, prod curls, live-run evidence per FID"
  key_links:
    - from: site/scripts/validate/all.mjs
      to: site/scripts/validate/news-dedup.mjs
      via: "VALIDATORS list entry 19"
      pattern: "news-dedup.mjs"
---

<objective>
Ship the Phase-36 code (pipeline + site), prove it on prod and on live cron runs, and add the FID-07 regression check that later plans and the close gate re-run.

This is a mid-phase push (G-34 as proposed, same reading as G-07(b)). FID-01, FID-04 and FID-05 are defined over new live incidents and over the live current.json, so they cannot be evidenced without a shipped pipeline.

Purpose:
- The owner's hotfix (quick-260922-t59, commit 45c2650) and G-27 (commit f3a84af) removed defamation-risk cards. FID-07 requires proof that the pipeline changes (FID-05 prune, verbatim titles) and the 36-10 backfill do not resurrect or displace them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/quick/260922-t59-hotfix-live-erroneous-news-cards/260922-t59-SUMMARY.md
@.planning/phases/36-classification-fidelity-attribution/36-05-SUMMARY.md
@.planning/phases/36-classification-fidelity-attribution/36-06-SUMMARY.md
@.planning/phases/36-classification-fidelity-attribution/36-08-SUMMARY.md

<interfaces>
- t59 SUMMARY:15-24: dropped ids c3db293fd1316ad1, d468d43eeda0316d, f9031873f12b9479, 8f18cd275b4a6692, 836e500e4e4b1f30, 60fa7e16887af782 (Fierro cluster, 2026-09-03), efa0fae52180e3e2 (2026-08-25) and c53a579a6c972705 (2026-08-26) (prison suicides). The retitled id 514872e1d06db7f2 is at :28-31. Commit 45c2650 (`git log -S c3db293fd1316ad1`).
- G-27: 63fb0c3ef0adca19 dropped in commit f3a84af (`git log -S 63fb0c3ef0adca19`).
- The dropped URLs are recoverable from `git show 45c2650^:data/incidents/current.json` and `git show f3a84af^:data/incidents/current.json`.
- 514872e1d06db7f2 is dated 2026-09-03. It leaves current.json when today − 30 d > 2026-09-03 (from 2026-10-04), then lives in archive/2026-09.json with the same strings (store.py archive loop copies rows unchanged).
- Deploy: deploy-on-code.yml triggers on site/** pushes (it is the sole prod build path for code, deploy-on-code.yml:7-10,30-31). news-pipeline commits deploy only on current.json/archive changes (35-02). Prod data is served at https://ischilesafe.com/data/incidents/current.json (sync-data copies data/incidents to public; HYG-01 is pending).
- Run evidence: `gh run list --workflow news-pipeline.yml`, `gh run view <id> --log` → the "Run summary: {json}" line (scrape_news.py:202) holds fidelity and gnews_decode (36-04/36-07).
- Baseline gate (directive RUN STATUS, Phase-35 close): pytest 642/1/2, vitest 184, validators 18/18, astro check 0 err, lint 0, secret-hygiene 0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FID-07 regression script (data + served HTML)</name>
  <files>pipeline/check_hotfix_regression.py, pipeline/tests/test_check_hotfix_regression.py</files>
  <behavior>
    - `--data data/incidents` exits 0 on the real tree at execution time (assert and record).
    - A fixture where a t59 id is re-added to current.json exits 1 naming the id. The same happens with 63fb0c3ef0adca19 in archive/2026-09.json.
    - A fixture where 514872e1d06db7f2's title_en is altered exits 1. When the id is absent from BOTH current and archive, it exits 1 ("retitled card lost").
    - `--html a.html b.html` exits 1 if any of the 9 dropped URLs appears, raw or with & as &amp;. It exits 0 otherwise. When --current is given and 514872e1d06db7f2 is inside that file, both the EN and ES retitled strings must appear in the corresponding html (EN in /news/, ES in /es/noticias/). Otherwise that check is skipped with a note.
  </behavior>
  <action>
Hardcode the 9 dropped ids and the 2 retitled strings, citing t59 SUMMARY / G-27. Hardcode the 9 dropped URLs, extracted ONCE at execution time from `git show 45c2650^:data/incidents/current.json` and `git show f3a84af^:data/incidents/current.json`. Record the extraction command in the module docstring.

Stdlib only.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_check_hotfix_regression.py -q -p no:cacheprovider && python pipeline/check_hotfix_regression.py --data data/incidents</automated>
    Expected: exit 0 on both. Negative control: add a t59 id to a copied fixture, and it must exit 1.
  </verify>
  <done>A committed, repeatable FID-07 check replaces the research session's ad-hoc curl/grep.</done>
</task>

<task type="auto">
  <name>Task 2: Ship gate at one HEAD → pre-push review → push → deploy → prod curls (orchestrator)</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-LIVE-EVIDENCE.md</files>
  <action>
Preconditions:
- 36-06 Decision is PASS, or FAILED with Part A only per its rule.
- 36-08 Decision is recorded.
- 36-05 PRUNE_EXISTING is decided.
- Phase-36 spend is ≤ 5.00.

1. Gate at one HEAD. Record the SHA and every number in 36-LIVE-EVIDENCE.md:
   - `PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider` (count by exit code);
   - from site/, ONE chained command: `npm run build && npm run validate; echo validate=$?; npx vitest run; echo vitest=$?; npx astro check > .astro-check.log 2>&1; echo check=$?` (OneDrive);
   - `bash .github/scripts/lint-workflows.sh`; `bash .github/scripts/check-secret-hygiene.sh`;
   - `python pipeline/check_hotfix_regression.py --data data/incidents`.
   Every threshold is as declared in must_haves. Any miss → fix within the owning plan's scope and re-gate.
2. Pre-push review: a fresh opus reviewer reads `git diff origin/master..HEAD` and must give GO. At most 2 fix cycles; NO-GO after 2 → pause point 1.
3. `git pull --rebase` (the cron commits data), re-run pytest + the FID-07 script.
3b. Ship-time drop-list review (premortem R-12), only when PRUNE_EXISTING=True: run `python pipeline/validate_news_dedup.py data/incidents/current.json --list > "$TEMP/36-dedup-ship.txt"` (exit 1 is expected while pairs exist; do not chain it with &&). Diff its pairs against the pair table of 36-DEDUP-REVIEW.md. Every pair not in that table goes to a fresh opus reviewer with the same G-30 rule as 36-05 Task 3 (same_event per pair; 514872e1d06db7f2 must not be a DROP). All pairs same_event → record `ship_drop_list_reviewed: N/N` (N = pairs in the ship list) and the new pairs' verdicts in 36-LIVE-EVIDENCE.md. Any false merge → set `PRUNE_EXISTING = False` in pipeline/news/store.py (comment citing 36-LIVE-EVIDENCE.md), re-run the step-1 gate, follow the forward-only branch (R-04) and record `ship_drop_list_reviewed: n/a (PRUNE_EXISTING=False)`. If PRUNE_EXISTING was already False, record the same n/a line.
3c. `git push` (never --force). If the push needs another `git pull --rebase` that brings new cron data, repeat 3b on the new list first.
4. Poll deploy-on-code until success (`gh run watch`). Wait ≥ 2 min, then curl prod:
   - /news/: "Machine-translated headline" + "Original Spanish headline" + "Automatically generated headline" counts == news-card count, and the "Automatically generated headline" count == the number of rows without title_src in the curled prod current.json (G-36, premortem R-01);
   - /es/noticias/: card count > 0;
   - /methodology/ has id="news-headlines", and /es/metodologia/ has id="titulares-noticias";
   - run `check_hotfix_regression.py --html` on the curled /news/ and /es/noticias/ with --current = the curled prod current.json.
   Record the counts.
5. Write 36-S0-IDS.txt: the incident ids of `git show <ship_sha>:data/incidents/current.json`, one per line (S0; used by Task 3, 36-10 and 36-11, and by the `--only-new-since` forward-only check).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && SHA=$(grep -oE "^ship_sha: [0-9a-f]+" .planning/phases/36-classification-fidelity-attribution/36-LIVE-EVIDENCE.md | cut -d" " -f2) && [ -n "$SHA" ] && git fetch -q origin && git merge-base --is-ancestor "$SHA" origin/master && curl -s https://ischilesafe.com/news/ -o "$TEMP/36-news.html" && curl -s https://ischilesafe.com/es/noticias/ -o "$TEMP/36-noticias.html" && curl -s https://ischilesafe.com/data/incidents/current.json -o "$TEMP/36-current.json" && python pipeline/check_hotfix_regression.py --html "$TEMP/36-news.html" "$TEMP/36-noticias.html" --current "$TEMP/36-current.json" && N=$(grep -o 'class="news-card"' "$TEMP/36-news.html" | wc -l) && MT=$(grep -o 'Machine-translated headline' "$TEMP/36-news.html" | wc -l) && OS=$(grep -o 'Original Spanish headline' "$TEMP/36-news.html" | wc -l) && AG=$(grep -o 'Automatically generated headline' "$TEMP/36-news.html" | wc -l) && LEG=$(python -c "import json,sys; print(sum(1 for i in json.load(open(sys.argv[1],encoding='utf-8'))['incidents'] if not (i.get('title_src') or '').strip()))" "$TEMP/36-current.json") && echo "cards=$N MT=$MT OS=$OS AG=$AG legacy_rows=$LEG" && [ "$N" -eq "$((MT+OS+AG))" ] && [ "$AG" -eq "$LEG" ] && grep -nE "^ship_drop_list_reviewed: (([0-9]+)/\2|n/a \(PRUNE_EXISTING=False\))" .planning/phases/36-classification-fidelity-attribution/36-LIVE-EVIDENCE.md && test -s .planning/phases/36-classification-fidelity-attribution/36-S0-IDS.txt</automated>
    Expected: the ship SHA is on origin/master, the FID-07 HTML check exits 0 on prod, every EN card on prod carries exactly one of the three labels (G-36), the legacy label count equals the served rows without title_src, the ship-time drop list is reviewed, and S0 is written. If a cron deploy lands between the page and JSON curls and the counts disagree, re-curl both once.
  </verify>
  <done>Phase-36 code is live on prod behind a full gate, and FID-07 holds on served HTML.</done>
</task>

<task type="auto">
  <name>Task 3: Live-run evidence (FID-01/04/05/06) + register validator #19</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-LIVE-EVIDENCE.md, site/scripts/validate/all.mjs</files>
  <action>
1. S0 = the ids in 36-S0-IDS.txt (Task 2 step 5, from `git show <ship_sha>:data/incidents/current.json`).
2. After each scheduled news run (≈ every 6 h; `gh run list --workflow news-pipeline.yml`): `git pull --rebase`. Added = ids in current.json not in S0 and not in any archive month at S0. Evaluate the must_haves live criteria over the cumulative added set:
   - FID-01 per row;
   - FID-05 `python pipeline/validate_news_dedup.py data/incidents/current.json` → 0 drops, from the first post-ship run on. If `grep -q '^PRUNE_EXISTING: bool = False' pipeline/news/store.py`, run it with `--only-new-since .planning/phases/36-classification-fidelity-attribution/36-S0-IDS.txt` instead (forward-only, premortem R-04);
   - after the FIRST post-ship run, write `first_post_ship_run: <run id> drops=<n> mode=<full|forward-only>` in 36-LIVE-EVIDENCE.md (36-10 Task 3 waits on this line, G-40);
   - FID-04 (G-37): (a) decode rate = Σdecoded / Σ(decoded + failed) of gnews_decode across the run summaries, with Σ attempts, vs live_decode_floor of 36-GNEWS-DECODE.md; (b) share = count(host news.google.com in url) / n_added;
   - FID-06 grep of the run logs for "[BioBioChile] Feed parse warning";
   - health (summary + gate step);
   - informational lines, never gated, over the cumulative added set:
     - `family_distribution_added: <family=n, …>; vida_share_added: X%` against 50% (new era, measured 52/104) and 51.4% (G-18) (premortem R-09);
     - `near_dup_pairs_info: cut_dt1_ge082=<n> bucket_060_082=<m>` = pairs with the same cut, |Δdate| ≤ 1 day and SequenceMatcher ratio ≥ 0.82 on dedup.normalize_title(title_es), and pairs in the same (cut, date) bucket with ratio in [0.60, 0.82) (premortem R-11; the locked rule is not changed);
     - `title_en_fallback_rate: <Σ fidelity.title_en_fallbacks>/<Σ accepted>` against an informational ≤ 5%; above it, fix false positives within 36-04's scope (fidelity.py) and record the fix (premortem R-13).
   Stop when n_added ≥ 20, or after 8 scheduled runs.
3. Verdicts:
   - each criterion is PASS/FAIL against its declared number;
   - FID-04: PASS iff (a) rate ≥ floor with Σ attempts ≥ 20 AND (b) share ≤ 43% with n ≥ 20 (G-37). (a) below the floor because Google blocks runner decoding (failed ≈ attempts) → PARTIAL (G-35(6)) with the rate recorded. Σ attempts < 20 or n_added < 20 after 8 runs → PARTIAL "insufficient n" with the measured values;
   - FID-05 ≠ 0 drops → FAIL (investigate within 36-05's scope; this is not a pause unless a fix would change the rule). Under PRUNE_EXISTING=False with 0 forward-only drops the line is `FID-05_live: PARTIAL (forward-only, G-30)`.
4. If FID-05 is 0 and PRUNE_EXISTING=True: add 'news-dedup.mjs' as entry 19 in all.mjs (VALIDATORS + header comment) as soon as the first post-ship run shows 0 drops. Then from site/: `npm run build && npm run validate` → 19/19 (freshness is live). Commit locally (pushed with 36-11). Under PRUNE_EXISTING=False: do NOT register #19; record `validators: 18/18 (#19 not registered: PRUNE_EXISTING=False, G-30)` in 36-LIVE-EVIDENCE.md (premortem R-04).
5. Re-run `check_hotfix_regression.py --data data/incidents` after the live runs (FID-07 after the pipeline change).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && E=.planning/phases/36-classification-fidelity-attribution/36-LIVE-EVIDENCE.md && grep -nE "^FID-0[1456]_live: (PASS|FAIL|PARTIAL)" "$E" && grep -nE "^first_post_ship_run: [0-9]+ drops=[0-9]+" "$E" && [ "$(grep -cE '^(family_distribution_added|near_dup_pairs_info|title_en_fallback_rate): ' "$E")" -ge 3 ] && if grep -q '^PRUNE_EXISTING: bool = False' pipeline/news/store.py; then python pipeline/validate_news_dedup.py data/incidents/current.json --only-new-since .planning/phases/36-classification-fidelity-attribution/36-S0-IDS.txt && ! grep -n "'news-dedup.mjs'" site/scripts/validate/all.mjs && grep -n "^FID-05_live: PARTIAL (forward-only, G-30)" "$E"; else python pipeline/validate_news_dedup.py data/incidents/current.json && grep -n "'news-dedup.mjs'" site/scripts/validate/all.mjs; fi && python pipeline/check_hotfix_regression.py --data data/incidents</automated>
    Expected: four FID live verdict lines, the first-post-ship-run line and the three informational lines. With PRUNE_EXISTING=True: dedup is 0 drops and #19 is registered. With PRUNE_EXISTING=False: the forward-only check is 0 drops, #19 is NOT registered, and FID-05_live is PARTIAL (forward-only, G-30). FID-07 exits 0. A FAIL line is reported as-is, never rounded to PASS.
  </verify>
  <done>Live evidence for FID-01/04/05/06 is recorded against declared numbers, and the dedup validator is enforced from now on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local master → origin → Cloudflare prod | public release |
| news cron (bot commits) ↔ local rebases | concurrent writers to data/ |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-25 | Tampering | unreviewed code to prod | mitigate | Full gate at one HEAD, a fresh-opus pre-push GO, `git pull --rebase`, and never --force (directive hard rules). |
| T-36-26 | Repudiation | FID-07 regression (defamation cards return) | mitigate | check_hotfix_regression.py runs on data before the push, after the live runs, and on served HTML. |
| T-36-27 | Denial of service | pipeline break on first live run | mitigate | The health gate + pipeline-failure-news issue (NREC-07) stay armed. Live evidence checks api_errors/health each run. Rollback = git revert of the ship commits (reversible). |
| T-36-28 | Information disclosure | secrets in logs | mitigate | check-secret-hygiene exit 0. gh run logs are read locally only. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
- Ship gate numbers are recorded at one SHA.
- The prod curl checks pass.
- The live verdict lines are recorded.
- Validators are 19/19 once #19 is registered (18/18 with the recorded reason under PRUNE_EXISTING=False).
- FID-07 exits 0 on data and HTML.
</verification>

<success_criteria>
The Phase-36 code runs on prod. New live incidents are verbatim-titled, publisher-linked where decodable, and rule-idempotent (0 drops under the locked 0.82 (cut, date) rule; residual near-duplicates outside the rule are reported, not claimed away). Every EN card carries one of the three disclosure labels. The hotfixed cards stay correct.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-09-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-01 | must_haves truth (three EN labels, AG == served rows without title_src); Task 2 step 4 (label sum) + verify (N == MT + OS + AG, AG == legacy rows of the curled current.json, replacing cards == MT) + Expected |
| R-03 | must_haves FID-04 truth (G-37: (a) live decode rate ≥ live_decode_floor with Σ attempts ≥ 20, (b) share ≤ 43% n ≥ 20; PARTIAL on blocking); Task 3 step 2 FID-04 bullet + step 3 verdict rule |
| R-04 | frontmatter (+36-S0-IDS.txt); must_haves FID-05 + #19 truths; Task 2 step 5 writes 36-S0-IDS.txt; Task 3 step 1 (S0 file), step 2 FID-05 forward-only branch, step 3 PARTIAL line, step 4 no #19 under False + `validators: 18/18 (…)` line; Task 3 verify branches on PRUNE_EXISTING; verification |
| R-09 | must_haves informational truth; Task 3 step 2 `family_distribution_added` / vida share line; verify counts it |
| R-11 | must_haves informational truth; Task 3 step 2 `near_dup_pairs_info` line; success_criteria reworded to "rule-idempotent" (no "duplicate-free" claim) |
| R-12 | frontmatter (+pipeline/news/store.py, flipped only on a ship-time false merge); must_haves truth; Task 2 new step 3b (ship-time list diff + fresh review, `ship_drop_list_reviewed`) and 3c (push); verify greps the line |
| R-13 | must_haves informational truth; Task 3 step 2 `title_en_fallback_rate` line vs ≤ 5% (fix within 36-04 scope if above) |
| R-17 (twin) | Task 3 step 2 `first_post_ship_run` line, on which 36-10 Task 3 waits (G-40) |

Knobs: G-36 (R-01), G-37 (R-03), G-40 (R-17); texts returned to the orchestrator with the R1 revision.
