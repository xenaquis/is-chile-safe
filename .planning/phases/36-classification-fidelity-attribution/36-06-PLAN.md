---
phase: 36-classification-fidelity-attribution
plan: 06
type: execute
wave: 3
depends_on: ["36-02", "36-04"]
files_modified:
  - pipeline/news/classifier.py
  - pipeline/news/schema.py
  - pipeline/tests/test_classifier.py
  - pipeline/tests/test_schema_incidents.py
  - .planning/phases/36-classification-fidelity-attribution/eval/
  - .planning/phases/36-classification-fidelity-attribution/36-EVAL-RESULTS.md
  - .planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json
autonomous: true
requirements: [FID-01, FID-02]
executor: opus (classifier.py prompt + ClassifierOutput contract); orchestrator = eval runs (Task 3)

must_haves:
  truths:
    - "The system prompt no longer asks for title_es. ClassifierOutput has no title_es field. title_en is specified as a faithful English translation of the given HEADLINE (no summarising; same facts, names, numbers, places, kinship terms; trailing ' - outlet' omitted) (FID-01: the LLM no longer rewrites headlines)."
    - "The prompt's non-crime rule is category-level and derived from the CEAD taxonomy plus the G-18/34-06 failure classes: accidents of any kind, suicides, deaths with no alleged crime, fires/emergencies/disasters without alleged arson, preventive/security announcements and enforcement balances, institutional/policy/administrative news. Judicial or procedural news about a specific crime stays in scope with the underlying crime's family."
    - "FID-02 gate on golden_set_v3 with deepseek/deepseek-v4.1-flash, variant enabled_false (G-16), declared before any run: not_crime_rate ≥ 0.80 (≥ ceil(0.8·N_nc)), v2 commune ≥ 39/41 and v2 family ≥ 37/41 on the 41 UNCONTESTED v2 labelled ids (G-38, amending G-32: gs-030/gs-032/gs-038 are reported per id and not gated; same allowed misses as 42/44 G-06 and 40/44 G-15), parse_errors 0, empty_content 0, finish_length 0 (G-13), v2 null 3/3, boundary family ≥ the 36-02 baseline count (no regression). The confusion matrix is recorded."
    - "At most 3 prompt iterations. If none passes, only the FID-01 headline change ships (Part A), the non-crime rules are reverted (Part B), FID-02 is recorded as failed, and the run pauses (pause point 2 analogue) with the owner question."
    - "No prompt text is fitted to items. The prompt's RULES text (SYSTEM_PROMPT minus the {_COMMUNE_LIST_STR} commune block and minus the JSON schema block, via pipeline/tests/shingle_guard.prompt_rules_text from 36-02) shares no normalized 5-word shingle with any v3 headline or description (pytest guard). The commune list is excluded because a place name shared with the list is not rule fitting (premortem R-07: 'san pedro de la paz' collided)."
    - "After PASS, the v2-subset metrics of the winning iteration are frozen as the DEPS-03 baseline in 36-DEPS03-BASELINE.json (G-41)."
    - "The model is unchanged: primary deepseek/deepseek-v4.1-flash, backup DeepSeek direct deepseek-v4-flash (model_config.py:31/34). The backup is run once on the final prompt, and its result is recorded (informational, not gating)."
  artifacts:
    - path: pipeline/news/classifier.py
      provides: "SYSTEM_PROMPT without title_es; tightened non-crime rules"
    - path: .planning/phases/36-classification-fidelity-attribution/36-EVAL-RESULTS.md
      provides: "per-iteration gate table, confusion matrix, spend, decision"
  key_links:
    - from: pipeline/experiments/eval_classifier.py
      to: pipeline/news/classifier.py SYSTEM_PROMPT
      via: "classifier_mod._request_completion (same production seam)"
      pattern: "SYSTEM_PROMPT"
---

<objective>
FID-01 (model side) and FID-02 (non-crime rejection): stop the LLM from authoring headlines, and tighten the non-crime rule until the pre-declared golden-set gate passes, without regressing the G-16 commune/family marks.

Purpose:
- The prompt still demands "concise Spanish headline" (classifier.py:176) and a translation "of title_es" (:177).
- The only non-crime rules are the generic one (:189) and traffic accidents (:190).
- G-18 measured q1 40/50 on the live prompt (accidents, natural deaths, fires, preventive notices, a landmine accepted as vida/armas).
- 34-06 found 19 OTHER_DEFECT live items (rafting, helicopter, workplace accidents, prisoner transfers, prosecutor meetings).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/36-classification-fidelity-attribution/36-02-SUMMARY.md
@.planning/phases/36-classification-fidelity-attribution/36-EVAL-BASELINE.md
@.planning/phases/34-news-classification-restore/34-BACKFILL-AUDIT.md
@.planning/phases/34-news-classification-restore/34-06-HITS-VERDICTS.json
@pipeline/news/classifier.py

<interfaces>
Evidence at HEAD 9e3abaf:
- classifier.py:
  - :169-194 SYSTEM_PROMPT (JSON schema :172-180; commune list :182-183; rules :185-193). It must keep the word "json" (DeepSeek JSON mode, :167).
  - :241-292 `_parse_content` validates ClassifierOutput (shared by the eval runner).
  - :479 user_content = "HEADLINE: {title}\nSUMMARY: {description[:500]}". Unchanged: the input format measured by G-16/36-02.
- schema.py:94-123 ClassifierOutput has title_es at :113. Pydantic ignores extra keys by default, so the G-18 cache lines that still contain title_es keep validating.
- 34-06 OTHER_DEFECT classes (34-06-HITS-VERDICTS.json, 19 items, 18 dated 2026-08-25..09-04 = Granite era, 1 dated 2026-09-23 = aef6f3680c904f4c "Workplace accident at Minera Escondida"): rafting accident, helicopter accident, pedestrian and traffic collisions, bus near-miss, protest announcement, prisoner transfers, a Gendarmería policy announcement, a foiled prison escape, a white-collar custody ruling, a Chile–Argentina prosecutors meeting ×2, a viral VIF allegation under review, and 3 geolocation defects (not a prompt-family issue).
- CEAD taxonomy for family boundaries: pipeline/cache/cead_page.html group catalog (G-15 used 201/20102/202 → robos_violentos; 6xx → propiedad; 50102 → armas).
- The FID-02 scorer and gate come from 36-02 (`eval_classifier.py --score`). Its baseline figures are in 36-EVAL-BASELINE.md.
- Spend basis: $0.00025/item planning figure (36-02 interfaces). Per iteration ≈ N_v3 (~79) × 0.00025 ≈ $0.02. Plan cap $0.25 (3 iterations + 1 backup run ≈ $0.08 expected).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Part A — headline fidelity in the prompt + ClassifierOutput without title_es</name>
  <files>pipeline/news/classifier.py, pipeline/news/schema.py, pipeline/tests/test_classifier.py, pipeline/tests/test_schema_incidents.py</files>
  <behavior>
    - "title_es" does not appear in SYSTEM_PROMPT. "title_en" appears once in the schema block, and the instruction says to translate the HEADLINE faithfully, including kinship terms and names, and to omit a trailing " - <outlet>".
    - ClassifierOutput.model_validate({commune_name, region_hint, family, title_en, summary, confidence}) validates. The same payload plus a stray "title_es" also validates (G-18 cache compatibility), and hasattr(result, "title_es") is False.
    - _parse_content on a canned model answer without title_es returns ("ok", out).
    - Shingle guard (premortem R-07): for every v3 item, no normalized (NFD accent-stripped, lowercase, punctuation → space) 5-word shingle of its headline or description appears in the normalized RULES text = pipeline/tests/shingle_guard.prompt_rules_text(SYSTEM_PROMPT, _COMMUNE_LIST_STR) (the prompt without the {_COMMUNE_LIST_STR} block and without the JSON schema block).
    - Guard-scope test: normalize("san pedro de la paz") occurs in the normalized full SYSTEM_PROMPT but not in the rules text, and the rules text contains no '"commune_name": "<' schema line, so the guard provably excludes both blocks.
    - The full pytest passes. Tests that built ClassifierOutput with title_es keep passing (the extra is ignored). Tests that asserted .title_es are updated to not read it.
  </behavior>
  <action>
Step 0: confirm 36-04 has landed (`! grep -n "result.title_es" pipeline/scrape_news.py`). Otherwise STOP: removing the field first would crash the live path.

Rewrite the JSON schema block (:172-180) to remove "title_es". Set title_en to "<faithful English translation of the HEADLINE exactly as given (omit a trailing ' - <outlet name>'); keep every fact, name, number, place and family-relationship term; do not summarise, add or drop anything; max 200 chars, plain text>". Update rule :193 to "Output plain text only for title_en and summary".

Remove `title_es` from ClassifierOutput (schema.py:113) and update the docstring.

Add the shingle-guard test and the guard-scope test, loading pipeline/tests/fixtures/golden_set_v3.json and importing prompt_rules_text / shared_shingles from pipeline/tests/shingle_guard.py (created by 36-02; do not re-implement it).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && ! grep -n "title_es" pipeline/news/classifier.py && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider; echo "pytest rc=$?"</automated>
    Expected: the grep finds nothing, and pytest rc=0 at ≥ 642/1/2 + new.
  </verify>
  <done>The model is only asked to translate the given headline. The contract no longer carries an LLM-authored Spanish headline.</done>
</task>

<task type="auto">
  <name>Task 2: Part B — category-level non-crime and family-boundary rules</name>
  <files>pipeline/news/classifier.py, pipeline/tests/test_classifier.py</files>
  <action>
Keep the Part A text. Insert the rules after the existing traffic rule (:190), as ONE rule list, category-level, with no names, places or dates from any item:
1. Deaths or injuries where the article alleges no crime are NOT crime incidents: natural death, suicide, drowning, a body found with no crime established, "authorities rule out third-party involvement". The same applies to every accident: work/mining, aviation, sport/recreation, domestic, explosive remnants/landmines, and traffic (keep the existing rule).
2. Fires, explosions, emergencies and natural disasters are NOT crime incidents unless the article alleges arson or another crime.
3. Institutional, policy, budget or administrative news is NOT a crime incident: meetings, statements, plans, preventive security deployments, enforcement or operation balances and statistics, prisoner transfers or prison policy, protests announced without incidents.
4. News about a specific, identifiable crime case (arrest, charge, formalisation, trial, sentence, escape of a person convicted for it) IS in scope, and family = the family of the underlying crime.
5. Family boundaries follow the CEAD catalog:
   - vida = homicide / femicide / attempted homicide / injuries;
   - robbery with violence or intimidation (including when it causes injuries but no death) = robos_violentos;
   - robbery or theft without confrontation = propiedad;
   - intra-family violence without death = vif;
   - sexual crimes = sexuales (existing rule :191);
   - never use vida as a default for "someone died".
For rules 1-3, set commune_name to null and confidence to 0.0 (mirrors the existing :189-190 mechanism).

Add a unit test asserting each category keyword ("suicid", "incendio", "accidente laboral" or its equivalent phrase you chose, "balance", "traslado") appears in SYSTEM_PROMPT. The shingle guard must stay green.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_classifier.py pipeline/tests/test_classifier_traffic.py -q -p no:cacheprovider</automated>
    Expected: exit 0, including the shingle guard.
  </verify>
  <done>The non-crime and family-boundary rules are expressed at category level, with no item fitting.</done>
</task>

<task type="auto">
  <name>Task 3: Eval loop against the pre-declared FID-02 gate (orchestrator) + backup run + decision</name>
  <files>.planning/phases/36-classification-fidelity-attribution/eval/, .planning/phases/36-classification-fidelity-attribution/36-EVAL-RESULTS.md</files>
  <action>
Spend guard: cap 0.25 for this plan via `--spend-cap` on a ledger at eval/spend-ledger-36-06.json. Before each run, check that the phase-36 spend (sum of the phase ledgers) + 0.10 ≤ 5.00; otherwise pause point 4.

Iteration k (k = 1..3):
1. Run `python pipeline/experiments/eval_classifier.py --provider openrouter --model deepseek/deepseek-v4.1-flash --reasoning enabled_false --golden pipeline/tests/fixtures/golden_set_v3.json --out-dir .planning/phases/36-classification-fidelity-attribution/eval --spend-ledger .../eval/spend-ledger-36-06.json --spend-cap 0.25`.
2. Rename the output to run-iter{k}.json and `--score` it into eval/36-EVAL-ITER{k}.{json,md}.
3. Gate (declared in must_haves): stop at the first passing iteration.
4. Between iterations, only category-level prompt edits are allowed. Every edit re-runs Task 1/2 pytest (including the shingle guard) before the next eval.

If iteration 3 fails: revert the Part B rules (keep Part A), re-run pytest, record FID-02 = FAILED, and write the pause question: "no prompt met non-crime ≥ 80% without regressing commune/family; options: accept the best iteration (numbers), extend the golden set, or evaluate another model under a new pre-declared rule".

After a pass: run the backup once with `--provider deepseek --model deepseek-v4-flash --reasoning thinking_disabled`, then score it. It is informational: if its not_crime_rate < 0.80, add a deferred-live note "backup weaker on non-crime", which matters only during failover.

Write 36-EVAL-RESULTS.md: the baseline vs iterations table (every gate member, with v2 commune/family on the 41 uncontested ids plus the all-44 figures and the per-id gs-030/gs-032/gs-038 outcome, G-38), the final confusion matrix, per-category non-crime rejection, the backup row, spend per run, and the decision as one exact line `**Decision**: PASS` or `**Decision**: FAILED` (the verify greps it). Record the phase-36 spend so far for RUN STATUS.

DEPS-03 baseline (G-41, premortem R-18), after PASS: from the winning iteration's run-iter{k}.json, extract the v2-subset metrics (the 44 labelled + 3 null v2 ids): commune_correct, family_correct, parse_errors, null_correct, served_by_counts, plus the model, variant, run file name and the classifier.py blob SHA (`git hash-object pipeline/news/classifier.py`). Write them to .planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json. Phase 37 DEPS-03 compares against this file (prompt at the ship SHA), not against 34-AB-RESULTS.json (old prompt).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && test -s .planning/phases/36-classification-fidelity-attribution/36-EVAL-RESULTS.md && grep -nE "^\*\*Decision\*\*: (PASS|FAILED)" .planning/phases/36-classification-fidelity-attribution/36-EVAL-RESULTS.md && python -c "import json; d=json.load(open('.planning/phases/36-classification-fidelity-attribution/eval/spend-ledger-36-06.json')); print('spend', d['total_usd']); assert d['total_usd'] <= 0.25" && { ! grep -q "^\*\*Decision\*\*: PASS" .planning/phases/36-classification-fidelity-attribution/36-EVAL-RESULTS.md || python -c "import json; d=json.load(open('.planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json',encoding='utf-8')); [d[k] for k in ('commune_correct','family_correct','parse_errors','null_correct','served_by_counts')]; print('deps03 baseline ok')"; }</automated>
    Expected: the results file has a Decision line and spend ≤ 0.25; on PASS, 36-DEPS03-BASELINE.json exists with the five metric keys. PASS is required for 36-08/36-09 to proceed with Part B. On FAILED, the run pauses (see action).
  </verify>
  <done>FID-02 is decided mechanically against numbers declared before the first run, with the confusion matrix and spend recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| RSS text → LLM prompt | untrusted input can attempt prompt injection |
| LLM JSON → pipeline | untrusted output |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-15 | Tampering | prompt injection via headline/summary | accept (unchanged) | Output is still closed-set validated: commune via resolve_cut, family via VALID_FAMILIES, title_en via the 36-04 guard and editorial filter. Description is truncated at 500 (:479). |
| T-36-16 | Repudiation | overfitting the gate | mitigate | Rules are category-level, enforced by the shingle guard. Out-of-sample checks follow: the 36-08 fresh 2-week audit and the 36-10 G-11 audit. |
| T-36-17 | Denial of service (budget) | eval loop | mitigate | --spend-cap 0.25, a 3-iteration cap, and the phase USD 5 pause rule. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
- pytest is green (exit code).
- 36-EVAL-RESULTS.md has the gate table and a Decision line.
- The spend ledger is ≤ 0.25.
</verification>

<success_criteria>
The LLM no longer authors headlines. Non-crime rejection is ≥ 80% on the v3 gate set with no commune/family/parse regression, or the failure is recorded mechanically and the run pauses with Part A shipped.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-06-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-05 | must_haves FID-02 gate truth: v2 commune ≥ 39/41 and family ≥ 37/41 on the 41 uncontested ids (G-38), contested gs-030/032/038 reported, not gated; Task 3 results table columns |
| R-07 | must_haves "no prompt text is fitted" truth (rules text = prompt minus commune block and JSON schema block); Task 1 behavior shingle guard on the rules text + new guard-scope test; Task 1 action (import pipeline/tests/shingle_guard.py from 36-02) |
| R-18 | frontmatter files_modified (+36-DEPS03-BASELINE.json); must_haves truth; Task 3 action (extract the v2-subset metrics of the winning iteration after PASS); verify + Expected (baseline file on PASS) |

Knobs: G-38 (R-05) and G-41 (R-18); texts returned to the orchestrator with the R1 revision. Twin fixed in 36-11 Task 3 (deferred-live line for DEPS-03). R-08 needs no edit here (the prompt does not change; the exclusion lives in 36-02 Task 2).
