---
phase: 36-classification-fidelity-attribution
plan: 07
type: execute
wave: 4
depends_on: ["36-04", "36-05"]
files_modified:
  - pipeline/experiments/measure_gnews_decode.py
  - .planning/phases/36-classification-fidelity-attribution/36-GNEWS-DECODE.md
  - pipeline/news/ingest_url.py
  - pipeline/tests/test_ingest_url.py
  - pipeline/scrape_news.py
  - pipeline/tests/test_scrape_news.py
  - pipeline/news/pending.py
  - pipeline/tests/test_pending_queue.py
autonomous: true
requirements: [FID-04]
executor: opus (scrape_news ingest kernel + seen/pending key contract); orchestrator = decode-rate measurement run (Task 1)

must_haves:
  truths:
    - "The decode success rate is measured on a seeded sample of 50 news.google.com URLs from data/incidents/current.json and recorded (old-format vs new-format vs failed). This happens BEFORE the wiring (V-10 fix step 3). It mechanically fixes the FID-04 live decode floor declared in G-37: live_decode_floor = min(0.70, success_rate − 0.20), written in 36-GNEWS-DECODE.md."
    - "A fresh Google-News candidate that passes is_crime_item and is NOT already in seen/pending (checked on the Google link) is decoded at most once. On success, url = the publisher URL (http/https, not news.google.com, utm-stripped), via_url = the Google link, and id = sha256(publisher url)[:16]. On failure or budget exhaustion, url stays the Google link and via_url is absent."
    - "seen.json records BOTH keys for an answered item (url and via_url). A decoded item whose publisher URL is already seen or pending is skipped without classification, and its Google link is marked seen so it is never decoded again (FID-04 'seen keys on both')."
    - "pending.json entries carry via_url, the queue's url set includes via_url, and queued items are NEVER re-decoded (id stability, research Pitfall 3). A queued entry without via_url yields via_url None, never \"\" (premortem R-14: \"\" would make build_incident return None)."
    - "Courtesy and budget (G-31 as proposed): REQUEST_DELAY 1.5 s between decode calls (fulltext.py:39), request timeout 10 s, at most 60 decodes and 180 s of decode wall time per run. The run summary gains `gnews_decode` = {candidates, decoded, failed, skipped_budget}."
    - "No decode happens for keyword-failing or already-seen entries, so the is_crime_item prefilter stays before any network call."
  artifacts:
    - path: pipeline/news/ingest_url.py
      provides: "resolve_publisher_url(gn_url, session, timeout); DecodeBudget"
      exports: ["resolve_publisher_url", "DecodeBudget"]
    - path: .planning/phases/36-classification-fidelity-attribution/36-GNEWS-DECODE.md
      provides: "measured decode success rate (n=50)"
  key_links:
    - from: pipeline/scrape_news.py feed loop
      to: pipeline/news/ingest_url.py resolve_publisher_url
      via: "after the seen/pending check on the Google link, before appending to fresh"
      pattern: "resolve_publisher_url"
    - from: pipeline/news/ingest_url.py
      to: pipeline/news/gnews_decoder.py decode_gnews_url
      via: "hardcoded batchexecute host (T-gf7-04) preserved"
      pattern: "decode_gnews_url"
---

<objective>
FID-04 ingest side: store the publisher URL for Google-News items, keep the Google link as via_url, key seen/pending on both, and record the decode success rate.

The dedup half (url/via_url identity) shipped in 36-05.

Purpose:
- V-10: 426 of 798 current incidents (53.4%) are news.google.com links (measured 2026-09-24). Among the 104 incidents dated ≥ 2026-09-23 (new model) the share is 48 (46.2%).
- canonical_url returns entry.link for Google feeds (feeds.py:135-136).
- decode_gnews_url exists but is imported only by fulltext.py:30 (R2 path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/36-classification-fidelity-attribution/36-04-SUMMARY.md
@.planning/phases/36-classification-fidelity-attribution/36-05-SUMMARY.md
@pipeline/scrape_news.py
@pipeline/news/pending.py
@pipeline/news/gnews_decoder.py

<interfaces>
Evidence at HEAD 9e3abaf (36-04 shifts scrape_news lines; re-grep):
- gnews_decoder.py:
  - :191-236 decode_gnews_url(url, session=None, timeout=15.0) → publisher URL | url unchanged (non-Google host) | None. It never raises.
  - :81 `_try_old_format` (no network); :100 `_try_new_format` (GET + POST to the hardcoded :31 `_BATCHEXECUTE_URL`).
  - :54-61 `_is_http_url` scheme allowlist on the decoder output.
- fulltext.py:37 USER_AGENT; :39 REQUEST_DELAY = 1.5.
- scrape_news.py:
  - :253 router built; the G-09 run budget (1200 s, classifier.py:92) counts from router construction (classifier.py:646), so decode time is spent from that budget. At most 180 s leaves ≥ 1,020 s.
  - :328-329 seen_set / pending_urls; :336-383 feed loop (:352 url = canonical_url; :362 prefilter; :368 seen/pending check; :371 seen_set.add; :373-379 fresh dict);
  - :391-394 queued candidates; :432 `seen[item["url"]] = item["date"]` in `_answered`; :490 `key = pq.item_id(item["url"])`; :497/:505 upsert_failure;
  - test_scrape_news.py:934-940 is the summary key pin (36-04 added "fidelity").
- pending.py:76-107 upsert_failure builds entries with the fixed fields {id, url, title, description, date, outlet, first_queued, attempts, last_error} (:96-106); :47-57 load_pending.
- dedup (36-05): url/via_url canonical identity.
- store.build_incident(via_url=…) validates the scheme (36-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the decode success rate on 50 current Google-News URLs (orchestrator)</name>
  <files>pipeline/experiments/measure_gnews_decode.py, .planning/phases/36-classification-fidelity-attribution/36-GNEWS-DECODE.md</files>
  <action>
Write measure_gnews_decode.py, a read-only experiment:
- Load data/incidents/current.json. Take incidents whose url host is news.google.com, and sample 50 with random.Random(3607).
- For each URL: old = gnews_decoder._try_old_format(url). If that fails, call decode_gnews_url(url, session=requests.Session() with USER_AGENT from fulltext.py, timeout=10).
- Sleep REQUEST_DELAY between URLs.
- Classify each outcome as old_format / new_format / failed, and record whether the decoded host differs from news.google.com and passes store.is_safe_url.
- Print a JSON summary: {n, old_format, new_format, failed, success_rate, seconds}.

Run it locally (network, no LLM spend; about 50 × ≤ (1.5 s + 2 requests)). Write 36-GNEWS-DECODE.md with the table, the rate (one line `success_rate: X/50`), the wall time, and a note: "measured from a residential IP; the Actions runner rate is measured live in 36-09 from the run summary".

The residential rate is not itself a gate (FID-04 says "recorded"), but it fixes, mechanically, the live decode floor of G-37 (premortem R-03, amends G-32 FID-04). Write one more line `live_decode_floor: F` with F = min(0.70, X/50 − 0.20), two decimals. 36-09 gates FID-04(a) on it: live decode rate = Σdecoded / Σ(decoded + failed) over the run summaries' gnews_decode, with Σ attempts ≥ 20, must be ≥ F; the news.google.com share ≤ 43 % (n ≥ 20) stays as the secondary condition (b). Reason: the new-model baseline share is already 48/104 = 46.2 %, so (b) alone passes with zero decoding in ~1 of 3 cases.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && test -s .planning/phases/36-classification-fidelity-attribution/36-GNEWS-DECODE.md && grep -nE "success_rate: [0-9]+/50" .planning/phases/36-classification-fidelity-attribution/36-GNEWS-DECODE.md && grep -nE "^live_decode_floor: (0|1)\.[0-9]{2}" .planning/phases/36-classification-fidelity-attribution/36-GNEWS-DECODE.md && git diff --quiet -- data/</automated>
    Expected: the rate and floor lines exist (floor = min(0.70, rate − 0.20)), and data/ is unchanged (read-only).
  </verify>
  <done>The decode success rate is recorded from a measured sample of 50, and the G-37 live decode floor is derived from it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingest_url.py + scrape_news/pending wiring (publisher url, via_url, both-key seen, budgeted courtesy decode)</name>
  <files>pipeline/news/ingest_url.py, pipeline/tests/test_ingest_url.py, pipeline/scrape_news.py, pipeline/tests/test_scrape_news.py, pipeline/news/pending.py, pipeline/tests/test_pending_queue.py</files>
  <behavior>
    - resolve_publisher_url(G, session, 10) with decode_gnews_url patched → "https://www.biobiochile.cl/x?utm_source=a" returns "https://www.biobiochile.cl/x". Patched → "https://news.google.com/…", "javascript:…" or None returns None.
    - DecodeBudget(max_items=60, max_seconds=180, clock=fake): allow() is False after 60 items or 180 s. wait() sleeps REQUEST_DELAY between decodes, never before the first (recorded via a patched sleep).
    - Live run, Google entry G decoded to P: the incident has url P, via_url G, id make_id(P); seen.json has both P and G; summary.gnews_decode == {candidates: 1, decoded: 1, failed: 0, skipped_budget: 0}.
    - Decode failure: url G, no via_url key, seen has G.
    - P already in seen.json: no router.classify call for it, seen gains G, and there is no incident.
    - A keyword-failing Google entry, or a Google entry whose G is already in seen: resolve_publisher_url is NOT called (the call count is asserted).
    - 61 Google candidates: 60 decode calls, then skipped_budget == 1, and the 61st keeps url G.
    - API_ERROR on the decoded item: the pending.json entry has url P and via_url G. The next run, with the feed again yielding G, sees G in pending_urls, so there is no duplicate, and the queued item is classified from the queue WITHOUT a decode call.
    - pending.upsert_failure copies via_url when present. load_pending keeps it, and old entries without via_url still load (test_pending_queue unedited + a new test).
    - Premortem R-14: a pending.json entry WITHOUT via_url whose retry returns OK → exactly 1 incident written, no via_url key on it, rc 0 (a "" via_url would make build_incident return None).
    - The summary key set = the 36-04 set + "gnews_decode" (update the pin with a "FID-04 36-07" comment). test_news_health_gate passes unedited.
  </behavior>
  <action>
ingest_url.py (new):
- `resolve_publisher_url(gn_url, session, timeout=10.0) -> str | None`: calls gnews_decoder.decode_gnews_url(gn_url, session=session, timeout=timeout) and accepts the result only if store.is_safe_url(result) and its host is not news.google.com. It returns dedup._canonical_url(result).
- `DecodeBudget` with max_items=60, max_seconds=180.0, delay=REQUEST_DELAY, and an injectable clock/sleep. These constants are env-overridable via NEWS_MAX_DECODE / NEWS_DECODE_BUDGET_S using scrape_news._int_env semantics.

scrape_news.py:
- Build one requests.Session (USER_AGENT from feeds.py:74) and one DecodeBudget before the feed loop.
- In the loop, AFTER the seen/pending check on `url` (:368) and only when urlparse(url).hostname == "news.google.com":
  1. increment candidates;
  2. if budget.allow(): wait, then call resolve_publisher_url. On success: via_url = url, url = P. If P is in seen_set or pending_urls, record `seen[via_url] = date` and continue (skip). Otherwise add P to seen_set. On failure: count failed.
  3. else: count skipped_budget.
- Put "via_url" into the fresh dict only when set.
- In `_answered`: also set `seen[item["via_url"]] = item["date"]` when present.
- In queued candidates (:391-394): include via_url, and add the pending via_urls to pending_urls (:329). Do NOT add "via_url" to the `{k: p.get(k) or "" for k in (...)}` comprehension (read 2026-09-24 at :391-394), which would turn a missing via_url into "": normalize it separately as `via_url = p.get("via_url") or None` and put the key into the candidate only when it is not None (premortem R-14).
- build_incident(…, via_url=item.get("via_url")).
- Add `gnews_decode` to summary.update.

pending.py upsert_failure: add "via_url" to the new entry when cand has it (key omitted otherwise, so the byte-identity of old queues is kept).

Queued items never pass through the decode path, because the decode sits only in the feed loop.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_ingest_url.py pipeline/tests/test_scrape_news.py pipeline/tests/test_pending_queue.py pipeline/tests/test_news_health_gate.py pipeline/tests/test_gnews_decoder.py -q -p no:cacheprovider && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider; echo "pytest rc=$?"</automated>
    Expected: both runs exit 0 (rc=0) at ≥ 642/1/2 + new. Negative controls:
    - move the decode above the seen check, and the "resolve_publisher_url NOT called for seen G" test must FAIL. Revert.
    - drop `seen[item["via_url"]]`, and the both-keys test must FAIL. Revert.
  </verify>
  <done>Google-News items are stored with publisher URLs where decodable, identity covers both URLs everywhere (seen, pending, dedup), and the decode is courteous and bounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pipeline → news.google.com | new outbound requests per run |
| decoded URL (attacker-influenceable blob) → stored url → href | SSRF / scheme injection surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-18 | Tampering / SSRF | decode POST target | mitigate | The hardcoded batchexecute host (gnews_decoder.py:31, T-gf7-04) is unchanged. Only the Google link is fetched. The publisher URL is never fetched by this pipeline. |
| T-36-19 | Tampering | stored decoded url | mitigate | is_safe_url + host ≠ news.google.com in resolve_publisher_url; the schema url and via_url validators (36-01); site safeUrl on render. |
| T-36-20 | Denial of service | run budget / courtesy | mitigate | 60 items / 180 s per run, a 1.5 s delay and a 10 s timeout. Decode happens only after the prefilter and the seen check. |
| T-36-21 | Tampering / integrity | duplicate cards across identities | mitigate | Seen on both keys, pending carries via_url, queued items are never re-decoded, and dedup identity is on both (36-05). |
| T-36-SC | Tampering | installs | accept | None (requests is already a dependency). |
</threat_model>

<verification>
- The full pytest run is green (exit code).
- 36-GNEWS-DECODE.md records the rate.
- data/ is untouched.
</verification>

<success_criteria>
New Google-News incidents carry the publisher URL when decodable, with via_url kept. Seen, pending and dedup identity cover both URLs, and the decode success rate is recorded.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-07-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-03 | must_haves truth 1 (floor derived from the residential rate); Task 1 action (`live_decode_floor: F`, F = min(0.70, rate − 0.20); the rate is no longer "recorded only"); verify greps the floor line; done |
| R-14 | must_haves truth (via_url None, never ""); Task 2 behavior (pending entry without via_url → 1 incident, rc 0) + action (`p.get("via_url") or None`, kept out of the `or ""` comprehension at scrape_news.py:391-394) |

Knob: G-37 (R-03, amends G-32 FID-04); text returned to the orchestrator with the R1 revision. Twins fixed in 36-09 (must_haves FID-04 + Task 3 verdict) and 36-11 (must_haves FID-04).
