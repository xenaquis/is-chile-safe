---
phase: 36-classification-fidelity-attribution
plan: 10
type: execute
wave: 6
depends_on: ["36-09"]
files_modified:
  - data/incidents/current.json
  - data/incidents/archive/2026-09.json
  - data/incidents/rejected/2026-09.json
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
  - .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md
autonomous: true
requirements: [FID-07]
executor: orchestrator (long job: dry-run, classify, apply, push, R2 dispatch); fresh opus = G-11 auditor; sonnet = methodology line (Task 3 step 3)

must_haves:
  truths:
    - "The withheld G-18 population (rejection_stage classifier_none, parsed first_seen ≥ 2026-09-04T20:13:49Z; 2,122 rows at G-18) is re-classified with the SHIPPED Phase-36 classifier (the 36-09 ship SHA) into a FRESH cache outside data/, after a dry-run (zero writes) and a scratchpad backup of current.json, archive/2026-09.json and rejected/2026-09.json."
    - "Publication happens ONLY if the same G-11/G-14 gate passes, unchanged: a fresh opus auditor; a stratified 50 (≥ 20 gnews_title_only, ≥ 10 non_chile_cue, the rest random) with seed 3610, excluding golden_set_v3 source_ids and the 36-08 sample ids; every stratum ≥ ceil(0.86·n_s) and overall ≥ 43/50; a failing stratum is withheld and 30 more from the non-withheld remainder are re-audited at ≥ 26/30. Otherwise nothing is published, and the rows stay classifier_none (G-18 status quo)."
    - "On publish: accepted rows go through the 36-04 title_src/guard/editorial path and the 36-05 cross-run dedup. last_new_incident_at is unchanged (G-07(a), enforced by run_apply). seen.json is untouched. FID-07 holds (check_hotfix_regression.py exit 0). The dedup validator gives 0 drops. A bilingual methodology line discloses the reclassification (owner default 'Backfilled incidents')."
    - "30-day window: rows dated before (apply date − 30 d) are merged into archive/YYYY-MM.json only and never appear on the live pages. From 2026-10-04 the 2026-09-04 rows fall out of the window. The report states the in-window/archived split. COVERAGE_GAPS stays declared (G-23)."
    - "Backfill URLs are NOT decoded (G-31 as proposed): NREC-09's 'without refetching' contract. FID-04's share metric is defined on live-run incidents (36-09)."
    - "Spend ≤ plan cap USD 1.00 (estimate 2,122 × $0.00025 ≈ $0.53; G-18 measured $0.41 on the old prompt)."
  artifacts:
    - path: .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md
      provides: "classify split, audit table, decision, apply report or no-publication record"
  key_links:
    - from: pipeline/backfill_classifier_outage.py --apply
      to: pipeline/news/store.py merge_and_write
      via: "bump_last_new=False + cross-run dedup (36-05)"
      pattern: "bump_last_new=False"
---

<objective>
Re-run the withheld NREC-09 backfill with the Phase-36 classifier (the G-18 hand-off). Publish only if the unchanged G-11/G-14 audit gate passes.

Purpose:
- G-18: the Phase-34 classify of the 2,122 rows failed the audit at 38/50 (gnews 17/20, non_chile 7/10, random 14/20; q1 40/50). The dominant failure was the non-crime acceptance that FID-02 targets.
- The deferred-live list asks for this re-run after the non-crime fix.
- A pass also closes part of NREC-09/NREC-10 (they stay owned by Phase 34 and are reported as an update, not re-closed here).
- FID-07 must hold after the backfill (ROADMAP criterion 6: "after the pipeline and backfill changes").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/34-news-classification-restore/34-05-PLAN.md
@.planning/phases/34-news-classification-restore/34-BACKFILL-AUDIT.md
@.planning/phases/36-classification-fidelity-attribution/36-09-SUMMARY.md
@pipeline/backfill_classifier_outage.py

<interfaces>
backfill_classifier_outage.py (usage docstring :44-53; post-36-04/36-05 behaviour):
- `--dry-run --cache C` (counts, zero writes); `--classify --cache C --spend-cap USD --spend-ledger L` (exit 3 on cap, exit 4 on router exhaustion → never audit or apply after 4);
- `--review --cache C`; `--audit-sample 50 --seed S --cache C --out O [--exclude-ids ID,ID] [--withhold-ids F]` (the exclude only affects sampling in this invocation, :846/:854);
- `--apply --cache C [--withhold-ids F] [--report R] [--spend-ledger L]`.
- run_apply: dedup vs existing (:697) + merge_and_write(bump_last_new=False) (:712-716); verifies ids written (:717-726) and last_new unchanged (:728-732); rewrites rejected/ stages (:767).
- G-11/G-14 constants: strata :120-131; ceil(0.86·n) per stratum; overall 43/50; re-audit 26/30.
- R2 regeneration is done by dispatching r2-archive.yml (workflow_dispatch, r2-archive.yml:6), whose credentials stay in Actions (backfill docstring :11-13).
- Hard rules: data/ mutation is allowed for this backfill with dry-run + scratchpad backup first. Push only after a green gate plus `git pull --rebase`, never --force. deploy-on-code deploys human data pushes of current.json/archive (35-02 R-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run, backup, fresh classify with the shipped classifier (orchestrator)</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md</files>
  <action>
1. `git pull --rebase`, then assert that HEAD contains the 36-09 ship SHA (`git merge-base --is-ancestor <ship_sha> HEAD`).
2. Back up data/incidents/current.json, archive/2026-09.json and rejected/2026-09.json to the scratchpad (sha256 recorded).
3. Run `--dry-run --cache $SCR/bf36-cache.jsonl`. Record `selected` (expected ≥ 2,122; G-06: growth allowed, and every G-18 row must be included) and `would_archive`.
4. Spend: estimate = selected × $0.00025. Pass `--spend-cap min(1.00, 5.00 − phase-36 spend so far)`. If the estimate exceeds the cap, pause point 4.
5. Run `--classify --cache $SCR/bf36-cache.jsonl --spend-ledger $SCR/bf36-ledger.json --spend-cap <cap>`. Exit 4 → re-invoke later. Exit 3 → pause.
6. Record the outcome split (ok / not_crime / parse_error / api_error) next to G-18's (1,162 / 953 / 5 / 2), plus the spend and failovers.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && grep -nE "^classify_split: " .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md && grep -nE "^spend_usd: " .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md && git diff --quiet -- data/</automated>
    Expected: the split and spend are recorded, and data/ is unchanged (classify writes only to the scratchpad).
  </verify>
  <done>The outage population is re-classified by the shipped Phase-36 classifier, with measured outcomes and spend.</done>
</task>

<task type="auto">
  <name>Task 2: G-11/G-14 audit, unchanged (fresh opus) → publish decision</name>
  <files>.planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md</files>
  <action>
1. Run `--review` (flagged rows), then a fresh opus review that lists exclusions, exactly as in 34-05 step 7.
2. Run `--audit-sample 50 --seed 3610 --exclude-ids <golden v3 source_ids + 36-08 sampled ids> --out $SCR/bf36-audit.jsonl`. Exit 5 (a stratum has 0 candidates) → STOP and report.
3. A FRESH opus auditor (not the 36-08 reviewer) answers q1-q4 per item, as in 34-BACKFILL-AUDIT.md.
4. Apply G-11/G-14 mechanically. Record the per-stratum agree/n/needs table, the overall result, and every withheld stratum. On a partial pass, write the withhold file and run a 30-item re-audit from the non-withheld remainder, which needs ≥ 26/30.
5. Write the decision as one line: `**Decision**: PUBLISH`, `**Decision**: PUBLISH-PARTIAL (withheld: …)` or `**Decision**: NO-PUBLICATION`.
6. On NO-PUBLICATION, skip Task 3 entirely: no apply, no data commit, no push, no R2, no disclosure line. Update deferred-live: "backfill still withheld after the Phase-36 classifier (numbers)".
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && grep -nE "^\*\*Decision\*\*: (PUBLISH|PUBLISH-PARTIAL|NO-PUBLICATION)" .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md && grep -nE "^overall: [0-9]+/50" .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md</automated>
    Expected: the decision and overall lines are present, and the decision follows mechanically from G-11/G-14.
  </verify>
  <done>The publication decision uses the Phase-34 gate verbatim on an out-of-sample audit.</done>
</task>

<task type="auto">
  <name>Task 3: Apply + verify + disclose + push + R2 (only on PUBLISH / PUBLISH-PARTIAL)</name>
  <files>data/incidents/current.json, data/incidents/archive/2026-09.json, data/incidents/rejected/2026-09.json, site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro, .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md</files>
  <action>
1. `git pull --rebase`. Run `--apply --cache $SCR/bf36-cache.jsonl [--withhold-ids F] --report $SCR/bf36-report.json --spend-ledger $SCR/bf36-ledger.json`. Record the report: accepted_in_window, accepted_archived, by_stage (including editorial_filter and dedup_duplicate), and last_new_incident_at before == after.
2. Verify all of the following:
   - `python pipeline/validate_news_dedup.py data/incidents/current.json` → 0 drops;
   - `python pipeline/check_hotfix_regression.py --data data/incidents` → exit 0;
   - every newly added incident has title_src with title_es == title_src;
   - an idempotency re-run of --apply changes no file (`git diff --stat` identical).
3. Methodology line (sonnet), per the owner default "Backfilled incidents: keep original dates + one bilingual line". Add one sentence to the 36-03 news-headlines section:
   - EN: "Incidents published between 2026-09-04 and 2026-09-23 were collected during a classifier outage and classified later, on <apply date>; they keep their original publication dates."
   - ES: "Los incidentes publicados entre el 2026-09-04 y el 2026-09-23 se recolectaron durante una falla del clasificador y se clasificaron después, el <fecha de aplicación>; conservan su fecha de publicación original."
4. Gate: full pytest (exit 0), then from site/ in ONE command `npm run build && npm run validate; npx vitest run`. Validators must be 19/19, vitest rc 0 and astro check 0 errors.
5. Commit the data plus the methodology change: `data: publish Phase-36 backfill (G-11/G-14 PASS)`. Then `git pull --rebase` and `git push` (never --force). Confirm the deploy-on-code run succeeded (human data push, R-04).
6. `gh workflow run r2-archive.yml`. Watch it to success, and record the run id.
7. Curl prod: /news/ shows incidents dated inside 2026-09-05..09-22 for rows still in the window, and check_hotfix_regression.py --html on the curled pages exits 0.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && if grep -q "^\*\*Decision\*\*: NO-PUBLICATION" .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md; then echo "skipped (no publication)"; git diff --quiet HEAD -- data/incidents/current.json; else python pipeline/validate_news_dedup.py data/incidents/current.json && python pipeline/check_hotfix_regression.py --data data/incidents && grep -nE "^r2_run: [0-9]+" .planning/phases/36-classification-fidelity-attribution/36-BACKFILL-AUDIT.md; fi</automated>
    Expected: on NO-PUBLICATION there is no data diff. On publish, dedup is 0, FID-07 exits 0, and the R2 run is recorded.
  </verify>
  <done>Either the backfill is published honestly (dates kept, disclosed, deduped, FID-07 intact, R2 regenerated), or it stays withheld with measured numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local backfill → data/ → prod | bulk publication of ~1,000 cards |
| local → paid API | spend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-29 | Repudiation / integrity | publishing non-crimes at scale | mitigate | The G-11/G-14 gate is applied unchanged by a fresh auditor on an out-of-sample stratified draw. Withhold-by-stratum applies, and NO-PUBLICATION is the default. |
| T-36-30 | Tampering | masking a live outage via freshness evidence | mitigate | bump_last_new=False plus the run_apply last_new equality check (G-07(a)). |
| T-36-31 | Tampering | irreversible data loss | mitigate | Scratchpad backup + dry-run first. The git commit is revertible, and seen.json is untouched. |
| T-36-32 | Denial of service (budget) | classify spend | mitigate | Cap ≤ min(1.00, 5 − phase spend), with exit 3 → pause. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
- The decision is recorded.
- On publish: dedup is 0, FID-07 exits 0, the gate is green, the push and deploy are verified, and R2 is dispatched.
- On no-publication: data/ has no diff.
</verification>

<success_criteria>
The G-18 hand-off is executed: the outage items are published under the unchanged audit gate with the Phase-36 classifier, or they stay withheld with measured evidence.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-10-SUMMARY.md` when done.
</output>
