---
phase: 36-classification-fidelity-attribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/news/schema.py
  - pipeline/news/store.py
  - pipeline/news/feeds.py
  - pipeline/tests/test_schema_incidents.py
  - pipeline/tests/test_store.py
  - pipeline/tests/test_feeds.py
autonomous: true
requirements: [FID-01, FID-06, FID-04]
executor: opus (schema.py / store.py data contract + feeds.py ingest kernel)

must_haves:
  truths:
    - "IncidentRecord accepts two new OPTIONAL fields, title_src and via_url. The 798 legacy rows in data/incidents/current.json (neither field) still validate, so the next cron run cannot raise (research Pitfall 1)."
    - "build_incident(title_src=X, …) returns title_src == X and title_es == X (G-28: title_es mirrors the verbatim headline for new rows). The legacy call build_incident(title_es=Y, …) returns the exact dict it returns today, with no title_src or via_url key."
    - "feeds.source_headline(title, outlet) returns the headline with html entities decoded, whitespace collapsed and exactly one trailing ' - <outlet>' suffix removed. That suffix is present on 2,212 of 2,212 Google-News rows in rejected/*.json (measured 2026-09-24; research assumption A1 is now verified)."
    - "feeds.strip_html decodes HTML entities after removing tags, and &nbsp; becomes a plain space. Every caller gets this: feeds.py:196 (is_crime_item) and scrape_news.py:358 (the classify input) (FID-06, research Pitfall 4)."
    - "fetch_feed logs a feedparser NonXMLContentType bozo (BioBio application/octet-stream) at DEBUG when entries were parsed. Every other bozo stays at WARNING (FID-06)."
  artifacts:
    - path: pipeline/news/schema.py
      provides: "IncidentRecord.title_src / via_url (optional) + validators"
      contains: "title_src: str | None = None"
    - path: pipeline/news/store.py
      provides: "build_incident(..., title_src=None, title_es=None, via_url=None)"
    - path: pipeline/news/feeds.py
      provides: "source_headline(), entity-decoding strip_html(), quiet NonXMLContentType"
      exports: ["source_headline", "strip_html"]
  key_links:
    - from: pipeline/news/store.py build_incident
      to: pipeline/news/schema.py IncidentRecord
      via: "dict keys title_src / via_url validated by validate_incidents_file"
      pattern: "title_src"
    - from: pipeline/news/feeds.py is_crime_item
      to: pipeline/news/feeds.py strip_html
      via: "the single choke point for entity decoding"
      pattern: "html.unescape"
---

<objective>
Lay the Phase-36 data contract and ingest helpers. Later plans wire them in: 36-04 (scrape and backfill), 36-05 (dedup), 36-07 (Google News decode) and 36-03 (UI).

- FID-01: store the outlet's verbatim headline as `title_src`.
- FID-04: add a place for the Google News link (`via_url`).
- FID-06: decode HTML entities before classification and silence the BioBio content-type bozo.

This plan does not touch the classifier and changes no caller. Every existing call keeps working, which proves back-compat.

Purpose: build_incident has no title_src parameter (store.py:51-63). The RSS title is dropped at the call site (scrape_news.py:474-485). The rejected ledger has entities in 2,782 of 4,427 descriptions (measured 2026-09-24: `&nbsp;` 4,424×, `&#8230;` 564×, `&#8220;` 118×) and in 0 titles. strip_html only removes tags (feeds.py:117-119).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/36-classification-fidelity-attribution/36-RESEARCH.md
@pipeline/news/schema.py
@pipeline/news/store.py
@pipeline/news/feeds.py

<interfaces>
Evidence at HEAD 9e3abaf (read 2026-09-24):
- schema.py:48-61 IncidentRecord: id, cut, lat, lng, title_es, title_en, date, outlet, url, family, `slug: str | None = None` (the optional back-compat pattern at :61). The url validator at :84-91 requires http/https. There is no title_src or via_url field anywhere in pipeline/ (research: grep returns 0).
- schema.py:140-142 validate_incidents_file validates the WHOLE file (all-or-nothing, D-15). store.py:254-261 skips the write on ValidationError.
- store.py:51-85 build_incident(*, url, cut, lat, lng, title_es, title_en, date, outlet, family, slug=None). It returns None for a non-http(s) url (:70-72), and it has two callers: scrape_news.py:474-485 and backfill_classifier_outage.py:659-665.
- store.py:263-272 is the FRESH-04 no-op guard. It compares previous_payload["incidents"] with the new list by equality, so legacy dicts must stay byte-for-byte identical in shape, with no new keys added to legacy rows.
- feeds.py:117-119 strip_html (tags only). feeds.py:141-159 resolve_outlet (the Google News outlet comes from <source>). feeds.py:166-183 fetch_feed logs every bozo at WARNING (:178-179). feeds.py:190-198 is_crime_item calls strip_html (:196).
- Measured 2026-09-24 over data/incidents/rejected/*.json (4,427 rows): Google-News rows 2,212, all with title ending " - " + outlet; titles with entities 0; descriptions with entities 2,782.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IncidentRecord title_src/via_url + build_incident compat signature</name>
  <files>pipeline/news/schema.py, pipeline/news/store.py, pipeline/tests/test_schema_incidents.py, pipeline/tests/test_store.py</files>
  <behavior>
    - A legacy row (no title_src / via_url) validates. The real file validates too: validate_incidents_file(json.load(data/incidents/current.json)) → no error (798 rows at 9e3abaf).
    - title_src="" or "   " → ValidationError. via_url="javascript:x" → ValidationError. via_url=None or absent → OK. title_src longer than 400 chars → ValidationError.
    - build_incident(url=U, title_src="Detienen a yerno de X", title_en="…", …) → dict with title_src == title_es == "Detienen a yerno de X" and no via_url key. With via_url=G → dict["via_url"] == G.
    - build_incident(url=U, title_es="Y", title_en="…", …) (legacy) → the dict equals today's output exactly (same keys, same order; no title_src, no via_url).
    - Both title_src and title_es given → title_src wins, and title_es == title_src. Neither given → ValueError.
    - build_incident with a non-http via_url → returns None and logs a warning, like the url rule at store.py:70-72.
    - Every existing test_store.py and test_schema_incidents.py test passes unedited.
  </behavior>
  <action>
Step 0, precondition: `grep -c '^- \[x\] \*\*Phase 35' .planning/ROADMAP.md` must print 1. If it does not, STOP without editing and report "Phase 35 not closed (standing order: one phase at a time)".

schema.py IncidentRecord changes. Add after `slug` (per G-28 as proposed in this phase's plans):
- `title_src: str | None = None`, the outlet's verbatim headline (FID-01). Its field_validator applies only when not None: strip() must be non-empty, and len ≤ 400.
- `via_url: str | None = None`, the Google News link when url is the decoded publisher URL (FID-04). Its field_validator applies only when not None: it must start with http:// or https://, mirroring :84-91.
- Update the title_es comment. For rows with title_src, title_es mirrors title_src verbatim. For legacy rows it is the Granite/DeepSeek-era LLM headline.
- Do NOT touch ClassifierOutput here (36-06 owns it).

store.py build_incident: make the signature keyword-only with `title_en` required and `title_src: str | None = None`, `title_es: str | None = None`, `via_url: str | None = None` optional. Rules:
- If title_src is given: title_es := title_src, and emit the key "title_src".
- elif title_es is given: legacy shape, exactly today's dict.
- else raise ValueError("build_incident needs title_src (FID-01)").
- Emit "via_url" only when it is not None, and require is_safe_url(via_url) or return None.
- Keep the key order of today's dict for the legacy path, so existing tests and the FRESH-04 equality guard see identical dicts. For the new path, append title_src (and via_url) after slug.
- Update the docstring with FID-01/G-28.

36-04 switches both callers to title_src and then removes the legacy title_es parameter. Do not remove it here.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_schema_incidents.py pipeline/tests/test_store.py pipeline/tests/test_scrape_news.py pipeline/tests/test_backfill_classifier_outage.py -q -p no:cacheprovider && python -c "import json,sys; sys.path.insert(0,'.'); from pipeline.news.schema import validate_incidents_file as v; v(json.load(open('data/incidents/current.json',encoding='utf-8'))); print('current.json validates')"</automated>
    Expected: exit 0. test_scrape_news and test_backfill are unedited and green, which proves the legacy callers are unaffected. The last line prints "current.json validates". Negative control: make title_src required (`title_src: str`), and the current.json validation line must FAIL. Revert.
  </verify>
  <done>The new optional fields validate. The legacy shape is byte-identical, the new shape mirrors title_es, and the real current.json still validates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: feeds.py — source_headline, entity-decoding strip_html, quiet NonXMLContentType (FID-01/FID-06)</name>
  <files>pipeline/news/feeds.py, pipeline/tests/test_feeds.py</files>
  <behavior>
    - strip_html("<p>Duane &#8220;Keffe D&#8221; Davis&nbsp;fue</p>") == "Duane “Keffe D” Davis fue", with no "&#" and no "\xa0" in the output.
    - strip_html("") == "" and strip_html(None) == "". Tag removal runs BEFORE unescape, so "&lt;b&gt;x" gives "<b>x" (literal text, never re-parsed).
    - is_crime_item({"title": "x", "description": "homicidio&nbsp;en"}) stays True, and the existing keyword tests are unchanged.
    - source_headline("Detienen a sujeto en Calama - soychile.cl", "soychile.cl") == "Detienen a sujeto en Calama".
    - source_headline("A - B - La Tercera", "La Tercera") == "A - B". Only ONE trailing suffix is stripped.
    - source_headline("Robo en Temuco", "BioBioChile") == "Robo en Temuco" (no suffix, unchanged). source_headline(" - La Tercera", "La Tercera") returns the input stripped (it never returns an empty string). source_headline("x  &amp;  y", "") == "x & y".
    - fetch_feed with feedparser.parse patched to return bozo=1, bozo_exception=feedparser.NonXMLContentType("application/octet-stream is not an XML media type") and 2 entries → returns the 2 entries, with no WARNING record and one DEBUG record. The same with a generic Exception bozo → a WARNING record (unchanged behaviour). NonXMLContentType with 0 entries → WARNING.
  </behavior>
  <action>
Step 0: run `python -c "import feedparser; print(feedparser.NonXMLContentType)"` and cite the printed class in the SUMMARY. If the attribute does not exist, import it from `feedparser.exceptions` and cite that instead.

feeds.py:
- `import html`.
- strip_html(text) returns `html.unescape(re.sub(r"<[^>]+>", "", text or "")).replace("\xa0", " ")`. Update the docstring to cite FID-06/V-19 and the measured 2,782/4,427 rows.
- New `source_headline(title: str, outlet: str) -> str`:
  1. t = collapse whitespace of html.unescape(title or "") and strip;
  2. if outlet.strip() is non-empty and t ends with " - " + outlet.strip() and the remainder, after rstrip, is non-empty, return the remainder rstripped;
  3. else return t.
  The rule is feed-agnostic on purpose, so queued (pending.json) and backfill rows, which carry title + outlet but no feed name, derive the same title_src. Cite the measured 2,212/2,212 Google-News rows.
- fetch_feed: when d.bozo is set, if isinstance(d.bozo_exception, NonXMLContentType) and d.entries is non-empty, log at DEBUG as "[%s] non-XML content-type ignored (entries parsed): %s". Otherwise keep the existing WARNING line.

Add the behavior tests to test_feeds.py. Patch `pipeline.news.feeds.feedparser.parse` with a feedparser.FeedParserDict carrying bozo/bozo_exception/entries, and use caplog.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_feeds.py pipeline/tests/test_scrape_news.py -q -p no:cacheprovider && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider; echo "pytest rc=$?"</automated>
    Expected: both runs exit 0 (rc=0). The full suite is ≥ 642 passed / 1 skipped / 2 xfailed (the Phase-35 close baseline) + this plan's new tests, with 0 failed, counted by exit code. Negative control: revert strip_html to tags-only, and the entity test must FAIL. Restore.
  </verify>
  <done>Entities are decoded at the single choke point. title_src derivation is deterministic and feed-agnostic. The BioBio content-type noise is at DEBUG only when entries parsed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| RSS feed text → pipeline | titles and descriptions are third-party, attacker-influenceable text |
| pipeline → current.json → static HTML | stored strings are rendered by 6 site consumers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering / XSS | title_src (verbatim, unescaped) | mitigate | Stored as plain text. Every consumer escapes on render: Astro text nodes, IncidentPinLayer escHtml (IncidentPinLayer.ts:118), React text, textContent in HomeNewsPulse (:245). 36-03 must keep those paths (no set:html/innerHTML). Unescape happens after tag stripping, so decoded "<" is literal text. |
| T-36-02 | Tampering | via_url scheme | mitigate | The schema validator and build_incident enforce http/https, mirroring url (schema.py:84-91, store.py:70-72). |
| T-36-03 | Denial of service | schema change breaks the next cron write | mitigate | Both fields are optional. The real current.json validation is part of verify. |
| T-36-SC | Tampering | package installs | accept | No new dependency (stdlib `html` only). |
</threat_model>

<verification>
- The full pytest run is green at ≥ 642/1/2 + new tests (exit code).
- The real current.json validates with the new schema.
- `git diff --stat` touches only the 6 listed files.
</verification>

<success_criteria>
The contract (title_src, via_url) and the ingest helpers (source_headline, entity-decoding strip_html, quiet NonXMLContentType) exist and are tested. Legacy behaviour is byte-identical.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-01-SUMMARY.md` when done.
</output>
