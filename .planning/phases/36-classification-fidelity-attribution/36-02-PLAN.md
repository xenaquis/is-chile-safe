---
phase: 36-classification-fidelity-attribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/experiments/eval_classifier.py
  - pipeline/tests/test_eval_classifier.py
  - pipeline/tests/fixtures/golden_set_v3.json
  - pipeline/tests/test_golden_v3.py
  - .planning/phases/36-classification-fidelity-attribution/36-GOLDEN-V3.md
  - .planning/phases/36-classification-fidelity-attribution/eval/
autonomous: true
requirements: [FID-02]
executor: sonnet (eval tooling + fixture assembly); fresh opus = blind labeller (Task 2); orchestrator = baseline eval run (Task 3)

must_haves:
  truths:
    - "pipeline/tests/fixtures/golden_set_v3.json exists. It holds the 47 golden_set_v2.json items byte-identical in id/headline/description/ground_truth, plus ≥ 20 keyword-passing non-crime items (target 24) and ≥ 8 labelled vida/vif/robos_violentos/sexuales/propiedad boundary items. All new items are exact production inputs taken from data/incidents/rejected/*.json."
    - "golden_set_v2.json is untouched. It stays the Phase-34 provenance set and the DEPS-03 (Phase 37) reproduction set, and eval_classifier.py DEFAULT_GOLDEN stays v2 (eval_classifier.py:71)."
    - "`eval_classifier.py --score RUN --golden V3` computes, offline: the FID-02 non-crime rejection (production semantics: low_conf, or commune null/unresolvable), commune/family on the 44 v2-labelled ids, boundary family, parse/empty/finish_length counts, and a family confusion matrix with a not_crime row. It writes .planning/phases/36-…/36-EVAL-*.json/md and NEVER the Phase-34 RESULTS files."
    - "A baseline run of the CURRENT prompt on v3 is recorded (36-EVAL-BASELINE.md) before 36-06 changes the prompt."
  artifacts:
    - path: pipeline/tests/fixtures/golden_set_v3.json
      provides: "FID-02 gate set"
    - path: pipeline/experiments/eval_classifier.py
      provides: "score_fidelity() + --score CLI; run_full rejected_in_prod per null/not_crime item"
    - path: .planning/phases/36-classification-fidelity-attribution/36-GOLDEN-V3.md
      provides: "fixture policy + per-item label reasons"
  key_links:
    - from: eval_classifier.py score_fidelity
      to: golden_set_v3.json ground_truth.not_crime / boundary
      via: "subset selection by flag, v2 ids by membership in golden_set_v2.json"
      pattern: "not_crime"
---

<objective>
FID-02 foundation: grow the golden set with keyword-passing non-crimes and boundary items (as a NEW fixture version), add the non-crime-rejection metric and a confusion matrix to the eval runner, and record the current prompt's baseline on the new set.

Purpose:
- V-07/V-08: the v2 set has only 3 null items (weather, recycling, hospital), none of them keyword-passing crime look-alikes, so non-crime rejection is unmeasured.
- G-18: the live model accepts accidents, natural deaths, fires and preventive notices (G-11 audit q1 40/50).
- The eval runner scores null items as null_correct only (eval_classifier.py:559-565), and its decide() writes Phase-34 files (:937-940).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/36-classification-fidelity-attribution/36-RESEARCH.md
@.planning/phases/34-news-classification-restore/34-BACKFILL-AUDIT.md
@.planning/phases/34-news-classification-restore/34-GOLDEN-V2-RELABEL.md
@pipeline/experiments/eval_classifier.py
@pipeline/tests/test_eval_classifier.py

<interfaces>
Evidence at HEAD 9e3abaf:
- eval_classifier.py:
  - :65 FAMILY_MIN_V2 = 40 (G-15); :71 DEFAULT_GOLDEN = golden_set_v2.json;
  - :446-622 run_full. Labelled items are scored at :545-558 (commune/family counted only when status ok). Null items at :559-565: null_correct = low_conf or (ok and commune_name None).
  - :848-858 `_family_confusion_matrix` (labelled rows only);
  - :914-949 decide() → select_winner + writes RESULTS_JSON/RESULTS_MD (Phase-34 paths :72-74);
  - :973-1041 main: `--golden`, `--out-dir`, `--spend-ledger`, `--spend-cap` (default 0.50), `--reasoning`. It writes `run-<slug>.json` in out-dir (:1032), and the name is overwritten on every run of the same model.
- Golden item shape (golden_set_v2.json, 47 items; family counts robos_violentos 10, drogas 8, vida 7, propiedad 6, armas 5, vif 4, incivilidades 4, null 3): {id, headline, description, ground_truth{commune_name, cut, family}, source_outlet, source_url}.
- The production inputs of new items come from data/incidents/rejected/*.json rows {id, url, outlet, date, title, description, rejection_stage, first_seen} (4,427 rows, 2026-09-24). Titles keep the Google-News " - outlet" suffix exactly as the classifier receives them (scrape_news.py:356/493).
- Candidate non-crime ids with stored inputs (verified present, 25/25, 2026-09-24). Verdicts are from 34-BACKFILL-AUDIT.md:
  - accidents: 92cd35fd1138995c, 1c49c011fc62c312 (landmine); 09fb832af5811fae (rollover); 5cf2a3a5d46c25a7, c92089ceee13bb8a, 55a4ffa1afc805b3, 3d3ea292cab52e9a, b1257359c32a8956, e89d73a9a375d4a2, dfa687821128d4e5 (traffic/vehicle); be407ac7f70b35ba (fall); cd93b9eed2592cce (volcano);
  - deaths without an established crime: de1e92f61b62df4f, 0be6b9e7f7ee99a9, 635bdfeebf2f1033;
  - fires/emergencies: 6d95b6be9d66266d, 578a89d569037cb8, af9b5c6730c423f2;
  - preventive/institutional: 183889a4fe9aef1c, 5a94f9f836618ec3, dd462453224e1b11.
  Boundary candidates: 4c8b86eb7d7f374e ("robo con violación", labelled vida), b2f330935b07d752 (truck robbery, no violence), 45bf5b41fc7f3eb2 and a13b289a57848648 (accidental death in a stolen car).
- Regex availability in rejected/*.json (2026-09-24): suicide cue (`suicid|quit\w* la vida`) 6 rows; institutional cue (`reuni[oó]n|seminario|anuncia|plan de seguridad|balance|mesa de trabajo|cuenta p[uú]blica`) 151 rows.
- Spend basis: G-16 measured $0.010017 / 47 items ≈ $0.000213/item; G-18 $0.410796 / 2,124 calls ≈ $0.000193. Planning figure $0.00025/item (+~15% for a longer prompt).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: eval_classifier.py — rejected_in_prod + score_fidelity + --score CLI (offline)</name>
  <files>pipeline/experiments/eval_classifier.py, pipeline/tests/test_eval_classifier.py</files>
  <behavior>
    - run_full on a null or not_crime item records row["rejected_in_prod"] = (kind == "low_conf") or (kind == "ok" and (out.commune_name is None or resolve_cut(out.commune_name, out.region_hint) is None)), and row["predicted_family"] when out exists. The existing null_correct semantics and counts are unchanged, so 34-AB numbers reproduce (DEPS-03).
    - score_fidelity(candidate, golden_items, v2_ids) on a synthetic candidate:
      - not_crime_total/rejected/rate count only items with ground_truth.not_crime == true;
      - per-category counts;
      - v2 commune/family recomputed over the v2 labelled ids only;
      - boundary family over ground_truth.boundary == true;
      - parse_errors, empty_content_count and finish_length_count taken from the candidate;
      - null_v2_correct over the 3 v2 null ids;
      - confusion matrix rows = ground-truth family, or "not_crime", with columns = predicted family, "rejected", "parse_error", "api_error" or "transient".
    - gate dict: not_crime_rate ≥ 0.80 AND v2 commune ≥ 42 AND v2 family ≥ 40 AND parse_errors == 0 AND empty == 0 AND finish_length == 0 AND null_v2_correct == 3. Each member is reported as a bool, plus overall pass.
    - Reproducibility pin: score_fidelity over the committed Phase-34 run `.planning/phases/34-news-classification-restore/ab/run-deepseek-deepseek-v4-1-flash.json` with golden v2 gives v2 commune 44 and family 42 (G-16).
    - `main(["--score", RUN, "--golden", V3, "--out-json", J, "--out-md", M])` makes no network call and writes J/M. It never writes RESULTS_JSON/RESULTS_MD (assert their mtimes are unchanged).
  </behavior>
  <action>
Implement exactly the behavior above. Details:
- Declare the module constants: FID02_NOT_CRIME_MIN_RATE = 0.80 (REQUIREMENTS FID-02), COMMUNE_MIN_V2 = 42 (G-06), and FAMILY_MIN_V2 = 40 (existing, G-15). PARSE_MAX = 0 (G-06 strict).
- v2_ids is loaded from golden_set_v2.json (labelled ids = ground_truth.commune_name not None; null ids = the rest).
- The markdown lists the gate table, the per-category table, and the confusion matrix, and prints the run's spend.
- Add `--score` to the mutually-independent CLI branch next to `--decide`. Do NOT route it through decide() or select_winner().
- Keep DEFAULT_GOLDEN = v2 (Phase-37 DEPS-03 reproduces NREC-01 on v2).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_eval_classifier.py -q -p no:cacheprovider && git diff --quiet -- .planning/phases/34-news-classification-restore/</automated>
    Expected: exit 0, and the Phase-34 directory is unchanged. Negative control: count parse_error as rejected_in_prod, and the rate test must FAIL. Revert.
  </verify>
  <done>Non-crime rejection, subset metrics and the not_crime confusion row are computable offline. The Phase-34 artifacts and the v2 reproduction are intact.</done>
</task>

<task type="auto">
  <name>Task 2: golden_set_v3.json — blind fresh-opus labelling of stored production inputs</name>
  <files>pipeline/tests/fixtures/golden_set_v3.json, pipeline/tests/test_golden_v3.py, .planning/phases/36-classification-fidelity-attribution/36-GOLDEN-V3.md</files>
  <action>
Step 0: the same Phase-35-closed precondition as 36-01 Step 0.

1. Candidate pool (sonnet, script in the scratchpad):
   - all 25 ids listed in <interfaces>;
   - every rejected/*.json row matching the suicide cue;
   - up to 40 rows matching the institutional cue.
   Dedupe by id. Keep only rows where feeds.is_crime_item({"title", "description"}) is True, with the description run through html.unescape and "\xa0"→" " (FID-06 parity; do not import 36-01 code, which may not be merged yet).

2. Blind labelling (a FRESH opus subagent, taxonomy from pipeline/cache/cead_page.html as in G-15). For every pool row it gets title + unescaped description only (no model output, no stage) and returns:
   - {id, not_crime: bool, category ∈ {accident, suicide, death_no_crime, fire_emergency, institutional_preventive, other_non_crime} when not_crime, else commune_name + family, boundary: bool, reason}.
   Families are the 8 news families (7 CEAD + sexuales, schema.py:22). The reviewer must never be told the thresholds.

3. Assemble v3:
   - the 47 v2 items copied byte-identical (no relabel; a relabel needs a written reason in 36-GOLDEN-V3.md and is expected to be 0);
   - then new items with id "gs3-NNN" and fields {headline: raw stored title, description: unescaped stored description, ground_truth: {commune_name, cut (via pipeline.news.resolver.resolve_cut for labelled items; the item is dropped if unresolvable), family (null when not_crime), not_crime, category, boundary}, source_outlet, source_url, source_id, label_reason}.
   - Quotas for not_crime items: accident ≥ 6, suicide ≥ 3, death_no_crime ≥ 3, fire_emergency ≥ 2, institutional_preventive ≥ 6. Total ≥ 20, target 24.
   - Boundary labelled crime items ≥ 8, including 4c8b86eb7d7f374e and b2f330935b07d752 when the labeller keeps them as crimes.
   - If a quota cannot be met from stored inputs, STOP and report the shortfall; never synthesize text.

4. test_golden_v3.py (structure only, no data/ reads):
   - ids unique;
   - the first 47 equal golden_set_v2.json;
   - ≥ 20 not_crime items, each passing is_crime_item on (headline, description);
   - quotas met;
   - ≥ 8 boundary items with valid family and a cut in VALID_CUTS;
   - no description contains "&#" or "&nbsp;".

5. 36-GOLDEN-V3.md records the fixture policy, per G-28 as proposed in this phase's plans:
   - v2 is frozen (Phase-34 provenance + DEPS-03);
   - v3 is the Phase-36 gate set;
   - further growth goes to v4 and never edits v3 in place.
   It also carries a per-item table (id, source_id, category/family, reason) and the list of v3 source_ids. 36-08 and 36-10 exclude those source_ids from their audit samples (leakage guard).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_golden_v3.py -q -p no:cacheprovider && git diff --quiet -- pipeline/tests/fixtures/golden_set_v2.json && python -c "import json; g=json.load(open('pipeline/tests/fixtures/golden_set_v3.json',encoding='utf-8')); nc=[x for x in g if x['ground_truth'].get('not_crime')]; b=[x for x in g if x['ground_truth'].get('boundary')]; print('total',len(g),'not_crime',len(nc),'boundary',len(b))"</automated>
    Expected: pytest exit 0; v2 unchanged; the printed not_crime count ≥ 20 and boundary ≥ 8.
  </verify>
  <done>v3 exists with measured composition, blind labels and reasons. v2 is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Baseline eval of the CURRENT prompt on v3 (orchestrator)</name>
  <files>.planning/phases/36-classification-fidelity-attribution/eval/, .planning/phases/36-classification-fidelity-attribution/36-EVAL-BASELINE.md</files>
  <action>
Spend: estimated N_v3 × $0.00025, about $0.02 for ~79 items. Cap: `--spend-cap 0.10`. Before running, read RUN STATUS spend (USD 0.53) and confirm that phase-36 spend + 0.10 ≤ 5.00; if not, pause point 4.

Run from the repo root:
`python pipeline/experiments/eval_classifier.py --provider openrouter --model deepseek/deepseek-v4.1-flash --reasoning enabled_false --golden pipeline/tests/fixtures/golden_set_v3.json --out-dir .planning/phases/36-classification-fidelity-attribution/eval --spend-ledger .planning/phases/36-classification-fidelity-attribution/eval/spend-ledger.json --spend-cap 0.10`

Then rename run-deepseek-deepseek-v4-1-flash.json to run-baseline.json, and run `--score .../eval/run-baseline.json --golden pipeline/tests/fixtures/golden_set_v3.json --out-json .../36-EVAL-BASELINE.json --out-md .../36-EVAL-BASELINE.md`.

Record in the SUMMARY: the baseline non-crime rate, v2 commune/family (expected 44/42 ± provider noise), boundary family count (this is the no-regression floor for 36-06), and spend. The baseline is NOT gated. It is the reference 36-06 must beat.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && test -s .planning/phases/36-classification-fidelity-attribution/36-EVAL-BASELINE.md && python -c "import json; d=json.load(open('.planning/phases/36-classification-fidelity-attribution/eval/spend-ledger.json')); print('spend', d['total_usd']); assert d['total_usd'] <= 0.10"</automated>
    Expected: the file exists and spend ≤ 0.10.
  </verify>
  <done>The current prompt's non-crime rejection, subset metrics and confusion matrix on v3 are recorded, with spend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| third-party news text → committed test fixture | stored press snippets are copied into the repo (they are already public in rejected/, V-17) |
| eval runner → paid API | spend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Repudiation / integrity | labels tuned to the model | mitigate | The labeller is blind (inputs only, no model output, no thresholds), and reasons are recorded per item. v3 source_ids are excluded from the 36-08 and 36-10 audit samples. |
| T-36-05 | Denial of service (budget) | eval spend | mitigate | --spend-cap 0.10 with a spend ledger. Phase spend is checked against the USD 5 pause rule before the run. |
| T-36-06 | Tampering | Phase-34 evidence overwritten | mitigate | --score never calls decide(). A git diff guard on the 34 directory runs in verify. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
- pytest for test_eval_classifier and test_golden_v3 exits 0.
- The v2 fixture and the Phase-34 directory have no diff.
- 36-EVAL-BASELINE.md exists and spend ≤ 0.10.
</verification>

<success_criteria>
A blind-labelled v3 gate set with ≥ 20 keyword-passing non-crimes and ≥ 8 boundary items, an offline FID-02 scorer, and a recorded baseline for the current prompt.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-02-SUMMARY.md` when done.
</output>
