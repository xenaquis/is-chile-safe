---
phase: 36-classification-fidelity-attribution
plan: 04
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - pipeline/news/fidelity.py
  - pipeline/tests/test_fidelity.py
  - pipeline/scrape_news.py
  - pipeline/tests/test_scrape_news.py
  - pipeline/backfill_classifier_outage.py
  - pipeline/tests/test_backfill_classifier_outage.py
  - pipeline/news/store.py
  - pipeline/tests/test_store.py
autonomous: true
requirements: [FID-01, FID-06, FID-07]
executor: opus (pipeline kernel: scrape_news OK path, backfill apply, store.build_incident contract)

must_haves:
  truths:
    - "Every incident built by scrape_news or the backfill carries title_src = feeds.source_headline(feed title, outlet). title_es == title_src, and the classifier's title_es is never read (FID-01: the LLM no longer authors the Spanish headline)."
    - "title_en passes a deterministic guard before storage. A trailing ' - <outlet>' is stripped, and a kinship-term mismatch between title_src and title_en replaces title_en with title_src. The V-06 case (yerno → 'Grandfather') and the G-27 case (madre → 'grandmother') both fall back."
    - "An item whose title_src or title_en contains a forbidden-language term (list pinned to site/scripts/validate/forbidden-language.mjs:35-61) is not published. It is recorded in rejected/ with stage 'editorial_filter' and counted as a downstream reject (G-29 as proposed; measured 2 of 4,427 raw titles)."
    - "The description handed to router.classify is entity-free in the live path (via strip_html, 36-01) AND in the backfill path (run_classify now strips/unescapes the stored description) (FID-06)."
    - "The run summary gains exactly one key, `fidelity` = {title_en_fallbacks, editorial_filtered}. Scalar GITHUB_OUTPUT lines are unchanged, because dicts are skipped (scrape_news.py:215-216)."
    - "build_incident no longer accepts a legacy title_es argument. Both production callers pass title_src."
    - "The live OK path handles a None or schema-invalid incident per row, like the backfill twin (backfill_classifier_outage.py:665-677): build_incident → None gives rejected stage 'url_rejected'; IncidentRecord.model_validate failure gives stage 'invalid_record'. One bad row never aborts the run (deduplicate at scrape_news.py:563 would raise on None) and never makes merge_and_write skip the whole write (store.py:254-261) (premortem R-14)."
    - "The kinship guard does not fire on commune names or on non-kinship words for children: commune names (Padre Las Casas, Padre Hurtado, …) are masked before matching; niño/niña/menor/bebé/guagua satisfy an EN child/children/minor/baby/kid; plural and colloquial forms (hijos/hijas, padres, mamá, papá, pareja) are mapped. The fallback rate over the legacy (title_es, title_en) pairs is measured offline and recorded (premortem R-13)."
    - "Review and audit artifacts show what gets published: run_review and --audit-sample emit title_src (feeds.source_headline) and the guarded title_en + title_en_fallback, never the classifier's title_es (premortem R-16)."
  artifacts:
    - path: pipeline/news/fidelity.py
      provides: "guard_title_en(), kinship_mismatch(), forbidden_term(), FORBIDDEN_TERMS"
      exports: ["guard_title_en", "kinship_mismatch", "forbidden_term"]
    - path: pipeline/scrape_news.py
      provides: "title_src wiring in the OK path"
      contains: "title_src="
  key_links:
    - from: pipeline/scrape_news.py _answered
      to: pipeline/news/fidelity.py
      via: "guard_title_en(title_src, result.title_en, outlet) + forbidden_term()"
      pattern: "guard_title_en"
    - from: pipeline/backfill_classifier_outage.py run_apply
      to: pipeline/news/store.py build_incident
      via: "title_src=source_headline(row title, row outlet)"
      pattern: "source_headline"
---

<objective>
FID-01 pipeline wiring. The outlet's verbatim headline becomes the stored Spanish headline for every new incident, live and backfill. The English headline is guarded deterministically against the V-06 defect class (kinship errors about named people) and against absolute safety wording. FID-06 gets parity in the backfill classify path. FID-07 is protected at the source: the defect class behind the t59/G-27 hotfixes cannot re-enter through title_en.

Purpose:
- V-06: the item["title"] available in scope is never passed to build_incident (scrape_news.py:474-485), and the backfill does the same (backfill_classifier_outage.py:659-665).
- G-18 audit: 1/50 title_en inverted victim and attacker.
- G-26/G-27: an EN/ES kinship error about a minor reached prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/36-classification-fidelity-attribution/36-01-SUMMARY.md
@pipeline/scrape_news.py
@pipeline/backfill_classifier_outage.py
@pipeline/news/store.py

<interfaces>
From 36-01:
- feeds.source_headline(title, outlet) -> str
- feeds.strip_html(text) (decodes entities)
- store.build_incident(*, url, cut, lat, lng, title_en, date, outlet, family, slug=None, title_src=None, title_es=None, via_url=None). With title_src set: title_es := title_src.

Evidence at HEAD 9e3abaf:
- scrape_news.py:
  - :356-358 title/raw_desc/description (strip_html);
  - :373-379 fresh candidate dict {url, title, description, date, outlet};
  - :391-394 queued candidates rebuilt from pending.json with keys (url, title, description, date, outlet);
  - :423-487 `_answered`: NOT_CRIME/PARSE_ERROR → rejected; OK → resolve_cut → centroid → build_incident(title_es=result.title_es, title_en=result.title_en, …) at :474-485;
  - downstream rejects are appended with stage names at :458/:470;
  - :160-186 `_base_summary`; :538-554 summary.update; :215-216 `_emit_summary` skips dict values for GITHUB_OUTPUT.
- test_scrape_news.py:934-940 pins the exact summary key set (24 keys), and :477-480 the required incident fields.
- backfill_classifier_outage.py:
  - :544 `router.classify(row["title"] or "", row["description"] or "", key=row["id"])` sends the stored raw description (entities present in 2,782/4,427 rows);
  - :659-665 build_incident(title_es=out.title_es, title_en=out.title_en, …);
  - :621-656 stage_of mapping (manual_exclusion, low_confidence, parse_error, commune_null, resolver_fail, centroid_fail, url_rejected, invalid_record).
- forbidden-language.mjs:
  - :35-61 FORBIDDEN_TERMS (13 ES + 10 EN = 23 terms; recounted 2026-09-24 at forbidden-language.mjs:35-61);
  - :80-82 accent strip via NFD;
  - :109-112 match with lookbehind/lookahead `(?<![a-z0-9])term(?![a-z0-9])` on lowercased, accent-stripped text.
- Kinship evidence:
  - V-06: slug "detienen-a-yerno-de-rosamel-fierro", title_es "suegro", title_en "Grandfather" (current.json 514872e1d06db7f2 before t59);
  - G-27: title "abuela" vs source "madre" (63fb0c3ef0adca19).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipeline/news/fidelity.py — title_en guard + editorial filter</name>
  <files>pipeline/news/fidelity.py, pipeline/tests/test_fidelity.py</files>
  <behavior>
    - kinship_mismatch("Detienen a yerno de Rosamel Fierro", "Grandfather of Rosamel Fierro arrested") is True. The same with "Son-in-law of Rosamel Fierro arrested" is False.
    - kinship_mismatch("Menor mató a su madre con un martillo", "Minor killed her grandmother with a hammer") is True. With "…killed her mother…" it is False.
    - kinship_mismatch("Detienen a sujeto por robo", "Man arrested for robbery") is False (no kinship on either side).
    - kinship_mismatch("Detienen a sujeto por robo", "Father arrested for robbery") is True (EN introduces kinship absent in ES).
    - Matching is accent- and case-insensitive and word-bounded, so "madrugada" never matches "madre" and "padrón" never matches "padre". Plural and feminine forms are covered (hijos, hijas, hermana, abuelos, suegra, nieta, sobrina, cuñada, esposa, nuera, hijastra, padrastro, madrastra, expareja/ex pareja, conviviente, pololo/polola).
    - False-positive guards (premortem R-13), all must be False:
      - kinship_mismatch("Fiscalía investiga robo en Padre Las Casas", "Prosecutor investigates robbery in Padre Las Casas") (commune names masked on both sides before matching; same for Padre Hurtado);
      - kinship_mismatch("Niño baleado en Puente Alto", "Child shot in Puente Alto");
      - kinship_mismatch("Asaltan a pareja en Maipú", "Couple robbed in Maipú");
      - kinship_mismatch("Detienen a hijos de empresario", "Children of businessman arrested"); kinship_mismatch("Mamá de víctima declara", "Victim's mother testifies").
    - guard_title_en(src, en + " - La Tercera", "La Tercera") strips the suffix. On a mismatch it returns (src, True); otherwise (cleaned en, False). An empty en gives (src, True).
    - forbidden_term("La comuna más peligrosa de Chile") == "mas peligrosa". forbidden_term("Balacera en zona peligrosa") == "zona peligrosa". forbidden_term("the safest city") == "the safest". forbidden_term("un sujeto peligroso") is None.
    - Pin: the FORBIDDEN_TERMS set equals the array parsed from site/scripts/validate/forbidden-language.mjs (regex over the `const FORBIDDEN_TERMS = [` … `];` block, :35-61), so drift fails pytest. The same test also parses the FORBIDDEN_TERMS array of site/src/components/CommuneNewsSection.astro (:81-89, "sync manually" comment at :76-80) and asserts set equality with the forbidden-language.mjs array, so the third copy cannot drift silently either (premortem R-19; the three lists are identical at 9e3abaf).
  </behavior>
  <action>
Create fidelity.py (stdlib only).

KINSHIP maps each ES concept to its accepted EN renderings, for example:
- yerno → son-in-law; suegro/a → father-in-law / mother-in-law / in-law;
- abuelo/a → grandfather / grandmother / grandparent; nieto/a → grandson / granddaughter / grandchild;
- padre → father / dad; madre → mother / mom;
- hijo/a → son / daughter / child; hijos/hijas → children / sons / daughters;
- padres → parents; mamá → mother / mom; papá → father / dad;
- hermano/a → brother / sister / sibling;
- tío/a → uncle / aunt; primo/a → cousin; sobrino/a → nephew / niece;
- esposo/a → husband / wife / spouse; cuñado/a → brother-in-law / sister-in-law; nuera → daughter-in-law;
- hijastro/a → stepson / stepdaughter / stepchild; padrastro → stepfather; madrastra → stepmother;
- pareja → couple / partner; expareja / conviviente / pololo/a → partner / ex-partner / boyfriend / girlfriend.

NON_KIN_CHILD (premortem R-13): the ES non-kinship words {niño, niña, niños, niñas, menor, menores, bebé, guagua} satisfy the EN renderings {child, children, minor, minors, baby, kid, kids} for rule (b) only. They never trigger rule (a), because they are not kinship.

Commune masking (premortem R-13): before any kinship search, replace every commune name in both src and en with a neutral placeholder. The names come from data/cead/meta/index.json (the same file resolver.py:22 and classifier.py:145 load; read it with pathlib + json, stdlib), normalized like the text (NFD accent strip, lowercase), matched longest-first and word-bounded. This removes "Padre Las Casas" / "Padre Hurtado" hits (12/798 current title_es, measured by the premortem).

kinship_mismatch(src, en) is True iff, after masking:
- (a) some ES kinship concept present in src has none of its EN renderings in en; OR
- (b) some EN kinship rendering present in en maps to no concept (kinship or NON_KIN_CHILD) present in src.

guard_title_en(title_src, title_en, outlet) -> tuple[str, bool] does three steps: strip ws, strip one trailing " - {outlet}", then apply the mismatch/empty fallback.

forbidden_term(text) -> str | None replicates the JS matching (NFD accent strip, lowercase, lookaround word boundary). The allow-list (forbidden-language.mjs:67-72) is deliberately NOT applied: headlines must be stricter. Document that the term list is a mirror kept in sync by the pin test.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_fidelity.py -q -p no:cacheprovider</automated>
    Expected: exit 0. Negative control: delete "yerno" from KINSHIP, and the V-06 test must FAIL. Revert. Second negative control: disable commune masking, and the Padre Las Casas test must FAIL. Revert.
  </verify>
  <done>A deterministic guard catches the V-06/G-27 kinship class and absolute safety wording without firing on commune names or non-kinship child words, and its term list is pinned to the site validator and to CommuneNewsSection.astro.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire title_src + guards into scrape_news and the backfill; FID-06 backfill parity; drop legacy build_incident title_es</name>
  <files>pipeline/scrape_news.py, pipeline/tests/test_scrape_news.py, pipeline/backfill_classifier_outage.py, pipeline/tests/test_backfill_classifier_outage.py, pipeline/news/store.py, pipeline/tests/test_store.py</files>
  <behavior>
    - A live run with a Google-News entry titled "Detienen a sujeto en Calama - soychile.cl" (outlet soychile.cl) and a fake OK result (title_en "Man arrested in Calama") stores title_src == title_es == "Detienen a sujeto en Calama" and title_en == "Man arrested in Calama".
    - A fake OK result whose title_en has the wrong kinship stores title_en == title_src, and summary.fidelity.title_en_fallbacks == 1.
    - A fake OK result with a forbidden term in the entry title: 0 incidents, a rejected/ row with rejection_stage "editorial_filter", the URL in seen.json, downstream_rejects == 1, and summary.fidelity.editorial_filtered == 1.
    - An entry summary "Duane &#8220;Keffe D&#8221; Davis&nbsp;fue detenido por homicidio" → the description passed to router.classify contains “Keffe D” and neither "&#" nor "&nbsp;" (FID-06 end-to-end).
    - Queued items (pending.json) derive title_src from their stored title + outlet the same way.
    - The summary key set equals the old 24 keys + "fidelity" (update the pin at test_scrape_news.py:934-940 with a comment "FID-01 36-04"). The GITHUB_OUTPUT lines are unchanged.
    - Backfill: run_classify sends feeds.strip_html(row["description"]) to router.classify. run_apply builds with title_src=source_headline(row title, row outlet), applies guard_title_en, and records editorial_filter rows as stage "editorial_filter".
    - build_incident(title_es=…) without title_src → TypeError (legacy parameter removed). A grep over pipeline/ finds no `title_es=` keyword argument passed to build_incident.
    - Per-row safety in the live OK path (premortem R-14): a fake OK result whose feed title_src is 401 characters → that item gets rejected stage "invalid_record", the other items of the same run are written to current.json, and rc 0. A build_incident that returns None (monkeypatched) → rejected stage "url_rejected", no crash in deduplicate, rc 0.
    - Review/audit fields (premortem R-16): run_review prints title_src (source_headline of the row title and outlet), the guarded title_en and title_en_fallback, and its REVIEW_RE text uses title_src instead of the classifier's title_es. --audit-sample writes, inside "output", "title_src", the guarded "title_en" and "title_en_fallback" instead of "title_es". The assert at test_backfill_classifier_outage.py:698 changes from `first["output"]["title_es"]` to `first["output"]["title_src"]`.
  </behavior>
  <action>
scrape_news.py:
- In the fresh candidate dict (:373-379), add "title_src": source_headline(title, outlet). Compute outlet once and reuse it.
- For queued candidates (:391-394), add title_src = source_headline(p.title, p.outlet). Do NOT add title_src to pending.json (derivable).
- Import source_headline from pipeline.news.feeds and guard_title_en/forbidden_term from pipeline.news.fidelity (local imports, as :276-294).
- In `_answered`, OK path, after the centroid check:
  1. `title_en, fell_back = guard_title_en(item["title_src"], result.title_en, item["outlet"])`;
  2. if forbidden_term(item["title_src"]) or forbidden_term(title_en), append a rejected row with stage "editorial_filter", increment rejected and downstream_rejects, and return;
  3. otherwise call build_incident(title_src=item["title_src"], title_en=title_en, …).
  4. Per-row guard (premortem R-14; mirrors backfill_classifier_outage.py:665-677, read 2026-09-24): if the incident is None, append a rejected row with stage "url_rejected", increment rejected and downstream_rejects, and return. Then `IncidentRecord.model_validate(incident)` in a try; on ValidationError append stage "invalid_record", increment rejected and downstream_rejects, and return. Only then `new_incidents.append(incident)` (today :486 appends unconditionally, and deduplicate at :563 would raise on None before merge_and_write at :571, losing the whole run while save_seen at :575 would not run either).
  Count the fallbacks and the filtered items, and add `"fidelity": {...}` to summary.update.

backfill_classifier_outage.py:
- :544 becomes `router.classify(row["title"] or "", strip_html(row["description"] or ""), key=row["id"])`, with a function-local import of feeds.strip_html.
- :659-665 applies the same title_src + guard + editorial filter; stage "editorial_filter" goes into stage_of.
- run_review (:813-827) and --audit-sample (:893-906) (premortem R-16): replace the classifier's output title_es with title_src = feeds.source_headline(row title, row outlet); emit title_en = guard_title_en(title_src, output title_en, outlet)[0] and title_en_fallback = [1]. In run_review the REVIEW_RE text (:817-818) becomes title + description + title_src. Update test_backfill_classifier_outage.py:698 accordingly.

store.py: remove the `title_es` parameter and the legacy branch from build_incident. title_src becomes required (keyword-only, non-empty; a ValueError otherwise). Update test_store.py tests that used title_es= to title_src=, and test_backfill/test_scrape_news assertions that compared title_es with the classifier's title_es: title_es now equals title_src.

Keep classify input = raw feed title (with suffix). This is the format measured by G-16 and 36-02; the classifier prompt itself is 36-06's scope.

Offline fallback measurement (premortem R-13, no LLM, no writes): over every row of data/incidents/current.json, count kinship_mismatch(title_es, title_en) (the legacy pairs; 798 at 9e3abaf) and record one SUMMARY line `legacy_kinship_fallback_rate: N/<rows>` with the list of hits. If the rate is above 5 %, inspect the hits and fix false positives inside fidelity.py (this plan's scope) before closing.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_scrape_news.py pipeline/tests/test_backfill_classifier_outage.py pipeline/tests/test_store.py pipeline/tests/test_news_health_gate.py pipeline/tests/test_fidelity.py -q -p no:cacheprovider && ! grep -n "title_es=" pipeline/scrape_news.py pipeline/backfill_classifier_outage.py && ! grep -n "result.title_es\|out.title_es" pipeline/scrape_news.py pipeline/backfill_classifier_outage.py && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider; rc=$?; echo "pytest rc=$rc"; [ $rc -eq 0 ]</automated>
    Expected: every command exits 0 (rc=0). The full suite is ≥ 642/1/2 + the new tests of 36-01 and 36-04 (exit code). test_news_health_gate is unedited and green. The SUMMARY carries `legacy_kinship_fallback_rate:`.
    Negative controls: pass result.title_en unguarded, and the kinship-fallback test must FAIL. Revert. Remove the model_validate guard in `_answered`, and the 401-char invalid_record test must FAIL. Revert.
  </verify>
  <done>Live and backfill incidents store the verbatim headline with a guarded translation. Absolute-safety headlines are filtered with an auditable stage. The backfill classify input is entity-free. A bad row is rejected per row in the live path, review/audit artifacts show title_src and the guarded title_en, and the legacy fallback rate is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LLM output (title_en) → published HTML | model text about named real people |
| outlet headline → published HTML | third-party wording |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | Repudiation / defamation | title_en kinship errors (V-06, G-27) | mitigate | The deterministic kinship guard falls back to the verbatim title_src. It is counted in the run summary and is visible in UI as "Original Spanish headline". |
| T-36-10 | Tampering / editorial | absolute safety wording from outlets or MT | mitigate | forbidden_term → stage editorial_filter (not published, auditable in rejected/). The term list is pinned to forbidden-language.mjs. |
| T-36-11 | Denial of service | summary contract change breaks the health gate | mitigate | Only one dict key is added. GITHUB_OUTPUT scalars are unchanged (:215-216), and test_news_health_gate stays green unedited. |
| T-36-SC | Tampering | installs | accept | None. |
</threat_model>

<verification>
- The full pytest run is green (exit code) at ≥ 642/1/2 + new.
- The grep guards pass: no legacy title_es callers, and no reads of the classifier title_es.
</verification>

<success_criteria>
FID-01 holds end-to-end in the pipeline: verbatim title_src/title_es, and a guarded title_en. FID-06 holds for the backfill classify input. The V-06/G-27 defect class is blocked at storage time.
</success_criteria>

<output>
Create `.planning/phases/36-classification-fidelity-attribution/36-04-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-13 | must_haves truth (masking, NON_KIN_CHILD, plurals, measured rate); Task 1 behavior (Padre Las Casas / Niño / pareja / hijos / mamá tests, all False) + action (KINSHIP plurals and colloquial forms, NON_KIN_CHILD for rule (b) only, commune masking from data/cead/meta/index.json, rules (a)/(b) after masking) + second negative control + done; Task 2 action offline `legacy_kinship_fallback_rate` measurement + verify Expected |
| R-14 | must_haves truth (per-row guard); Task 2 behavior (401-char title_src → invalid_record, None → url_rejected, rc 0) + action step 4 (None check + IncidentRecord.model_validate before append, citing scrape_news.py:486/:563/:571/:575 and backfill :665-677 as read) + negative control. The via_url half (queued `p.get('via_url') or None`) is in 36-07 Task 2. |
| R-16 | must_haves truth; Task 2 behavior (run_review / --audit-sample fields, test_backfill :698 assert) + action (backfill :813-827 and :893-906, read 2026-09-24) |
| R-19 | Task 1 behavior Pin also parses CommuneNewsSection.astro:81-89 and asserts set equality with forbidden-language.mjs:35-61 |

Twins fixed: 36-07 Task 2 (queued via_url normalization, R-14), 36-08 Task 1 --sample fields (R-16), 36-09 Task 3 fallback-rate report (R-13).

## Revision R2 (gate R1)

| id | change |
|---|---|
| NB-12 | interfaces: '13 ES + 11 EN' → '13 ES + 10 EN = 23' (re-read forbidden-language.mjs:35-61); Task 2 action and R1 table cite merge_and_write at scrape_news.py:571 and save_seen at :575 (re-read :560-576; :563 deduplicate and :486 append confirmed) |
| NB-08 | Task 2 verify: exit code of the chain now fails the verify (`rc=$?; …; [ $rc -eq 0 ]`) |
| NB-09 | Task 2 verify: the single-line regex (tautological on the multi-line calls at scrape_news.py:474-485 / backfill :659-665) replaced by `! grep -n "title_es=" pipeline/scrape_news.py pipeline/backfill_classifier_outage.py` (today it matches exactly :479 and :661) |
