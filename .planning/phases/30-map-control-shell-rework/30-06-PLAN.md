Fable review: PENDING

---
phase: 30-map-control-shell-rework
plan: 06
type: execute
wave: 5
depends_on: ["30-05"]
files_modified:
  - site/scripts/phase30-gate.mjs
autonomous: false
requirements: [MAPSH-03, MAPSH-05, MAPSH-06]
must_haves:
  truths:
    - "score.mjs run unchanged against the real, rebuilt /es/mapa/ reproduces spec section 12's bar at 320/375/481/639/1296: c1/c2/c5 improve to PASS, c3 stays PASS, controlCoveragePct stays at or below the F-46 ceilings (33.2%@1296, 43.1%@481, 32.2%@320, 22.7%@375) at every corresponding width"
    - "The F-49 three-part c4 evidence chain is recorded regardless of what criterion4 reports: negativeTabindexCount===0, a positive grep hit for keydown/keyup/preventDefault in the map island's own built JS chunk (manually confirmed to only act on an open panel, never blocking Tab/Shift+Tab), and a real Tab walk through the documented focus order (spec section 6) confirming no trap"
    - "Every existing map behavior (?cut= deep link, choropleth year/family/mode switching, commune panel open/close, geolocation, incident pins) still works after the rework, plus the new ?region= deep link and its silent degradation on an unknown value"
    - "ChoroplethLayer.ts / IncidentPinLayer.ts / LowZoomDotLayer.ts are byte-identical to pre-Phase-30; dist/ contains no .filters-row / .ev-chip; no react-leaflet declarative layer component was introduced; zero new npm packages"
  artifacts:
    - path: "site/scripts/phase30-gate.mjs"
      provides: "One Node script (no shell regex-escape risk, F-36) performing: astro-check baseline compare (4 errors/0 warnings), git diff --exit-code on the 3 protected Leaflet files + package.json/package-lock.json, and dist/ grep assertions (.filters-row/.ev-chip absence, showModal( presence in the built JS, numeric z-index values in the built CSS) — exits non-zero on any failure"
    - path: ".planning/phases/30-map-control-shell-rework/30-CLOSE.md"
      provides: "The verbatim post-change score.mjs captures at all 5 widths, the verdict() diff against 30-BASELINE.md, the F-49 c4 evidence chain, the manual regression checklist results, and the explicit no-17th-validator decision record"
  key_links:
    - from: "site/scripts/phase30-gate.mjs"
      to: "ChoroplethLayer.ts, IncidentPinLayer.ts, LowZoomDotLayer.ts"
      via: "execSync('git diff --exit-code -- <the 3 files>') — throws (non-zero) if any byte differs"
      pattern: "git diff --exit-code"
    - from: "site/scripts/phase30-gate.mjs"
      to: "dist/_astro/*.js, dist/_astro/*.css"
      via: "string scan for showModal( and numeric z-index values, and absence-scan for .filters-row/.ev-chip"
      pattern: "showModal("
---

<objective>
Wave 5 (final) of Phase 30 — the phase-close gate. Two parts:

1. **Automated, non-browser gate** (`phase30-gate.mjs`): a single Node script (not a shell one-liner with regex escapes — F-36) asserting everything `dist/`/`git` can prove without a browser: the astro-check 4/0 baseline, the byte-identical Leaflet-file guarantee (F-47's hard invariant), the zero-new-dependency guarantee, and the `.filters-row`/`.ev-chip` absence + `showModal(`/z-index presence in the real built assets. **Decision recorded here, not discovered mid-phase**: this script is deliberately NOT registered as a 17th entry in `scripts/validate/all.mjs` — it stays a standalone phase-close script invoked directly by this plan's gate command, so the "16 validators" count (and every place it is hardcoded, including `.planning/v2.1-AUTONOMOUS-DIRECTIVE.md`) does not need updating. The rationale: most of what this phase must verify is fundamentally browser-only (Q6's finding — `MapIsland` is `client:only="react"`, so none of the `data-role` hooks exist in static `dist/` HTML), so folding a partial, dist-only check into the permanent validator suite would create a check whose PASS means much less than its name implies; keeping it a one-off phase-close script is more honest about what it actually proves.

2. **ORCHESTRATOR-INLINE regression + acceptance pass**: the real `score.mjs` run against the rebuilt `/es/mapa/` at all five widths, diffed against Plan 30-01's `30-BASELINE.md`; the F-49 three-part c4 evidence chain; and a manual click-through of every pre-existing map behavior plus the two new deep-link params.

Purpose: this is the phase's only gate — nothing in Phase 30 is "done" until both parts are green and recorded.

Output: `phase30-gate.mjs`, `30-CLOSE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-map-control-shell-rework/30-FABLE-CONSTRAINTS.md
@.planning/phases/30-map-control-shell-rework/30-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
@.planning/phases/30-map-control-shell-rework/30-BASELINE.md
@.planning/sketches/029-map-controls/score.mjs
@site/scripts/validate/all.mjs
@site/scripts/validate/map.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase30-gate.mjs — the non-browser automated gate</name>
  <files>site/scripts/phase30-gate.mjs</files>
  <action>
    Create `site/scripts/phase30-gate.mjs` (plain Node, ESM, no external deps — mirrors the style of `scripts/validate/map.mjs`, `pass()`/`fail()`/`failures` counter pattern). It must run AFTER `npm run build` (it reads `dist/`) and does NOT itself invoke the build. Assertions, each independently reported PASS/FAIL:
    1. Astro-check baseline: run `npx astro check` via `child_process.execSync` wrapped in try/catch (its non-zero exit at the 4-error baseline is expected — catch it, don't let it kill the script), capture stdout+stderr, strip ANSI color codes, parse the summary line(s) for the error/warning counts using plain string matching (`indexOf`/`split`, no regex needing backslash-escaped digit classes — F-36 is specifically about `\d`/`\b` collapsing inside a *shell-quoted* `node -e`; inside a real `.mjs` file this restriction does not apply, but prefer simple string ops anyway for clarity), compare to the baseline 4 errors / 0 warnings, fail if different.
    2. `git diff --exit-code -- site/src/components/map/ChoroplethLayer.ts site/src/components/map/IncidentPinLayer.ts site/src/components/map/LowZoomDotLayer.ts` (run from repo root via `execSync` with `cwd` set appropriately) — must exit 0 (no diff). Fail loudly with the diff output if not.
    3. `git diff --exit-code -- site/package.json site/package-lock.json` — must exit 0 (zero new dependencies).
    4. Scan `dist/es/mapa/index.html` and `dist/map/index.html` (if present) plus every file under `dist/_astro/` for the literal substrings `filters-row` and `ev-chip` — must be ABSENT everywhere (F-47/spec §9a: `score.mjs` falls back to these classes when no `data-role` hook is found, so any survivor silently rebinds the instrument to legacy markup).
    5. Scan every `.js` file under `dist/_astro/` for the literal substring `showModal(` — must be present at least once (proves the dialog code shipped into the real bundle; the interactive `dialog.matches(':modal')` proof is settled in the ORCHESTRATOR-INLINE step below, since a string match alone cannot fully distinguish `showModal()` usage from a stray comment mentioning it).
    6. Scan every `.css` file under `dist/_astro/` for the literal (whitespace-free, since minifiers typically strip it) substrings `z-index:750`, `z-index:800`, and `z-index:801` — all three must be present at least once, proving the pinned ladder actually reached the built stylesheet.
    Exit `process.exit(1)` if any assertion failed, else `process.exit(0)` with a final `PASS phase30-gate: all assertions passed` line.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && node scripts/phase30-gate.mjs</automated>
  </verify>
  <done>`phase30-gate.mjs` exists and, run against the real post-Phase-30 build, reports all assertions PASS: astro-check baseline 4/0, both git-diff checks clean, `.filters-row`/`.ev-chip` absent everywhere in `dist/`, `showModal(` present, and all 3 z-index values present in the built CSS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 30 map control-shell rework across Plans 30-01..30-05, plus this plan's non-browser automated gate.</what-built>
  <how-to-verify>
    The interactive/visual proof cannot be automated (Q6) and is the ORCHESTRATOR-INLINE step below — hand off to the orchestrator now to run the full regression + acceptance pass and write `30-CLOSE.md`.
  </how-to-verify>
  <resume-signal>Orchestrator resumes after `30-CLOSE.md` is written with every item below recorded PASS/FAIL, and any FAIL is either fixed (new plan cycle) or explicitly accepted with rationale.</resume-signal>
</task>

</tasks>

<orchestrator_inline_step>
**ORCHESTRATOR-INLINE — NOT an executor task.**

1. `cd site && npm run build && npm run validate && npm test` (chained, OneDrive rule) — confirm build/validate/vitest all green; record the new vitest total (37 + 4 from Plan 30-02 = 41 expected; if `EntryPointsRail`/`FilterSheet` gained any pure-logic tests during execution, record the real total, not the predicted one).
2. `node scripts/phase30-gate.mjs` (Task 1, already run by the executor — re-confirm PASS).
3. `npx astro preview --port 4321 --host`.
4. In BrowserOS: navigate to `http://127.0.0.1:4321/es/mapa/`. For each width in `[320, 375, 481, 639, 1296]`: emulate via a same-origin iframe (never `file://`); run `(${scoreControlShell.toString()})({stressCondition: 'postchange-<width>'})`; record the verbatim JSON and its `verdict()`. Diff every field against the matching width's entry in `30-BASELINE.md` — confirm c1/c2/c5 move FAIL→PASS (or stay PASS), c3 stays PASS, and `controlCoveragePct` is at or below the F-46 ceiling for that width (33.2%@1296, 43.1%@481, 32.2%@320, 22.7%@375 — these are ceilings the real implementation may not exceed, not targets to hit exactly).
5. Settle c4 per F-49's three-part chain regardless of what `criterion4` reports: (a) confirm `negativeTabindexCount === 0` from the same captured JSON; (b) grep the built map-island JS chunk under `dist/_astro/` for `keydown`/`keyup`/`preventDefault`, read the matched context, and confirm by inspection it only fires when a tracked panel is open and never calls `preventDefault()` on Tab/Shift+Tab; (c) do a real Tab walk in BrowserOS through the documented focus order (spec §6: skip-link → header nav → search → locate → news toggle → Año → Tipo de delito → Modo → state summary → map → legend → zoom), confirming focus is never trapped and Escape only closes the currently-open panel. Record all three, plainly, even if they disagree with what the automated `criterion4` field says.
6. Manual regression click-through against the real rebuilt page, each item PASS/FAIL: `?cut=13101` deep link still flies to and highlights Santiago; choropleth recolors correctly switching year, crime family, and mode (composite/family); clicking a commune opens `ResultPanel` and closing it resets style; the geolocate button still works (or degrades gracefully without permission); incident pins still render when the news toggle is on; the news toggle is visibly labeled and reachable at every one of the 5 widths; `?region=13` (or any known `region_id` from `data/cead/meta/index.json`) fits the map to that region's combined bounds WITHOUT opening `ResultPanel` or selecting a commune; `?region=99999` (unknown) produces zero console errors, zero network 404s, and the map simply loads at its default extent.
7. Confirm the three UX mitigations from F-46: the state summary truncates with ellipsis and never wraps to two lines at any tested width; the topbar's translucent gradient is still visible over the map (spot-check visually); coverage figures recorded in step 4 do not exceed the ceilings.
8. Write `.planning/phases/30-map-control-shell-rework/30-CLOSE.md` containing: the 5 verbatim post-change captures + verdicts, the diff summary against `30-BASELINE.md`, the F-49 c4 evidence chain (verbatim), the regression checklist with PASS/FAIL per item, the F-46 mitigation confirmations, and the final vitest/pytest/validator counts for this phase's STATE.md update.
9. Only after `30-CLOSE.md` is all-PASS (or every FAIL is explicitly triaged and either fixed via a new plan cycle or accepted with recorded rationale) does Phase 30 close.
</orchestrator_inline_step>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI/local build output (`dist/`) → gate script | Trusted, locally-produced build artifact — no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-30-07 | Repudiation | Phase-close claim of "regression-clean" | mitigate | `30-CLOSE.md` records verbatim captured JSON, not a summary paraphrase — any future re-audit can re-diff the raw numbers against `30-BASELINE.md` |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | `phase30-gate.mjs` assertion 3 (`git diff --exit-code` on package.json/lock) is the enforcement; zero packages installed across the whole phase |
</threat_model>

<verification>
`phase30-gate.mjs`'s own exit code is the automated verification for everything expressible without a browser: it fails on a single differing byte in the 3 protected Leaflet files, a single new dependency, a single surviving `.filters-row`/`.ev-chip` occurrence, a missing `showModal(`, a missing z-index value, or an astro-check count that drifts from 4/0 — and passes only when every one of those is simultaneously true against the real post-Phase-30 tree. Everything else (the actual regression proof, the c4 resolution, the coverage-ceiling proof) is inherently interactive and is the ORCHESTRATOR-INLINE step's job, per Q6's finding that `client:only="react"` islands render nothing measurable into static HTML.
</verification>

<success_criteria>
- `phase30-gate.mjs` exits 0 against the real post-Phase-30 build.
- `30-CLOSE.md` records a full PASS regression + acceptance pass, or every FAIL is explicitly triaged.
- Phase 30's ROADMAP.md success criteria and MAPSH-01..07 are all satisfied and traceable to a specific plan/task above.
</success_criteria>

<output>
Create `.planning/phases/30-map-control-shell-rework/30-06-SUMMARY.md` when done.
</output>
