Fable review: APPROVED 2026-07-30 — after an Opus plan-check (9 blockers / 10 warnings, every <automated> gate executed against the real tree) and an Opus premortem (16 ranked failure modes). Both passes were applied INLINE by Fable rather than in a third planner round (F-22/F-26/F-34/F-44 precedent). Every gate edited here was re-verified by execution in BOTH directions — green on a compliant scratch implementation, red on a targeted mutation. Binding decisions for this phase: F-47..F-53 in STATE.md.

---
phase: 30-map-control-shell-rework
plan: 06
type: execute
wave: 5
depends_on: ["30-05"]
files_modified:
  - site/scripts/phase30-gate.mjs
autonomous: false
requirements: [MAPSH-03, MAPSH-05, MAPSH-06]
must_haves:
  truths:
    - "score.mjs run unchanged against the real, rebuilt /es/mapa/ at 320/375/481/639/1296: c1 and c2 improve to PASS at all five widths; c3's touchTargetDetail contains ZERO elements inside .map-topbar/.legend/.zoom-ctl or carrying a data-role attribute, and the post-change list is a strict SUBSET of the baseline list (F-51 — the baseline is c3 FAIL with 20 violations, and score.mjs scans the whole document including header/footer/Leaflet attribution, which are outside MAPSH-05's confinement); c5's raw criterion5 is PASS (zIndexViolations === []) at all five widths; and controlCoveragePct is at or below the F-46 ceilings (33.2%@1296, 43.1%@481, 32.2%@320, 22.7%@375) at every corresponding width"
    - "The two pre-registered instrument tensions are recorded, not resolved by editing score.mjs (F-49/F-53): verdict()'s c4_keyboardTrapFree is FALSE in every table this phase produces, pre AND post, because score.mjs:145-151 takes the NOT_MEASURED_FAIL branch unconditionally on any Astro page (external module script, empty textContent) — this is a property of the instrument on a bundled page, never a Phase 30 regression; and verdict()'s c5 is FALSE at 320 by construction, because it applies STRESS.md's <=25% coverage rule to every width <=375 while F-50's own measured ceiling at 320 is 32.2%"
    - "The F-49 three-part c4 evidence chain is recorded regardless of what criterion4 reports: negativeTabindexCount===0, a positive grep hit for keydown/keyup/preventDefault in the map island's own built JS chunk (manually confirmed to only act on an open panel, never blocking Tab/Shift+Tab), and a real Tab walk through the documented focus order (spec section 6) confirming no trap"
    - "Every existing map behavior (?cut= deep link, choropleth year/family/mode switching, commune panel open/close, geolocation, incident pins) still works after the rework, plus the new ?region= deep link and its silent degradation on an unknown value"
    - "ChoroplethLayer.ts / IncidentPinLayer.ts / LowZoomDotLayer.ts are byte-identical to pre-Phase-30; dist/ contains no .filters-row / .ev-chip; no react-leaflet declarative layer component was introduced; zero new npm packages"
  artifacts:
    - path: "site/scripts/phase30-gate.mjs"
      provides: "One Node script (no shell regex-escape risk, F-36) performing: astro-check baseline compare (4 errors/0 warnings), git diff --exit-code on the 3 protected Leaflet files + package.json/package-lock.json, and dist/ grep assertions (.filters-row/.ev-chip absence, showModal( presence in the built JS, numeric z-index values in the built CSS) — exits non-zero on any failure"
    - path: ".planning/phases/30-map-control-shell-rework/30-CLOSE.md"
      provides: "The verbatim post-change score.mjs captures at all 5 widths, the verdict() diff against 30-BASELINE.md, the F-49 c4 evidence chain, the manual regression checklist results, and the explicit no-17th-validator decision record"
  key_links:
    - from: "site/scripts/phase30-gate.mjs"
      to: "ChoroplethLayer.ts, IncidentPinLayer.ts, LowZoomDotLayer.ts"
      via: "execSync('git diff --exit-code -- <the 3 files>') — throws (non-zero) if any byte differs"
      pattern: "git diff --exit-code"
    - from: "site/scripts/phase30-gate.mjs"
      to: "dist/_astro/*.js, dist/_astro/*.css"
      via: "string scan for showModal( and numeric z-index values, and absence-scan for .filters-row/.ev-chip"
      pattern: "showModal("
---

<objective>
Wave 5 (final) of Phase 30 — the phase-close gate. Two parts:

1. **Automated, non-browser gate** (`phase30-gate.mjs`): a single Node script (not a shell one-liner with regex escapes — F-36) asserting everything `dist/`/`git` can prove without a browser: the astro-check 4/0 baseline, the byte-identical Leaflet-file guarantee (F-47's hard invariant), the zero-new-dependency guarantee, and the `.filters-row`/`.ev-chip` absence + `showModal(`/z-index presence in the real built assets. **Decision recorded here, not discovered mid-phase**: this script is deliberately NOT registered as a 17th entry in `scripts/validate/all.mjs` — it stays a standalone phase-close script invoked directly by this plan's gate command, so the "16 validators" count (and every place it is hardcoded, including `.planning/v2.1-AUTONOMOUS-DIRECTIVE.md`) does not need updating. The rationale: most of what this phase must verify is fundamentally browser-only (Q6's finding — `MapIsland` is `client:only="react"`, so none of the `data-role` hooks exist in static `dist/` HTML), so folding a partial, dist-only check into the permanent validator suite would create a check whose PASS means much less than its name implies; keeping it a one-off phase-close script is more honest about what it actually proves.

2. **ORCHESTRATOR-INLINE regression + acceptance pass**: the real `score.mjs` run against the rebuilt `/es/mapa/` at all five widths, diffed against Plan 30-01's `30-BASELINE.md`; the F-49 three-part c4 evidence chain; and a manual click-through of every pre-existing map behavior plus the two new deep-link params.

Purpose: this is the phase's only gate — nothing in Phase 30 is "done" until both parts are green and recorded.

Output: `phase30-gate.mjs`, `30-CLOSE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-map-control-shell-rework/30-FABLE-CONSTRAINTS.md
@.planning/phases/30-map-control-shell-rework/30-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
@.planning/phases/30-map-control-shell-rework/30-BASELINE.md
@.planning/sketches/029-map-controls/score.mjs
@site/scripts/validate/all.mjs
@site/scripts/validate/map.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase30-gate.mjs — the non-browser automated gate</name>
  <files>site/scripts/phase30-gate.mjs</files>
  <action>
    Create `site/scripts/phase30-gate.mjs` (plain Node, ESM, no external deps — mirrors the style of `scripts/validate/map.mjs`, `pass()`/`fail()`/`failures` counter pattern). It must run AFTER `npm run build` (it reads `dist/`) and does NOT itself invoke the build. Assertions, each independently reported PASS/FAIL:
    1. Astro-check baseline: run `npx astro check` via `child_process.execSync` wrapped in try/catch (its non-zero exit at the 4-error baseline is expected — catch it, don't let it kill the script), capture stdout+stderr, strip ANSI color codes, parse the summary line(s) for the error/warning counts using plain string matching (`indexOf`/`split`, no regex needing backslash-escaped digit classes — F-36 is specifically about `\d`/`\b` collapsing inside a *shell-quoted* `node -e`; inside a real `.mjs` file this restriction does not apply, but prefer simple string ops anyway for clarity), **assert on the SET of `file:line` error LOCATIONS, not on the count.** The pre-existing baseline is 4 errors, all in `ComparatorPairsLinks.astro:28`; five new `.tsx`/`.ts` files land in this phase, and a count-based gate turns any new error into a red whose cheapest "fix" is bumping the baseline — which then permanently hides a real regression. Any error location NOT in the baseline set = FAIL; the same locations = PASS regardless of count.
    2. Read `PHASE30_BASE_SHA` from `.planning/phases/30-map-control-shell-rework/30-BASELINE.md` (fail the assertion loudly if that line is absent or malformed), then run, from the repo root via `execSync`:
       `git diff --exit-code <PHASE30_BASE_SHA> -- site/src/components/map/ChoroplethLayer.ts site/src/components/map/IncidentPinLayer.ts site/src/components/map/LowZoomDotLayer.ts .planning/sketches/029-map-controls/score.mjs`
       — must exit 0. **This compares against the pre-phase commit, NOT the working tree against HEAD.** A plain `git diff --exit-code` CANNOT FAIL here: this run commits mid-phase (GSD atomic commits), so any Wave-4 edit to a protected Leaflet file would already be committed and the working-tree diff would be empty — leaving F-47's hard invariant completely unenforced. `score.mjs` is included in the same assertion because F-49 forbids editing the instrument.
    3. `git diff --exit-code <PHASE30_BASE_SHA> -- site/package.json site/package-lock.json` — must exit 0 (zero new dependencies), same pre-phase-SHA rule and for the same reason.
    4. Scan `dist/es/mapa/index.html` and `dist/map/index.html` (if present) plus every file under `dist/_astro/` for the literal substrings `filters-row` and `ev-chip` — must be ABSENT everywhere (F-47/spec §9a: `score.mjs` falls back to these classes when no `data-role` hook is found, so any survivor silently rebinds the instrument to legacy markup).
    5. Scan every `.js` file under `dist/_astro/` for the literal substring `showModal(` — must be present at least once (proves the dialog code shipped into the real bundle; the interactive `dialog.matches(':modal')` proof is settled in the ORCHESTRATOR-INLINE step below, since a string match alone cannot fully distinguish `showModal()` usage from a stray comment mentioning it).
    6. Scan every `.css` file under `dist/_astro/` for the pinned ladder. **Strip ALL whitespace from the CSS text first (`text.replace(/\s+/g,'')`) and then look for `z-index:750`, `z-index:800`, `z-index:801`** — do not assume minification is on, or the gate reds against a perfectly correct implementation whose CSS was emitted unminified. All three must be present at least once. Additionally assert `.legend{…z-index:700` is ABSENT from the whitespace-stripped text: a leftover unscoped rule elsewhere in the 970-line file could still win the cascade and re-tie `.leaflet-popup-pane`, which is the one z-index defect Phase 29 actually measured.
    7. Scan every `.tsx`/`.ts` file under `site/src/components/map/` for the substrings `<GeoJSON`, `<Marker`, `from 'react-leaflet'` and `from "react-leaflet"` — all must be ABSENT (MAPSH-05 / the ROADMAP risk annotation: a declarative react-leaflet layer component reintroduces the solved re-render-on-hover bug). `react-leaflet` is not in `site/package.json` today, so this is belt-and-braces with assertion 3, and cheap.
    Exit `process.exit(1)` if any assertion failed, else `process.exit(0)` with a final `PASS phase30-gate: all assertions passed` line.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && node scripts/phase30-gate.mjs</automated>
  </verify>
  <done>`phase30-gate.mjs` exists and, run against the real post-Phase-30 build, reports all assertions PASS: astro-check baseline 4/0, both git-diff checks clean, `.filters-row`/`.ev-chip` absent everywhere in `dist/`, `showModal(` present, and all 3 z-index values present in the built CSS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 30 map control-shell rework across Plans 30-01..30-05, plus this plan's non-browser automated gate.</what-built>
  <how-to-verify>
    The interactive/visual proof cannot be automated (Q6) and is the ORCHESTRATOR-INLINE step below — hand off to the orchestrator now to run the full regression + acceptance pass and write `30-CLOSE.md`.
  </how-to-verify>
  <resume-signal>Orchestrator resumes after `30-CLOSE.md` is written with every item below recorded PASS/FAIL, and any FAIL is either fixed (new plan cycle) or explicitly accepted with rationale.</resume-signal>
</task>

</tasks>

<orchestrator_inline_step>
**ORCHESTRATOR-INLINE — NOT an executor task.**

1. `cd site && npm run build && npm run validate && npm test` (chained, OneDrive rule) — confirm build/validate/vitest all green; record the new vitest total (37 + 8 from Plan 30-02 = 45 expected; if any wave gained further pure-logic tests, record the REAL total, never the predicted one). Then, from the repo root, `python -m pytest -q` — confirm **344 passed / 1 skipped / 1 xfailed**. The xfail is Phase 26's deliberate `xfail(strict=True)` quarantine and must never be "fixed"; any OTHER xfail or failure is a real regression. If `validate` goes red, the gate goes red — F-19's freshness branch is NEVER encoded in a gate command (F-34); the orchestrator checks its preconditions out of band and records the exclusion in STATE.md.
2. `node scripts/phase30-gate.mjs` (Task 1, already run by the executor — re-confirm PASS).
3. `npx astro preview --port 4321 --host`.
4. In BrowserOS: navigate to `http://127.0.0.1:4321/es/mapa/`. For each width in `[320, 375, 481, 639, 1296]`: emulate via a same-origin iframe (never `file://`), **pinned to `height = 800`, the same height `30-BASELINE.md` used — refuse to compare any two captures whose `viewportHeightPx` differ**, because `controlCoveragePct`, `inViewport` (c1/c2) and `mapWidthRatio` are all functions of viewport height (premortem §13). Run `(${scoreControlShell.toString()})({stressCondition: 'postchange-<width>'})`; record the verbatim JSON and its `verdict()`. Diff every field against the matching width's entry in `30-BASELINE.md`:
   - c1 and c2 must be PASS at all five widths.
   - c3: `touchTargetDetail` must contain **zero** elements inside `.map-topbar`, `.legend`, `.zoom-ctl`, or carrying `data-role`, and must be a strict subset of the baseline list. Enumerate every surviving violation and classify each as (i) a map control this phase owns — **must be fixed**, no exceptions — or (ii) outside MAPSH-05's confinement (header nav, footer, skip link, `.leaflet-control-attribution`). Class (ii) is **accepted and recorded by name** in `30-CLOSE.md` per F-51, never silently dropped and never quietly rescoped mid-close.
   - c5: raw `criterion5` must be `PASS` (`zIndexViolations === []`) at all five widths. `verdict()`'s `c5` will be FALSE at 320 by construction (F-53) — record the disagreement explicitly as a pre-registered tension, and never edit `score.mjs` or `verdict()` to resolve it.
   - `controlCoveragePct` at or below the F-46 ceiling for that width (33.2%@1296, 43.1%@481, 32.2%@320, 22.7%@375 — ceilings, not targets). **If a ceiling is exceeded at any band, the disposition is a FIX** (reduce topbar padding/height, or drop a redundant `data-role` off a nested wrapper — the latter only if it does not remove a hook spec §8 mandates). It is NOT admissible to average widths, to substitute the union-coverage figure, or to reinterpret F-50: F-50 says "may not exceed", and STRESS.md's freeze forbids loosening the ≤25%@375 rule.
   - Record the union coverage figure alongside the raw one, exactly as `30-BASELINE.md` did, so the two are comparable.
5. Settle c4 per F-49's three-part chain regardless of what `criterion4` reports: (a) confirm `negativeTabindexCount === 0` from the same captured JSON; (b) grep the built map-island JS chunk under `dist/_astro/` for `keydown`/`keyup`/`preventDefault`, read the matched context, and confirm by inspection it only fires when a tracked panel is open and never calls `preventDefault()` on Tab/Shift+Tab; (c) do a real Tab walk in BrowserOS through the documented focus order (spec §6: skip-link → header nav → search → locate → news toggle → Año → Tipo de delito → Modo → state summary → map → legend → zoom), confirming focus is never trapped and Escape only closes the currently-open panel. Record all three, plainly, even if they disagree with what the automated `criterion4` field says.
5b. **Modal proof (spec §5/§12 — verified NOWHERE in the first plan draft; this is the entirety of MAPSH-02's verification).** At 375px inside the same-origin iframe: click the FAB, then `evaluate_script`
    `(() => { const d = document.getElementById('filter-sheet'); const fab = document.querySelector('.filter-fab'); return { isModal: d.matches(':modal'), hasOpenAttrOnly: d.hasAttribute('open') && !d.matches(':modal'), fabExpandedWhileOpen: fab.getAttribute('aria-expanded') }; })()`
    — require `isModal === true` and `fabExpandedWhileOpen === 'true'`. (`grep -c "showModal"` cannot distinguish `showModal()` from `<dialog open>`; `:modal` can.) Then press Escape and re-evaluate
    `(() => { const d = document.getElementById('filter-sheet'); const fab = document.querySelector('.filter-fab'); return { isModal: d.matches(':modal'), fabExpanded: fab.getAttribute('aria-expanded'), focusReturned: document.activeElement === fab }; })()`
    — require `isModal === false`, `fabExpanded === 'false'`, `focusReturned === true`. **Repeat the whole sequence via the sheet's own close button.** Record all six values verbatim; any false is a FAIL, not a note.
    Then, with a commune selected at 375px, run the `elementFromPoint` topmost check on `[data-role="filter-entry"]` to prove the FAB clears `.result-panel`'s 40vh sheet (spec §4a). Score that state with `isTopmost` ONLY — never with `verdict()`, because `.family-bar-label` truncates by design when `ResultPanel` is open and would poison the document-wide overflow scan.

6. **Effect assertions, not appearance checks (premortem §9 — the named invariant with NO gate is "every control still drives the state it drove before").** The prop chain grows from two hops to four (`MapIsland → MapTopbar → EntryPointsRail/FilterSheet → FilterFields`); TypeScript catches a missing required prop but not `onYearChange={() => {}}`, and not a prop forwarded to the rail but not to the sheet — and the sheet is the ≤480px surface no desktop reviewer opens. **Run every item below at 1296 AND at 375 through the `<dialog>`:**
   - **News toggle**: click it, wait 1500ms, assert `document.querySelectorAll('.ev-dot').length > 0`; click again, assert it returns to 0. (Proves `showEvents` still reaches `IncidentPinLayer`.)
   - **Crime type**: select `Homicidios` and assert the legend's band set DIFFERS from `Todos` at the same year and mode. A wrong `familyIndex`/`key` mapping is invisible to the eye — the map still recolors via `buildStyleMapFromLevel` — but a differing band set is not. This is the gate for the phase's highest-damage silent regression (premortem §6).
   - **Year**: switch 2025 → 2019 and assert the legend breaks change (or a `Datos no disponibles` toast appears), proving `onYearChange` still reaches the fetch effect (`MapIsland.tsx:246-297`).
   - **Mode**: `Índice` ↔ `Por delito` and assert `Legend`'s title toggles between the composite title and the banded legend (`MapIsland.tsx:505-512`).
   - **Search**: type a commune, pick it, assert `ResultPanel` appears; close it and assert it disappears and the polygon style resets.
   - **`partial-year-badge`** renders fully clear of the topbar at all five widths (it is on by default whenever the current year's payload carries `partial_year`, and this phase changes the topbar height — premortem §11).
   - **Console**: assert the error count is 0 across the whole pass (`get_console_logs`), recorded verbatim.

   Then the remaining click-through items, each PASS/FAIL: `?cut=13101` deep link still flies to and highlights Santiago; choropleth recolors correctly switching year, crime family, and mode (composite/family); clicking a commune opens `ResultPanel` and closing it resets style; the geolocate button still works (or degrades gracefully without permission); incident pins still render when the news toggle is on; the news toggle is visibly labeled and reachable at every one of the 5 widths; **`?region=13` must be verified POSITIVELY**: record the map's `getBounds()` (or centre+zoom) before and after, and assert it CHANGED and now sits inside Región Metropolitana, WITHOUT opening `ResultPanel` or selecting a commune. A "the map looks about right" tick cannot distinguish a working region focus from a silently no-op'd one — and the unknown-value case is *specified* to look identical (premortem §12). Then `?region=99999` (unknown): zero console errors, zero network 404s, map at its default extent. Then `?cut=13101&region=05`: the commune deep link must WIN — `ResultPanel` opens on Santiago and the map does not fly to Valparaíso.
7. Confirm the three UX mitigations from F-46, and dump the overflow scan:
   - **Before running `verdict()`, dump `hiddenOverflowElements` verbatim.** If it is non-empty, name every entry and its `cls`. A non-empty list at close is a REAL defect (it is exactly the baseline's failure signature), not a rounding issue, and must be fixed rather than narrated. Score c1/c2 with `ResultPanel` **CLOSED** (spec §10) — `.family-bar-label` truncates by design and would otherwise poison the scan.
   - The state summary **never wraps** to a second line at any tested width, AND `el.scrollWidth - el.clientWidth <= 8` at all five widths — i.e. the band-composed string actually fits and the ellipsis never engages. A truly-truncating summary would itself set `filterOverflowsContainer` and fail c2 (premortem §2).
   - The news toggle's label is **visibly present** at each of the five widths: read `getComputedStyle` of BOTH label spans and assert **exactly one** is `display !== 'none'` at each width, recorded verbatim. Criterion 1 tests presence and stacking, never legibility — the bare-orange-dot variant scored PASS in Phase 29 and MAPSH-01 forbids it.
   - The topbar keeps its translucent gradient, so covered map area stays partially legible (F-46 mitigation 2, visual spot-check).
   - Coverage figures recorded in step 4 do not exceed the F-50 ceilings.
8. Write `.planning/phases/30-map-control-shell-rework/30-CLOSE.md` containing: the 5 verbatim post-change captures + verdicts, the diff summary against `30-BASELINE.md`, the F-49 c4 evidence chain (verbatim), the regression checklist with PASS/FAIL per item, the F-46 mitigation confirmations, and the final vitest/pytest/validator counts for this phase's STATE.md update.
9. Only after `30-CLOSE.md` is all-PASS (or every FAIL is explicitly triaged and either fixed via a new plan cycle or accepted with recorded rationale) does Phase 30 close.
</orchestrator_inline_step>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI/local build output (`dist/`) → gate script | Trusted, locally-produced build artifact — no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-30-07 | Repudiation | Phase-close claim of "regression-clean" | mitigate | `30-CLOSE.md` records verbatim captured JSON, not a summary paraphrase — any future re-audit can re-diff the raw numbers against `30-BASELINE.md` |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | `phase30-gate.mjs` assertion 3 (`git diff --exit-code` on package.json/lock) is the enforcement; zero packages installed across the whole phase |
</threat_model>

<verification>
`phase30-gate.mjs`'s own exit code is the automated verification for everything expressible without a browser: it fails on a single differing byte in the 3 protected Leaflet files, a single new dependency, a single surviving `.filters-row`/`.ev-chip` occurrence, a missing `showModal(`, a missing z-index value, or an astro-check count that drifts from 4/0 — and passes only when every one of those is simultaneously true against the real post-Phase-30 tree. Everything else (the actual regression proof, the c4 resolution, the coverage-ceiling proof) is inherently interactive and is the ORCHESTRATOR-INLINE step's job, per Q6's finding that `client:only="react"` islands render nothing measurable into static HTML.
</verification>

<success_criteria>
- `phase30-gate.mjs` exits 0 against the real post-Phase-30 build. **This is NECESSARY AND NOT SUFFICIENT and may never be cited as the phase gate on its own** (premortem §15): it returns 0 for a bundle whose island throws at hydration, and `validate/map.mjs`'s locate-`aria-label` assertion is satisfied by the static span at `map.astro:32-36`, not by the island — so `16/16 validators` stays green over a map that renders nothing at all. Phase 30 does not close without `30-CLOSE.md` carrying five verbatim post-change `score.mjs` captures plus the step-6 effect assertions. **If the browser pass cannot be run, the phase is PAUSED, not closed.**
- `30-CLOSE.md` records a full PASS regression + acceptance pass, or every FAIL is explicitly triaged.
- Phase 30's ROADMAP.md success criteria and MAPSH-01..07 are all satisfied and traceable to a specific plan/task above.
</success_criteria>

<output>
Create `.planning/phases/30-map-control-shell-rework/30-06-SUMMARY.md` when done.
</output>
