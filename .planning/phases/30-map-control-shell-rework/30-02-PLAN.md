Fable review: PENDING

---
phase: 30-map-control-shell-rework
plan: 02
type: execute
wave: 1
depends_on: ["30-01"]
files_modified:
  - site/src/config/i18n.ts
  - site/src/lib/mapFilterDefs.ts
  - site/src/components/map/FilterFields.tsx
  - site/src/lib/regionParam.ts
  - site/src/lib/regionParam.test.ts
autonomous: true
requirements: [MAPSH-04, MAPSH-07]
must_haves:
  truths:
    - "The six new grouped-entry-point/news-toggle labels (spec 29-DESIGN-SPEC.md section 7) exist in both EN_STRINGS and ES_STRINGS with the exact copy the spec pins"
    - "The crime-type option list (including the homicide 8th option) and the available-years list used by every later component come from ONE shared module, not re-derived per component"
    - "?region= resolves to the exact set of CUTs belonging to a known region_id, and resolves to null (never throws, never triggers a fetch) for an unknown or malformed region_id"
  artifacts:
    - path: "site/src/config/i18n.ts"
      provides: "map_entry_year, map_entry_family, map_entry_mode, map_news_toggle_long, map_news_toggle_short, map_filters_fab keys in I18nStrings/EN_STRINGS/ES_STRINGS"
    - path: "site/src/lib/mapFilterDefs.ts"
      provides: "CHIP_DEFS (ported verbatim from FiltersRow.tsx:30-43, including the homicide entry) and AVAILABLE_YEARS (ported verbatim from FiltersRow.tsx:48-52), both exported for reuse by every filter-rendering component"
    - path: "site/src/components/map/FilterFields.tsx"
      provides: "YearField, FamilyField, ModeField — pure, wrapper-less (no details/dialog) React components rendering the actual controls, reused unwrapped by the mobile sheet and wrapped in details by the desktop rail"
    - path: "site/src/lib/regionParam.ts"
      provides: "resolveRegionFocus(search, idx) returning string[] | null — single-value ?region= parser, own guard, never reuses parseFilterParams"
  key_links:
    - from: "site/src/lib/regionParam.ts"
      to: "data/cead/meta/index.json region_id field"
      via: "idx.filter(c => c.region_id === focusRegion) — membership check against already-loaded data, no fetch"
      pattern: "region_id ==="
    - from: "site/src/components/map/FilterFields.tsx"
      to: "site/src/lib/mapFilterDefs.ts"
      via: "import { CHIP_DEFS, AVAILABLE_YEARS }"
      pattern: "from '../../lib/mapFilterDefs'"
---

<objective>
Wave 1 of Phase 30 — the interface-first task. Before any details/dialog markup exists, this plan lays down every piece of shared, dependency-free data and pure logic that Waves 2-4 will consume: the six i18n label keys (spec §7), the crime-family/year option data ported verbatim out of the doomed `FiltersRow.tsx` (Pitfall 4 — the file is deleted in Wave 4, but its `CHIP_DEFS` data must not die with it), the field-level React components that render the actual select/radiogroup controls with zero opinion about whether they sit inside a `<details>` panel or a mobile `<dialog>`, and the `?region=` guard (F-48/MAPSH-04) as an isolable, unit-testable pure function — mirroring the existing `?cut=` pattern (`MapIsland.tsx:228-232`) but explicitly NOT reusing `parseFilterParams` (Phase 28 Outcome N1 / F-35 N1).

Purpose: every later plan (EntryPointsRail, FilterSheet, MapIsland wiring) imports from here rather than re-deriving family/year data or re-inventing the region guard — this is the "define contracts first" step so Waves 2-4 implement against a fixed shape instead of discovering it mid-task.

Output: `i18n.ts` additions, `mapFilterDefs.ts`, `FilterFields.tsx`, `regionParam.ts` + its vitest spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-map-control-shell-rework/30-FABLE-CONSTRAINTS.md
@.planning/phases/30-map-control-shell-rework/30-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
@site/src/config/i18n.ts
@site/src/components/map/FiltersRow.tsx
@site/src/components/map/SearchBox.tsx
@site/src/lib/newsFilterLogic.ts
@site/src/lib/newsFilterLogic.test.ts
</context>

<interfaces>
Existing `CommuneIndexEntry` (from `SearchBox.tsx:20-25`, re-exported, do not redefine):
  interface CommuneIndexEntry { cut: string; name: string; slug: string; region_id: string; }

Existing `?cut=` pattern to mirror in spirit, NOT in code location (`MapIsland.tsx:228-232`, unchanged, do not edit in this plan):
  const focusCut = new URLSearchParams(window.location.search).get('cut');
  if (focusCut && /^\d{4,5}$/.test(focusCut)) { setTimeout(() => selectCommune(focusCut), 100); }

Existing `CHIP_DEFS` shape to port verbatim (`FiltersRow.tsx:20-43`):
  interface ChipDef { key: string | null; familyIndex: number | null; labelEs: string; labelEn: string; featured: boolean; }
  8 entries in display order: null/todos, vida(0), propiedad(5), robos_violentos(1), incivilidades(6), vif(2), drogas(3), armas(4), homicidios(familyIndex null, featured true).

Existing `AVAILABLE_YEARS` derivation to port verbatim (`FiltersRow.tsx:45-52`):
  CEAD_EARLIEST_YEAR = 2005; years generated from current year down to 2005, newest first.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the six i18n keys (spec section 7) — exact ES/EN copy, no invention</name>
  <files>site/src/config/i18n.ts</files>
  <action>
    Add six new keys to the `I18nStrings` interface (there is no dedicated map section today — add a clearly-commented "Phase 30 — map control-shell entry points" block, following the existing per-phase-block commenting convention visible at the "Phase 28 — news faceting UI" block, `i18n.ts:135-150`) and populate both `EN_STRINGS` and `ES_STRINGS` with the exact copy pinned by `29-DESIGN-SPEC.md` §7: `map_entry_year` ("Year"/"Año"), `map_entry_family` ("Crime type"/"Tipo de delito"), `map_entry_mode` ("Map mode"/"Modo"), `map_news_toggle_long` ("Recent incidents"/"Noticias recientes"), `map_news_toggle_short` ("News"/"Noticias"), `map_filters_fab` ("Filters"/"Filtros"). Do not touch any existing key or reorder the file.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node -e "const fs=require('fs'); const s=fs.readFileSync('src/config/i18n.ts','utf8'); const keys=['map_entry_year','map_entry_family','map_entry_mode','map_news_toggle_long','map_news_toggle_short','map_filters_fab']; const missing=keys.filter(function(k){var re=new RegExp(k+String.fromCharCode(58),'g'); var count=(s.match(re)||[]).length; return count<3;}); if(missing.length){console.error('FAIL missing/incomplete keys: '+missing.join(', ')); process.exit(1);} console.log('PASS all 6 keys present in interface+EN+ES');"</automated>
  </verify>
  <done>All 6 keys appear in `I18nStrings`, `EN_STRINGS`, and `ES_STRINGS` (3 occurrences each), with the exact spec §7 copy; no existing key changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract mapFilterDefs.ts + FilterFields.tsx + regionParam.ts (+ vitest)</name>
  <files>site/src/lib/mapFilterDefs.ts, site/src/components/map/FilterFields.tsx, site/src/lib/regionParam.ts, site/src/lib/regionParam.test.ts</files>
  <behavior>
    regionParam.test.ts (vitest, pure function, no DOM/fetch needed):
    - Test 1: `resolveRegionFocus('?region=13', idx)` where `idx` contains at least one entry with `region_id: '13'` returns an array of every matching CUT (order not asserted beyond "same CUTs present").
    - Test 2: `resolveRegionFocus('?region=99', idx)` where no entry has `region_id: '99'` returns `null` (unknown region — silent, no throw).
    - Test 3: `resolveRegionFocus('', idx)` (no `?region=` param at all) returns `null`.
    - Test 4: `resolveRegionFocus('?region=13&cut=13101', idx)` still resolves the `region` param correctly and ignores `cut` (proves it does not accidentally consume the sibling `?cut=` param or require it).
  </behavior>
  <action>
    Create `site/src/lib/mapFilterDefs.ts` exporting the `ChipDef` interface, `CHIP_DEFS` array, and `AVAILABLE_YEARS` array — ported verbatim (same 8 entries, same `familyIndex` mapping, same CEAD_EARLIEST_YEAR=2005 derivation) from `FiltersRow.tsx:20-52`, importing `FAMILY_DEFS_EN`/`FAMILY_DEFS_ES` from `../lib/familyDefs` exactly as `FiltersRow.tsx:18` does today (MAPSH-07: shared vocabulary via the existing data module, not a new code dependency on the news pages).

    Create `site/src/components/map/FilterFields.tsx` exporting three components, none of which renders a `<details>`, `<dialog>`, or any disclosure wrapper — they render only the interactive controls themselves, so they can be reused unwrapped inside `FilterSheet`'s dialog body (Plan 30-04) and wrapped in `<details>` panels inside `EntryPointsRail` (Plan 30-03):
    - `YearField({ lang, year, onYearChange })`: a native `<select>` over `AVAILABLE_YEARS`, `aria-label` per lang (mirrors `FiltersRow.tsx:76-85`).
    - `FamilyField({ lang, crimeFamily, onFamilyChange })`: a `<div role="radiogroup" aria-label={map_entry_family label}>` wrapping one `<button role="radio" aria-checked>` per `CHIP_DEFS` entry (NOT `role="group"` — spec §5 explicitly calls that invalid for `role="radio"` children). Port the accent-dot and `title` tooltip behavior from `FiltersRow.tsx:99-107` verbatim onto the featured/non-active entries.
    - `ModeField({ lang, mode, onModeChange })`: a `<div role="radiogroup" aria-label={map_entry_mode label}>` wrapping two `role="radio"` buttons for `'composite'`/`'family'`, replacing the plain `role="group"` markup at `MapIsland.tsx:440-472` — this component owns only the JSX/markup; the recolor side-effect logic stays in `MapIsland.tsx`'s `onModeChange` callback (wired in Plan 30-05, per F-47's exact scope).
    Import all three's props types from a small local `Props` union or per-component interface — do not import from `MapIsland.tsx` (would create a reverse dependency); `MapIsland.tsx` instead imports these three from here in Plan 30-05.

    Create `site/src/lib/regionParam.ts` exporting `resolveRegionFocus(search: string, idx: CommuneIndexEntry[]): string[] | null`. Behavior: parse `new URLSearchParams(search).get('region')`; if absent, return `null`; if present, filter `idx` for `c.region_id === focusRegion` and return the array of `c.cut` values if non-empty, else `null` (unknown value — silent). Import `CommuneIndexEntry` as a type-only import from `./../components/map/SearchBox` (existing export, `SearchBox.tsx:20-25`) — do not redefine the interface. This function performs no fetch and no DOM mutation — it is a pure data transform, callable from a test with a plain array and string, and callable from `MapIsland.tsx`'s `loadData()` closure in Plan 30-05 with the real `idx` local variable (Pitfall 1 — MUST be called with the local `idx`, never the `communeIndex` state, because the state has not committed yet in the same closure).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run src/lib/regionParam.test.ts && npx vitest run src/lib/regionParam.test.ts 2>&1 | grep -E "4 passed|[4-9][0-9]* passed"</automated>
  </verify>
  <done>`mapFilterDefs.ts` exports `CHIP_DEFS` (8 entries, homicide included) and `AVAILABLE_YEARS`; `FilterFields.tsx` exports `YearField`/`FamilyField`/`ModeField` with `FamilyField`/`ModeField` using `role="radiogroup"`/`role="radio"` (never `role="group"`); `regionParam.ts` exports `resolveRegionFocus`; all 4 vitest cases in `regionParam.test.ts` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `window.location.search` → `resolveRegionFocus` | Untrusted URL query string parsed client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-30-02 | Tampering | `regionParam.ts` `?region=` value | mitigate | Value is used only as an opaque lookup key against an already-loaded, code-controlled array (`idx`) — never interpolated into a fetch URL, DOM string, or `dangerouslySetInnerHTML`; unknown values fail the membership check and return `null`, never throwing or triggering a request (same discipline as the existing `?cut=` handling, `MapIsland.tsx:226-232`'s own comment "opaque string — Map lookup only, no DOM injection") |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed in this plan; verified via `git diff --exit-code -- site/package.json site/package-lock.json` in Plan 30-06 |
</threat_model>

<verification>
`npx vitest run src/lib/regionParam.test.ts` exits non-zero if any of the 4 cases fails or the file does not exist; exits 0 with all 4 passing against the real implementation. The i18n grep gate counts literal key occurrences (not comments) and requires exactly 3 (interface + EN + ES) per key, so a stray comment mentioning a key name without defining it would not falsely pass (a bare `grep -c` would be defeated by a header comment listing the 6 key names — this gate counts `key:` occurrences specifically, avoiding that class of false positive per the CLAUDE.md grep-gate-hygiene note).
</verification>

<success_criteria>
- 4/4 new vitest cases pass; overall suite count becomes 37 + 4 = 41 (recorded for Plan 30-06's final count, not gated here).
- `mapFilterDefs.ts`/`FilterFields.tsx`/`regionParam.ts` exist and are importable with zero TypeScript errors attributable to them (confirmed transitively once Plan 30-03/30-05 import them; `astro check`'s 4/0 baseline is unaffected by this plan in isolation since nothing yet imports these new files into a `.astro` page).
</success_criteria>

<output>
Create `.planning/phases/30-map-control-shell-rework/30-02-SUMMARY.md` when done.
</output>
