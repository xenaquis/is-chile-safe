Fable review: PENDING

---
phase: 30-map-control-shell-rework
plan: 05
type: execute
wave: 4
depends_on: ["30-04"]
files_modified:
  - site/src/components/map/MapTopbar.tsx
  - site/src/components/map/FiltersRow.tsx
  - site/src/components/map/MapIsland.tsx
autonomous: true
requirements: [MAPSH-04, MAPSH-05, MAPSH-07]
must_haves:
  truths:
    - "FiltersRow.tsx is deleted, not refactored — its file no longer exists in the repo"
    - "MapTopbar.tsx composes SearchBox + NewsToggle + EntryPointsRail + FilterSheet, receiving mode/onModeChange as plain props from MapIsland.tsx (F-47 option (a)) instead of a pre-built modeToggle React-node prop"
    - "?region= resolves to a region-extent fitBounds (never a commune selection, never opening ResultPanel), degrades silently for an unknown value, and the pre-existing ?cut= deep link keeps working completely unchanged"
    - "ChoroplethLayer.ts, IncidentPinLayer.ts, and LowZoomDotLayer.ts are byte-identical to their state before this plan runs"
  artifacts:
    - path: "site/src/components/map/MapTopbar.tsx"
      provides: "Rewritten composition: SearchBox + NewsToggle in row 1; EntryPointsRail at >=481px; FilterSheet's FAB+dialog at <=480px, both present in the DOM with CSS media queries deciding visibility"
    - path: "site/src/components/map/MapIsland.tsx"
      provides: "mode-toggle JSX (MapIsland.tsx:439-472) removed; mode/onModeChange passed as plain props; ?region= parsing added via resolveRegionFocus(window.location.search, idx) placed after the existing ?cut= block, computing a union L.latLngBounds over polyIdxRef entries and calling map.fitBounds (never setSelected)"
  key_links:
    - from: "site/src/components/map/MapIsland.tsx"
      to: "site/src/lib/regionParam.ts"
      via: "resolveRegionFocus(window.location.search, idx) called with the local idx variable (Pitfall 1), never the communeIndex state"
      pattern: "resolveRegionFocus\\("
    - from: "site/src/components/map/MapTopbar.tsx"
      to: "site/src/components/map/NewsToggle.tsx, EntryPointsRail.tsx, FilterSheet.tsx"
      via: "import + compose, replacing the FiltersRow import"
      pattern: "from './NewsToggle'"
    - from: "site/src/components/map/MapIsland.tsx"
      to: "ChoroplethLayer.ts / IncidentPinLayer.ts / LowZoomDotLayer.ts"
      via: "UNCHANGED — no import, call signature, or file edit in this plan touches these three files"
      pattern: "N/A — negative assertion, gated via git diff in Plan 30-06"
---

<objective>
Wave 4 of Phase 30 — the wiring plan. Deletes `FiltersRow.tsx`, rewrites `MapTopbar.tsx` to compose the four Wave 1-3 components (`SearchBox` unchanged, `NewsToggle`, `EntryPointsRail`, `FilterSheet`), and makes the one narrowly-scoped `MapIsland.tsx` edit F-47 authorizes: moving the mode-toggle's JSX construction (`MapIsland.tsx:439-472`) out into a plain `mode`/`onModeChange` prop pair, consumed by `EntryPointsRail`'s `ModeField`. Also implements `?region=` (MAPSH-04/F-48) using Wave-1's `resolveRegionFocus`, placed in the same `loadData()` closure as the existing `?cut=` guard but reading the local `idx` variable per the state/closure race documented in Research Pitfall 1.

This is the only plan in the phase that touches `MapIsland.tsx`, and its edit is bounded exactly to what F-47/F-48 authorize: the mode-toggle prop wiring and the `?region=` addition. `ChoroplethLayer.ts`, `IncidentPinLayer.ts`, and `LowZoomDotLayer.ts` are not imported differently, not re-mounted, and not edited at all — the byte-identical gate in Plan 30-06 proves this.

Purpose: complete the control-shell rework's structural change (delete the scroll row, compose the new always-visible controls) and the one new deep-link feature this phase owns, without touching anything the protected Leaflet layer files depend on.

Output: `MapTopbar.tsx` (rewritten), `FiltersRow.tsx` (deleted), `MapIsland.tsx` (bounded edit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-map-control-shell-rework/30-FABLE-CONSTRAINTS.md
@.planning/phases/30-map-control-shell-rework/30-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
@site/src/components/map/MapTopbar.tsx
@site/src/components/map/MapIsland.tsx
@site/src/components/map/NewsToggle.tsx
@site/src/components/map/EntryPointsRail.tsx
@site/src/components/map/FilterSheet.tsx
@site/src/lib/regionParam.ts
</context>

<interfaces>
Current `MapTopbar` props (MapTopbar.tsx:8-21) — the `modeToggle?: React.ReactNode` prop is REMOVED this plan, replaced by `mode: 'composite'|'family'` and `onModeChange: (m:'composite'|'family')=>void`, forwarded straight through to `EntryPointsRail`/`FilterSheet`.

Current mode-toggle construction to remove from MapIsland.tsx (lines 439-472) — the recolor side-effect logic inside each button's onClick (calling `buildStyleMapFromCompositeIndex`/`buildStyleMapFromHomicide`/`buildStyleMapFromFamily`/`buildStyleMapFromLevel` then `applyStyleMap`) MUST be preserved, just relocated into a single `onModeChange` callback passed as a prop — do not duplicate it per-component.

Current ?cut= block to leave untouched (MapIsland.tsx:226-232), immediately followed (still inside the same `if (!cancelled && mapRef.current) { ... }` block, MapIsland.tsx:211-233) by the new ?region= block:
  const regionCuts = resolveRegionFocus(window.location.search, idx);
  if (regionCuts) {
    const bounds = L.latLngBounds([]);
    regionCuts.forEach((cut) => {
      const ly = polyIdxRef.current.get(cut);
      if (ly && 'getBounds' in ly) bounds.extend((ly as L.Polygon).getBounds());
    });
    if (bounds.isValid()) map.fitBounds(bounds, { padding: [40, 40] });
  }
Note: `idx` here is the destructured local variable from `Promise.all` (MapIsland.tsx:169-172), NOT the `communeIndex` state — the state setter `setCommuneIndex(idx)` (line 183) has not committed within this synchronous closure (Pitfall 1). Never call resolveRegionFocus(window.location.search, communeIndex).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete FiltersRow.tsx, rewrite MapTopbar.tsx to compose the new controls</name>
  <files>site/src/components/map/MapTopbar.tsx, site/src/components/map/FiltersRow.tsx</files>
  <action>
    Delete `site/src/components/map/FiltersRow.tsx` entirely (not refactored — Pitfall 4/spec §9a; its `CHIP_DEFS`/`AVAILABLE_YEARS` data already migrated to `mapFilterDefs.ts` in Plan 30-02, so nothing is lost).

    Rewrite `MapTopbar.tsx`: remove the `FiltersRow` import and its render call; remove the `modeToggle?: React.ReactNode` prop; add `mode: 'composite'|'family'` and `onModeChange: (m:'composite'|'family')=>void` to `Props`. Row 1 (`map-topbar-row1`) now renders `SearchBox` + `NewsToggle` (replacing the removed `{modeToggle}` slot). Below row 1, render `EntryPointsRail` (forwarding `lang`, `year`, `onYearChange`, `crimeFamily`, `onFamilyChange`, `mode`, `onModeChange`) AND `FilterSheet` (same forwarded props) both unconditionally in the DOM — let the existing CSS media-query bands (`@media max-width:480px` hides the rail, shows the FAB; wider hides the FAB, shows the rail) decide visibility, exactly the pattern the codebase already uses for `.zoom-ctl` (map.css:257-275, `display:none` by default, `display:flex` at `min-width:640px`). This keeps both controls' `data-role` hooks present in the real DOM at every width, which `score.mjs`'s filter-entry query (`score.mjs:66-73`) explicitly expects — it filters candidates to the one that is actually rendered (`display !== 'none'`), so having both present but CSS-toggled is correct, not redundant markup. Add the required CSS to `map.css` in this same task if not already covered by Plan 30-03/30-04 (`.entry-points-rail` hidden at `<=480px`; `.filter-fab` hidden at `>480px`).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node -e "const fs=require('fs'); const exists=fs.existsSync('src/components/map/FiltersRow.tsx'); if(exists){console.error('FAIL: FiltersRow.tsx still exists'); process.exit(1);} const tb=fs.readFileSync('src/components/map/MapTopbar.tsx','utf8'); const checks=[[!tb.includes('FiltersRow'),'MapTopbar still references FiltersRow'],[!tb.includes('modeToggle'),'MapTopbar still has the old modeToggle prop'],[tb.includes('onModeChange'),'MapTopbar missing onModeChange prop'],[tb.includes('NewsToggle'),'MapTopbar missing NewsToggle import/use'],[tb.includes('EntryPointsRail'),'MapTopbar missing EntryPointsRail import/use'],[tb.includes('FilterSheet'),'MapTopbar missing FilterSheet import/use']]; const fails=checks.filter(function(c){return !c[0];}).map(function(c){return c[1];}); if(fails.length){console.error('FAIL: '+fails.join('; ')); process.exit(1);} console.log('PASS FiltersRow deleted, MapTopbar composes the 4 new controls');"</automated>
  </verify>
  <done>`FiltersRow.tsx` no longer exists; `MapTopbar.tsx` imports and renders `SearchBox`, `NewsToggle`, `EntryPointsRail`, and `FilterSheet`, with `mode`/`onModeChange` as plain props (no `modeToggle` prop remains).</done>
</task>

<task type="auto">
  <name>Task 2: MapIsland.tsx — move mode-toggle wiring out (F-47), add ?region= parsing (F-48)</name>
  <files>site/src/components/map/MapIsland.tsx</files>
  <action>
    Remove the `modeToggle={...}` JSX block (`MapIsland.tsx:439-472`) from the `<MapTopbar>` call. In its place, pass `mode={mode}` and an `onModeChange` callback that, given the target mode, calls `setMode(next)` and then reproduces the exact recolor branch the two removed buttons had inline (checking `next === 'composite'` vs `crimeIsHomicide`/`crimeFamilyIndex !== null` to choose `buildStyleMapFromCompositeIndex`/`buildStyleMapFromHomicide`/`buildStyleMapFromFamily`/`buildStyleMapFromLevel`, then `applyStyleMap`) — this is the same logic, unified into one callback taking the target mode as its argument, so nothing about D-12's "setStyle in place, never re-mount" behavior changes.

    Import `resolveRegionFocus` from `../../lib/regionParam` (a plain value import, not type-only — the function is called at runtime). Immediately after the existing `?cut=` block (after `MapIsland.tsx:232`, still inside the `if (!cancelled && mapRef.current) { ... }` guard), add the `?region=` block exactly as specified in this plan's `<interfaces>` section: call `resolveRegionFocus(window.location.search, idx)` — `idx` is the local destructured variable from the `Promise.all` at the top of `loadData()`, never `communeIndex` (the React state, which has not committed within this same synchronous closure — Pitfall 1). If it returns a non-null CUT array, build an `L.latLngBounds([])`, extend it with each matched polygon's `getBounds()` (looked up via `polyIdxRef.current.get(cut)`, guarding for `undefined`), and call `map.fitBounds(bounds, { padding: [40, 40] })` only `if (bounds.isValid())`. This must NOT call `setSelected` and must NOT open `ResultPanel` (which is gated on `selected !== null`, `MapIsland.tsx:518`) — `?region=` is a zoom-to-extent action, not a commune selection (Q3's explicit distinction). The existing `?cut=` block must not be reordered, edited, or have its regex/timeout logic touched in any way.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/map/MapIsland.tsx','utf8'); const checks=[[!/modeToggle=\{/.test(s),'modeToggle prop block still present'],[s.includes('onModeChange={'),'onModeChange prop wiring missing'],[s.includes(\"regionParam\"),'regionParam import/reference missing'],[s.includes('resolveRegionFocus('),'resolveRegionFocus call missing'],[/focusCut[\s\S]{0,400}resolveRegionFocus/.test(s),'region= block must come after the existing cut= block, not before or interleaved'],[!/setSelected[\s\S]{0,80}resolveRegionFocus|resolveRegionFocus[\s\S]{0,120}setSelected\(/.test(s),'region focus path must not call setSelected — should remain a fitBounds-only action']]; const fails=checks.filter(function(c){return !c[0];}).map(function(c){return c[1];}); if(fails.length){console.error('FAIL: '+fails.join('; ')); process.exit(1);} console.log('PASS mode-toggle moved out, region param wired after cut param, no setSelected coupling');"</automated>
  </verify>
  <done>`MapIsland.tsx` no longer constructs `modeToggle` JSX; `mode`/`onModeChange` are passed as plain props with the recolor logic preserved; `resolveRegionFocus` is imported and called with the local `idx` variable after the unedited `?cut=` block; the region-focus path never calls `setSelected`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `window.location.search` → `?region=`/`?cut=` → `map.fitBounds`/`selectCommune` | Untrusted URL query parsed client-side, used only as an in-memory lookup key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-30-05 | Tampering | `?region=` value flowing into `map.fitBounds` | mitigate | Value only ever indexes into `polyIdxRef`/`idx`, both code-controlled in-memory structures populated from a fetched, same-origin JSON file — never interpolated into a URL, fetch call, or DOM string; an unmatched value yields `null` from `resolveRegionFocus` and the block is a no-op (no fetch, no DOM write, no console error) |
| T-30-06 | Repudiation (of the byte-identical guarantee) | `ChoroplethLayer.ts`/`IncidentPinLayer.ts`/`LowZoomDotLayer.ts` | mitigate | This plan's own task list makes zero edits to these three files; Plan 30-06's `git diff --exit-code` gate is the enforcement mechanism, not a promise |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; verified in Plan 30-06 |
</threat_model>

<verification>
Both automated gates operate on the real, post-edit source files: the first fails if `FiltersRow.tsx` still exists or if `MapTopbar.tsx` still references it, still carries the old `modeToggle` prop, or is missing any of the 3 new component imports; the second fails if the old `modeToggle={` JSX block remains, if `onModeChange`/`resolveRegionFocus` wiring is absent, if the `?region=` block appears before/interleaved with the `?cut=` block (a substring-distance regex check), or if the region-focus path is coupled to `setSelected` (which would wrongly open `ResultPanel`). Both gates exit 0 only once every named condition is real; a stub or partial edit fails at least one check. The byte-identical Leaflet-file proof is deferred to Plan 30-06's `git diff` gate, since asserting "unchanged" from inside this plan (which is the one making the edit) would not be independent evidence.
</verification>

<success_criteria>
- `FiltersRow.tsx` deleted; `MapTopbar.tsx` composes `SearchBox`+`NewsToggle`+`EntryPointsRail`+`FilterSheet` with `mode`/`onModeChange` plain props.
- `MapIsland.tsx`'s mode-toggle JSX is gone, replaced by the prop pair; `?region=` resolves via `resolveRegionFocus(window.location.search, idx)` placed after the unedited `?cut=` block; region focus never sets `selected`.
- `ChoroplethLayer.ts`/`IncidentPinLayer.ts`/`LowZoomDotLayer.ts` untouched (confirmed in Plan 30-06).
</success_criteria>

<output>
Create `.planning/phases/30-map-control-shell-rework/30-05-SUMMARY.md` when done.
</output>
