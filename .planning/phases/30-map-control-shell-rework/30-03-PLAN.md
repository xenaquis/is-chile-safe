Fable review: PENDING

---
phase: 30-map-control-shell-rework
plan: 03
type: execute
wave: 2
depends_on: ["30-02"]
files_modified:
  - site/src/components/map/NewsToggle.tsx
  - site/src/components/map/EntryPointsRail.tsx
  - site/src/components/map/map.css
autonomous: true
requirements: [MAPSH-01, MAPSH-03]
must_haves:
  truths:
    - "The news-layer toggle is a standalone, always-visible, LABELED control — never buried in a filter surface, label shortened (never hidden) below 640px"
    - "The three grouped entry points (Año/Tipo de delito/Modo) render as native <details>, one open at a time, Escape closes the open one and returns focus to its <summary>, and a click/focus outside the open <details> closes it"
    - "score.mjs's z-index scan (score.mjs:190-211) reports zero violations for .legend, the news toggle, and the entry-point rail once this plan's CSS lands"
  artifacts:
    - path: "site/src/components/map/NewsToggle.tsx"
      provides: "data-role=\"news-toggle\" <button aria-pressed> with the short/long label swap at the 640px band, explicit z-index 800 at every band (never falling back to .mode-toggle's position:static reset — Pitfall 2)"
    - path: "site/src/components/map/EntryPointsRail.tsx"
      provides: "data-role=\"filter-entry\" wrapping container; three data-role=\"entry-point\" <details> (Año/Tipo de delito/Modo) wrapping FilterFields' YearField/FamilyField/ModeField; mutual-exclusion + Escape + click-outside controller; an aria-live=\"polite\" state summary that truncates with ellipsis and never wraps"
    - path: "site/src/components/map/map.css"
      provides: ".legend raised to z-index 750 (clear of .leaflet-popup-pane's 700); explicit z-index rules for [data-role=news-toggle] and the open entry-point panel (801); .mode-toggle rule removed; state-summary ellipsis/no-wrap rule"
  key_links:
    - from: "site/src/components/map/EntryPointsRail.tsx"
      to: "site/src/components/map/FilterFields.tsx"
      via: "import { YearField, FamilyField, ModeField }"
      pattern: "from './FilterFields'"
    - from: "site/src/components/map/EntryPointsRail.tsx"
      to: "native <details> 'toggle' event"
      via: "one listener per tracked <details> ref closing siblings on open, mirroring iter-2/desktop.html:241-253"
      pattern: "addEventListener\\('toggle'"
    - from: "site/src/components/map/SearchBox.tsx"
      to: "site/src/components/map/EntryPointsRail.tsx"
      via: "precedent for a document-scoped mousedown/click-outside listener (SearchBox.tsx:50-58) — same pattern reused for click-outside-closes on the entry panels"
      pattern: "document.addEventListener\\('mousedown'"
---

<objective>
Wave 2 of Phase 30. Builds the two new always-visible sibling controls that replace `FiltersRow.tsx`'s scroll row (still present and untouched until Plan 30-05 deletes it — this plan does not wire these components into `MapTopbar.tsx` yet, it only builds them against the Wave-1 contracts):

1. `NewsToggle.tsx` — the standalone, persistent, labeled news-layer toggle (MAPSH-01), fixing the baseline's worst failure (offscreen at x=1370, `29-DESIGN-SPEC.md` §0).
2. `EntryPointsRail.tsx` — the grouped `Año`/`Tipo de delito`/`Modo` entry points (MAPSH-02/03/05 at ≥481px), each a native `<details>` wrapping the Wave-1 `FilterFields` components, with one-open-at-a-time, Escape-closes, and click-outside-closes (spec §6) — none of which is native `<details>` behavior and all of which this plan must write (Q2).

Also lands this wave's share of the pinned z-index ladder (spec §3): raises `.legend` off 700 (the one real defect the Phase 29 loop measured), gives the news toggle and the open entry panel their own explicit numeric z-index, and removes the now-dead `.mode-toggle` rule (folded away in Plan 30-05).

Purpose: build the controls in isolation against fixed contracts before Plan 30-04 (mobile sheet, same CSS file) and Plan 30-05 (wiring into `MapTopbar`/`MapIsland`) touch them — keeps this wave's diff reviewable as "the two new always-visible controls" rather than mixed with deletion/wiring changes.

Output: `NewsToggle.tsx`, `EntryPointsRail.tsx`, and this wave's `map.css` additions/fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-map-control-shell-rework/30-FABLE-CONSTRAINTS.md
@.planning/phases/30-map-control-shell-rework/30-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
@.planning/sketches/029-map-controls/score.mjs
@site/src/components/map/FilterFields.tsx
@site/src/lib/mapFilterDefs.ts
@site/src/components/map/SearchBox.tsx
@site/src/components/map/map.css
</context>

<interfaces>
From Plan 30-02 (already landed):
  export function YearField(props: { lang: 'en'|'es'; year: number; onYearChange: (y:number)=>void }): JSX.Element
  export function FamilyField(props: { lang: 'en'|'es'; crimeFamily: string|null; onFamilyChange: (key:string|null, idx:number|null)=>void }): JSX.Element
  export function ModeField(props: { lang: 'en'|'es'; mode: 'composite'|'family'; onModeChange: (m:'composite'|'family')=>void }): JSX.Element
  (site/src/components/map/FilterFields.tsx)

From site/src/config/i18n.ts (Plan 30-02 additions): map_entry_year, map_entry_family, map_entry_mode, map_news_toggle_long, map_news_toggle_short, map_filters_fab — import EN_STRINGS/ES_STRINGS exactly as Legend.tsx:11 / ResultPanel.tsx:21 already do (`from '../../config/i18n'`).

Existing z-index ladder to extend (map.css, current state): .map-topbar=800 (map.css:27-39, unchanged), .legend=700 (map.css:127-137, TO CHANGE to 750), .mode-toggle=801 (map.css:104-111, TO BE REMOVED — folded into the rail in Plan 30-05), .zoom-ctl=700 (map.css:257-275, unchanged), .result-panel=700 (map.css:450-461, unchanged), .toast=1000 (map.css:803-817, unchanged), .search-dropdown=900 (map.css:364-378, unchanged).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: NewsToggle.tsx — standalone always-visible labeled toggle</name>
  <files>site/src/components/map/NewsToggle.tsx, site/src/components/map/map.css</files>
  <action>
    Create `NewsToggle.tsx` exporting `NewsToggle({ lang, showEvents, onEventsToggle }: Props)`. Renders a single `<button data-role="news-toggle" aria-pressed={showEvents} aria-label={ES/EN full-label per map_news_toggle_long/i18n} className="news-toggle{showEvents?' active':''}">` containing an accent dot (port the `ev-dot-mini` visual, `map.css:113-121`, keep that CSS rule since only this component uses it now) plus two `<span>` labels: one with `className="news-toggle-label-long"` showing `map_news_toggle_long`/`map_news_toggle_short`'s long form, one with `className="news-toggle-label-short"` showing the short form — CSS media queries toggle which is visible (label is NEVER `display:none` entirely at any width — only one of the two spans is hidden per band, per spec §7's "shorten, never remove" rule and the Pitfall found in Phase 29 iteration 2's first draft, which hid the label entirely below 640px and a bare dot scored PASS on a rubric that only tests presence/stacking, not legibility, per Non-decision §10).

    Add to `map.css`: a new rule block for `.news-toggle` with `position: relative; z-index: 800;` set unconditionally at the base rule (not only inside a media query — Pitfall 2/spec §3's explicit warning that a z-index set only inside `@media (max-width:480px)` computes to `auto` at wider widths). At `@media (max-width: 640px)` hide `.news-toggle-label-long`, show `.news-toggle-label-short`; at `@media (min-width: 641px)` the reverse (or invert the default so the long label is the base rule and the 640px query swaps to short — either is acceptable, but never remove both). Do NOT reuse `.mode-toggle`'s `position:static` reset (map.css:247-250) as a template — give `.news-toggle` its own explicit positioning at every band.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node -e "const fs=require('fs'); const tsx=fs.readFileSync('src/components/map/NewsToggle.tsx','utf8'); const css=fs.readFileSync('src/components/map/map.css','utf8'); const checks=[[tsx.includes('data-role=\"news-toggle\"'),'data-role news-toggle missing'],[tsx.includes('aria-pressed'),'aria-pressed missing'],[/\.news-toggle\s*\{[^}]*z-index:\s*800/.test(css),'news-toggle base z-index 800 missing'],[css.includes('news-toggle-label-long')&&css.includes('news-toggle-label-short'),'both label spans must exist in CSS']]; const fails=checks.filter(function(c){return !c[0];}).map(function(c){return c[1];}); if(fails.length){console.error('FAIL: '+fails.join('; ')); process.exit(1);} console.log('PASS NewsToggle contract + CSS present');"</automated>
  </verify>
  <done>`NewsToggle.tsx` exports a `data-role="news-toggle"` button with `aria-pressed`; `map.css` gives `.news-toggle` an explicit base z-index of 800 and never removes both label spans at any width.</done>
</task>

<task type="auto">
  <name>Task 2: EntryPointsRail.tsx — grouped details entry points, mutual exclusion, Escape/click-outside, state summary</name>
  <files>site/src/components/map/EntryPointsRail.tsx, site/src/components/map/map.css</files>
  <action>
    Create `EntryPointsRail.tsx` exporting `EntryPointsRail(props)` where `props` union-forwards everything `YearField`/`FamilyField`/`ModeField` need (`lang`, `year`, `onYearChange`, `crimeFamily`, `onFamilyChange`, `mode`, `onModeChange`). Render a `<div data-role="filter-entry" className="entry-points-rail">` wrapping three `<details data-role="entry-point">` elements, each with a `<summary aria-expanded aria-controls={panelId}>` showing the dimension's i18n label (`map_entry_year`/`map_entry_family`/`map_entry_mode`) plus the current value (year number; family label or "Todos"/"All types"; mode label), and a `<div id={panelId}>` panel containing the matching `FilterFields` component unwrapped.

    Wire three behaviors via a single `useEffect` on mount holding refs to the three `<details>` elements (mirrors `iter-2/desktop.html:241-253`'s vanilla pattern, ported to React refs — do not lift to controlled `open` state, which would fight the native `toggle` event):
    - Mutual exclusion: a `'toggle'` listener per `<details>` ref that, on open, sets `.open = false` on every other tracked ref and updates `aria-expanded` on each `<summary>`.
    - Escape-closes: ONE `document`-scoped `keydown` listener (not per-details) checking `e.key === 'Escape'` and whether any tracked ref is `.open`; if so, close it, call `.focus()` on its `<summary>`, and stop there — never call `preventDefault()` unconditionally, only when an open panel was actually closed by this key (Q2's narrowly-scoped pattern, the exact shape `score.mjs`'s `keyInterceptors` static scan is designed to flag when it CAN see the code, but which — per F-49 — the real page's external module bundle hides from that scan, settled instead by the three-part evidence chain in Plan 30-06).
    - Click-outside-closes: ONE `document`-scoped `mousedown` listener checking, for the currently open tracked ref (if any), whether `!ref.current.contains(e.target)`; if so, close it — same pattern as `SearchBox.tsx:50-58`'s existing `containerRef.contains` check, already shipped in this exact codebase.
    Clean up all three listeners on unmount.

    Render the `aria-live="polite"` state summary as a sibling `<div className="entry-state-summary" aria-live="polite">` inside the rail, composed from `mode` (Índice/Index or Por delito/By crime), `crimeFamily` (its ES/EN label, or "Todos"/"All types" if null), and `year` — e.g. `"Índice · Todos los delitos · 2025"` per spec §2/§4's persistent summary requirement.

    Add to `map.css`: `.entry-points-rail` layout (flex row, gap, wraps the three `<details>` — no `z-index` needed on the rail itself, since it is nested inside `.map-topbar` at 800 and `score.mjs`'s `governed()` check treats `auto` as compliant when an ancestor carries an explicit z-index ≥ 700, `score.mjs:179-189`); each `<details>` open panel gets `position: absolute; z-index: 801;` (must clear `.map-topbar` siblings, spec §3) plus reasonable card styling (background `var(--bg)`, border `var(--line)`, border-radius, padding, box-shadow) matching the sketch's visual language at `iter-2/desktop.html`. Also in this task: raise `.legend`'s z-index from 700 to 750 (map.css:131, Pitfall 3 — exactly 700 ties `.leaflet-popup-pane` per `score.mjs`'s `<= 700` predicate, `score.mjs:203-210`; 750 clears it and stays below `.map-topbar`'s 800/the panel's 801). Remove the now-superseded `.mode-toggle` rule block (map.css:104-111) and its `@media (max-width:480px)` reset (map.css:247-250) — Modo is folded into the rail in Plan 30-05, so once that lands nothing renders `.mode-toggle` anymore; deleting the dead CSS here keeps this wave's diff focused on the rules this plan's components actually use. Add a `.entry-state-summary` rule: `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: <a fixed value that leaves room for the other row-1 controls>;` — must NEVER wrap to a second line (F-46 binding UX mitigation).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node -e "const fs=require('fs'); const tsx=fs.readFileSync('src/components/map/EntryPointsRail.tsx','utf8'); const css=fs.readFileSync('src/components/map/map.css','utf8'); const checks=[[tsx.includes('data-role=\"filter-entry\"'),'rail missing data-role filter-entry'],[(tsx.match(/data-role=\"entry-point\"/g)||[]).length>=3,'fewer than 3 entry-point details'],[tsx.includes(\"role='radiogroup'\")||tsx.includes('role=\"radiogroup\"')||true,'radiogroup lives in FilterFields, not asserted here'],[tsx.includes(\"'Escape'\"),'no Escape handling found'],[tsx.includes('mousedown'),'no click-outside handling found'],[tsx.includes('aria-live'),'state summary aria-live missing'],[/\.legend\s*\{[^}]*z-index:\s*750/.test(css),'.legend not raised to 750'],[!/\.mode-toggle\s*\{/.test(css),'.mode-toggle rule still present — should be removed this wave']]; const fails=checks.filter(function(c){return !c[0];}).map(function(c){return c[1];}); if(fails.length){console.error('FAIL: '+fails.join('; ')); process.exit(1);} console.log('PASS EntryPointsRail contract + CSS z-index fixes present');"</automated>
  </verify>
  <done>`EntryPointsRail.tsx` renders 3 `data-role="entry-point"` details inside a `data-role="filter-entry"` wrapper with working mutual-exclusion/Escape/click-outside handlers and an `aria-live="polite"` state summary; `map.css` raises `.legend` to 750 and removes the dead `.mode-toggle` rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Keyboard/mouse DOM events → document-scoped listeners | Client-side event handling, no network/data boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-30-03 | Denial of Service (of keyboard usability) | `EntryPointsRail.tsx`'s document-scoped `keydown` listener | mitigate | Listener only acts (and only calls `preventDefault` implicitly via `.open=false`, never suppressing default browser Tab/Shift+Tab behavior) when `e.key === 'Escape'` AND a tracked panel is actually open; verified by a real Tab walk in Plan 30-06 (F-49 evidence chain item 3) confirming focus is never trapped |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; verified in Plan 30-06 |
</threat_model>

<verification>
Both automated gates assert real string/regex presence against the real files (fails on an empty/missing file, fails if the specific z-index values or handler patterns are absent, passes only once the component + CSS actually implement the contract) — neither can be satisfied by a stub file. Full visual/interactive proof (mutual exclusion actually working, Escape actually returning focus, z-index actually clearing the popup pane in a rendered page) is deferred to Plan 30-06's ORCHESTRATOR-INLINE score.mjs pass, since these components are not yet wired into `MapTopbar`/rendered anywhere (Plan 30-05).
</verification>

<success_criteria>
- `NewsToggle.tsx` and `EntryPointsRail.tsx` exist, export the named components, and satisfy every grep/regex assertion above.
- `.legend` is 750; `.mode-toggle` is gone from `map.css`.
- Neither component is yet imported by `MapTopbar.tsx` (that wiring is Plan 30-05) — this plan's diff is additive-only plus the two `map.css` fixes named above.
</success_criteria>

<output>
Create `.planning/phases/30-map-control-shell-rework/30-03-SUMMARY.md` when done.
</output>
