Fable review: PENDING

---
phase: 30-map-control-shell-rework
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [site/src/layouts/BaseLayout.astro]
autonomous: false
requirements: [MAPSH-06]
must_haves:
  truths:
    - "An es-locale browser opening an EN page under local dev (http://127.0.0.1:4321) stays on localhost — it is never bounced to https://ischilesafe.com"
    - "The pre-implementation score.mjs baseline against the real, unmodified /es/mapa/ is captured verbatim at all five widths (320/375/481/639/1296) BEFORE any Phase 30 component code changes land"
    - "The c4 (keyboard) baseline value is measured against the real page, not assumed from 29-DESIGN-SPEC.md §0 or STATE.md's prior (possibly stale/inconsistent) figures"
  artifacts:
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "F-40 fix: EN->ES redirect keeps current origin (pathname+search+hash of esLink.href), never jumps to the absolute production URL"
    - path: ".planning/phases/30-map-control-shell-rework/30-BASELINE.md"
      provides: "Verbatim score.mjs JSON per width (320/375/481/639/1296) against the real, unmodified /es/mapa/, plus the verdict() PASS/FAIL table and the resolved c4 tension (F-49)"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro:122-125"
      to: "window.location.replace(...)"
      via: "new URL(esLink.href).pathname + search + hash instead of the raw absolute esLink.href"
      pattern: "new URL\\(esLink\\.href\\)"
---

<objective>
Wave 0 of Phase 30. Two things, in order, neither of which touches the map control-shell components themselves:

1. Fix the real, already-diagnosed F-40 cross-origin redirect defect in `BaseLayout.astro` so that driving `http://127.0.0.1:4321/es/mapa/` (and `/map/`) during the rest of this phase never silently substitutes production for the local build under test.
2. Capture the pre-change `score.mjs` baseline against the real, unmodified `/es/mapa/` at the five widths this phase must reproduce (320/375/481/639/1296), and record the currently-unresolved c4 tension (29-DESIGN-SPEC.md §0 reports `c4 PASS`; `score.mjs`'s own `externalScripts.length > 0` branch, `score.mjs:145-151`, suggests `NOT_MEASURED_FAIL` is equally possible against a real Astro build). F-49 requires this be measured now, not assumed, and is explicit that neither prior document may substitute for a fresh run.

Purpose: every subsequent wave's regression claims ("nothing broke") are meaningless without a real pre-change number to diff against, and F-40's redirect bug would otherwise make every later BrowserOS observation in this phase silently invalid (a tab could bounce to the live production site without any visible error).

Output: one-line fix in `BaseLayout.astro`; `30-BASELINE.md` with the verbatim captured JSON and verdict table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-map-control-shell-rework/30-FABLE-CONSTRAINTS.md
@.planning/phases/30-map-control-shell-rework/30-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
@.planning/sketches/029-map-controls/score.mjs
@site/src/layouts/BaseLayout.astro
</context>

<interfaces>
<!-- Exact block to edit, verified BaseLayout.astro:116-131 -->
The EN-only browser-language-redirect script currently does:
  var esLink = document.querySelector('link[rel="alternate"][hreflang="es"]');
  if (esLink && esLink.href) {
    window.location.replace(esLink.href);
  }
`esLink.href` is an absolute URL built from `BASE_URL` (BaseLayout.astro lines 72-74) — on any non-production origin (including `http://127.0.0.1:4321`) this is an absolute **production** URL, so the replace() call bounces the tab to `https://ischilesafe.com`, silently substituting production for the build under test.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix F-40 — keep current origin on the EN→ES locale redirect</name>
  <files>site/src/layouts/BaseLayout.astro</files>
  <action>
    In the inline EN-locale redirect script (`is:inline`, guarded by `lang === 'en'`, `BaseLayout.astro:117-130`), change the `window.location.replace(esLink.href)` call so it never carries the absolute origin. Build the target from `new URL(esLink.href)` and replace using only `.pathname + .search + .hash` of that parsed URL — never the raw `esLink.href` string. Keep the existing `try/catch` wrapper (fail silently, never block rendering) and the existing `localStorage.getItem('lang-pref')` gate untouched. Do not touch any other line in `BaseLayout.astro`. No `{expr}` interpolation inside the `<script>` block (Astro gotcha — plain JS only, exactly as the surrounding code already is).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node -e "const fs=require('fs'); const s=fs.readFileSync('src/layouts/BaseLayout.astro','utf8'); const m=s.match(/lang === 'en'[\s\S]{0,700}?window\.location\.replace\(([^)]*)\)/); if(!m){console.error('FAIL: redirect block not found'); process.exit(1);} const call=m[1]; const ok = call.includes('new URL(esLink.href)') && call.includes('.pathname') && !/replace\(\s*esLink\.href\s*\)/.test(s); if(!ok){console.error('FAIL: replace() still passes the raw absolute esLink.href — got: '+call); process.exit(1);} console.log('PASS: redirect keeps current origin ('+call+')');"</automated>
  </verify>
  <done>`BaseLayout.astro`'s EN→ES redirect calls `window.location.replace` with an argument built from `new URL(esLink.href).pathname` (plus `search`/`hash`), never the bare `esLink.href`; no other line in the file changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>F-40's one-line redirect fix (Task 1). Nothing else has changed in the map island yet — this checkpoint exists to hand off to the ORCHESTRATOR-INLINE baseline capture below, which requires a live browser session this executor cannot run.</what-built>
  <how-to-verify>
    Automated Task 1 verify above confirms the source fix. The actual regression proof (an es-locale tab on `/map/` staying on `127.0.0.1`) and the score.mjs baseline capture happen in the **ORCHESTRATOR-INLINE** step below — hand control back to the orchestrator session now.
  </how-to-verify>
  <resume-signal>Orchestrator resumes after completing the ORCHESTRATOR-INLINE baseline capture below and writing 30-BASELINE.md.</resume-signal>
</task>

</tasks>

<orchestrator_inline_step>
**ORCHESTRATOR-INLINE — NOT an executor task. `gsd-executor` has no BrowserOS tools; a browser step placed inside an executor task is silently skipped (operating lesson 1). The orchestrator session runs this itself, after Task 1 lands, before Wave 1 starts.**

1. Chain build + validate in one command (OneDrive `dist/` desync rule):
   `cd site && npm run build && npm run validate`
2. Serve the build (no `npm run preview` script exists):
   `npx astro preview --port 4321 --host`
3. In BrowserOS (inline session, never gsd-executor):
   - Navigate to `http://127.0.0.1:4321/es/mapa/` (never `/map/` — even post-fix, prefer `/es/mapa/` for continuity with the Phase 29 baseline per 30-FABLE-CONSTRAINTS.md).
   - **Regression-prove F-40 first**: open `http://127.0.0.1:4321/map/` in a tab with an es-locale `navigator.language` (or set `localStorage['lang-pref']` unset and spoof `navigator.languages`), confirm the tab stays on `127.0.0.1:4321/es/mapa/` and never shows `ischilesafe.com`. Record this as PASS/FAIL in `30-BASELINE.md`.
   - For each width in `[320, 375, 481, 639, 1296]`: emulate via a same-origin iframe served from `http://127.0.0.1:4321` (never `file://` — F-44, opaque origin returns null `contentDocument`); run `evaluate_script` with `(${scoreControlShell.toString()})({stressCondition: 'baseline-<width>'})` (the wrapper invocation is mandatory — a bare function definition returns nothing); record the full JSON verbatim.
   - Run `verdict()` over each captured JSON.
4. Write `.planning/phases/30-map-control-shell-rework/30-BASELINE.md` containing: the F-40 regression-proof result, the five verbatim JSON captures, the five verdict() tables, and an explicit resolution of the c4 tension (state plainly whether the real page returns `c4 PASS` or `c4 NOT_MEASURED_FAIL` at each width, and if they differ across widths, record that too — do not average or summarize away a disagreement).

This step's output (`30-BASELINE.md`) is a required input to Plan 30-06 (the final regression/gate plan), which diffs the pre-change and post-change numbers.
</orchestrator_inline_step>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser `navigator.language`/`localStorage` → redirect target | Client-controlled locale signal drives a client-side same-origin-only navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-30-01 | Tampering/Spoofing | `BaseLayout.astro` EN→ES redirect | mitigate | Redirect target is derived from `new URL(esLink.href).pathname/.search/.hash` only — origin is never taken from an attacker- or environment-influenced value, so the redirect can never leave the current origin regardless of `esLink.href`'s value |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed in this plan; `git diff --exit-code -- site/package.json site/package-lock.json` stays clean (verified in Plan 30-06) |
</threat_model>

<verification>
`node -e` gate above confirms the fix textually; the ORCHESTRATOR-INLINE step is the real regression proof (browser tab stays on localhost) and produces the mandatory pre-change baseline that Plan 30-06 diffs against.
</verification>

<success_criteria>
- `BaseLayout.astro`'s redirect never carries an absolute-origin URL.
- `30-BASELINE.md` exists with five verbatim `score.mjs` captures + verdicts + a settled c4 statement, captured against the real unmodified page.
</success_criteria>

<output>
Create `.planning/phases/30-map-control-shell-rework/30-01-SUMMARY.md` when done.
</output>
