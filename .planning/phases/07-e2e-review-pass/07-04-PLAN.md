---
phase: 07-e2e-review-pass
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - .planning/REVIEW-E2E-FINDINGS.md
  - .planning/ui-reviews/
autonomous: true
requirements: [REVIEW-04]
must_haves:
  truths:
    - "Mobile viewport set to 375×812 before navigation"
    - "Money pages and the map checked at mobile width with screenshots"
    - "Horizontal overflow checked via document.body.scrollWidth > window.innerWidth"
    - "Map remains usable at mobile width; touch targets spot-checked"
    - "REVIEW-04 section appended to REVIEW-E2E-FINDINGS.md (closes Phase 3 deferred mobile UAT)"
  artifacts:
    - path: ".planning/REVIEW-E2E-FINDINGS.md"
      provides: "REVIEW-04 (mobile viewport) section with overflow check results"
      contains: "## REVIEW-04"
    - path: ".planning/ui-reviews/"
      provides: "Mobile screenshots for money pages + map"
  key_links:
    - from: ".planning/REVIEW-E2E-FINDINGS.md"
      to: ".planning/ui-reviews/r04-*.png"
      via: "mobile screenshot path per page"
      pattern: "ui-reviews/r04-"
---

<objective>
Set the browser to a ~375px mobile viewport and check the money pages and the
map for horizontal overflow, map usability, and adequate touch targets,
capturing mobile screenshots and a JS overflow measurement per page.

Purpose: REVIEW-04 — mobile render is verified on the highest-value pages,
closing the visual/mobile UAT deferred from Phase 3 of v1.0.
Output: REVIEW-04 section appended to REVIEW-E2E-FINDINGS.md; r04- screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-e2e-review-pass/07-RESEARCH.md
@.planning/phases/07-e2e-review-pass/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 4): Mobile (375×812) check of money pages + map</name>
  <files>.planning/REVIEW-E2E-FINDINGS.md, .planning/ui-reviews/</files>
  <read_first>
    - .planning/phases/07-e2e-review-pass/07-RESEARCH.md (§ Architecture Patterns — Wave 4 page list; § Validation Architecture — REVIEW-04 scrollWidth check)
    - site/src/components/map/MapIsland.tsx (map controls layout — what "usable" means on mobile)
    - .planning/phases/07-e2e-review-pass/07-01-SUMMARY.md (BrowserOS tool names + base URL)
  </read_first>
  <action>
    Using the BrowserOS tool names and base URL from 07-01-SUMMARY.md, FIRST set
    the viewport to 375×812 (mobile) using the discovered set-viewport tool.
    Confirm the viewport took effect (a screenshot should be ~375px wide). Then,
    for each page below: navigate, screenshot to .planning/ui-reviews/ with an
    r04-mobile- prefix (e.g. r04-mobile-home.png, r04-mobile-map.png), and run
    the discovered evaluate tool to compute
    `document.body.scrollWidth > window.innerWidth` (true = horizontal overflow =
    finding) and record the boolean per page.

    Money pages to check at 375px (both locales where the pair is a money page):
    EN /, /is-chile-safe/, /is-santiago-safe/, /chile-crime-map/,
    /safest-cities-in-chile/, /map/
    ES /es/, /es/mapa-delito-chile/, /es/mapa/

    On /map/ and /es/mapa/ specifically, after navigation confirm the map is
    usable at mobile width: the choropleth is visible, the filters row / topbar
    controls are reachable (not clipped off-screen), and opening the commune
    panel does not break the layout. Spot-check that primary tappable controls
    (nav toggle, filter chips, search, locate) appear visually adequate (~44px);
    where ambiguous, use the evaluate tool to measure a control's bounding-box
    height and record it.

    Log every horizontal-overflow:true page, clipped/unreachable control, or
    unusable map state as a finding (Critical if the page is unusable, Warning
    for overflow/clipping, Polish for tight-but-usable targets). A page with
    overflow:false and reachable controls is a PASS with just an evidence row.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readdirSync('.planning/ui-reviews').filter(f=>f.startsWith('r04-'));if(d.length<9){console.error('Expected >=9 r04- mobile screenshots, got '+d.length);process.exit(1)}console.log('r04 screenshots: '+d.length)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ui-reviews/ contains >=9 r04-mobile- screenshots (money pages + map, both locales where applicable)
    - REVIEW-E2E-FINDINGS.md has a "## REVIEW-04" section recording the document.body.scrollWidth > window.innerWidth boolean for EACH checked page
    - Map usability at 375px is explicitly recorded for /map/ and /es/mapa/ (controls reachable, panel opens without breaking layout)
    - Any overflow:true or unusable-map result carries a severity-tagged finding
  </acceptance_criteria>
  <done>Viewport set to 375×812; all money pages + map screenshotted at mobile width; per-page overflow boolean recorded; map-usability noted; REVIEW-04 section appended.</done>
</task>

</tasks>

<threat_model>
Read-only QA review; no code, no data mutation, no new attack surface — no
applicable threats. Viewport resize + navigation + DOM measurement on a local
dev server only.
</threat_model>

<verification>
- >=9 r04-mobile- screenshots exist
- REVIEW-E2E-FINDINGS.md contains a "## REVIEW-04" section with a per-page overflow boolean
- Map usability at 375px recorded for /map/ and /es/mapa/
</verification>

<success_criteria>
ROADMAP Phase 7 success criterion 4 is satisfied: mobile viewport (~375px) is
checked on money pages and the map — no horizontal overflow (or overflow logged
as a finding), map usable, touch targets spot-checked.
</success_criteria>

<output>
Create `.planning/phases/07-e2e-review-pass/07-04-SUMMARY.md` when done.
Record: per-page overflow booleans, map-usability outcome, and any findings.
</output>
