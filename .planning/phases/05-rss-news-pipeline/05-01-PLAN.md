---
phase: 05-rss-news-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pipeline/requirements.txt
  - pipeline/news/__init__.py
  - pipeline/news/schema.py
  - pipeline/scripts/build_centroids.py
  - pipeline/scripts/audit_incidents.py
  - data/incidents/centroids.json
  - data/incidents/cut_list.json
  - pipeline/tests/test_schema_incidents.py
  - pipeline/tests/test_centroids.py
  - pipeline/tests/test_feeds.py
  - pipeline/tests/test_classifier.py
  - pipeline/tests/test_dedup.py
  - pipeline/tests/test_store.py
  - pipeline/tests/fixtures/feed_biobio_sample.xml
  - pipeline/tests/fixtures/feed_cooperativa_sample.xml
  - pipeline/tests/fixtures/feed_latercera_sample.xml
  - pipeline/tests/fixtures/deepseek_response_sample.json
autonomous: true
requirements: [NEWS-02, NEWS-05]
tags: [python, pydantic, rss, deepseek, pipeline]

must_haves:
  truths:
    - "The IncidentsFile Pydantic model serializes to exactly the IncidentPinLayer.ts Incident/IncidentsFile interface field names"
    - "An incident with a CUT not in the 346-commune set is rejected by the schema"
    - "An incident with an invalid family key is rejected by the schema"
    - "Every CUT in the 346 set resolves to a (lat, lng) centroid inside Chile's bounds"
    - "All six new pytest modules exist and collect without import error"
  artifacts:
    - path: "pipeline/news/schema.py"
      provides: "IncidentRecord + IncidentsFile + ClassifierOutput Pydantic v2 models; VALID_CUTS/VALID_FAMILIES; validate_incidents_file()"
      contains: "class IncidentsFile"
    - path: "pipeline/scripts/build_centroids.py"
      provides: "One-shot TopoJSON->centroids.json generator (Node decode + Shoelace)"
      contains: "def main"
    - path: "data/incidents/centroids.json"
      provides: "CUT -> {lat,lng} lookup for all 346 communes"
    - path: "data/incidents/cut_list.json"
      provides: "Static 346-CUT list (no live CEAD call needed per news run)"
    - path: "pipeline/scripts/audit_incidents.py"
      provides: "D-16 50-incident human-audit dump tool"
      contains: "def main"
    - path: "pipeline/tests/fixtures/deepseek_response_sample.json"
      provides: "Mocked DeepSeek JSON response for classifier unit tests (no live API)"
  key_links:
    - from: "pipeline/news/schema.py"
      to: "data/incidents/cut_list.json"
      via: "module-import load of VALID_CUTS"
      pattern: "cut_list\\.json"
    - from: "pipeline/news/schema.py"
      to: "pipeline/shared/schema.py"
      via: "import FAMILY_KEYS"
      pattern: "from pipeline\\.shared\\.schema import FAMILY_KEYS"
---

<objective>
Build the Wave-0 foundation for the RSS news pipeline: the locked output schema (Pydantic v2 models that serialize to the exact IncidentPinLayer.ts contract), the deterministic centroid + CUT-list static assets, the D-16 audit tool, and ALL new pytest modules + fixtures (with DeepSeek mocked). Every downstream plan depends on these contracts and tests.

Purpose: Define the data contract and test scaffold first (interface-first ordering) so Wave-1 plans implement against fixed signatures with no codebase scavenger hunt, and so feedback sampling has real test files from task 1.
Output: pipeline/news/schema.py, build_centroids.py, audit_incidents.py, centroids.json, cut_list.json, 6 test modules, 4 fixtures, feedparser+openai added to requirements.txt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rss-news-pipeline/05-CONTEXT.md
@.planning/phases/05-rss-news-pipeline/05-RESEARCH.md
@.planning/phases/05-rss-news-pipeline/05-PATTERNS.md
@.planning/phases/05-rss-news-pipeline/05-VALIDATION.md

<interfaces>
<!-- LOCKED output contract — pipeline/news/schema.py MUST serialize to these exact field names. -->
From site/src/components/map/IncidentPinLayer.ts:
```typescript
export interface Incident {
  id: string; cut: string; lat: number; lng: number;
  title_es: string; title_en: string; date: string;
  outlet: string; url: string; family: string;
}
export interface IncidentsFile {
  generated: string; window_days: number; incidents: Incident[];
}
```

From pipeline/shared/schema.py (line 22) — the ONLY valid family values:
FAMILY_KEYS = ["vida","robos_violentos","vif","drogas","armas","propiedad","incivilidades"]

ClassifierOutput (DeepSeek raw response shape, D-06):
{commune_cut: str|null, family: str, title_es: str, title_en: str, summary: str, confidence: float}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Incident schema models + static CUT list + dependency declaration</name>
  <files>pipeline/news/__init__.py, pipeline/news/schema.py, data/incidents/cut_list.json, pipeline/requirements.txt, pipeline/tests/test_schema_incidents.py</files>
  <read_first>
    - pipeline/shared/schema.py (analog — Pydantic v2 model + field_validator + validate_all_communes gate; FAMILY_KEYS at line 22)
    - 05-PATTERNS.md "pipeline/news/schema.py" section (model shape, VALID_CUTS load, FAMILY_KEYS import, banned v1 patterns)
    - site/src/components/map/IncidentPinLayer.ts (LOCKED interface — Incident/IncidentsFile field names; see <interfaces>)
    - pipeline/tests/test_schema.py (analog test structure + ValidationError assertion)
    - 05-RESEARCH.md "Output Schema Fidelity (D-15)" + "Security Domain" (title_es/title_en/summary MUST be PLAIN TEXT, not HTML)
  </read_first>
  <behavior>
    - IncidentsFile.model_validate(valid_dict) succeeds; model_dump() top keys == {generated, window_days, incidents}; each incident == {id,cut,lat,lng,title_es,title_en,date,outlet,url,family}
    - IncidentRecord with cut="99999" (not in 346 set) raises ValidationError
    - IncidentRecord with family="banditry" raises ValidationError
    - IncidentRecord with empty outlet or empty url is rejected (NEWS-05 attribution)
    - ClassifierOutput accepts commune_cut=None; rejects invalid family
  </behavior>
  <action>
    Create pipeline/news/__init__.py (empty package marker, analog pipeline/cead/__init__.py).
    Generate data/incidents/cut_list.json: a JSON array of the 346 CUT code strings sourced from filenames in data/cead/comunas/*.json (filename stem == CUT; 346 files confirmed). Resolves Open Question 2 — the news pipeline reads this static file instead of a live CEAD session. File MUST contain exactly 346 unique string entries.
    Create pipeline/news/schema.py (Pydantic v2 ONLY — no @validator, no parse_obj, no class Config; use @field_validator/@classmethod/model_validate per 05-PATTERNS.md). Define: VALID_FAMILIES = set(FAMILY_KEYS) imported from pipeline.shared.schema (do NOT redefine families); VALID_CUTS loaded at import from data/incidents/cut_list.json via pathlib.Path(__file__).parents[2]; IncidentRecord (fields exactly id,cut,lat,lng,title_es,title_en,date,outlet,url,family; @field_validator on cut [must be in VALID_CUTS] and family [must be in VALID_FAMILIES]; reject empty outlet/url per NEWS-05/D-15); ClassifierOutput (commune_cut: str|None, family, title_es, title_en, summary, confidence: float); IncidentsFile (generated: str, window_days: int, incidents: list[IncidentRecord]); validate_incidents_file(data) -> IncidentsFile wrapping model_validate (all-or-nothing gate, D-15). Field names are NON-NEGOTIABLE — must match IncidentPinLayer.ts exactly (D-15). title_es/title_en/summary are plain str (security V5 — never inject HTML; downstream IncidentPinLayer escapes on render).
    Append to pipeline/requirements.txt: feedparser==6.0.11 (only genuinely new dep) and openai==2.34.0 (installed locally but ABSENT from requirements.txt — pin it so CI has the DeepSeek client; no deepseek-specific SDK).
    Create pipeline/tests/test_schema_incidents.py covering the behaviors above (no live API; pure model validation).
  </action>
  <verify>
    <automated>cd "$REPO" && python -c "import json; c=json.load(open('data/incidents/cut_list.json')); assert len(c)==346 and len(set(c))==346, len(c); print('cut_list OK')" && pytest pipeline/tests/test_schema_incidents.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - cut_list.json has exactly 346 unique string CUTs
    - schema.py uses @field_validator only: grep -v '^#' pipeline/news/schema.py | grep -c '@validator' yields 0; no parse_obj present
    - schema.py imports FAMILY_KEYS from pipeline.shared.schema (does not redefine the 7 families)
    - test_schema_incidents.py: invalid CUT, invalid family, empty outlet/url all raise ValidationError; valid IncidentsFile round-trips to exact TS field names
    - requirements.txt contains feedparser==6.0.11 and openai==2.34.0
    - pytest pipeline/tests/test_schema_incidents.py -x exits 0
  </acceptance_criteria>
  <done>schema.py rejects CUT-not-in-346, invalid family, empty attribution; serializes to exact IncidentPinLayer.ts field names; cut_list.json (346) committed; feedparser+openai pinned; schema tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Centroid generator + centroids.json + centroid tests</name>
  <files>pipeline/scripts/build_centroids.py, data/incidents/centroids.json, pipeline/tests/test_centroids.py</files>
  <read_first>
    - pipeline/scripts/fetch_ine_population.py (exact analog — REPO_ROOT path, main()+__main__ guard, atomic write, Santiago sanity-check, SystemExit on missing)
    - 05-PATTERNS.md "pipeline/scripts/build_centroids.py" section (Node TopoJSON decode subprocess, Shoelace ring_centroid, MultiPolygon largest-ring rule, object key communes-rekeyed / prop id)
    - 05-RESEARCH.md "Commune Centroids (D-08)" (Node decode via site/node_modules/topojson-client; Shoelace; bbox fallback for island/elongated communes per Assumption A2)
    - pipeline/tests/test_catalog.py (analog test structure)
    - 05-PATTERNS.md "pipeline/tests/test_centroids.py" (monkeypatch _CENTROIDS reset; minimal fixture)
  </read_first>
  <action>
    Create pipeline/scripts/build_centroids.py (one-shot, mirrors fetch_ine_population.py). REPO_ROOT=parents[2]; TOPO_PATH=data/cead/geo/communes.topo.json; OUTPUT_PATH=data/incidents/centroids.json. Decode TopoJSON->GeoJSON by shelling out to Node using the already-installed site/node_modules/topojson-client (subprocess.run(["node","-e",script]); object key "communes-rekeyed"; property "id" = CUT string) — avoids any new Python geo dependency (D-08, Claude's-discretion centroid method). Compute each commune centroid in pure Python via the Shoelace ring_centroid formula (GeoJSON coords are [lng,lat]; return lat,lng); for MultiPolygon use the largest sub-polygon ring by absolute area; for degenerate/elongated/island communes where the centroid falls outside the polygon bounding box, fall back to bounding-box center (Assumption A2). Write via atomic_write_json from pipeline.shared.atomic_write. Sanity-check before write: assert len(centroids)==346, centroids["13101"] lat in (-34,-33) & lng in (-71,-70), and all lat in (-56,-17) lng in (-110,-66) (Chilean bounds incl. Easter Island) else SystemExit. Then RUN the script to produce committed data/incidents/centroids.json.
    Create pipeline/tests/test_centroids.py: load generated centroids.json; assert 346 keys, every value float lat/lng within Chilean bounds, "13101" near Santiago. monkeypatch any module cache reset per PATTERNS analog.
  </action>
  <verify>
    <automated>cd "$REPO" && python pipeline/scripts/build_centroids.py && python -c "import json; c=json.load(open('data/incidents/centroids.json')); assert len(c)==346; v=c['13101']; assert -34<v['lat']<-33 and -71<v['lng']<-70; print('centroids OK')" && pytest pipeline/tests/test_centroids.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - build_centroids.py runs to completion; data/incidents/centroids.json has exactly 346 CUT keys
    - Every centroid lat in (-56,-17) and lng in (-110,-66); "13101" within Santiago bounds
    - No new Python geometry dependency added (requirements.txt has no shapely/topojson)
    - build_centroids.py uses atomic_write_json and has main()+__main__ guard
    - pytest pipeline/tests/test_centroids.py -x exits 0
  </acceptance_criteria>
  <done>centroids.json (346 valid Chilean centroids) generated via Node-decode + Shoelace, zero new Python deps; centroid tests green.</done>
</task>

<task type="auto">
  <name>Task 3: RSS fixtures + mocked DeepSeek fixture + audit tool + Wave-1 test scaffolds</name>
  <files>pipeline/tests/fixtures/feed_biobio_sample.xml, pipeline/tests/fixtures/feed_cooperativa_sample.xml, pipeline/tests/fixtures/feed_latercera_sample.xml, pipeline/tests/fixtures/deepseek_response_sample.json, pipeline/tests/test_feeds.py, pipeline/tests/test_classifier.py, pipeline/tests/test_dedup.py, pipeline/tests/test_store.py, pipeline/scripts/audit_incidents.py</files>
  <read_first>
    - 05-PATTERNS.md "pipeline/tests/fixtures/feed_*.xml" (RSS 2.0 skeleton; biobio uses <link> not <guid> per Pitfall 2; cooperativa <guid>=canonical; latercera includes <content:encoded> to prove it is NOT sent to LLM, D-01)
    - 05-PATTERNS.md test sections test_feeds/test_classifier/test_dedup/test_store (mock openai pattern, tmp_path store fixture, inline dedup dicts)
    - pipeline/tests/conftest.py (FIXTURES_DIR, pytest.skip-if-missing pattern)
    - pipeline/scripts/fetch_ine_population.py (analog for audit_incidents.py — main()+guard, SystemExit on missing file, read-only)
    - 05-VALIDATION.md (per-task verify map — exact test function names each module must contain; D-16 audit row)
    - 05-RESEARCH.md "DeepSeek Classifier Test Strategy" (MOCK openai.Client.chat.completions.create — NEVER live API)
  </read_first>
  <action>
    Create 3 RSS fixture XML files (5 items each, committed static analog of cead_response_sample.html): feed_biobio_sample.xml (2 crime, 2 sport, 1 ambiguous; <link> canonical, <guid> is a ?p= WordPress id to exercise Pitfall 2; HTML in <description>); feed_cooperativa_sample.xml (5 policial items; <guid>=canonical URL; plain-text descriptions); feed_latercera_sample.xml (2 crime, 3 non-crime; include <content:encoded> full-article HTML so a test can assert it is NOT used per D-01; <guid isPermaLink="true"> canonical).
    Create pipeline/tests/fixtures/deepseek_response_sample.json — a known-good ClassifierOutput JSON ({commune_cut:"13101", family:"propiedad", title_es, title_en, summary, confidence:0.87}) used to MOCK openai responses (D-05/D-06; no live API in CI).
    Create the four Wave-1 test modules as scaffolds importing from their (not-yet-existing) Wave-1 modules, each containing the exact test functions named in 05-VALIDATION.md so contracts are fixed before implementation. Use the conftest pytest.skip-if-fixture-missing pattern AND skip-if-module-missing (try/except ImportError -> pytest.skip at module top) so collection never errors before Wave 1:
      - test_feeds.py: test_keyword_filter_crime_passes, test_keyword_filter_noncrime_skipped, test_seen_ledger_known_url_skipped, test_fetch_feed_failure_returns_empty, test_biobio_uses_link_not_guid.
      - test_classifier.py: test_invalid_cut_rejected, test_low_confidence_rejected, test_invalid_family (MOCK openai via unittest.mock.patch on pipeline.news.classifier client; load deepseek_response_sample.json; mutate for negative cases).
      - test_dedup.py: test_url_dedup, test_title_similarity, test_no_false_positive (inline incident dicts; threshold 0.82).
      - test_store.py: test_30day_window, test_idempotent_merge, test_archive_write (tmp_path fixture; pre-populated current.json).
    Create pipeline/scripts/audit_incidents.py (D-16 human-UAT gate; analog fetch_ine_population.py): read-only; REPO_ROOT=parents[2]; reads data/incidents/current.json; SystemExit if absent ("run scrape_news.py first"); samples up to 50 random incidents (random.sample, guard if fewer); prints a TSV/table of (title_es, outlet, url, assigned cut + commune name) to stdout for human spot-check of commune hallucination; main()+__main__ guard; no file writes.
  </action>
  <verify>
    <automated>cd "$REPO" && python -c "import feedparser,glob;[print(p,len(feedparser.parse(open(p,encoding='utf-8').read()).entries)) for p in glob.glob('pipeline/tests/fixtures/feed_*.xml')]" && pytest pipeline/tests/ -q --collect-only 1>nul && python -c "import ast; ast.parse(open('pipeline/scripts/audit_incidents.py').read()); print('audit parses')"</automated>
  </verify>
  <acceptance_criteria>
    - 3 feed_*.xml fixtures each parse with feedparser to exactly 5 entries
    - feed_biobio_sample.xml: entry.link != entry.id (guid is ?p= form) — exercises Pitfall 2
    - deepseek_response_sample.json validates against ClassifierOutput shape
    - pytest pipeline/tests/ --collect-only succeeds with NO import/collection errors (scaffolds skip-on-missing-module)
    - audit_incidents.py parses, has main()+__main__ guard, performs no writes
    - All four Wave-1 test modules contain the exact test function names listed in 05-VALIDATION.md
  </acceptance_criteria>
  <done>3 RSS fixtures (feedparser-parseable, 5 items each), mocked DeepSeek fixture, D-16 audit tool, and 4 Wave-1 test scaffolds present; full collection clean; no live API anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline -> data/incidents/*.json | Pipeline writes a PUBLIC JSON consumed by the browser map island; untrusted upstream is RSS/LLM text |
| build_centroids.py -> Node subprocess | Local Node invocation decoding a committed TopoJSON asset (trusted input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | schema.py output fields | mitigate | IncidentsFile.model_validate gate before any write; title_es/title_en/summary constrained to plain str (V5) — IncidentPinLayer escapes on render, so no HTML injected here |
| T-05-01-02 | Tampering | build_centroids.py Node subprocess | accept | Decodes a committed, trusted local TopoJSON asset; no network/user input; argv is a fixed inline script |
| T-05-01-SC | Tampering | feedparser / openai pip installs | mitigate | feedparser CITED in CLAUDE.md (authoritative local source); openai already CLAUDE.md standard SDK; both pinned. See blocking-human checkpoint in Plan 02 (T-05-02-SC) before first install |
</threat_model>

<verification>
- `pytest pipeline/tests/test_schema_incidents.py pipeline/tests/test_centroids.py -x -q` green
- `pytest pipeline/tests/ --collect-only` clean (no collection errors)
- data/incidents/{cut_list.json (346), centroids.json (346)} present and valid
- requirements.txt pins feedparser==6.0.11 and openai==2.34.0
</verification>

<success_criteria>
- IncidentsFile/IncidentRecord serialize to exact IncidentPinLayer.ts field names; reject CUT∉346, invalid family, empty attribution
- 346 valid Chilean centroids generated with zero new Python deps
- All 6 new test modules + 4 fixtures present; DeepSeek mocked; collection clean
- D-16 audit tool scaffolded
</success_criteria>

<output>
Create `.planning/phases/05-rss-news-pipeline/05-01-SUMMARY.md` when done
</output>
