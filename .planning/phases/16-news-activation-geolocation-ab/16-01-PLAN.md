---
phase: 16-news-activation-geolocation-ab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/news/resolver.py
  - pipeline/news/classifier.py
  - pipeline/news/schema.py
  - pipeline/tests/test_resolver.py
  - pipeline/tests/test_classifier.py
autonomous: true
requirements: [NEWS-01]
must_haves:
  truths:
    - "A real Chilean commune name resolves to its correct CUT, accent-insensitively"
    - "An unknown/invented commune name resolves to None (never a hallucinated CUT)"
    - "The classifier emits commune_name + region_hint instead of a bare CUT code"
  artifacts:
    - path: "pipeline/news/resolver.py"
      provides: "Deterministic name→(cut, slug) lookup built from data/cead/meta/index.json"
      contains: "def resolve_cut"
    - path: "pipeline/tests/test_resolver.py"
      provides: "RED tests: exact, accent, unknown→None, disambiguation"
      contains: "test_unknown_name_returns_none"
    - path: "pipeline/news/classifier.py"
      provides: "Prompt emits commune_name + region_hint; CUT membership check removed"
      contains: "commune_name"
  key_links:
    - from: "pipeline/news/resolver.py"
      to: "data/cead/meta/index.json"
      via: "read_text at module load, build {normalized_name: cut} map"
      pattern: "index\\.json"
    - from: "pipeline/news/classifier.py"
      to: "pipeline/news/schema.py ClassifierOutput"
      via: "model_validate on commune_name/region_hint fields"
      pattern: "commune_name"
---

<objective>
Redesign the news geolocation so the LLM emits the comuna NAME (plus a region hint) and a NEW deterministic resolver maps that name to a CUT against `data/cead/meta/index.json`. This fixes the ~60-70% wrong-CUT rate caused by handing the model 346 bare opaque codes (classifier.py line 34 `_CUT_LIST_STR` + prompt lines 64-65). Implements NEWS-01.

Purpose: The geolocation fix is the DESIGN, not a vendor switch. The resolver MUST return None for unknown names so a hallucinated CUT can never reach the store. Centroid-from-CUT (coordinates) stays untouched.

Output: `pipeline/news/resolver.py` (new), rewritten classifier prompt + `ClassifierOutput` schema, RED tests for the resolver and extended classifier tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md

<interfaces>
<!-- index.json entry shape (346 entries) — VERIFIED from codebase -->
data/cead/meta/index.json entry: {"cut":"10101","name":"Puerto Montt","slug":"puerto-montt","region_id":"10","population":280955,"low_population":false}

<!-- Current ClassifierOutput (pipeline/news/schema.py lines 81-96) — to be changed -->
class ClassifierOutput(BaseModel):
    commune_cut: str | None   # → REPLACE with commune_name + region_hint
    family: str
    title_es: str
    title_en: str
    summary: str
    confidence: float

<!-- NFD accent-strip pattern already used in frontend (communes/index.astro): -->
<!-- s.normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase()  → Python equivalent below -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pipeline/news/resolver.py with RED tests</name>
  <files>pipeline/news/resolver.py, pipeline/tests/test_resolver.py</files>
  <read_first>
    - .planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md (§ "Resolver module skeleton" — VERIFIED skeleton; § NEWS-01 Code Map for `_normalize` Python pattern)
    - pipeline/news/schema.py (lines 22-30 — the `pathlib.Path(__file__).parents[2] / "data" / ...` path pattern to mirror for loading index.json)
    - pipeline/tests/test_classifier.py (lines 1-43 — fixtures dir + skip-if-not-implemented test scaffold conventions)
  </read_first>
  <behavior>
    - test_exact_match: resolve_cut("Las Condes") returns a tuple (cut, slug) where cut == the index.json CUT for Las Condes and slug == its slug.
    - test_accent_insensitive: resolve_cut("Cochamo") (no accent) resolves to the same CUT as the accented "Cochamó" in index.json.
    - test_unknown_name_returns_none: resolve_cut("Gotham") returns None.
    - test_none_input_returns_none: resolve_cut(None) returns None.
    - test_case_insensitive: resolve_cut("las condes") resolves the same as "Las Condes".
    - test_slug_for_cut: slug_for_cut(<a valid cut>) returns the matching slug; slug_for_cut("99999") returns None.
    - (disambiguation) test_region_hint_unused_when_unique: a unique name resolves regardless of region_hint value.
  </behavior>
  <action>
    Create `pipeline/news/resolver.py` as a module-load deterministic lookup mirroring the geometry re-key `srcCodeToCut` (a pure function, no external state at call time). Load `data/cead/meta/index.json` once at import using `pathlib.Path(__file__).parents[2] / "data" / "cead" / "meta" / "index.json"` (same path logic as schema.py). Build three module-level dicts: `_NAME_TO_CUT` (normalized name→cut), `_NAME_TO_SLUG` (normalized name→slug), `_CUT_TO_SLUG` (cut→slug). Define `_norm(s)` using `unicodedata.normalize('NFD', s.lower())` joined over chars whose `unicodedata.category(c) != 'Mn'` (strips combining diacritics — the Python equivalent of the frontend NFD pattern). Expose `resolve_cut(name: str | None, region_hint: str | None = None) -> tuple[str, str] | None` returning `(cut, slug)` or None when name is falsy or not found; and `slug_for_cut(cut: str) -> str | None`. For region_hint: only consult it if a normalized name ever maps to multiple CUTs (the 346-name set is effectively unique, so a plain dict is sufficient — keep the parameter for API stability and filter by region_id when a multi-map collision is detected, else ignore). Then write `pipeline/tests/test_resolver.py` covering the behaviors above; do NOT use the skip-if-unimplemented guard here (the module ships in this same task). Resolve the expected CUTs by reading index.json inside the test (do not hardcode codes that could drift).
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_resolver.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_resolver.py -x -q` passes (all 7 behaviors green).
    - `resolve_cut("Gotham")` returns None and `resolve_cut(None)` returns None (asserted in tests).
    - `resolve_cut("Cochamo")` equals `resolve_cut("Cochamó")` (accent-insensitive, asserted).
    - resolver.py contains no fenced code that calls any network/LLM API.
  </acceptance_criteria>
  <done>resolver.py exists with resolve_cut + slug_for_cut; all resolver tests pass; unknown/None inputs return None.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch classifier to name-emit + update ClassifierOutput schema</name>
  <files>pipeline/news/classifier.py, pipeline/news/schema.py, pipeline/tests/test_classifier.py</files>
  <read_first>
    - pipeline/news/classifier.py (lines 33-73 — `_CUT_LIST_STR` build + SYSTEM_PROMPT; lines 118-125 — the VALID_CUTS membership reject to remove)
    - pipeline/news/schema.py (lines 81-96 — ClassifierOutput to amend)
    - .planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md (§ NEWS-01 "Required Changes" 2 & 3, and "Commune list string for prompt")
    - pipeline/tests/test_classifier.py (existing mock pattern `_make_mock_response` + `patch("pipeline.news.classifier.client")`)
  </read_first>
  <behavior>
    - test_name_emit_passes_through: a mocked response with commune_name="Las Condes", region_hint="Metropolitana", valid family, confidence 0.9 returns a ClassifierOutput whose commune_name == "Las Condes" (classifier no longer rejects on CUT membership — resolution happens downstream).
    - test_null_commune_name_allowed: a mocked response with commune_name=null, confidence 0.0 returns a ClassifierOutput (or None via the existing confidence gate) WITHOUT raising — never crashes on null location.
    - test_low_confidence_still_rejected: confidence 0.3 → classify returns None (existing threshold preserved).
    - test_invalid_family_rejected: family "banana" → classify returns None (Pydantic validator preserved).
  </behavior>
  <action>
    In `pipeline/news/schema.py`, replace `ClassifierOutput.commune_cut: str | None` with two fields: `commune_name: str | None` and `region_hint: str | None = None`. Keep the existing `family` validator and all other fields/validators unchanged. In `pipeline/news/classifier.py`: delete the `_CUT_LIST_STR` build (line 34); add a `_COMMUNE_LIST_STR` built from `data/cead/meta/index.json` as one entry per line in the form `"<name> (region_id:<region_id>)"` (use the same parents[2]/data/cead/meta/index.json path). Rewrite `SYSTEM_PROMPT` so the JSON contract emits `commune_name` (exact Spanish commune name or null) and `region_hint` (region name/number or null) instead of `commune_cut`; the prompt must still contain the literal word "json" (DeepSeek JSON-mode requirement) and instruct: commune_name MUST be a real Chilean commune spelled in Spanish, never invented; null if location unknown or not a crime item; include the plain-language `_COMMUNE_LIST_STR` reference so the model gets correct spellings. In `classify()`, remove the `commune_cut not in VALID_CUTS` reject block (lines 118-125) — name→CUT resolution + rejection now lives in the resolver (consumed by the orchestrator in Plan 03). Keep the confidence-threshold gate and the empty-response retry. Update the existing tests in `test_classifier.py`: the fixture-based `test_invalid_cut_rejected` no longer applies — replace it with the behaviors above using the same `_make_mock_response`/`patch("pipeline.news.classifier.client")` mock pattern (build mock dicts inline rather than from the old `commune_cut` fixture).
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_resolver.py tests/test_classifier.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "commune_cut" pipeline/news/classifier.py` returns no lines (bare-CUT path fully removed).
    - `grep -n "commune_name" pipeline/news/schema.py` matches the new ClassifierOutput field.
    - SYSTEM_PROMPT still contains the substring "json" (verify: `grep -ci json pipeline/news/classifier.py` >= 1).
    - `pytest tests/test_resolver.py tests/test_classifier.py -x -q` passes; no live API call (client is mocked).
  </acceptance_criteria>
  <done>ClassifierOutput emits commune_name+region_hint; prompt uses named commune list; CUT membership reject removed; classifier tests pass with mocked client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSS feed / LLM → pipeline | Untrusted headline text and LLM-generated `commune_name` cross into the classifier |
| LLM output → resolver | `commune_name` is attacker/model-influenceable; must never become a raw CUT in the store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | classifier `commune_name` | mitigate | resolver `_norm` lookup against the closed 346-name index; any name not in the map → `resolve_cut` returns None (Plan 03 drops the incident). LLM never emits a CUT directly. |
| T-16-02 | Tampering | resolver input | mitigate | `resolve_cut` returns only `(cut, slug)` drawn from index.json — a closed set of 346 known values; no LLM string ever reaches the CUT field unvalidated |
| T-16-03 | Information Disclosure | DEEPSEEK_API_KEY | accept | key read from env only in classifier (existing pattern, never logged); unchanged in this plan |
| T-16-SC | Tampering | npm/pip installs | accept | no new packages installed (unicodedata, pathlib, json are stdlib); audit table not required |
</threat_model>

<verification>
- `cd pipeline && python -m pytest tests/test_resolver.py tests/test_classifier.py -x -q` green
- `cd pipeline && python -m pytest tests/ -q` green (no regression in schema/store/scrape_news tests; note: scrape_news still references `commune_cut` — Plan 03 updates the orchestrator, so a known failure there is acceptable until Wave 3 IF the existing test mocks the classifier; if `tests/test_scrape_news.py` breaks on the schema change, that is expected and addressed in Plan 03)
</verification>

<success_criteria>
- resolver.py resolves real names to correct CUTs accent-insensitively and returns None for unknowns
- classifier emits commune_name + region_hint; no bare-CUT prompt remains
- All resolver + classifier unit tests pass
</success_criteria>

<output>
Create `.planning/phases/16-news-activation-geolocation-ab/16-01-SUMMARY.md` when done
</output>
