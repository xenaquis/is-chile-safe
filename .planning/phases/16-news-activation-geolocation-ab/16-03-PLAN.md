---
phase: 16-news-activation-geolocation-ab
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - pipeline/news/schema.py
  - pipeline/news/store.py
  - pipeline/scrape_news.py
  - pipeline/tests/test_schema_incidents.py
  - pipeline/tests/test_store.py
  - data/incidents/current.json
autonomous: false
requirements: [NEWS-03]
user_setup:
  - service: deepseek
    why: "Live news classification run (or minimax winner from Plan 02)"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "DeepSeek dashboard; gitignored pipeline/.env"
      - name: MINIMAX_API_KEY
        source: "MiniMax dashboard; gitignored pipeline/.env (if minimax is the winner)"
must_haves:
  truths:
    - "Every stored incident carries a resolved cut AND a resolved slug (non-null)"
    - "The orchestrator resolves the commune name via resolve_cut before building an incident"
    - "data/incidents/current.json exists and validates against the IncidentsFile schema"
  artifacts:
    - path: "pipeline/news/schema.py"
      provides: "IncidentRecord gains slug (optional for back-compat)"
      contains: "slug"
    - path: "pipeline/scrape_news.py"
      provides: "Orchestrator resolves name→(cut, slug); drops incident when resolve returns None"
      contains: "resolve_cut"
    - path: "data/incidents/current.json"
      provides: "Real geolocated incidents (live run) or empty-valid fallback"
      contains: "incidents"
  key_links:
    - from: "pipeline/scrape_news.py"
      to: "pipeline/news/resolver.py"
      via: "resolve_cut(result.commune_name, result.region_hint)"
      pattern: "resolve_cut"
    - from: "pipeline/news/store.py build_incident"
      to: "pipeline/news/schema.py IncidentRecord"
      via: "slug passed through to the validated record"
      pattern: "slug"
---

<objective>
Extend the incident schema with `slug`, rewire the orchestrator to resolve the LLM-emitted commune name → (cut, slug) via the Plan-01 resolver, then run the pipeline for real (key-gated) to produce `data/incidents/current.json` with real, correctly-geolocated incidents that all carry non-null cut + slug. Implements NEWS-03.

Purpose: Turn the dark pipeline on. The orchestrator currently reads `result.commune_cut` (removed in Plan 01) — it must now call `resolve_cut(commune_name, region_hint)` and drop the incident if the resolver returns None (anti-hallucination guard).

Output: schema+store+orchestrator updated for slug, current.json produced (live) or empty-valid fallback.

`autonomous: false`: schema/store/orchestrator changes + their unit tests are key-free and complete unconditionally; only Task 3 (the live run) is key-gated with a fallback that writes an empty-valid current.json so downstream NEWS-04 still builds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md
@.planning/phases/16-news-activation-geolocation-ab/16-01-SUMMARY.md
@.planning/phases/16-news-activation-geolocation-ab/16-02-SUMMARY.md

<interfaces>
<!-- Plan 01 resolver: -->
def resolve_cut(name, region_hint=None) -> tuple[str, str] | None   # (cut, slug)
def slug_for_cut(cut) -> str | None

<!-- Current IncidentRecord (schema.py 38-78): id, cut, lat, lng, title_es, title_en, date, outlet, url, family -->
<!-- build_incident (store.py 38-66) keyword args: url, cut, lat, lng, title_es, title_en, date, outlet, family -->
<!-- Orchestrator broken line (scrape_news.py 209): cut = result.commune_cut  ← commune_cut no longer exists -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add slug to IncidentRecord + build_incident (back-compat optional)</name>
  <files>pipeline/news/schema.py, pipeline/news/store.py, pipeline/tests/test_schema_incidents.py, pipeline/tests/test_store.py</files>
  <read_first>
    - pipeline/news/schema.py (lines 38-78 IncidentRecord; line 99-104 IncidentsFile; validate gate 112-114)
    - pipeline/news/store.py (lines 38-66 build_incident; merge_and_write 99-187)
    - .planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md (§ NEWS-03 "Schema After NEWS-01 Redesign"; Pitfall 1 — make slug optional for back-compat)
    - pipeline/tests/test_schema_incidents.py + pipeline/tests/test_store.py (existing assertion style)
  </read_first>
  <behavior>
    - test_incident_record_accepts_slug: an IncidentRecord built with slug="las-condes" validates and exposes .slug.
    - test_incident_record_slug_optional: an IncidentRecord dict WITHOUT slug still validates (slug defaults to None) — back-compat with any pre-existing current.json (Pitfall 1).
    - test_build_incident_passes_slug: build_incident(..., slug="las-condes") returns a dict whose "slug" == "las-condes".
  </behavior>
  <action>
    In `pipeline/news/schema.py`, add `slug: str | None = None` to `IncidentRecord` (Optional with default for back-compat per Pitfall 1 — an existing current.json lacking slug must still validate). Keep all existing validators (cut_must_be_valid, family, outlet, url) unchanged. In `pipeline/news/store.py`, add a `slug: str | None = None` keyword parameter to `build_incident` and include `"slug": slug` in the returned dict. Add the three behavior tests above to test_schema_incidents.py / test_store.py using the existing assertion style.
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_schema_incidents.py tests/test_store.py tests/test_resolver.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "slug" pipeline/news/schema.py` >= 1 and slug is declared with a default (Optional).
    - An IncidentRecord dict without slug validates (test_incident_record_slug_optional green).
    - build_incident returns a dict containing the slug key (test green).
    - `pytest tests/test_schema_incidents.py tests/test_store.py tests/test_resolver.py -x -q` passes.
  </acceptance_criteria>
  <done>IncidentRecord.slug (optional) + build_incident slug param exist; back-compat preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire orchestrator to resolve name→(cut, slug); fix key check</name>
  <files>pipeline/scrape_news.py, pipeline/tests/test_scrape_news.py</files>
  <read_first>
    - pipeline/scrape_news.py (lines 86-93 key check; lines 191-238 classify→cut→centroid→build_incident loop, esp. line 209 `cut = result.commune_cut`)
    - pipeline/news/resolver.py (resolve_cut signature from Plan 01)
    - .planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md (§ NEWS-03 "Key Wiring", Pitfall 6 — provider-aware key check)
    - pipeline/tests/test_scrape_news.py (how the orchestrator is exercised / mocked)
  </read_first>
  <behavior>
    - test_orchestrator_resolves_name: with classify mocked to return a ClassifierOutput(commune_name="Las Condes", region_hint=..., family=valid, confidence=0.9) and a centroid present, the built incident has the resolved cut for Las Condes AND a non-null slug.
    - test_orchestrator_drops_unresolved_name: when classify returns commune_name="Gotham" (resolve_cut → None), the incident is rejected (not written), no crash.
    - test_key_check_provider_aware: with NEWS_PROVIDER=minimax and only MINIMAX_API_KEY set, the orchestrator does NOT early-exit on a missing DEEPSEEK_API_KEY (Pitfall 6).
  </behavior>
  <action>
    In `pipeline/scrape_news.py`: (1) Make the key check provider-aware (Pitfall 6): `key_env = "MINIMAX_API_KEY" if os.environ.get("NEWS_PROVIDER","deepseek")=="minimax" else "DEEPSEEK_API_KEY"`, then read api_key from key_env; keep the graceful exit-0 message (never print the key). (2) Replace the broken `cut = result.commune_cut` (line 209) with a resolver call: `from pipeline.news.resolver import resolve_cut` then `resolved = resolve_cut(result.commune_name, result.region_hint) if result.commune_name else None`; if resolved is None, log a debug reject ("unresolved commune name") and `continue` (anti-hallucination — never let an unresolved/hallucinated name through). Unpack `cut, slug = resolved`. (3) Keep the existing centroid lookup `get_centroid(cut)` unchanged (coordinates stay centroid-from-CUT). (4) Pass `slug=slug` into the `build_incident(...)` call. Update test_scrape_news.py with the three behaviors above (mock `classify`, `get_centroid`, and the merge/write as the existing tests do).
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_scrape_news.py tests/test_resolver.py tests/test_classifier.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "commune_cut" pipeline/scrape_news.py` == 0 (broken reference gone).
    - `grep -c "resolve_cut" pipeline/scrape_news.py` >= 1.
    - test_orchestrator_drops_unresolved_name confirms an unresolved name writes no incident.
    - test_key_check_provider_aware confirms minimax key path (Pitfall 6) works.
    - `pytest tests/test_scrape_news.py tests/test_resolver.py tests/test_classifier.py -x -q` passes.
  </acceptance_criteria>
  <done>Orchestrator resolves name→(cut, slug), drops unresolved names, passes slug to build_incident, provider-aware key check; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live pipeline run → current.json (key-gated, empty-valid fallback)</name>
  <action>Key-gated checkpoint. Run `python pipeline/scrape_news.py` with the winning provider's key present, then assert data/incidents/current.json validates against IncidentsFile and every incident has non-null cut + slug. If the key is absent, write an empty-valid current.json and record "live run pending keys" in 16-VERIFICATION.md. See how-to-verify for exact steps.</action>
  <what-built>schema (slug), store, and orchestrator are rewired and unit-tested key-free. This checkpoint runs the real pipeline, which needs the winning provider's API key in pipeline/.env.</what-built>
  <how-to-verify>
    KEY-ABSENT FALLBACK: if the required key (per the Plan-02 winner; default DEEPSEEK_API_KEY) is missing, the orchestrator already exits 0 with no write. In that case, write an empty-valid `data/incidents/current.json` `{"generated": "<ISO timestamp>", "window_days": 30, "incidents": []}` so the NEWS-04 news page + sync build cleanly, record "live run pending keys" in 16-VERIFICATION.md, and proceed. Do NOT block the phase.

    WITH KEYS:
    1. From repo root: `python pipeline/scrape_news.py` (loads pipeline/.env automatically).
    2. Confirm exit 0 and that `data/incidents/current.json` now exists.
    3. Run `cd pipeline && python -c "import json; from pipeline.news.schema import validate_incidents_file; d=json.load(open('../data/incidents/current.json',encoding='utf-8')); f=validate_incidents_file(d); assert all(i.cut and i.slug for i in f.incidents), 'every incident needs cut+slug'; print('valid', len(f.incidents), 'incidents')"` — MUST print a count with every incident carrying non-null cut + slug.
    4. Spot-check 5-10 incidents: the commune name in the title matches the resolved slug (no obvious wrong-comuna geolocation) — this is the qualitative proof the redesign works on real data.
  </how-to-verify>
  <acceptance_criteria>
    - data/incidents/current.json exists and validates against IncidentsFile (assertion above passes), OR an empty-valid current.json was written with "live run pending keys" recorded in 16-VERIFICATION.md.
    - If incidents are present, every incident has non-null cut AND slug.
    - Pipeline run exited 0 (no traceback).
  </acceptance_criteria>
  <resume-signal>Type "approved" once current.json validates (real or empty-valid fallback), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM commune_name → store | Resolved name must map to a closed-set CUT or the incident is dropped |
| pipeline → data/incidents/current.json | Written file is the contract consumed at build time by the frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Spoofing | orchestrator resolve step | mitigate | resolve_cut returns None for unknown names → orchestrator `continue` (drops incident); only closed-set (cut, slug) from index.json ever reaches build_incident |
| T-16-08 | Tampering | current.json write | mitigate | merge_and_write validates the full payload via validate_incidents_file before atomic write; on ValidationError it preserves last-good (D-15) — a bad slug/cut cannot corrupt the published file |
| T-16-09 | Information Disclosure | API key (provider-aware) | mitigate | provider-aware key check reads from env only; graceful exit-0 message never prints the key value (existing pattern preserved) |
| T-16-SC | Tampering | pip installs | accept | no new packages; audit table not required |
</threat_model>

<verification>
- `cd pipeline && python -m pytest tests/ -q` green (full suite — orchestrator no longer references commune_cut)
- (key-gated) data/incidents/current.json validates; every incident has non-null cut + slug; OR empty-valid fallback written + documented
</verification>

<success_criteria>
- IncidentRecord carries slug; orchestrator resolves name→(cut, slug) and drops unresolved names
- current.json exists and validates (real incidents or empty-valid fallback)
- Full pytest suite green
</success_criteria>

<output>
Create `.planning/phases/16-news-activation-geolocation-ab/16-03-SUMMARY.md` when done
</output>
