---
phase: 16-news-activation-geolocation-ab
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - pipeline/news/classifier.py
  - pipeline/tests/fixtures/golden_set.json
  - pipeline/experiments/ab_score.py
  - .planning/phases/16-news-activation-geolocation-ab/16-VERIFICATION.md
autonomous: false
requirements: [NEWS-02]
user_setup:
  - service: deepseek
    why: "A/B classification leg (DeepSeek provider)"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "DeepSeek dashboard; stored in gitignored pipeline/.env"
  - service: minimax
    why: "A/B classification leg (MiniMax provider)"
    env_vars:
      - name: MINIMAX_API_KEY
        source: "MiniMax dashboard; stored in gitignored pipeline/.env"
must_haves:
  truths:
    - "A labelled golden set of 30-50 incidents with ground-truth commune_name + cut + family exists"
    - "The LLM provider is selectable via the NEWS_PROVIDER env var (deepseek|minimax)"
    - "ab_score.py reports commune accuracy, family accuracy, latency, cost, and output reliability per provider"
    - "The winning provider is documented and wired as the env default"
  artifacts:
    - path: "pipeline/tests/fixtures/golden_set.json"
      provides: "30-50 labelled incidents (ground_truth: commune_name, cut, family)"
      min_lines: 30
    - path: "pipeline/experiments/ab_score.py"
      provides: "Scoring harness: 5 metrics → results/ab_{provider}.json"
      contains: "commune_accuracy"
    - path: "pipeline/news/classifier.py"
      provides: "NEWS_PROVIDER env-configurable client (deepseek default, minimax)"
      contains: "NEWS_PROVIDER"
  key_links:
    - from: "pipeline/experiments/ab_score.py"
      to: "pipeline/news/resolver.py"
      via: "resolve_cut(predicted_name) compared to ground_truth.cut"
      pattern: "resolve_cut"
    - from: "pipeline/news/classifier.py"
      to: "MiniMax API"
      via: "base_url api.minimaxi.chat/v1, model MiniMax-Text-01, no response_format"
      pattern: "minimaxi"
---

<objective>
Build the labelled golden set (30-50 real incidents), make the LLM provider env-configurable (`NEWS_PROVIDER`: deepseek|minimax), build the `ab_score.py` scoring harness, then run the A/B (gated on API keys), document the winner in `16-VERIFICATION.md`, and wire the winner as the default. Implements NEWS-02.

Purpose: Choose the provider on evidence (comuna accuracy, family accuracy, latency, cost, output reliability) and prove the name-emit redesign beats the bare-CUT baseline (~30-40%).

Output: golden_set.json, ab_score.py, provider-configurable classifier, ab_{provider}.json results, winner documented.

This plan is `autonomous: false`: the golden set + scoring harness + provider config are key-free and complete unconditionally; only Task 3 (the live A/B run) is a key-gated checkpoint. If keys are absent at execution time, do Tasks 1-2 fully and pause at Task 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md
@.planning/phases/16-news-activation-geolocation-ab/16-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (pipeline/news/resolver.py): -->
def resolve_cut(name, region_hint=None) -> tuple[str, str] | None   # (cut, slug) or None
def slug_for_cut(cut) -> str | None

<!-- ClassifierOutput after Plan 01 (pipeline/news/schema.py): -->
class ClassifierOutput: commune_name: str | None; region_hint: str | None; family; title_es; title_en; summary; confidence

<!-- MiniMax call differences (CONTEXT Wave-0): -->
<!-- model "MiniMax-Text-01", base_url "https://api.minimaxi.chat/v1", api_key MINIMAX_API_KEY -->
<!-- response_format json_object → HTTP 400 on MiniMax → OMIT; parse JSON from content (may be in json fences) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build golden_set.json + make provider env-configurable</name>
  <files>pipeline/tests/fixtures/golden_set.json, pipeline/news/classifier.py</files>
  <read_first>
    - pipeline/news/classifier.py (lines 43-46 current hardcoded DeepSeek client; lines 140-157 `_call_api`)
    - .planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md (§ NEWS-02 "Provider Selection", "MiniMax Call Differences" table, "MiniMax-compatible `_call_api`")
    - pipeline/tests/fixtures/deepseek_response_sample.json (existing fixture shape)
    - data/cead/meta/index.json (source of truth for ground-truth cut values)
  </read_first>
  <action>
    Create `pipeline/tests/fixtures/golden_set.json`: an array of 30-50 real Chilean crime incidents, each with the keys id, headline, description, ground_truth (an object with commune_name, cut, family), source_outlet, source_url. Coverage MUST include: (a) major cities likely to succeed (Santiago/Las Condes/Providencia/Valparaiso/Concepcion), (b) smaller/ambiguous communes likely to fail (e.g. Cochamo, Quintero, Tome), (c) at least 3 non-crime items where ground_truth.commune_name is null (to measure null_rate), (d) a spread across all crime families (vida, propiedad, etc.). For each labelled item, set ground_truth.cut to the resolver output for ground_truth.commune_name (use the real CUT from index.json — never invent). Then make the classifier provider env-configurable in `pipeline/news/classifier.py`: add `_PROVIDER = os.environ.get("NEWS_PROVIDER", "deepseek").lower()`; construct client and `_MODEL` conditionally — minimax uses OpenAI client with api_key MINIMAX_API_KEY, base_url https://api.minimaxi.chat/v1, `_MODEL` MiniMax-Text-01; else deepseek keeps the existing client with `_MODEL` deepseek-v4-flash. Update `_call_api` to use `model=_MODEL` and include response_format json_object ONLY when `_PROVIDER == "deepseek"`; for minimax, parse JSON from the content string after stripping optional markdown fences (regex anchored start `^` triple-backtick optional json, and anchored end triple-backtick). Default stays deepseek so existing tests are unaffected.
  </action>
  <verify>
    <automated>cd pipeline && python -c "import json; d=json.load(open('tests/fixtures/golden_set.json',encoding='utf-8')); assert 30<=len(d)<=50; assert sum(1 for x in d if x['ground_truth']['commune_name'] is None)>=3; print('golden ok',len(d))" && python -m pytest tests/test_classifier.py tests/test_resolver.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - golden_set.json parses as JSON, has 30-50 entries, >=3 null-commune items (assertion above passes).
    - `grep -c NEWS_PROVIDER pipeline/news/classifier.py` >= 1.
    - With NEWS_PROVIDER unset, `python -c "import pipeline.news.classifier as c; print(c._MODEL)"` prints `deepseek-v4-flash`.
    - `pytest tests/test_classifier.py tests/test_resolver.py -x -q` passes (mocked client; default provider unchanged).
  </acceptance_criteria>
  <done>golden_set.json (30-50 labelled, >=3 null) exists; classifier client + _call_api are NEWS_PROVIDER-driven; deepseek default; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Build ab_score.py scoring harness</name>
  <files>pipeline/experiments/ab_score.py</files>
  <read_first>
    - .planning/phases/16-news-activation-geolocation-ab/16-RESEARCH.md (§ NEWS-02 "Scoring Script" — inputs, 5 metrics, output format)
    - pipeline/news/classifier.py (the `classify` entry point + `_PROVIDER`/`_MODEL` from Task 1)
    - pipeline/news/resolver.py (resolve_cut — to map predicted name → cut for comparison)
    - pipeline/experiments/probe_coverage.py (existing experiments-dir script conventions: argparse, stdout reporting)
  </read_first>
  <action>
    Create `pipeline/experiments/ab_score.py` — a CLI harness with argparse flags `--golden` (default pipeline/tests/fixtures/golden_set.json), `--provider` (deepseek|minimax), and `--output` (default results/ab_{provider}.json). For each golden item it calls the classifier (the provider is selected via the NEWS_PROVIDER env var the script sets before importing/using the client, OR by passing provider through — match whatever the Task-1 module-load contract requires; document the chosen mechanism in a module docstring). It records per-item: predicted commune_name resolved via resolve_cut → predicted_cut, predicted_family, parse success, empty/truncated content, and wall-clock latency. It computes and writes a JSON report with: commune_accuracy (predicted_cut == ground_truth.cut over labelled items), family_accuracy, null_rate (non-crime items correctly null), parse_failure_rate, empty_response_rate, mean_latency_ms, and estimated_cost_usd (token-based; mark the per-token pricing constants as [ASSUMED] in a comment per RESEARCH Assumptions A1/A2). Print a human-readable markdown table to stdout AND write the JSON to --output. The script must NOT run at import (guarded by `if __name__ == "__main__"`) so it never makes live calls during pytest collection. It must exit cleanly with a clear message if the required API key for the chosen provider is absent (mirror scrape_news graceful-exit) rather than crashing.
  </action>
  <verify>
    <automated>cd pipeline && python -c "import ast; ast.parse(open('experiments/ab_score.py',encoding='utf-8').read()); print('ab_score parses')" && python -m pytest tests/ -q -k "classifier or resolver"</automated>
  </verify>
  <acceptance_criteria>
    - ab_score.py parses (ast.parse) and guards execution under `if __name__ == "__main__"` (grep confirms).
    - `grep -c commune_accuracy pipeline/experiments/ab_score.py` >= 1 and all 5 metric names present (commune_accuracy, family_accuracy, null_rate or parse/empty rate, mean_latency_ms, estimated_cost_usd).
    - Importing the module performs no network call (no `client.chat` at module top level — grep that `client.chat` only appears below the main guard or inside functions).
    - `python experiments/ab_score.py --provider deepseek` with NO key prints a graceful key-absent message and exits without traceback (manually observable; not asserted here).
  </acceptance_criteria>
  <done>ab_score.py computes all 5 metrics, writes results/ab_{provider}.json + markdown table, key-absent safe, no import-time API call.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Run A/B (key-gated), document winner, wire default</name>
  <action>Key-gated checkpoint. Run the DeepSeek-vs-MiniMax A/B via ab_score.py against golden_set.json (both providers), compare the 5 metrics, document the winner in 16-VERIFICATION.md, and set the classifier NEWS_PROVIDER default to the winner. If keys are absent, record "A/B pending keys" and keep deepseek as default. See how-to-verify for exact steps.</action>
  <what-built>The provider-configurable classifier, golden_set.json (30-50 labelled), and ab_score.py harness are complete and key-free. This checkpoint runs the actual DeepSeek vs MiniMax A/B which REQUIRES `DEEPSEEK_API_KEY` + `MINIMAX_API_KEY` in pipeline/.env.</what-built>
  <how-to-verify>
    KEY-ABSENT FALLBACK: if either key is missing in pipeline/.env, do NOT fail — record in 16-VERIFICATION.md that the A/B run is pending keys, leave NEWS_PROVIDER default as deepseek, and complete the rest of the phase. The no-key work (Tasks 1-2) already shipped.

    WITH KEYS:
    1. From repo root: `cd pipeline && NEWS_PROVIDER=deepseek python experiments/ab_score.py --provider deepseek --output results/ab_deepseek.json`
    2. `cd pipeline && NEWS_PROVIDER=minimax python experiments/ab_score.py --provider minimax --output results/ab_minimax.json`
    3. Confirm both results/ab_*.json exist and each has commune_accuracy, family_accuracy, mean_latency_ms, estimated_cost_usd, and a reliability metric (parse_failure_rate / empty_response_rate).
    4. Confirm name-emit commune_accuracy beats the ~30-40% bare-CUT baseline (target >70%); if neither provider clears 70%, note the actual numbers and proceed — the relative winner still decides the default.
    5. Pick the winner (primary: commune_accuracy, then output reliability, then cost). Write `16-VERIFICATION.md` with the comparison table + winner rationale.
    6. Wire the winner as default: set the classifier's `os.environ.get("NEWS_PROVIDER", "<winner>")` default to the winning provider.
  </how-to-verify>
  <acceptance_criteria>
    - results/ab_deepseek.json AND results/ab_minimax.json exist with all 5 metrics (OR 16-VERIFICATION.md records "A/B pending keys" with deepseek retained as default).
    - 16-VERIFICATION.md contains a DeepSeek-vs-MiniMax comparison table and a stated winner.
    - The classifier NEWS_PROVIDER default equals the documented winner.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the winner is documented (or "pending keys" recorded), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (DeepSeek/MiniMax) → pipeline | Model output (commune_name, family, titles) is untrusted; validated by Pydantic + resolver |
| env → classifier | API keys cross from gitignored .env into the OpenAI client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Information Disclosure | MINIMAX_API_KEY / DEEPSEEK_API_KEY | mitigate | keys read from gitignored pipeline/.env only; ab_score.py + classifier never log key values; results JSON contains no secrets |
| T-16-05 | Tampering | MiniMax JSON parse (no response_format) | mitigate | strip markdown fences then json.loads; Pydantic ClassifierOutput validates the parsed object; unparseable → counted as parse_failure, not written to store |
| T-16-06 | DoS / cost | A/B + classification volume | mitigate | golden set bounded to 30-50; ab_score iterates the fixed set; classify already retries at most once |
| T-16-SC | Tampering | pip installs | accept | no new packages (openai, argparse, json, time stdlib/existing); audit table not required |
</threat_model>

<verification>
- `cd pipeline && python -m pytest tests/ -q -k "classifier or resolver"` green
- golden_set.json: 30-50 entries, >=3 null-commune items
- ab_score.py: parses, no import-time API call, all 5 metrics present
- (key-gated) results/ab_deepseek.json + results/ab_minimax.json OR documented "pending keys" with deepseek default
</verification>

<success_criteria>
- Labelled golden set + scoring harness exist and are key-free
- Provider is NEWS_PROVIDER-configurable; default = documented winner (or deepseek if A/B pending)
- Winner documented in 16-VERIFICATION.md with the 5-metric comparison
</success_criteria>

<output>
Create `.planning/phases/16-news-activation-geolocation-ab/16-02-SUMMARY.md` when done
</output>
