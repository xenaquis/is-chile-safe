---
phase: 19-tech-debt-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
  - pipeline/scrape_cead.py
  - pipeline/tests/test_scrape_cead.py
autonomous: true
requirements: [TD-01, TD-06]
must_haves:
  truths:
    - "The methodology page (EN) describes the national comparison figure as an unweighted mean of non-low-population commune rates, not population-weighted"
    - "The methodology page (ES) describes the same figure as media simple/no ponderada, not media ponderada por poblacion"
    - "methodology.astro, es/metodologia.astro, data.ts, and the is-chile-safe FAQ are internally consistent on unweighted/typical-commune"
    - "Every emitted map-payload (map-payload.json and per-year map-payload-YYYY.json) carries an explicit top-level partial_year boolean"
  artifacts:
    - path: "site/src/pages/methodology.astro"
      provides: "Corrected unweighted-mean methodology wording (EN)"
      contains: "unweighted"
    - path: "site/src/pages/es/metodologia.astro"
      provides: "Corrected unweighted-mean methodology wording (ES)"
    - path: "pipeline/scrape_cead.py"
      provides: "build_map_payload emits partial_year flag"
      contains: "partial_year"
  key_links:
    - from: "site/src/pages/methodology.astro"
      to: "site/src/lib/data.ts"
      via: "wording matches loadNationalAverage() unweighted-mean semantics"
      pattern: "unweighted"
    - from: "pipeline/scrape_cead.py build_map_payload"
      to: "data/cead/map-payload-*.json"
      via: "top-level partial_year key in emitted payload"
      pattern: "partial_year"
---

<objective>
Fix the trust-critical methodology math contradiction (TD-01) and add the partial_year data flag to map payloads (TD-06).

The methodology pages (EN+ES) CLAIM a "population-weighted mean" for the national comparison figure, but site/src/lib/data.ts (loadNationalAverage / loadRegionalAverage, lines 13-17) computes an UNWEIGHTED mean of non-low-population commune rates, and the is-chile-safe FAQ (is-chile-safe.astro:43) already correctly says "unweighted". data.ts is the source of truth and MUST NOT change — only the methodology prose is fixed (per 19-CONTEXT.md decision TD-01).

Separately, data/cead/map-payload*.json carry only {generated, year, comunas} with no top-level partial_year flag, so the frontend cannot caveat partial years. Add the flag in the generator (TD-06).

Purpose: Eliminate the headline weighted/unweighted contradiction and make partial-year data machine-distinguishable.
Output: Corrected methodology EN+ES prose; map-payload generator emits partial_year.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-tech-debt-sweep/19-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
data.ts is the SOURCE OF TRUTH — do NOT modify it. The methodology prose must MATCH this comment.

From site/src/lib/data.ts:13-17:
  national.json/regions series rate_per_100k are POPULATION-WEIGHTED incidence (true per-capita) and that statement is correct.
  BUT loadNationalAverage()/loadRegionalAverage() — the headline comparison figures shown on commune pages and the
  lowest-incidence ranking — compute the UNWEIGHTED mean of non-low-population commune rates (a typical-commune statistic).

Current WRONG EN text (methodology.astro:191-194):
  "we compute the population-weighted mean of non-low-population commune rates ... This is the correct per-capita comparative figure."
Current WRONG ES text (es/metodologia.astro:193-195):
  "calculamos la media ponderada por poblacion de las tasas comunales de comunas con poblacion >= 10.000 habitantes ... Esta es la cifra comparativa per capita correcta."
Correct reference (is-chile-safe.astro:43 FAQ) already says "unweighted mean".

From pipeline/scrape_cead.py:
  build_map_payload(records, year, all_rates) -> dict   # returns {generated, year, comunas}
  mark_partial_year(series, run_date) sets a year record partial=True when run_date < Dec 31 of that year.
  Main map-payload built at scrape_cead.py:317 for last_complete_year; per-year payloads at :350 via build_map_payload(communes, year=yr, ...).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct methodology national-mean wording EN+ES (TD-01)</name>
  <files>site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro</files>
  <action>
In methodology.astro (paragraph ~186-195 beginning "Important note on national and regional figures"): replace "population-weighted mean of non-low-population commune rates" with "unweighted mean of non-low-population commune rates", and replace the trailing claim "This is the correct per-capita comparative figure." with a clarifying sentence that it is an unweighted average across communes (a typical-commune figure), NOT a true population-weighted per-capita average. Keep the surrounding CEAD sum-totals explanation unchanged. Do NOT alter any separate (and correct) statement that national.json series rates are population-weighted incidence — only the loadNationalAverage comparison-figure paragraph is wrong.
In es/metodologia.astro (paragraph ~187-195): replace "media ponderada por poblacion de las tasas comunales de comunas con poblacion >= 10.000 habitantes" with "media simple (no ponderada) de las tasas comunales de comunas con poblacion >= 10.000 habitantes", and replace "Esta es la cifra comparativa per capita correcta." with a sentence stating it is una cifra de comuna tipica, no un promedio per capita ponderado por poblacion. Preserve EN/ES structural parity (same paragraph + H2 count) and legal-safe tone. Do NOT modify site/src/lib/data.ts.
  </action>
  <verify>
    <automated>cd site; npm run build; npm run validate</automated>
    <grep>grep -ci "unweighted" site/src/pages/methodology.astro returns >=1; grep -in "ponderada por poblaci" site/src/pages/es/metodologia.astro returns 0</grep>
  </verify>
  <done>methodology.astro contains "unweighted" for the comparison figure and no longer claims "population-weighted" for loadNationalAverage; es/metodologia.astro says "media simple (no ponderada)" and drops "media ponderada por poblacion"; build + 12/12 validators green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit partial_year flag in map-payload generator (TD-06)</name>
  <files>pipeline/scrape_cead.py, pipeline/tests/test_scrape_cead.py</files>
  <behavior>
    - build_map_payload returns a dict containing a top-level boolean key partial_year.
    - For a year whose series record has partial=True (current year before Dec 31), partial_year is True.
    - For a fully consolidated year (e.g. 2025 per DQ-PARTIAL-YEAR-2025), partial_year is False.
    - generated, year, comunas keys remain present and unchanged in shape.
  </behavior>
  <action>
Extend build_map_payload(records, year, all_rates) to compute partial_year for the requested year: derive it from whether that year's records are partial (reuse the per-year partial marker set by mark_partial_year — a year is partial if any included commune series record for that year has partial=True, equivalently year == current year and run date < Dec 31). Add partial_year boolean to the returned dict alongside generated/year/comunas. The main payload (scrape_cead.py:317, last_complete_year) is by definition complete so partial_year is False; per-year payloads (:350) reflect their own year. Keep the under-30KB compact-serialization budget intact (a single boolean is negligible; re-run the existing size assertion). Add a unit test in test_scrape_cead.py asserting partial_year is present and correct for one complete and one partial year fixture.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_scrape_cead.py -x -q</automated>
  </verify>
  <done>build_map_payload output includes partial_year; pytest passes for complete-year (False) and partial-year (True) cases; full pipeline suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline -> static JSON | scrape_cead.py writes data/cead/*.json consumed at build by data.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information disclosure | methodology prose vs data.ts | mitigate | Wording corrected to match loadNationalAverage() (unweighted) — removes misrepresentation of own computation |
| T-19-02 | Tampering | map-payload size budget | accept | partial_year is one boolean; existing <30KB assertion still gates the write |
</threat_model>

<verification>
- cd site; npm run build; npm run validate -> build green + 12/12 validators pass (OneDrive-safe single chained command).
- python -m pytest pipeline/tests/ -q -> pipeline suite green.
- grep confirms unweighted wording present (EN) and no "ponderada por poblacion" (ES) on the comparison-figure paragraph.
</verification>

<success_criteria>
- Methodology EN+ES describe the national comparison figure as unweighted (typical-commune), consistent with data.ts + FAQ.
- No user-facing "population-weighted" claim describes the loadNationalAverage comparison figure.
- build_map_payload emits a correct top-level partial_year boolean; tests prove both states.
- Build + all validators + pipeline pytest green.
</success_criteria>

<output>
Create `.planning/phases/19-tech-debt-sweep/19-01-SUMMARY.md` when done
</output>
