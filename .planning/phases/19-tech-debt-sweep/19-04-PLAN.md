---
phase: 19-tech-debt-sweep
plan: 04
type: execute
wave: 3
depends_on: ["19-06"]
files_modified:
  - site/public/robots.txt
  - site/src/pages/is-santiago-safe.astro
  - site/src/pages/es/mapa-seguridad-santiago.astro
  - site/src/components/PageHeader.astro
  - site/src/components/PageFooter.astro
  - site/astro.config.mjs
  - site/src/pages/crime/[family].astro
  - site/src/pages/es/delito/[family].astro
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03, TD-04]
must_haves:
  truths:
    - "site/public/robots.txt exists with an Allow rule and a Sitemap: pointer to the sitemap index"
    - "/is-santiago-safe/ and /es/mapa-seguridad-santiago/ form a reciprocal hreflang pair; the toggle does not send ES users to the EN page"
    - "The glossary is reachable from nav and/or footer in both locales (/glossary/, /es/glosario/)"
    - "Ranking routes have an explicit sitemap-inclusion rule before the catch-all in astro.config.mjs"
    - "/crime/homicide/ redirects to /crime-ranking/homicide/ and /es/delito/homicidios/ redirects to /es/ranking-delito/homicidios/"
  artifacts:
    - path: "site/public/robots.txt"
      provides: "Allow + Sitemap pointer"
      contains: "Sitemap:"
    - path: "site/astro.config.mjs"
      provides: "redirects for homicide vida-page + explicit ranking sitemap rule"
      contains: "redirects"
  key_links:
    - from: "site/src/pages/is-santiago-safe.astro"
      to: "site/src/pages/es/mapa-seguridad-santiago.astro"
      via: "reciprocal enPath/esPath"
      pattern: "mapa-seguridad-santiago"
    - from: "site/astro.config.mjs redirects"
      to: "/crime-ranking/homicide/"
      via: "/crime/homicide/ -> canonical"
      pattern: "crime-ranking/homicide"
---

<objective>
Tighten the SEO spine: add robots.txt (SEO-02), fix the Santiago hreflang pair (SEO-01), surface the glossary in nav/footer + add an explicit ranking sitemap rule (SEO-03), and redirect the ambiguous /crime/homicide/ vida-aggregate page to its canonical target (TD-04).

is-santiago-safe.astro:70-71 sets esPath="/is-santiago-safe/" (self) so both en+es hreflang resolve to the EN page and the toggle strands ES users; pair it with /es/mapa-seguridad-santiago/. No robots.txt exists. The glossary pages (/glossary/, /es/glosario/) exist but are only reachable two hops deep. The sitemap catch-all returns true for ranking routes with no explicit rule. /crime/homicide/ (FAMILY_SLUGS.vida.en='homicide') is an editorially-ambiguous vida-family aggregate; DECISIONS(c) = redirect to its canonical target. The dedicated homicide ranking lives at /crime-ranking/homicide/ (its own comment notes the namespace was chosen to avoid collision with /crime/homicide/); ES canonical is /es/ranking-delito/homicidios/.

Purpose: Structural SEO signals present and audit-safe; no orphaned/ambiguous homicide URL; correct bidirectional hreflang.
Output: robots.txt; Santiago reciprocal pair; glossary in nav+footer; explicit ranking sitemap rule; homicide redirects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-tech-debt-sweep/19-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
PageHeader.astro nav: hardcoded per-locale hrefs (mapHref, communesHref, rankingsHref, methodHref, newsHref) — getRelativeLocaleUrl does NOT translate ES slugs (memory i18n-localized-slug-pitfall). Nav label keys: nav_home, nav_map, nav_communes, nav_rankings, nav_methodology, nav_news. i18n key glossary_link already exists.
PageFooter.astro footer-spine: per-locale hardcoded hrefs (/map/, /communes/, /rankings/, /news/, guide) and a footer-legal row.
astro.config.mjs: defineConfig with i18n + sitemap integration; sitemapFilter(url) returns true for unlisted pages (catch-all at end). Astro supports a top-level redirects: { '/old/': '/new/' } map that emits static redirect pages for static output.
Canonical homicide targets: EN /crime-ranking/homicide/ (Phase 15), ES /es/ranking-delito/homicidios/.
crime/[family].astro getStaticPaths maps catalog.family_keys -> FAMILY_SLUGS[key].en; es/delito/[family].astro -> FAMILY_SLUGS[key].es. The homicide/homicidios slug comes from the vida family. The crime-type-spoke already filters key !== 'vida' (no internal link churn expected).
hreflang.mjs validator asserts EN+ES alternates resolve to existing dist/ files (reciprocity) + x-default points to EN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: robots.txt + Santiago hreflang pair + glossary spine + ranking sitemap rule (SEO-01/02/03)</name>
  <files>site/public/robots.txt, site/src/pages/is-santiago-safe.astro, site/src/pages/es/mapa-seguridad-santiago.astro, site/src/components/PageHeader.astro, site/src/components/PageFooter.astro, site/astro.config.mjs</files>
  <action>
SEO-02: Create site/public/robots.txt with: User-agent: *, then Allow: /, then a blank line, then Sitemap: https://ischilesafe.com/sitemap-index.xml. public/ assets copy to dist/ verbatim.
SEO-01: In is-santiago-safe.astro change esPath to "/es/mapa-seguridad-santiago/" (keep enPath="/is-santiago-safe/"). In es/mapa-seguridad-santiago.astro set enPath="/is-santiago-safe/" and esPath="/es/mapa-seguridad-santiago/" so they reciprocate. Confirm each page JSON-LD url is self-consistent (only change if it hardcodes the wrong alternate).
SEO-03: In PageHeader.astro add a glossary nav link (glossaryHref = locale === 'en' ? '/glossary/' : '/es/glosario/') using the existing t.glossary_link label, placed after methodology. In PageFooter.astro add the same glossary link to the footer-spine row for both locales. In astro.config.mjs sitemapFilter, add an explicit rule BEFORE the catch-all returning true for ranking routes (/crime-ranking/{slug}/, /es/ranking-delito/{slug}/, /rankings/, /es/rankings/) so inclusion is audit-explicit instead of relying on the default return true. Hardcode ES slugs (no getRelativeLocaleUrl).
  </action>
  <verify>
    <automated>cd site; npm run build; npm run validate</automated>
    <grep>dist/robots.txt exists and contains "Sitemap:"; grep -c "mapa-seguridad-santiago" site/src/pages/is-santiago-safe.astro returns >=1; grep -c "glossary\|glosario" site/src/components/PageHeader.astro returns >=1; hreflang.mjs passes</grep>
  </verify>
  <done>robots.txt in dist with Allow + Sitemap; Santiago EN<->ES reciprocal hreflang (validator passes); glossary in nav + footer both locales; explicit ranking sitemap rule present; build + 12/12 validators green.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect /crime/homicide/ and /es/delito/homicidios/ to canonical homicide pages (TD-04)</name>
  <files>site/astro.config.mjs, site/src/pages/crime/[family].astro, site/src/pages/es/delito/[family].astro</files>
  <action>
Add a redirects map to defineConfig in astro.config.mjs:
  redirects: { '/crime/homicide/': '/crime-ranking/homicide/', '/es/delito/homicidios/': '/es/ranking-delito/homicidios/' }
Then prevent the crime/[family].astro and es/delito/[family].astro getStaticPaths from also generating the vida (homicide/homicidios) page so the redirect is not shadowed by a real page: filter out the vida family key in both getStaticPaths (e.g. catalog.family_keys.filter(k => k !== 'vida')). Confirm no internal link targets /crime/homicide/ (grep showed only a code comment); the crime-type-spoke already excludes vida. Ensure the sitemap does not list the redirected old URLs (they are no longer generated). Keep all other family pages (propiedad, etc.) generating normally.
  </action>
  <verify>
    <automated>cd site; npm run build; npm run validate</automated>
    <grep>dist/crime/homicide/index.html is a redirect to /crime-ranking/homicide/ (or absent in favor of the redirect) and dist/crime-ranking/homicide/index.html exists; grep -c "redirects" site/astro.config.mjs returns >=1; coverage.mjs reports zero orphan links</grep>
  </verify>
  <done>/crime/homicide/ and /es/delito/homicidios/ resolve as redirects to the canonical ranking pages; vida no longer generates a competing /crime/ page; no orphan links; build + 12/12 validators green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler -> static site | robots.txt + sitemap shape crawl; redirects consolidate SEO signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Spoofing (SEO) | Santiago hreflang self-loop | mitigate | reciprocal enPath/esPath verified by hreflang.mjs |
| T-19-09 | Repudiation (SEO ambiguity) | /crime/homicide/ orphan | mitigate | static redirect to canonical ranking page; coverage.mjs orphan check |
</threat_model>

<verification>
- cd site; npm run build; npm run validate -> build green + 12/12 validators (OneDrive-safe chained).
- dist/robots.txt present with Sitemap pointer.
- hreflang.mjs passes for the Santiago pair; coverage.mjs reports zero orphans after the redirect.
</verification>

<success_criteria>
- robots.txt with Allow + Sitemap pointer ships in dist.
- Santiago EN<->ES reciprocal hreflang; glossary reachable from nav + footer both locales; explicit ranking sitemap rule.
- /crime/homicide/ + /es/delito/homicidios/ redirect to canonical homicide pages; no competing vida /crime/ page.
- Build + all validators green.
</success_criteria>

<output>
Create `.planning/phases/19-tech-debt-sweep/19-04-SUMMARY.md` when done
</output>
