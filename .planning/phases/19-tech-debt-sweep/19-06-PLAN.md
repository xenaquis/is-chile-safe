---
phase: 19-tech-debt-sweep
plan: 06
type: execute
wave: 2
depends_on: ["19-03"]
files_modified:
  - site/src/config/i18n.ts
  - site/src/pages/commune/[slug].astro
  - site/src/pages/es/comuna/[slug].astro
  - site/src/pages/crime/[family].astro
  - site/src/pages/es/delito/[family].astro
  - site/src/pages/crime-ranking/[crime].astro
  - site/src/pages/es/ranking-delito/[crime].astro
  - site/src/components/CookieConsent.astro
autonomous: true
requirements: [BIL-04]
must_haves:
  truths:
    - "Section headings on commune pages (View on Map, Similar Communes, Rankings by Crime Type) come from i18n.ts, not hardcoded literals per locale"
    - "Crime and crime-ranking table headers + footnotes come from i18n.ts (single source of truth)"
    - "Cookie-consent banner/accept/reject/privacy strings come from i18n.ts, not duplicated inline literals"
    - "EN and ES render the same headings sourced from one keyset; no literal English heading remains in the ES page source"
  artifacts:
    - path: "site/src/config/i18n.ts"
      provides: "New i18n keys for commune section headings, table headers, footnotes, cookie strings (EN_STRINGS + ES_STRINGS)"
      contains: "map_spoke_heading"
    - path: "site/src/components/CookieConsent.astro"
      provides: "Cookie strings derived from i18n.ts"
  key_links:
    - from: "site/src/pages/commune/[slug].astro"
      to: "site/src/config/i18n.ts"
      via: "t.map_spoke_heading / t.similar_communes_heading / t.crime_type_heading"
      pattern: "t\\.(map_spoke|similar_communes|crime_type)"
    - from: "site/src/components/CookieConsent.astro"
      to: "site/src/config/i18n.ts"
      via: "t.cookie_banner_* / t.footer_privacy"
      pattern: "cookie_banner"
---

<objective>
Route hardcoded section headings, table headers, footnotes, and cookie-consent strings through i18n.ts so each is a single source of truth (BIL-04).

Today these strings are duplicated as literals per locale: commune-page spoke headings ("View on Map", "Similar Communes in This Region", "Rankings by Crime Type") at commune/[slug].astro:224,232,239 and the ES literals at es/comuna/[slug].astro; crime-page table headers/footnotes at crime/[family].astro:221-225,252 and es/delito/[family].astro:212-216; crime-ranking table headers at crime-ranking/[crime].astro:253-257,284 and es/ranking-delito/[crime].astro; and CookieConsent.astro:13-20 inline conditionals that duplicate EN_STRINGS/ES_STRINGS.

Purpose: One keyset per string; no per-locale literal drift; cleaner future translation.
Output: New i18n.ts keys + every listed page/component reads them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-tech-debt-sweep/19-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
i18n.ts exports an interface (Strings) plus EN_STRINGS and ES_STRINGS objects. Existing pattern: const t = locale === 'en' ? EN_STRINGS : ES_STRINGS; then t.key. Some keys already exist: family_breakdown_heading (commune page uses t.family_breakdown_heading.replace('{year}',...)), cookie_banner_text, cookie_banner_accept, cookie_banner_reject, footer_privacy, glossary_link.

Hardcoded literals to replace (EN side shown; ES counterpart files hold the ES literals):
  commune/[slug].astro:224 "View on Map"; :232 "Similar Communes in This Region"; :239 "Rankings by Crime Type" (and the "See X on the interactive Chile crime map" CTA + "Communes in {region} with similar..." intro).
  crime/[family].astro:221-225 table headers (Rank/Commune/Region/Rate per 100k/Trend); :250-253 low-pop footnote.
  crime-ranking/[crime].astro:253-257 table headers; :283-285 low-pop footnote.
CookieConsent.astro currently builds bannerText/privacyLabel/acceptLabel/rejectLabel via inline locale ternaries (lines 13-20) — replace with t.cookie_banner_text / t.cookie_banner_accept / t.cookie_banner_reject and t.footer_privacy, keeping privacyHref per-locale.

When adding keys: add to the TS interface AND both EN_STRINGS and ES_STRINGS. Use {year}/{region} placeholders + .replace() where a value is interpolated (matches family_breakdown_heading pattern).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys + wire commune/crime/crime-ranking headings + table headers (BIL-04)</name>
  <files>site/src/config/i18n.ts, site/src/pages/commune/[slug].astro, site/src/pages/es/comuna/[slug].astro, site/src/pages/crime/[family].astro, site/src/pages/es/delito/[family].astro, site/src/pages/crime-ranking/[crime].astro, site/src/pages/es/ranking-delito/[crime].astro</files>
  <action>
Add to i18n.ts (interface + EN_STRINGS + ES_STRINGS) keys: map_spoke_heading, map_spoke_cta (with {name} placeholder), similar_communes_heading, similar_communes_intro ({region} placeholder), crime_type_heading; table header keys th_rank, th_commune, th_region, th_rate_per_100k ({year} placeholder where the EN/ES literal interpolates the year), th_trend; and low_pop_footnote. Use existing ES translations from the ES page literals as the ES values (preserve current wording, ensuring ES uses "comuna"/"comunas"). In commune/[slug].astro and es/comuna/[slug].astro replace the hardcoded spoke headings/intro/CTA with t.* (and .replace() for placeholders). In crime/[family].astro, es/delito/[family].astro, crime-ranking/[crime].astro, es/ranking-delito/[crime].astro replace the hardcoded <th> labels and the low-pop footnote literal with t.* . Do NOT change FAMILY_SLUGS, FAMILY_KEYS, or any JS identifier; do NOT alter the existing family_breakdown_heading usage. Keep CommuneRankingTable/RankingTableEnhancer calls intact.
  </action>
  <verify>
    <automated>cd site; npm run build; npm run validate</automated>
    <grep>grep -c "map_spoke_heading\|similar_communes_heading\|crime_type_heading" site/src/config/i18n.ts returns >=3; grep -c "View on Map" site/src/pages/commune/[slug].astro returns 0</grep>
  </verify>
  <done>New i18n keys exist in interface + EN_STRINGS + ES_STRINGS; commune/crime/crime-ranking pages (EN+ES) render headings + table headers + footnotes from t.*; no hardcoded English heading remains in the ES page sources; build + 12/12 validators green.</done>
</task>

<task type="auto">
  <name>Task 2: Source CookieConsent strings from i18n.ts (BIL-04)</name>
  <files>site/src/components/CookieConsent.astro</files>
  <action>
In CookieConsent.astro, import EN_STRINGS/ES_STRINGS, set const t = locale === 'en' ? EN_STRINGS : ES_STRINGS, and replace the inline bannerText/acceptLabel/rejectLabel/privacyLabel ternaries (lines 13-20) with t.cookie_banner_text, t.cookie_banner_accept, t.cookie_banner_reject, and t.footer_privacy. Keep privacyHref as the existing per-locale URL ternary (it is a slug, not a label). Do NOT change the is:inline localStorage script (it has no {expr} interpolation — keep it that way per astro-script pitfall). If any of those i18n keys is missing or worded differently from the current inline text, reuse the existing key value (do not introduce a second wording).
  </action>
  <verify>
    <automated>cd site; npm run build; npm run validate</automated>
    <grep>grep -c "cookie_banner_text\|cookie_banner_accept\|cookie_banner_reject" site/src/components/CookieConsent.astro returns >=3; grep -c "We use cookies to serve ads" site/src/components/CookieConsent.astro returns 0</grep>
  </verify>
  <done>CookieConsent derives banner/accept/reject/privacy labels from i18n.ts; no duplicated inline English/Spanish literals remain; the inline localStorage script is unchanged; build + 12/12 validators green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n config -> rendered text | static string substitution only; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | i18n placeholder interpolation | accept | .replace('{year}'/'{region}') substitutes trusted build-time values only; no user input |
</threat_model>

<verification>
- cd site; npm run build; npm run validate -> build green + 12/12 validators (OneDrive-safe chained).
- grep confirms new i18n keys present and hardcoded literals removed from page/component sources.
</verification>

<success_criteria>
- Commune section headings, crime/crime-ranking table headers + footnotes, and cookie strings all sourced from i18n.ts.
- EN+ES render from one keyset; no per-locale literal drift.
- Build + all validators green.
</success_criteria>

<output>
Create `.planning/phases/19-tech-debt-sweep/19-06-SUMMARY.md` when done
</output>
