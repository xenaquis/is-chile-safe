---
phase: 24-rankings-ux-dynamic-sortable-tables-and-visual-polish
plan: 03
type: execute
wave: 3
gap_closure: true
depends_on:
  - 24-02-rankings-ux-dynamic-sortable-tables-and-visual-polish
files_modified:
  - site/src/components/CommuneRankingTable.astro
  - site/src/pages/index.astro
  - site/src/pages/es/index.astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "On / and /es/, the two home preview tables (Lowest/Highest Reported Rate) carry NO data-ranking-table attribute and inject NO RankingTableEnhancer script — they are static pre-sorted teasers."
    - "Region pages (/region/*, /es/region/*) and any other CommuneRankingTable caller keep their existing sortable behavior unchanged (data-ranking-table present, enhancer rendered)."
    - "npm run build + npm run validate exit 0; @astrojs/check passes; no new dependencies."
  artifacts:
    - path: "site/src/components/CommuneRankingTable.astro"
      provides: "sortable prop (default true) that gates data-ranking-table flag + RankingTableEnhancer mount + per-row enhancer data-attrs"
      contains: "sortable"
  key_links:
    - from: "index.astro home teasers"
      to: "CommuneRankingTable sortable=false"
      via: "prop omits data-ranking-table so RankingTableEnhancer skips them"
      pattern: "sortable"
---

<objective>
Close GAP-24A (24-02-SUMMARY): home ranking previews carry a vestigial `data-ranking-table`
flag but are not interactively sortable. User decision: accept them as static pre-sorted
teasers and REMOVE the misleading flag (no implied-but-absent affordance). Do NOT make home
sortable.
</objective>

<context>
@.planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-02-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add `sortable` prop to CommuneRankingTable; gate flag + enhancer behind it</name>
  <files>site/src/components/CommuneRankingTable.astro</files>
  <read_first>
    - site/src/components/CommuneRankingTable.astro (current markup: data-ranking-table on line 49, per-row data-rates line 60, RankingTableEnhancer mount line 85)
    - site/src/components/RankingTableEnhancer.astro (hasRatesData guard lines 87-88 — confirms flag-without-data is a silent no-op today)
  </read_first>
  <action>
    Add an optional prop `sortable?: boolean` to the Props interface, defaulting to `true`
    (`const { rows, locale, sortable = true } = Astro.props;`). When `sortable` is true, keep
    today's markup EXACTLY (data-ranking-table + data-rate-col/name-col/format/locale on the
    table; per-row data-rates + data-low-pop; the `<RankingTableEnhancer locale={locale} />`
    mount). When `sortable` is false, render the SAME static table but: (a) omit the
    `data-ranking-table` attribute and the `data-rate-col`/`data-name-col`/`data-rate-format`
    enhancer attributes from the `<table>`; (b) omit the per-row `data-rates` attribute (keep
    the `low-pop` class, the `*` marker, and `data-low-pop` is fine to drop too since it is an
    enhancer hint); (c) do NOT render `<RankingTableEnhancer>`. Use conditional attribute
    spreading or `{sortable && (...)}` so the non-sortable path emits clean static HTML. Keep
    `data-locale` harmless either way. Do not change styles.
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - CommuneRankingTable.astro Props includes `sortable` with default true.
    - When sortable=false: the rendered `<table>` has no `data-ranking-table`; no `<tr data-rates>`; no RankingTableEnhancer script/JSON block emitted for that instance.
    - When sortable=true (default): markup byte-identical to before for region/commune callers.
    - `cd site && npm run build && npm run validate` exits 0; @astrojs/check passes.
  </acceptance_criteria>
  <done>CommuneRankingTable supports a sortable flag; default-true callers unchanged.</done>
</task>

<task type="execute">
  <name>Task 2: Pass sortable={false} on the four home teaser tables (EN + ES)</name>
  <files>site/src/pages/index.astro,site/src/pages/es/index.astro</files>
  <read_first>
    - site/src/pages/index.astro (lines 105 + 111: CommuneRankingTable for lowestRows/highestRows)
    - site/src/pages/es/index.astro (lines 105 + 111: same, locale es)
  </read_first>
  <action>
    In both home pages, add `sortable={false}` to all CommuneRankingTable usages that render
    `lowestRows` and `highestRows` (4 total: EN lowest, EN highest, ES lowest, ES highest).
    Leave the "See full ranking →" / "Browse all communes →" links untouched — they remain the
    path to the full sortable pages. Do not touch region/commune callers.
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - index.astro + es/index.astro: all four lowestRows/highestRows CommuneRankingTable usages pass `sortable={false}`.
    - Built / and /es/ home pages contain NO `data-ranking-table` and NO `rte-strings` enhancer JSON block.
    - Built /crime-ranking/* and /region/* still contain `data-ranking-table` (regression guard).
    - `cd site && npm run build && npm run validate` exits 0.
  </acceptance_criteria>
  <done>Home teasers emit clean static HTML; full ranking surfaces unchanged.</done>
</task>

</tasks>

<verification>
- Build + validate pass; @astrojs/check clean; no new deps.
- Home (/ and /es/) emit no enhancer flag/script; region + crime-ranking + safest-cities unchanged.
</verification>

<success_criteria>
- GAP-24A closed: home previews are honest static teasers with no implied sort affordance.
- No regression to the four PASS surfaces from 24-02.
</success_criteria>

<output>
Create .planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-03-SUMMARY.md when done.
</output>
