---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/cead-scraper.yml
  - pipeline/tests/test_workflow_order.py
  - .github/workflows/news-pipeline.yml
  - pipeline/tests/test_news_deploy_gate.py
autonomous: true
requirements: [HYG-04]
directive_items: ["G-59 / deferred-live: news-pipeline.yml stale checkout on chained runs"]
executor: opus (CI workflows)

must_haves:
  truths:
    - "cead-scraper.yml has no continue-on-error on any data-producing step. The only remaining continue-on-error is the non-data 'Ensure alert label exists' step (F-87 rationale). `grep -c 'continue-on-error: true' .github/workflows/cead-scraper.yml` == 1."
    - "A failing ENUSC, composite-index or map-payload builder now fails the job BEFORE 'Commit data if changed', so partial CEAD data can never be committed silently. HYG-04 supersedes D-11 (G-66). The step order scrape → builders → commit is unchanged."
    - "'Build ENUSC victimization snapshot' carries `if: hashFiles('pipeline/cache/enusc_vhdv.xlsx') != ''`. On Actions the xlsx is absent (pipeline/cache is gitignored; only .gitkeep is tracked), so the step is skipped and build_enusc_enrichment.py reads the tracked data/snapshots/enusc_vhdv.json, instead of failing with FileNotFoundError forever once continue-on-error is gone (R-08, G-74). HYG-04 is exercised only when the Actions scrape succeeds (CI-only); the local path is covered by the DEPLOYMENT.md runbook."
    - "The CEAD failure issue body no longer says 'Run pipeline/scrape_cead.py locally and push'; it says 'Run the DEPLOYMENT.md CEAD quarterly procedure (fresh worktree, empty CEAD cache) and push', pinned textually (R-05)."
    - "news-pipeline.yml checks out `ref: master` (the branch tip at job start), so a run queued behind another run (concurrency group news-pipeline, cancel-in-progress false) starts from the previous run's pushed data commit instead of the trigger-time GITHUB_SHA (G-59 root cause)."
    - "Every existing workflow pin stays green: test_news_health_gate.py step order and gate pins, test_workflow_guards.py persist-credentials/SHA pins, lint-workflows (actionlint + shellcheck + zizmor) and check-secret-hygiene."
  artifacts:
    - path: pipeline/tests/test_workflow_order.py
      provides: "D-11 continue-on-error pins inverted to HYG-04 no-continue-on-error pins for all 4 builder steps + allowlist of the label step + the ENUSC hashFiles `if` pin + the alert-body text pin"
    - path: pipeline/tests/test_news_deploy_gate.py
      provides: "pin: news-pipeline checkout step `with.ref == 'master'` and fetch-depth 0 kept"
  key_links:
    - from: .github/workflows/news-pipeline.yml (checkout)
      to: master tip
      via: "actions/checkout with: ref: master"
      pattern: "ref: master"
---

<objective>
HYG-04: remove continue-on-error from the four data-producing steps of cead-scraper.yml. Also fix the G-59 latent defect routed to this phase: news-pipeline.yml checks out the trigger-time SHA, so chained runs re-classify with a stale seen.json and then fail the push rebase.

Purpose: cead-scraper.yml:71-87 lets any of the four builders fail silently while the job may still commit CEAD data (V-27). G-59 measured the chained-run failure live: dispatch 36089456308 checked out 8a7eff6, which is older than the scheduled run's data commit 9b2eb62, re-classified 19 items (≈ USD 0.005) and aborted on a rebase conflict.
Output: two workflow edits + pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.github/workflows/cead-scraper.yml
@.github/workflows/news-pipeline.yml
@pipeline/tests/test_workflow_order.py
@pipeline/tests/test_news_deploy_gate.py

<interfaces>
Evidence read at 72a683a:
- cead-scraper.yml: :63-69 "Run CEAD scraper" deliberately has NO continue-on-error (CRON-05 comment). The builder steps with `continue-on-error: true` are :71-75 "Build ENUSC victimization snapshot" (:72), :77-79 "Enrich commune JSONs with ENUSC VHDV" (:78), :81-83 "Build composite index" (:82) and :85-87 "Build map payload" (:86). :89-101 is the commit step. :115-122 "Ensure alert label exists" keeps continue-on-error at :117 (non-data, the F-87 pattern also in news-pipeline.yml:141-143). :124-139 is the alert, whose body already says "If the failure is NOT a 403, this is a real regression". That catch-all covers a builder failure after a successful scrape, so there is no copy change (research Open Question 2, resolved in G-66).
- cead-scraper.yml:138 (alert body): "… Run pipeline/scrape_cead.py locally and push. If the failure is NOT a 403, …". fetch_enusc_vhdv.py:123-129 raises FileNotFoundError when pipeline/cache/enusc_vhdv.xlsx is absent under CACHE_ONLY=1; build_enusc_enrichment.py:40 reads the tracked data/snapshots/enusc_vhdv.json.
- test_workflow_order.py:1-15 docstring (D-11 graceful degrade). :117-126 test_composite_index_step_continue_on_error and :129-138 test_map_payload_step_continue_on_error assert `step.get("continue-on-error") is True`. These are the pins HYG-04 must invert. Keep :100-114 (order before commit).
- news-pipeline.yml:13-15 concurrency group news-pipeline, cancel-in-progress false. :37-41 `actions/checkout@3d3c42e5… # v7.0.1` with `fetch-depth: 0` plus a zizmor artipacked ignore comment (persisted credential needed for the push). :93-96 is the commit and push-with-rebase.sh. There is no `ref:` today, so actions/checkout uses GITHUB_SHA (the trigger-time commit), which is the G-59 mechanism.
- test_news_deploy_gate.py (created by 35-02) holds the news-pipeline.yml structure pins; test_workflow_guards.py:512-527 pins persist-credentials (news-pipeline.yml and cead-scraper.yml must have NO persist-credentials key). This plan must not add one.
- Phase-36/37 collision check: neither phase touches these four files (36/37 files_modified lists). 37-06 edits test_workflow_guards.py; this plan does not.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cead-scraper.yml — no continue-on-error on data-producing steps (HYG-04, supersedes D-11)</name>
  <files>.github/workflows/cead-scraper.yml, pipeline/tests/test_workflow_order.py</files>
  <behavior>
    - For each of the 4 builder steps (matched by run containing fetch_enusc_vhdv.py, build_enusc_enrichment.py, build_composite_index.py, build_map_payload.py): "continue-on-error" not in step.
    - Every step of the job that has continue-on-error is named "Ensure alert label exists" (allowlist, exactly 1).
    - The steps for scrape_cead.py < the 4 builders < "Commit data if changed" (the existing order tests stay).
    - A raw-text check with comment lines stripped: `continue-on-error: true` occurs exactly once.
    - The ENUSC step (run contains fetch_enusc_vhdv.py) has `if` == "hashFiles('pipeline/cache/enusc_vhdv.xlsx') != ''"; the other 3 builder steps have no `if`.
    - The raw workflow text contains "Run the DEPLOYMENT.md CEAD quarterly procedure (fresh worktree, empty CEAD cache) and push" and does not contain "Run pipeline/scrape_cead.py locally and push".
  </behavior>
  <action>
Step 0 (G-62): `grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md` must print 1, else STOP.

Delete the four `continue-on-error: true` lines (:72, :78, :82, :86). Add `if: hashFiles('pipeline/cache/enusc_vhdv.xlsx') != ''` to the 'Build ENUSC victimization snapshot' step (R-08, G-74): on Actions the xlsx is absent, so without the guard the step would always fail and, with continue-on-error gone, the job could never commit CEAD data. Above the ENUSC step, add one comment block: "HYG-04 (G-66, supersedes D-11): builders are NOT continue-on-error. A builder failure must fail the job before the commit step, so CEAD data is never committed without its enrichment, composite index or map payload. HYG-04 is exercised only when the Actions scrape succeeds (today CEAD 403s runner IPs, CRON-05, so the scrape step fails first). The ENUSC step runs only when pipeline/cache/enusc_vhdv.xlsx exists (gitignored, absent on Actions; the tracked data/snapshots/enusc_vhdv.json is used). The real quarterly path is local: DEPLOYMENT.md § CEAD quarterly procedure (builders chained with &&, STOP on any red)." In the failure-issue body (:138), replace "Run pipeline/scrape_cead.py locally and push." with "Run the DEPLOYMENT.md CEAD quarterly procedure (fresh worktree, empty CEAD cache) and push." (R-05). In 38-04-SUMMARY.md write: "HYG-04: CI-only; the local path is covered by the runbook (builders chained with &&, STOP on any red)."

In test_workflow_order.py:
- rewrite the module docstring's D-11 paragraph to state the HYG-04 rule;
- replace the two `…_continue_on_error` tests with `test_builder_steps_fail_loudly` (parametrized over the 4 scripts) and `test_only_label_step_has_continue_on_error`;
- add the raw-text count test.
- add `test_enusc_step_guarded_by_xlsx` and `test_alert_body_points_to_runbook` (the two new behaviors).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_workflow_order.py pipeline/tests/test_workflow_guards.py -q -p no:cacheprovider && [ "$(grep -v '^\s*#' .github/workflows/cead-scraper.yml | grep -c 'continue-on-error: true')" -eq 1 ] && grep -qF "hashFiles('pipeline/cache/enusc_vhdv.xlsx') != ''" .github/workflows/cead-scraper.yml && grep -qF "Run the DEPLOYMENT.md CEAD quarterly procedure (fresh worktree, empty CEAD cache) and push" .github/workflows/cead-scraper.yml && ! grep -qF "Run pipeline/scrape_cead.py locally and push" .github/workflows/cead-scraper.yml</automated>
    Expected: exit 0, count 1, the ENUSC `if` guard and the new alert sentence present, the old sentence gone. Negative control: re-add `continue-on-error: true` to "Build map payload", and the parametrized test and the count test must FAIL; remove the ENUSC `if`, and test_enusc_step_guarded_by_xlsx must FAIL. Revert.
  </verify>
  <done>No data-producing CEAD step can fail silently, and the pins encode HYG-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: news-pipeline.yml checkout ref: master (G-59 fix) + pin</name>
  <files>.github/workflows/news-pipeline.yml, pipeline/tests/test_news_deploy_gate.py</files>
  <behavior>
    - With the news-pipeline.yml job steps parsed via yaml.safe_load, the first step `uses` starts with "actions/checkout@", and its with == {"fetch-depth": 0, "ref": "master"}. There are exactly these two keys, and no persist-credentials key.
    - The checkout is still step index 0, and the Health gate step order and pins in test_news_health_gate.py pass unedited.
  </behavior>
  <action>
Add `ref: master` under the checkout `with:` (news-pipeline.yml:38-39), keeping `fetch-depth: 0` and the zizmor comment. Add the comment: "G-59/G-67: check out the branch tip at job start, not GITHUB_SHA. The concurrency group serializes runs, so a queued run must start from the previous run's pushed data commit; otherwise it re-classifies with a stale seen.json and its push rebase conflicts (dispatch 36089456308)."

Add the pin test to test_news_deploy_gate.py, skipped if PyYAML is absent (the existing pattern in that file).

Do not touch cead-scraper.yml's checkout: it is not chained on data and is outside G-59.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_news_deploy_gate.py pipeline/tests/test_news_health_gate.py pipeline/tests/test_workflow_guards.py pipeline/tests/test_workflow_order.py -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-secret-hygiene.sh && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider; rc=$?; echo "pytest rc=$rc"; [ $rc -eq 0 ]</automated>
    Expected: all exit 0. lint-workflows prints its canary line. Full pytest ≥ the Phase-37 close count (floor 642/1/2 at 72a683a) + new tests, 0 failed. Negative control: remove `ref: master`, and the pin must FAIL. Revert.
  </verify>
  <done>Chained news runs start from the current master, and the checkout contract is pinned. Lint and secret hygiene are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Actions → master push | bot commits data to master |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09 | Tampering / integrity | CEAD builders | mitigate | continue-on-error is removed, so partial data is never committed. Pinned. |
| T-38-10 | Denial of service (spend) | chained news runs | mitigate | `ref: master` means no duplicate classification and no rebase-conflict aborts. Pinned. |
| T-38-11 | Elevation of privilege | checkout ref change | accept | The ref names this repository's own protected default branch. There is no untrusted input (zizmor clean) and no change to permissions or persist-credentials. |
</threat_model>

<verification>
- `git diff .github/workflows/` shows only: 4 deleted lines + a comment + the ENUSC `if:` line + the alert-body sentence in cead-scraper.yml, and 1 added `ref:` line + a comment in news-pipeline.yml.
- lint-workflows exit 0 and check-secret-hygiene exit 0.
</verification>

<success_criteria>
HYG-04 holds (1 continue-on-error left, non-data), and the G-59 stale-checkout defect is fixed and pinned.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-04-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

Applied 2026-09-25 from 38-PREMORTEM.md and orchestrator decision G-73.
- R-05: the cead-scraper.yml failure-issue body now points to the DEPLOYMENT.md CEAD quarterly procedure (fresh worktree, empty CEAD cache), with a textual pin (Task 1 action, behavior, verify, truth, artifact provides, verification).
- R-08: the ENUSC step gets `if: hashFiles('pipeline/cache/enusc_vhdv.xlsx') != ''` with a pin; the comment says HYG-04 is exercised only when the Actions scrape succeeds; 38-04-SUMMARY records "CI-only; the local path is covered by the runbook". The continue-on-error count stays 1. This refines G-66 (see G-74).
