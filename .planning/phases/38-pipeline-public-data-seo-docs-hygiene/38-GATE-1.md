---
phase: 38
artifact: adversarial gate
round: 1
plans_evaluated: 38-01..38-09 @ bf1eb9d
verdict: GO-CONDICIONADO
blockers: 3
agents: 5 (haiku, 3 opus lenses high, fresh opus arbiter xhigh) + citation script
subagent_tokens: 1012935
cycle: 1 of 2
---

# Phase 38 — Adversarial Gate, Round 1

## Verdict: GO-CONDICIONADO

Arbitraje de la ronda 1 sobre los planes 38-01..38-09 (HEAD bf1eb9d), revisión R1 del premortem. Verifiqué con evidencia propia, sin escribir nada al repo ni a data/, sin LLM ni CEAD, y con curl GET a prod.

**Bloqueantes confirmados (3).** Los tres se arreglan con ediciones de texto, sin re-diseño:
- **BF-01 (38-08).** El scrape corre a través de `| tee`, así que scrape_rc siempre da 0 (medido: pipe_rc=0, pipefail off). Además, communes_written=346 cuenta los JSON del checkout. Si la validación todo-o-nada o el parse del homicidio lanzan, main devuelve 1 sin escribir nada, y el registro sale 198/198, 346, core_diff 0 → IDENTICAL_LIVE → HYG-06 PASS falso. El runbook de 38-07 repite el defecto. La corrección es pipefail o PIPESTATUS, más 'Pipeline completed successfully.' en el log y last_updated igual a la fecha de la corrida; la medí viable.
- **BF-02 (38-03).** La regex del pin R-16 no reconoce subíndices anidados: da [] sobre las escrituras vivas :320/:432 y sobre la forma via_url de 36-07, y el control negativo del plan no se puede cumplir. Un pin AST sí las detecta (medido: [320, 432]).
- **BF-03 (38-07).** DEPLOYMENT.md § Known Gaps seguiría diciendo que M-07 (continue-on-error) está abierto después de que 38-04 lo cierra, y HYG-05 se firmaría PASS. El precedente es 37-06 con DEP-01.

**Resto.** 15 no bloqueantes. Los más relevantes:
- NB-01: los restos del carve-out en el frontmatter de 38-08 y en ROADMAP (o3).
- NB-02: greps del deferred-live que ya pasan hoy.
- NB-12: los captions '2005–2025' en 3 páginas editoriales prioritarias, medidos en prod.
- NB-13: el '198' fijo del runbook, que en 2027 son 207.
- NB-15 (o4): el cierre del milestone v2.2 no está ni resuelto ni excluido.

Descarté C-R1-06, B-R1-09 y las 3 escaladas, con su razón. Las observaciones o1 y o2 quedan resueltas: las colisiones están declaradas y el vintage y los ranks se recalculan solos; la única ruptura por los movimientos de la 36 es BF-02.

**Veredicto: GO-CONDICIONADO.** La condición es aplicar BF-01, BF-02 y BF-03, más las ediciones previas no aplicadas (G-73(1) en frontmatter/ROADMAP; los gemelos R-14, R-10 y R-17 en los verify de 38-09), y re-verificar con las mediciones pendientes antes de ejecutar.

## Final blockers

| id | origin | title | evidence | required edit |
|---|---|---|---|---|
| BF-01 | A-R1-01 + B-R1-02 (dedup; los dos CONFIRMADO). El árbitro lo verificó con medición y lectura propias. | 38-08: `\| tee` enmascara scrape_rc y communes_written=346 no prueba nada, así que un scraper caído termina registrado como IDENTICAL_LIVE | 38-08-PLAN.md:96-99 (comando con tee, 'rc 0', ls\|wc 346), :108 (verify ^scrape_rc: 0$ / ^communes_written: 346$); pipeline/scrape_cead.py:176-180 (validación antes de escribir), :378 (parse de homicidio fuera de try), :632 (run_date = date.today() local), :658 'Pipeline completed successfully.', :662-663 'Pipeline failed' → return 1, :461/:570/:584 (last_updated = run_date); :31-34 logging INFO (las líneas llegan al log con 2>&1). Medición propia: `bash -c 'python -c "import sys;sys.exit(1)" 2>&1 \| tee /dev/null; echo $?'` → 0; con `set -o pipefail` → 1. 38-07-PLAN.md:115 (gemelo en el runbook). | 38-08 Task 1 Step 3:<br>(1) Correr `set -o pipefail; cd "$SCR/hyg06" && PYTHONPATH=. python pipeline/scrape_cead.py 2>&1 \| tee "$SCR/hyg06-scrape.log"` y registrar scrape_rc desde ese rc (o desde ${PIPESTATUS[0]}). Exigir además `grep -c 'Pipeline completed successfully.' log` == 1 y `grep -c 'Pipeline failed' log` == 0.<br>(2) Redefinir communes_written como el número de data/cead/comunas/*.json del worktree con last_updated == la fecha LOCAL de la corrida (scrape_cead usa date.today() local, no la UTC de hyg06_date). Exigir 346, más 16 regions/*.json y national.json con el mismo last_updated.<br>(3) Si falla cualquiera de las dos cosas, el outcome es ABORTED (sin commit). Agregarlo a la regla de soundness de Task 2 y al verify de Task 1.<br>(4) 38-07 Task 2, ítem 4 del procedimiento: reemplazar '346 comunas' por '346 comunas reescritas (last_updated = hoy) y el log con "Pipeline completed successfully."'. |
| BF-02 | B-R1-01 (CONFIRMADO; A-R1-C04 afirmó lo contrario sobre la cobertura del pin: queda refutado por la medición). El árbitro lo verificó con medición propia. | 38-03: el pin estructural de R-16 no detecta las escrituras reales `seen[x["k"]] = …` (subíndice anidado) y su control negativo no puede fallar | 38-03-PLAN.md:22 (truth), :106 (regex en behavior), :115 (control negativo); pipeline/scrape_news.py:320, :432 (grep); 36-07-PLAN.md:136, :139 (formas via_url). Medición propia: re.findall(pat, src_sin_comentarios) → []; con 'seen[item["via_url"]] = item["date"]' → []; con 'seen[via_url] = d' → ['seen[via_url] =']; pin AST (Assign/AugAssign/AnnAssign/Delete con target Subscript(Name 'seen') + Call seen.update/setdefault/pop/clear) → [320, 432]. | En 38-03, reemplazar la regex en la truth (:22), el behavior (:106) y el control negativo (:115) por un pin AST sobre pipeline/scrape_news.py. Debe dar cero nodos ast.Assign/AugAssign/AnnAssign cuyo target sea ast.Subscript con value Name id=='seen', cero ast.Delete sobre ese Subscript y cero Call a seen.update/setdefault/pop/clear. Se acepta también, como alternativa, la regex por línea `\bseen\[.*?\]\s*=(?!=)`. Agregar al test un control positivo: el pin aplicado a un fuente que contenga `seen[item["url"]] = x` tiene que fallar. Además, antes de editar el código, el pin sobre HEAD debe reportar exactamente :320 y :432 (o sus líneas desplazadas después de la Fase 36, más las líneas via_url de 36-07). |
| BF-03 | C-R1-01 (CONFIRMADO). El árbitro releyó DEPLOYMENT.md y los planes 38-07, 38-04 y 37-06. | HYG-05: DEPLOYMENT.md § Known Gaps sigue diciendo que M-07 está abierto después de que 38-04 lo cierra, y HYG-05 se firmaría PASS | DEPLOYMENT.md:361 (Known Gaps), :399-409 (H-05 RESOLVED + cuerpo histórico), :411-421 (M-07 abierto), leídos por el árbitro; 38-07-PLAN.md:23-24, :103-131, :133 (sin M-07 en DEPLOYMENT.md); 38-04-PLAN.md:83 (elimina los 4 continue-on-error); REQUIREMENTS.md:59 (HYG-05); 37-06-PLAN.md:127 (precedente DEP-01). | 38-07 Task 2 (DEPLOYMENT.md): reescribir el encabezado del párrafo M-07 al estilo de H-05: '**M-07 -- RESOLVED <date> by 38-04 (HYG-04, G-66/G-74; CI-only, the local path is § CEAD quarterly procedure)**', conservando el texto histórico. Opcionalmente, agregar al cuerpo histórico de H-05 '(superseded: deploy-on-code fires on data/cead/** since 35-02)'. En la truth (:23), agregar 'the M-07 Known-Gaps entry is marked RESOLVED'. En el verify, agregar `grep -q '^\*\*M-07 -- RESOLVED' DEPLOYMENT.md`, que hoy falla. |

## Conditions

1. BF-01: 38-08 Task 1 Step 3: (1) Correr `set -o pipefail; cd "$SCR/hyg06" && PYTHONPATH=. python pipeline/scrape_cead.py 2>&1 | tee "$SCR/hyg06-scrape.log"` y registrar scrape_rc desde ese rc (o desde ${PIPESTATUS[0]}). Exigir además `grep -c 'Pipeline completed successfully.' log` == 1 y `grep -c 'Pipeline failed' log` == 0. (2) Redefinir communes_written como el número de data/cead/comunas/*.json del worktree con last_updated == la fecha LOCAL de la corrida (scrape_cead usa date.today() local, no la UTC de hyg06_date). Exigir 346, más 16 regions/*.json y national.json con el mismo last_updated. (3) Si falla cualquiera de las dos cosas, el outcome es ABORTED (sin commit). Agregarlo a la regla de soundness de Task 2 y al verify de Task 1. (4) 38-07 Task 2, ítem 4 del procedimiento: reemplazar '346 comunas' por '346 comunas reescritas (last_updated = hoy) y el log con "Pipeline completed successfully."'.
2. BF-02: En 38-03, reemplazar la regex en la truth (:22), el behavior (:106) y el control negativo (:115) por un pin AST sobre pipeline/scrape_news.py. Debe dar cero nodos ast.Assign/AugAssign/AnnAssign cuyo target sea ast.Subscript con value Name id=='seen', cero ast.Delete sobre ese Subscript y cero Call a seen.update/setdefault/pop/clear. Se acepta también, como alternativa, la regex por línea `\bseen\[.*?\]\s*=(?!=)`. Agregar al test un control positivo: el pin aplicado a un fuente que contenga `seen[item["url"]] = x` tiene que fallar. Además, antes de editar el código, el pin sobre HEAD debe reportar exactamente :320 y :432 (o sus líneas desplazadas después de la Fase 36, más las líneas via_url de 36-07).
3. BF-03: 38-07 Task 2 (DEPLOYMENT.md): reescribir el encabezado del párrafo M-07 al estilo de H-05: '**M-07 -- RESOLVED <date> by 38-04 (HYG-04, G-66/G-74; CI-only, the local path is § CEAD quarterly procedure)**', conservando el texto histórico. Opcionalmente, agregar al cuerpo histórico de H-05 '(superseded: deploy-on-code fires on data/cead/** since 35-02)'. En la truth (:23), agregar 'the M-07 Known-Gaps entry is marked RESOLVED'. En el verify, agregar `grep -q '^\*\*M-07 -- RESOLVED' DEPLOYMENT.md`, que hoy falla.

## Non-blockers

| id | title | detail |
|---|---|---|
| NB-01 | Restos del carve-out G-62 en 38-08 y ROADMAP (o3) | 38-08-PLAN.md:5-6 sigue con `wave: 1` y `depends_on: []`, y ROADMAP.md:216 ('38-08 HYG-06 independent and date-gated', '9 plans in 6 waves') y :226 ('W1 independent') también. Todo eso contradice G-73(1), el cuerpo de 38-08:82 ('schedules 38-08 after 38-01..38-07') y 38-05:103. execute-phase agrupa por wave: si la fase arranca antes del 10-01, la ola 1 queda esperando la fecha o cierra NOT_YET. Si arranca después, el impacto es bajo. Edición: en 38-08 poner `wave: 6` y `depends_on: ["38-01",…,"38-07"]`; en 38-09 poner `wave: 7`; en ROADMAP:216/:226, 'runs after 38-07 (G-73(1))', '(W6, orchestrator)' y '9 plans in 7 waves'. Origen: B-R1-03 + C-R1-05. |
| NB-02 | 38-09 Task 3: dos de los greps del deferred-live ya pasan hoy | 'seen.json/rejected/ with third-party headlines' y 'chile crime map' ya están en directive:170 (G-73), fuera de la sección '## Deferred-live list' (:174). Los medí: 1 y 1. En cambio, 'MapIsland default year 2025' da 0. Los ítems (d), (e) y (f) no se verifican. Edición: extraer DL=$(awk '/^## Deferred-live list/{f=1;next} /^## /{f=0} f' $D) y correr los greps (a)-(f) sobre "$DL". Para los 7 ítems cerrados, exigir que ninguno siga en DL. Origen: B-R1-04 + C-R1-04. |
| NB-03 | 38-08: num_to_null no tiene control positivo | Si la columna del año viene vacía o con '-', by_family queda en None (parser.py:36-38; scrape_cead.py:318-319), mientras rate_per_100k sigue siendo numérico: da 0.0 o una suma parcial (scrape_cead.py:403-405). Una implementación que mire solo rate_per_100k pasa las dos autopruebas. Edición: en Step 2(b), poner en null un valor de by_family de 13101, exigir `num_to_null=1` y agregarlo a la regex del verify. Origen: A-R1-02. |
| NB-04 | 38-03: dentro de la ventana del cutover, el test integrado no discrimina el re-clasificado | Con now=2026-10-02 y cutover igual a la fecha del commit (≥09-25), max(v, C) ≥ run−60 conserva incluso '2021-09-16'. Si se revierte :432, la corrida 2 igual hace 0 llamadas; el test falla solo por la aserción de valor. Edición: fechar las corridas 1 y 2 en ≥ cutover+61, o parchar el cutover. Origen: A-R1-03. |
| NB-05 | 38-09 Task 2: la regla de medianoche puede dar mismatches falsos | `now` se captura al inicio del script (scrape_news.py:298). Si la corrida cruza la medianoche UTC, el valor puede ser el día de startedAt, y el plan exige el de updatedAt. Con el cron 0 */6 y retrasos de ≤3 h es casi imposible, pero el instrumento no debe fallar con el sistema sano. Edición: aceptar value ∈ {día de startedAt, día de updatedAt} y registrar cuál fue. Origen: A-R1-04. |
| NB-06 | Encabezado 'Annual evolution (first–last)' del panel del mapa sin marca de año parcial | ResultPanel.tsx:366-369 muestra `(${firstYear}–${lastYear})`, con lastYear = el máximo de la serie, que incluye 2026 parcial (:204-211). No contradice el rango, pero es el mismo copy que 38-05 corrige en la página de región. Edición: agregar la marca en 38-05 Task 1 (el archivo ya está en files_modified) o mandarlo a deferred-live. Origen: A-R1-05. |
| NB-07 | 38-08 NEW_DATA: data_commit y el <sha> de served-routes sin definir después del rebase | El commit se crea antes de `rebase FETCH_HEAD` (:136), y los pasos 4-5 usan '<sha>'/data_commit sin decir cuál. Es un falso rojo, no un falso verde. Edición: data_commit = `git -C "$SCR/hyg06" rev-parse HEAD` después del push, y el mismo sha para 37-served-routes.sh y `gh run list --commit`. Origen: A-R1-06 + B-R1-07. |
| NB-08 | 38-09 Task 1: el verify exige `^GATE rc=0` en 38-VERIFICATION.md, pero la acción solo lo escribe en el log del gate | 38-09:71 hace echo 'GATE rc=$?' dentro del tee a $SCR/38-gate.log, y el verify en :84 lo busca en $V. Da falso rojo. Edición: en el paso 1 agregar 'copy the GATE rc=… line into 38-VERIFICATION.md', o apuntar el grep al log. Origen: A-R1-07, la parte CONFIRMADA. |
| NB-09 | 38-07 verify R-11: la línea :563 (con backticks) no tiene guarda | El patrón 'only if NEWS_PROVIDER=deepseek\\|consumed only if .NEWS_PROVIDER=deepseek' matchea hoy :19 y :245 (count 2, medido), pero no ':563 (optional, only if `NEWS_PROVIDER=deepseek`)'. 'nothing in this repo does today' está partido en :146-147, pero el gemelo 'No effect on the default configuration' (:145) sí protege ese bloque. Edición: usar el patrón `only if .\?NEWS_PROVIDER=deepseek`. Origen: B-R1-05. |
| NB-10 | 38-06 Task 1: el verify termina en `; true` | 38-06:92 da siempre rc 0, aunque vitest falle. Lo compensa el vitest completo del verify de Task 2. Edición: dejar como tolerante solo el conteo registrado (`{ grep -c … \|\| true; }`). Origen: B-R1-06. |
| NB-11 | 38-08: una clave faltante (<198) sin 403 ni rc≠0 queda sin outcome; 'Failed homicide subgroup fetch' no cuenta en failed_batches | Una falla de fetch en el homicidio loguea 'Failed homicide subgroup fetch' y hace continue (scrape_cead.py:368), así que la clave no se cachea y queda 197/198. El grep 'Failed batch' no la ve. El verify de Task 1 exige 198/198 salvo en ABORTED, y el ejecutor no tiene un mapeo explícito. Es fail-safe: no se publica. Edición: n<198 → ABORTED_SUSPECT con la lista de claves faltantes; contar también 'Failed homicide' en failed_batches. Origen: B-R1-08 (PLAUSIBLE, subido a CONFIRMADO por lectura de código). |
| NB-12 | Gemelo del título de región sin barrer en 3 páginas editoriales prioritarias | Lo medí con curl a prod: /is-chile-safe/, /is-santiago-safe/ y /safest-cities-in-chile/ muestran '(CEAD, 2005–2025)' y dibujan `opacity="0.5" data-year="2026"` (is-chile-safe.astro:127-130; is-santiago-safe.astro:139; safest-cities-in-chile.astro:181-183). Edición: sumarlas a 38-05 Task 2 con lastDrawnYear/lastDrawnPartial, o agregar un ítem deferred-live explícito en 38-09 Task 3 paso 6. Origen: C-R1-02. |
| NB-13 | Runbook trimestral permanente con '198' fijo; en 2027 son 207 | El número de claves es 9×(Y−2004) (scrape_cead.py:265-266; 7 familias + 2 medidas de homicidio): 198 en 2026 y 207 en 2027. Edición: en 38-07 Task 2, ítems 3-4, escribir '9 × (run year − 2004) keyed files (198 in 2026, 207 in 2027)'. Origen: C-R1-03. |
| NB-14 | 38-07: el verify del stack de CLAUDE.md no chequea Astro | La regex de 38-07:93 recorre solo requests, lxml, tenacity y openai de requirements.txt. La acción pide Astro/@astrojs/react/React y HYG-05 nombra 'astro versions'. Edición: leer `"astro": "X.Y.Z"` de site/package.json y exigir `\| Astro \| X.Y.x` en el bloque GSD:stack. Origen: C-R1-07. |
| NB-15 | (o4) El cierre del milestone v2.2 (audit / complete-milestone) no está resuelto ni excluido | 38-09 Task 3 escribe el reporte final y lleva STATE a 'v2.2 complete', pero ni 38-09 ni la directiva mencionan /gsd:audit-milestone ni /gsd:complete-milestone. Los milestones anteriores tienen MILESTONE-AUDIT y archivo (ROADMAP.md:30-105). Edición: en 38-09 Task 3, decir explícitamente que el audit y el archivo del milestone quedan fuera de la corrida, como acción del owner en el reporte final ('exact commands the owner must run': /gsd:audit-milestone v2.2 y luego /gsd:complete-milestone), o ejecutarlos. Origen: árbitro (observación del orquestador). |

## Discarded

- C-R1-06 El tooltip de ResultPanel y el histograma de /es/noticias/ no tendrían prueba servida: El histograma ES se valida en el gate: news-freshness.mjs:70-71 revisa dist/news y dist/es/noticias con checkNewsPage, y 38-06 agrega ahí la aserción de .day-bar. Además, el fingerprint G-51 (37-served-routes.sh) prueba que el build servido es el mismo que pasó el gate. El tooltip queda fijado por vitest en ese mismo build. Pedir un check de bundle es opcional, no un defecto.
- B-R1-09 V37 multilínea si 37-VERIFICATION.md tuviera dos líneas `gate:`: La sección '## Baseline gate' de G-61(a) se escribe en 37-TRIAGE-LOG.md (37-01-PLAN.md:38, :142), no en 37-VERIFICATION.md. 37-07-PLAN.md:77 define una sola línea `gate:`. El riesgo de un prefijo o viñeta (primera mitad de A-R1-07) es fail-safe: da rojo, no verde. Además, el verify de 37-07 exige las líneas hermanas al inicio de línea (^close_sha:, ^served_routes:, ^DEPS-0N:), así que el formato sin prefijo es el esperable.
- ESC-1 38-01: validators_expected sería una referencia hacia adelante a su propio SUMMARY: 38-01 lee V37 de la línea `gate:` de 37-VERIFICATION.md (38-01-PLAN.md:120, leído). Su SUMMARY solo lo consumen 38-02, 38-05 y 38-06, que dependen de 38-01.
- ESC-2 38-08: conflicto entre la caché vacía y el instrumento: El instrumento lee solo data/cead y git (ls-tree/show; 38-08-PLAN.md:85-86). La aserción de 0 cead_* es una precondición del scraper (G-69), no del instrumento.
- ESC-3 38-09: ¿se verifican todos los meses de rejected/ o un subconjunto?: El verify recorre `git ls-files data/incidents/rejected` completo (38-09-PLAN.md:77, :84), sin poda. El '3 files' es informativo. Mover los ledgers a almacenamiento privado es la pregunta al owner de G-73(3), que ya está ruteada a deferred-live.

## Orchestrator observations

| obs | status | detail |
|---|---|---|
| (o1) Citas archivo:línea que las Fases 36/37 van a mover | CONFIRMADA-POR-ARBITRO | Los planes lo reconocen:<br>- 38-03 (:55 're-read … Phase 36 changes these files first'; :58 'whatever its line number after Phase 36')<br>- 38-06 (:58 '36-03 edits both pages … re-read')<br>- 38-01 (:67 all.mjs/36-09)<br>- 38-07 (:63, :67-68: 37-06 en DEPLOYMENT.md; 36-11/37-07 en STATE.md)<br><br>38-05 dice 'Phases 36/37 touch none of these files', y lo verifiqué: ningún files_modified de 36/37 incluye ResultPanel.tsx ni region/[slug].astro. store.py lo tocan 36-01/04/05/09, pero ningún plan de la 36 cambia window_days ni el cutoff (grep), así que la semántica que usa 38-06 se mantiene. El único lugar donde el movimiento de la 36 rompe algo es el pin R-16: la forma `seen[item["via_url"]] = …` que agrega 36-07 se escapa de la regex (BF-02). |
| (o2) En NEW_DATA, ¿el vintage, el año parcial y los rankings se actualizan solos? | CUBIERTA-POR-LENTE | C-R1-C5 lo cubrió para FRESH-05, y el árbitro verificó:<br>- last_updated = run_date (scrape_cead.py:461/:570/:584).<br>- ceadVintage.ts:24-55 y cead-vintage.lib.mjs:16-39 derivan lastUpdated, partialYear y latestCompleteYear del JSON, sin años hardcodeados.<br>- mark_partial_year (:417) mantiene 2026 parcial en octubre.<br>- compute_ranks recalcula national_rank sobre el último año no parcial (:465-469), con la dirección bloqueada sin cambios, y los builders regeneran map payload y composite.<br><br>Residuales verdaderos pero no falsos: MapIsland useState(2025) (deferred-live c) y los captions editoriales '2005–2025' con la barra 2026 dibujada (NB-12). |
| (o3) Restos del carve-out G-62 que permitan correr 38-08 antes de cerrar la 37 | CUBIERTA-POR-LENTE | B-R1-03 y C-R1-05, confirmados por el árbitro: 38-08-PLAN.md:5-6 (wave 1, depends_on []), ROADMAP.md:216 ('independent') y :226 ('W1 independent'). El Step 0 de 38-08 sí exige la Fase 37 tildada y 37-served-routes.sh, así que no puede correr antes de cerrar la 37. Lo que puede es correr en la ola 1 de la 38, antes de 38-01..07, en contra de G-73(1). Ver NB-01. |
| (o4) ¿El cierre de la 38 cierra también el milestone v2.2? | CONFIRMADA-POR-ARBITRO | Ningún lente lo trató. 38-09 Task 3 escribe el reporte final y lleva STATE a 'v2.2 complete', pero no invoca ni excluye /gsd:audit-milestone ni /gsd:complete-milestone, y la directiva (:191-193) tampoco los menciona. Los milestones anteriores tienen audit y archivo. Va como no bloqueante (NB-15): declararlo fuera de alcance, como comando del owner en el reporte final, o ejecutarlo. |

## Closures verified

- R-03, línea base de soundness: medí con pipeline.cead.parser.parse_cead_table (solo lectura) los 198 archivos que matchean la regex de claves de 38-08 en pipeline/cache. Resultado: Counter({346: 198}), sobre 199 cead_* en total (198 + cead_page.html).
- R-02, instrumento core_diff: están ls-tree, as_posix, exit 2, added/removed por diferencia de listados, autoprueba cero con files_added/removed=0 y control positivo, tanto en el cuerpo (38-08:85-94) como en la truth (:21), el artifact (:29), T-38-20 y el verify (:108). La lectura del lente A no se contradijo.
- HYG-01: el defecto está vivo en prod (curl propio): seen.json 200 (2,046,128 B), rejected/2026-09.json 200 (2,180,738 B), pending.json 404 (9,818 B, página 404 custom), current.json 200.
- HYG-02, línea base en prod (curl propio): /map/ tiene '<title>Chile Crime Map — Interactive</title>' y 0 <h1>. Los títulos objetivo son idénticos en 38-02:97/:102, en 38-09:78 y en chile-crime-map.astro:41.
- Arreglos de BF-01 y BF-02 medidos como viables: con `set -o pipefail` el rc es 1 y con ${PIPESTATUS[0]} también es 1. El pin AST sobre scrape_news.py en HEAD detecta [320, 432]; la regex vigente detecta [].
- 38-06: la aritmética del histograma es correcta. computeDayBuckets usa lower = anchor − (width−1) y buckets contiguos (newsDayFacets.ts:97-107), así que con width = W+1 cubre anchor−W.
- HYG-05/R-11: el patrón del verify de 38-07 matchea hoy 2 líneas (DEPLOYMENT.md:19, :245) y no :563 (backtick), lo que da NB-09. :145-147 queda cubierto por 'No effect on the default configuration'.
- DEP-01 Known Gap ('astro stays at 6.4.6', DEPLOYMENT.md:363-364, contra package.json:20 7.1.6) ya lo barre 37-06 (37-06-PLAN.md:23, :70, :127). No es un hueco de la 38 y sirve de precedente para BF-03.
- o1: las colisiones con 36/37 están declaradas con re-lectura en 38-01, 38-03, 38-06 y 38-07. Lo que 38-05 dice ('no collision') es cierto: ninguna 36/37 toca ResultPanel.tsx ni region/[slug].astro.
- o2: el vintage y el año parcial se derivan de los datos (ceadVintage.ts, cead-vintage.lib.mjs) y los ranks se recalculan (scrape_cead.py:465-469).
- Escaladas 1-3: resueltas (ver descartados ESC-1..3).

## Previous edits not applied

- G-73(1)/R-04: el R1 actualizó el campo `independent` de 38-08, pero el gemelo estructural que usa execute-phase (38-08 `wave: 1`, `depends_on: []`) y ROADMAP.md:216/:226 ('independent', 'W1 independent', '6 waves') siguen describiendo el carve-out retirado (NB-01).
- R-16: la ampliación del pin (update/setdefault/del) está en el texto, pero la alternativa de subíndice `\bseen\[[^\]]+\]` no matchea subíndices anidados. No detecta :320, :432 ni la forma via_url de 36-07, y el control negativo de 38-03:115 es incumplible (BF-02).
- R-14 en 38-09 Task 1: el verify exige `^GATE rc=0` en 38-VERIFICATION.md, pero la acción solo lo imprime en el log del gate (NB-08).
- R-10/R-17 (G-73(3)/(4)) en 38-09 Task 3: los ítems deferred-live están en la acción, pero su verificación es vacua. Los strings que se buscan ya existen en directive:170 (NB-02).

## Measurements reserved to the orchestrator

- Después de corregir BF-02: correr el pin nuevo sobre pipeline/scrape_news.py en el HEAD de ejecución, antes de editar, y exigir que reporte las escrituras vivas (hoy [320, 432], más las líneas via_url si 36-07 ya aterrizó). Después de editar, debe dar [].
- Después de corregir BF-01: en Git Bash, verificar que el comando exacto del Step 3 con pipefail devuelve el rc de python (el árbitro midió 1 con un exit(1) sintético), y que 'Pipeline completed successfully.' aparece en el log de una corrida sana.
- Al ejecutar 37-07: confirmar que 37-VERIFICATION.md escribe `gate: … validators <k>/<k>` al inicio de línea, porque las extracciones de V37 en 38-01 y 38-09 lo requieren (fail-safe).
- Fecha de arranque de la Fase 38 frente al 2026-10-01: define el impacto práctico de NB-01 si el frontmatter de 38-08 no se corrige.

## Lens verdicts

- A — mediciones, estadística e instrumentos (ronda 1, HEAD bf1eb9d; planes 38-01..38-09 en su revisión R1): NO-GO (15 findings)
- C — producto, ruta servida y coherencia con el pedido del owner (ronda 1, HEAD bf1eb9d): NO-GO (15 findings)
- B — ejecutabilidad de gates, tareas e invariantes (ronda 1, planes 38-01..38-09 @ HEAD bf1eb9d): NO-GO (16 findings)

## Condition closure log (append)

