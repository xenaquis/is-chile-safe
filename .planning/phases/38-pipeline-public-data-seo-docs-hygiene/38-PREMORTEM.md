---
phase: 38
artifact: premortem
plans_evaluated: 38-01..38-09 @ 545d6ff
method: 3 opus perspectives (técnica, metodológica, integración) + fresh opus arbiter (xhigh)
risks: 17
subagent_tokens: 790535
---

# Phase 38 — Premortem

## Arbiter summary

Arbitraje del premortem de la Fase 38 sobre 545d6ff (HEAD = origin/master). Leí los 9 planes, en especial 38-01, 03, 04, 05, 06, 07, 08 y 09 completos, junto con la directiva (standing order, hard rules, defaults del owner, G-59 y G-62..G-72, deferred-live), REQUIREMENTS HYG-01..07, ROADMAP y 36-07. Todo sin escribir nada y sin red ni LLM.

**Qué verifiqué en el código:**
- fetch_enusc_vhdv.py:248/:258 reescribe incondicionalmente data/snapshots/enusc_vhdv.json, que está versionado, con un `fetched` nuevo. rebase.autoStash no está configurado.
- `git show 'HEAD:data\cead\national.json'` falla en esta máquina.
- scrape_cead.py:326-329 se traga las excepciones por lote, y parser.py:66-67 devuelve [] sin tabla. La caché actual parsea 346 filas formato_4 en los 198 archivos.
- client.py:196-199: la caché no tiene TTL, y los archivos 2025/2026 del árbol principal son del 06-12 y 06-16.
- 37-served-routes.sh todavía no existe (lo crea 37-01).
- check-heartbeat.sh:173-176, junto con el último commit a data/cead (ee91f21, 06-19), deja el heartbeat CEAD en rojo desde el 10-01.
- ResultPanel.tsx:196-198 muestra la tasa del `year` seleccionado. MapIsland.tsx:117 tiene `useState(2025)`.
- DEPLOYMENT.md:145-147 es gemelo de :19/:245/:563 y no está en la lista de la 38-07.
- STATE.md:694 sigue con 'NEXT: /gsd:plan-phase 28'.
- figure-registry.mjs:395-405 exige 'granite' y no fija el modelo vivo.
- archive_r2.py:172-197 sube solo rejected; seen y pending no llegan a R2.
- El repo es PUBLIC (gh).

**Qué se sostiene:** el allowlist de la 38-01 (sin consumidores de los ledgers), `ref: master` como fix de G-59, la premisa de HYG-03 (G-65) y el ancho 31 del histograma. No hay P0.

**Los 5 P1:**
- **R-01.** La rama NEW_DATA de la 38-08 casi seguro rompe en el rebase: el worktree queda sucio fuera de data/cead. NEW_DATA es el resultado más probable, porque la caché es de junio.
- **R-02.** La autoprueba del instrumento core_diff es vacua (todo en cero sobre un árbol intacto, sin files_added=0 ni control positivo). Con el riesgo de backslash de Windows, puede declarar IDENTICAL_LIVE con los datos cambiados.
- **R-03.** Una página de error de CEAD con status 200 se cachea, cuenta para 198/198, parsea 0 filas y se publica como NEW_DATA con tasas subestimadas.
- **R-04.** La exención G-62 permite correr la 38-08 el 10-01, a mitad de las Fases 36/37 y contra el standing order, cuando el script de prueba de deploy todavía no existe.
- **R-05.** La trampa de la caché queda armada en el árbol principal. En ene-2027, seguir el aviso de la alerta ('Run scrape_cead.py locally') publicaría 2026 como año completo con datos de junio.

**Los P2** son ediciones baratas: línea base del test en vivo de HYG-03, tooltip de años contra el año mostrado, HYG-04 inerte en Actions con una rotura latente, heartbeat CEAD rojo permanente sin registro, redacción de SOURCES.md sobre R2 y el repo público, gemelo en DEPLOYMENT, STATE.md Operator Next Steps, DOCS-03 fijado al modelo vivo, N/N autorreferencial, ruteo G-54 por mensaje y pin estructural de seen.

**Aceptado:** R-17 (HYG-02 queda on-page según el default G-64; sin GSC no se mide el efecto).

**Descartados:** T-08 (el veredicto@build es informativo, G-71 está bloqueado y 0/60 snapshots históricos tienen fecha máxima posterior a `generated`) y M-11 (la brecha de cutover es de días e impacto medido 1/4443; ningún plan de la 36/37 toca SOURCES.md).

## Final risks

| id | sev | decision | origin | plans | scenario | plan edit / acceptance |
|---|---|---|---|---|---|---|
| R-01 | P1 | MITIGAR | T-01 (CONFIRMADO por árbitro) | 38-08 | 38-08 NEW_DATA: tras el scrape y los builders, fetch_enusc_vhdv.py:248/:258 reescribe data/snapshots/enusc_vhdv.json (versionado) con un `fetched` nuevo. El xlsx está fijado por sha en :53, así que es lo único que cambia. El paso 3 hace solo `git add data/cead`, commit y `git rebase FETCH_HEAD`, y git se niega con 'You have unstaged changes' (rebase.autoStash vacío). El cron de noticias ya movió origin/master (~4 commits/día), así que el rebase es obligatorio. El ejecutor improvisa (stash, add -A, reset): o mete rutas fuera de data/cead o deja HYG-06 ABORTED con datos buenos sin publicar. NEW_DATA es el resultado probable, porque la última caché es del 06-12/16. | 38-08 Task 2 NEW_DATA:<br>- Paso 1: después de build_map_payload, `git -C "$SCR/hyg06" checkout -- data/snapshots`. El snapshot es un input versionado y el xlsx está fijado por sha; solo cambia `fetched`.<br>- Paso 3: después de `git add data/cead`, `git -C "$SCR/hyg06" checkout -- data/comparator-pairs.json 2>/dev/null \|\| true`, y exigir `[ -z "$(git -C "$SCR/hyg06" status --porcelain --untracked-files=no \| grep -v ' data/cead/')" ]`. Tras el commit, exigir `git -C "$SCR/hyg06" status --porcelain --untracked-files=no` vacío antes del `rebase FETCH_HEAD`.<br>- Registrar `worktree_clean_before_rebase: yes` en 38-HYG06.md y agregarlo al verify de la rama NEW_DATA. |
| R-02 | P1 | MITIGAR | M-02 (CONFIRMADO por árbitro) | 38-08 | La única prueba de 38-cead-core-diff.py es correrlo sobre el árbol principal intacto y exigir 'communes=0 regions=0 national=0'. Un instrumento roto también da cero. Si el script arma la ruta con pathlib/os.path, `git show HEAD:data\cead\...` falla (verificado: "fatal: path 'data\cead\national.json' exists on disk, but not in 'HEAD'"). Si esa falla se cuenta como archivo agregado, imprime C=R=N=0 y files_added=363. El verify no mira files_added/removed ni tiene control positivo, así que pasa. En el worktree, el scrape real da IDENTICAL_LIVE aunque CEAD haya publicado: no hay commit y prod sigue con los números de junio. | 38-08 Task 1 Step 1 (instrumento):<br>- Listar HEAD con `git -C root ls-tree -r --name-only HEAD -- data/cead/comunas data/cead/regions data/cead/national.json`.<br>- Construir las rutas de `git show` con '/' (Path.as_posix()).<br>- Si `git show` da rc≠0 en una ruta listada, imprimir `core_diff: ERROR <path>` y salir con 2.<br>- Calcular files_added/removed como diferencia de listados, nunca a partir de fallas.<br><br>Mover la autoprueba DESPUÉS del Step 2 (worktree limpio):<br>- (a) Sobre `$SCR/hyg06` sin tocar, exigir `communes=0 regions=0 national=0 files_added=0 files_removed=0` → línea `selftest_zero:`.<br>- (b) Control positivo: sumar 1 a series[year=2025].rate_per_100k de `$SCR/hyg06/data/cead/comunas/13101.json` y exigir `communes=1 regions=0 national=0` más una línea `example data/cead/comunas/13101.json 2025 rate_per_100k` → línea `selftest_positive:`. Después, `git -C "$SCR/hyg06" checkout -- data/cead`.<br><br>Verify de Task 1: reemplazar el grep sobre el árbol principal por `grep -qE '^selftest_zero: .*files_added=0 files_removed=0' && grep -qE '^selftest_positive: communes=1 regions=0 national=0'` en 38-HYG06.md. |
| R-03 | P1 | MITIGAR | T-02 + M-03 (dedup; ruta de código CONFIRMADA por árbitro, disparador PLAUSIBLE) | 38-08 | CEAD responde 200 con una página de error, un soft-block o un esquema nuevo. Se cachea (cache_raw) y cuenta para 198/198. parse_cead_table devuelve [] sin tabla (parser.py:66-67), o la excepción se traga (scrape_cead.py:326-329), y rc queda en 0. La familia o año queda en None. rate_per_100k = suma de las familias restantes (:403-405), más baja pero plausible. validate_all_communes solo exige 346 comunas y el rango (schema.py:175-189). core_diff ve cambios → NEW_DATA → builders → gate (ningún validador compara contra el vintage anterior) → push → deploy de tasas falsas por comuna. Además, un reordenamiento del catálogo puede generar diferencias de float en las medias ponderadas regionales/nacionales, que JSON-equality cuenta como NEW_DATA. | 38-08 Task 1:<br>- Step 3, exigir además:<br>  - (i) `grep -c 'Failed batch' "$SCR/hyg06-scrape.log"` == 0;<br>  - (ii) cada uno de los 198 archivos `cead_*` del worktree parsea con pipeline.cead.parser.parse_cead_table a ≥346 filas con class formato_4. Línea base medida por el árbitro sobre la caché de junio: mínimo 346 en familias y en subgroup101. Registrar `cead_parse_min_rows: N`.<br>- Step 1, el instrumento imprime además:<br>  - `num_to_null=K` (cualquier campo numérico de series/by_family que pasa a null);<br>  - `years_changed=<año:n,...>`;<br>  - compara floats con tolerancia relativa 1e-9 y reporta aparte `float_only=F`.<br>- NEW_DATA requiere failed_batches=0, cead_parse_min_rows≥346, num_to_null=0 y C+R+N≥1 después de aplicar la tolerancia. Si no se cumple, outcome `ABORTED_SUSPECT`: sin commit, con pregunta al owner y extracto del log.<br>- Los cambios en años cerrados se reportan en 38-HYG06.md y no bloquean.<br>- Agregar ABORTED_SUSPECT a la regex de outcome del verify de Task 2. |
| R-04 | P1 | MITIGAR | I-02 (CONFIRMADO por árbitro) | 38-08 | G-62 exime a la 38-08 de la precondición de la Fase 37, y el Step 0 solo compara la fecha con el 10-01. Hoy la Fase 35 sigue abierta y las Fases 36 (11 planes) y 37 (7 planes) no se ejecutaron, así que el orquestador puede correr la 38-08 el 10-01 a mitad de la 36. Hace el scrape y el push de data/cead, y deploy-on-code dispara un build de prod. Recién en el paso 4 invoca .planning/phases/37-dependency-security-hygiene/37-served-routes.sh, que hoy no existe (ls: No such file; lo crea 37-01). bash devuelve 127, un código fuera de la tabla 0/1/10/11/12: los datos quedan en prod sin prueba G-51. Además, el standing order ('one at a time, each fully closed before the next begins', G-19: 'execution and close are serialized') queda violado sin ratificación del owner, y un push de datos puede intercalarse con la cola de merges G-61(b) de la 37. | 38-08 Task 1 Step 0: agregar `test -f .planning/phases/37-dependency-security-hygiene/37-served-routes.sh`. Si falla, `outcome: NOT_YET (37-01 not executed)`, sin scrape.<br><br>Task 2 paso 3:<br>- Pushear solo entre planes (ningún plan de 36/37 en ejecución).<br>- Si la cola de merges de la 37 está abierta, esperar a que el merge pendiente tenga su línea served_routes.<br><br>Task 2 paso 4: cualquier rc de 37-served-routes.sh fuera de {0,1,10,11,12} es un error de instrumento: STOP, sin PASS y sin revert.<br><br>Registrar el carve-out del standing order como G-NN y ponerlo en la lista de decisiones del owner para ratificar. |
| R-05 | P1 | MITIGAR | I-03 (CONFIRMADO por árbitro) | 38-08, 38-04, 38-07 | La 38-08 esquiva la caché solo una vez, en un worktree efímero. El árbol principal conserva los `cead_*` 2025/2026 del 06-12/16, y load_cached no tiene TTL (client.py:196-199). Todas las rutas por defecto siguen diciendo 'corré scrape_cead.py localmente': el cuerpo del alert (cead-scraper.yml:138), DEPLOYMENT.md:20/:402 y la memoria. En ene-2027, una corrida en el árbol principal toma 2026 de la caché de junio. mark_partial_year (scrape_cead.py:84-89) marca como parcial solo 2027, así que 2026 se publica como año COMPLETO con ~5 meses de datos: Santiago 2026 = 2059 contra 9309 en 2025, tendencias en 'bajando' y rankings reordenados. rc 0 y diff≠0 lo presentan como dato nuevo. Si la 38-08 publicó NEW_DATA en octubre, esa corrida además lo revierte. | 38-08 Task 2, en toda rama salvo NOT_YET: mover los `pipeline/cache/cead_*.html` del árbol principal a `pipeline/cache/stale-2026-06/`. Es gitignored, local y reversible; ningún test depende de ellos (test_client.py usa tmp_path). Registrar `main_cache_disarmed: <n> files`.<br><br>38-04 Task 1: en cead-scraper.yml, cambiar el cuerpo del issue 'Run pipeline/scrape_cead.py locally and push' por 'Run the DEPLOYMENT.md CEAD quarterly procedure (fresh worktree, empty CEAD cache) and push'. Agregar un pin textual en test_workflow_order.py.<br><br>38-07 Task 2:<br>- Actualizar DEPLOYMENT.md:20 y :402 para que remitan a ese procedimiento.<br>- La frase 'The procedure is executed in 38-08' pasa a condicionarse al outcome de 38-HYG06.md.<br><br>Deferred-live (agente): un guard en código en scrape_cead que ignore el hit de caché para años ≥ run_date.year−1. |
| R-06 | P2 | MITIGAR | T-04 + M-04 + I-10 (dedup; texto del plan CONFIRMADO, disparador PLAUSIBLE) | 38-09 | La 38-09 Task 2 toma S0 = seen.json en close_sha. Un run de noticias iniciado antes del push (workflow y código viejos) que commitea después de close_sha escribe fechas de publicación y poda con date.today()−60 (feeds.py:245). Eso da mismatches>0 o pruned>0: un FAIL falso y un gap plan sobre código correcto. Además, `git log --since=<createdAt> -1` devuelve el commit MÁS NUEVO, que puede ser de un run posterior con otra fecha UTC. Si el push se reintenta, close_sha no se vuelve a registrar. | 38-09 Task 2:<br>- Identificar el bot_commit del run medido por su ventana startedAt..updatedAt (`gh run view <id> --json startedAt,updatedAt,headSha`) y el primer commit 'auto-update news' de github-actions en esa ventana.<br>- S0 = `git show <bot_commit>^:data/incidents/seen.json`; S1 = `<bot_commit>:...`.<br>- Exigir `git merge-base --is-ancestor <close_sha> <headSha del run>` antes de juzgar.<br>- Aceptar como fecha del run el día UTC de startedAt, o el de updatedAt si cruzó medianoche.<br>- Declarar que la evaluación puede hacerse retroactivamente desde git y gh (sin sesión viva de 9 h).<br><br>38-09 Task 1 paso 2: si el push se reintenta, volver a registrar close_sha después del rebase. |
| R-07 | P2 | MITIGAR | M-05 (CONFIRMADO por árbitro) | 38-05 | El panel muestra la tasa del año seleccionado (ResultPanel.tsx:196-198 seriesEntry = year; :317 year-badge). El año por defecto está hardcodeado en MapIsland.tsx:117 `useState(2025)`. El fix de la 38-05 compara COMPOSITE_YEAR contra latestCompleteCeadYear(series), no contra el año mostrado. Con 2024 seleccionado, el tooltip explica una diferencia que no existe. Cuando CEAD complete 2026 (scrape de ene-2027), el tooltip dirá '(2026)' y el panel seguirá mostrando 2025 por defecto: el fix crea una contradicción que el literal actual no tenía. El vitest fija (2024,2025) y no falsa la hipótesis. | 38-05 Task 1: firma `yearMismatchTip(lang, compositeYear, displayedYear, displayedPartial, latestCompleteYear)`, con displayedYear = seriesEntry.year.<br>- displayedYear === compositeYear → null.<br>- displayedYear === latestCompleteYear → el literal actual, con pin byte a byte.<br>- Otro caso → variante 'The incident rate shown uses {displayedYear}' (+ ' (partial year)' / ' (año parcial)' si es parcial).<br><br>Casos vitest: (2024, 2024, …) → null; (2024, 2025, false, 2025) → literal; (2024, 2026, true, 2025) → variante que contiene '2026' y la marca de parcial.<br><br>Gemelo MapIsland.tsx:117: derivar el default del último año completo del payload, o, si excede el alcance, agregar al deferred-live 'MapIsland default year 2025 debe seguir al último año CEAD completo (scrape ene-2027)'. |
| R-08 | P2 | MITIGAR | T-03 + M-01 + I-09 (dedup; CONFIRMADO por árbitro) | 38-04, 38-07 | HYG-04 se cumple en el YAML (1 continue-on-error), pero no cambia ninguna ejecución real:<br>- En Actions, el scrape falla con 403 primero (cead-scraper.yml:68-69) y los builders se saltan.<br>- La ruta real (local) no corre el workflow.<br><br>Rotura latente: si CEAD deja pasar las IPs del runner, 'Build ENUSC victimization snapshot' con CACHE_ONLY=1 lanza FileNotFoundError siempre. pipeline/cache está en gitignore y solo se trackea .gitkeep (fetch_enusc_vhdv.py:123-129). Antes la falla era benigna, porque build_enusc_enrichment.py:40 lee el snapshot versionado. Después de HYG-04, el job de Actions no puede commitear CEAD nunca. | 38-04 Task 1:<br>- Agregar `if: hashFiles('pipeline/cache/enusc_vhdv.xlsx') != ''` al paso 'Build ENUSC victimization snapshot', con un test en test_workflow_order.py que fije ese `if`. La cuenta de continue-on-error sigue en 1.<br>- Reescribir el comentario 'On Actions the scrape step already fails first' para que diga que HYG-04 solo se ejerce cuando el scrape tiene éxito.<br><br>En 38-04-SUMMARY y en la evidencia HYG-04 de 38-VERIFICATION: 'CI-only; la ruta local la cubre el runbook (builders encadenados con &&, STOP ante uno rojo)'.<br><br>38-07 Task 2: el runbook CEAD de DEPLOYMENT.md lista los 4 builders encadenados con `&&`. |
| R-09 | P2 | MITIGAR | T-06 + M-07 + I-01 (dedup; CONFIRMADO por árbitro) | 38-09 | Desde el 2026-10-01, check-heartbeat.sh:173-176 cuenta 2 bordes (07-01 y 10-01) desde el último commit de data/cead (ee91f21, 06-19) y falla todos los días. Con IDENTICAL_LIVE, ABORTED o NOT_YET no hay commit y G-72(4) mantiene la regla: queda rojo hasta que CEAD publique. La 38-09 no mide ni reporta el estado del heartbeat, así que la fase cierra en verde con una alarma perpetua. La etiqueta pipeline-failure-heartbeat es compartida con R2 (heartbeat.yml:80-86), pero r2-archive.yml tiene su propia alerta de falla (pipeline-failure-r2, :66-86). El enmascaramiento afecta solo el caso 'R2 no corrió'. | 38-09 Task 1 paso 4: agregar la línea `heartbeat_cead: <conclusion del último heartbeat.yml, paso CEAD> (<boundaries>)`, informativa y fuera de los ≥13 PASS. Si queda rojo tras un outcome ≠ NEW_DATA, en Task 3 el reporte final lleva la pregunta explícita al owner, con fecha de salida esperada.<br><br>Se ACEPTA no separar la etiqueta de R2 en esta fase: R2 conserva su alerta de falla propia. Anotarlo en el deferred-live junto al marcador 'last live-verified' de G-72(4). |
| R-10 | P2 | MITIGAR | T-05 + M-10 + I-06 (dedup; CONFIRMADO por árbitro) | 38-07, 38-01, 38-09 | HYG-01 da 404 en el sitio, pero:<br>- El repo es PUBLIC (gh repo view). seen.json y rejected/*.json, con titulares y snippets de terceros, siguen legibles en GitHub/raw.<br>- La frase de la 38-07 en SOURCES.md, igual que el header de sync-data y T-38-02 de la 38-01, afirma que seen/rejected/pending 'are kept in the repository and the R2 archive'. archive_r2.py:172-197 consolida solo rejected; seen.json y pending.json nunca se suben a R2.<br><br>La metodología publicada afirmaría algo falso, y el verify (grep 'not published on the site (HYG-01)') lo deja pasar. | 38-07 Task 1, frase de Storage: 'Internal pipeline ledgers (data/incidents/seen.json, rejected/, pending.json) are not published on the site (HYG-01). They remain in the public GitHub repository; rejected candidates are also archived in R2.'<br><br>38-01 Task 1: el header de sync-data.mjs y T-38-02 dicen lo mismo ('repo (public); rejected/ also in R2').<br><br>38-09 Task 3: evidencia de HYG-01 = 'served copy removed; repository copy still public (owner default)'.<br><br>La decisión de sacar los ledgers del repo va a la lista del owner. |
| R-11 | P2 | MITIGAR | M-06 (CONFIRMADO por árbitro) | 38-07 | La 38-07 corrige DEEPSEEK_API_KEY en :19, :245 y :563, pero deja vivo el gemelo DEPLOYMENT.md:145-147: 'Without DEEPSEEK_API_KEY: No effect on the default configuration … nothing in this repo does today'. :123 y :453 dicen lo contrario (backup NREC-04). El grep del verify ('only if NEWS_PROVIDER=deepseek\|consumed only if') no lo detecta. Un operador puede borrar el secret creyendo que no tiene efecto, y el failover muere en silencio. | 38-07 Task 2: agregar :145-147 a la lista de reemplazo, con la redacción NREC-04 de :123. Verify: sumar `! grep -q 'nothing in this repo does today' DEPLOYMENT.md && ! grep -q 'No effect on the default configuration' DEPLOYMENT.md`. |
| R-12 | P2 | MITIGAR | I-07 (CONFIRMADO por árbitro) | 38-07 | HYG-05 ('STATE.md matches the repo') cierra en verde, pero STATE.md:690-697 '## Operator Next Steps' sigue diciendo 'NEXT: `/gsd:plan-phase 28`', y :680 conserva '## Phase 29 readiness'. La 38-07 toca solo el banner y § Blockers, y la 38-09 solo Current Position; ningún verify mira esas secciones. Una sesión nueva o el owner reciben instrucciones de v2.1. | 38-07 Task 2 (STATE.md):<br>- Reemplazar el cuerpo de '## Operator Next Steps' por los residuales de v2.2 (GSC/HYG-07, procedimiento CEAD trimestral, decisión de la era Granite, heartbeat CEAD), apuntando al RUN STATUS.<br>- Marcar '## Phase 29 readiness' como histórico.<br><br>Verify: `! grep -qF 'NEXT: \`/gsd:plan-phase 28\`' .planning/STATE.md`. |
| R-13 | P2 | MITIGAR | I-08 (CONFIRMADO por árbitro) | 38-07 | El validador #13 figure-registry (DOCS-03, figure-registry.mjs:395-405) EXIGE /granite/ en SOURCES.md y no fija el modelo vivo. La 38-07 comprueba 'deepseek/deepseek-v4.1-flash' con un grep puntual de una sola vez. Un próximo cambio de proveedor deja SOURCES.md mintiendo sin que ningún validador lo detecte. El instrumento protege la palabra vieja, no la verdad nueva. | 38-07 Task 1: agregar site/scripts/validate/figure-registry.mjs a files. Extender DOCS03_CHECKS con un check que lea DEFAULT_OPENROUTER_MODEL y DEFAULT_BACKUP_MODEL de pipeline/news/model_config.py (regex sobre el literal) y exija ambos strings en SOURCES.md. Mantener /granite/ como historia. Control negativo: con un model_config temporal que tenga otro id, el check falla. |
| R-14 | P2 | MITIGAR | M-08 (CONFIRMADO por árbitro) | 38-09, 38-01, 38-02, 38-05, 38-06 | Los verify de 38-01, 38-02, 38-05 y 38-06 calculan N del propio all.mjs y exigen 'N/N validators passed'. Desregistrar un validador rojo sigue dando verde. El ancla N = V37+1 existe solo en la prosa de las must_haves de la 38-09; su verify automático exige solo 'GATE rc=0' y ≥13 checks PASS. | 38-09 Task 1:<br>- Escribir en 38-VERIFICATION.md `validators_expected: <V37+1>`, con V37 tomado de 37-VERIFICATION.md.<br>- Exigir en el verify `grep -q "$E/$E validators passed" "$SCR/38-gate.log"`, con E leído de esa línea.<br>- Listar los nombres PASS del resumen.<br><br>38-01 Task 2 verify: después de registrar, `[ "$N" -eq <V37+1> ]`. 38-02, 38-05 y 38-06: exigir que N sea igual al valor registrado en 38-01-SUMMARY. |
| R-15 | P2 | MITIGAR | M-09 (PLAUSIBLE → subido: texto de 38-06:351 y de 38-09 Task 1 paso 1 verificado) | 38-06, 38-08, 38-09 | Con la 38-06, news-freshness (#18) suma dos modos de falla que vienen del código: 'day X has incidents but no .day-bar (G-71)' y 'data-build-now missing'. La 38-09 Task 1 (y la 38-08 Task 2 paso 2) manda leer TODO rojo de news-freshness como un incidente real de noticias (G-54): registrarlo y no pushear. Una regresión del histograma pausa la fase en lugar de abrir un gap plan. O al revés: un rojo de datos se 'arregla' tocando el validador. | 38-06 (objective/verification), 38-09 Task 1 paso 1 y 38-08 Task 2 paso 2: la regla G-54 se aplica según el mensaje, no según el validador. Solo los errores de edad o evidencia de freshness/news-freshness son incidentes de noticias. Los mensajes 'no .day-bar (G-71)' y 'data-build-now missing' son defectos de código y van a gap plan. |
| R-16 | P2 | MITIGAR | T-07 (PLAUSIBLE; texto de 36-07:136 verificado) | 38-03 | La 36-07 prescribe `seen[via_url] = date` (la fecha del ítem) en el camino decode-skip. El pin de la 38-03 detecta solo asignaciones por subíndice y permite 'skip if the via_url path does not exist'. Si la implementación de la 36-07 usó update/setdefault/helper, el gemelo sobrevive. Las URLs G con fecha vieja se podan en cada save, se re-decodifican en cada run y consumen DecodeBudget(60/180 s). Resultado: más URLs news.google.com en el sitio. | 38-03 Task 2:<br>- Pin ampliado: en scrape_news.py (sin comentarios), 0 ocurrencias de `seen[...] =`, `seen.update(`, `seen.setdefault(` y `del seen[`.<br>- Eliminar la opción de skip. Si 'via_url' aparece en scrape_news.py, el test decode-skip (P ya visto → G marcado con la fecha del run) es obligatorio. |
| R-17 | P2 | ACEPTAR | I-04 + I-05 (dedup; hechos CONFIRMADOS; decisión cubierta por default del owner) | 38-09 | HYG-02 cierra en verde con títulos diferenciados, pero:<br>- /chile-crime-map/ también monta MapIsland (chile-crime-map.astro:11);<br>- los anchors internos 'mapa interactivo' apuntan a ambas URLs;<br>- norm3 pasa con un reordenamiento de palabras.<br><br>Además, /map/ pierde el término 'Chile Crime Map' sin datos de GSC. El efecto en el ranking no se mide. | Se acepta: G-64 (default del owner) fija la diferenciación por títulos/H1, sin noindex ni redirect. Cambiar anchors o el título editorial excede ese default. Registro obligatorio:<br>- 38-09 Task 3 escribe en 38-VERIFICATION 'HYG-02: on-page differentiation per G-64; ranking effect unmeasured (no GSC)'.<br>- El deferred-live suma un control a 28 días en GSC de la consulta 'chile crime map' por URL, ligado a HYG-07. |

## Discarded

- T-08: (1) La línea PASS 'news stale=' es informativa. No tiene consumidor automático, y G-71 fija el reloj de la página por decisión. La staleness viva de prod la cubren freshness.mjs y el heartbeat de news (48 h, reloj real). (2) Fecha futura que desplaza el ancla: revisé los últimos 60 commits de data/incidents/current.json y 0 tienen max(date) > fecha de `generated`. Tampoco apareció en vivo el caso que se afirma.
- M-11: Cutover en el commit de 38-03 contra el push de 38-09: la brecha es de días, y las entradas escritas en ese intervalo reciben el mismo trato legacy que G-65 y G-72(5) ya aceptan. El propio M midió el impacto en 1 de 4.443 filas. La segunda parte, que el cap de 'granite' ≤1 empuje a borrar divulgaciones, no tiene sustento: ningún plan de las Fases 36 o 37 toca data/SOURCES.md (grep: 0), y la 38-07 conserva la frase histórica fechada que exige DOCS-03.
- T-(a..e): No son riesgos, sino verificaciones de lo que NO está roto (allowlist sin consumidores de ledgers, ref: master con news-deploy-needed HEAD~1..HEAD, premisa de HYG-03, 198 claves, histograma flex). El árbitro las confirma en lo que leyó (38-01 interfaces, feeds.py según G-65, recuento de caché 199 = 198 + cead_page.html).

## Owner decisions surfaced

- Ratificar o rechazar el carve-out G-62: la 38-08 (HYG-06) puede ejecutar y pushear data/cead ANTES de que cierre la Fase 37, contra el standing order 'one phase at a time' (G-19: execution serialized). Si no hay respuesta, se aplica R-04: exige que 37-served-routes.sh exista y que no haya un plan de 36/37 en ejecución.
- Heartbeat CEAD rojo desde el 2026-10-01: si HYG-06 da IDENTICAL_LIVE, ABORTED o NOT_YET, queda rojo todos los días hasta que CEAD publique. Opciones: aceptar el rojo, adoptar el marcador 'last live-verified scrape' (diferido por G-72(4)) o silenciarlo hasta ene-2027.
- Ledgers en el repo PÚBLICO: HYG-01 solo deja de servirlos en ischilesafe.com. seen.json y rejected/*.json, con titulares y snippets de terceros, siguen legibles en GitHub (el default del owner dice 'keep them in the repo'). ¿Se mantienen o se mueven a un almacenamiento privado (p. ej. R2)? Mover implica cambiar el pipeline y no se puede purgar la historia (hard rule).
- HYG-02: /map/ pierde el término 'Chile Crime Map' y lo conserva /chile-crime-map/, decidido sin datos de GSC. Si el owner tiene acceso a GSC con rendimiento por URL, confirmar la asignación. Si no, aceptar el control a 28 días ligado a HYG-07.
- Si la 38-08 termina en ABORTED_SUSPECT (R-03: lotes fallidos, filas parseadas < 346 o números que pasan a null) o muestra revisiones masivas de años cerrados, decidir si se publica, se reintenta en otro momento o se espera.
- Paso operativo propio del owner cada trimestre: el scrape CEAD corre solo en la máquina local (CEAD da 403 a las IPs de Actions). Requiere el procedimiento de worktree con caché CEAD vacía (~198 requests, 10-35 min) y, ante un deploy sin build visible, revisar el log de Cloudflare Pages (pause point 5, G-51).

## Raw perspective counts

- Perspectiva T, técnica. Leí los 9 planes y verifiqué el código en HEAD 545d6ff. Lo que no encontré roto:

(a) Allowlist de sync-data: ningún consumidor del sitio, de los validadores ni de R2 lee seen/rejected/pending desde public/ o por HTTP. Los únicos consumidores son CommuneNewsSection.astro:39, HomeNewsPulse.astro:32/259, IncidentPinLayer.ts:85, news.astro:47/185 y es/noticias.astro:46/183, todos sobre current.json o archive/. facets.mjs:40-41 y freshness.mjs:35 leen la copia del repo. archive_r2.py y centroids.py leen data/. site/public/data está en gitignore (site/.gitignore:3).

(b) `ref: master` es correcto como fix de G-59. news-deploy-needed.sh se sigue evaluando como HEAD~1..HEAD después de push-with-rebase (news-pipeline.yml:96), así que el gate de deploy de la 35 y CRON-06 no cambian. Además, un run encolado ya no hereda en su diff el current.json del run previo. Ningún test fija `with` exacto en el checkout (grep fetch-depth en pipeline/tests: 0).

(c) La premisa de HYG-03 está confirmada: save_seen poda por valor (feeds.py:245-250) y scrape_news guarda la fecha de publicación (:320, :432). No hay tests previos de save_seen que se rompan. El max(value, cutover) también cubre las entradas escritas por el cron viejo entre el commit de la 38-03 y el push.

(d) La regex de 198 claves coincide con la caché real: 198 más cead_page.html, que el scraper no usa. La caché es cache-first sin TTL (client.py:196-199, scrape_cead.py:289-306). Los archivos 2025/2026 son del 06-12 y 06-16, así que G-69 está bien fundado. Los builders resuelven sus rutas dentro del worktree. deploy-on-code dispara con data/cead/** (deploy-on-code.yml:4-10).

(e) Histograma: computeDayBuckets ya recorta el límite inferior a la fecha válida más antigua (newsDayFacets.ts:99-100), así que el ancho 31 no inventa barras en cero para días ya archivados. Es layout flex (news.astro:995-1003), sin grid fijo de 30. Ningún validador fija 30 barras.

Los riesgos reales están en otro lado:
- La rama NEW_DATA de la 38-08 casi seguro falla en el rebase porque el worktree queda sucio fuera de data/cead.
- El instrumento core_diff no distingue datos nuevos de un lote que falló en silencio.
- HYG-04 no hace nada en la única ruta donde corre (Actions) y deja una rotura latente.
- La verificación en vivo de HYG-03 en la 38-09 puede dar un falso FAIL.
- La exposición de los ledgers sigue vía el repo, que es público.: 8
- M — Metodológica. Probé los instrumentos de la Fase 38 contra el código real en HEAD 545d6ff, sin escribir nada y sin llamadas a red ni a LLM. Resultado: public-data.mjs y seo.mjs [MI] fallan hoy y pasarían después por la razón correcta. En la ruta real y en el cierre hay 5 fallas P1. (1) La cuenta "exactamente 1 continue-on-error" pasa, pero el cambio no toca la ruta CEAD que de verdad corre (la local) y deja la de Actions condenada a fallar siempre. (2) El diff del núcleo CEAD solo se autoprueba con todo en cero, sin control positivo; en Windows `git show HEAD:<ruta con \>` falla (lo verifiqué), así que puede declarar IDENTICAL_LIVE con los datos cambiados. (3) "Cambió ≥1 cifra" y "198/198" no distinguen datos nuevos de revisiones de años cerrados, ruido de floats o páginas de error cacheadas. (4) La prueba en vivo de HYG-03 usa una línea base equivocada (close_sha) y puede dar rojo con el sistema sano. (5) El arreglo del tooltip de años compara contra la variable equivocada, y con el default de año 2025 hardcodeado (gemelo) va a mentir cuando CEAD complete 2026. Las correcciones del research (G-65: la poda sí existe en feeds.py:236-253; G-69: el caché no expira, client.py:196-199) las confirmé en el código. La tesis de HYG-03 se sostiene: no hay filtro por fecha antes de clasificar (scrape_news.py:350-378). Pero su beneficio medido es mínimo: 1 URL de 4.443 filas en rejected/ tenía más de 60 días al verse por primera vez.: 11
- PERSPECTIVA I (integración y producto). Hoy (HEAD 545d6ff) la Fase 35 sigue sin tildar (ROADMAP:120-123 tiene las 4 fases en "[ ]"), y las Fases 36 (11 planes) y 37 (7 planes) no se ejecutaron. Por eso la 38 va a empezar casi seguro después del 2026-10-01. Hay un punto de roce: 38-08 queda exenta de G-62 y puede correr apenas llega esa fecha, pero depende de 37-served-routes.sh, que hoy no existe (lo crea 37-01). Encontré tres modos de "cerrar en verde mientras el sitio o las alarmas siguen mintiendo". (1) Desde el 1-oct el heartbeat CEAD se pone rojo todos los días; con IDENTICAL_LIVE sigue rojo para siempre y tapa las alertas de R2. (2) La trampa del cache sin TTL se arregla solo en la documentación, con un worktree de un solo uso. La ruta por defecto (la que indican el cuerpo de la alerta de cead-scraper.yml y DEPLOYMENT.md:20/:402) sigue armada: en ene-2027 publicaría 2026 como año completo usando el HTML parcial de junio. (3) La diferenciación de HYG-02 es cosmética. La página editorial también monta MapIsland, los anchors internos "mapa interactivo" apuntan a las dos URLs, y el chequeo norm3 pasa con solo reordenar palabras. Además, la decisión de qué URL se queda con "Chile Crime Map" se tomó sin datos de GSC. HYG-01 cumple en el sitio, pero el repo es PÚBLICO: los ledgers siguen legibles en GitHub. No hay consumidores internos de las URLs servidas (grep: solo comentarios en news-pipeline.yml:113 y news-deploy-needed.sh:10); los externos solo se ven en Cloudflare Analytics, que es del owner. Drift que queda: STATE.md "Operator Next Steps" (:690-697) dice todavía "NEXT: /gsd:plan-phase 28", y figure-registry DOCS-03 (figure-registry.mjs:395-405) exige la palabra "granite" pero no fija el modelo vivo. Pasos que requieren al owner y los planes no lo dicen todos: GSC (HYG-07, este sí declarado); el log y las analytics de Cloudflare (pause point 5 y consumidores de ledgers); la decisión sobre el heartbeat tras IDENTICAL_LIVE; la máquina local encendida ≥35 min más polling de deploy en 38-08; y ≈9 h de sesión viva en 38-09 T2. Lo confirmé con la CLI de GitHub: `gh repo view` → PUBLIC y `git log -- data/cead` → ee91f21 2026-06-19.: 10
