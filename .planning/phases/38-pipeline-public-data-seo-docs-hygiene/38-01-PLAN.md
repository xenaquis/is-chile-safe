---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/scripts/sync-data.lib.mjs
  - site/scripts/sync-data.mjs
  - site/scripts/sync-data.test.ts
  - site/scripts/validate/public-data.mjs
  - site/scripts/validate/all.mjs
autonomous: true
requirements: [HYG-01]
directive_items: ["deferred-live: sync-data copies seen/rejected to public/ (Phase-35 review F4)", "G-06: pending.json public exposure"]
executor: opus (public data surface / information disclosure)

must_haves:
  truths:
    - "The build copies only an allowlist of data/incidents entries into site/public/data/incidents: current.json, archive/, centroids.json and cut_list.json. seen.json, rejected/ and pending.json are never copied, whether or not they exist at build time (research Pitfall 1)."
    - "seen.json, rejected/ and pending.json stay in the repo (data/incidents/; the repo is public on GitHub, owner default 'keep them in the repo'). rejected/ is also archived in R2 (archive_r2.py:172-197); seen.json and pending.json are not. Only the served copy goes away (owner default: 'stop serving them on prod')."
    - "A new validator, public-data.mjs, fails the build when dist/data/incidents contains any entry outside the allowlist, and fails when current.json is missing (non-vacuous). It is registered in all.mjs as the next number after the validators present at the Phase-37 close."
    - "No page or validator loses an input: every site consumer reads current.json or archive/ only."
  artifacts:
    - path: site/scripts/sync-data.lib.mjs
      provides: "SERVED_INCIDENT_ENTRIES (4 names) + syncIncidents(srcDir, destDir) → {copied: string[], skipped: string[]}"
    - path: site/scripts/validate/public-data.mjs
      provides: "closed-world check of dist/data/incidents (HYG-01)"
  key_links:
    - from: site/scripts/sync-data.mjs
      to: site/scripts/sync-data.lib.mjs
      via: "import { syncIncidents }"
      pattern: "syncIncidents\\("
    - from: site/scripts/validate/all.mjs
      to: site/scripts/validate/public-data.mjs
      via: "VALIDATORS entry"
      pattern: "'public-data.mjs'"
---

<objective>
HYG-01: stop serving the pipeline's internal ledgers (seen.json, rejected/*.json and, when it exists, pending.json) on prod, per the owner default in the directive. Keep them in the (public) repo; rejected/ is also archived in R2. Make the exclusion structural (allowlist copy) and validator-enforced.

Purpose: prod serves seen.json (200, 2,023,856 bytes) and rejected/2026-09.json (200, 2,150,403 bytes), measured by curl on 2026-09-25. These files carry third-party headlines and snippets, and their public copies go stale between deploys since FRESH-04 stopped the no-op redeploys (Phase-35 review F4).
Output: allowlist copy, vitest, validator #N+1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-RESEARCH.md
@site/scripts/sync-data.mjs
@site/scripts/validate/all.mjs
@site/scripts/validate/map.mjs

<interfaces>
Evidence read in the current tree (HEAD 72a683a, 2026-09-25):
- site/scripts/sync-data.mjs:43-50: `INCIDENTS_SRC = data/incidents`; if it exists, `rmSync(INCIDENTS_DEST)` then `cpSync(INCIDENTS_SRC, INCIDENTS_DEST, { recursive: true })`. It is a wholesale copy with no filter. :21 imports cpSync, existsSync, mkdirSync, rmSync.
- data/incidents/ holds: archive/, centroids.json, current.json, cut_list.json, rejected/ (2026-07, 2026-08, 2026-09), seen.json (7,012 keys). pending.json is absent right now; pipeline/scrape_news.py:246 writes it only when the retry queue is non-empty.
- Site consumers of data/incidents (grep of site/src and site/scripts): current.json at CommuneNewsSection.astro:39, HomeNewsPulse.astro:32 and :259 (fetch), IncidentPinLayer.ts:85 (fetch), news.astro:47, es/noticias.astro:46; archive/ at news.astro:185 and es/noticias.astro:183. Validators facets.mjs:40-41 and freshness.mjs read the REPO copy (data/incidents), not public/. There are 0 references to seen.json, rejected or pending.json. centroids.json and cut_list.json have 0 site consumers but are CEAD-derived, not ledgers, so they stay served (served 200 today, 22,866 and 3,949 bytes). Removing them is outside the owner default.
- all.mjs:36-55 is the VALIDATORS array (18 entries at 72a683a). The count pattern `grep -cE "^  '[a-z0-9-]+\.mjs',$" site/scripts/validate/all.mjs` prints 18. Phase 36 plan 36-09 (Task 3 step 4) may register 'news-dedup.mjs' as #19, so the number of this new validator is V37 + 1, where V37 is the count at the start of this plan. all.mjs:97 prints "VALIDATION SUITE SUMMARY", and :109 prints "N/N validators passed".
- map.mjs:233-250 already asserts the runtime CEAD assets exist in dist (the precedent for a dist existence check).
- vitest (site/package.json "test": "vitest run") picks up `*.test.ts` under site/scripts (35-05 precedent: scripts/validate/cead-vintage.test.ts).
- Collision note: all.mjs is also touched by 36-09 (the #19 registration). That plan executes before this one; re-read all.mjs before editing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allowlist copy of data/incidents (sync-data.lib.mjs + vitest)</name>
  <files>site/scripts/sync-data.lib.mjs, site/scripts/sync-data.mjs, site/scripts/sync-data.test.ts</files>
  <behavior>
    - With a tmp src holding current.json, archive/2026-08.json, centroids.json, cut_list.json, seen.json, pending.json and rejected/2026-09.json, syncIncidents(src, dest) produces a dest holding exactly {current.json, archive/2026-08.json, centroids.json, cut_list.json}. seen.json, pending.json and rejected/ are absent. The return value lists skipped = ['pending.json', 'rejected', 'seen.json'] (sorted).
    - An unknown new entry in src (e.g. `ledger2.json`) is NOT copied and appears in skipped. This is the closed-world rule: a future ledger never leaks by default.
    - A stale dest (pre-existing dest/seen.json from an old build) is removed. dest is recreated clean.
    - A src without archive/ copies the other allowlisted entries without throwing.
    - SERVED_INCIDENT_ENTRIES deep-equals ['archive', 'centroids.json', 'current.json', 'cut_list.json'].
  </behavior>
  <action>
Step 0 (phase order, G-62): run `grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md`. It must print 1. Otherwise STOP without editing and report "G-62: Phase 37 not closed".

Create site/scripts/sync-data.lib.mjs (ESM, node:fs + node:path only). It exports:
- the frozen SERVED_INCIDENT_ENTRIES;
- `syncIncidents(srcDir, destDir)`, which runs `rmSync(destDir, {recursive, force})`, then `mkdirSync(destDir, {recursive})`, then iterates `readdirSync(srcDir)`. It `cpSync`s each allowlisted entry (recursive) and collects every other name into `skipped`. It returns `{copied, skipped}`, both sorted.

In sync-data.mjs, replace lines 46-49 (the wholesale rmSync + cpSync) with a call to syncIncidents(INCIDENTS_SRC, INCIDENTS_DEST). Log `[sync-data] copied data/incidents (allowlist: …) -> public/data/incidents; not served: <skipped>`. Keep the existsSync guard (:45) and the CEAD copy (:37-40) unchanged. Update the header comment: "HYG-01 (G-63): data/incidents is copied by allowlist; the pipeline ledgers seen.json, rejected/ and pending.json stay in the repo (public on GitHub; rejected/ also in R2) and are never served."

Write site/scripts/sync-data.test.ts with the behaviors above (tmp dirs via fs.mkdtempSync(os.tmpdir())).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run scripts/sync-data.test.ts && node scripts/sync-data.mjs && test ! -e public/data/incidents/seen.json && test ! -e public/data/incidents/rejected && test -f public/data/incidents/current.json && test -d public/data/incidents/archive</automated>
    Expected: vitest passes (the ≥ 5 cases above), and the real sync leaves no seen.json or rejected/ under public/. Negative control: add 'seen.json' to SERVED_INCIDENT_ENTRIES, and the first and last behaviors must FAIL. Revert.
  </verify>
  <done>public/data/incidents is built from a 4-entry allowlist, and ledgers never reach public/, present or future.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validator public-data.mjs (closed-world dist check), registered in all.mjs</name>
  <files>site/scripts/validate/public-data.mjs, site/scripts/validate/all.mjs</files>
  <behavior>
    - On a real build, it prints `PASS public-data: dist/data/incidents = [archive, centroids.json, current.json, cut_list.json]; ledgers absent (seen.json, rejected/, pending.json)` and exits 0.
    - With site/dist/data/incidents/seen.json planted, it exits 1 with "FAIL public-data: not allowlisted: seen.json".
    - With site/dist/data/incidents/rejected/2026-09.json planted, it exits 1 naming rejected.
    - With site/dist/data/incidents/current.json removed, it exits 1 ("current.json missing — vacuous"). With dist/ missing, it exits 1.
  </behavior>
  <action>
Create public-data.mjs in the style of map.mjs (DIST_DIR = site/dist, a failures counter, a PASS/FAIL console line, process.exit). Import SERVED_INCIDENT_ENTRIES from ../sync-data.lib.mjs so the allowlist is defined once. Checks:
(a) dist/ and dist/data/incidents exist;
(b) readdirSync(dist/data/incidents) ⊆ SERVED_INCIDENT_ENTRIES;
(c) current.json exists;
(d) explicitly, none of seen.json / rejected / pending.json exists. (d) overlaps (b) on purpose, so the failure message names the ledger.

Register 'public-data.mjs' as the LAST entry of VALIDATORS in all.mjs and add the header-comment line `N. public-data.mjs — HYG-01 served data allowlist (no pipeline ledgers in dist)`, with N = V37 + 1 measured by the grep pattern in interfaces. Also update the header's "run all NN validation scripts" count. Record `validators_expected: <V37+1>` as a line of 38-01-SUMMARY.md (V37 from the `gate:` line of 37-VERIFICATION.md, `validators <k>/<k>`); 38-02, 38-05 and 38-06 match their N against it (R-14).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && N=$(grep -cE "^  '[a-z0-9-]+\.mjs',$" scripts/validate/all.mjs) && V37=$(grep -oE '^gate: .* validators [0-9]+/' ../.planning/phases/37-dependency-security-hygiene/37-VERIFICATION.md | grep -oE 'validators [0-9]+' | cut -d' ' -f2) && [ "$N" -eq "$((V37+1))" ] && npm run build && npm run validate 2>&1 | tee "$TEMP/38-01-val.log" && npx vitest run && grep -q "$N/$N validators passed" "$TEMP/38-01-val.log" && cp ../data/incidents/seen.json dist/data/incidents/seen.json && { node scripts/validate/public-data.mjs; test $? -eq 1; } && rm dist/data/incidents/seen.json && mkdir -p dist/data/incidents/rejected && echo '{}' > dist/data/incidents/rejected/2026-09.json && { node scripts/validate/public-data.mjs; test $? -eq 1; } && rm -r dist/data/incidents/rejected && node scripts/validate/public-data.mjs</automated>
    Expected: this is ONE chained command (OneDrive, G-21). The build and `npm run validate` give N/N with N = V37 + 1, the full vitest is green, both plants exit 1, and the clean dist exits 0. N must equal V37 + 1 from 37-VERIFICATION.md (R-14). A red is read by its message (G-54, R-15): a freshness/news-freshness error about news age or evidence is a real news signal (if it is the only red, STOP and report it); a code-defect message goes to a gap plan; never exclude or unregister a validator.
  </verify>
  <done>Validator #N+1 is registered and green on the real build, and fails on each planted ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| repo data/ → public CDN | anything copied into site/public is world-readable on ischilesafe.com |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information disclosure | sync-data.mjs wholesale copy | mitigate | 4-entry allowlist (closed world). New ledgers are excluded by default. Enforced by public-data.mjs in the build gate. |
| T-38-02 | Repudiation / audit loss | ledgers removed from prod | accept | The ledgers stay in git history and in data/ (the repo is public on GitHub, owner default); rejected/ is also archived in R2 (archive_r2.py:172-197), seen.json and pending.json are not. Only the served copy goes. Reversible by one allowlist entry. Moving them out of the public repo is an owner question in deferred-live (G-73(3)). |
| T-38-03 | Information disclosure | copies already cached by crawlers/CDN | accept | Cloudflare serves the new deploy immediately. Earlier third-party copies are outside our control. There is no history purge (hard rule). |
</threat_model>

<verification>
- `git diff --stat` touches only the 5 files listed.
- Full build + validate + vitest in one command: N/N (V37 + 1), vitest ≥ the Phase-37 close count + this plan's new tests.
- Prod proof is in 38-09 (404 on the ledger URLs after deploy).
</verification>

<success_criteria>
dist/data/incidents contains only the four allowlisted entries, a validator proves it on every build, and nothing that reads current.json or archive/ changes.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-01-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

Applied 2026-09-25 from 38-PREMORTEM.md and orchestrator decision G-73.
- R-10: "stay in the repo and R2" corrected everywhere it appears in this plan (must_haves truth, objective, sync-data.mjs header comment, T-38-02): the repo is public on GitHub; only rejected/ is archived in R2 (archive_r2.py:172-197). The move-out question is an owner item (G-73(3)), carried by 38-07/38-09.
- R-14: Task 2 writes `validators_expected: <V37+1>` to 38-01-SUMMARY.md, and its verify requires N == V37 + 1 read from 37-VERIFICATION.md.
- R-15 (twin): the Task 2 Expected reads a red by its message.
