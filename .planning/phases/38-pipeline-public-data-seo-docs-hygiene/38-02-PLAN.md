---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - site/scripts/validate/seo.mjs
  - site/src/pages/map.astro
  - site/src/pages/es/mapa.astro
autonomous: true
requirements: [HYG-02]
executor: sonnet (SEO copy + validator)

must_haves:
  truths:
    - "/map/ and /es/mapa/ are titled and headed as the interactive TOOL. /chile-crime-map/ and /es/mapa-delito-chile/ keep their editorial titles and H1s unchanged. Within each locale pair, the titles, the H1s and the meta descriptions all differ, and the first 3 normalized title words differ."
    - "Each of the 4 pages has exactly one <h1> in its static HTML. /map/ and /es/mapa/ gain one; they have zero today (the island is client:only)."
    - "No noindex and no redirect (owner default): all 4 pages keep `<meta name=\"robots\" content=\"index, follow\">` and a self-canonical, and none has a meta refresh."
    - "seo.mjs asserts all of the above mechanically, and it FAILS on the pre-change titles (the RED proof)."
    - "No absolute safe/dangerous wording; CEAD stays attributed (forbidden-language.mjs stays green)."
  artifacts:
    - path: site/scripts/validate/seo.mjs
      provides: "assertion block [MI] map-intent differentiation (HYG-02)"
  key_links:
    - from: site/scripts/validate/seo.mjs
      to: dist/map/index.html, dist/chile-crime-map/index.html, dist/es/mapa/index.html, dist/es/mapa-delito-chile/index.html
      via: "readFileSync + title/h1/description/canonical/robots regexes"
      pattern: "\\[MI\\]"
---

<objective>
HYG-02: resolve the /map/ vs /chile-crime-map/ cannibalization by intent (owner default: differentiate titles/H1; no noindex, no redirect) and make a validator enforce it.

Purpose: prod titles measured 2026-09-25 are "Chile Crime Map — Interactive" (/map/) and "Chile Crime Map — Interactive Choropleth by Commune" (/chile-crime-map/). Both open with the same head term, and /map/ has no H1 at all (curl showed no <h1>).
Output: RED-first validator block, two retitled tool pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-RESEARCH.md
@site/src/pages/map.astro
@site/src/pages/es/mapa.astro
@site/scripts/validate/seo.mjs

<interfaces>
Evidence read in the current tree (72a683a):
- map.astro:21-27: BaseLayout title="Chile Crime Map — Interactive", description "Explore reported crime incidence across Chile's 346 communes. Filter by year and crime type. Data: CEAD.", enPath /map/, esPath /es/mapa/. :33-37 is a visually hidden span, with inline style `position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0`. :40 is `<MapIsland client:only="react" …/>`. The page has no <h1>.
- es/mapa.astro:23: title "Mapa de Incidencia en Chile — Interactivo" (same structure as map.astro).
- chile-crime-map.astro:41 title "Chile Crime Map — Interactive Choropleth by Commune"; :48 `<h1>Chile Crime Map — Reported Incidence by Commune</h1>`. es/mapa-delito-chile.astro:32 title "Mapa de Delitos en Chile — Coroplético por Comuna — Chile Safety Map"; :39 h1 "Mapa de Delitos en Chile — Incidencia Reportada por Comuna". These two pages are NOT edited.
- BaseLayout.astro:36-37 sets BASE_URL 'https://ischilesafe.com' and canonicalPath per lang. :69 `<meta name="robots" content="index, follow" />`. :75 `<link rel="canonical" href={BASE_URL + canonicalPath} />`. og:title and twitter:title reuse the title (:79, :89).
- seo.mjs (312 lines): a `failures` counter, SAMPLES at :182-199 (map pages are NOT sampled; adding them there would impose the JSON-LD shape checks, and map.astro has no JSON-LD, so use a separate block), exit at :308/:312.
- spine.mjs:253-290 [K] checks the H1 count on home only. map.mjs checks only file existence and runtime assets for /map/ (no H1 or title assertion).
- The page layout is full-viewport below the 64px header (map.astro:6), so a visible H1 would shift the island. Hence the H1 is visually hidden with the existing inline pattern (G-64; the owner may prefer a visible one, see the open question in the final report).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: seo.mjs [MI] map-intent assertion (RED on the current pages)</name>
  <files>site/scripts/validate/seo.mjs</files>
  <behavior>
    - For the pairs EN (/map/, /chile-crime-map/) and ES (/es/mapa/, /es/mapa-delito-chile/), with dist/<path>/index.html read for each of the 4 pages:
      - all 4 files exist (missing → FAIL, so the check can never pass vacuously);
      - exactly 1 match of /<h1[\s>]/ per page;
      - within a pair: title ≠ title, h1 text ≠ h1 text, description ≠ description;
      - norm3(title A) ≠ norm3(title B), where norm3 = lowercase → NFD → strip \p{Diacritic} → replace [^a-z0-9]+ with ' ' → trim → first 3 tokens joined by ' ';
      - every title is ≤ 65 characters after HTML-entity decoding and stripping the "— Chile Safety Map" suffix;
      - canonical href === 'https://ischilesafe.com' + own path;
      - robots meta content === 'index, follow', and no `http-equiv="refresh"` is present.
    - Against the CURRENT pages the block FAILS: EN norm3 is 'chile crime map' on both pages, and /map/ and /es/mapa/ have 0 H1.
  </behavior>
  <action>
Step 0 (G-62): `grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md` must print 1, else STOP. Execution mode: this plan builds site/, so it never runs alongside another site-building plan (G-21(1)/G-62).

Append a block `// ASSERTION MI: map-intent differentiation (HYG-02, G-64)` before the final exit in seo.mjs. It has a local `norm3` helper, a tiny `decodeEntities` (&amp; &quot; &#39; &lt; &gt; and numeric) and per-check messages. Each check prints `PASS [MI] …` or `FAIL [MI] …` and increments failures. Leave the existing assertions untouched.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && { node scripts/validate/seo.mjs > "$TEMP/38-02-red.log" 2>&1; test $? -eq 1; } && grep -c "FAIL \[MI\]" "$TEMP/38-02-red.log"</automated>
    Expected: this is one chained command. seo.mjs exits 1, and the FAIL [MI] count is ≥ 3 (EN norm3, /map/ H1 count, /es/mapa/ H1 count). Commit as `test(38-02): …`.
  </verify>
  <done>The validator block exists and demonstrably fails on today's cannibalizing titles.</done>
</task>

<task type="auto">
  <name>Task 2: Retitle /map/ and /es/mapa/ as the tool page, with a static H1 (GREEN)</name>
  <files>site/src/pages/map.astro, site/src/pages/es/mapa.astro</files>
  <action>
Per G-64 (owner default: differentiate by intent; no noindex or redirect; the editorial pages stay unchanged):

map.astro:
- title = "Interactive Map — Reported Crime by Commune in Chile"
- description = "Full-screen map tool: filter Chile's 346 communes by year and crime type and see recent press-reported incidents. Data: CEAD; each incident cites its outlet."
- Immediately after the BaseLayout open tag, add `<h1 style="…">Interactive map of reported crime by commune in Chile</h1>`, reusing the exact inline visually-hidden style string of map.astro:34. Add a comment: "HYG-02/G-64: static H1 for crawlers; visually hidden because the island fills the viewport."

es/mapa.astro:
- title = "Mapa interactivo — Delitos reportados por comuna en Chile"
- description = "Herramienta de mapa a pantalla completa: filtra las 346 comunas de Chile por año y tipo de delito y revisa incidentes recientes reportados por la prensa. Fuente: CEAD; cada incidente cita su medio."
- h1 "Mapa interactivo de delitos reportados por comuna en Chile", with the same pattern.

Do not change enPath/esPath, the locate-label span, MapIsland props, BaseLayout, or the two editorial pages. Do not add a robots, noindex, canonical override or redirect anywhere (Pitfall 2).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && npm run validate 2>&1 | tee "$TEMP/38-02-val.log" && npx vitest run && N=$(grep -cE "^  '[a-z0-9-]+\.mjs',$" scripts/validate/all.mjs) && E=$(grep -oE '^validators_expected: [0-9]+' ../.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-01-SUMMARY.md | cut -d' ' -f2) && [ "$N" -eq "$E" ] && grep -q "$N/$N validators passed" "$TEMP/38-02-val.log" && [ "$(grep -c 'FAIL \[MI\]' "$TEMP/38-02-val.log")" -eq 0 ] && grep -q "<title>Interactive Map — Reported Crime by Commune in Chile</title>" dist/map/index.html && grep -q "<title>Chile Crime Map — Interactive Choropleth by Commune</title>" dist/chile-crime-map/index.html && [ "$(grep -o '<h1[ >]' dist/es/mapa/index.html | wc -l)" -eq 1 ] && ! grep -qi "noindex" dist/map/index.html dist/es/mapa/index.html dist/chile-crime-map/index.html dist/es/mapa-delito-chile/index.html</automated>
    Expected: one chained command. The result is N/N validators, with N == validators_expected from 38-01-SUMMARY.md (R-14), (seo, forbidden-language, hreflang and map included) with 0 FAIL [MI], vitest green, the new /map/ title, the unchanged editorial title, 1 H1 on /es/mapa/, and noindex nowhere. Negative control: restore map.astro's old title, rebuild and run seo.mjs → exit 1. Revert.
  </verify>
  <done>The two map intents are distinguishable by title, H1 and description, and every page stays indexable and self-canonical, as the validator enforces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build → search engines | titles/H1/meta are what Google indexes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Tampering (SEO regression) | future title edits | mitigate | The [MI] block runs in every `npm run validate`. |
| T-38-05 | Repudiation (editorial) | new copy | mitigate | forbidden-language.mjs gates absolute safety terms, and the copy attributes CEAD and the outlets. |
| T-38-SC | Tampering | installs | accept | none |
</threat_model>

<verification>
- The RED log (Task 1) and GREEN log (Task 2) are both kept in the SUMMARY.
- `git diff site/src/pages/chile-crime-map.astro site/src/pages/es/mapa-delito-chile.astro` is empty.
</verification>

<success_criteria>
HYG-02 holds under the owner default: differentiated by intent, no noindex or redirect, and validator-enforced.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-02-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

Applied 2026-09-25 from 38-PREMORTEM.md.
- R-14: the Task 2 verify requires N (the all.mjs count) to equal `validators_expected` recorded in 38-01-SUMMARY.md, so unregistering a red validator cannot produce a green N/N.
