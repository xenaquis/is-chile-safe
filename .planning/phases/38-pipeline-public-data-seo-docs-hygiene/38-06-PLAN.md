---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 06
type: execute
wave: 4
depends_on: ["38-05"]
files_modified:
  - site/scripts/validate/news-freshness.lib.mjs
  - site/scripts/validate/news-freshness.mjs
  - site/scripts/validate/news-freshness.test.ts
  - site/src/lib/newsDayFacets.ts
  - site/src/lib/newsDayFacets.test.ts
  - site/src/pages/news.astro
  - site/src/pages/es/noticias.astro
autonomous: true
requirements: [HYG-05]
directive_items: ["deferred-live: /news/ histogram 30-day cap hides the oldest store day (G-20(4))", "deferred-live: news-freshness validator judges staleness vs its own clock, not data-build-now (validator F2)"]
executor: sonnet (validators + news page wiring)

must_haves:
  truths:
    - "On /news/ and /es/noticias/, every distinct valid incident date d in current.json with d ≥ anchor − window_days (window_days read from the payload, default 30) has a .day-bar[data-day=d]. The histogram width is window_days + 1, so the oldest day the store keeps (store.py:178 cutoff = today − window_days, inclusive) is no longer dropped."
    - "checkNewsPage FAILS when a payload date inside that range has no bar. It FAILS against today's 30-bar pages whenever the payload holds a date at anchor − 30 (the RED proof via fixture)."
    - "news-freshness.mjs judges its summary verdict against the /news/ page's data-build-now attribute, not the validator's Date.now(). It FAILS if that attribute is missing or unparseable. The per-page checks already use data-build-now (news-freshness.lib.mjs:136-141, :261-265)."
    - "The 'Last 30 days' filter, facets.mjs, HomeNewsPulse and NewsStrip are unchanged (out of the named item's scope)."
  artifacts:
    - path: site/src/lib/newsDayFacets.ts
      provides: "newsHistogramWidth(windowDays: unknown): number  (= valid positive integer + 1, else DAY_WINDOW_WIDTH + 1)"
  key_links:
    - from: site/src/pages/news.astro
      to: newsHistogramWidth(payload.window_days)
      via: "third argument of computeDayBuckets"
      pattern: "computeDayBuckets\\([^)]*newsHistogramWidth"
---

<objective>
Close two news-surface items the directive routes to Phase 38: the /news/ histogram hides the oldest stored day, and validator #18 reports staleness against its own clock instead of the page's build clock.

Purpose: G-20(4) kept a 30-day cap (newsDayFacets.ts:39 DAY_WINDOW_WIDTH = 30, applied from the anchor at :97), while store.py keeps window_days + 1 dates (cutoff = today − 30, inclusive). At check time 71 cards dated 08-25 had no bar (directive deferred-live list). news-freshness.mjs:136 uses `Date.now()` for the reported verdict, which diverges from what the page rendered when the validator runs later (35-07 prod reuse).
G-54 reading after this plan (R-15): news-freshness (#18) gains two code-defect messages, "day X has incidents but no .day-bar (G-71)" and "data-build-now missing". Every later gate (38-08, 38-09) reads a news-freshness red by its MESSAGE: only news age/evidence errors are a news incident; these two are code defects and go to a gap plan.
Output: RED-first validator assertion, histogram width from the payload, page-clock verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@site/scripts/validate/news-freshness.lib.mjs
@site/scripts/validate/news-freshness.mjs
@site/src/lib/newsDayFacets.ts
@.planning/phases/35-honest-freshness-signals/35-06-SUMMARY.md

<interfaces>
Evidence read at 72a683a:
- newsDayFacets.ts:39 `DAY_WINDOW_WIDTH = 30`. :86-121 computeDayBuckets(dates, anchorDate, width = DAY_WINDOW_WIDTH, gaps = COVERAGE_GAPS): lower = anchor − (width − 1), clipped to the earliest valid date (:97-100). newsDayFacets.test.ts:11 and :160 pin the default width 30. Keep the default; the pages pass an explicit width.
- news.astro:47 dataPath; :53 `payload`; :66-70 `payload = raw; incidents = raw.incidents`; :206 `computeDayBuckets(incidents.map((i) => i.date), facets.anchorDate)`. es/noticias.astro:204 is the same call. Phase 36 plan 36-03 edits both pages (headline display) before this plan, so re-read the line numbers.
- store.py:139 window_days default 30; :178 `cutoff = ref_date − timedelta(days=window_days)`; :203 `if inc_date >= cutoff` is kept.
- news-freshness.lib.mjs:116 `checkNewsPage(html, payload, locale)`; :94 DAY_BAR_RE (button.day-bar); :166-172 payloadDates Set; :174-189 the bars loop, where `bars` (Map date→gap) is filled and the gap per bar is checked; data-day is the attribute.
- news-freshness.mjs:57-64 loads payload from site/public/data/incidents/current.json (or NEWS_FRESHNESS_CURRENT_JSON); :136-139 `buildNowMs = Date.now()` → verdict/newsStale are used ONLY in the PASS line (:152-155).
- news-freshness.test.ts exists (35-06) with fixture-based lib tests; extend it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validator — every stored day has a bar; summary verdict on the page clock (RED)</name>
  <files>site/scripts/validate/news-freshness.lib.mjs, site/scripts/validate/news-freshness.mjs, site/scripts/validate/news-freshness.test.ts</files>
  <behavior>
    - Fixture: payload {window_days: 30, incidents dated 2026-08-25 … 2026-09-24 (31 distinct dates)} and HTML with 30 bars 08-26..09-24 → checkNewsPage returns an error containing "day 2026-08-25 has incidents but no .day-bar".
    - The same HTML with 31 bars 08-25..09-24 gives no such error.
    - A payload date older than anchor − window_days with no bar gives no error (outside the range).
    - A payload without window_days uses 30.
    - news-freshness.mjs: with dist/news/index.html lacking data-build-now → exit 1 and "FAIL news-freshness: /news/ data-build-now missing". The PASS line prints `build-now <page data-build-now>` (the value of the attribute), not the validator clock.
  </behavior>
  <action>
Step 0 (G-62): `grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md` must print 1, else STOP. This plan builds site/ and runs alone.

In checkNewsPage, after the bars loop:
- anchor = the max valid payload date;
- W = the payload's window_days when it is a positive integer, else 30;
- lower = anchor − W days (UTC string arithmetic, the addDaysStr precedent in news-freshness-fixtures.mjs);
- for each payload date d with lower ≤ d ≤ anchor and !bars.has(d), push `day ${d} has incidents but no .day-bar (G-71)`.

In news-freshness.mjs, replace :136 `Date.now()` with the parsed data-build-now of dist/news/index.html (the p.freshness node). If it is missing or NaN, push the FAIL error. Keep every other output identical.

Add the fixture tests to news-freshness.test.ts.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run scripts/validate/news-freshness.test.ts && npm run build && { node scripts/validate/news-freshness.mjs > "$TEMP/38-06-red.log" 2>&1; echo "rc=$?"; } && grep -c "has incidents but no .day-bar" "$TEMP/38-06-red.log"; true</automated>
    Expected: vitest passes (the new cases plus all existing ones). The real-build run is RECORDED, not gated. It exits 1 with ≥ 1 "no .day-bar" line if current.json at that moment holds a date at anchor − 30; otherwise it exits 0, and the fixture tests carry the RED proof. Put the measured rc and count in the SUMMARY.
  </verify>
  <done>The validator can see a dropped stored day and reports on the page's own clock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Histogram width = window_days + 1 on /news/ and /es/noticias/ (GREEN)</name>
  <files>site/src/lib/newsDayFacets.ts, site/src/lib/newsDayFacets.test.ts, site/src/pages/news.astro, site/src/pages/es/noticias.astro</files>
  <behavior>
    - newsHistogramWidth(30) === 31; newsHistogramWidth(7) === 8; newsHistogramWidth(undefined) === 31; newsHistogramWidth(0) === 31; newsHistogramWidth(-3) === 31; newsHistogramWidth('30') === 31 (not numeric → default).
    - computeDayBuckets(dates 08-25..09-24, '2026-09-24', newsHistogramWidth(30), []) has length 31, and its first bucket is 2026-08-25.
    - The existing default-width tests (newsDayFacets.test.ts:11, :160) pass unedited.
  </behavior>
  <action>
Export newsHistogramWidth from newsDayFacets.ts, with a comment citing store.py's inclusive cutoff and G-71. In both pages, pass `newsHistogramWidth(payload.window_days)` as the third argument of computeDayBuckets. Leave the 'Last 30 days' filter, the facet windows and the gap-caption logic untouched.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && npm run validate 2>&1 | tee "$TEMP/38-06-val.log" && npx vitest run && N=$(grep -cE "^  '[a-z0-9-]+\.mjs',$" scripts/validate/all.mjs) && E=$(grep -oE '^validators_expected: [0-9]+' ../.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-01-SUMMARY.md | cut -d' ' -f2) && [ "$N" -eq "$E" ] && grep -q "$N/$N validators passed" "$TEMP/38-06-val.log" && python -c 'import json,re,datetime as d;p=json.load(open("public/data/incidents/current.json",encoding="utf-8"));ds=sorted({i["date"] for i in p["incidents"] if re.fullmatch(r"\d{4}-\d{2}-\d{2}",str(i.get("date","")))});a=d.date.fromisoformat(ds[-1]);lo=(a-d.timedelta(days=p.get("window_days",30))).isoformat();need={x for x in ds if x>=lo};h=open("dist/news/index.html",encoding="utf-8").read();bars={m.group(1) for t in re.findall(r"<button[^>]*>",h) if "class=\"day-bar\"" in t for m in [re.search(r"data-day=\"([0-9-]+)\"",t)] if m};miss=need-bars;print("need",len(need),"bars",len(bars),"missing",sorted(miss));assert not miss'</automated>
    Expected: one chained command. N/N validators with N == validators_expected from 38-01-SUMMARY.md (R-14) (news-freshness #18 with the new assertion, facets #16 unchanged), vitest green, and the independent python check prints `missing []`. The same check on the 72a683a build (2026-09-25) printed `need 16 bars 30 missing ['2026-08-25']`, so the defect is live today. Negative control: remove the width argument on news.astro and rebuild; news-freshness.mjs exits 1 whenever the payload spans anchor − 30 (else the vitest fixture is the proof). Revert.
  </verify>
  <done>Every stored day has a bar on both news pages, and a validator keeps it that way.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-13 | Repudiation (data honesty) | histogram | mitigate | Width derived from the payload; the validator asserts coverage of every stored day. |
| T-38-14 | Tampering (false green) | validator clock | mitigate | The verdict uses the page's data-build-now, with a hard fail when absent. |
| T-38-SC | Tampering | installs | accept | none |
</threat_model>

<verification>
- The build + validate + vitest chain is green in one command.
- The Task-1 real-build rc is recorded in the SUMMARY (honest RED or "fixture-only RED").
- A news-freshness red is read by its message (R-15): "no .day-bar (G-71)" and "data-build-now missing" are code defects (gap plan), not news incidents.
</verification>

<success_criteria>
The oldest stored news day is visible on /news/ and /es/noticias/, and validator #18 reasons on the page's clock.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-06-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

Applied 2026-09-25 from 38-PREMORTEM.md.
- R-15: the objective and verification state that the two new news-freshness messages are code defects, not G-54 news incidents; 38-08 Task 2 and 38-09 Task 1 apply the same by-message rule.
- R-14: the Task 2 verify requires N == validators_expected from 38-01-SUMMARY.md.
