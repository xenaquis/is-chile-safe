---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 09
type: execute
wave: 6
depends_on: ["38-01", "38-02", "38-03", "38-04", "38-05", "38-06", "38-07", "38-08"]
files_modified:
  - .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-VERIFICATION.md
  - DEPLOYMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03, HYG-04, HYG-05, HYG-06, HYG-07]
executor: orchestrator (close gate, push, deploy polling, live-run evidence, records); fresh opus = served-route validator (Task 3)

must_haves:
  truths:
    - "The close gate runs at ONE HEAD in ONE command (OneDrive): build + validate + vitest + astro check + pytest + lint-workflows + check-secret-hygiene. It needs exit 0, validators N/N with N = V37 + 1 (recorded as `validators_expected:` and matched in the gate log, R-14), vitest ≥ V37_vitest + the Phase-38 tests, pytest ≥ P37 + the Phase-38 tests with 0 failed (exit code), astro 0 errors, and lint and secret hygiene at 0. V37/P37 come from 37-VERIFICATION.md. Floors measured at 72a683a on 2026-09-25: pytest 642 passed / 1 skipped / 2 xfailed, vitest 184 (15 files), validators 18."
    - "Push after `git pull --rebase --autostash origin master`, never --force; close_sha is the commit that reached origin/master (re-recorded after any retry rebase). Prod serves the pushed build by the G-51 method: 37-served-routes.sh PASS with data-build-now ≥ the deploy-on-code run's createdAt."
    - "HYG-01 on prod: /data/incidents/seen.json, /data/incidents/pending.json and /data/incidents/rejected/<m>.json for EVERY month file at HEAD return 404. /data/incidents/current.json, centroids.json and cut_list.json return 200. The repository copy stays public on GitHub (owner default), and the HYG-01 evidence says so (R-10)."
    - "HYG-02 on prod: the /map/ and /es/mapa/ titles equal the 38-02 strings, each of the 4 map pages has exactly 1 <h1>, /chile-crime-map/ is unchanged, and none carries noindex. The HYG-02 evidence records on-page differentiation per G-64 with the ranking effect unmeasured (no GSC); a 28-day GSC follow-up tied to HYG-07 goes to deferred-live (R-17 acceptance, G-73(4))."
    - "HYG-03 + G-59 live: the first scheduled news run whose headSha descends from close_sha concludes success. Its bot commit is the first 'auto-update news' commit inside the run's startedAt..updatedAt window; S0 = seen.json at that commit's parent, S1 = at the commit. Every key added has value == the run's UTC date (the startedAt day, or the updatedAt day when the run crossed midnight), so mismatches = 0, and 0 legacy keys are pruned (the cutover protects them). The check can be done retroactively from git + gh (R-06)."
    - "The /news/ prod histogram has a bar for every distinct current.json date ≥ anchor − window_days (missing = 0)."
    - "A fresh opus validator signs 38-VERIFICATION.md against prod/gh evidence. HYG-01..05 need PASS. HYG-06 is PASS (38-HYG06.md outcome NEW_DATA or IDENTICAL_LIVE) or DEFERRED (NOT_YET, ABORTED, ABORTED_SUSPECT, ABORTED_BUILDER or ABORTED_GATE → deferred-live, directive rule). HYG-07 is DEFERRED (operator, owner default G-70). ROADMAP Phase 38 is ticked only when HYG-01..05 are PASS."
    - "38-VERIFICATION.md carries an informational `heartbeat_cead:` line. If the CEAD heartbeat is red after a non-NEW_DATA HYG-06 outcome, that is the accepted expected signal (G-73(2)), and the final report asks the owner explicitly, with the expected exit date (R-09)."
  artifacts:
    - path: .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-VERIFICATION.md
      provides: "`HYG-0N: PASS|FAIL|DEFERRED` lines with evidence, close_sha, gate counts, served_routes line, live-run line, validator verdict"
  key_links:
    - from: 38-VERIFICATION.md
      to: ROADMAP `- [x] **Phase 38`
      via: "only when HYG-01..05 are PASS"
      pattern: "^HYG-0[1-5]: PASS"
---

<objective>
Close Phase 38: gate, push, prove the served surface and the next live pipeline run, get an independent validation, and update the run's records. This is the last phase of v2.2, so it also triggers the directive's final report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/37-dependency-security-hygiene/37-VERIFICATION.md
@.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
- all.mjs validator count: `grep -cE "^  '[a-z0-9-]+\.mjs',$" site/scripts/validate/all.mjs` (18 at 72a683a; +1 if 36-09 registered news-dedup; +1 public-data from 38-01). The summary line is "N/N validators passed" (all.mjs:109).
- 37-served-routes.sh <sha> (37-01, G-51/G-61): it resolves D (the newest non-[skip ci] commit touching the deploy-on-code paths), requires the deploy-on-code run for D = success and data-build-now on /news/ and /es/noticias/ ≥ its createdAt, plus 8 routes 200 and the 404 route. It exits 0/1/10/11/12 and has a 30-min cap.
- The prod ledger URLs measured on 2026-09-25 (all 200 except pending): seen.json 200 2,023,856 B; rejected/2026-09.json 200 2,150,403 B; pending.json 404; centroids.json 200; cut_list.json 200; current.json 200; sitemap-index.xml 200.
- The news cron is '0 */6 * * *' (news-pipeline.yml:5); measured start delays are up to about 3 h. The bot commit message is "data: auto-update news [skip ci]" (news-pipeline.yml:93).
- REQUIREMENTS.md:55-61 HYG-01..07 checkboxes; :110-116 traceability rows. ROADMAP Phase 38 list line: `- [ ] **Phase 38: …` (:123).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close gate at one HEAD, push, served-route proof + HYG-01/02/histogram prod checks</name>
  <files>.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-VERIFICATION.md</files>
  <action>
Step 0 (G-62, G-73(1)): `grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md` == 1, and 38-01..38-08 SUMMARYs exist (38-08 may have recorded any outcome). Else STOP.

1. `git pull --rebase --autostash origin master`. Read V37 from the `gate:` line of 37-VERIFICATION.md (`validators <k>/<k>`) and write `validators_expected: <V37+1>` to 38-VERIFICATION.md (R-14). Then run the gate as ONE background command, tee'd to "$SCR/38-gate.log": `cd site && npm run build && npm run validate && npx vitest run && npx astro check && cd .. && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-secret-hygiene.sh; echo "GATE rc=$?"`.
   Require: GATE rc=0; "E/E validators passed" with E = the validators_expected value; vitest and pytest counts ≥ the thresholds in must_haves; astro "0 errors". Write `validators_pass: <the names of the PASS validators from the VALIDATION SUITE SUMMARY>`.
   Read a red by its MESSAGE, not by the validator name (G-54, R-15). Only a freshness/news-freshness error about news age or evidence is a real news incident: record it and do not push. The news-freshness messages "has incidents but no .day-bar (G-71)" and "data-build-now missing" are code defects → gap plan. Any other red → fix via a gap plan; never exclude a check or unregister a validator.
2. `git push origin master` (no --force), then record `close_sha: <the pushed HEAD>`. If the push is rejected: pull --rebase again, re-run the gate, push, and re-record close_sha after that rebase, so the recorded value is the commit that reached origin/master (R-06).
3. Poll `bash .planning/phases/37-dependency-security-hygiene/37-served-routes.sh <close_sha>` every 60 s up to 30 min, and log `served_routes: PASS deployable=<D> built_at=<ts>`. At the cap with exit 10/11 → pause point 5 (G-51(d)).
4. Prod checks (curl with `-H 'Cache-Control: no-cache'`), each logged as `check <name>: <observed> (expected <x>) PASS|FAIL`:
   - HYG-01: 404 for seen.json, pending.json and every rejected/<m>.json listed in `git ls-files data/incidents/rejected` (3 files at 72a683a). 200 for current.json, centroids.json and cut_list.json.
   - HYG-02: /map/ `<title>Interactive Map — Reported Crime by Commune in Chile</title>`; /es/mapa/ `<title>Mapa interactivo — Delitos reportados por comuna en Chile</title>`; /chile-crime-map/ `<title>Chile Crime Map — Interactive Choropleth by Commune</title>`; `grep -o '<h1[ >]' | wc -l` == 1 on each of the 4 map pages; 0 "noindex".
   - Histogram: the 38-06 python check run against prod current.json and prod /news/ HTML gives `missing []`.
   - Region: one EN region page shows "Annual evolution (2005–<max series year>" with the partial marker when that year is partial.
   - CEAD heartbeat (informational, not a `check` line, R-09): `heartbeat_cead: <conclusion of the "Check CEAD reminder heartbeat" step of the latest heartbeat.yml run> (<the boundaries count from its log>)`, via `gh run list --workflow=heartbeat.yml -L1 --json databaseId` and `gh run view <id> --json jobs`. A red after a 38-HYG06.md outcome other than NEW_DATA is the accepted expected signal (G-73(2)); Task 3 carries it to the final report.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && V=.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-VERIFICATION.md && grep -qE "^GATE rc=0" "$V" && E=$(grep -oE '^validators_expected: [0-9]+' "$V" | cut -d' ' -f2) && V37=$(grep -oE '^gate: .* validators [0-9]+/' .planning/phases/37-dependency-security-hygiene/37-VERIFICATION.md | grep -oE 'validators [0-9]+' | cut -d' ' -f2) && [ "$E" -eq "$((V37+1))" ] && grep -q "$E/$E validators passed" "${SCR:?SCR must be the session scratchpad}/38-gate.log" && grep -qE "^validators_pass: " "$V" && grep -qE "^heartbeat_cead: " "$V" && grep -qE "^close_sha: [0-9a-f]{7,40}" "$V" && git fetch -q origin master && git merge-base --is-ancestor "$(grep -oE '^close_sha: [0-9a-f]+' "$V" | tail -1 | cut -d' ' -f2)" origin/master && grep -qE "^served_routes: PASS deployable=[0-9a-f]+ built_at=" "$V" && [ "$(grep -c '^check .* FAIL$' "$V")" -eq 0 ] && [ "$(grep -c '^check .* PASS$' "$V")" -ge 13 ] && for u in seen.json pending.json $(git ls-files data/incidents/rejected | sed 's#data/incidents/##'); do [ "$(curl -s -o /dev/null -w '%{http_code}' -H 'Cache-Control: no-cache' https://ischilesafe.com/data/incidents/$u)" = 404 ] || exit 1; done && [ "$(curl -s -o /dev/null -w '%{http_code}' https://ischilesafe.com/data/incidents/current.json)" = 200 ]</automated>
    Expected: the gate is green with E/E validators, where E = V37 + 1 from 37-VERIFICATION.md (a validator unregistered to get green fails here). The close sha is on origin/master, served routes PASS, ≥ 13 PASS checks and 0 FAIL checks, the heartbeat_cead line is present (informational), and a live re-curl confirms 404 on every ledger and 200 on current.json.
  </verify>
  <done>Phase-38 code is live on prod, proven by fingerprint and per-requirement curls.</done>
</task>

<task type="auto">
  <name>Task 2: Live pipeline evidence for HYG-03 and G-59 (first scheduled news run after the push)</name>
  <files>.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-VERIFICATION.md</files>
  <action>
The evaluation can be done retroactively from git + gh; no live 9-hour session is required (R-06).
1. Find the measured run: the first `schedule` news-pipeline run created after the push (`gh run list --workflow=news-pipeline.yml --event schedule --json databaseId,createdAt,conclusion`; poll every 15 min up to 9 h wall-clock, or look it up later). Read `gh run view <id> --json startedAt,updatedAt,headSha,conclusion`. Require conclusion == success and `git merge-base --is-ancestor <close_sha> <headSha>`: a run started from older code does not count, so take the next one. Write `live_run_ancestry: <close_sha> is-ancestor-of <headSha> yes`.
2. Its bot commit is the FIRST commit on origin/master by github-actions whose message contains 'auto-update news' with a committer date inside [startedAt, updatedAt]: `git log origin/master --author=github-actions --grep='auto-update news' --since=<startedAt> --until=<updatedAt> --format='%H %cI' --reverse | head -1`. Never use `--since … -1`, which returns the NEWEST commit (possibly from a later run with another UTC date).
3. S0 = `git show <bot_commit>^:data/incidents/seen.json` and S1 = `git show <bot_commit>:data/incidents/seen.json`, saved to the scratchpad.
4. added = keys(S1) − keys(S0). Every added value must equal the run date: the UTC day of startedAt, or of updatedAt when the run crossed midnight. mismatches = 0.
5. pruned = keys(S0) − keys(S1); require 0 (the cutover protects legacy keys for 60 days).
6. From the run log, `gh run view <id> --log | grep -m1 "Fetch summary"`, record fetched / keyword_passed / unseen_candidates as information.

G-59: record the run's checkout line (`gh run view <id> --log | grep -m2 -E "ref|Checking out"`) as information. The pin test is the gate.

If the run produced no commit (a no-op), take the next scheduled run, at most 3 runs. If 3 consecutive runs add no key, record `HYG-03_live: PARTIAL (no new URLs observed; pytest-proven)`.

Log `live_run: <id> added=<n> mismatches=0 pruned=0 unseen_candidates=<n>`.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && V=.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-VERIFICATION.md && { { grep -qE "^live_run: [0-9]+ added=[0-9]+ mismatches=0 pruned=0 unseen_candidates=[0-9]+" "$V" && grep -qE "^live_run_ancestry: [0-9a-f]+ is-ancestor-of [0-9a-f]+ yes$" "$V"; } || grep -qE "^HYG-03_live: PARTIAL" "$V"; }</automated>
    Expected: a live_run line with mismatches 0 and pruned 0 for a run proven to execute the new code (live_run_ancestry), or the declared PARTIAL line.
  </verify>
  <done>First_seen semantics are observed on a production run.</done>
</task>

<task type="auto">
  <name>Task 3: Fresh-opus served-route validation, records, final report trigger</name>
  <files>.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-VERIFICATION.md, DEPLOYMENT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/v2.2-AUTONOMOUS-DIRECTIVE.md</files>
  <action>
1. Spawn a FRESH opus validator (no prior context). It is given only the must_haves of 38-01..38-08, 38-VERIFICATION.md and prod URLs. It re-curls prod itself (it does not trust the log) and writes `HYG-0N: PASS|FAIL|DEFERRED — <evidence>` for N = 1..7, plus `validator_verdict: PASS|FAIL`:
   - HYG-01 evidence includes 'served copy removed; repository copy still public (owner default)' (R-10);
   - HYG-02 evidence includes 'on-page differentiation per G-64; ranking effect unmeasured (no GSC)' (R-17 acceptance, G-73(4));
   - HYG-04 evidence includes 'CI-only; the local path is covered by the DEPLOYMENT.md runbook (builders chained with &&, STOP on any red)' (R-08);
   - HYG-06 = PASS iff 38-HYG06.md outcome ∈ {NEW_DATA with served_routes PASS, IDENTICAL_LIVE}; otherwise DEFERRED (NOT_YET, ABORTED, ABORTED_SUSPECT, ABORTED_BUILDER, ABORTED_GATE);
   - HYG-07 = DEFERRED (G-70).
   Any FAIL → gap plan (`/gsd:plan-phase 38 --gaps`), with no tick.
2. If 38-HYG06.md has a NEW_DATA or IDENTICAL_LIVE outcome, append a dated "Oct-2026 live-verified scrape" paragraph under DEPLOYMENT.md's CEAD quarterly procedure section, quoting the core_diff line.
3. REQUIREMENTS.md: tick HYG-01..05 (and HYG-06 on PASS). Traceability rows: Complete / Deferred (operator) for HYG-07 / Deferred-live for HYG-06 when deferred.
4. ROADMAP: `- [x] **Phase 38` only if HYG-01..05 are PASS. Replace the Phase 38 `**Plans**:` line with "9 plans (38-01..38-09) — complete <date>", noting HYG-06/07 status.
5. STATE.md Current Position → v2.2 complete (or its exact residuals).
6. Directive:
   - RUN STATUS: phases row, git sha, spend (Phase 38 LLM spend ≈ 0, only live cron operations), baseline for the next run (the gate counts);
   - append the G-NN outcome record;
   - deferred-live: remove the 7 Phase-38-routed items that closed (G-59 checkout, yearMismatchTip, sync-data ledgers, histogram cap, region title, validator clock, SOURCES.md drift); keep GSC (HYG-07), /map/ noindex/redirect if the titles are not enough, the Granite-era decision, and HYG-06 when deferred plus any cadence owner question from 38-HYG06.md;
   - deferred-live, add: (a) owner: "seen.json/rejected/ with third-party headlines remain readable on GitHub; move to private storage? (history cannot be purged without a rewrite, which is forbidden)" (G-73(3), R-10); (b) a 28-day GSC check of the query "chile crime map" per URL (/map/ vs /chile-crime-map/), tied to HYG-07 (G-73(4), R-17); (c) "MapIsland default year 2025 must follow the latest complete CEAD year (scrape of Jan-2027)" (MapIsland.tsx:117 `useState(2025)`, R-07); (d) agent: a scrape_cead code guard that ignores cache hits for years ≥ run_date.year − 1 (R-05); (e) the 'last live-verified scrape' marker (G-72(4)), noting that the pipeline-failure-heartbeat label is shared with R2, which keeps its own pipeline-failure-r2 alert (R-09); (f) when 38-HYG06.md is ABORTED_SUSPECT, its owner question with the evidence (G-73(5)).
7. Write the directive's final report ("When the run finishes"), since 38 is the last phase, and update memory v2.2-milestone-news-recovery. If heartbeat_cead is red after a non-NEW_DATA HYG-06 outcome, the report asks the owner explicitly, with the expected exit date (the next data/cead commit: CEAD publication or the next quarterly procedure) (R-09, G-73(2)).
8. Commit `docs(38): close` (+ push after `git pull --rebase`, since docs-only commits do not deploy).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && V=.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-VERIFICATION.md && D=.planning/v2.2-AUTONOMOUS-DIRECTIVE.md && [ "$(grep -cE '^HYG-0[1-5]: PASS' "$V")" -eq 5 ] && grep -qE "^HYG-06: (PASS|DEFERRED)" "$V" && grep -qE "^HYG-07: DEFERRED" "$V" && grep -q "^validator_verdict: PASS" "$V" && grep -q "served copy removed; repository copy still public (owner default)" "$V" && grep -q "ranking effect unmeasured (no GSC)" "$V" && grep -q "CI-only; the local path is covered" "$V" && [ "$(grep -c '^- \[x\] \*\*Phase 38' .planning/ROADMAP.md)" -eq 1 ] && [ "$(grep -cE '^- \[x\] \*\*HYG-0[1-5]\*\*' .planning/REQUIREMENTS.md)" -eq 5 ] && ! grep -q "ResultPanel yearMismatchTip hardcodes" "$D" && grep -q "seen.json/rejected/ with third-party headlines" "$D" && grep -q "chile crime map" "$D" && grep -q "MapIsland default year 2025" "$D"</automated>
    Expected: 5 PASS + the HYG-06/07 lines + a validator PASS, with the HYG-01/02/04 evidence phrases; Phase 38 ticked; 5 requirements ticked; the routed deferred items removed and the new owner/agent items (public-repo ledgers, GSC 28-day check, MapIsland default year) present in the directive.
  </verify>
  <done>Phase 38 is closed with independent served-route validation, and the run's records and final report are current.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-21 | Repudiation (false green) | close evidence | mitigate | G-51 fingerprint, per-requirement curls, and a fresh-opus re-curl. |
| T-38-22 | Tampering | push | mitigate | Gate at one HEAD, pull --rebase, never --force. Revert only under G-51(d)/G-54. |
</threat_model>

<verification>
38-VERIFICATION.md holds validators_expected, validators_pass, GATE, close_sha, served_routes, the check lines, heartbeat_cead, live_run (+ live_run_ancestry) and the HYG-0N lines, and the validator verdict is PASS.
</verification>

<success_criteria>
Phase 38 is closed per the directive: gate ≥ baseline + new tests, pushed, deploy verified on prod, validated by a fresh opus against served routes, and the records are updated.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-09-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

Applied 2026-09-25 from 38-PREMORTEM.md and orchestrator decision G-73.
- R-06: Task 2 identifies the run's bot commit inside startedAt..updatedAt (first, not newest), takes S0/S1 at that commit's parent/commit, requires close_sha to be an ancestor of the run's headSha (`live_run_ancestry`, in the verify), dates by startedAt/updatedAt, and allows retroactive evaluation. Task 1 step 2 re-records close_sha after a retry rebase. Twins: truths (push, HYG-03 live).
- R-09: Task 1 step 4 writes the informational `heartbeat_cead:` line (in the verify, outside the ≥ 13 PASS count); Task 3 steps 6-7 carry the owner question and the shared-label note. Twin: new truth.
- R-10: the HYG-01 evidence phrase (Task 3 step 1, verify, truth); the public-repo ledgers owner item goes to deferred-live (G-73(3)).
- R-14: Task 1 writes `validators_expected: <V37+1>` from 37-VERIFICATION.md and the verify checks it plus "E/E validators passed" in the gate log, plus `validators_pass`. Twin: truth.
- R-15: Task 1 step 1 reads reds by message.
- R-17 (accepted) + G-73(4): the HYG-02 evidence phrase and the 28-day GSC deferred-live item (Task 3, verify, truth).
- R-05 / R-07 / R-08 / G-73(5) twins: deferred-live items (c) MapIsland default year, (d) scrape_cead cache guard, (f) the ABORTED_SUSPECT question; the HYG-04 evidence phrase; the HYG-06 DEFERRED mapping now lists ABORTED_SUSPECT and ABORTED_GATE.
- G-73(1): depends_on gains 38-08 and Step 0 requires 38-01..38-08 SUMMARYs (38-08 is no longer exempt; it always leaves a record). The Oct-2026 paragraph goes under DEPLOYMENT.md's "CEAD quarterly procedure" section (38-07's new name).
