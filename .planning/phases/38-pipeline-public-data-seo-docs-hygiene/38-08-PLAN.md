---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 08
type: execute
wave: 1
depends_on: []
independent: "HYG-06 only. Normal phase order (G-73(1) withdraws the G-62 carve-out): starts only after Phase 37 is ticked in ROADMAP and 37-served-routes.sh exists, on/after 2026-10-01, with no other plan executing. Runs on the local machine in an isolated git worktree of origin/master, so its only commit is data/cead/** and no unpushed or ungated Phase 36/37/38 commit can ride along."
files_modified:
  - "data/cead/** (only on NEW_DATA; HYG-06 output, the directive's data/ exception)"
  - .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-cead-core-diff.py
  - .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md
  - ".planning/v2.2-AUTONOMOUS-DIRECTIVE.md (one RUN STATUS line on non-NEW_DATA outcomes: the expected CEAD-heartbeat red, G-73(2))"
autonomous: true
requirements: [HYG-06]
executor: orchestrator (long local job: live scrape ≈ 198 courteous requests, gate, push, deploy polling)

must_haves:
  truths:
    - "The scrape runs locally (never in Actions: CEAD 403s runner IPs), only after Phase 37 is ticked in ROADMAP and 37-served-routes.sh exists (G-73(1): normal phase order; the G-62 carve-out is withdrawn), on or after 2026-10-01, with the scraper's existing courtesy delay unchanged (time.sleep(2.5) at scrape_cead.py:305 and :365) and single-threaded."
    - "Every CEAD key is fetched LIVE: the worktree's pipeline/cache holds 0 `cead_*` files at start, and after the run it holds 198 keyed files (7 families × 22 years 2005-2026 + 22 years × 2 homicide measures). The non-CEAD builder inputs (enusc_vhdv.xlsx, spd_*, sii_*) are copied in, so the builders run as they do in production."
    - "Exit 0 alone is never treated as evidence (DEPLOYMENT.md:582-585). The decision is the crime-core diff against origin/master with last_updated, composite_index, spd_homicide_rate, sii_exposure_index and enusc_vhdv excluded: C changed communes + R changed regions + N national, counting only differences beyond a 1e-9 relative float tolerance (float-only differences are reported as float_only). The instrument lists HEAD paths with git ls-tree, builds git-show paths with '/', exits 2 on any git-show failure, and is self-tested in the untouched worktree (all zeros, files_added=0 files_removed=0) and by a positive control (one planted 13101 change gives communes=1 regions=0 national=0). The scrape is sound iff the log has 0 'Failed batch' lines, every one of the 198 CEAD files parses to >= 346 formato_4 rows, and num_to_null = 0. NEW_DATA iff sound and C+R+N >= 1; IDENTICAL_LIVE iff sound and C+R+N == 0; not sound = ABORTED_SUSPECT (no commit; one retry >= 24 h later; if still suspect, an owner question with the evidence, G-73(5)). Changes to closed years are reported and do not block."
    - "NEW_DATA: the 4 builders run in the cead-scraper.yml order and any red builder STOPs (G-66); data/snapshots is then restored (fetch_enusc_vhdv.py rewrites the tracked snapshot's `fetched`). The full gate is green in the worktree. Before the commit the worktree has no tracked change outside data/cead; after the commit it is fully clean before the rebase (worktree_clean_before_rebase: yes). The commit contains only data/cead/** paths and is pushed after rebase (never --force), between plans. The deploy is proven on prod by the G-51 method (deploy-on-code fires on data/cead/**, per 35-02/G-21(6)); deploy-manual is dispatched only if no deploy-on-code run exists for the SHA within 10 min. A 37-served-routes.sh exit code outside {0,1,10,11,12} is an instrument error: STOP, no PASS, no revert."
    - "IDENTICAL_LIVE: nothing is committed (committing a no-op would reset the heartbeat's CEAD clock, DEPLOYMENT.md:576-581). The outcome and the quarterly-cadence re-evaluation are recorded as an owner question in deferred-live."
    - "38-HYG06.md carries machine-checkable lines: hyg06_date, selftest_zero, selftest_positive, cead_keys_fetched_live, scrape_rc, communes_written, failed_batches, cead_parse_min_rows, core_diff, exactly one outcome, main_cache_disarmed (every outcome except NOT_YET), and on NEW_DATA also worktree_clean_before_rebase, data_commit, deploy and served_routes."
    - "Every outcome except NOT_YET disarms the main tree's cache-first trap: the main tree's pipeline/cache/cead_*.html files move to pipeline/cache/stale-2026-06/ (gitignored, local, reversible; no test reads them), so a later run on the main tree cannot re-publish June's HTML as a complete 2026 (R-05)."
    - "On every outcome other than NEW_DATA, the directive RUN STATUS gets one heartbeat_cead_expected_red line: the CEAD heartbeat is red from 2026-10-01 until a data/cead commit lands, an accepted expected signal (G-73(2)); the 'last live-verified scrape' marker stays deferred-live (G-72(4))."
  artifacts:
    - path: .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-cead-core-diff.py
      provides: "stdlib diff of the crime core vs `git show HEAD:<posix path>` over the git ls-tree listing; prints `core_diff: communes=C regions=R national=N files_added=A files_removed=D num_to_null=K float_only=F years_changed=<y:n,...|none>` + <=5 examples; exits 2 on any git-show failure"
    - path: .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md
      provides: "HYG-06 outcome record"
  key_links:
    - from: worktree push (data/cead/**)
      to: .github/workflows/deploy-on-code.yml on.push.paths 'data/cead/**'
      via: "human push (not GITHUB_TOKEN, no [skip ci])"
      pattern: "data/cead/\\*\\*"
---

<objective>
HYG-06: execute the Oct-1 local CEAD scrape and record the outcome. Either new data is committed and deployed, or live-verified identical output is documented and the quarterly-cadence assumption is re-evaluated.

Purpose: a planning-time finding changes how this must run. scrape_cead.py is cache-first with no TTL. load_cached (pipeline/cead/client.py:196-199) returns any existing file, and the fetch at scrape_cead.py:289-306 (families) and :348-365 (homicide subgroup) happens only on a miss. The machine's cache holds all 198 keys; the 2025/2026 files are dated 2026-06-12 and 2026-06-16. A run on this machine's working tree would therefore re-parse June's HTML, which is the most likely reason the 2026-08-06 run came back byte-identical (DEPLOYMENT.md:571-577). A fresh worktree with the CEAD keys absent is the only way to ask CEAD.
Output: 38-HYG06.md, plus a data/cead commit on NEW_DATA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@DEPLOYMENT.md
@pipeline/scrape_cead.py
@.github/workflows/cead-scraper.yml

<interfaces>
Evidence read at 72a683a:
- scrape_cead.py:44-46 `_get_data_dir()` (CEAD_DATA_DIR override, else the default under the script's repo); :666-667 `sys.exit(_main_with_live_session())`, which creates a CEAD session and fetches the commune catalog live (:629-640). The family loop fetches for all 7 FAMILIA_IDS (catalog.py:44-52) × years 2005..run_date.year and breaks after the first region (:325); the homicide loop covers years × {rate, count}.
- pipeline/cead/client.py:31 `CACHE_DIR = Path(__file__).parent.parent / "cache"`, so it is worktree-local. :187-193 cache_raw; :196-199 load_cached (no TTL).
- The builders resolve paths from __file__ (build_composite_index.py:60, build_map_payload.py:33, build_enusc_enrichment.py:27), so they are also worktree-local. fetch_enusc_vhdv.py:12-22 needs pipeline/cache/enusc_vhdv.xlsx with CACHE_ONLY=1.
- pipeline/cache/ is gitignored (.gitignore:14); only .gitkeep is tracked. The local cache (measured 2026-09-25) has 198 keyed `cead_<region>_<fam>_<year>.html` / `cead_subgroup101_<year>_<rate|count>.html` files + cead_page.html, and 24 non-CEAD inputs (enusc_vhdv.xlsx, spd_vhc.xlsx, sii_pub_comu.xlsb, sii_emp.xlsx, …).
- data/cead today: 346 comunas/*.json, 16 regions/*.json, national.json; last data/cead commit ee91f21 (2026-06-19); last_updated 2026-06-16; Santiago 13101 2026 partial rate 2059.4045246118, 2025 = 9309.4099259081.
- cead-scraper.yml:68-87 builder order: fetch_enusc_vhdv.py (CACHE_ONLY=1) → build_enusc_enrichment.py → build_composite_index.py → build_map_payload.py (PYTHONPATH=. at job level :40).
- deploy-on-code.yml on.push.paths includes 'data/cead/**' (35-02 Task 2, G-21(6)). The G-51 check tool is `.planning/phases/37-dependency-security-hygiene/37-served-routes.sh <sha>` (37-01), with exit codes 0 PASS / 1 route / 10 stale build / 11 run missing or failed / 12 pending, and a 30-min polling cap.
- 37-served-routes.sh does not exist until 37-01 executes (absent on 2026-09-25). Step 0 checks it.
- pipeline/cead/parser.py:46 `parse_cead_table(html)` returns a list of {'name', 'class': list[str], 'values'}, and [] when the HTML has no table (:66-67). scrape_cead.py:324-328 logs "Failed batch region=… familia=… year=…" and swallows the exception, so rc stays 0. cache_raw (client.py:187-193) writes utf-8. On the June cache every one of the 198 files parses to >= 346 rows with class formato_4 (families and subgroup101; premortem R-03 baseline).
- fetch_enusc_vhdv.py:243-258 rewrites the tracked data/snapshots/enusc_vhdv.json with a fresh "fetched" timestamp on every run; the xlsx is sha-pinned (:53 KNOWN_SHA256), so `fetched` is the only change. rebase.autoStash is not configured, so any unstaged tracked change blocks `git rebase`.
- data/cead/comunas/13101.json: `series` is a list of {year, rate_per_100k, by_family, partial}; the year-2025 rate_per_100k is 9309.4099259081 (the positive-control target).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preconditions (Phase 37 ticked, served-routes tool, date), isolated worktree with an empty CEAD cache, instrument self-test, live courteous scrape, soundness checks, crime-core diff</name>
  <files>.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-cead-core-diff.py, .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md</files>
  <action>
Step 0, preconditions (G-73(1): normal phase order; the G-62 carve-out is withdrawn). Every record written below starts with `hyg06_date: <date -u +%F>`.
- `grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md` must print 1, and no other plan may be executing. Otherwise STOP without writing a record (the plan has not started).
- `test -f .planning/phases/37-dependency-security-hygiene/37-served-routes.sh`. If it fails, write `outcome: NOT_YET (37-01 not executed)` and STOP, with no scrape (R-04).
- Date gate. 38-09 depends on this plan, so the orchestrator schedules 38-08 after 38-01..38-07. If `date -u +%F` < 2026-10-01 at that point, wait for 2026-10-01 (wall clock). Only if the run cannot wait, write `outcome: NOT_YET (date <date> < 2026-10-01)` and STOP; 38-09 then carries HYG-06 to deferred-live (directive Phase 35-38 note).

Step 1, the instrument (R-02, R-03). Write 38-cead-core-diff.py (stdlib, argv: repo root):
- HEAD listing: `git -C root ls-tree -r --name-only HEAD -- data/cead/comunas data/cead/regions data/cead/national.json`. Working listing: glob data/cead/comunas/*.json, data/cead/regions/*.json and data/cead/national.json as '/'-joined relative paths. files_added and files_removed are the set differences of the two listings, never inferred from a failed git show.
- For each path in both listings, load the working copy and `git -C root show HEAD:<path>`, building <path> with Path.as_posix() (on Windows `git show HEAD:data\cead\national.json` fails with "exists on disk, but not in 'HEAD'", measured). If git show returns rc != 0 for a listed path, print `core_diff: ERROR <path>` and exit 2.
- Drop the top-level keys {last_updated, composite_index, spd_homicide_rate, sii_exposure_index, enusc_vhdv}, then compare recursively. Two numbers are equal when math.isclose(a, b, rel_tol=1e-9). A file that differs under exact equality but not under the tolerance counts in float_only, not in C/R/N.
- num_to_null = the number of numeric leaves under series (rate_per_100k, by_family values, any other numeric field) that are numbers at HEAD and null in the working copy. years_changed = for each series year, the number of files whose entry for that year changed beyond the tolerance.
- Print `core_diff: communes=C regions=R national=N files_added=A files_removed=D num_to_null=K float_only=F years_changed=<year:n,...|none>`, plus up to 5 lines `example <path> <year> <field> <old> -> <new>`. Exit 0.

Step 2, isolation (G-69) and instrument self-test. SCR = the session scratchpad. Run `git fetch origin master` and `git worktree add --detach "$SCR/hyg06" origin/master`. Copy every regular file directly under pipeline/cache/ (`find pipeline/cache -maxdepth 1 -type f`, so the stale-2026-06/ subdirectory of an earlier attempt is never copied) whose name does NOT start with `cead_` into "$SCR/hyg06/pipeline/cache/". Assert `ls "$SCR/hyg06/pipeline/cache" | grep -c '^cead_'` == 0. Then self-test the instrument in the worktree, before any scrape:
- (a) Zero. `python 38-cead-core-diff.py "$SCR/hyg06"` on the untouched worktree must print `communes=0 regions=0 national=0 files_added=0 files_removed=0 num_to_null=0`. Write `selftest_zero: <the output line without the "core_diff: " prefix>`.
- (b) Positive control. Add 1 to the year-2025 rate_per_100k of "$SCR/hyg06/data/cead/comunas/13101.json" (json load and dump; formatting is irrelevant because the instrument compares parsed JSON), re-run the instrument, and require `communes=1 regions=0 national=0` plus a line `example data/cead/comunas/13101.json 2025 rate_per_100k`. Write `selftest_positive: <the output line without the "core_diff: " prefix>`. Then run `git -C "$SCR/hyg06" checkout -- data/cead`.
If either self-test fails, fix the instrument and repeat. Never scrape with an unproven instrument.

Step 3, the scrape. Record the start time and run `cd "$SCR/hyg06" && PYTHONPATH=. python pipeline/scrape_cead.py 2>&1 | tee "$SCR/hyg06-scrape.log"` in the background, polling for completion. Expected runtime is ≈ 198 × (2.5 s + latency) ≈ 10-35 min. Require:
- rc 0;
- `ls pipeline/cache | grep -cE '^cead_([0-9]+_[1-7]_[0-9]{4}|subgroup101_[0-9]{4}_(rate|count))\.html$'` == 198 in the worktree;
- `ls data/cead/comunas/*.json | wc -l` == 346.
On a 403, a network error or rc != 0: record outcome ABORTED with the last 20 log lines, remove the worktree, and go to Task 2's "Every outcome except NOT_YET" block (deferred-live, owner question). Never retry from Actions.
Soundness (R-03), recorded here and decided in Task 2:
- `failed_batches: <n>`, where n = `grep -c 'Failed batch' "$SCR/hyg06-scrape.log"`;
- `cead_parse_min_rows: <N>`, where N = the minimum, over the 198 keyed cead_* files of the worktree cache, of the number of rows returned by pipeline.cead.parser.parse_cead_table (file read as utf-8, PYTHONPATH=. in the worktree) whose class list contains "formato_4". The June baseline is 346. List every file below 346.

Step 4. Run `python 38-cead-core-diff.py "$SCR/hyg06"` and write the 38-HYG06.md lines: attempt (1 or 2), cead_keys_fetched_live: n/198, scrape_rc, scrape_wall_s, communes_written, failed_batches, cead_parse_min_rows, and the core_diff line with its examples. Always include Santiago 13101's 2025 and 2026 rates old → new, even when unchanged. Report changes to closed years (years <= run year − 2, from years_changed) as `closed_years_changed: <year:n,...|none>`; they do not block. An attempt-2 record goes under a `## Attempt 2` heading. Exactly one `outcome:` line exists at the end (Task 2 writes it; attempt 1 of a retried run uses `attempt_1:`).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && H=.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md && grep -qE "^hyg06_date: [0-9]{4}-[0-9]{2}-[0-9]{2}$" "$H" && if grep -qE "^outcome: NOT_YET" "$H"; then true; else grep -qE "^selftest_zero: communes=0 regions=0 national=0 files_added=0 files_removed=0 num_to_null=0" "$H" && grep -qE "^selftest_positive: communes=1 regions=0 national=0" "$H" && if grep -qE "^outcome: ABORTED( |$)" "$H"; then true; else grep -qE "^cead_keys_fetched_live: 198/198$" "$H" && grep -qE "^scrape_rc: 0$" "$H" && grep -qE "^communes_written: 346$" "$H" && grep -qE "^failed_batches: [0-9]+$" "$H" && grep -qE "^cead_parse_min_rows: [0-9]+$" "$H" && grep -qE "^core_diff: communes=[0-9]+ regions=[0-9]+ national=[0-9]+ files_added=[0-9]+ files_removed=[0-9]+ num_to_null=[0-9]+ float_only=[0-9]+ years_changed=" "$H"; fi; fi</automated>
    Expected: the record starts with hyg06_date. The instrument passed its zero self-test (including files_added=0 files_removed=0) and its positive control (communes=1 regions=0 national=0) in the worktree. The record shows 198/198 live keys, rc 0, 346 communes, both soundness lines and a full core_diff line. On NOT_YET only hyg06_date and the outcome line are required. On ABORTED (403, network or rc) the self-test lines and the outcome line are required.
  </verify>
  <done>Whether CEAD has published anything new is measured against live responses, not the local cache, with an instrument proven to see a change and a scrape proven sound.</done>
</task>

<task type="auto">
  <name>Task 2: Act on the outcome — NEW_DATA (builders, gate, clean isolated commit, push, prod proof), IDENTICAL_LIVE (no commit, re-evaluation) or ABORTED_SUSPECT (no commit, one retry); disarm the main-tree cache</name>
  <files>data/cead/** (NEW_DATA only), .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md, pipeline/cache/ (main tree, gitignored move), .planning/v2.2-AUTONOMOUS-DIRECTIVE.md (one RUN STATUS line, non-NEW_DATA)</files>
  <action>
Decide from the Task-1 record (R-03). The scrape is sound iff failed_batches == 0, cead_parse_min_rows >= 346 and num_to_null == 0.
- ABORTED (recorded by Task 1) → go to "Every outcome except NOT_YET".
- Not sound → ABORTED_SUSPECT.
- Sound and C+R+N == 0 → IDENTICAL_LIVE (float_only > 0 is reported, not treated as new data).
- Sound and C+R+N >= 1 → NEW_DATA.

If ABORTED_SUSPECT (G-73(5)): no commit, no push, never publish. Write `attempt_<k>: ABORTED_SUSPECT <failed conditions>` with the last 20 log lines and the files below 346 rows, then `git worktree remove --force "$SCR/hyg06"`.
- k == 1: retry once, at least 24 h after attempt 1 started: a fresh worktree, Task 1 Steps 2-4 and this decision again. The plan stays open meanwhile; 38-09 waits for it.
- k == 2 and still not sound: write `outcome: ABORTED_SUSPECT` and the owner question for deferred-live (38-09 carries it): "CEAD live scrape unsound twice (<conditions>, <dates>): publish anyway, retry later, or wait?", with the evidence excerpt.

If IDENTICAL_LIVE:
- Run `git -C "$SCR/hyg06" checkout -- data/cead`, then `git worktree remove --force "$SCR/hyg06"`.
- Append to 38-HYG06.md: `outcome: IDENTICAL_LIVE`, plus a re-evaluation paragraph. It covers: this is the first live-verified quarter (the 2026-08-06 result was cache-tainted, G-69); the heartbeat's Jan/Apr/Jul/Oct boundary assumption (check-heartbeat.sh cead mode, F-93) is not supported by this evidence; and the owner question "keep quarterly boundaries, or move the CEAD heartbeat to an explicit 'last live-verified scrape' marker?" goes to deferred-live.
- No data commit, no push.

If NEW_DATA:
1. In the worktree with PYTHONPATH=., run in order: `CACHE_ONLY=1 python pipeline/snapshots/fetch_enusc_vhdv.py`, `python pipeline/build_enusc_enrichment.py`, `python pipeline/build_composite_index.py`, `python pipeline/build_map_payload.py`. Any rc != 0 → STOP, outcome ABORTED_BUILDER, no commit (G-66). Then run `git -C "$SCR/hyg06" checkout -- data/snapshots` (R-01: fetch_enusc_vhdv.py:248/:258 rewrites the tracked snapshot with a new `fetched`; the xlsx is sha-pinned, so nothing else changes).
2. Gate in the worktree, as ONE command: `cd "$SCR/hyg06/site" && npm ci && npm run build && npm run validate && npx vitest run && npx astro check && cd .. && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-secret-hygiene.sh`. Require exit 0, N/N validators (N = the all.mjs count at origin/master) and astro 0 errors. Read a red by its MESSAGE, not by the validator name (G-54, R-15). Only a freshness/news-freshness error about news age or evidence is a real news signal: record it, do NOT push, and pause. The news-freshness messages "has incidents but no .day-bar (G-71)" and "data-build-now missing" are code defects: record them, do NOT push, outcome ABORTED_GATE, gap plan. Any other red: do NOT push, outcome ABORTED_GATE with the failing check.
3. Run `git -C "$SCR/hyg06" add data/cead`, then `git -C "$SCR/hyg06" checkout -- data/comparator-pairs.json 2>/dev/null || true`. Require `[ -z "$(git -C "$SCR/hyg06" status --porcelain --untracked-files=no | grep -v ' data/cead/')" ]` and `git -C "$SCR/hyg06" diff --cached --name-only | grep -vc '^data/cead/'` == 0. Otherwise STOP with outcome `ABORTED_GATE (dirty worktree: <paths>)` and no commit. Commit as `data(38-08): CEAD quarterly scrape <UTC date> (HYG-06)`, with no [skip ci] so deploy-on-code runs. Require `git -C "$SCR/hyg06" status --porcelain --untracked-files=no` to be empty, and write `worktree_clean_before_rebase: yes` (R-01). Push only between plans (G-73(1): no other plan is executing). Then `git -C "$SCR/hyg06" fetch origin master && git -C "$SCR/hyg06" rebase FETCH_HEAD && git -C "$SCR/hyg06" push origin HEAD:master`. Never --force.
4. Deploy proof: poll `bash .planning/phases/37-dependency-security-hygiene/37-served-routes.sh <sha>` every 60 s up to 30 min (G-51). Any exit code outside {0, 1, 10, 11, 12} (e.g. 127) is an instrument error: STOP, no PASS, no revert, and report it (R-04). If `gh run list --workflow=deploy-on-code.yml --commit <sha>` is empty after 10 min, run `gh workflow run deploy-manual.yml` once and keep polling. On success, also require:
   - `curl -s https://ischilesafe.com/data/cead/comunas/13101.json` last_updated == the new value;
   - the /methodology/ page contains "CEAD data as of <formatted new date>".
   At the cap with exit 10/11: pause point 5 (Cloudflare log, G-51(d)). No revert without a fresh fingerprint and a failing route.
5. Append outcome NEW_DATA, data_commit, deploy (deploy-on-code run id or deploy-manual), served_routes: PASS deployable= built_at=, and the prod checks. Remove the worktree.

Every outcome except NOT_YET (after the final attempt's worktree is removed):
- Disarm the main tree's cache-first trap (R-05): in the main tree run `mkdir -p pipeline/cache/stale-2026-06 && mv pipeline/cache/cead_*.html pipeline/cache/stale-2026-06/`. It is gitignored, local and reversible, and no test depends on these files (test_client.py uses tmp_path). Write `main_cache_disarmed: <n> files`, with n = the number of files in stale-2026-06/.
- If the outcome is not NEW_DATA (G-73(2)), append one line to the directive's RUN STATUS: `heartbeat_cead_expected_red: from 2026-10-01 until a data/cead commit lands (HYG-06 <outcome> <date>); accepted expected signal (G-73(2)); 'last live-verified scrape' marker deferred-live (G-72(4))`.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && H=.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md && grep -qE "^outcome: (NEW_DATA|IDENTICAL_LIVE|ABORTED|ABORTED_SUSPECT|ABORTED_BUILDER|ABORTED_GATE|NOT_YET)" "$H" && [ "$(grep -c '^outcome: ' "$H")" -eq 1 ] && if grep -q "^outcome: NEW_DATA" "$H"; then C=$(grep -oE "^data_commit: [0-9a-f]{7,40}" "$H" | cut -d' ' -f2) && git fetch -q origin master && git merge-base --is-ancestor "$C" origin/master && [ "$(git show --name-only --format= "$C" | grep -vc '^data/cead/')" -eq 0 ] && grep -qE "^worktree_clean_before_rebase: yes$" "$H" && grep -qE "^served_routes: PASS deployable=[0-9a-f]+ built_at=" "$H"; else ! git log origin/master --since="$(grep -oE '^hyg06_date: [0-9-]+' "$H" | head -1 | cut -d' ' -f2)" --format=%h -- data/cead | grep -q .; fi && if ! grep -q "^outcome: NOT_YET" "$H"; then grep -qE "^main_cache_disarmed: [0-9]+ files" "$H" && [ "$(ls pipeline/cache | grep -c '^cead_')" -eq 0 ] && { grep -q "^outcome: NEW_DATA" "$H" || grep -q "^heartbeat_cead_expected_red: " .planning/v2.2-AUTONOMOUS-DIRECTIVE.md; }; fi && ! git worktree list | grep -q hyg06</automated>
    Expected: exactly one outcome line is present. On NEW_DATA, the data commit is on origin/master and touches only data/cead/**, the worktree was clean before the rebase, and served_routes is PASS. On any other outcome, no data/cead commit exists since the scrape date. Except on NOT_YET, the main tree holds 0 cead_* cache files (disarmed), and a non-NEW_DATA outcome left the heartbeat_cead_expected_red line in RUN STATUS. The worktree is gone.
  </verify>
  <done>HYG-06 is executed and recorded: new CEAD data is live on prod, or live-verified identity is documented along with the cadence question for the owner, or a suspect scrape is withheld and escalated. The main-tree cache trap is disarmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local machine → CEAD (government server) | ≈ 198 POSTs, courtesy-limited |
| local worktree → origin/master | a human push that triggers a prod build |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-17 | Denial of service (courtesy) | CEAD requests | mitigate | Single-threaded, with the existing 2.5 s delay per request. One run per quarter. No Actions retries. |
| T-38-18 | Tampering / integrity | partial data commit | mitigate | Red builders STOP. data/snapshots is restored and the worktree must be clean outside data/cead before the commit and fully clean before the rebase. Commit only when the diff is data/cead/**. The full gate runs in the worktree. |
| T-38-19 | Elevation (ungated code to prod) | push from a tree with unpushed phase commits | mitigate | Phase 37 ticked and no other plan executing (G-73(1)). The worktree comes from origin/master, the cached-diff path check applies, and there is no --force. |
| T-38-20 | Repudiation | false "no new data" | mitigate | Empty CEAD cache (198/198 live keys asserted) + the crime-core diff instrument: git ls-tree listing, posix paths, exit 2 on a git-show failure, zero self-test and positive control in the worktree. |
| T-38-23 | Tampering / integrity | a 200-status CEAD error page cached and parsed as data | mitigate | Soundness gate: 0 'Failed batch' lines, >= 346 formato_4 rows per file, num_to_null = 0; otherwise ABORTED_SUSPECT, never published, one retry >= 24 h later, then an owner question (G-73(5)). |
| T-38-24 | Tampering / integrity | main-tree cache-first trap (June HTML re-published as a complete year) | mitigate | Main-tree cead_* files moved to pipeline/cache/stale-2026-06/; the alert body (38-04) and the runbook (38-07) point to the fresh-worktree procedure; a code guard is deferred-live. |
</threat_model>

<verification>
- 38-HYG06.md carries all record lines for the branch taken.
- The main OneDrive tree is untouched by this plan, except: the two files under .planning/phases/38-…/ (committed with 38-09's docs, or separately as `docs(38-08): …` on a non-NEW_DATA outcome, together with the RUN STATUS line); the gitignored cache move pipeline/cache/cead_*.html → pipeline/cache/stale-2026-06/; and, on a non-NEW_DATA outcome, the single heartbeat_cead_expected_red line in the directive's RUN STATUS.
</verification>

<success_criteria>
HYG-06 closes with a measured, soundness-checked outcome. The cache-first trap is avoided in the worktree and disarmed in the main tree.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-08-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

Applied 2026-09-25 from 38-PREMORTEM.md and orchestrator decision G-73.
- R-01: Task 2 NEW_DATA step 1 restores data/snapshots; step 3 restores data/comparator-pairs.json, requires no tracked change outside data/cead before the commit and a clean worktree before the rebase (`worktree_clean_before_rebase: yes`, checked in the Task 2 verify). Twins: must_haves truths, T-38-18.
- R-02: the instrument (Task 1 Step 1) lists HEAD with git ls-tree, builds posix paths, exits 2 on a git-show failure, and derives files_added/removed from listings. The self-test moved to Step 2 in the untouched worktree (zero incl. files_added=0 files_removed=0) plus a positive control on 13101; the Task 1 verify greps selftest_zero/selftest_positive instead of running on the main tree. Twins: truth, artifact provides, T-38-20.
- R-03: soundness lines (failed_batches, cead_parse_min_rows), num_to_null / float_only / years_changed / closed_years_changed; outcome ABORTED_SUSPECT with one retry >= 24 h later, then an owner question (G-73(5)); added to the Task 2 regex. Twin: T-38-23.
- R-04: under G-73(1) the G-62 carve-out is withdrawn: Step 0 requires Phase 37 ticked and 37-served-routes.sh present (else NOT_YET), pushes happen between plans, and a served-routes rc outside {0,1,10,11,12} is a STOP. Twins: frontmatter `independent`, truth, T-38-19; 38-09 now depends on 38-08. Not applied (moot): registering the carve-out as G-NN, and waiting on the Phase-37 merge queue (Phase 37 is closed before this plan runs).
- R-05: every outcome except NOT_YET moves the main tree's cead_* cache to pipeline/cache/stale-2026-06/ (`main_cache_disarmed`, checked in the verify); Step 2 copies regular files only. Twins: truth, verification, T-38-24. The alert body is in 38-04, the runbook in 38-07, and the code guard goes to deferred-live via 38-09.
- R-15: Task 2 step 2 reads gate reds by message; ABORTED_GATE was added for code-defect and other reds (previously undefined).
- G-73(2): a non-NEW_DATA outcome writes heartbeat_cead_expected_red to RUN STATUS (files_modified, Task 2 files, verify).
- Also fixed: every record (NOT_YET included) starts with hyg06_date, so the Task 2 verify's `git log --since` is never empty; the Task 2 verify requires exactly one outcome line.
