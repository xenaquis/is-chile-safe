---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 08
type: execute
wave: 1
depends_on: []
independent: "HYG-06 only. Exempt from the Phase-37 precondition (G-62). Runs on/after 2026-10-01 on the local machine in an isolated git worktree of origin/master, so its only commit is data/cead/** and no unpushed or ungated Phase 36/37/38 commit can ride along."
files_modified:
  - "data/cead/** (only on NEW_DATA; HYG-06 output, the directive's data/ exception)"
  - .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-cead-core-diff.py
  - .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md
autonomous: true
requirements: [HYG-06]
executor: orchestrator (long local job: live scrape ≈ 198 courteous requests, gate, push, deploy polling)

must_haves:
  truths:
    - "The scrape runs locally (never in Actions: CEAD 403s runner IPs), on or after 2026-10-01, with the scraper's existing courtesy delay unchanged (time.sleep(2.5) at scrape_cead.py:305 and :365) and single-threaded."
    - "Every CEAD key is fetched LIVE: the worktree's pipeline/cache holds 0 `cead_*` files at start, and after the run it holds 198 keyed files (7 families × 22 years 2005-2026 + 22 years × 2 homicide measures). The non-CEAD builder inputs (enusc_vhdv.xlsx, spd_*, sii_*) are copied in, so the builders run as they do in production."
    - "Exit 0 alone is never treated as evidence (DEPLOYMENT.md:582-585). The decision is the crime-core diff against origin/master with last_updated, composite_index, spd_homicide_rate, sii_exposure_index and enusc_vhdv excluded: C changed communes + R changed regions + N national. It is NEW_DATA iff C+R+N ≥ 1."
    - "NEW_DATA: the 4 builders run in the cead-scraper.yml order and any red builder STOPs (G-66). The full gate is green in the worktree. The commit contains only data/cead/** paths and is pushed after rebase (never --force). The deploy is proven on prod by the G-51 method (deploy-on-code fires on data/cead/**, per 35-02/G-21(6)); deploy-manual is dispatched only if no deploy-on-code run exists for the SHA within 10 min."
    - "IDENTICAL_LIVE: nothing is committed (committing a no-op would reset the heartbeat's CEAD clock, DEPLOYMENT.md:576-581). The outcome and the quarterly-cadence re-evaluation are recorded as an owner question in deferred-live."
    - "38-HYG06.md carries machine-checkable lines: hyg06_date, cead_keys_fetched_live, scrape_rc, communes_written, core_diff, outcome, and on NEW_DATA also data_commit, deploy and served_routes."
  artifacts:
    - path: .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-cead-core-diff.py
      provides: "stdlib diff of the crime core vs `git show HEAD:<path>`; prints `core_diff: communes=C regions=R national=N files_added=A files_removed=D` + ≤5 examples"
    - path: .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md
      provides: "HYG-06 outcome record"
  key_links:
    - from: worktree push (data/cead/**)
      to: .github/workflows/deploy-on-code.yml on.push.paths 'data/cead/**'
      via: "human push (not GITHUB_TOKEN, no [skip ci])"
      pattern: "data/cead/\\*\\*"
---

<objective>
HYG-06: execute the Oct-1 local CEAD scrape and record the outcome. Either new data is committed and deployed, or live-verified identical output is documented and the quarterly-cadence assumption is re-evaluated.

Purpose: a planning-time finding changes how this must run. scrape_cead.py is cache-first with no TTL. load_cached (pipeline/cead/client.py:196-199) returns any existing file, and the fetch at scrape_cead.py:289-306 (families) and :348-365 (homicide subgroup) happens only on a miss. The machine's cache holds all 198 keys; the 2025/2026 files are dated 2026-06-12 and 2026-06-16. A run on this machine's working tree would therefore re-parse June's HTML, which is the most likely reason the 2026-08-06 run came back byte-identical (DEPLOYMENT.md:571-577). A fresh worktree with the CEAD keys absent is the only way to ask CEAD.
Output: 38-HYG06.md, plus a data/cead commit on NEW_DATA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@DEPLOYMENT.md
@pipeline/scrape_cead.py
@.github/workflows/cead-scraper.yml

<interfaces>
Evidence read at 72a683a:
- scrape_cead.py:44-46 `_get_data_dir()` (CEAD_DATA_DIR override, else the default under the script's repo); :666-667 `sys.exit(_main_with_live_session())`, which creates a CEAD session and fetches the commune catalog live (:629-640). The family loop fetches for all 7 FAMILIA_IDS (catalog.py:44-52) × years 2005..run_date.year and breaks after the first region (:325); the homicide loop covers years × {rate, count}.
- pipeline/cead/client.py:31 `CACHE_DIR = Path(__file__).parent.parent / "cache"`, so it is worktree-local. :187-193 cache_raw; :196-199 load_cached (no TTL).
- The builders resolve paths from __file__ (build_composite_index.py:60, build_map_payload.py:33, build_enusc_enrichment.py:27), so they are also worktree-local. fetch_enusc_vhdv.py:12-22 needs pipeline/cache/enusc_vhdv.xlsx with CACHE_ONLY=1.
- pipeline/cache/ is gitignored (.gitignore:14); only .gitkeep is tracked. The local cache (measured 2026-09-25) has 198 keyed `cead_<region>_<fam>_<year>.html` / `cead_subgroup101_<year>_<rate|count>.html` files + cead_page.html, and 24 non-CEAD inputs (enusc_vhdv.xlsx, spd_vhc.xlsx, sii_pub_comu.xlsb, sii_emp.xlsx, …).
- data/cead today: 346 comunas/*.json, 16 regions/*.json, national.json; last data/cead commit ee91f21 (2026-06-19); last_updated 2026-06-16; Santiago 13101 2026 partial rate 2059.4045246118, 2025 = 9309.4099259081.
- cead-scraper.yml:68-87 builder order: fetch_enusc_vhdv.py (CACHE_ONLY=1) → build_enusc_enrichment.py → build_composite_index.py → build_map_payload.py (PYTHONPATH=. at job level :40).
- deploy-on-code.yml on.push.paths includes 'data/cead/**' (35-02 Task 2, G-21(6)). The G-51 check tool is `.planning/phases/37-dependency-security-hygiene/37-served-routes.sh <sha>` (37-01), with exit codes 0 PASS / 1 route / 10 stale build / 11 run missing or failed / 12 pending, and a 30-min polling cap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Date gate, isolated worktree with an empty CEAD cache, live courteous scrape, crime-core diff</name>
  <files>.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-cead-core-diff.py, .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md</files>
  <action>
Step 0, date gate. If `date -u +%F` < 2026-10-01, STOP. Write "HYG-06 not yet actionable (<date>)" to 38-HYG06.md, and let 38-09 carry HYG-06 to deferred-live (directive Phase 35-38 note). There is NO Phase-37 precondition for this plan (G-62).

Step 1, the instrument. Write 38-cead-core-diff.py (stdlib, argv: repo root):
- For every data/cead/comunas/*.json, data/cead/regions/*.json and data/cead/national.json, load the working copy and `git -C root show HEAD:<path>`.
- Drop the top-level keys {last_updated, composite_index, spd_homicide_rate, sii_exposure_index, enusc_vhdv}, then compare with json equality.
- Print `core_diff: communes=C regions=R national=N files_added=A files_removed=D`, plus up to 5 lines `example <path> <year> <field> <old> -> <new>`.
- Self-test before use: run it on the untouched main tree and require all zeros.

Step 2, isolation (G-62/G-69). SCR = the session scratchpad. Run `git fetch origin master` and `git worktree add --detach "$SCR/hyg06" origin/master`. Copy every file in pipeline/cache/ whose name does NOT start with `cead_` into "$SCR/hyg06/pipeline/cache/". Assert `ls "$SCR/hyg06/pipeline/cache" | grep -c '^cead_'` == 0.

Step 3, the scrape. Record the start time and run `cd "$SCR/hyg06" && PYTHONPATH=. python pipeline/scrape_cead.py 2>&1 | tee "$SCR/hyg06-scrape.log"` in the background, polling for completion. Expected runtime is ≈ 198 × (2.5 s + latency) ≈ 10-35 min. Require:
- rc 0;
- `ls pipeline/cache | grep -cE '^cead_([0-9]+_[1-7]_[0-9]{4}|subgroup101_[0-9]{4}_(rate|count))\.html$'` == 198 in the worktree;
- `ls data/cead/comunas/*.json | wc -l` == 346.
On a 403, a network error or rc ≠ 0: record outcome ABORTED with the last 20 log lines, remove the worktree, and STOP (deferred-live, owner question). Never retry from Actions.

Step 4. Run `python 38-cead-core-diff.py "$SCR/hyg06"` and write the 38-HYG06.md lines: hyg06_date, cead_keys_fetched_live: n/198, scrape_rc, scrape_wall_s, communes_written, and the core_diff line with its examples. Always include Santiago 13101's 2025 and 2026 rates old → new, even when unchanged.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && H=.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md && python .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-cead-core-diff.py . | grep -q "communes=0 regions=0 national=0" && grep -qE "^cead_keys_fetched_live: 198/198$" "$H" && grep -qE "^scrape_rc: 0$" "$H" && grep -qE "^communes_written: 346$" "$H" && grep -qE "^core_diff: communes=[0-9]+ regions=[0-9]+ national=[0-9]+" "$H"</automated>
    Expected: the instrument self-test is all zeros on the main tree, and the record shows 198/198 live keys, rc 0, 346 communes and a numeric core_diff. (On the date-gate or ABORTED branches the record holds that line instead, and this verify is replaced by `grep -qE "^outcome: (NOT_YET|ABORTED)" "$H"`.)
  </verify>
  <done>Whether CEAD has published anything new is measured against live responses, not the local cache.</done>
</task>

<task type="auto">
  <name>Task 2: Act on the outcome — NEW_DATA (builders, gate, isolated commit, push, prod proof) or IDENTICAL_LIVE (no commit, re-evaluation)</name>
  <files>data/cead/** (NEW_DATA only), .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md</files>
  <action>
If C+R+N == 0 (IDENTICAL_LIVE):
- Run `git -C "$SCR/hyg06" checkout -- data/cead`, then `git worktree remove --force "$SCR/hyg06"`.
- Append to 38-HYG06.md: `outcome: IDENTICAL_LIVE`, plus a re-evaluation paragraph. It covers: this is the first live-verified quarter (the 2026-08-06 result was cache-tainted, G-69); the heartbeat's Jan/Apr/Jul/Oct boundary assumption (check-heartbeat.sh cead mode, F-93) is not supported by this evidence; and the owner question "keep quarterly boundaries, or move the CEAD heartbeat to an explicit 'last live-verified scrape' marker?" goes to deferred-live.
- No data commit, no push.

If C+R+N ≥ 1 (NEW_DATA):
1. In the worktree with PYTHONPATH=., run in order: `CACHE_ONLY=1 python pipeline/snapshots/fetch_enusc_vhdv.py`, `python pipeline/build_enusc_enrichment.py`, `python pipeline/build_composite_index.py`, `python pipeline/build_map_payload.py`. Any rc ≠ 0 → STOP, outcome ABORTED_BUILDER, no commit (G-66).
2. Gate in the worktree, as ONE command: `cd "$SCR/hyg06/site" && npm ci && npm run build && npm run validate && npx vitest run && npx astro check && cd .. && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-secret-hygiene.sh`. Require exit 0, N/N validators (N = the all.mjs count at origin/master) and astro 0 errors. A red freshness/news-freshness is a real news signal (G-54): record it, do NOT push, and pause.
3. `git -C "$SCR/hyg06" add data/cead`. Require `git -C "$SCR/hyg06" diff --cached --name-only | grep -vc '^data/cead/'` == 0. Commit as `data(38-08): CEAD quarterly scrape <UTC date> (HYG-06)`, with no [skip ci] so deploy-on-code runs. Then `git -C "$SCR/hyg06" fetch origin master && git -C "$SCR/hyg06" rebase FETCH_HEAD && git -C "$SCR/hyg06" push origin HEAD:master`. Never --force.
4. Deploy proof: poll `bash .planning/phases/37-dependency-security-hygiene/37-served-routes.sh <sha>` every 60 s up to 30 min (G-51). If `gh run list --workflow=deploy-on-code.yml --commit <sha>` is empty after 10 min, run `gh workflow run deploy-manual.yml` once and keep polling. On success, also require:
   - `curl -s https://ischilesafe.com/data/cead/comunas/13101.json` last_updated == the new value;
   - the /methodology/ page contains "CEAD data as of <formatted new date>".
   At the cap with exit 10/11: pause point 5 (Cloudflare log, G-51(d)). No revert without a fresh fingerprint and a failing route.
5. Append outcome NEW_DATA, data_commit, deploy (deploy-on-code run id or deploy-manual), served_routes: PASS deployable= built_at=, and the prod checks. Remove the worktree.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && H=.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md && grep -qE "^outcome: (NEW_DATA|IDENTICAL_LIVE|ABORTED|ABORTED_BUILDER|NOT_YET)" "$H" && if grep -q "^outcome: NEW_DATA" "$H"; then C=$(grep -oE "^data_commit: [0-9a-f]{7,40}" "$H" | cut -d' ' -f2) && git fetch -q origin master && git merge-base --is-ancestor "$C" origin/master && [ "$(git show --name-only --format= "$C" | grep -vc '^data/cead/')" -eq 0 ] && grep -qE "^served_routes: PASS deployable=[0-9a-f]+ built_at=" "$H"; else ! git log origin/master --since="$(grep -oE '^hyg06_date: [0-9-]+' "$H" | cut -d' ' -f2)" --format=%h -- data/cead | grep -q .; fi && ! git worktree list | grep -q hyg06</automated>
    Expected: an outcome line is present. On NEW_DATA, the data commit is on origin/master, touches only data/cead/** and has served_routes PASS. On any other outcome, no data/cead commit since the scrape date exists. The worktree is gone.
  </verify>
  <done>HYG-06 is executed and recorded: either new CEAD data is live on prod, or live-verified identity is documented along with the cadence question for the owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local machine → CEAD (government server) | ≈ 198 POSTs, courtesy-limited |
| local worktree → origin/master | a human push that triggers a prod build |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-17 | Denial of service (courtesy) | CEAD requests | mitigate | Single-threaded, with the existing 2.5 s delay per request. One run per quarter. No Actions retries. |
| T-38-18 | Tampering / integrity | partial data commit | mitigate | Red builders STOP. Commit only when the diff is data/cead/**. The full gate runs in the worktree. |
| T-38-19 | Elevation (ungated code to prod) | push from a tree with unpushed phase commits | mitigate | The worktree comes from origin/master, the cached-diff path check applies, and there is no --force. |
| T-38-20 | Repudiation | false "no new data" | mitigate | Empty CEAD cache (198/198 live keys asserted) + the crime-core diff instrument, self-tested. |
</threat_model>

<verification>
- 38-HYG06.md carries all record lines for the branch taken.
- The main OneDrive tree is untouched by this plan, except the two files under .planning/phases/38-…/, which are committed with 38-09's docs (or separately as `docs(38-08): …` on an IDENTICAL/ABORTED outcome).
</verification>

<success_criteria>
HYG-06 closes with a measured outcome, and the cache-first trap is avoided.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-08-SUMMARY.md` when done.
</output>
