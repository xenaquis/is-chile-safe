---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 05
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - site/src/lib/yearMismatch.ts
  - site/src/lib/yearMismatch.test.ts
  - site/src/components/map/ResultPanel.tsx
  - site/src/pages/region/[slug].astro
  - site/src/pages/es/region/[slug].astro
autonomous: true
requirements: [HYG-05]
directive_items: ["deferred-live: ResultPanel yearMismatchTip hardcodes '(2025)' (Phase-35 review F3)", "deferred-live: region chart title '(2005–2025)' with a faded 2026 bar (validator F3)"]
executor: sonnet (UI copy / i18n)

must_haves:
  truths:
    - "The ResultPanel 'Why years differ' tooltip names the latest complete CEAD year derived from the commune's own series (max year with partial !== true), not a literal 2025. The composite year stays COMPOSITE_YEAR (2024)."
    - "When the two years are equal, the 'Why years differ' tooltip is not rendered, because there is no difference to explain."
    - "The region 'Annual evolution' heading's end year equals the last year the Sparkline draws (max series year). When that year is partial, the heading marks it: EN '(2005–2026; 2026 partial)', ES '(2005–2026; 2026 parcial)'. No invented month cutoff; cead-vintage.mjs (#17) stays green."
  artifacts:
    - path: site/src/lib/yearMismatch.ts
      provides: "yearMismatchTip(lang, compositeYear, latestCompleteYear): string | null; latestCompleteCeadYear(series): number | null"
  key_links:
    - from: site/src/components/map/ResultPanel.tsx
      to: site/src/lib/yearMismatch.ts
      via: "import; tip only when non-null"
      pattern: "yearMismatchTip\\("
---

<objective>
Close two copy-drift items that the directive routes to Phase 38 (HYG-05 "match the repo"): a hardcoded year in the map result panel, and a region chart heading that contradicts the bar the chart draws.

Purpose: when CEAD 2026 completes, ResultPanel.tsx:289-290 would still say "(2025)". The region heading "(2005–{latestCompleteYear})" (region/[slug].astro:221, es :213) reads 2005–2025 while Sparkline.astro:41 draws a faded 2026 bar (series 2026 partial=true, measured in data/cead/national.json and regions/*.json on 2026-09-25).
Output: pure helper + vitest, wired panel, honest region headings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@site/src/components/map/ResultPanel.tsx
@site/src/pages/region/[slug].astro
@site/src/lib/ceadVintage.ts

<interfaces>
Evidence read at 72a683a:
- ResultPanel.tsx:39-42 `const COMPOSITE_YEAR = 2024;`. :54 SeriesEntry has `partial?: boolean`. :76 `series: SeriesEntry[]`. :288-290 yearMismatchTip is ES/EN literals containing "(2025)". :291 yearMismatchLabel. :297 `<RateTooltip lang={lang} label={yearMismatchLabel} tip={yearMismatchTip} />` inside the composite-index h4.
- region/[slug].astro:49-60 latestCompleteYear (region series non-partial max, national fallback). :63 `ceadV = ceadVintage(region.series, …)`. :220-222 section, h2 "Annual evolution (2005–{latestCompleteYear})" and `<Sparkline series={region.series} activeYear={latestCompleteYear} />`. es/region/[slug].astro:59 ceadV and :212-213 h2 "Evolución anual (2005–{latestCompleteYear})".
- Sparkline.astro:14 series type with partial?; :41 `opacity = s.partial ? 0.5 : 1`, which draws every series year.
- cead-vintage.lib.mjs:131-133 bans "is a partial year" / "es un año parcial" only INSIDE the .cead-vintage node when partialYear is null. :161-163 findBannedPartialLabels bans Jan–Jun/ene–jun/(partial data)/(datos parciales) site-wide. The new heading must avoid those four strings: "2026 partial" and "2026 parcial" are allowed.
- The description meta (:140) and temporalCoverage (:154) describe rates and keep latestCompleteYear. They are unchanged.
- No validator references the heading text (grep 'Annual evolution|Evolución anual|2005–' in scripts/validate: 0 hits).
- Collision check: Phases 36/37 touch none of these files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: yearMismatch helper + ResultPanel wiring</name>
  <files>site/src/lib/yearMismatch.ts, site/src/lib/yearMismatch.test.ts, site/src/components/map/ResultPanel.tsx</files>
  <behavior>
    - latestCompleteCeadYear([{year:2024},{year:2025},{year:2026,partial:true}]) === 2025; [] gives null; all partial gives null.
    - yearMismatchTip('en', 2024, 2025) contains "uses 2024" and "latest complete CEAD year (2025)", and equals today's EN literal byte-for-byte (a regression pin on the copy). The same holds for 'es' with "(2025)".
    - yearMismatchTip('en', 2024, 2026) contains "(2026)" and not "(2025)".
    - yearMismatchTip(lang, 2024, 2024) === null; yearMismatchTip(lang, 2024, null) === null.
  </behavior>
  <action>
Step 0 (G-62): `grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md` must print 1, else STOP. This plan builds site/ and never runs alongside another site-building plan.

Create yearMismatch.ts: a pure function with no imports beyond types. Move the two literals from ResultPanel.tsx:288-290 into it, replacing the "2024" and "2025" occurrences with the parameters.

In ResultPanel.tsx, compute `const tip = yearMismatchTip(lang, COMPOSITE_YEAR, latestCompleteCeadYear(data.series))`, and render the RateTooltip only when tip !== null. The label and year badge are unchanged.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run src/lib/yearMismatch.test.ts && ! grep -n "(2025)" src/components/map/ResultPanel.tsx</automated>
    Expected: vitest passes, and no "(2025)" literal remains in ResultPanel.tsx.
  </verify>
  <done>The tooltip year is data-derived, and the copy is pinned against today's wording.</done>
</task>

<task type="auto">
  <name>Task 2: Region evolution heading = drawn range, partial year marked (EN/ES)</name>
  <files>site/src/pages/region/[slug].astro, site/src/pages/es/region/[slug].astro</files>
  <action>
In each region page, add `const lastDrawnYear = region.series.length ? Math.max(...region.series.map((s) => s.year)) : latestCompleteYear;` and `const lastDrawnPartial = region.series.some((s) => s.year === lastDrawnYear && s.partial === true);`.

Headings:
- EN: `Annual evolution (2005–{lastDrawnYear}{lastDrawnPartial ? `; ${lastDrawnYear} partial` : ''})`
- ES: `Evolución anual (2005–{lastDrawnYear}{lastDrawnPartial ? `; ${lastDrawnYear} parcial` : ''})`

Leave the Sparkline props, the .cead-vintage paragraph, the description and the JSON-LD unchanged.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && npm run validate 2>&1 | tee "$TEMP/38-05-val.log" && npx vitest run && N=$(grep -cE "^  '[a-z0-9-]+\.mjs',$" scripts/validate/all.mjs) && grep -q "$N/$N validators passed" "$TEMP/38-05-val.log" && R=$(ls dist/region | head -1) && grep -q "Annual evolution (2005–2026; 2026 partial)" "dist/region/$R/index.html" && E=$(ls dist/es/region | head -1) && grep -q "Evolución anual (2005–2026; 2026 parcial)" "dist/es/region/$E/index.html" && ! grep -rq "Annual evolution (2005–2025)" dist/region</automated>
    Expected: one chained command. N/N validators (cead-vintage #17 and forbidden-language included), vitest green, the new headings present on a sampled EN and ES region page, and no "(2005–2025)" region heading left. The thresholds assume 2026 is partial, as in the data on 2026-09-25. If HYG-06 (38-08) has already committed a CEAD vintage that changes the partial year, substitute the measured max year and state it in the SUMMARY.
  </verify>
  <done>The region heading describes exactly what the chart draws.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-12 | Repudiation (editorial accuracy) | year labels | mitigate | Derived from the series, with vitest + cead-vintage.mjs + forbidden-language.mjs in the gate. |
| T-38-SC | Tampering | installs | accept | none |
</threat_model>

<verification>
- The file list in `git diff --stat` matches files_modified.
- The build + validate + vitest chain is green in one command.
</verification>

<success_criteria>
No hardcoded CEAD year remains in the map tooltip, and region headings match the drawn series.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-05-SUMMARY.md` when done.
</output>
