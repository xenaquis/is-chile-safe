---
phase: 38-pipeline-public-data-seo-docs-hygiene
plan: 07
type: execute
wave: 5
depends_on: ["38-01", "38-02", "38-03", "38-04", "38-05", "38-06"]
files_modified:
  - data/SOURCES.md
  - site/scripts/validate/figure-registry.mjs
  - CLAUDE.md
  - DEPLOYMENT.md
  - .planning/STATE.md
  - "~/.claude/projects/C--Users-Carlo-OneDrive---pjud-cl-Documentos-GitHub-Is-Chile-Safe/memory/*.md (outside git; orchestrator)"
autonomous: true
requirements: [HYG-05, HYG-07]
directive_items: ["deferred-live: data/SOURCES.md:417 model-name drift (Granite named)", "G-06: data/SOURCES.md wording for the public ledgers"]
executor: sonnet (Tasks 1-2, docs); orchestrator (Task 3, memory files outside the repo)

must_haves:
  truths:
    - "data/SOURCES.md names the live classifier exactly as the code resolves it: OpenRouter `deepseek/deepseek-v4.1-flash` primary (model_config.py:28-31, chosen by the G-16 A/B) with DeepSeek-direct `deepseek-v4-flash` backup (model_config.py:34). There are 0 occurrences of 'granite' outside a clearly dated history sentence. The cadence says every 6 hours (news-pipeline.yml:5). The storage section states that seen.json, rejected/ and pending.json are internal ledgers not published on the site (HYG-01, G-63), that they remain in the public GitHub repository, and that only rejected candidates are also archived in R2 (archive_r2.py:172-197; seen.json and pending.json are not in R2, R-10). Every ingestion claim matches the code as of this plan's HEAD (including 36-07 publisher-URL decoding)."
    - "The CLAUDE.md stack block (between the GSD stack markers) states the versions pinned at this plan's HEAD (pipeline/requirements.txt, site/package.json) and describes the classifier pattern as above. .planning/research/STACK.md is NOT edited: it is the v2.1 research document (116 lines, 'Stack Research — v2.1') and has already diverged from the CLAUDE.md block (a 10,187-char block vs a 15,545-char file, and the block's first line is not in the file), so it is not a live mirror."
    - "DEPLOYMENT.md: the banner date is today. The stale DEEPSEEK_API_KEY claims (:19, :145-147, :245, :563) state the NREC-04 backup role (R-11). There are new operational notes for HYG-01 (repository copy still public; owner question in deferred-live), HYG-03, HYG-04 (CI-only; the local path is the runbook) and G-59. A 'CEAD quarterly procedure' section records the cache-first finding and the owner's quarterly local procedure (fresh worktree, empty CEAD cache, ≈198 requests, soundness checks, crime-core diff, the 4 builders chained with &&, main-tree cache disarmed, Cloudflare-log check on a stale build; G-73(6)); :20, :402 and the cead-scraper.yml alert body (38-04) point to it (R-05). A GSC operator block names https://ischilesafe.com/sitemap-index.xml (HYG-07, deferred-live by owner default)."
    - "STATE.md: the '_Last updated_' banner (line 18, stamped 2026-07-30) is replaced by a current one. § Blockers marks H-05 and M-07 RESOLVED with the resolving commit/plan. No blocker resolved in phases 29-38 is left open. '## Operator Next Steps' lists the v2.2 residuals instead of 'NEXT: `/gsd:plan-phase 28`', and '## Phase 29 readiness' is marked historical (R-12)."
    - "The memory entries whose carry-overs are resolved are updated (orchestrator)."
    - "figure-registry.mjs (#13, which reads data/SOURCES.md) stays green, and its DOCS-03 block also requires the live DEFAULT_OPENROUTER_MODEL and DEFAULT_BACKUP_MODEL literals of pipeline/news/model_config.py in SOURCES.md, so the next provider change cannot leave SOURCES.md stale unnoticed (R-13; negative control proven)."
  artifacts:
    - path: data/SOURCES.md
      provides: "accurate news-layer methodology (classifier, cadence, ledgers)"
    - path: DEPLOYMENT.md
      provides: "HYG-01/03/04/G-59 notes, corrected CEAD runbook, GSC operator block"
  key_links:
    - from: data/SOURCES.md
      to: pipeline/news/model_config.py
      via: "model ids quoted verbatim"
      pattern: "deepseek/deepseek-v4.1-flash"
    - from: site/scripts/validate/figure-registry.mjs
      to: pipeline/news/model_config.py
      via: "DOCS-03 check reads DEFAULT_OPENROUTER_MODEL / DEFAULT_BACKUP_MODEL by regex and requires both in data/SOURCES.md"
      pattern: "DEFAULT_OPENROUTER_MODEL"
---

<objective>
HYG-05: make CLAUDE.md, STATE.md, DEPLOYMENT.md and data/SOURCES.md match the repo, and clear stale memory carry-overs. HYG-07: write the operator instruction for the GSC sitemap and record it as deferred-live (owner default).

Purpose: data/SOURCES.md:416-420 still calls the dead Granite model "the default model for this milestone". CLAUDE.md:53-58 lists requests 2.32.x, bs4 4.14.x, lxml 5.x, openai 1.x and tenacity 8.x, while requirements.txt pins 2.34.2, 4.15.0, 6.1.1, 2.53.0 and 9.1.4 (72a683a; Phase 37 may bump more). The DEPLOYMENT.md CEAD runbook (§ "CEAD quarterly scrape — verified state (2026-08-06)", :569-595) concludes that CEAD published nothing, but the scraper is cache-first with no TTL (pipeline/cead/client.py:196-199; scrape_cead.py:289-306 and :348-365 only fetch on a cache miss). The 2025/2026 cache files are dated 2026-06-12/16, so that run most likely re-read June's HTML.
Output: corrected docs; memory updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-RESEARCH.md
@data/SOURCES.md
@DEPLOYMENT.md
@pipeline/news/model_config.py

<interfaces>
Evidence read at 72a683a (re-read at execution, since Phases 36/37 and 38-01..06 land first):
- data/SOURCES.md:398-406 ingestion (gnews_decoder resolves before full-text fetch; verify against 36-07's ingest_url.py and scrape_news.py at HEAD); :414-420 classification (Granite); :440-442 storage (current.json + archive/ only); :452 "Vintage: Refreshed via GitHub Actions cron (daily)". The news cron is '0 */6 * * *' (news-pipeline.yml:5).
- model_config.py:28 DEFAULT_PROVIDER "openrouter"; :31 DEFAULT_OPENROUTER_MODEL "deepseek/deepseek-v4.1-flash"; :34 DEFAULT_BACKUP_MODEL "deepseek-v4-flash". The workflow can override via vars.NEWS_MODEL/NEWS_PROVIDER (news-pipeline.yml:78-80). Check with `gh variable list`; if one is set, document the served value and say so.
- CLAUDE.md :24 `<!-- GSD:stack-start source:research/STACK.md -->` … :170 `<!-- GSD:stack-end -->`. Edit only inside these markers. The named source .planning/research/STACK.md is a diverged v2.1 research doc (measured 2026-09-25), so leave it untouched. Do NOT touch the project/conventions/architecture/skills/workflow/profile blocks. Stack drift lines: :34 Astro "7.1.x" (site/package.json:20 "astro": "7.1.6" at 72a683a; 37-01 raises it to ≥ 7.2.8 per G-49), :53 requests, :54 bs4, :55 lxml, :57 openai, :58 tenacity, :61-68 "DeepSeek API Usage Pattern" (deepseek-v4-flash for classification), :139 and :155 openai 1.x. Record the exact installed version from `pip show`/requirements.txt and package-lock for each.
- DEPLOYMENT.md:3 "Last updated: 2026-08-04"; :17-19 the overview's DEEPSEEK_API_KEY "only if NEWS_PROVIDER=deepseek"; :145-147 "Without `DEEPSEEK_API_KEY`: No effect on the default configuration … nothing in this repo does today" (R-11); :245 the cadence-table row ("consumed only if NEWS_PROVIDER=deepseek"); :563 the checklist "(optional, only if NEWS_PROVIDER=deepseek)". The correct role is already stated at :123 and :454 (NREC-04 backup on failover/force_backup). 37-06 adds DEPS-04 sections to this file before this plan (re-read). HYG-06's outcome paragraph is written by 38-09, not here.
- STATE.md:18 banner (2026-07-30, v2.1). § Blockers (:193-204): H-05 "new CEAD data pushed by a maintainer never triggers a production build" (resolved by deploy-manual.yml 2026-08-06, DEPLOYMENT.md:399, and by deploy-on-code data paths in 35-02) and M-07 "four continue-on-error steps in cead-scraper.yml" (resolved by 38-04). Both are still listed as BACKLOG. 36-11/37-07 edit STATE.md's Current Position before this plan.
- Memory (outside git): MEMORY.md index + ui-360-diagnostic-pending.md ("8 astro-check errors" carry-over; the baseline is 0 errors), v2.1-autonomous-run.md ("15 Dependabot PRs, 12 security alerts", superseded by the Phase-37 outcome), phase-16-news-live.md ("DeepSeek default", superseded by G-16), granite-default-classifier.md ("replacement in v2.2 P34", done, G-16), prod-deploy-hook-secret-gap.md ("only 22-03 GSC sitemap deferred", now HYG-07 deferred-live, G-70), cead-scraper-quarterly-local.md (add the cache-first caveat, G-69).
- G-68: data/SOURCES.md is documentation that lives under data/. Editing it is the HYG-05 docs work the directive deferred here (G-06), not a data/ mutation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: data/SOURCES.md + CLAUDE.md stack block match the code; figure-registry pins the live model ids</name>
  <files>data/SOURCES.md, CLAUDE.md, site/scripts/validate/figure-registry.mjs</files>
  <action>
Step 0 (G-62): `grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md` must print 1, else STOP.

data/SOURCES.md (G-68):
- Rewrite the Classification paragraph with the served provider/model pair and the backup. Keep one dated history sentence: "Until 2026-09-04 the default was ibm-granite/granite-4.1-8b, delisted by OpenRouter; replaced after the Phase-34 A/B (2026-09-23)."
- Change the Vintage line to "Refreshed by a GitHub Actions cron every 6 hours".
- Add to Storage (R-10): "Internal pipeline ledgers (data/incidents/seen.json, rejected/, pending.json) are not published on the site (HYG-01). They remain in the public GitHub repository; rejected candidates are also archived in R2." In the SUMMARY, list the owner item for 38-09's deferred-live: "seen.json/rejected/ with third-party headlines remain readable on GitHub; move to private storage? (history cannot be purged without a rewrite, which is forbidden)" (G-73(3)).
- Re-check the Ingestion paragraph against the code at HEAD, and correct it only where it differs.
- Never write absolute safe/dangerous language, and keep the outlet attribution sentence.
site/scripts/validate/figure-registry.mjs (R-13): extend DOCS03_CHECKS (figure-registry.mjs:397) with one check labelled 'live classifier ids (model_config.py)'. It reads pipeline/news/model_config.py (path overridable by the env var FIGURE_REGISTRY_MODEL_CONFIG, resolved against REPO_ROOT, used only for the negative control), extracts the DEFAULT_OPENROUTER_MODEL and DEFAULT_BACKUP_MODEL string literals by regex (`^DEFAULT_OPENROUTER_MODEL: str = "([^"]+)"`, the same for DEFAULT_BACKUP_MODEL), and requires both strings in SOURCES.md. It FAILS when either literal cannot be extracted. Keep the existing /granite/ check (it now guards the dated history sentence). Change nothing else in the validator.

CLAUDE.md stack block: update each drifting version to the pinned value at HEAD, formatted uniformly as `<major>.<minor>.x` (e.g. requests 2.34.2 → "2.34.x", tenacity 9.1.4 → "9.1.x"; the verify regex expects that form). Apply the same form to Astro/@astrojs/react/React from site/package.json. Rewrite "DeepSeek API Usage Pattern" to describe the live pattern (OpenRouter primary deepseek/deepseek-v4.1-flash, DeepSeek-direct backup deepseek-v4-flash, openai SDK for both). Fix the openai rows at :139/:155. Change nothing else.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && node site/scripts/validate/figure-registry.mjs && grep -q "deepseek/deepseek-v4.1-flash" data/SOURCES.md && [ "$(grep -ci 'granite' data/SOURCES.md)" -le 1 ] && ! grep -q "cron (daily)" data/SOURCES.md && grep -q "not published on the site (HYG-01)" data/SOURCES.md && grep -q "remain in the public GitHub repository" data/SOURCES.md && python -c "import re;a=open('CLAUDE.md',encoding='utf-8').read();s=a.split('<!-- GSD:stack-start source:research/STACK.md -->')[1].split('<!-- GSD:stack-end -->')[0].strip();req=open('pipeline/requirements.txt').read();ok=all(re.search(r'\| '+p+r' \| '+re.escape(v.rsplit('.',1)[0]),s) for p,v in re.findall(r'^(requests|lxml|tenacity|openai)==([0-9.]+)',req,re.M));print('versions',ok);assert ok" && cp pipeline/news/model_config.py pipeline/cache/38-07-mc-negative.py && sed -i 's#deepseek/deepseek-v4.1-flash#other/other-model#' pipeline/cache/38-07-mc-negative.py && { FIGURE_REGISTRY_MODEL_CONFIG=pipeline/cache/38-07-mc-negative.py node site/scripts/validate/figure-registry.mjs > /dev/null 2>&1; rc=$?; rm -f pipeline/cache/38-07-mc-negative.py; test $rc -eq 1; }</automated>
    Expected: figure-registry PASS; the live model is named; ≤ 1 dated "granite" mention; the cadence is fixed; the ledger sentence (not served, public repo, rejected in R2) is present; the stack rows match requirements.txt major.minor for requests, lxml, tenacity and openai. Negative control: figure-registry exits 1 against a model_config copy (gitignored pipeline/cache/) whose primary id differs, and the copy is removed.
  </verify>
  <done>The reader-facing methodology and the agent-facing stack both describe the running system.</done>
</task>

<task type="auto">
  <name>Task 2: DEPLOYMENT.md + STATE.md drift, CEAD runbook correction, GSC operator block</name>
  <files>DEPLOYMENT.md, .planning/STATE.md</files>
  <action>
DEPLOYMENT.md:
- Set the banner to today's date.
- Replace the stale DEEPSEEK_API_KEY claims (:19, :145-147, :245, :563) with the NREC-04 wording already used at :123 and :453 (R-11). :145-147 currently says "Without `DEEPSEEK_API_KEY`: No effect on the default configuration … nothing in this repo does today"; it must say that the key is the NREC-04 failover / force_backup target, so without it that backup path is unavailable (CEAD scraping is unaffected either way).
- In § 10 Operational Notes, add four dated lines:
  - HYG-01 (ledgers not served; allowlist in site/scripts/sync-data.lib.mjs; validator public-data.mjs; the repository copy stays public on GitHub, owner default, with the owner question in deferred-live, G-73(3));
  - HYG-03 (seen.json values = first_seen UTC date, 60-day prune, cutover constant in feeds.py);
  - HYG-04 (CEAD builders fail the job; no partial commits. CI-only: on Actions the ENUSC step runs only when pipeline/cache/enusc_vhdv.xlsx exists, and the real quarterly path is local, covered by the CEAD quarterly procedure's &&-chained builders; R-08);
  - G-59 (news checkout ref: master).
- Under the CEAD quarterly runbook, add a section "CEAD quarterly procedure (fresh worktree, empty CEAD cache; G-69)": the owner's own quarterly step (G-73(6)). It opens with a "Cache-first caveat (2026-09-25, G-69)" paragraph: scrape_cead.py reads pipeline/cache/cead_*.html before any network call and the cache has no TTL (client.py:196-199), so a run on a machine with a warm cache re-parses old HTML. The 2026-08-06 "byte-identical" result is therefore not evidence that CEAD published nothing, and in Jan-2027 a warm-cache run would publish 2026 as a complete year from June's HTML. Then the numbered procedure:
  1. `git worktree add --detach <scratch>/cead origin/master`;
  2. copy only the non-CEAD inputs (regular files in pipeline/cache/ not starting with cead_: the enusc/spd/sii files), so the CEAD cache is empty;
  3. in the worktree run `PYTHONPATH=. python pipeline/scrape_cead.py`: every CEAD key is fetched live, single-threaded, with the existing 2.5 s delay (≈198 requests, 10-35 min), never from Actions (403);
  4. soundness: 0 "Failed batch" log lines, 198 keyed cead_* files each parsing to ≥ 346 formato_4 rows, 346 comunas; otherwise do not publish;
  5. diff the crime core against origin/master with the 38-08 instrument (.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-cead-core-diff.py, created by 38-08); no change → commit nothing;
  6. on new data run the builders chained, stopping at the first red: `CACHE_ONLY=1 python pipeline/snapshots/fetch_enusc_vhdv.py && python pipeline/build_enusc_enrichment.py && python pipeline/build_composite_index.py && python pipeline/build_map_payload.py`, then `git checkout -- data/snapshots`;
  7. run the full gate, commit only data/cead/**, pull --rebase, push (never --force); deploy-on-code fires on data/cead/**;
  8. prove the deploy with the G-51 served-route check; if no build appears or the fingerprint stays stale, check the Cloudflare Pages build log (pause point 5, G-51(d));
  9. keep the main tree's CEAD cache disarmed (pipeline/cache/stale-2026-06/).
  End with: "The first live-verified run and its outcome are recorded in .planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-HYG06.md (38-08)." (The Oct-2026 result paragraph is appended by 38-09.)
- Point DEPLOYMENT.md:20 (the architecture diagram's `python pipeline/scrape_cead.py` under GH Actions) and :402 (the historical H-05 runbook sentence) to that section, e.g. "(Actions: 403; quarterly run = § CEAD quarterly procedure)". Do not rewrite the historical H-05 text beyond the pointer (R-05).
- Add a "Google Search Console (HYG-07, operator)" block: property https://ischilesafe.com/ → Sitemaps → submit `https://ischilesafe.com/sitemap-index.xml` (200 on 2026-09-25; /sitemap.xml 404 is expected with @astrojs/sitemap). "Deferred-live: needs the owner's Google account (directive default)."

STATE.md:
- Replace the line-18 banner with a one-paragraph current banner (v2.2, phases 34-38, pointing to the directive RUN STATUS).
- In § Blockers, prefix the H-05 and M-07 bullets with "RESOLVED <date> (<evidence>)", and mark any other bullet whose resolution is recorded in phases 29-37 summaries or the directive decision log.
- Replace the body of '## Operator Next Steps' (v2.1 text, including 'NEXT: `/gsd:plan-phase 28`') with the v2.2 residuals, pointing to the directive RUN STATUS: GSC sitemap (HYG-07, operator), the CEAD quarterly procedure (DEPLOYMENT.md), the Granite-era decision, the CEAD heartbeat red signal (G-73(2)) and the public-repo ledgers question (G-73(3)) (R-12).
- Prefix the '## Phase 29 readiness (checked 2026-07-30 …)' body with "Historical (v2.1; Phase 29 is closed):" and keep its text.
- Do not delete other history, and do not touch Current Position (38-09 does).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && [ "$(grep -c 'only if NEWS_PROVIDER=deepseek\|consumed only if .NEWS_PROVIDER=deepseek' DEPLOYMENT.md)" -eq 0 ] && ! grep -q 'nothing in this repo does today' DEPLOYMENT.md && ! grep -q 'No effect on the default configuration' DEPLOYMENT.md && grep -q "sitemap-index.xml" DEPLOYMENT.md && grep -q "Cache-first caveat" DEPLOYMENT.md && grep -q "empty CEAD cache" DEPLOYMENT.md && [ "$(grep -c 'CEAD quarterly procedure' DEPLOYMENT.md)" -ge 3 ] && grep -qF "build_composite_index.py && python pipeline/build_map_payload.py" DEPLOYMENT.md && grep -q "HYG-01" DEPLOYMENT.md && grep -q "HYG-04" DEPLOYMENT.md && grep -q "ref: master" DEPLOYMENT.md && ! sed -n 18p .planning/STATE.md | grep -q "2026-07-30" && [ "$(awk '/^### Blockers/{f=1;next} /^### /{f=0} f' .planning/STATE.md | grep -c '^- \*\*BACKLOG')" -eq 0 ] && ! grep -qF 'NEXT: `/gsd:plan-phase 28`' .planning/STATE.md && grep -ci "granite" DEPLOYMENT.md | awk '{exit !($1<=1)}'</automated>
    Expected: 0 stale DeepSeek-only claims (including :145-147), the GSC block, the CEAD quarterly procedure section with its cache caveat and &&-chained builders, referenced ≥ 3 times (section + :20 + :402 pointers), the notes present, the STATE banner current, 0 open BACKLOG blockers, no v2.1 'NEXT' instruction in STATE.md, and ≤ 1 dated Granite mention.
  </verify>
  <done>Operational docs match the repo, and the operator knows the exact GSC step.</done>
</task>

<task type="auto">
  <name>Task 3 (orchestrator): Clear stale memory carry-overs</name>
  <files>~/.claude/projects/C--Users-Carlo-OneDrive---pjud-cl-Documentos-GitHub-Is-Chile-Safe/memory/ (MEMORY.md + the six files in interfaces)</files>
  <action>
Edit only what is stale, and cite the evidence:
- ui-360-diagnostic-pending: "8 astro-check errors" → "astro check 0 errors since Phase 34 baseline (directive RUN STATUS)".
- v2.1-autonomous-run: the Dependabot/security counts → pointer to the Phase-37 outcome (37-VERIFICATION.md).
- phase-16-news-live and granite-default-classifier: replacement done → deepseek/deepseek-v4.1-flash via OpenRouter + DeepSeek-direct backup (G-16).
- prod-deploy-hook-secret-gap: GSC → HYG-07 deferred-live (G-70).
- cead-scraper-quarterly-local: add the cache-first caveat (G-69), and replace 'run scrape_cead.py locally' with a pointer to DEPLOYMENT.md § CEAD quarterly procedure (R-05).
- MEMORY.md: the matching one-line index hooks.

These files are outside git: there is no commit. List each file and the changed line in the SUMMARY.
  </action>
  <verify>
    <automated>M="$HOME/.claude/projects/C--Users-Carlo-OneDrive---pjud-cl-Documentos-GitHub-Is-Chile-Safe/memory" && ! grep -q "8 astro-check errors" "$M/MEMORY.md" && grep -qi "cache" "$M/cead-scraper-quarterly-local.md" && grep -q "v4.1-flash" "$M/granite-default-classifier.md"</automated>
    Expected: exit 0.
  </verify>
  <done>No memory entry asserts a state the repo has moved past.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-15 | Repudiation (methodology accuracy) | data/SOURCES.md | mitigate | Model ids quoted from model_config.py; figure-registry.mjs in the gate now requires the live ids read from model_config.py (R-13). |
| T-38-16 | Information disclosure | docs | mitigate | No secret values are written (names only); check-secret-hygiene runs in the 38-09 gate. |
</threat_model>

<verification>
- `git diff --stat` shows only the 5 repo files (data/SOURCES.md, CLAUDE.md, DEPLOYMENT.md, .planning/STATE.md, site/scripts/validate/figure-registry.mjs). The memory edits are listed in the SUMMARY.
- figure-registry PASS.
</verification>

<success_criteria>
HYG-05 docs match the repo at this HEAD. HYG-07 has an exact operator instruction and is deferred-live per the owner default.
</success_criteria>

<output>
Create `.planning/phases/38-pipeline-public-data-seo-docs-hygiene/38-07-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

Applied 2026-09-25 from 38-PREMORTEM.md and orchestrator decision G-73.
- R-05: Task 2 turns the cache caveat into the "CEAD quarterly procedure" section and points DEPLOYMENT.md:20 and :402 to it; "The procedure is executed in 38-08" became a pointer to 38-HYG06.md (38-08 now runs after this plan, G-73(1)); memory cead-scraper-quarterly-local points to it (Task 3). Twin: truth; verify (≥ 3 references).
- R-08: the procedure lists the 4 builders chained with &&; the HYG-04 note says CI-only. Twins: truth, verify.
- R-10: the Storage sentence now says "not published on the site … remain in the public GitHub repository; rejected candidates are also archived in R2" (Task 1, truth, verify); the owner item is handed to 38-09 via the SUMMARY (G-73(3)).
- R-11: DEPLOYMENT.md:145-147 added to the replacement list; the verify rejects both stale phrases. Twin: truth.
- R-12: STATE.md Operator Next Steps rewritten to the v2.2 residuals, Phase 29 readiness marked historical; the verify rejects 'NEXT: `/gsd:plan-phase 28`'. Twin: truth.
- R-13: figure-registry.mjs added (files_modified, Task 1 files, key_links, truth, T-38-15, verification's file count 4 → 5); a DOCS-03 check reads the live ids from model_config.py; negative control in the verify (copy under gitignored pipeline/cache/).
- G-73(6): the quarterly local procedure (fresh worktree, empty cache, ≈198 requests, CF-log check on a stale build) is documented by Task 2.
