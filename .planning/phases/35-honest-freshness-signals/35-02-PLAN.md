---
phase: 35-honest-freshness-signals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/news/store.py
  - pipeline/tests/test_store.py
  - .github/scripts/news-deploy-needed.sh
  - .github/workflows/news-pipeline.yml
  - .github/workflows/heartbeat.yml
  - .github/workflows/deploy-on-code.yml
  - DEPLOYMENT.md
  - pipeline/tests/test_news_deploy_gate.py
autonomous: true
requirements: [FRESH-04, FRESH-01, FRESH-03]
executor: opus (pipeline kernel store.py + workflow contract)

must_haves:
  truths:
    - "A merge_and_write run that adds no id and ages no id out leaves current.json byte-identical: the file is not rewritten, and `generated` is not bumped."
    - "An archive/YYYY-MM.json whose merge adds no new id is not rewritten."
    - "In news-pipeline.yml, a run whose committed data/ diff touches neither data/incidents/current.json nor data/incidents/archive/ may still commit (seen.json, rejected/, pending.json), but it does NOT trigger the Cloudflare deploy hook."
    - "R1/R-09: the deploy decision runs AFTER the commit and push and fails OPEN (deploy=true with a ::warning::), so it can never block data collection (F-84)."
    - "R1/R-04: a human push that touches only data/incidents/current.json, data/incidents/archive/** or data/cead/** deploys through deploy-on-code.yml. Bot commits use GITHUB_TOKEN and [skip ci], so they do not trigger it and there is no loop."
    - "The Health gate step is unchanged: same name, no `if:`, no continue-on-error, same run line, still after the deploy step. G-04/G-09 semantics are untouched, and the heartbeat's news check (G-05, `check-heartbeat.sh news 2`) is untouched."
    - "When the news heartbeat check fails (evidence > 48h), heartbeat.yml triggers ONE Cloudflare rebuild in that run, so the build-time stale notice (FRESH-01) and commune caveat (FRESH-03) reach prod even though FRESH-04 removed the no-op redeploys."
  artifacts:
    - path: .github/scripts/news-deploy-needed.sh
      provides: "prints deploy=true|false from the staged index, public-path allowlist"
    - path: pipeline/tests/test_news_deploy_gate.py
      provides: "temp-git-repo behaviour tests + workflow structure pins"
  key_links:
    - from: .github/workflows/news-pipeline.yml (Commit data if changed)
      to: .github/scripts/news-deploy-needed.sh
      via: "bash .github/scripts/news-deploy-needed.sh HEAD~1 HEAD >> \"$GITHUB_OUTPUT\" || fail-open, after git commit + push"
      pattern: "news-deploy-needed.sh HEAD~1 HEAD"
    - from: .github/workflows/news-pipeline.yml (Trigger Cloudflare Pages deploy)
      to: steps.commit.outputs.deploy
      via: "if:"
      pattern: "steps.commit.outputs.deploy == 'true'"
---

<objective>
FRESH-04: a no-op pipeline run produces a byte-identical current.json and fires no deploy. Deploys fire only when the served incident set changes. Also keep FRESH-01/03 honest on prod: with the no-op redeploys gone, the outage-time rebuild moves to the heartbeat.

Purpose: research measured 11 of 14 recent auto-commits as generated-only rewrites that still deployed (35-RESEARCH § FRESH-04). V-12 also names seen/rejected-only commits as deploy triggers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/35-honest-freshness-signals/35-RESEARCH.md
@pipeline/news/store.py
@pipeline/tests/test_store.py
@.github/workflows/news-pipeline.yml
@.github/workflows/heartbeat.yml
@pipeline/tests/test_news_health_gate.py
@pipeline/tests/test_workflow_guards.py

<interfaces>
Evidence at HEAD 8d1f432:
- store.py:169-171: `cutoff = today − window_days` is date-granular, and `ref_now` is the run clock.
- store.py:176-177: previous_payload is loaded via _load_payload (returns {} if absent or unreadable).
- store.py:119-127: _merge_by_id preserves existing order, and existing wins, so a 0-new, 0-aged run reproduces the identical `incidents` list.
- store.py:207-226: every month in `aged_out` is re-written with a fresh `datetime.now()` generated (:212), even when the merge adds nothing.
- store.py:232-236: `generated` = ref_now, unconditionally. This is the root cause.
- store.py:237-241: the last_new_incident_at bump/carry rule (G-05/G-07(a)). Keep it exactly.
- store.py:253: atomic_write_json always writes. pipeline/shared/atomic_write.py:13-35 has no content dedupe (this resolves research Open Question 2 and Assumption A3: an explicit skip guard is required).
- Callers: scrape_news.py:571 `merge_and_write(deduped, current_path, archive_dir)`. The backfill at backfill_classifier_outage.py:712-723 verifies written ids after the call; it only calls when `accepted` is non-empty, so a skip never hides a needed write there.
- news-pipeline.yml:83-95: the commit step does `git add data/`, then, if `git diff --staged --quiet`, sets changed=false; otherwise it runs commit + push-with-rebase.sh and sets changed=true. :109-111 is the deploy step `if: steps.commit.outputs.changed == 'true'`, with the canonical curl line `curl -X POST --fail --silent --show-error --retry 3 --retry-all-errors --max-time 60 "$CF_HOOK"`. :121-124 is the Health gate, and CF_HOOK is job-level env (:30-31).
- test_news_health_gate.py:408-447 pins the step order run < commit < deploy < gate < label < alert, the gate's no `if`/continue-on-error and its exact run line, and the absence of `${{` in the scrape/gate run bodies. All of these must stay green without editing that file.
- heartbeat.yml:70-75 is the news_hb step, :108-123 the news alert. test_workflow_guards.py pins `id: news_hb`, the literal `if: failure() && steps.news_hb.outcome == 'failure'`, persist-credentials count 1 for heartbeat.yml, and no `${{` in the named new-step run bodies.
- Served data: site/scripts/sync-data.mjs:44-49 copies ALL of data/incidents to public (seen.json and rejected/ included, until Phase 38 HYG-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: store.py no-op guard (byte-identical current.json and archive)</name>
  <files>pipeline/news/store.py, pipeline/tests/test_store.py</files>
  <behavior>
    - no-op: current.json (written with json.dumps compact, deliberately not atomic_write_json's format) holds one fresh incident and last_new_incident_at. merge_and_write([same incident], now=later) leaves read_bytes() before == after, and returns 0.
    - no-op across a cutoff move with nothing crossing (today=D then D+1, incidents dated D−5) gives identical bytes.
    - aging-only: an incident dated today−31 gives a rewritten file with generated == now, the id moved to archive/YYYY-MM.json, and last_new_incident_at carried verbatim.
    - new incident gives generated == now and last_new_incident_at == now (existing G-05 behaviour).
    - absent current.json gives a written file. An unreadable current.json ("{not json") gives a written file.
    - archive month file already containing id X; new_incidents=[X aged] gives archive bytes identical.
    - All existing test_store.py tests pass unedited.
  </behavior>
  <action>
Step 0, the G-19 precondition (R1/R-14). Run `grep -c '^- \[x\] \*\*Phase 34' .planning/ROADMAP.md`. It must print 1. Otherwise STOP without editing anything and report "G-19: Phase 34 not closed".

Execution mode (R1/R-12, G-21): this plan builds nothing under site/, so it may run in wave 1 alongside 35-01, and only alongside 35-01.

In merge_and_write, after building `payload` (store.py:232-241) and after validate_incidents_file succeeds, add the no-op guard. The run is a no-op when ALL of these hold:
- previous_payload is non-empty;
- set(previous_payload) ⊆ {"generated","window_days","incidents","last_new_incident_at"};
- previous_payload.get("incidents") == current_incidents;
- previous_payload.get("window_days") == window_days;
- previous_payload.get("last_new_incident_at") == payload.get("last_new_incident_at").

On a no-op, log at INFO "current.json unchanged (no new or aged ids) — write skipped (FRESH-04)" and return new_in_window_count, which is 0 here, WITHOUT calling atomic_write_json. On any other outcome, write as today with generated = ref_now.

In the archive loop (store.py:207-226), skip the month when archive_path exists and len(merged_archive) == len(existing_archive). Because existing wins in _merge_by_id, equal length means no new id. Also replace the archive `datetime.datetime.now(...)` at :212 with ref_now, which makes it deterministic for tests.

Update the merge_and_write docstring: `generated` now means "the time current.json's content last changed", not "the time the pipeline last ran". The G-05 liveness signal remains last_new_incident_at.

Do not touch lines 237-241 semantics or the bump_last_new parameter. Add the behavior tests to test_store.py.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_store.py pipeline/tests/test_backfill_classifier_outage.py -q -p no:cacheprovider</automated>
    Expected: exit 0, with all old and new tests passing. Negative control: delete the guard's `return`, and the byte-identity tests must FAIL. Restore it.
  </verify>
  <done>A no-op run writes nothing, and the aging and new-incident paths still write. G-05/G-07(a) behaviour is unchanged, as proven by the existing last_new tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deploy only on served incident-set changes (news-pipeline.yml) + human data pushes deploy (deploy-on-code.yml)</name>
  <files>.github/scripts/news-deploy-needed.sh, .github/workflows/news-pipeline.yml, .github/workflows/deploy-on-code.yml, DEPLOYMENT.md, pipeline/tests/test_news_deploy_gate.py</files>
  <behavior>
    - In a temp git repo with a committed baseline (data/incidents/current.json, seen.json, rejected/2026-09.json, archive/2026-08.json), run `news-deploy-needed.sh HEAD~1 HEAD` with cwd = repo after a second commit that changes:
      - seen.json only: stdout "deploy=false", exit 0;
      - rejected/2026-09.json only: "deploy=false";
      - a new pending.json only: "deploy=false";
      - current.json: "deploy=true";
      - a new archive/2026-09.json: "deploy=true";
      - seen.json + current.json: "deploy=true".
    - The script exits non-zero with "::error::" on stderr when cwd is not a git repo, when an argument is missing, or when a ref is unknown.
    - Fail-open, run as a bash harness of the exact run-body snippet: with the script path replaced by a failing command, $GITHUB_OUTPUT (a tmp file) ends with "deploy=true" and stdout contains "::warning::".
    - Workflow pins (yaml.safe_load):
      - in the commit-step run body, `git commit` and `push-with-rebase.sh` both appear BEFORE `news-deploy-needed.sh HEAD~1 HEAD`;
      - the run body contains the fail-open fallback `|| {` with `deploy=true` and `::warning::`;
      - the unchanged branch writes both `changed=false` and `deploy=false`;
      - the deploy step `if` == "steps.commit.outputs.deploy == 'true'";
      - the deploy step run is the canonical curl line, byte-identical;
      - all of test_news_health_gate.py::TestNewsPipelineWorkflowStructure passes unedited.
    - deploy-on-code.yml pins (R-04): on.push.paths contains 'site/**', '.github/workflows/deploy-on-code.yml', 'data/incidents/current.json', 'data/incidents/archive/**' and 'data/cead/**'; branches == ['master']. test_workflow_guards.py's persist-credentials pin (deploy-on-code.yml: 1 × `persist-credentials: false`) stays green unedited.
  </behavior>
  <action>
Create .github/scripts/news-deploy-needed.sh with `set -euo pipefail`. Usage: `news-deploy-needed.sh <from-ref> <to-ref>`, and it exits 1 with a usage ::error:: unless it gets exactly 2 arguments. The public allowlist is `data/incidents/current.json` and `data/incidents/archive/` (knob G-20(6)).

The script:
1. Captures `git diff --quiet <from> <to> -- data/incidents/current.json data/incidents/archive/` and its rc under `set +e`/`set -e`.
2. Prints `deploy=false` on rc 0 and `deploy=true` on rc 1.
3. On rc ≥ 2, prints `::error::news-deploy-needed: git diff failed (rc=N)` to stderr and exits 1.

In news-pipeline.yml "Commit data if changed" (R1/R-09: fail-open, after the commit):
- In the `git diff --staged --quiet` true branch, echo both `changed=false` and `deploy=false`.
- In the else branch, keep `git commit -m "data: auto-update news [skip ci]"`, then `bash .github/scripts/push-with-rebase.sh master`, then `echo "changed=true" >> "$GITHUB_OUTPUT"`, all exactly as today, and only then add:
  `bash .github/scripts/news-deploy-needed.sh HEAD~1 HEAD >> "$GITHUB_OUTPUT" || { echo "deploy=true" >> "$GITHUB_OUTPUT"; echo "::warning::news-deploy-needed failed; deploying (fail-open)"; }`
  After the rebase push, HEAD is the bot commit rebased on origin, so HEAD~1..HEAD is exactly this run's data diff.
- Change ONLY the deploy step's `if:` to `steps.commit.outputs.deploy == 'true'`, with a comment citing FRESH-04/V-12 and R-09.
- Do not rename, reorder or edit any other step (Guard deploy hook, Health gate).

deploy-on-code.yml (R1/R-04): add to on.push.paths `'data/incidents/current.json'`, `'data/incidents/archive/**'` and `'data/cead/**'`, with the comment "FRESH-04/R-04: human data-only pushes (hotfixes like 45c2650, the HYG-06 local CEAD scrape, Phase-36 migrations) deploy here. Bot pushes (news-pipeline, cead-scraper) use GITHUB_TOKEN and carry [skip ci], so they never trigger this workflow: no loop."

DEPLOYMENT.md §10 (Operational Notes, line ~237): add the line "data/ pushes by a human (data/incidents/current.json, data/incidents/archive/**, data/cead/**) deploy via deploy-on-code; news-cron commits deploy only when the incident set changes (FRESH-04)."

Write test_news_deploy_gate.py:
- Use a tmp_path git repo (git init, user.name/email config, a baseline commit, then the change commit).
- Call the script via the bash-invocation helper style of test_workflow_guards.py's run_script.
- For the fail-open test, extract the else-branch fallback line from the parsed YAML and run it under bash with GITHUB_OUTPUT set and the script path pointed at a non-existent file.
- Include the YAML pins, skipped if PyYAML is absent, which is the pattern of test_news_health_gate.py:384-398.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_news_deploy_gate.py pipeline/tests/test_news_health_gate.py pipeline/tests/test_workflow_guards.py -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-secret-hygiene.sh</automated>
    Expected: pytest exit 0; lint-workflows exit 0 with "canary fired correctly"; check-secret-hygiene exit 0.
    Negative controls:
    - Add `data/incidents/seen.json` to the script's pathspec, and the "seen.json only gives deploy=false" test must FAIL. Revert.
    - Move the news-deploy-needed line above `git commit`, and the ordering pin must FAIL. Revert.
    - Remove 'data/cead/**' from deploy-on-code.yml, and the R-04 pin must FAIL. Revert.
  </verify>
  <done>Deploy fires iff the served incident set changed, the decision is fail-open after the commit, and human data-only pushes deploy through deploy-on-code. The Health gate and its pins are untouched, and lint is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Heartbeat-triggered rebuild while news is stale (keeps FRESH-01/03 visible on prod)</name>
  <files>.github/workflows/heartbeat.yml, pipeline/tests/test_news_deploy_gate.py</files>
  <action>
Why: before FRESH-04, no-op runs redeployed every 6 h (cron '0 */6 * * *', news-pipeline.yml:5), so a build-time stale notice would appear within hours of going stale. After Task 2, an outage produces no incident change and therefore no deploy. The static page would keep its last fresh build and never show the FRESH-01 notice or the FRESH-03 caveat, which is exactly the 18-day silent outage of G-18/V-05.

Append one step to heartbeat.yml, AFTER "Alert news heartbeat via GitHub Issue" so the issue is filed first:
- name: "Rebuild site so stale-news notices render (FRESH-01/03)"
- if: `failure() && steps.news_hb.outcome == 'failure'`
- step-level env: `CF_HOOK: ${{ secrets.CF_DEPLOY_HOOK_URL }}` (this uses the existing repo secret and changes no secrets or settings)
- run, as two lines: `bash .github/scripts/require-env.sh CF_HOOK`, then the canonical curl line from news-pipeline.yml:111, byte-identical.

Do not change the news_hb step, its `check-heartbeat.sh news 2` threshold, the permissions block, or the checkout (it keeps persist-credentials: false, so the pinned count stays 1).

This step adds at most 1 deploy per day, and only while evidence is older than 48h. At the 500 builds/month free tier that is a maximum of 31 builds per month, and it is outside the FRESH-04 count, which is defined on news-pipeline runs. The 35-07 measurement reports these deploys separately as H.

Add pytest pins to test_news_deploy_gate.py:
- the step exists, and its index is greater than the index of the news_hb step;
- its `if` equals the literal above;
- env CF_HOOK equals `${{ secrets.CF_DEPLOY_HOOK_URL }}`;
- the run body contains "require-env.sh CF_HOOK" and the canonical curl line, and has no `${{`.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && PYTHONIOENCODING=utf-8 python -m pytest pipeline/tests/test_news_deploy_gate.py pipeline/tests/test_workflow_guards.py -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-secret-hygiene.sh && python -m pytest pipeline -q -p no:cacheprovider</automated>
    Expected: all commands exit 0. The full pytest is ≥ 611 passed / 1 skipped / 2 xfailed (baseline at 8d1f432) + this plan's new tests, with 0 failed. Count by exit code, not by grepping "failed". Negative control: change the step `if:` to `failure()`, and the pin test must FAIL. Revert.
  </verify>
  <done>The heartbeat rebuilds prod once per failing day while news is stale. Every existing heartbeat pin is green. Lint and secret hygiene are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GitHub Actions → Cloudflare deploy hook | a secret URL; possession triggers a prod build |
| pipeline → data/ commit | automated commits to master |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | Information disclosure | CF_HOOK in heartbeat.yml | mitigate | Step-level env from the secret. It is never interpolated into run (zizmor, and the pytest `${{` pin). The canonical curl uses --silent --show-error (no verbose), and check-secret-hygiene.sh must pass. |
| T-35-04 | Denial of service | Deploy quota | mitigate | The heartbeat rebuild fires at most 1/day and only while stale (≤ 31/month of 500). The news deploy now fires only on incident-set change. |
| T-35-05 | Tampering / integrity | store.py skip-write | mitigate | Skip only on deep equality of the incidents, window_days and last_new fields, and only with no unknown keys. Any other difference writes, and validation still gates the write. |
| T-35-06 | Repudiation | Health gate masking | mitigate | The gate step is untouched and pinned by the existing tests. A commit with deploy=false still reaches the gate. |
| T-35-16 | Denial of service | Deploy decision blocking collection (R-09) | mitigate | The script runs after commit+push, with a fail-open fallback (deploy=true + ::warning::), pinned by pytest. |
| T-35-17 | Tampering | Stale prod after a human data hotfix (R-04) | mitigate | deploy-on-code.yml paths include the data/incidents and data/cead paths. Bot pushes cannot loop (GITHUB_TOKEN + [skip ci]). |
| T-35-SC | Tampering | installs | accept | none |

Accepted (premortem, no edit): R-15. The served HTML is not monitored (the heartbeat reads git). That gap predates this phase; it is bounded by the daily aging deploys and the heartbeat rebuild, and it is a Phase-38/v2.3 backlog candidate.
</threat_model>

<verification>
- Full pytest ≥ baseline + new tests, exit 0.
- lint-workflows exit 0.
- check-secret-hygiene exit 0.
- No change to the Health gate step, as proven by `git diff .github/workflows/news-pipeline.yml`, which shows only the commit-step body and the deploy-step `if:` plus comments.
- `git diff .github/workflows/deploy-on-code.yml` shows only the three added paths plus the comment.
</verification>

<success_criteria>
The no-op run is byte-identical and deploys nothing. Deploys fire only on changes to current.json or archive/. The heartbeat rebuilds prod while stale, so the build-time notices can render.
</success_criteria>

<output>
Create `.planning/phases/35-honest-freshness-signals/35-02-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-04 | files_modified (+deploy-on-code.yml, DEPLOYMENT.md); must_haves truth; Task 2 behavior/action/verify (paths + pytest pin + DEPLOYMENT.md §10 line); T-35-17 |
| R-09 | must_haves truth + key_link; Task 2 behavior/action/verify (script `<from> <to>` after commit+push, fail-open fallback deploy=true + ::warning::, ordering pin); T-35-16 |
| R-12 | Task 1 action "Execution mode" |
| R-14 | Task 1 action Step 0 |
| R-15 | threat_model "Accepted" note |
