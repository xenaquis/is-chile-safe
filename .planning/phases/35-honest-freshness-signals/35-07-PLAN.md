---
phase: 35-honest-freshness-signals
plan: 07
type: execute
wave: 6
depends_on: ["35-01", "35-02", "35-03", "35-04", "35-05", "35-06"]
files_modified:
  - .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FRESH-01, FRESH-02, FRESH-03, FRESH-04, FRESH-05]
executor: orchestrator (gate commands, push, deploy polling, gh/git measurement); fresh opus for the served-route verdict

must_haves:
  truths:
    - "The close gate is green in one session, all at the same HEAD: the fixture harness PASS; build + 18/18 validators PASS, including freshness; vitest ≥ baseline + Phase-35 tests; pytest ≥ baseline + Phase-35 tests; astro check 0 errors; lint-workflows exit 0; check-secret-hygiene exit 0."
    - "Prod /news/ and /es/noticias/ serve the data-derived latest-incident stamp, with no Updated/Actualizado stamp. Their stale notice agrees with the shared rule at the page's data-build-now. Their first histogram bar is not earlier than the deployed data's earliest incident date."
    - "Prod commune pages (santiago plus the newest incident's commune, EN and ES) and region pages (antofagasta EN/ES, R1/R-02) pass news-freshness and cead-vintage when those validators are run on the fetched HTML together with the SERVED /data/incidents/current.json (R1/R-10)."
    - "R1/R-01: on prod /news/ and /es/noticias/, every data-day inside a declared COVERAGE_GAPS range with no incident carries data-gap=\"true\"."
    - "Prod /methodology/ and /es/metodologia/ serve the CEAD as-of stamp, the partial-year label and the data-driven '(currently N)'."
    - "FRESH-04 interim: over the first 4 scheduled news-pipeline runs after the push, D (runs whose deploy step succeeded) ≤ C (auto-commits that changed current.json/archive incidents), and G (generated-only commits) = 0. The no-op proof follows G-22(c): up to K = 4 induced dispatches. A live no-op (sha1 unchanged, deploy skipped) proves it; otherwise the interim passes with the recorded residual 'no-op path proven by pytest only' (R1/R-11, R2/F35-R1-07)."
    - "R1/R-13: the FRESH-04 7-day check (due SHIP+7d) is recorded in STATE.md § Deferred-live list AND as a dated GitHub issue whose number is in RUN STATUS."
  artifacts:
    - path: .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
      provides: "RUN STATUS Phase 35 ☑ (or the reason it is held), measured numbers, dated FRESH-04 7-day check"
  key_links:
    - from: https://ischilesafe.com/news/
      to: data/incidents/current.json at the deployed sha
      via: "deploy-on-code (site/** push) → Cloudflare build → sync-data"
      pattern: "Latest incident: "
---

<objective>
Close Phase 35: run the full close gate, push, deploy, verify the served routes, take the FRESH-04 live measurement, and update the tracking docs.

Precondition (G-19): Phase 34 is CLOSED (verification passed, STATE/ROADMAP updated) before ANY Phase 35 plan executes. So live classification is flowing, and freshness is expected green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/35-honest-freshness-signals/35-01-SUMMARY.md
@.planning/phases/35-honest-freshness-signals/35-02-SUMMARY.md
@.planning/phases/35-honest-freshness-signals/35-03-SUMMARY.md
@.planning/phases/35-honest-freshness-signals/35-04-SUMMARY.md
@.planning/phases/35-honest-freshness-signals/35-05-SUMMARY.md
@.planning/phases/35-honest-freshness-signals/35-06-SUMMARY.md

<interfaces>
Baseline:
- The Phase-35 baseline = the Phase-34 close-gate numbers recorded in RUN STATUS.
- For reference, measured by the planner at HEAD 8d1f432 on 2026-09-23: pytest `python -m pytest pipeline -q -p no:cacheprovider` = 611 passed / 1 skipped / 2 xfailed (exit 0); vitest = 97/97 in 10 files; validators 16. If Phase 34's close recorded higher numbers, those are the floor.

Deploy path:
- deploy-on-code.yml fires on push to master touching 'site/**' (deploy-on-code.yml on.push.paths).
- deploy-manual.yml is workflow_dispatch-only. Use it only if deploy-on-code did not run or failed for the pushed sha.
- Cloudflare auto-build is OFF (memory "prod deploy-hook secret gap"), so every prod build comes from a hook.

Measurement hooks:
- The news-pipeline step names are "Commit data if changed" and "Trigger Cloudflare Pages deploy".
- The heartbeat rebuild step is "Rebuild site so stale-news notices render (FRESH-01/03)".
- Commits by the cron carry the message "data: auto-update news [skip ci]" and author github-actions[bot].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close gate (all at one HEAD, freshness included)</name>
  <files>(none modified — measurement only)</files>
  <action>
0. Precondition (R1/R-14): `grep -c '^- \[x\] \*\*Phase 34' .planning/ROADMAP.md` must print 1. If it does not, STOP and report "G-19: Phase 34 not closed". Execution mode (R1/R-12, G-21): this plan runs ALONE (wave 6), after all others.
1. Run `git pull --rebase`, so that local data/incidents/current.json is origin's latest. The cron commits about 4 times a day.
2. Run the gate from the repo root, as ONE chained command for the site part (OneDrive hazard):
   - `cd site && node scripts/validate/news-freshness-fixtures.mjs && npm run build && npm run validate && npx vitest run && npx astro check`
   - then, separately: `PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider`, `bash .github/scripts/lint-workflows.sh` and `bash .github/scripts/check-secret-hygiene.sh`.
   The harness runs first, and `npm run build`'s prebuild re-syncs public/, so the final dist is built from real data. R2/B-R1-06: run the chained gate with run_in_background (tool cap 10 min) and record its wall time.
3. Numeric pass criteria:
   - harness prints PASS;
   - all.mjs prints "18/18 validators passed";
   - vitest has 0 failed and a count ≥ baseline + the tests added by 35-01, 35-03, 35-05 and 35-06;
   - pytest exit 0 with passed ≥ baseline + the tests added by 35-02 (count by exit code; do not grep "failed", which also matches xfailed);
   - astro check "0 errors";
   - lint-workflows exit 0 and check-secret-hygiene exit 0.
4. Freshness-red policy. freshness is a real signal and is never excluded:
   a. Confirm that local matches origin: `git log -1 --format=%cI origin/master -- data/incidents/current.json`.
   b. Measure the evidence with `python pipeline/news_evidence.py data/incidents/current.json`, and compute its age.
   c. If the age is > 48h on origin's data, that is a live news outage, and a Phase-34 regression. Do NOT push, and do NOT close Phase 35. Record it in RUN STATUS.
   d. Re-check after the next scheduled news run (R2/A-R1-07: scheduled runs were measured 3-5h late, e.g. 03:04 → 10:52Z = 7.8h apart; wait for the next completed run, whatever the gap). If it is still red after 24h, open Phase-34 gap planning (`/gsd:plan-phase 34 --gaps`) and keep Phase 35 held.
   e. Never lower MAX_AGE_HOURS, and never drop freshness from all.mjs.
5. Any other red: fix it inside the owning plan's scope (max 2 repair attempts, node_repair_budget=2), and re-run the WHOLE gate.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node scripts/validate/news-freshness-fixtures.mjs && npm run build && npm run validate && npx vitest run && { npx astro check > .astro-check.log 2>&1; rc=$?; tail -3 .astro-check.log; [ $rc -eq 0 ]; } && cd .. && PYTHONIOENCODING=utf-8 python -m pytest pipeline -q -p no:cacheprovider && bash .github/scripts/lint-workflows.sh && bash .github/scripts/check-secret-hygiene.sh</automated>
    Expected: every command exits 0; the output contains "18/18 validators passed" and "0 errors"; the pytest summary shows passed ≥ floor, with 1 skipped and 2 xfailed (or the Phase-34 floor).
  </verify>
  <done>The gate is green at a single HEAD, and the numbers are recorded for RUN STATUS.</done>
</task>

<task type="auto">
  <name>Task 2: Push, deploy, served-route verification (EN/ES)</name>
  <files>(none modified)</files>
  <action>
1. Commit the Phase-35 work if the executors left anything uncommitted. Then run `git pull --rebase && git push` (never --force). Record SHA = `git rev-parse HEAD` and SHIP = `date -u +%FT%TZ`, captured immediately after the successful push (R2/A-R1-02). The measurement window W then excludes runs whose headSha does not descend from SHA (`git merge-base --is-ancestor $SHA <headSha>`).
2. Wait for the deploy:
   - Poll `gh run list --workflow deploy-on-code.yml --limit 5 --json headSha,status,conclusion` until the run for SHA is completed.
   - If it is absent after 10 min, or its conclusion is not success, run `gh workflow run deploy-manual.yml` and poll that workflow the same way.
   - Then poll prod every 60 s, for at most 20 min, until `curl -s -H 'Cache-Control: no-cache' https://ischilesafe.com/news/ | grep -c 'Latest incident: '` = 1.
3. Fetch the served HTML into scratch. Let P = C:/Users/Carlo/AppData/Local/Temp/claude/prod35, which is outside OneDrive and matches the verify command. Fetch:
   - news/index.html, es/noticias/index.html, methodology/index.html, es/metodologia/index.html;
   - commune/santiago/index.html and es/comuna/santiago/index.html;
   - region/antofagasta/index.html and es/region/antofagasta/index.html (R1/R-02);
   - index.html and es/index.html (R2/C-R1-05). Assert data-coverage-gaps is present, and that the .pulse-stale count equals 1 iff evidenceVerdict(computeEvidence(P/current.json), Date.parse(.pulse-card data-build-now), 48) ∈ {'stale','none'}, else 0;
   - map/index.html and es/mapa/index.html, reachability only (200 OK). The NewsStrip is client-rendered and is covered by 35-04 Task 4's vitest;
   - commune/<S>/index.html and es/comuna/<S>/index.html, where S = the slug (via data/cead/meta/index.json) of the commune of the newest incident in the served current.json below;
   - in the SAME batch (R1/R-10): `curl -s -H 'Cache-Control: no-cache' https://ischilesafe.com/data/incidents/current.json -o "$P/current.json"`. This is the exact file the prod build consumed (sync-data copies it), and it is served with max-age=0 and must-revalidate.
   Consistency guard (R-10): latestIncidentDate(P/current.json), computed with `node -e` importing site/src/lib/newsEvidence.mjs, must equal data-latest-incident on BOTH fetched news pages. If it does not, a deploy landed mid-batch: re-fetch the whole batch, at most 3 times, then FAIL.
4. Run the validators on the served HTML:
   - `NEWS_FRESHNESS_DIST=P NEWS_FRESHNESS_CURRENT_JSON=P/current.json node site/scripts/validate/news-freshness.mjs` must exit 0.
   - `CEAD_VINTAGE_DIST=P node site/scripts/validate/cead-vintage.mjs` must exit 0 in subset mode, reporting 2 methodology + 1/1 region + 2/2 commune pages and 0 banned partial labels.
5. Run the explicit greps on P:
   - news EN: 'Latest incident: ' = 1, and '>Updated ' = 0;
   - news ES: 'Último incidente: ' = 1, and '>Actualizado ' = 0;
   - on both news pages, the first `data-day="…"` ≥ min(date) of P/current.json. This is FRESH-02; there must be no pre-coverage bar;
   - on both news pages, every `data-day` in 2026-09-05..2026-09-22 with no incident of that date in P/current.json carries `data-gap="true"` (R1/R-01). This is also asserted by news-freshness in step 4; the grep is the independent cross-check;
   - region antofagasta EN/ES: no 'Jan–Jun' / 'ene–jun', and 'CEAD data as of ' / 'Datos CEAD al ' = 1 (R-02);
   - methodology EN: 'CEAD data as of ' and '(currently 2025)';
   - methodology ES: 'Datos CEAD al ' and '(actualmente 2025)';
   - santiago EN / ES: '2026 is a partial year' / '2026 es un año parcial'.
   Substitute the years if HYG-06 refreshed CEAD, reading them from data/cead/national.json.
6. A fresh opus validator reviews P and the outputs of steps 4 and 5, and signs PASS/FAIL. A FAIL goes back to the owning plan via a gap plan; the phase does not close.
  </action>
  <verify>
    <automated>P="C:/Users/Carlo/AppData/Local/Temp/claude/prod35"; cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && NEWS_FRESHNESS_DIST="$P" NEWS_FRESHNESS_CURRENT_JSON="$P/current.json" node site/scripts/validate/news-freshness.mjs && CEAD_VINTAGE_DIST="$P" node site/scripts/validate/cead-vintage.mjs && grep -c 'Latest incident: ' "$P/news/index.html" && grep -c 'Último incidente: ' "$P/es/noticias/index.html" && ! grep -q '>Updated ' "$P/news/index.html" && ! grep -q '>Actualizado ' "$P/es/noticias/index.html" && ! grep -qE 'Jan–Jun|ene–jun' "$P/region/antofagasta/index.html" "$P/es/region/antofagasta/index.html"</automated>
    Expected: both validators print PASS on the prod HTML; the grep counts are 1 and 1; the negated greps succeed.
  </verify>
  <done>The served routes in both locales satisfy FRESH-01/02/03/05, verified with the same validators on prod HTML and signed by fresh opus.</done>
</task>

<task type="auto">
  <name>Task 3: FRESH-04 live measurement + docs close</name>
  <files>.planning/v2.2-AUTONOMOUS-DIRECTIVE.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Instrument. Define it once and use it for both the interim and the 7-day window, with W = [SHIP, end).
- Runs: `gh run list --workflow news-pipeline.yml --created ">=$SHIP" --limit 100 --json databaseId,event,createdAt,conclusion`. For each run id, get `gh run view <id> --json jobs --jq '.jobs[].steps[] | select(.name=="Trigger Cloudflare Pages deploy") | .conclusion'`.
  - D = the number of runs where that conclusion is "success".
  - N = the number of scheduled runs.
- Commits: `git fetch origin && git log origin/master --since="$SHIP" --author='github-actions' --format=%H -- data/incidents/current.json data/incidents/archive/`. For each sha, run a python check: the incident set changed iff json(sha^:current.json)['incidents'] != json(sha:current.json)['incidents'], OR `git diff --name-only sha^ sha -- data/incidents/archive/` is non-empty.
  - C = the number of changed commits.
  - G = the number of the rest, which are generated-only commits.
- No-op runs (R1/R-11): N_noop = the number of news-pipeline runs in W whose store.py log contains "current.json unchanged (no new or aged ids) — write skipped (FRESH-04)" AND whose commit, if any, does not touch data/incidents/current.json.
- Heartbeat rebuilds: H = the number of heartbeat.yml runs in W whose step "Rebuild site so stale-news notices render (FRESH-01/03)" concluded success. Report H separately; it is not part of the FRESH-04 inequality.
- What discriminates (R2/F35-R1-07, G-22(c)): D ≤ C and G = 0 do NOT discriminate under healthy flow. 116/116 bot commits 2026-08-05..09-04 added ids (min 3), so they hold by construction. Only an observed no-op proves the store guard and the deploy gate live. The deploy gate counts as live-proven ONLY IF an observed no-op run committed seen/rejected (changed=true) AND its "Trigger Cloudflare Pages deploy" step was skipped.

Interim gate (phase close). This is the single no-op rule (G-22(c)), replacing the earlier two-step wording:
- wait for N ≥ 4 scheduled runs after SHIP;
- PASS requires D ≤ C, G = 0, and every news-pipeline run concluded success or has a diagnosed Health-gate reason (G-04/G-09). The Health gate step conclusion must never be "skipped".
- Record R = runs whose "Commit data if changed" ran but D was not incremented. This is informational.
- Induction procedure, K = 4 attempts at most:
  - Each attempt is `gh workflow run news-pipeline.yml` (no inputs), either queued while a scheduled run is in progress (concurrency group news-pipeline, no cancel-in-progress, news-pipeline.yml:13-15) or ≤ 15 min after a scheduled run ends, preferring 06-12 UTC.
  - For each attempt, record the run id, current.json sha1 before and after (git fetch), whether the commit touched current.json, the "Commit data if changed" changed output, and the deploy-step conclusion.
  - Stop at the first no-op, meaning sha1 unchanged and deploy skipped.
- Outcome:
  - If ≥ 1 attempt (or a natural run) is a no-op, "no-op proven live" (and "deploy gate proven live" if it also committed seen/rejected).
  - If none of the K = 4 is a no-op, the interim still PASSES, with the recorded residual "no-op path proven by pytest only (test_store byte-identity + test_news_deploy_gate)", and the SAME induction procedure is carried into the 7-day check.

7-day check (FRESH-04 literal):
- Write into RUN STATUS: "FRESH-04 7-day check due <SHIP+7d, UTC>: rerun the 35-07 Task 3 instrument over [SHIP, SHIP+7d] including the K=4 induction procedure if no no-op was proven live; PASS iff D ≤ C, G = 0, and (N_noop ≥ 1 or the pytest-only residual is re-recorded)".
- R1/R-13:
  - (a) Append to STATE.md § Deferred-live list: "FRESH-04 7-day check due <SHIP+7d UTC> — rerun 35-07 Task 3 instrument incl. the K=4 induction procedure; PASS iff D ≤ C, G = 0, and (N_noop ≥ 1 or pytest-only residual re-recorded)".
  - (b) Run `gh issue create --title "FRESH-04 7-day check due <SHIP+7d UTC>" --label pipeline-failure --body "<the instrument commands above, verbatim, INCLUDING the K=4 induction procedure>"`, and write the issue number into RUN STATUS.
- The orchestrator executes it on that date, whatever phase is active. A FAIL opens `/gsd:plan-phase 35 --gaps`.
- REQUIREMENTS FRESH-04: the checkbox stays `[ ]`, and the traceability row reads "Interim PASS (N runs: D=…, C=…, G=0); 7-day check due <date>". Tick it and mark it Complete only when the 7-day check passes.

Docs:
- REQUIREMENTS: FRESH-01/02/03/05 → Complete, with evidence pointers.
- ROADMAP § Phase 35: set the Plans list to 7 plans and check the box.
- STATE: Current Position → Phase 36.
- Directive RUN STATUS: Phase 35 ☑, the gate numbers, prod checks, D/C/G/H/R, and the validators count 18 as the new baseline. Confirm that the planner's knob G-NNs were appended BEFORE execution; this is not done here.
- Commit with `docs(35): close phase — FRESH-01..05`, then `git pull --rebase && git push`.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && grep -c "FRESH-04 7-day check due" .planning/v2.2-AUTONOMOUS-DIRECTIVE.md && grep -c "FRESH-04 7-day check due" .planning/STATE.md && gh issue list --state open --search "FRESH-04 7-day check due in:title" --json number --jq length && grep -E "^- \[x\] \*\*FRESH-0[1235]\*\*" .planning/REQUIREMENTS.md | wc -l && grep -c "\[x\] \*\*Phase 35" .planning/ROADMAP.md</automated>
    Expected: the counts are 1 (directive), 1 (STATE.md), 1 (open issue), 4 and 1. RUN STATUS contains the issue number and either a recorded live no-op (sha1 before == after, deploy skipped) or the pytest-only residual with all K ≤ 4 attempts listed. The interim numbers in RUN STATUS satisfy D ≤ C and G = 0 (fresh opus checks them against the raw gh/git output pasted into 35-07-SUMMARY.md).
  </verify>
  <done>FRESH-04 is measured live with a pre-declared instrument, the 7-day check is scheduled, and the docs are closed and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local → origin/master → prod | push and deploy of a public site |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-14 | Tampering | Push of ungated code | mitigate | Push only after the Task-1 gate is green at the same HEAD, and after `git pull --rebase`. Never --force (directive hard rules). |
| T-35-15 | Repudiation | Harness-only proof | mitigate | Served-route validation runs the same validators on prod HTML, with a fresh opus verdict. |
| T-35-SC | Tampering | installs | accept | none |

Accepted (premortem, no edit):
- R-16: a freshness-red close holds the push (knob G-20(9), owner policy).
- R-15: the served HTML is not monitored; this is a Phase-38/v2.3 backlog candidate.
</threat_model>

<verification>
Gate green, prod verified in EN/ES, FRESH-04 interim PASS, 7-day check scheduled, docs pushed.
</verification>

<success_criteria>
Phase 35 closes only with freshness green, prod verified, and the FRESH-04 inequality measured live.
</success_criteria>

<output>
Create `.planning/phases/35-honest-freshness-signals/35-07-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-01 | must_haves truth; Task 2 step 5 (data-gap cross-check) |
| R-02 | must_haves truth; Task 2 step 3 (fetch region/antofagasta EN/ES), step 4 (cead-vintage subset: 1/1 region), step 5 (no Jan–Jun), verify |
| R-10 | must_haves truth; Task 2 step 3 (served /data/incidents/current.json in the same batch + latestIncidentDate guard with re-fetch ≤ 3) |
| R-11 | must_haves truth; Task 3 instrument (N_noop, induced no-op via workflow_dispatch within 15 min, sha1 before/after, deploy step skipped; interim requires ≥ 1 no-op); 35-RESEARCH.md FRESH-04 table relabelled "aging-only rewrite (0 added)" |
| R-12 | frontmatter wave 6; Task 1 step 0 "Execution mode" |
| R-13 | must_haves truth; Task 3 Docs (a) STATE.md Deferred-live entry, (b) dated gh issue; verify (STATE grep + open issue count) |
| R-14 | Task 1 step 0 |
| R-15, R-16 | threat_model "Accepted" note |

## Revision R2 (gate R1)

| Item | Applied where |
|---|---|
| Condition 7 (F35-R1-07, G-22(c)) | must_haves truth; Task 3 instrument: N_noop definition, "what discriminates" (116/116), a single no-op rule with K=4 induction, pytest-only residual, live-proof condition for the deploy gate; 7-day check text in RUN STATUS / STATE / issue body carries the same procedure; verify Expected updated |
| Condition 6 (F35-R1-06) twin | Task 2 step 3: /map/ and /es/mapa/ reachability (the strip itself is covered by 35-04 Task 4 vitest). The closeout wording is unchanged, because option (a) keeps "every recency signal" true |
| A-R1-02 (non-blocker) | Task 2 step 1: SHIP = `date -u` right after a successful push; W filtered by `git merge-base --is-ancestor $SHA <headSha>` |
| A-R1-07 (non-blocker) | Task 1 step 4d: measured cadence wording replaces "at most 6h" |
| B-R1-06 (non-blocker) | Task 1 step 2: run_in_background + record the gate wall time |
| C-R1-05 (non-blocker) | Task 2 step 3: home EN/ES fetched; .pulse-stale count vs the verdict at .pulse-card data-build-now |
| B-R1-05 (non-blocker) | verify: astro check rc captured |

Deferred non-blocker: A-R1-06 (JS/Python G-05 parity on non-canonical dates; a round-trip in latestIncidentDate plus a shared fixture H). This is more than a one-line change: it touches newsEvidence.mjs, news_evidence.py and both test suites. feeds.py writes canonical dates, so there is no live exposure. Candidate for a Phase-38 hygiene item.
