---
phase: 35
artifact: premortem
plans_evaluated: 35-01..35-07 @ b3f1f84
method: 3 opus perspectives + fresh opus arbiter
risks: 18
---

# Phase 35 — Premortem

## Arbiter summary

Arbitraje del premortem de la Fase 35 sobre b3f1f84. Leí los 7 planes, la directiva (G-05, G-18, G-19, G-20), REQUIREMENTS, ROADMAP y la sección V-05 de la auditoría. Contra el código real verifiqué HomeNewsPulse, las páginas de región, ResultPanel, CrimeFamilyCharts, data.ts, CommuneNewsSection, feeds.py, news-pipeline, heartbeat, deploy-on-code, cead-scraper y config.json. Contra prod hice GET a /region/antofagasta/, /commune/santiago/ y /data/incidents/current.json, y medí git: origin tiene 742 incidentes del 08-24 al 09-04, sin last_new, y hay 3 reescrituras diarias que solo envejecen (0 agregados). El núcleo es sólido: helper G-05 único, guard de store.py, gate por allowlist y harness frozen/fresh.

Quedan 4 riesgos P1 a mitigar, todos del tipo 'el sitio sigue mintiendo':
- R-01: las 18 barras del outage (09-05..09-22) quedan como ceros internos después del recorte, hasta el 10-05.
- R-02: las 16 regiones siguen diciendo '2025: partial year (Jan–Jun)', y el panel del mapa '2025 (partial data)'. Eso contradice el nuevo sello CEAD e inventa el corte mensual que V-13 refutó.
- R-03: el pulso de noticias de la home conserva el mismo relleno, los ceros del outage y no tiene aviso. Ningún plan lo toca.
- R-04: FRESH-04 elimina el redeploy implícito que publicaba pushes humanos de solo datos (hotfix 45c2650, CEAD del 10-01). Se arregla ampliando los paths de deploy-on-code; los commits del bot llevan [skip ci] y usan GITHUB_TOKEN, así que no hay loop.

Los P2 a mitigar son ediciones baratas:
- Copy CEAD que niega el uso de 2026 en 'tendencias' mientras el gráfico lo dibuja.
- Copy del caveat de comuna que afirma algo sobre la geolocalización.
- 'future' tratado como stale.
- El validador #18 sin acotar a la sección cnews.
- El script de deploy corriendo antes del commit (hay que hacerlo fail-open).
- P/current.json tomado de git en vez de prod.
- FRESH-04 interim vacuo (hay que inducir un no-op).
- Ejecución paralela sobre un árbol compartido en OneDrive (serializar).
- El chequeo de 7 días sin recordatorio.
- G-19 sin precheck ejecutable.

Se aceptan con razón M-04 (monitoreo del HTML servido: hueco previo, acotado por los deploys diarios de envejecimiento y el heartbeat), M-06 (knob G-20(9)), M-09 (convención UTC) y T-04 (regla G-05 fijada; la fecha mostrada es verídica). Se descarta T-05. No hay P0.

## Final risks

| id | sev | decision | origin | scenario | plan edit / acceptance |
|---|---|---|---|---|---|
| R-01 | P1 | MITIGAR | M-01 + T-01 (dedup) | Una vez que la clasificación en vivo vuelve a agregar incidentes con fecha ≥09-23, current.json queda con los días 08-24..09-04 más los ≥09-23. El recorte de 35-01 solo corta el borde izquierdo, así que las 18 barras 09-05..09-22 quedan DENTRO del tramo y se dibujan como '· 0' hasta que el 09-04 salga de la ventana (≥10-05). Es exactamente el 'false zero coverage' de V-05: esos días tuvieron noticias que se perdieron en el outage (2.073 ítems), y G-18 retuvo el backfill. No hay aviso de desactualizado porque la evidencia está fresca, y el chequeo de prod de 35-07 paso 5 ('primer data-day ≥ min(date)') igual da PASS. | 35-01 Task 2: agregar un site/src/lib/newsCoverageGaps.ts puro que exporte COVERAGE_GAPS = [{from:'2026-09-05', to:'2026-09-22', source:'G-18 classifier outage'}], con comentario de origen (G-18, 34-BACKFILL-AUDIT.md). computeDayBuckets(dates, anchor, width, gaps = COVERAGE_GAPS) agrega `gap: boolean` a cada bucket (true iff la fecha cae en un gap declarado Y count == 0). Tests: la forma de prod más fechas 09-23..09-25 da gap=true en 09-05..09-22, y un incidente real dentro del gap da gap=false para ese día. 35-04 Task 1: news.astro y es/noticias.astro renderizan los buckets gap con data-gap="true" y title/aria 'no data collected' / 'sin datos recolectados' en lugar de '· 0', con estilo rayado y sin regla display. Si algún bucket gap cae en la ventana, agregar un caption estático bajo el histograma (todo sustituido server-side, sin {date} literal por facets.mjs). EN: 'No incidents were collected from September 5 to September 22, 2026 because of a classification outage in our pipeline; those days are not zero-incident days.' ES: 'No se recolectaron incidentes entre el 5 y el 22 de septiembre de 2026 por una falla de clasificación en nuestro pipeline; esos días no son días sin incidentes.' 35-07 Task 2 paso 5: agregar la aserción 'todo data-day dentro de un gap declarado tiene data-gap="true"'. Registrar el knob como G-21 antes de ejecutar. |
| R-02 | P1 | MITIGAR | I-01 | Después de la Fase 35, las páginas de metodología y de comuna dirán '2026 es un año parcial; el último año completo es 2025'. Mientras tanto, las 16 páginas de región EN/ES siguen diciendo '* 2025: partial year data (Jan–Jun)' bajo 'Annual evolution (2005–2025*)', y el panel del mapa dice 'The incident rate uses 2025 (partial data)'. El sitio queda con vintages CEAD contradictorios, y uno de ellos inventa el corte mensual que V-13 refutó y G-20(10) prohíbe. El validador cead-vintage no cubre regiones, así que queda en verde. | 35-03: sumar a files_modified site/src/pages/region/[slug].astro, site/src/pages/es/region/[slug].astro y site/src/components/map/ResultPanel.tsx. En Task 2, en las dos páginas de región: quitar el '*' del heading y reemplazar el footnote '* {latestCompleteYear}: partial year data (Jan–Jun)' / 'año parcial (ene–jun)' por el mismo párrafo .cead-vintage (v = ceadVintage(region.series, region.last_updated ?? loadNational().last_updated)). En ResultPanel.tsx:289-290 reemplazar '2025 (partial data)' / '2025 (datos parciales)' por 'the latest complete CEAD year (2025)' / 'el último año completo de CEAD (2025)'. 35-05 Task 2: cead-vintage.mjs también recorre dist/region/<slug> y dist/es/region/<slug> (conteo esperado 16/16, FAIL si es 0 o EN≠ES), y agrega un grep negativo que falle si aparece 'Jan–Jun\|ene–jun\|(partial data)\|(datos parciales)' en dist/**/*.html o en site/src/components/map/ResultPanel.tsx. 35-07 paso 5: sumar region/antofagasta EN/ES al fetch de prod. |
| R-03 | P1 | MITIGAR | M-02 + I-02 (dedup) | La home, que es la superficie de más tráfico, sigue mostrando 'News pulse — last 30 days' con un histograma armado en el cliente desde anchor−29. Tiene el mismo relleno previo a la cobertura que FRESH-02 corrige en /news/ (hoy son 18 barras cero), los mismos ceros internos del outage (R-01) y ningún aviso de desactualizado. Ningún plan ni validador toca HomeNewsPulse, y la meta de la fase dice 'every date and recency signal a visitor or Google sees'. | 35-04: agregar un Task 3 con HomeNewsPulse.astro y homeV2Strings.ts en files_modified. (a) En render(), iniciar `days` en max(shiftDays(anchor,-29), fecha mínima válida de `all`), igual que computeDayBuckets. (b) Marcar los días de COVERAGE_GAPS con count 0 como gap, igual que R-01. Pasar la lista al cliente por un atributo data-coverage-gaps con JSON en el .pulse-card, nunca con {expr} dentro de <script>. (c) Calcular en build, con newsEvidence.mjs sobre site/public/data/incidents/current.json, el veredicto G-05. Si es 'stale', renderizar en el HTML estático, dentro de la tarjeta, un <p class="pulse-stale"> con la misma copy que news_stale_notice, sin regla display. 35-06 Task 2: en el harness, el fixture frozen debe dar 1 'pulse-stale' en dist/index.html y en dist/es/index.html, y el fresh debe dar 0. Si se decide dejar la home fuera, registrar la exclusión como G-21 y quitar 'every recency signal' del closeout. |
| R-04 | P1 | MITIGAR | T-02 + I-04 (dedup) | Con 35-02, un push humano que solo toca data/ deja de publicarse solo. Pasa con un hotfix de contenido dañino como 45c2650 (una afirmación falsa de parentesco sobre una persona real), con las migraciones o el backfill de la Fase 36 y con el scrape CEAD local del 10-01 (HYG-06). Antes, la siguiente corrida del cron siempre redesplegaba porque cambiaba `generated`. Ahora deploy=false si no cambia el set de incidentes, y deploy-on-code solo escucha site/**. El contenido corregido sigue viejo en prod hasta el próximo cambio de incidentes: ≤6 h con noticias fluyendo, ~24 h durante un outage. | 35-02 Task 2: sumar .github/workflows/deploy-on-code.yml a files_modified y agregar a on.push.paths 'data/incidents/current.json', 'data/incidents/archive/**' y 'data/cead/**', con un comentario (FRESH-04: los pushes humanos que solo tocan datos se publican solos; los commits del bot llevan [skip ci] y GITHUB_TOKEN, así que no disparan). Agregar en test_news_deploy_gate.py un pin yaml.safe_load de que esos tres paths están presentes, y que el pin persist-credentials=1 de test_workflow_guards.py siga verde. Agregar una línea en DEPLOYMENT.md §10: 'data/ pushes by a human deploy via deploy-on-code'. |
| R-05 | P2 | MITIGAR | M-03 | La nueva frase CEAD dice que 2026 'no se usa para la tasa principal, los rankings ni las tendencias', pero en esa misma página de comuna CrimeFamilyCharts traza la polilínea de evolución por categoría hasta el punto 2026, atenuado pero conectado. Con 2026 en una fracción del año, la línea muestra una caída abrupta. El texto afirma algo que el gráfico de al lado contradice, y el validador cead-vintage solo verifica que el texto esté presente. | 35-03 Task 1, cambiar cead_partial_cutoff. EN: '{year} is a partial year: its figures include only what CEAD had published by {date}. It is not used for the headline rate or rankings, and appears as a faded point in the trend charts. The latest complete year is {complete}.' ES: '{year} es un año parcial: sus cifras incluyen solo lo que el CEAD había publicado al {date}. No se usa para la tasa principal ni los rankings, y aparece como un punto atenuado en los gráficos de evolución. El último año completo es {complete}.' 35-05 Task 1: agregar un caso negativo que dé error si el texto visible del nodo contiene 'or trends' / 'ni las tendencias'. |
| R-06 | P2 | MITIGAR | M-08 | El caveat de comuna afirma que 'nuestro pipeline no ha geolocalizado incidentes de prensa más nuevos en {name} en los últimos 7 días'. La regla, en cambio, mide la fecha de publicación del ítem más nuevo MOSTRADO, después del filtro de términos prohibidos. Si hay un ítem más nuevo filtrado, o si el pipeline aceptó ayer una nota con pubDate de hace 8 días (cola pending, Google News), la frase es falsa. Eso choca con la regla editorial de atribuir sin sobreafirmar. | 35-04 Task 2, cambiar commune_news_stale_note. EN: 'The newest report listed for {name} was published on {date}; none of the listed reports was published in the last 7 days.' ES: 'La nota más reciente de la lista para {name} se publicó el {date}; ninguna de las notas listadas se publicó en los últimos 7 días.' No mencionar geolocalización ni actividad del pipeline. Actualizar los literales de 35-06 si alguno depende de la nota. |
| R-07 | P2 | MITIGAR | T-07 | CommuneNewsSection y el aviso de /news/ tratan el veredicto 'future' como stale (`!== 'fresh'`). parse_pub_date no tiene clamp contra fechas futuras. Un feed con la zona horaria mal etiquetada puede dejar un ítem con fecha de mañana UTC. Si el build corre a última hora UTC, date+1d supera now+24h y esa comuna muestra '(none in the last 7 days)' justo sobre su ítem más nuevo. El validador #18 comparte la regla y no lo detecta. | 35-04 Task 2: `stale = items.length > 0 && evidenceVerdict(...) === 'stale'`. Task 1: `showStale = verdict === 'stale' \|\| verdict === 'none'`. La evidencia futura ya la marcan en rojo freshness.mjs y el heartbeat, y no debe convertirse en un aviso falso de '>48h' para el visitante. 35-06 Task 1: la lib usa la misma regla y se agrega un caso de test con el ítem más nuevo fechado mañana UTC y build-now a las 23:30Z, que debe dar la sección no-stale. |
| R-08 | P2 | MITIGAR | T-03 | 35-03 inserta `<time datetime="2026-06-16">` (vintage CEAD) antes de CommuneNewsSection, y la página ya tiene más de 6 `<h2 class="section-heading" data-astro-cid-…>` antes de la sección de noticias. Si checkCommuneSection toma el primer <time> o el primer h2 de toda la página, juzga toda comuna como stale por 06-16. Eso da FAIL en comunas frescas (fixture fresh y all.mjs en vivo) y quema el presupuesto de reparación (2), o da PASS por la razón equivocada con el fixture frozen. Los tests unitarios usan HTML sintético sin esos nodos. | 35-06 Task 1 action: checkCommuneSection primero extrae el substring desde `<section class="cnews-section"` hasta su `</section>`, y busca solo ahí el h2, data-news-stale y el primer `<time datetime=`. Debe tolerar atributos data-astro-cid-* y cualquier orden de atributos. Agregar al behavior un fixture con `<p class="cead-vintage"><time datetime="2026-06-16">` y dos h2.section-heading antes de la sección fresca: debe dar []. |
| R-09 | P2 | MITIGAR | T-06 | 35-02 corre `bash .github/scripts/news-deploy-needed.sh >> "$GITHUB_OUTPUT"` ANTES de `git commit`, dentro de un step con bash -e. Si el script sale con 1 (git diff con rc≥2, script renombrado, error de sintaxis), el step aborta antes del commit y del push. Se pierden seen/rejected/current de esa corrida. Es el acoplamiento deploy→recolección que F-84 eliminó. | 35-02 Task 2: en el else del commit step, correr `git commit` y push-with-rebase PRIMERO y después `bash .github/scripts/news-deploy-needed.sh HEAD~1 HEAD >> "$GITHUB_OUTPUT" \|\| { echo "deploy=true" >> "$GITHUB_OUTPUT"; echo "::warning::news-deploy-needed failed; deploying (fail-open)"; }`. El script compara con `git diff --quiet <a> <b> -- data/incidents/current.json data/incidents/archive/`. Otra opción es mantener --cached con el mismo `\|\| fail-open`. Pin pytest: en el run body, 'git commit' aparece antes que 'news-deploy-needed.sh', y existe el fallback '\|\| {' con deploy=true. |
| R-10 | P2 | MITIGAR | T-08 + M-07 + I-05 (dedup) | La verificación de prod de 35-07 reconstruye P/current.json eligiendo 'el commit de origin cuyo max date coincide con data-latest-incident'. Varios commits del día comparten max(date) y difieren en last_new_incident_at, que es lo que decide el aviso. news-freshness sobre el HTML de prod puede dar FAIL espurio (bloquea el cierre y abre un gap plan inútil) o PASS falso cerca del umbral de 48h. | 35-07 Task 2 paso 3: reemplazar 'pick the origin commit whose max date equals it' por `curl -s -H 'Cache-Control: no-cache' https://ischilesafe.com/data/incidents/current.json -o "$P/current.json"`, en el mismo lote que los HTML. Antes de correr los validadores, afirmar que latestIncidentDate(P/current.json) == data-latest-incident de ambas páginas de noticias. Si no coincide, re-fetch de todo el lote, porque hubo un deploy intermedio. |
| R-11 | P2 | MITIGAR | M-05 + T-08 (parte interim) | El 'Interim PASS' de FRESH-04 puede quedar vacuo. D≤C se cumple por construcción del gate, y G=0 solo prueba el guard de store.py si hubo corridas no-op. Con noticias fluyendo después de la Fase 34, casi todas las corridas agregan ids, y la primera del día envejece ítems, así que el camino no-op nunca se ejercita en vivo. Además, el research etiqueta mal las reescrituras diarias de solo envejecimiento como 'backfill classify write'. | 35-07 Task 3: agregar al instrumento N_noop = corridas cuyo commit no toca data/incidents/current.json. Para el interim, inducir un no-op: tras una corrida scheduled exitosa, `gh workflow run news-pipeline.yml` dentro de los 15 minutos siguientes. Registrar el sha1 de current.json antes y después, si hubo commit de current.json y la conclusión del step 'Trigger Cloudflare Pages deploy' (esperado skipped). El interim PASS exige además un no-op observado (inducido o natural) con sha1 idéntico y deploy skipped. Si la corrida inducida agrega ids, repetir una vez; si tampoco sale no-op, dejarlo registrado y exigirlo en el chequeo de 7 días. Corregir en 35-RESEARCH la etiqueta de esos commits a 'aging-only rewrite (0 added)'. |
| R-12 | P2 | MITIGAR | I-03 | Con parallelization=true y use_worktrees=false, las waves corren en paralelo sobre el mismo árbol en OneDrive. En la wave 2, 35-04 y 35-05 ejecutan npm run build a la vez: el prebuild sync-data hace rmSync de public/data mientras el otro build lee, y 35-05 muta dist/commune/santiago con sed y mv mientras 35-04 regenera dist. En la wave 1, 35-03 buildea mientras 35-01 comenta la línea del clip como control negativo. También hay choques de index.lock en los commits. Resultado: fallas espurias que consumen node_repair_budget=2, o evidencia de PASS registrada sobre un dist mezclado. El gate final de 35-07 es secuencial y protege el resultado final, pero no el presupuesto ni los SUMMARY. | Registrar G-21 en la directiva: 'Phase 35 executes sequentially (one plan at a time) because plans share site/public/data and site/dist'. En frontmatter: 35-03 depends_on ["35-01"] (wave 2), 35-04 depends_on ["35-01","35-03"] (wave 3), 35-05 depends_on ["35-04"] (wave 4), 35-06 wave 5, 35-07 wave 6. 35-02 puede seguir en wave 1 junto a 35-01, porque no buildea el sitio. En 35-05 Task 2, el control de mutación opera sobre una copia en os.tmpdir() usando CEAD_VINTAGE_DIST, no sobre site/dist. |
| R-13 | P2 | MITIGAR | I-07 | El chequeo literal de 7 días de FRESH-04 depende de que 'el orquestador lo ejecute en SHIP+7d, esté la fase que esté', y no hay cron, issue fechado ni entrada en el Deferred-live list. Si la corrida autónoma se pausa (HYG-06, deferred-live, etc.), al 10-10 FRESH-04 sigue en [ ] o alguien lo tilda sin la medición. | 35-07 Task 3, en Docs: (a) agregar una entrada en STATE.md § Deferred-live list: 'FRESH-04 7-day check due <SHIP+7d UTC> — rerun 35-07 Task 3 instrument; PASS iff D ≤ C, G = 0, N_noop ≥ 1'. (b) `gh issue create --title "FRESH-04 7-day check due <fecha>" --label pipeline-failure` con el instrumento pegado en el body. Verify: `grep -c "FRESH-04 7-day check due" .planning/STATE.md` = 1 y el número de issue anotado en RUN STATUS. |
| R-14 | P2 | MITIGAR | I-06 | La precondición G-19 ('ningún código de la 35 se ejecuta ni commitea hasta cerrar la 34') solo aparece en prosa en 35-07. 35-01..35-06 son autonomous:true y no la repiten. Con branching 'none', si alguien lanza execute-phase 35 durante la espera de la 34, el push de cierre de 34-06 sube commits de la 35 que no pasaron el close gate de la 35. | Como primer paso del action de la primera task de 35-01, 35-02 y 35-03, agregar: `grep -c '^- \[x\] \*\*Phase 34' .planning/ROADMAP.md` debe imprimir 1; si no, abortar sin editar, con el mensaje 'G-19: Phase 34 not closed'. |
| R-15 | P2 | ACEPTAR | M-04 | Nada vigila el HTML servido. Si el hook se acepta pero el build de Cloudflare falla, prod queda en un build viejo sin aviso, y el heartbeat pasa porque mira git. Además, con la fase vigente, el aviso de 48h puede llegar a prod con hasta ~24h de retraso. | Sin edición de plan. Razón: es un hueco de monitoreo previo a la fase, no algo que la fase introduce. Un build de CF fallido se sana con el siguiente deploy por cambio de incidentes (≤6h con noticias fluyendo, ~24h por envejecimiento durante un outage), y el retraso del aviso queda acotado a ≤24h por esos mismos deploys diarios. Una sonda del HTML servido en heartbeat (data-latest-incident servido vs git) es un feature nuevo: anotarla como candidata de backlog en la Fase 38 o v2.3. |
| R-16 | P2 | ACEPTAR | M-06 | Si al cierre freshness está en rojo (el outage vuelve), 35-07 prohíbe el push, así que la funcionalidad que declara el outage no llega a prod justo cuando se necesita. | Sin edición. Razón: G-20(9) es un knob aceptado explícitamente por el orquestador. La probabilidad es baja, porque la precondición G-19 exige la 34 cerrada con corridas en vivo sanas, y un rojo en el cierre es una regresión de la Fase 34 que tiene su propia ruta de gaps (paso 4d). Si el dueño prefiere separar el envío del cierre, es una decisión de política para él, no un defecto del plan. |
| R-17 | P2 | ACEPTAR | M-09 | Las fechas de incidente son fechas UTC de publicación. Para un lector en Chile, lo publicado después de las 21:00 CLT aparece con la fecha de mañana, y los umbrales de 48h/168h del fallback pueden disparar hasta ~24h tarde. | Sin edición. Razón: es la convención UTC que ya usan todo el sitio y el gemelo Python del heartbeat. Nunca produce un aviso falso positivo, y cambiarla solo en la vista desalinearía la página con freshness.mjs y news_evidence.py. Si se quiere corregir, corresponde hacerlo en el pipeline (America/Santiago) en otra fase. |
| R-18 | P2 | ACEPTAR | T-04 | Si una corrida solo agrega ids con pubDate viejo, last_new_incident_at se renueva y /news/ muestra 'Latest incident: <hace 5 días>' sin aviso, lo que se aparta del literal de FRESH-01 ('newest incident >48h'). | Sin edición. Razón: G-05/G-20(2) fijan deliberadamente la evidencia 'se agregaron incidentes nuevos'. La copy del aviso ('No new incidents have been added…') es coherente con esa regla, y la línea 'Latest incident: <fecha>' sigue mostrando la fecha real, así que el visitante no recibe información falsa. El escenario exige ≥48h en que solo lleguen ítems viejos, poco probable con la clasificación restaurada. |

## Discarded

| id | reason |
|---|---|
| T-05 | Según la medición de la perspectiva I, un build dura ~20-35 s, así que el gate completo (2 builds del harness + build + 18 validadores + vitest + astro check) cabe cómodo en los 10 min del tool. Aunque el proceso muriera, el prebuild de cualquier `npm run build` vuelve a sincronizar public/data. Cloudflare construye desde git (public/data está en .gitignore), así que no hay fuga a prod. El riesgo que queda (un `astro preview` suelto sobre datos de fixture) es cosmético y local. |

## Evidence

- **R-01**: LEÍDA: 35-01-PLAN.md:22 ('a zero day inside the covered span is still emitted') y :118-120 (inicio = fecha válida más temprana). v2.2-AUDIT-260922.md V-05 ('false zero coverage'). Directiva:114 G-18 (backfill retenido). MEDIDO: origin current.json = 742 incidentes, 2026-08-24..2026-09-04, sin last_new_incident_at. Commits de envejecimiento e4774b0/6576275/c754a15 = 0 agregados y 57/78/68 removidos, así que el mínimo avanza 1 día por día y el 09-04 recién sale el 10-05.
- **R-02**: GET prod /region/antofagasta/: 'Annual evolution (2005–2025*)' + 'partial year data (Jan–Jun)'. LEÍDA: site/src/pages/region/[slug].astro:207-209 y es/region/[slug].astro:201-203 (footnote hardcodeado sobre latestCompleteYear, que viene de series no parciales = 2025). site/src/components/map/ResultPanel.tsx:289-290 ('2025 (partial data)' / '2025 (datos parciales)'). data/cead/national.json: 2025 partial=False, 2026 partial=True, last_updated 2026-06-16. 35-03 files_modified y 35-05 no incluyen region ni ResultPanel.
- **R-03**: LEÍDA: site/src/components/home/HomeNewsPulse.astro:91-94 (`for d = shiftDays(anchor,-29) .. anchor`), :161-175 (fetch de /data/incidents/current.json en el cliente, título + 'last {n} days'). site/src/config/homeV2Strings.ts:50 'last {n} days' y :57 'press incidents · 30 days'. Incluido en index.astro:92 y es/index.astro:101. Ningún files_modified de 35-01..35-07 contiene HomeNewsPulse. ROADMAP § Phase 35 Goal.
- **R-04**: LEÍDA: deploy-on-code.yml:27-32 (paths 'site/**' y el propio workflow). `git show --stat 45c2650`: solo data/incidents/current.json y .planning. MEDIDO: 45c2650 (humano, 00:03Z 09-23) salió a prod con el cron e4774b0 (03:05Z), que agregó 0 ids y era solo envejecimiento. news-pipeline.yml:83-95 y cead-scraper.yml:98: los bots commitean con '[skip ci]' usando GITHUB_TOKEN persistido, y ninguna de las dos cosas dispara workflows de push, así que ampliar los paths no crea loops.
- **R-05**: LEÍDA: 35-03-PLAN.md Task 1 (texto cead_partial_cutoff). site/src/components/CrimeFamilyCharts.astro:36 (sortedSeries = todas las series) y :64-68 (years/values/polyline incluyen los años partial). data.ts:194-196 y :394-397 (la tasa y los rankings sí excluyen partial). Prod santiago: h2 'Crime evolution by category (2005–present)'.
- **R-06**: LEÍDA: 35-04-PLAN.md Task 2 (texto commune_news_stale_note). CommuneNewsSection.astro:108-110 (safe = filtro de términos prohibidos; items = safe.slice(0,6)). pipeline/news/feeds.py:204-217 (date = pubDate UTC). Directiva G-03 (pending hasta 14 días).
- **R-07**: LEÍDA: pipeline/news/feeds.py:204-217 (sin clamp; fallback date.today()). 35-04-PLAN.md Task 1 (`showStale = verdict !== 'fresh'`) y Task 2 (`!== 'fresh'`). 35-01-PLAN.md:87 ('future' cuando evidence.ms > now+24h).
- **R-08**: GET prod /commune/santiago/: 7 h2.section-heading antes de 'Recent Incidents in the News'; los <time> actuales están solo dentro de la sección cnews, que lleva data-astro-cid. LEÍDA: 35-03-PLAN.md interfaces (el vintage va en la línea 337, antes de la línea 389). 35-06-PLAN.md Task 1 behaviors (fixtures sin <time> CEAD).
- **R-09**: LEÍDA: news-pipeline.yml:83-95 (commit step sin continue-on-error) y :97-99 (principio F-84: 'a rotated hook cannot also stop data collection'). 35-02-PLAN.md Task 2 (script antes del commit; rc≥2 → exit 1).
- **R-10**: MEDIDO: los commits f5761ea, 3a0d94e y e4774b0 del 09-23 tienen el mismo max date 2026-09-04. GET https://ischilesafe.com/data/incidents/current.json → 200, Cache-Control 'public, max-age=0, must-revalidate', cf-cache-status DYNAMIC. sync-data.mjs copia a public exactamente el archivo del build. LEÍDA: 35-07-PLAN.md Task 2 paso 3.
- **R-11**: MEDIDO: e4774b0/6576275/c754a15 = 0 agregados y 57/78/68 removidos. LEÍDA: 35-07-PLAN.md Task 3 ('R ≥ 0 is informational'). 35-RESEARCH.md tabla FRESH-04 (~línea 246).
- **R-12**: LEÍDA: .planning/config.json:4 "parallelization": true, :46 "use_worktrees": false, :23 node_repair_budget 2. Frontmatter: 35-04 wave 2 y 35-05 wave 2 corren npm run build, y 35-05 hace sed -i + mv sobre dist. 35-01/35-02/35-03 wave 1.
- **R-13**: LEÍDA: 35-07-PLAN.md Task 3 ('The orchestrator executes it on that date, whatever phase is active'); files_modified solo incluye .planning/*. STATE.md:462 tiene un '§ Deferred-live list', que es el mecanismo usado por el proyecto (memoria MEMORY.md v2.1).
- **R-14**: LEÍDA: grep 'G-19\|Phase 34 is CLOSED' en 35-01..35-06 = 0 coincidencias. ROADMAP.md:119 '- [ ] **Phase 34:' (aún abierta). config.json branching_strategy 'none'. Directiva:115 G-19.
- **R-15**: LEÍDA: heartbeat.yml:70-75 (news_hb evalúa el checkout, sin GET a prod). news-pipeline.yml:109-111 (curl --fail solo prueba que el hook se aceptó). MEDIDO: durante un outage igual hay un commit de envejecimiento diario que cambia current.json y despliega, más el rebuild del heartbeat (35-02 T3).
- **R-16**: LEÍDA: 35-07-PLAN.md Task 1 paso 4c. Directiva G-20(9) (knob aceptado: 'Freshness red at close holds the phase'). G-19: la 35 solo se ejecuta con la 34 cerrada y la clasificación restaurada.
- **R-17**: LEÍDA: pipeline/news/feeds.py:204-217 ('publication date as a UTC date'). 35-04 formatea con timeZone 'UTC', igual que las tarjetas actuales (CommuneNewsSection.astro:118-122).
- **R-18**: LEÍDA: REQUIREMENTS.md:30 (literal FRESH-01). Directiva G-05 y G-20(2) (el aviso usa la regla G-05, last_new primero). store.py (last_new se renueva ante cualquier id nuevo).
