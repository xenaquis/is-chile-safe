---
phase: 35-honest-freshness-signals
plan: 06
type: execute
wave: 5
depends_on: ["35-04", "35-05"]
files_modified:
  - site/scripts/validate/news-freshness.lib.mjs
  - site/scripts/validate/news-freshness.mjs
  - site/scripts/validate/news-freshness.test.ts
  - site/scripts/validate/news-freshness-fixtures.mjs
  - site/scripts/validate/fixtures/news-current-frozen-2026-09-04.json
  - site/scripts/validate/all.mjs
autonomous: true
requirements: [FRESH-01, FRESH-02, FRESH-03]
executor: sonnet

must_haves:
  truths:
    - "Validator #18 news-freshness.mjs FAILS when /news/ or /es/noticias/ has a latest-incident date ≠ max(date) of the current.json the build consumed, when either page still shows 'Updated'/'Actualizado', or when a stale notice is present or absent against the shared helper's verdict at the page's own data-build-now."
    - "It also FAILS when any commune news section's heading or data-news-stale disagrees with the 168h rule (verdict === 'stale'; R-07) on its newest listed date, or when EN/ES section counts differ. The commune check parses ONLY the substring of <section class=\"cnews-section\" … </section> (R1/R-08), so the CEAD <time> and the other h2.section-heading nodes on the page are ignored."
    - "R1/R-01: it FAILS when a news-page data-day bar inside a declared COVERAGE_GAPS range, with no incident of that date in the payload, lacks data-gap=\"true\", or when a bar outside the gaps carries it."
    - "R1/R-07: the news-page stale expectation is verdict 'stale' or 'none'; 'future' expects no notice."
    - "With the committed Sep-4-frozen fixture, the fixture harness builds the site and finds the stale notice PRESENT on both news pages, the caveated heading on the fixture communes, and exactly 1 .pulse-stale on each home page (EN+ES, R1/R-03)."
    - "With a fresh fixture generated at run time, the harness finds the notice ABSENT on both news pages, the normal heading on the fixture communes, and 0 .pulse-stale on each home page."
  artifacts:
    - path: site/scripts/validate/fixtures/news-current-frozen-2026-09-04.json
      provides: "40 newest incidents of git f5761ea:data/incidents/current.json; generated 2026-09-23T03:37:54.191098Z; window_days 30; NO last_new_incident_at; max date 2026-09-04"
    - path: site/scripts/validate/news-freshness-fixtures.mjs
      provides: "two real astro builds (frozen / fresh) into OS-temp outDirs with absolute assertions; restores public/ via sync-data"
  key_links:
    - from: site/scripts/validate/news-freshness.lib.mjs
      to: site/src/lib/newsEvidence.mjs
      via: "import (the single G-05 definition)"
      pattern: "newsEvidence.mjs"
---

<objective>
The FRESH-01/FRESH-03 validators, including the literal "Sep-4-frozen fixture" assertion (present with frozen data, absent with fresh data) that REQUIREMENTS FRESH-01 and audit Phase-35 SC-1 demand.

Two layers:
- (a) validator #18 runs on every build in all.mjs. It checks that the served HTML is consistent with the data the build consumed, using the one shared rule. It is data-agnostic, so it passes whether the live data is fresh or stale.
- (b) news-freshness-fixtures.mjs builds the site twice, from a frozen and a fresh fixture, and asserts absolute outcomes. Because its expectations are hard-coded, it is independent of the helper. It runs at the close gate (35-07), not inside all.mjs, because it costs two full builds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-honest-freshness-signals/35-04-SUMMARY.md
@.planning/phases/35-honest-freshness-signals/35-05-SUMMARY.md
@site/scripts/validate/all.mjs
@site/scripts/sync-data.mjs

<interfaces>
- Markup from 35-04:
  - news pages: `<p class="freshness" data-latest-incident="YYYY-MM-DD|''" data-build-now="ISO">`, with `<time datetime=…>Latest incident: <en-US long></time>` / 'Último incidente: <es-CL long>', and `<p class="news-stale-notice" role="status" data-stale-hours="48">` iff stale.
  - commune: `<section class="cnews-section" data-news-stale="true|false" data-build-now="ISO" …>`, `<h2 class="section-heading">`, which is 'Recent Incidents in the News' / 'Incidentes Recientes en la Prensa' (fresh) or 'Incidents in the News (none in the last 7 days)' / 'Incidentes en la Prensa (ninguno en los últimos 7 días)' (stale). The list items carry `<time datetime="YYYY-MM-DD">`, newest first.
- Shared helper (35-01): site/src/lib/newsEvidence.mjs exports latestIncidentDate, computeEvidence, evidenceVerdict, MAX_AGE_HOURS=48, COMMUNE_NEWS_STALE_HOURS=168. Gap list: site/src/lib/newsCoverageGaps.ts COVERAGE_GAPS. It is TS, so the .mjs validator cannot import it. The lib therefore keeps a literal copy with a comment naming the source file, and a vitest test asserts the copy deep-equals the TS export (the vitest side can import both).
- Commune pages hold 7 h2.section-heading nodes before the news section (prod /commune/santiago/, premortem R-08), and after 35-03 a `<p class="cead-vintage"><time datetime="2026-06-16">` before it. Astro adds data-astro-cid-* attributes, and attribute order is not guaranteed.
- Home pages: dist/index.html and dist/es/index.html carry `<p class="pulse-stale"` iff the build verdict is 'stale' or 'none' (35-04 Task 3).
- Build input: news.astro and CommuneNewsSection read site/public/data/incidents/current.json via process.cwd() (news.astro:45, CommuneNewsSection.astro:38). sync-data.mjs:44-49 re-copies data/incidents to public/, and it is the restore step. site/.gitignore:3 ignores public/data/, so overwriting the public copy never touches git or data/.
- `astro build --outDir <dir>` is supported (verified with `npx astro build --help`, astro 7.1.6). The astro bin is at site/node_modules/astro/bin/astro.mjs. Spawn it with process.execPath, not npx, for Windows safety.
- Frozen source: git f5761ea (2026-09-23 03:37Z) data/incidents/current.json = 742 incidents, dates 2026-08-24..2026-09-04, generated 2026-09-23T03:37:54.191098Z, no last_new_incident_at (measured).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: news-freshness.lib.mjs + runner (#18) + frozen fixture file</name>
  <files>site/scripts/validate/news-freshness.lib.mjs, site/scripts/validate/news-freshness.test.ts, site/scripts/validate/news-freshness.mjs, site/scripts/validate/fixtures/news-current-frozen-2026-09-04.json, site/scripts/validate/all.mjs</files>
  <behavior>
    - checkNewsPage(html, frozenPayload, 'en'), for HTML with data-build-now 2026-09-23T12:00:00.000Z, latest 2026-09-04, text 'Latest incident: September 4, 2026' and one news-stale-notice, returns [].
    - The same HTML without the notice returns an error containing 'stale notice expected'.
    - Fresh payload (last_new_incident_at = build-now − 1h) with the notice present returns 'stale notice not expected'.
    - Wrong data-latest-incident returns an error. An '>Updated ' text node returns an error. The ES variant with 'Último incidente: 4 de septiembre de 2026' returns [].
    - checkCommuneSection: build-now 2026-09-23T12:00Z, newest item time 2026-09-04, data-news-stale="true" and the stale heading gives []. data-news-stale="false" gives an error. Newest 2026-09-20 with "false" and the normal heading gives []. The ES headings are handled. HTML without a cnews-section gives [].
    - R-08: a page with `<p class="cead-vintage" data-astro-cid-x><time datetime="2026-06-16">` and two `<h2 class="section-heading" data-astro-cid-y>Other</h2>` BEFORE a fresh cnews section (newest 2026-09-20, "false", normal heading; attributes in a different order, with data-astro-cid-*) gives [].
    - R-07: newest item dated tomorrow UTC (2026-09-24) with build-now 2026-09-23T23:30:00Z expects a NON-stale section (data-news-stale="false", normal heading), which gives []. A news page with payload last_new_incident_at 25h in the future expects NO notice.
    - R-01: a news page whose payload has dates 08-24..09-04 and 09-23..09-25 and whose bars 09-05..09-22 carry data-gap="true" gives []. Dropping data-gap from the 09-10 bar gives an error naming 2026-09-10. The payload plus a real 09-10 incident with data-gap on 09-10 gives an error.
    - The COVERAGE_GAPS literal copy in the lib deep-equals the TS export.
  </behavior>
  <action>
Step 0, precondition (R1/R-14): `grep -c '^- \[x\] \*\*Phase 34' .planning/ROADMAP.md` must print 1. If it does not, STOP without editing and report "G-19: Phase 34 not closed". Execution mode (R1/R-12, G-21): run this plan ALONE (wave 5, after 35-05). It builds site/.

Create fixtures/news-current-frozen-2026-09-04.json from `git show f5761ea:data/incidents/current.json`:
- keep `generated` and `window_days` verbatim;
- keep the 40 newest incidents, sorted by date desc then id asc, with fields verbatim;
- ensure the ABSENCE of last_new_incident_at.
Write it with a one-off python or node snippet (not committed as a script), formatted with indent 2 and UTF-8. Assert with python afterwards: max date == '2026-09-04', 40 incidents, and the key is absent.

Create news-freshness.lib.mjs:
- It imports latestIncidentDate, computeEvidence, evidenceVerdict, MAX_AGE_HOURS and COMMUNE_NEWS_STALE_HOURS from '../../src/lib/newsEvidence.mjs'. This is the single rule. Independence comes from the harness in Task 2.
- It exports checkNewsPage(html, payload, locale) and checkCommuneSection(html, locale), per the behavior list, using regex parsing like the other validators.
  - Stale expectations must mirror 35-04 exactly (R-07): news notice iff verdict ∈ {'stale','none'}; commune stale iff verdict === 'stale'.
  - checkCommuneSection first extracts the substring from `<section class="cnews-section"` to its matching `</section>` (R-08). Match the class via a regex that tolerates any attribute order and data-astro-cid-*. It searches ONLY inside that substring for the h2, data-news-stale, data-build-now and the first `<time datetime=`.
  - checkNewsPage also runs the R-01 gap check: for each `data-day="D"` bar, expected gap = D is in a COVERAGE_GAPS range AND no payload incident has date D; compare that to the presence of data-gap="true" on the same button.
- The commune stale note (35-04, R-06 wording) is not asserted literally; only the headings are.
- Heading literals are duplicated here with a comment naming the i18n keys (commune_news_heading, commune_news_heading_stale). This is the same precedent as the FORBIDDEN_TERMS copy in CommuneNewsSection.astro:76-88.

Create news-freshness.mjs:
- payload = the file at env NEWS_FRESHNESS_CURRENT_JSON, else site/public/data/incidents/current.json (the exact file the build consumed). A missing or unparseable file gives payload {}.
- dist = env NEWS_FRESHNESS_DIST, else site/dist.
- Check dist/news/index.html ('en') and dist/es/noticias/index.html ('es'). Then check every dist/commune/*/index.html ('en') and dist/es/comuna/*/index.html ('es').
- FAIL if the EN and ES counts of cnews-section differ, or if payload.incidents is non-empty and the EN section count is 0.
- Print `PASS news-freshness: news stale=<true|false> (evidence <label|none>, build-now <iso>); communes EN <n> sections / <s> caveated, ES <n>/<s>`, or the errors, and exit 1.

Register it in all.mjs as #18 after 'cead-vintage.mjs', and update the header count to 18.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run scripts/validate/news-freshness.test.ts && python -c "import json;d=json.load(open('scripts/validate/fixtures/news-current-frozen-2026-09-04.json',encoding='utf-8'));assert len(d['incidents'])==40 and max(i['date'] for i in d['incidents'])=='2026-09-04' and 'last_new_incident_at' not in d and d['generated']=='2026-09-23T03:37:54.191098Z';print('fixture OK')" && npm run build && node scripts/validate/news-freshness.mjs && npm run validate 2>&1 | tail -24</automated>
    Expected:
    - vitest all pass, and the negative cases return errors.
    - The python check prints "fixture OK".
    - news-freshness prints PASS against the real build, whatever the live data state.
    - The all.mjs summary shows 18 validators. freshness may be FAIL only from the live data state (35-07 policy); every other validator is PASS.
    This is ONE chained command (OneDrive).
  </verify>
  <done>Validator #18 is registered and green on the real build. The frozen fixture is committed-ready.</done>
</task>

<task type="auto">
  <name>Task 2: news-freshness-fixtures.mjs — frozen vs fresh real-build harness</name>
  <files>site/scripts/validate/news-freshness-fixtures.mjs</files>
  <action>
Create the harness. It is NOT registered in all.mjs because it costs two full builds, and it is run by 35-07's close gate. Steps:
1. Run `node scripts/sync-data.mjs` via spawnSync(process.execPath). Exit 1 on a non-zero result.
2. Build the fixtures in memory.
   - FROZEN is the committed file.
   - FRESH is a deep copy of FROZEN, adjusted as follows:
     - shift every incident date by k = (today UTC − 2026-09-04) days, using UTC string arithmetic as in newsDayFacets addDays, so that max date == today UTC;
     - set `generated` to now ISO;
     - set `last_new_incident_at` to (now − 1h) ISO with Z.
3. For each fixture in order [frozen, fresh]:
   - write it over site/public/data/incidents/current.json;
   - spawnSync(process.execPath, [node_modules/astro/bin/astro.mjs, 'build', '--outDir', <os.tmpdir()>/ics-fresh35-<name>]) with env {...process.env, ROLLOUT_ALL:'true'}, cwd site, stdio inherit, timeout 900000 ms. Use OS temp, never OneDrive, per the memory "OneDrive build artifacts desync".
4. Assert absolute outcomes, where fixture communes = the slugs, via data/cead/meta/index.json, of the distinct cuts in the fixture:
   - frozen:
     - EN and ES home pages (dist/index.html, dist/es/index.html) each have exactly 1 `class="pulse-stale"` (R1/R-03);
     - EN and ES news pages each have exactly 1 `class="news-stale-notice"`;
     - both have data-latest-incident="2026-09-04";
     - EN text contains 'Latest incident: September 4, 2026' and ES contains 'Último incidente: 4 de septiembre de 2026';
     - for fixture communes, ≥ 1 EN and ≥ 1 ES page, and EVERY such page, has data-news-stale="true" and the stale heading.
   - fresh:
     - both home pages have 0 `class="pulse-stale"` (R1/R-03);
     - both news pages have 0 news-stale-notice;
     - data-latest-incident = today UTC;
     - every fixture-commune page EN/ES has data-news-stale="false" and the normal heading.
   - For both fixtures, spawn news-freshness.mjs with NEWS_FRESHNESS_DIST=<outDir> and NEWS_FRESHNESS_CURRENT_JSON=<fixture temp file>, and it must exit 0.
5. In `finally`: rerun sync-data.mjs to restore the public copy, and rm the temp outDirs.
6. Print `PASS news-freshness-fixtures: frozen → notice EN/ES present, <n> communes caveated; fresh → notice absent, <n> communes normal`, or list failures and exit 1.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && time node scripts/validate/news-freshness-fixtures.mjs && git -C .. status --porcelain site/public data | wc -l</automated>
    Expected: PASS line with both fixtures satisfied, and the git status count 0 (the public copy is ignored and restored, and data/ is untouched). Record the harness wall time in the SUMMARY.

    Negative control (mandatory, record it in the SUMMARY): temporarily change the stale condition in news.astro to `showStale = false` and rerun the harness. It must exit 1, naming the frozen EN notice. Revert. Temporarily force `showStale = false` in HomeNewsPulse.astro, rerun, and it must exit 1 naming the frozen EN home pulse. Revert. Temporarily change COMMUNE_NEWS_STALE_HOURS to 100000, rerun, and it must exit 1 on the frozen communes. Revert.
  </verify>
  <done>The Sep-4-frozen fixture proves the notice and caveat are present, and a fresh fixture proves they are absent, through real builds in both locales, with mutations shown to fail. The working tree is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| harness → site/public/data (gitignored) | temporary overwrite of the build input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-12 | Tampering | A fixture leaking into a real deploy build | mitigate | The harness restores via sync-data in `finally`. Every real build (`npm run build`) re-syncs in prebuild (package.json prebuild), and Cloudflare builds from git, where public/data is ignored. The fixture never touches data/, and verify checks git status for 0 changes. |
| T-35-13 | Repudiation | Tautological validator | mitigate | #18 uses the shared rule for consistency. The harness asserts hard-coded absolute outcomes, and mutation controls run in verify. |
| T-35-SC | Tampering | installs | accept | none |
</threat_model>

<verification>
- vitest green.
- The harness prints PASS, and both negative controls fail as expected.
- all.mjs shows 18 validators with every non-freshness validator PASS.
</verification>

<success_criteria>
FRESH-01's "validator asserts it with the Sep-4-frozen fixture (present with frozen data, absent with fresh data)" and FRESH-03's "both locales" are proven through real builds.
</success_criteria>

<output>
Create `.planning/phases/35-honest-freshness-signals/35-06-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-01 | requirements (+FRESH-02); must_haves truth; interfaces (COVERAGE_GAPS copy + parity test); Task 1 behavior + action (per-bar gap check) |
| R-03 | must_haves truths; interfaces (home pages); Task 2 frozen/fresh .pulse-stale assertions + home-pulse negative control |
| R-06 | Task 1 action: the note is not asserted literally; only headings (unchanged) are |
| R-07 | must_haves truths; Task 1 behavior (tomorrow-UTC case, future-evidence case) + action (mirror 35-04 mapping) |
| R-08 | must_haves truth; interfaces (7 h2 + CEAD <time> before the section); Task 1 behavior (cead-vintage + two h2 fixture) + action (section-substring scoping, attribute-order and data-astro-cid tolerant) |
| R-12 | frontmatter wave 5; Task 1 "Execution mode" |
| R-14 | Task 1 action Step 0 |
