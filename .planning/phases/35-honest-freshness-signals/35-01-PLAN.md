---
phase: 35-honest-freshness-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/lib/newsEvidence.mjs
  - site/src/lib/newsEvidence.d.mts
  - site/src/lib/newsEvidence.test.ts
  - site/scripts/validate/freshness.mjs
  - site/src/lib/newsDayFacets.ts
  - site/src/lib/newsDayFacets.test.ts
autonomous: true
requirements: [FRESH-01, FRESH-02, FRESH-03]
executor: sonnet

must_haves:
  truths:
    - "The G-05 evidence rule (last_new_incident_at, else max(date)+1d; 48h; 24h future refusal) is defined ONCE in JS, in site/src/lib/newsEvidence.mjs. freshness.mjs imports it and no longer contains its own computeEvidence or its own 48/24 constants."
    - "freshness.test.ts passes UNCHANGED after the refactor (behaviour-preserving proof), and TestNewsHeartbeatG05 in pytest still passes (Python twin untouched)."
    - "computeDayBuckets never emits a bucket dated before the earliest valid incident date in its input (FRESH-02); a zero day inside the covered span is still emitted."
  artifacts:
    - path: site/src/lib/newsEvidence.mjs
      provides: "latestIncidentDate, computeEvidence, evidenceVerdict, MAX_AGE_HOURS=48, FUTURE_ALLOWANCE_HOURS=24, COMMUNE_NEWS_STALE_HOURS=168"
    - path: site/src/lib/newsEvidence.d.mts
      provides: "strict TS types for the .mjs helper (astro check 0 errors)"
    - path: site/src/lib/newsDayFacets.ts
      provides: "coverage-clipped computeDayBuckets"
  key_links:
    - from: site/scripts/validate/freshness.mjs
      to: site/src/lib/newsEvidence.mjs
      via: "ESM import"
      pattern: "from '../../src/lib/newsEvidence.mjs'"
---

<objective>
Create the one shared freshness-evidence helper that every Phase-35 staleness decision uses (FRESH-01 notice, FRESH-03 caveat, freshness.mjs validator), and clip the day histogram to the data's coverage (FRESH-02).

Purpose: the directive's G-05 rule already exists twice (freshness.mjs:87-109 in JS, pipeline/news_evidence.py in Python for the stdlib-only heartbeat). Phase 35 adds page consumers, which must not add a third copy. Output: newsEvidence.mjs (+ types + tests), refactored freshness.mjs, clipped newsDayFacets.ts (+ updated tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/35-honest-freshness-signals/35-RESEARCH.md
@site/scripts/validate/freshness.mjs
@site/scripts/validate/freshness.test.ts
@site/src/lib/newsDayFacets.ts
@site/src/lib/newsDayFacets.test.ts

<interfaces>
Evidence at HEAD (read 2026-09-23, HEAD 8d1f432):
- freshness.mjs:37-38 `MAX_AGE_HOURS = 48`, `FUTURE_ALLOWANCE_HOURS = 24`; :87-109 `computeEvidence(payload)` returns `{ ms, source: 'last_new_incident_at' | 'max(date)+1d', label }` or null; fallback label = `new Date(maxMs + 86400000).toISOString().replace(/\.\d{3}Z$/, 'Z')`; :126-132 future refusal `evidence.ms > nowMs + 24h`; :137-151 FAIL iff `ageHours > 48` (exactly 48h passes). Env injections FRESHNESS_CURRENT_JSON / FRESHNESS_NOW (:35, :45-55) and every console message must stay byte-identical because freshness.test.ts asserts on stderr substrings ('FAIL freshness', 'max(date)+1d').
- pipeline/news_evidence.py:30-65 is the Python twin (stdlib only, heartbeat.yml:74). It is NOT modified in Phase 35. Parity is already pinned by the shared fixtures A-G in freshness.test.ts and pipeline/tests/test_workflow_guards.py::TestNewsHeartbeatG05 (lines ~715-790).
- newsDayFacets.ts:33 `DAY_WINDOW_WIDTH = 30`; :63-95 `computeDayBuckets(dates, anchorDate, width = 30)` emits the dense window `[anchor-(width-1), anchor]`. The callers are news.astro:190 and es/noticias.astro:188 (`computeDayBuckets(incidents.map(i => i.date), facets.anchorDate)`). Measured defect (research FRESH-02; prod /news/ 2026-09-23): anchor 2026-09-04, earliest incident 2026-08-24, so 18 of 30 bars (08-06..08-23) have no data behind them.
- site/tsconfig.json extends astro/tsconfigs/strict (allowJs true, no checkJs). A sibling `newsEvidence.d.mts` is what TS resolves for `newsEvidence.mjs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared evidence helper newsEvidence.mjs + freshness.mjs refactor</name>
  <files>site/src/lib/newsEvidence.mjs, site/src/lib/newsEvidence.d.mts, site/src/lib/newsEvidence.test.ts, site/scripts/validate/freshness.mjs</files>
  <behavior>
    - latestIncidentDate({incidents:[{date:'2026-09-03'},{date:'2026-09-04'},{date:'bad'},{}]}) === '2026-09-04'; no valid dates, or incidents not an array, gives null.
    - computeEvidence({last_new_incident_at:'2026-05-29T12:00:00Z', incidents:[]}) gives source 'last_new_incident_at' and label === that string. An unparseable or blank last_new_incident_at falls through to the date fallback.
    - computeEvidence({incidents:[{date:'2026-05-29'}]}) gives source 'max(date)+1d', label '2026-05-30T00:00:00Z'. {incidents:[]} gives null.
    - evidenceVerdict(null, now) === 'none'; as-of 2026-06-01T12:00:00Z: last_new −47h is 'fresh', −48h exactly is 'fresh', −48h−1s is 'stale', +25h is 'future', +23h is 'fresh'.
    - evidenceVerdict(ev, now, COMMUNE_NEWS_STALE_HOURS) for newest item dated 2026-09-15 (evidence 2026-09-16T00:00:00Z): now 2026-09-23T00:00:00Z (168h) gives 'fresh'; now 2026-09-23T00:00:01Z gives 'stale'.
    - Sep-4-frozen payload (generated 2026-09-23T03:37:54.191098Z, no last_new_incident_at, max date 2026-09-04) at now 2026-09-23T12:00:00Z is 'stale'. The same payload with last_new_incident_at 2026-09-23T11:00:00Z is 'fresh'.
  </behavior>
  <action>
Create site/src/lib/newsEvidence.mjs: pure ESM with no fs and no process. Export these constants:
- MAX_AGE_HOURS = 48 (source: G-05 and REQUIREMENTS FRESH-01).
- FUTURE_ALLOWANCE_HOURS = 24 (source: F-100, freshness.mjs:38).
- COMMUNE_NEWS_STALE_HOURS = 168 (source: REQUIREMENTS FRESH-03 "7 days" = 7×24).

Export these functions:
- latestIncidentDate(payload): the max of incidents[].date. Count a date only if it matches /^\d{4}-\d{2}-\d{2}$/ and Date.parse(d+'T00:00:00Z') is not NaN. Use string compare, which is safe on this canonical format.
- computeEvidence(payload): the semantics of freshness.mjs:87-109, moved verbatim. The fallback branch must call latestIncidentDate, so there is only one max loop.
- evidenceVerdict(evidence, nowMs, maxAgeHours = MAX_AGE_HOURS): returns 'none' when evidence is null. Returns 'future' when evidence.ms > nowMs + FUTURE_ALLOWANCE_HOURS·3.6e6. Returns 'stale' when (nowMs − evidence.ms)/3.6e6 > maxAgeHours. Otherwise returns 'fresh'.

Put a JSDoc header in the file that names G-05, G-07(a) and the Python twin pipeline/news_evidence.py. The header must say that any rule change must update both, plus the shared fixtures A-G.

Create newsEvidence.d.mts with exact types: Evidence {ms:number; source:'last_new_incident_at'|'max(date)+1d'; label:string}, Verdict union, and the payload typed as `{ last_new_incident_at?: unknown; incidents?: unknown }`.

Refactor freshness.mjs:
- Import computeEvidence, evidenceVerdict, MAX_AGE_HOURS and FUTURE_ALLOWANCE_HOURS from '../../src/lib/newsEvidence.mjs'.
- Delete the local computeEvidence and the two local constants.
- Keep the env injection, the exit codes and EVERY console message string byte-identical. Branch on the verdict: 'none' prints the existing no-evidence message, 'future' prints the existing future message, and 'stale' prints the existing age message, whose ageHours is still computed for the text.

Write newsEvidence.test.ts covering every behavior bullet. RED first: the test file must fail before the helper exists.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run src/lib/newsEvidence.test.ts scripts/validate/freshness.test.ts && grep -c "function computeEvidence" scripts/validate/freshness.mjs; grep -v '^\s*//' scripts/validate/freshness.mjs | grep -v '^\s*\*' | grep -c "newsEvidence.mjs"</automated>
    Expected: both test files pass with 0 failures, and freshness.test.ts is not edited (`git diff --quiet -- scripts/validate/freshness.test.ts` exits 0). The first grep prints 0. The second grep prints 1. Negative control: temporarily change MAX_AGE_HOURS to 72 in newsEvidence.mjs, and the "48h+1s is stale" test and freshness.test.ts Fixture B must then FAIL. Revert.
  </verify>
  <done>One JS definition of the G-05 rule. freshness.mjs is a thin consumer, and its 12 existing fixture tests are green unmodified. New helper tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FRESH-02 clip the day histogram to the data's coverage</name>
  <files>site/src/lib/newsDayFacets.ts, site/src/lib/newsDayFacets.test.ts</files>
  <behavior>
    - dates 2026-08-24..2026-09-04 (the prod shape), anchor 2026-09-04, default width gives exactly 12 buckets, first '2026-08-24', last '2026-09-04'; no bucket earlier than 2026-08-24.
    - dates ['2026-08-07','2026-08-03'], anchor '2026-08-07', width 5 gives dates 08-03..08-07 and counts [1,0,0,0,1]. An internal zero day is still drawn.
    - dates spanning more than 30 days (['2026-07-01','2026-08-07']), anchor '2026-08-07' gives 30 buckets from '2026-07-09' (the width cap still applies).
    - dates [] with anchor '2026-03-02', width 4 is unchanged (4 buckets, no clip without data). A null anchor still gives []. A malformed anchor still gives [].
    - Malformed entries in dates ('bad', '') never become the coverage start.
  </behavior>
  <action>
In computeDayBuckets (newsDayFacets.ts:63-95), after computing `lower`, compute `earliest`. This is the min over dates matching /^\d{4}-\d{2}-\d{2}$/ with a parseable Date. If earliest is not null and earliest > lower, use earliest as the window start. Keep the signature unchanged, so the news.astro:190 and es/noticias.astro:188 call sites need no edit.

Knob decided here: coverage start = the earliest valid incident date in current.json. This is NOT `generated − window_days`, because FRESH-04 (35-02) changes what `generated` means. The earliest date is always inside store.py's retention window (store.py:195 `inc_date >= cutoff`), so no bar can be drawn outside coverage.

Update the module header contract. Add the sentence: "the window starts at max(anchor−(width−1), earliest incident date) — days before the data's own start are not coverage (V-05, FRESH-02)".

Update the existing tests that encoded the old padding:
- newsDayFacets.test.ts:9-14 ("emits one dense bucket…"): supply ['2026-07-09','2026-08-07'] so that 30 buckets remain correct.
- newsDayFacets.test.ts:16-26: use ['2026-08-07','2026-08-03'] and counts [1,0,0,0,1].

Add the new behavior tests listed above. The peakBucket tests must still pass as written.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run src/lib/newsDayFacets.test.ts</automated>
    Expected: all tests pass, including the new "12 buckets from 2026-08-24" case. Negative control: comment out the clip line, and that case must FAIL with 30 buckets. Restore it.
  </verify>
  <done>No histogram bucket precedes the earliest incident date. Dense internal zeros are preserved. Full `npx vitest run` is ≥ 97 + new tests, with 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| data/incidents/current.json → build | pipeline-written JSON parsed at build and in validators |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | newsEvidence.mjs date parsing | mitigate | Regex plus Date.parse guard. Malformed dates are ignored, never thrown on, and never become the coverage start or the latest date. |
| T-35-02 | Repudiation | Drift between the JS and Python evidence rules | mitigate | One JS definition. The Python twin stays pinned by the shared A-G fixtures in both test suites. |
| T-35-SC | Tampering | npm/pip installs | accept | No new dependencies in this plan. |
</threat_model>

<verification>
Run from site/: `npx vitest run` exits 0 with ≥ 97 + new tests. `npx astro check` reports 0 errors, confirming the .d.mts types are resolved.
</verification>

<success_criteria>
The G-05 rule has a single JS definition that is consumed by freshness.mjs, with its fixtures unchanged and green. The histogram is clipped to coverage, backed by vitest.
</success_criteria>

<output>
Create `.planning/phases/35-honest-freshness-signals/35-01-SUMMARY.md` when done.
</output>
