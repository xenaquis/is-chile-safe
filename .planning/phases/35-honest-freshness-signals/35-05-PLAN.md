---
phase: 35-honest-freshness-signals
plan: 05
type: execute
wave: 4
depends_on: ["35-03", "35-04"]
files_modified:
  - site/scripts/validate/cead-vintage.lib.mjs
  - site/scripts/validate/cead-vintage.mjs
  - site/scripts/validate/cead-vintage.test.ts
  - site/scripts/validate/all.mjs
autonomous: true
requirements: [FRESH-05]
executor: sonnet

must_haves:
  truths:
    - "Validator #17 cead-vintage.mjs FAILS when any of these pages lacks exactly one .cead-vintage node whose data-* values and visible text match the CEAD JSON: /methodology/, /es/metodologia/, or any built commune page EN/ES."
    - "R1/R-02: it also checks all 16 region pages per locale (dist/region/<slug>, dist/es/region/<slug>; FAIL if a count is 0 or EN≠ES). It FAILS if 'Jan–Jun', 'ene–jun', '(partial data)' or '(datos parciales)' appears in any dist HTML or in site/src/components/map/ResultPanel.tsx."
    - "R1/R-05: it FAILS if a .cead-vintage node's visible text contains 'or trends' / 'ni las tendencias'."
    - "It also FAILS if the methodology '(currently N)' / '(actualmente N)' year ≠ the latest complete year in national.json, or if it checked 0 commune pages."
    - "Its expected values are computed from raw data JSON independently of site/src/lib/ceadVintage.ts (facets.mjs precedent: validators do not import the code they check)."
  artifacts:
    - path: site/scripts/validate/cead-vintage.lib.mjs
      provides: "pure checkCeadVintagePage(html, expected, locale) → string[] errors"
    - path: site/scripts/validate/cead-vintage.mjs
      provides: "dist scanner; exit 1 on any error"
  key_links:
    - from: site/scripts/validate/all.mjs
      to: cead-vintage.mjs
      via: "VALIDATORS array"
      pattern: "'cead-vintage.mjs'"
---

<objective>
FRESH-05 validator: assert that the CEAD vintage and the partial-year label are present and correct on every methodology and commune page, in both locales.

Purpose: REQUIREMENTS FRESH-05 requires "with a validator". Research also recommends cross-checking the narrative year so that a CEAD refresh cannot silently invalidate the prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-honest-freshness-signals/35-03-SUMMARY.md
@site/scripts/validate/all.mjs
@site/scripts/validate/figure-registry.lib.mjs

<interfaces>
- all.mjs:1-31 is the header listing the 16 validators; :39-56 is the VALIDATORS array. Validators spawn with ROLLOUT_ALL=true (:74). Each validator is a standalone node script and exits non-zero on failure.
- Precedent for lib + test split: figure-registry.lib.mjs is imported by figure-registry.mjs and tested by figure-registry.test.ts (a validator with side effects at load is never imported by vitest; freshness.test.ts:3-7).
- Region pages (R-02): dist/region/<slug>/index.html and dist/es/region/<slug>/index.html; the region JSON is data/cead/regions/<n>.json (fields id, name, slug, series, last_updated). There are 16 files.
- Markup produced by 35-03: `<p class="cead-vintage" data-cead-last-updated="YYYY-MM-DD" data-partial-year="YYYY|''" data-latest-complete-year="YYYY">`, with visible text 'CEAD data as of <en-US long>' / 'Datos CEAD al <es-CL long>', plus when partial '<Y> is a partial year' / '<Y> es un año parcial', plus 'The latest complete year is <C>.' / 'El último año completo es <C>.'. The R-05 wording contains 'appears as a faded point in the trend charts' / 'aparece como un punto atenuado en los gráficos de evolución'.
- Data: data/cead/national.json {series, last_updated}; data/cead/comunas/<cut>.json {series, last_updated}; data/cead/meta/index.json = [{cut, name, slug, region_id, …}]. Dist paths: dist/methodology/index.html, dist/es/metodologia/index.html, dist/commune/<slug>/index.html, dist/es/comuna/<slug>/index.html (ES slugs are hardcoded, per memory "i18n localized-slug pitfall"; commune slugs are shared).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cead-vintage.lib.mjs pure checker + vitest</name>
  <files>site/scripts/validate/cead-vintage.lib.mjs, site/scripts/validate/cead-vintage.test.ts</files>
  <behavior>
    - A good EN page (one node, attrs 2026-06-16/2026/2025, text 'CEAD data as of June 16, 2026' and '2026 is a partial year' and 'latest complete year is 2025') gives [].
    - Good ES page gives [].
    - A missing node gives an error containing 'no .cead-vintage'. Two nodes give 'expected exactly 1'.
    - data-cead-last-updated '2026-03-01' when expected '2026-06-16' gives an error naming both values.
    - The partial year expected 2026 but the label missing from the text gives an error. expected partialYear null with data-partial-year '' and no label gives [].
    - Visible date in the wrong locale format ('June 16, 2026' on ES) gives an error.
    - Methodology mode: '(currently 2024)' when expected 2025 gives an error; '(actualmente 2025)' on ES gives [].
    - R-05: a node whose text contains 'or trends' (EN) or 'ni las tendencias' (ES) gives an error.
    - R-02: findBannedPartialLabels('…partial year data (Jan–Jun)…') returns 1 hit; the same for 'ene–jun', '(partial data)' and '(datos parciales)'; clean HTML returns [].
  </behavior>
  <action>
Step 0, precondition (R1/R-14): `grep -c '^- \[x\] \*\*Phase 34' .planning/ROADMAP.md` must print 1. If it does not, STOP without editing and report "G-19: Phase 34 not closed". Execution mode (R1/R-12, G-21): run this plan ALONE (wave 4, after 35-04). It builds site/.

Create cead-vintage.lib.mjs.

Export expectedFromJson(json):
- lastUpdated = the json.last_updated string when it matches YYYY-MM-DD, else null;
- partialYear = the max year with partial true, else null;
- latestCompleteYear = the max year with partial not true, else null.
Write this locally. Do NOT import site/src/lib/ceadVintage.ts, because the independence is deliberate.

Export formatDate(iso, locale), using Intl en-US / es-CL, long month, timeZone 'UTC'.

Export checkCeadVintagePage(html, expected, locale, {methodology=false}) → string[] errors. It:
- finds `<p class="cead-vintage"` occurrences by regex, and requires exactly 1;
- extracts the three data attributes and compares them to expected;
- strips tags from the node's inner HTML to get the visible text;
- asserts that the text includes the prefix ('CEAD data as of ' / 'Datos CEAD al ') + formatDate;
- when partialYear is not null, asserts `${partialYear} is a partial year` / `${partialYear} es un año parcial`, plus `latest complete year is ${C}` / `último año completo es ${C}`;
- when partialYear is null, asserts that no partial label is present;
- in methodology mode, additionally asserts that the page text contains `(currently ${C})` / `(actualmente ${C})`;
- R-05: asserts that the node text does not contain 'or trends' / 'ni las tendencias'.

Export findBannedPartialLabels(text) → the matched strings for /Jan–Jun|ene–jun|\(partial data\)|\(datos parciales\)/g (R-02).

Write the vitest tests from the behavior list. RED first.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run scripts/validate/cead-vintage.test.ts</automated>
    Expected: all pass. Every negative case returns a non-empty error list, so the checker can fail.
  </verify>
  <done>A pure checker with passing and failing fixtures exists in both locales.</done>
</task>

<task type="auto">
  <name>Task 2: cead-vintage.mjs dist scanner + register as validator #17</name>
  <files>site/scripts/validate/cead-vintage.mjs, site/scripts/validate/all.mjs</files>
  <action>
Create cead-vintage.mjs, resolving paths from import.meta.url the same way freshness.mjs:31-34 does. The dist root is env CEAD_VINTAGE_DIST, else site/dist. This test-and-verification injection is what 35-07 uses to run this validator on HTML fetched from prod.

1. Check dist/methodology/index.html against the national.json expectation in 'en' methodology mode, and dist/es/metodologia/index.html against the same in 'es' methodology mode.
1b. R1/R-02: for each data/cead/regions/*.json, check dist/region/<slug>/index.html ('en') and dist/es/region/<slug>/index.html ('es') against expectedFromJson(region), where lastUpdated falls back to national's. Count them. FAIL if either locale count is 0 or EN ≠ ES; expect 16/16. In the default dist (no CEAD_VINTAGE_DIST), a missing region page FAILs naming the slug; there is no silent skip. In subset mode (CEAD_VINTAGE_DIST set, which is how 35-07 checks fetched prod pages), missing region and commune pages are skipped, but each locale count must still be ≥ 1 and EN == ES.
2. For every entry in data/cead/meta/index.json:
   - if dist/commune/<slug>/index.html exists, check it against comunas/<cut>.json, falling back to national.json's last_updated when the commune file lacks the field;
   - do the same for dist/es/comuna/<slug>/index.html.
3. Count the pages checked per locale. FAIL if either locale count is 0 or the EN count ≠ the ES count. With the ROLLOUT_ALL=true build, expect 346 each.
3b. R1/R-02: scan every dist/**/*.html, plus site/src/components/map/ResultPanel.tsx, with findBannedPartialLabels. Any hit is an error naming the file.
4. Print `PASS cead-vintage: 2 methodology + R/R region + N/N commune pages (EN/ES), 0 banned partial labels in K html files, CEAD as of <date>`. FAIL if K == 0 (R2/F35-R1-02)., or print every error (capped at 20 lines plus a total) and exit 1.

In all.mjs:
- append 'cead-vintage.mjs' to VALIDATORS after 'facets.mjs';
- update the header count "run all 16" to 17 and add a line "17. cead-vintage.mjs — FRESH-05 CEAD as-of + partial-year label on methodology + commune pages (EN/ES)".
Plan 35-06 then adds #18.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && node scripts/validate/cead-vintage.mjs && M="$(cygpath -m "$TEMP")/ics-cv-mut" && [ -n "${M%/ics-cv-mut}" ] && rm -rf "$M" && cp -r dist "$M" && sed -i 's/CEAD data as of/CEAD data at/' "$M/commune/santiago/index.html" && (CEAD_VINTAGE_DIST="$M" node scripts/validate/cead-vintage.mjs; echo "mutated-exit=$?") && [ -n "${M%/ics-cv-mut}" ] && rm -rf "$M" && cp -r dist "$M" && sed -i 's#</body>#<p>(partial data)</p></body>#' "$M/news/index.html" && (CEAD_VINTAGE_DIST="$M" node scripts/validate/cead-vintage.mjs 2>&1 | tee "$M.banned.log"; echo "banned-exit=${PIPESTATUS[0]}") && grep -q "news/index.html" "$M.banned.log" && [ -n "${M%/ics-cv-mut}" ] && rm -rf "$M" "$M.banned.log" && node scripts/validate/cead-vintage.mjs && npm run validate 2>&1 | tail -22</automated>
    Expected:
    - The first run prints "PASS cead-vintage: 2 methodology + 16/16 region + 346/346 commune pages, … 0 banned partial labels", and names the count of HTML files scanned by 3b.
    - The mutated runs print "mutated-exit=1" and "banned-exit=1".
    - R2/F35-R1-02: the banned mutation injects `<p>(partial data)</p>` into news/index.html. The checker does not parse that page's .cead-vintage node, so only the 3b scan can catch it, and the log must name news/index.html.
    - M is assigned without backslashes inside double quotes (`cygpath -m "$TEMP"`), and every `rm -rf` is preceded by the `[ -n "${M%/ics-cv-mut}" ]` guard.
    - R1/R-12: the mutations run on a copy in the OS temp dir via CEAD_VINTAGE_DIST. site/dist is never edited.
    - The final run on site/dist prints PASS.
    - The all.mjs summary lists 17 validators with cead-vintage PASS. freshness may be FAIL only from the live data state (35-07 policy); every other validator is PASS.
    This is ONE chained command (OneDrive).
  </verify>
  <done>The validator is registered as #17, covers methodology, region and commune pages in EN/ES plus the banned partial-label scan, is green on the real build, and is proven to fail on mutated copies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| dist HTML → validator | read-only scan |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-11 | Repudiation | Vacuous validator pass | mitigate | FAIL on 0 pages checked or on an EN/ES count mismatch. A mutation proof runs in verify. |
| T-35-SC | Tampering | installs | accept | none |
</threat_model>

<verification>
Validators 17/17 PASS, excluding freshness if the live data is stale. That exclusion applies only at this plan; 35-07 requires all to be green. The cead-vintage vitest is green.
</verification>

<success_criteria>
The FRESH-05 validator exists, is registered, is green, and has been shown to fail when the stamp is broken.
</success_criteria>

<output>
Create `.planning/phases/35-honest-freshness-signals/35-05-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-02 | must_haves truth; interfaces (region paths); Task 1 behavior + action (findBannedPartialLabels); Task 2 action steps 1b/3b (16/16 region pages, banned-label scan over dist + ResultPanel.tsx, subset mode for prod) and verify (region count + banned mutation) |
| R-05 | must_haves truth; Task 1 behavior + action ('or trends' / 'ni las tendencias' is an error) |
| R-12 | frontmatter wave 4, depends_on ["35-03","35-04"]; Task 1 "Execution mode"; Task 2 verify mutations on an os.tmpdir() copy via CEAD_VINTAGE_DIST (site/dist is never edited) |
| R-14 | Task 1 action Step 0 |

## Revision R2 (gate R1)

| Item | Applied where |
|---|---|
| Condition 2 (F35-R1-02) | Task 2 action step 4 (PASS prints the 3b scanned count; FAIL on 0) + verify: M via `cygpath -m "$TEMP"`, a `[ -n "${M%/ics-cv-mut}" ]` guard before every rm -rf, the banned mutation injects `<p>(partial data)</p>` into news/index.html, banned-exit=1, and the log names the file |
