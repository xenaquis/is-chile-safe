---
phase: 35-honest-freshness-signals
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - site/src/lib/ceadVintage.ts
  - site/src/lib/ceadVintage.test.ts
  - site/src/config/i18n.ts
  - site/src/lib/data.ts
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
  - site/src/pages/commune/[slug].astro
  - site/src/pages/es/comuna/[slug].astro
  - site/src/pages/region/[slug].astro
  - site/src/pages/es/region/[slug].astro
  - site/src/components/map/ResultPanel.tsx
autonomous: true
requirements: [FRESH-05]
executor: sonnet

must_haves:
  truths:
    - "/methodology/ and /es/metodologia/ show 'CEAD data as of June 16, 2026' / 'Datos CEAD al 16 de junio de 2026', taken from data/cead/national.json last_updated rather than typed text."
    - "Every commune page in both locales shows the same stamp from that commune's own last_updated, falling back to national.json."
    - "Where the series has a partial year, both page types label it: '2026 is a partial year …' / '2026 es un año parcial …', naming the latest complete year. The methodology '(currently 2025)' / '(actualmente 2025)' is computed from data."
    - "R1/R-02: the 16 region pages in both locales carry the same .cead-vintage paragraph (region.last_updated, else national). The invented footnotes '* 2025: partial year data (Jan–Jun)' / 'año parcial (ene–jun)' and the '*' in the heading are gone, and the map ResultPanel no longer calls 2025 'partial data'."
    - "R1/R-05: the partial-year copy does not deny what the trend chart draws. It says the partial year appears as a faded point in the trend charts and is excluded only from the headline rate and rankings."
    - "No month-level cutoff is claimed. The data holds none: partial means 'current calendar year as of the scrape date' (scrape_cead.py:67-89)."
  artifacts:
    - path: site/src/lib/ceadVintage.ts
      provides: "ceadVintage(series, lastUpdated) → {lastUpdated, partialYear, latestCompleteYear}; formatCeadDate(iso, locale)"
    - path: site/src/config/i18n.ts
      provides: "cead_as_of, cead_as_of_note, cead_partial_cutoff (EN+ES)"
  key_links:
    - from: site/src/pages/methodology.astro
      to: data/cead/national.json
      via: "loadNational() (site/src/lib/data.ts:165-169)"
      pattern: "class=\"cead-vintage\""
---

<objective>
FRESH-05 display: disclose the CEAD vintage and the partial-year cutoff on the methodology and commune pages, in both locales, as static build-time HTML with machine-checkable data-* markers. The validator itself is plan 35-05.

Purpose: V-13 found that grep of site/src for last_updated returns only types (data.ts:65, ResultPanel.tsx:77), and that /methodology/ and /commune/santiago/ show no as-of date, while the data is dated 2026-06-16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-honest-freshness-signals/35-RESEARCH.md
@site/src/lib/data.ts
@site/src/config/i18n.ts

<interfaces>
Evidence at HEAD 8d1f432:
- data/cead/national.json = {name, series, last_updated:'2026-06-16'}; series[-1] = {year:2026, partial:true, rate_per_100k:1380.3…}; 2025 is partial:false.
- All 346 data/cead/comunas/*.json and 16 regions/*.json carry last_updated '2026-06-16', measured with python and a Counter. This resolves research Assumption A1. meta/catalog.json has none; meta/index.json is a list.
- data.ts:38-43 SeriesEntry {year, rate_per_100k, by_family, partial?}; :65 CommuneData.last_updated?: string; :165-169 loadNational(): NationalData {name, series} (last_updated is present in the JSON but not typed, so add `last_updated?: string` to NationalData).
- scrape_cead.py:67-89 mark_partial_year: the current calendar year is partial if run_date < Dec 31. There is NO month or quarter cutoff field anywhere. V-13 refuted the "~5.5 months" claim.
- methodology.astro:92-99 is the "Partial-year data" paragraph, containing the literal "(currently 2025)" at :96. es/metodologia.astro:85-92 has "(actualmente 2025)" at :89. Neither page imports data or i18n today (methodology.astro:22 imports only EditorialLayout).
- commune/[slug].astro:337 and es/comuna/[slug].astro:340 hold `<p class="chart-partial-note">{t.chart_partial_note}</p>` after CrimeFamilyCharts. `data = loadCommune(cut)` is at :61, and `t` = EN_STRINGS / ES_STRINGS (ES at :168).
- i18n.ts:7 `interface I18nStrings`; EN_STRINGS at :250, ES_STRINGS at :492. A key missing from one locale is a TS error that astro check catches.
- R-02 twins (read by the premortem arbiter at c9ee956):
  - region/[slug].astro:207-211 has heading `Annual evolution (2005–{latestCompleteYear}*)` and footnote `* {latestCompleteYear}: partial year data (Jan–Jun)`;
  - es/region/[slug].astro:200-204 has `Evolución anual (2005–{latestCompleteYear}*)` and `* {latestCompleteYear}: año parcial (ene–jun)`;
  - `region = loadRegion(regionFileId)` is at region/[slug].astro:45;
  - RegionData (data.ts:83-89) has no last_updated field, although all 16 region JSONs carry '2026-06-16';
  - ResultPanel.tsx:289-290 contains '…La tasa de incidentes usa 2025 (datos parciales)…' / '…The incident rate uses 2025 (partial data)…'.
- Date formatting rule (facets.mjs assertion 11 practice): every toLocaleDateString with `month:` passes timeZone:'UTC'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ceadVintage helper + i18n strings</name>
  <files>site/src/lib/ceadVintage.ts, site/src/lib/ceadVintage.test.ts, site/src/config/i18n.ts, site/src/lib/data.ts</files>
  <behavior>
    - ceadVintage(national-like series 2005..2026 with 2026 partial, '2026-06-16') gives {lastUpdated:'2026-06-16', partialYear:2026, latestCompleteYear:2025}.
    - A series with no partial entry gives partialYear null, and latestCompleteYear = max year.
    - lastUpdated undefined, '' or 'not-a-date' gives lastUpdated null (the caller then renders no stamp; the 35-05 validator fails that page).
    - formatCeadDate('2026-06-16','en') === 'June 16, 2026'; formatCeadDate('2026-06-16','es') === '16 de junio de 2026' (Intl es-CL, timeZone UTC).
  </behavior>
  <action>
Step 0, the G-19 precondition (R1/R-14). Run `grep -c '^- \[x\] \*\*Phase 34' .planning/ROADMAP.md`. It must print 1. Otherwise STOP without editing anything and report "G-19: Phase 34 not closed".

Execution mode (R1/R-12, G-21): run this plan ALONE (wave 2, after 35-01). It builds site/, so no other Phase-35 plan may run concurrently.

Create site/src/lib/ceadVintage.ts. It must be pure, with no fs.
- Export ceadVintage(series: SeriesEntry[], lastUpdated?: string | null). lastUpdated is valid iff it matches /^\d{4}-\d{2}-\d{2}$/ and parses. partialYear = the max year with partial === true, else null. latestCompleteYear = the max year with partial !== true, else null.
- Export formatCeadDate(iso, locale: 'en'|'es'), using toLocaleDateString('en-US' | 'es-CL', {timeZone:'UTC', year:'numeric', month:'long', day:'numeric'}).

Add `last_updated?: string` to NationalData AND to RegionData in data.ts (the RegionData addition is R-02). Do not change the loader logic.

Add three keys to I18nStrings and to both locale objects. The microcopy is decided here: neutral and attributed, with no safety verdict.
- cead_as_of: EN 'CEAD data as of {date}' / ES 'Datos CEAD al {date}'
- cead_as_of_note: EN '(date of our last download from CEAD)' / ES '(fecha de nuestra última descarga desde el CEAD)'
- cead_partial_cutoff (R1/R-05 wording, because CrimeFamilyCharts.astro:36,64-68 does draw the partial year): EN '{year} is a partial year: its figures include only what CEAD had published by {date}. It is not used for the headline rate or rankings, and appears as a faded point in the trend charts. The latest complete year is {complete}.' / ES '{year} es un año parcial: sus cifras incluyen solo lo que el CEAD había publicado al {date}. No se usa para la tasa principal ni los rankings, y aparece como un punto atenuado en los gráficos de evolución. El último año completo es {complete}.'

Write ceadVintage.test.ts covering every behavior bullet.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run src/lib/ceadVintage.test.ts</automated>
    Expected: all pass, also under `TZ=America/Santiago npx vitest run src/lib/ceadVintage.test.ts`. Include the case formatCeadDate('2026-01-01','en') === 'January 1, 2026'. Negative control: remove timeZone:'UTC', and under TZ=America/Santiago that case must FAIL ('December 31, 2025'). Restore it.
  </verify>
  <done>Helper and strings exist for both locales. Tests are green under both UTC and America/Santiago.</done>
</task>

<task type="auto">
  <name>Task 2: Render the vintage + partial-year label on methodology, commune and region pages (EN+ES); fix ResultPanel</name>
  <files>site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro, site/src/pages/commune/[slug].astro, site/src/pages/es/comuna/[slug].astro, site/src/pages/region/[slug].astro, site/src/pages/es/region/[slug].astro, site/src/components/map/ResultPanel.tsx</files>
  <action>
In each page, build one paragraph. Do NOT add a CSS `display` rule to .cead-vintage, per the memory hazard "Astro scoped style defeats [hidden]". Use no client JS.

Markup:
`<p class="cead-vintage" data-cead-last-updated={v.lastUpdated} data-partial-year={v.partialYear ?? ''} data-latest-complete-year={v.latestCompleteYear ?? ''}>`

Contents:
- `<time datetime={v.lastUpdated}>{t.cead_as_of with {date}=formatCeadDate}</time>`, then a space and cead_as_of_note;
- when partialYear is not null, a space and cead_partial_cutoff with {year}, {date} and {complete} substituted.

Substitute every {token} server-side, so that no literal `{` token survives in the HTML.

Methodology EN/ES:
- Import loadNational, ceadVintage/formatCeadDate, and EN_STRINGS or ES_STRINGS.
- Compute v = ceadVintage(nat.series, nat.last_updated).
- Insert the paragraph immediately after the "Partial-year data" / "Datos de año parcial" paragraph (methodology.astro:92-99, es/metodologia.astro:85-92).
- Replace the literal "(currently 2025)" / "(actualmente 2025)" with `(currently {v.latestCompleteYear})` / `(actualmente {v.latestCompleteYear})`.

Commune EN/ES:
- Compute v = ceadVintage(data.series, data.last_updated ?? loadNational().last_updated).
- Insert the paragraph directly after the chart-partial-note line (EN :337, ES :340).
- Style it with the existing muted small-text tokens, copying the .chart-partial-note rule's font-size and color. Add no display property.

Region EN/ES (R1/R-02):
- Import ceadVintage/formatCeadDate, loadNational and EN_STRINGS/ES_STRINGS.
- Compute v = ceadVintage(region.series, region.last_updated ?? loadNational().last_updated).
- Remove the `*` from the heading, so it reads `Annual evolution (2005–{latestCompleteYear})` / `Evolución anual (2005–{latestCompleteYear})`.
- Replace the `<p class="chart-footnote">* … (Jan–Jun)</p>` / `(ene–jun)` line with the same .cead-vintage paragraph and markup as above.

ResultPanel.tsx:289-290 (R1/R-02, rewritten per R2/F35-R1-03): replace BOTH yearMismatchTip strings entirely.
- EN: 'Why do years differ? The Composite Index uses 2024, the latest year in which all of its sources (CEAD, SPD, SII) are final. The incident rate uses the latest complete CEAD year (2025). Rankings may differ because they come from two calculation methods.'
- ES: '¿Por qué difieren los años? El Índice Compuesto usa 2024, el último año en que todas sus fuentes (CEAD, SPD, SII) son definitivas. La tasa de incidentes usa el último año completo de CEAD (2025). Los rankings pueden diferir al provenir de dos métodos de cálculo.'
- Source: pipeline/composite_config.py:11-14 REFERENCE_YEAR = 2024, and national.json 2025 partial=False.
- Change nothing in ResultPanel.tsx outside yearMismatchTip.

Run the build and spot-check the output.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && grep -c 'CEAD data as of June 16, 2026' dist/methodology/index.html dist/commune/santiago/index.html dist/region/antofagasta/index.html && grep -c 'Datos CEAD al 16 de junio de 2026' dist/es/metodologia/index.html dist/es/comuna/santiago/index.html dist/es/region/antofagasta/index.html && grep -c '2026 is a partial year' dist/methodology/index.html && grep -c '2026 es un año parcial' dist/es/comuna/santiago/index.html && grep -c '(currently 2025)' dist/methodology/index.html && grep -c '(actualmente 2025)' dist/es/metodologia/index.html && ! grep -rlE 'Jan–Jun|ene–jun|\(partial data\)|\(datos parciales\)' dist --include=*.html && ! grep -nE '\(partial data\)|\(datos parciales\)|2024 as the latest complete CEAD year|2024 como el año completo más reciente' src/components/map/ResultPanel.tsx && ls dist/commune | wc -l && { npx astro check > .astro-check.log 2>&1; rc=$?; tail -3 .astro-check.log; [ $rc -eq 0 ]; }</automated>
    Expected:
    - every positive grep prints ≥ 1 on each file;
    - both `! grep` guards succeed, so the banned strings are gone from dist and from ResultPanel.tsx;
    - the commune dir count is 346, because the build sets ROLLOUT_ALL=true;
    - astro check reports "0 errors".
    This must be ONE chained command because of the OneDrive dist hazard. The expected date strings assume data/cead is unchanged (last_updated 2026-06-16). If HYG-06 has refreshed CEAD, substitute that value, read with `python -c "import json;print(json.load(open('../data/cead/national.json',encoding='utf-8'))['last_updated'])"`.
  </verify>
  <done>Methodology, commune and region pages in both locales carry a single data-derived cead-vintage paragraph with a partial-year label. The Jan–Jun / (partial data) inventions are gone. The methodology "currently" year is data-driven. astro check reports 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| data/cead/*.json → HTML | locally scraped government data rendered at build |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | last_updated injection into attributes | mitigate | The value is regex-validated (YYYY-MM-DD) before rendering, and Astro escapes attributes. No set:html. |
| T-35-08 | Repudiation | Overclaiming the CEAD coverage | mitigate | The wording states only the download date and the "published by" scope. No month range is invented (V-13 correction). |
| T-35-SC | Tampering | installs | accept | none |
</threat_model>

<verification>
From site/: `npm run build && npm run validate; npx vitest run`. B-R1-05: vitest runs even if freshness is red, and each rc is read separately. It exits 0 except for freshness, whose state depends on the live data (see 35-07). forbidden-language, hreflang and schema are all PASS.
</verification>

<success_criteria>
The CEAD vintage and the partial-year cutoff are visible and data-derived on the methodology and commune pages in both locales.
</success_criteria>

<output>
Create `.planning/phases/35-honest-freshness-signals/35-03-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-02 | files_modified (+region EN/ES, ResultPanel.tsx); must_haves truth; interfaces (region/ResultPanel evidence); Task 1 action (RegionData.last_updated); Task 2 name/files/action (region paragraph, '*' removed, ResultPanel copy); Task 2 verify (region greps + dist-wide banned-label guard) |
| R-05 | must_haves truth; Task 1 action (cead_partial_cutoff wording) |
| R-12 | frontmatter wave 2, depends_on ["35-01"]; Task 1 action "Execution mode" |
| R-14 | Task 1 action Step 0 |

## Revision R2 (gate R1)

| Item | Applied where |
|---|---|
| Condition 3 (F35-R1-03) | Task 2 action: full EN/ES yearMismatchTip rewrite (2024 = all sources final; 2025 = latest complete CEAD year); "change nothing" is limited to outside yearMismatchTip; verify `! grep` adds '2024 as the latest complete CEAD year\|2024 como el año completo más reciente' |
| B-R1-05 (non-blocker) | verify: astro check rc captured; <verification>: vitest separated from validate |
