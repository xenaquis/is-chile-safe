---
phase: 35-honest-freshness-signals
plan: 04
type: execute
wave: 2
depends_on: ["35-01", "35-03"]
files_modified:
  - site/src/config/i18n.ts
  - site/src/pages/news.astro
  - site/src/pages/es/noticias.astro
  - site/src/components/CommuneNewsSection.astro
autonomous: true
requirements: [FRESH-01, FRESH-03]
executor: sonnet

must_haves:
  truths:
    - "/news/ shows 'Latest incident: <Month D, YYYY>' and /es/noticias/ shows 'Último incidente: <D de mes de YYYY>'. The date is latestIncidentDate(current.json), the literal max incident date, with no +1d."
    - "The 'Updated <generated>' / 'Actualizado <generated>' build stamp is gone from both pages."
    - "A bilingual stale notice (.news-stale-notice) renders iff evidenceVerdict(computeEvidence(payload), buildNow, 48) !== 'fresh'. It uses the same shared helper as freshness.mjs, with no second definition."
    - "A commune news section whose newest displayed item's evidence (max(date)+1d) is > 168h before build time shows the caveated heading in both locales. Otherwise it shows the normal heading."
    - "All freshness decisions are pre-rendered static HTML, with no client JS added."
  artifacts:
    - path: site/src/pages/news.astro
      provides: "p.freshness[data-latest-incident][data-build-now] + conditional p.news-stale-notice"
    - path: site/src/components/CommuneNewsSection.astro
      provides: "section.cnews-section[data-news-stale][data-build-now], heading swap"
  key_links:
    - from: site/src/pages/news.astro
      to: site/src/lib/newsEvidence.mjs
      via: "import { latestIncidentDate, computeEvidence, evidenceVerdict, MAX_AGE_HOURS }"
      pattern: "newsEvidence.mjs"
    - from: site/src/components/CommuneNewsSection.astro
      to: site/src/lib/newsEvidence.mjs
      via: "evidenceVerdict(…, COMMUNE_NEWS_STALE_HOURS)"
      pattern: "COMMUNE_NEWS_STALE_HOURS"
---

<objective>
FRESH-01 (news pages) and FRESH-03 (commune news section): replace build-time recency signals with data-derived ones, and caveat stale content. Both locales. Build-time static HTML only.

Purpose: V-05. Prod shows "Updated September 23, 2026" over cards whose newest is dated 2026-09-04, and 112 of 112 communes with news carry "Recent Incidents in the News" headings over items that are more than 7 days old.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-honest-freshness-signals/35-RESEARCH.md
@.planning/phases/35-honest-freshness-signals/35-01-SUMMARY.md
@site/src/components/CommuneNewsSection.astro

<interfaces>
From 35-01 (site/src/lib/newsEvidence.mjs):
  latestIncidentDate(payload) → 'YYYY-MM-DD' | null
  computeEvidence(payload) → { ms, source, label } | null
  evidenceVerdict(evidence, nowMs, maxAgeHours = 48) → 'none' | 'future' | 'stale' | 'fresh'
  MAX_AGE_HOURS = 48, FUTURE_ALLOWANCE_HOURS = 24, COMMUNE_NEWS_STALE_HOURS = 168

Evidence at HEAD 8d1f432:
- news.astro:47-83: `generated`, `incidents` and formattedDate, from `raw` parsed at :62-65 (data path via process.cwd() at :45, per the memory "News page build path drift"). :239-244 is `{generated && (<p class="freshness"><time datetime={generated}>Updated {formattedDate}</time></p>)}` inside the first <section>. :700-704 is the .freshness style (no display rule). `generated` has no other use in the file.
- es/noticias.astro: the same structure. Data at :44-66, formattedDate at :70-83 ('es-CL'), markup at :239-243 "Actualizado".
- The facets.mjs constraints quoted in news.astro:16-28: no literal {n}/{date} may survive outside the single data-result-count-template (so substitute server-side); every toLocaleDateString with `month:` needs timeZone:'UTC'; no `display` rule on `.news-card` selectors.
- CommuneNewsSection.astro:108-110: `items = safe.slice(0, 6)` sorted newest-first (:49-51). :133-135 is the section and h2 `{t.commune_news_heading}`, and the section aria-label is t.commune_news_aria. The header says "Zero client JS (D-09)". The class prefix `cnews-` must not reuse news-card.
- i18n.ts EN :485-489 / ES :730-734 hold the commune_news_* keys; interface :243-247.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FRESH-01 Latest-incident stamp + stale notice on /news/ and /es/noticias/</name>
  <files>site/src/config/i18n.ts, site/src/pages/news.astro, site/src/pages/es/noticias.astro</files>
  <action>
Add these i18n keys (interface plus both locales). The microcopy is decided here: neutral, with no safety verdict.
- news_latest_incident: EN 'Latest incident: {date}' / ES 'Último incidente: {date}'
- news_stale_notice: EN 'No new incidents have been added for more than 48 hours (newest incident dated {date}). The news feed may be delayed, so the list below is not current.' / ES 'No se han agregado incidentes nuevos en más de 48 horas (el más reciente es del {date}). El flujo de noticias puede estar retrasado, por lo que la lista de abajo no está al día.'
- news_stale_notice_nodate: EN 'No new incidents have been added for more than 48 hours. The news feed may be delayed.' / ES 'No se han agregado incidentes nuevos en más de 48 horas. El flujo de noticias puede estar retrasado.'

In BOTH news.astro and es/noticias.astro (edit both, since they are duplicated per locale by design):
1. Keep the parsed `raw` object as `payload`, defaulting to {} on a missing or malformed file.
2. Delete `generated`, `formattedDate` and the "Updated"/"Actualizado" `<time>`.
   Knob, decided: the build stamp is REMOVED, not relabelled. V-05 names it as the misleading signal. After 35-02, `generated` means "content last changed", and that includes aging-only rewrites that add no incident, so any visible "Updated" would still assert a freshness the incident data lacks. The data-derived "Latest incident" replaces it, and `generated` stays in the JSON for operators.
3. Compute:
   - `buildNowMs = Date.now()`;
   - `latest = latestIncidentDate(payload)`;
   - `verdict = evidenceVerdict(computeEvidence(payload), buildNowMs, MAX_AGE_HOURS)`;
   - `showStale = verdict !== 'fresh'`.
   Knob: "now" = the Astro build wall clock. The page is static, so the verdict is "as of this build". 35-02 Task 3 guarantees a daily rebuild while the evidence is stale.
4. Markup, in place of :239-244:
   - `<p class="freshness" data-latest-incident={latest ?? ''} data-build-now={new Date(buildNowMs).toISOString()}>`;
   - when latest is set, `<time datetime={latest}>{news_latest_incident with {date} = long date}</time>`, formatted en-US / es-CL, month 'long', timeZone 'UTC';
   - then `{showStale && (<p class="news-stale-notice" role="status" data-stale-hours="48">{latest ? news_stale_notice(date substituted) : news_stale_notice_nodate}</p>)}`.
5. Style .news-stale-notice with a left border using an existing token (for example the var(--line) or warning colour already present in the file), padding, and font-size var(--text-label). Add NO `display` property.
6. Do not touch the facet row ('Latest data: {date}', news_window_latest), the histogram call, or any client script.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && grep -c 'Latest incident: ' dist/news/index.html && grep -c 'Último incidente: ' dist/es/noticias/index.html && grep -c 'data-build-now=' dist/news/index.html dist/es/noticias/index.html && ! grep -q '>Updated ' dist/news/index.html && ! grep -q '>Actualizado ' dist/es/noticias/index.html && grep -o 'class="news-stale-notice"' dist/news/index.html dist/es/noticias/index.html | wc -l && node scripts/validate/facets.mjs && node scripts/validate/forbidden-language.mjs && node scripts/validate/hreflang.mjs && npx astro check 2>&1 | tail -3</automated>
    Expected:
    - "Latest incident: " and "Último incidente: " give 1 each, and data-build-now= gives 1 per file.
    - The `! grep -q` guards pass, so neither '>Updated ' nor '>Actualizado ' appears.
    - The stale-notice count is 2 when local data/incidents/current.json evidence is older than 48h, and 0 when fresh. It is the same in both locales, EN == ES. Record which case applied, and the payload's last_new_incident_at or max(date), in the SUMMARY. The exact-state assertion is 35-06's fixture harness.
    - facets, forbidden-language and hreflang all PASS, and astro check reports 0 errors.
    This is ONE chained command because of the OneDrive hazard.
  </verify>
  <done>Both news pages carry the data-derived latest-incident stamp and the helper-driven stale notice. The build stamp is gone. facets.mjs assertions are still green.</done>
</task>

<task type="auto">
  <name>Task 2: FRESH-03 caveated commune news heading (EN+ES)</name>
  <files>site/src/config/i18n.ts, site/src/components/CommuneNewsSection.astro</files>
  <action>
Add these i18n keys (interface plus both locales):
- commune_news_heading_stale: EN 'Incidents in the News (none in the last 7 days)' / ES 'Incidentes en la Prensa (ninguno en los últimos 7 días)'
- commune_news_stale_note: EN 'The newest item listed is from {date}; no newer press incident has been geolocated in {name} by our pipeline in the last 7 days.' / ES 'El incidente más reciente de la lista es del {date}; nuestro pipeline no ha geolocalizado incidentes de prensa más nuevos en {name} en los últimos 7 días.'
- commune_news_aria_stale: EN 'Incidents reported in the news' / ES 'Incidentes reportados en la prensa'

In CommuneNewsSection.astro, after `items` (:110):
- `buildNowMs = Date.now()`;
- `stale = items.length > 0 && evidenceVerdict(computeEvidence({ incidents: items }), buildNowMs, COMMUNE_NEWS_STALE_HOURS) !== 'fresh'`.

The rule is threshold 168h (7 days, REQUIREMENTS FRESH-03) applied to the G-05 fallback evidence max(date)+1d of the newest DISPLAYED item, measured against the build wall clock. The last_new_incident_at field is deliberately ignored here: it is global, not per-commune. The rule is decided here and recorded as a knob.

Markup:
- the section gets `data-news-stale={stale ? 'true' : 'false'}`, `data-build-now={ISO}`, and aria-label = stale ? commune_news_aria_stale : commune_news_aria;
- the h2 = stale ? commune_news_heading_stale : commune_news_heading;
- when stale, add `<p class="cnews-stale-note">` directly after the h2, with {date} = the newest item's long date (en-US / es-CL, UTC) and {name} = communeName, both substituted server-side.

Style .cnews-stale-note like .cnews-intro, without display. Keep zero client JS (D-09).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && grep -l 'class="cnews-section"' dist/commune/*/index.html | wc -l && grep -l 'class="cnews-section"' dist/es/comuna/*/index.html | wc -l && grep -l 'data-news-stale="true"' dist/commune/*/index.html | wc -l && grep -l 'data-news-stale="true"' dist/es/comuna/*/index.html | wc -l && grep -c 'none in the last 7 days' dist/commune/santiago/index.html && node scripts/validate/forbidden-language.mjs && node scripts/validate/commune.mjs && npx astro check 2>&1 | tail -3</automated>
    Expected:
    - The EN and ES cnews-section counts are equal and ≥ 1; research measured 112 with the 2026-09-23 data.
    - The EN and ES stale counts are equal. With data whose max date is ≤ build date − 7d (for example still frozen at 2026-09-04), stale == section count. With live data they may differ; record the numbers in the SUMMARY.
    - The santiago grep is ≥ 1 only if santiago is stale. Otherwise run `grep -c 'Recent Incidents in the News' dist/commune/santiago/index.html` and it must give ≥ 1. The exact-state assertion is 35-06's fixture harness.
    - forbidden-language and commune validators PASS, and astro check reports 0 errors.
  </verify>
  <done>The commune news heading switches to the caveated form, with a dated note, whenever its newest item is more than 7 days old. EN/ES parity holds, and there is no client JS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| current.json (pipeline; LLM-processed press text) → static HTML | incident dates and names rendered at build |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Tampering / XSS | date and communeName substitution | mitigate | Only regex-validated YYYY-MM-DD dates reach Intl. communeName comes from data/cead/meta/index.json, not press text. Astro text escaping applies, and no set:html is added. |
| T-35-10 | Repudiation | Misleading recency | mitigate | The notice decision uses the shared G-05 helper. Heading and notice copy make no safety verdict; forbidden-language.mjs gates the build. |
| T-35-SC | Tampering | installs | accept | none |
</threat_model>

<verification>
One chained command from site/: `npm run build && npm run validate && npx vitest run`. Every validator is PASS except freshness, which reflects the live data state (35-07 policy). astro check reports 0 errors.
</verification>

<success_criteria>
FRESH-01 and FRESH-03 are rendered in static HTML for both locales, driven by the single shared helper.
</success_criteria>

<output>
Create `.planning/phases/35-honest-freshness-signals/35-04-SUMMARY.md` when done.
</output>
