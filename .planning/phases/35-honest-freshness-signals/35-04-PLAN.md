---
phase: 35-honest-freshness-signals
plan: 04
type: execute
wave: 3
depends_on: ["35-01", "35-03"]
files_modified:
  - site/src/config/i18n.ts
  - site/src/pages/news.astro
  - site/src/pages/es/noticias.astro
  - site/src/components/CommuneNewsSection.astro
  - site/src/components/home/HomeNewsPulse.astro
  - site/src/config/homeV2Strings.ts
  - site/src/lib/newsStripDays.ts
  - site/src/lib/newsStripDays.test.ts
  - site/src/components/map/NewsStrip.tsx
  - site/src/components/map/MapIsland.tsx
  - site/src/config/mapV2Strings.ts
autonomous: true
requirements: [FRESH-01, FRESH-02, FRESH-03]
executor: sonnet

must_haves:
  truths:
    - "/news/ shows 'Latest incident: <Month D, YYYY>' and /es/noticias/ shows 'Último incidente: <D de mes de YYYY>'. The date is latestIncidentDate(current.json), the literal max incident date, with no +1d."
    - "The 'Updated <generated>' / 'Actualizado <generated>' build stamp is gone from both pages."
    - "A bilingual stale notice (.news-stale-notice) renders iff the verdict of evidenceVerdict(computeEvidence(payload), buildNow, 48) is 'stale' or 'none' (R1/R-07). 'future' does not render it, because freshness.mjs and the heartbeat already flag it red. The notice uses the same shared helper as freshness.mjs, with no second definition."
    - "R1/R-01: histogram buckets flagged gap (declared G-18 outage days with 0 incidents) render with data-gap=\"true\" and 'no data collected' / 'sin datos recolectados' instead of '· 0'. When any gap bucket is inside the window, a static caption explains the outage."
    - "A commune news section whose newest displayed item's evidence (max(date)+1d) gets verdict 'stale' against the 168h threshold at build time (R1/R-07: 'future' is not stale) shows the caveated heading and a dated note in both locales. The note makes no claim about geolocation or pipeline activity (R1/R-06)."
    - "R1/R-03: the home news pulse (EN/ES) clips its histogram to the data's coverage and marks declared gap days. When the build-time G-05 verdict is 'stale' or 'none', it carries a static .pulse-stale notice."
    - "R2/F35-R1-06 (G-22(b)): the /map/ and /es/mapa/ NewsStrip day range starts at max(anchor−(windowDays−1), the earliest valid UNFILTERED incident date). The pre-coverage extension (NewsStrip.tsx:72-73) is removed, and declared gap days with 0 unfiltered incidents render striped with the gap aria-label."
    - "All freshness decisions are pre-rendered static HTML. The only new client logic is the pulse's existing enhancer, which reads data-* attributes and never uses {expr} inside <script>."
  artifacts:
    - path: site/src/pages/news.astro
      provides: "p.freshness[data-latest-incident][data-build-now] + conditional p.news-stale-notice + gap bars + gap caption"
    - path: site/src/components/CommuneNewsSection.astro
      provides: "section.cnews-section[data-news-stale][data-build-now], heading swap"
    - path: site/src/components/home/HomeNewsPulse.astro
      provides: "coverage-clipped pulse, data-coverage-gaps, static p.pulse-stale"
  key_links:
    - from: site/src/pages/news.astro
      to: site/src/lib/newsEvidence.mjs
      via: "import { latestIncidentDate, computeEvidence, evidenceVerdict, MAX_AGE_HOURS }"
      pattern: "newsEvidence.mjs"
    - from: site/src/components/CommuneNewsSection.astro
      to: site/src/lib/newsEvidence.mjs
      via: "evidenceVerdict(…, COMMUNE_NEWS_STALE_HOURS)"
      pattern: "COMMUNE_NEWS_STALE_HOURS"
    - from: site/src/components/home/HomeNewsPulse.astro
      to: site/src/lib/newsCoverageGaps.ts
      via: "data-coverage-gaps={JSON.stringify(COVERAGE_GAPS)}"
      pattern: "data-coverage-gaps"
---

<objective>
FRESH-01 (news pages), FRESH-02 twins (outage gap bars, home pulse) and FRESH-03 (commune news section): replace build-time recency signals with data-derived ones, and caveat stale or missing coverage. Both locales. Build-time static HTML.

Purpose: V-05. Prod shows "Updated September 23, 2026" over cards whose newest is dated 2026-09-04. 112 of 112 communes with news carry "Recent Incidents in the News" over items more than 7 days old. The home pulse (HomeNewsPulse.astro:91-95) pads 30 days from the anchor. Premortem R-01 and R-03 add the outage days 09-05..09-22, which would otherwise be drawn as real zeros.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-honest-freshness-signals/35-RESEARCH.md
@.planning/phases/35-honest-freshness-signals/35-PREMORTEM.md
@.planning/phases/35-honest-freshness-signals/35-01-SUMMARY.md
@site/src/components/CommuneNewsSection.astro
@site/src/components/home/HomeNewsPulse.astro

<interfaces>
From 35-01:
  site/src/lib/newsEvidence.mjs:
    latestIncidentDate(payload) → 'YYYY-MM-DD' | null
    computeEvidence(payload) → { ms, source, label } | null
    evidenceVerdict(evidence, nowMs, maxAgeHours = 48) → 'none' | 'future' | 'stale' | 'fresh'
    MAX_AGE_HOURS = 48, FUTURE_ALLOWANCE_HOURS = 24, COMMUNE_NEWS_STALE_HOURS = 168
  site/src/lib/newsCoverageGaps.ts: COVERAGE_GAPS = [{from:'2026-09-05', to:'2026-09-22', source:'G-18 classifier outage'}]
  site/src/lib/newsDayFacets.ts: DayBucket {date, count, pct, gap: boolean}

Evidence at HEAD 8d1f432 / c9ee956:
- news.astro:
  - :47-83 `generated`, `incidents` and formattedDate, from `raw` parsed at :62-65 (data path via process.cwd() at :45; memory "News page build path drift");
  - :239-244 `{generated && (<p class="freshness"><time datetime={generated}>Updated {formattedDate}</time></p>)}`;
  - :310-336 the histogram. Each bucket is a `<button class="day-bar" data-day={b.date} aria-pressed="false" style={`--h:${b.pct}%`}>` holding `<span class="news-sr">{dayLabel(b.date)} · {b.count}</span>`;
  - :700-704 the .freshness style (no display rule);
  - `generated` has no other use.
- es/noticias.astro has the same structure: data at :44-66, formattedDate at :70-83 ('es-CL'), stamp at :239-243 "Actualizado", histogram at ~:309-335.
- facets.mjs constraints (news.astro:16-28): no literal {n}/{date} outside the single data-result-count-template (substitute server-side); every toLocaleDateString with `month:` has timeZone:'UTC'; no `display` rule on `.news-card` selectors.
- CommuneNewsSection.astro:
  - :108-110 `items = safe.slice(0, 6)` (after the forbidden-term filter), sorted newest-first (:49-51);
  - :133-135 the section and `h2 {t.commune_news_heading}`, with aria-label t.commune_news_aria;
  - "Zero client JS (D-09)"; the class prefix is `cnews-`.
- HomeNewsPulse.astro:
  - :25 `<div class="pulse-card" data-home-pulse … hidden>` (the static shell, revealed by the enhancer);
  - :91-95 the days loop `for d = shiftDays(anchor,-29) .. anchor`;
  - :98-106 bars with `title = dayLabel · count`;
  - :161-174 the fetch plus the LPX-8 30-day clip;
  - :192 `.pulse-card[hidden] { display: none; }` (keep it);
  - included by index.astro:92 and es/index.astro:101.
- homeV2Strings.ts: :50 'last {n} days', :57 'press incidents · 30 days'.
- i18n.ts EN :485-489 / ES :730-734 hold commune_news_*; the interface is at :243-247.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FRESH-01 Latest-incident stamp + stale notice, and R-01 outage gap bars, on /news/ and /es/noticias/</name>
  <files>site/src/config/i18n.ts, site/src/pages/news.astro, site/src/pages/es/noticias.astro</files>
  <action>
Step 0, precondition (R1/R-14): `grep -c '^- \[x\] \*\*Phase 34' .planning/ROADMAP.md` must print 1. If it does not, STOP without editing and report "G-19: Phase 34 not closed". Execution mode (R1/R-12, G-21): run this plan ALONE (wave 3). It builds site/.

Add these i18n keys (interface plus both locales). The microcopy is decided here: neutral, with no safety verdict.
- news_latest_incident: EN 'Latest incident: {date}' / ES 'Último incidente: {date}'
- news_stale_notice: EN 'No new incidents have been added for more than 48 hours (newest incident dated {date}). The news feed may be delayed, so the list below is not current.' / ES 'No se han agregado incidentes nuevos en más de 48 horas (el más reciente es del {date}). El flujo de noticias puede estar retrasado, por lo que la lista de abajo no está al día.'
- news_stale_notice_nodate (R2/C-R1-07, neutral: the 'none' case has no evidence to date): EN 'No incident data is available in the current build; the news feed may be delayed.' / ES 'No hay datos de incidentes disponibles en esta versión del sitio; el flujo de noticias puede estar retrasado.'
- news_gap_bar_label (R-01): EN 'no data collected' / ES 'sin datos recolectados'
- news_gap_caption (R-01, knob G-21(3) as amended by G-22(a): {from}/{to} = a contiguous gap-bucket span): EN 'No incidents were collected from {from} to {to} because of a classification outage in our pipeline; those days are not zero-incident days.' / ES 'No se recolectaron incidentes entre el {from} y el {to} por una falla de clasificación en nuestro pipeline; esos días no son días sin incidentes.' {from}/{to} are long dates (en-US / es-CL, UTC).

In BOTH news.astro and es/noticias.astro (edit both; they are duplicated per locale by design):
1. Keep the parsed `raw` object as `payload`, defaulting to {} on a missing or malformed file.
2. Delete `generated`, `formattedDate` and the "Updated"/"Actualizado" `<time>`. Knob G-20(1): the build stamp is REMOVED, not relabelled.
3. Compute:
   - `buildNowMs = Date.now()`;
   - `latest = latestIncidentDate(payload)`;
   - `verdict = evidenceVerdict(computeEvidence(payload), buildNowMs, MAX_AGE_HOURS)`;
   - `showStale = verdict === 'stale' || verdict === 'none'`.
   Per R1/R-07, 'future' is NOT a visitor-facing stale notice: freshness.mjs and the heartbeat already flag future evidence red. "Now" is the Astro build wall clock (G-20(2)).
4. Markup, in place of :239-244:
   - `<p class="freshness" data-latest-incident={latest ?? ''} data-build-now={new Date(buildNowMs).toISOString()}>`;
   - when latest is set, `<time datetime={latest}>{news_latest_incident, {date} = long date}</time>`, formatted en-US / es-CL, month 'long', timeZone 'UTC';
   - then, as a SIBLING placed after the closing `</p>` of .freshness and never nested inside it (C-R1-06), `{showStale && (<p class="news-stale-notice" role="status" data-stale-hours="48">{latest ? news_stale_notice(date substituted) : news_stale_notice_nodate}</p>)}`.
5. Style .news-stale-notice with a left border using an existing token, padding, and font-size var(--text-label). Add NO `display` property.
6. R1/R-01, histogram:
   - For buckets with `b.gap`, add `data-gap="true"` to the `.day-bar` button, and render the news-sr text as `{dayLabel(b.date)} · {t.news_gap_bar_label}` instead of `· 0`.
   - Style `.day-bar[data-gap="true"] .day-bar-fill` with a striped background (repeating-linear-gradient on existing tokens) and a fixed small visible height. Add no display property.
   - R2/F35-R1-04 (G-22(a), amends G-21(3)): compute the contiguous SPANS of buckets with `gap === true`, where a span is a maximal run of consecutive days. Render one `<p class="day-gap-caption" data-gap-from="YYYY-MM-DD" data-gap-to="YYYY-MM-DD">{news_gap_caption}</p>` per span, directly under the `.day-axis` paragraph. {from}/{to} are the span's first and last gap-bucket dates, and NEVER the declared COVERAGE_GAPS range: a real incident on 09-22 makes the span, and the caption, end on 09-21. Substitute server-side, so no literal {from}/{to} survives. With no gap buckets, render no caption.
7. Do not touch the facet row (news_window_latest) or the client filter script. Gap bars keep data-day, so the existing day filter works unchanged.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && grep -c 'Latest incident: ' dist/news/index.html && grep -c 'Último incidente: ' dist/es/noticias/index.html && grep -c 'data-build-now=' dist/news/index.html dist/es/noticias/index.html && ! grep -q '>Updated ' dist/news/index.html && ! grep -q '>Actualizado ' dist/es/noticias/index.html && ! grep -qE '\{(from|to)\}' dist/news/index.html dist/es/noticias/index.html && grep -o 'class="news-stale-notice"' dist/news/index.html dist/es/noticias/index.html | wc -l && grep -o 'data-gap="true"' dist/news/index.html | wc -l && grep -o 'data-gap="true"' dist/es/noticias/index.html | wc -l && node scripts/validate/facets.mjs && node scripts/validate/forbidden-language.mjs && node scripts/validate/hreflang.mjs && { npx astro check > .astro-check.log 2>&1; rc=$?; tail -3 .astro-check.log; [ $rc -eq 0 ]; }</automated>
    Expected:
    - "Latest incident: " and "Último incidente: " give 1 each, and data-build-now= gives 1 per file.
    - The `! grep` guards pass: no build stamp, and no unsubstituted {from}/{to}. The {n}/{date} tokens are policed by facets.mjs assertion 18.
    - The stale-notice count is 2 when the local evidence verdict is 'stale' or 'none', and 0 when 'fresh'. EN == ES. Record the case in the SUMMARY.
    - The EN and ES data-gap counts are equal. They equal the number of days in 2026-09-05..2026-09-22 that are inside the rendered window and have no incident. With local data frozen at 09-04 that is 0; with live data after 09-23 it is 18. Record it.
    - facets, forbidden-language and hreflang PASS; astro check reports 0 errors.
    This is ONE chained command (OneDrive).
  </verify>
  <done>Both news pages carry the data-derived latest-incident stamp, the helper-driven stale notice ('stale'/'none' only) and honest outage gap bars with a caption. The build stamp is gone, and the facets.mjs assertions are still green.</done>
</task>

<task type="auto">
  <name>Task 2: FRESH-03 caveated commune news heading (EN+ES)</name>
  <files>site/src/config/i18n.ts, site/src/components/CommuneNewsSection.astro</files>
  <action>
Add these i18n keys (interface plus both locales):
- commune_news_heading_stale (R2/F35-R1-05: no zero quantifier; the heading must not claim zero during an outage): EN 'Earlier Incidents in the News' / ES 'Incidentes Anteriores en la Prensa'
- commune_news_stale_note (R1/R-06; no claim about geolocation or the pipeline): EN 'The newest report listed for {name} was published on {date}; none of the listed reports was published in the last 7 days.' / ES 'La nota más reciente de la lista para {name} se publicó el {date}; ninguna de las notas listadas se publicó en los últimos 7 días.'
- commune_news_aria_stale: EN 'Incidents reported in the news' / ES 'Incidentes reportados en la prensa'

In CommuneNewsSection.astro, after `items` (:110):
- `buildNowMs = Date.now()`;
- `stale = items.length > 0 && evidenceVerdict(computeEvidence({ incidents: items }), buildNowMs, COMMUNE_NEWS_STALE_HOURS) === 'stale'`.

Per R1/R-07, 'future' (a newest item mis-dated into tomorrow UTC) is NOT stale. The rule is 168h (7 days, REQUIREMENTS FRESH-03) on the G-05 fallback evidence max(date)+1d of the newest DISPLAYED item, against the build clock (G-20(3)).

Markup:
- the section gets `data-news-stale={stale ? 'true' : 'false'}`, `data-build-now={ISO}`, and aria-label = stale ? commune_news_aria_stale : commune_news_aria;
- the h2 = stale ? commune_news_heading_stale : commune_news_heading;
- when stale, add `<p class="cnews-stale-note">` directly after the h2, with {date} = the newest item's long date (en-US / es-CL, UTC) and {name} = communeName, substituted server-side.

Style .cnews-stale-note like .cnews-intro, without display. Keep zero client JS (D-09).
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && grep -l 'class="cnews-section"' dist/commune/*/index.html | wc -l && grep -l 'class="cnews-section"' dist/es/comuna/*/index.html | wc -l && grep -l 'data-news-stale="true"' dist/commune/*/index.html | wc -l && grep -l 'data-news-stale="true"' dist/es/comuna/*/index.html | wc -l && ! grep -rlE 'has been geolocated in|no ha geolocalizado' dist/commune dist/es/comuna && ! grep -rlE 'none in the last 7 days|ninguno en los últimos 7 días' dist/commune dist/es/comuna && node scripts/validate/forbidden-language.mjs && node scripts/validate/commune.mjs && { npx astro check > .astro-check.log 2>&1; rc=$?; tail -3 .astro-check.log; [ $rc -eq 0 ]; }</automated>
    Expected:
    - The EN and ES cnews-section counts are equal and ≥ 1 (research measured 112).
    - The EN and ES stale counts are equal. With data frozen at 09-04, stale == section count; with live data they may differ. Record the numbers.
    - Both `! grep` guards pass (R2/F35-R1-01, F35-R1-05). The first matches only the old stale-NOTE wording ('has been geolocated in' / 'no ha geolocalizado'). commune_news_intro ('geolocated in {name}', i18n.ts:486/731) is untouched and must not be matched. The second proves that no zero-quantifier heading ships.
    - forbidden-language and commune PASS; astro check reports 0 errors.
    The exact-state assertion is 35-06's harness.
  </verify>
  <done>The commune news heading switches to the caveated form, with a dated and accurate note, whenever its newest listed item is more than 7 days old. EN/ES parity holds, and there is no client JS.</done>
</task>

<task type="auto">
  <name>Task 3: R-03 home news pulse — coverage clip, gap days, static stale notice (EN+ES)</name>
  <files>site/src/components/home/HomeNewsPulse.astro, site/src/config/homeV2Strings.ts</files>
  <action>
Add to homeV2Strings.ts (both locales):
- `news_gap_label`: EN 'no data collected' / ES 'sin datos recolectados' (the same copy as i18n news_gap_bar_label).

In HomeNewsPulse.astro frontmatter (build time):
- Read site/public/data/incidents/current.json via `path.resolve(process.cwd(), 'public/data/incidents/current.json')` (memory "News page build path drift"), using the existsSync/try pattern and defaulting to {}.
- Compute `verdict = evidenceVerdict(computeEvidence(payload), Date.now(), MAX_AGE_HOURS)` with `showStale = verdict === 'stale' || verdict === 'none'` (R-07), and `latest = latestIncidentDate(payload)`.
- Import COVERAGE_GAPS from ../../lib/newsCoverageGaps.

Markup:
- On `.pulse-card` add `data-coverage-gaps={JSON.stringify(COVERAGE_GAPS)}`, `data-gap-label={t.news_gap_label}` and `data-build-now={ISO}` (C-R1-05, for the 35-07 prod check). Pass server data only through data-*, never through {expr} inside <script> (memory "Astro <script> no expr interpolation").
- When showStale, render inside the card, after `.pulse-head`, `<p class="pulse-stale" role="status">` with the SAME copy as i18n news_stale_notice (or news_stale_notice_nodate), imported from EN_STRINGS/ES_STRINGS, with {date} substituted server-side. Give .pulse-stale no display rule. Known limit (A-R1-09, documented and accepted): the card stays [hidden] when there are no incidents (:164), so a 'none' pulse notice is never visible. Only the 'stale' case is visible on the home page, and /news/ carries the 'none' notice. The card-level `.pulse-card[hidden]` belt (:192) stays.

In render() (:89-108):
- (a) Start `days` at max(shiftDays(anchor,-29), the minimum valid date of `all`), the same clip as computeDayBuckets. `all` is sorted desc, so the min is the last element's date after the LPX-8 filter; skip malformed dates.
- (b) Parse `JSON.parse(card.dataset.coverageGaps || '[]')` once, in a try/catch that falls back to []. A day is a gap iff it lies inside a declared range AND no incident in `all` (not in `pool`) has that date. Gap bars get class `pulse-bar pulse-bar--gap`, a fixed small height, and `title = dayLabel(d) + ' · ' + card.dataset.gapLabel`.
- Add a `:global()` style for `.pulse-bar--gap` using a striped background (LPX-1: client-created nodes need :global under the card).
- Do not change the fetch, the LPX-8 clip, the chips or the list.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build && grep -c 'data-coverage-gaps=' dist/index.html dist/es/index.html && grep -o 'class="pulse-stale"' dist/index.html dist/es/index.html | wc -l && node scripts/validate/forbidden-language.mjs && node scripts/validate/map.mjs && { npx astro check > .astro-check.log 2>&1; rc=$?; tail -3 .astro-check.log; [ $rc -eq 0 ]; }</automated>
    Expected:
    - data-coverage-gaps gives 1 per home page.
    - The pulse-stale count is 2 when the local verdict is 'stale' or 'none', and 0 when 'fresh' (the same case as Task 1). Record it.
    - forbidden-language and map (the content-page React guard) PASS; astro check reports 0 errors.
    The absolute frozen/fresh assertion is 35-06 Task 2.
  </verify>
  <done>The home pulse no longer pads before coverage, marks the declared outage days as "no data collected", and carries a build-time stale notice in both locales.</done>
</task>


<task type="auto" tdd="true">
  <name>Task 4: R2/F35-R1-06 map NewsStrip — coverage clip + gap days (G-22(b))</name>
  <files>site/src/lib/newsStripDays.ts, site/src/lib/newsStripDays.test.ts, site/src/components/map/NewsStrip.tsx, site/src/components/map/MapIsland.tsx, site/src/config/mapV2Strings.ts</files>
  <behavior>
    - computeStripDays('2026-09-04', 30, prodDates(08-24..09-04)) gives 12 days, 08-24..09-04, all gap=false.
    - The same dates plus 09-23..09-25 with anchor '2026-09-25' gives days from 08-27 (anchor−29) to 09-25; 09-05..09-22 have gap=true.
    - Adding a real 09-22 date gives gap=false on 09-22.
    - Malformed dates ('bad', '') never become the start. anchor '' gives []. gaps [] gives no gap days.
    - Gap is decided on the UNFILTERED dates: a family filter with no incident on 09-10, while another family has one, still gives gap=false on 09-10.
  </behavior>
  <action>
Create site/src/lib/newsStripDays.ts, pure:
`computeStripDays(anchor: string, windowDays: number, unfilteredDates: string[], gaps: CoverageGap[] = COVERAGE_GAPS): { date: string; gap: boolean }[]`.
- start = max(anchor − (windowDays − 1), min valid YYYY-MM-DD of unfilteredDates), using UTC string arithmetic like newsDayFacets addDays.
- gap = the date is inside a range AND is absent from unfilteredDates.

Write newsStripDays.test.ts from the behavior list.

MapIsland.tsx (:435-443 area): add `const stripDays = useMemo(() => incidentsFile ? computeStripDays(incidentsAnchor, incidentsFile.window_days, incidentsFile.incidents.map(i => i.date)) : [], [incidentsFile, incidentsAnchor])`, and pass `days={stripDays}` to NewsStrip (:580-591).

NewsStrip.tsx:
- Add the prop `days: { date: string; gap: boolean }[]`.
- DELETE the internal range computation at :68-81, including the :72-73 windowStart extension and the floor cap.
- Map over `days`. A gap day gets className `news-strip-bar gap` and aria-label `${dayLabel} : ${v2.news_strip_gap}`. Bar counts still come from the family-filtered `incidents`.
- Add a `.news-strip-bar.gap` striped style next to the existing strip CSS. Add no display rule.

mapV2Strings.ts: add `news_strip_gap`: EN 'no data collected' / ES 'sin datos recolectados' (interface plus both locales).

Keep one-plan-at-a-time sequencing (G-21(1)). Known residual: the header count may include incidents dated before the clipped bar range (the old MPX-C2 note). The count is unchanged, and only pre-coverage empty bars disappear.
  </action>
  <verify>
    <automated>cd "/c/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx vitest run src/lib/newsStripDays.test.ts && ! grep -n "windowStart < minDate" src/components/map/NewsStrip.tsx && npm run build && node scripts/validate/map.mjs && { npx astro check > .astro-check.log 2>&1; rc=$?; tail -3 .astro-check.log; [ $rc -eq 0 ]; }</automated>
    Expected: vitest passes; the `! grep` succeeds, so the :72-73 extension is gone; the build passes; map.mjs PASS; astro check rc 0 with "0 errors". Negative control: make computeStripDays ignore the min date, and the 12-day test must FAIL. Revert.
  </verify>
  <done>The map news strip no longer pads before coverage, and it marks outage days as "no data collected", in EN/ES.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| current.json (pipeline; LLM-processed press text) → static HTML / client pulse | incident dates and names rendered at build and in the pulse enhancer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Tampering / XSS | date, communeName and gap substitution | mitigate | Only regex-validated YYYY-MM-DD dates reach Intl. communeName comes from data/cead/meta/index.json. The gap JSON is a code constant. Astro escapes attributes and text, and no set:html is added. The pulse keeps textContent-only insertion (T-03-03-02). |
| T-35-10 | Repudiation | Misleading recency or zero-coverage | mitigate | The notice decision uses the shared G-05 helper. Outage days are labelled "no data collected", not 0. Copy makes no safety verdict, and forbidden-language.mjs gates the build. |
| T-35-SC | Tampering | installs | accept | none |

Accepted (premortem, no edit):
- R-17: UTC publication dates are the site-wide convention, so notices can fire up to ~24h late and never falsely.
- R-18: the G-05 "added" evidence may keep the notice off while the newest date is older, but the displayed "Latest incident" date is still true.
</threat_model>

<verification>
From site/: `npm run build && npm run validate; npx vitest run`. B-R1-05: vitest runs even if freshness is red, and each rc is read separately. Every validator is PASS except freshness, which reflects the live data (35-07 policy). astro check reports 0 errors.
</verification>

<success_criteria>
FRESH-01, FRESH-03 and the FRESH-02 twins (outage gaps, home pulse) are rendered honestly in both locales, driven by the single shared helper and the declared gap list.
</success_criteria>

<output>
Create `.planning/phases/35-honest-freshness-signals/35-04-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

| Risk | Applied where |
|---|---|
| R-01 | must_haves truth; Task 1 i18n keys (news_gap_bar_label, news_gap_caption), action step 6 (data-gap, striped bar, caption), verify (data-gap count) |
| R-03 | files_modified (+HomeNewsPulse.astro, homeV2Strings.ts); must_haves truth + artifact + key_link; new Task 3 |
| R-06 | must_haves truth; Task 2 commune_news_stale_note wording; verify guard against the old wording |
| R-07 | must_haves truths; Task 1 step 3 (`'stale' \|\| 'none'`); Task 2 (`=== 'stale'`); Task 3 (same as Task 1) |
| R-12 | frontmatter wave 3; Task 1 action "Execution mode" |
| R-14 | Task 1 action Step 0 |
| R-17, R-18 | threat_model "Accepted" note |

Knobs: see 35-01 "Knobs added (R1)" (G-21).

## Revision R2 (gate R1)

| Item | Applied where |
|---|---|
| Condition 1 (F35-R1-01) | Task 2 verify: guard now `'has been geolocated in\|no ha geolocalizado'` (old note wording only; commune_news_intro untouched) + Expected note |
| Condition 4 (F35-R1-04, G-22(a)) | Task 1 i18n news_gap_caption note + action step 6: one caption per contiguous gap span, with data-gap-from/-to |
| Condition 5 (F35-R1-05) | Task 2 i18n commune_news_heading_stale = 'Earlier Incidents in the News' / 'Incidentes Anteriores en la Prensa'; verify `! grep` for the zero-quantifier headings |
| Condition 6 (F35-R1-06, G-22(b), option a) | files_modified (+newsStripDays.ts/.test.ts, NewsStrip.tsx, MapIsland.tsx, mapV2Strings.ts); must_haves truth; new Task 4 (pure helper + vitest, :72-73 extension removed, gap days) |
| C-R1-06 (non-blocker) | Task 1 step 4: .news-stale-notice is a sibling after </p> of .freshness |
| C-R1-07 (non-blocker) | Task 1 i18n news_stale_notice_nodate: neutral copy |
| A-R1-09 (non-blocker) | Task 3 action: documented that the 'none' pulse notice is invisible (card hidden) |
| C-R1-05 (non-blocker, part) | Task 3 markup: data-build-now on .pulse-card |
| B-R1-05 (non-blocker) | all verifies: astro check rc captured; <verification>: vitest separated |
