---
phase: 10-high-resolution-commune-geometry
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
  - .planning/phases/10-high-resolution-commune-geometry/10-HUMAN-UAT.md
autonomous: false
requirements: [GEO-01, GEO-02]

must_haves:
  truths:
    - "The site methodology page (EN + ES) credits the chilemapas boundary source with its GPL-3 license"
    - "A HUMAN-UAT gate documents the two intrinsic manual checks: visual recognizability and throttled-mobile pan/zoom/hover perf"
  artifacts:
    - path: "site/src/pages/methodology.astro"
      provides: "EN boundary-source attribution credit (chilemapas, GPL-3, INE/SUBDERE/BCN)"
      contains: "chilemapas"
    - path: "site/src/pages/es/metodologia.astro"
      provides: "ES boundary-source attribution credit (parity with EN)"
      contains: "chilemapas"
    - path: ".planning/phases/10-high-resolution-commune-geometry/10-HUMAN-UAT.md"
      provides: "Manual-gate instructions for visual + mobile-perf verification"
  key_links:
    - from: "site/src/pages/methodology.astro"
      to: "site/src/pages/es/metodologia.astro"
      via: "equivalent attribution content in both locales (READ/parity requirement)"
      pattern: "chilemapas"
---

<objective>
Add the visible boundary-source attribution credit (GPL-3 + good practice, LOCKED) to the site methodology pages in both locales, and create the HUMAN-UAT gate document for the two intrinsic manual verifications that no automated check can cover: (1) polygons read as recognizable comuna shapes on `/map/` and `/es/mapa/`, and (2) smooth pan/zoom/hover with a clean console on a throttled-mobile profile.

Purpose: GPL-3 compliance + good attribution practice for the chilemapas swap (CONTEXT: "a visible credit in the site methodology/colophon"). The HUMAN-UAT gate closes GEO-01 Success-Criterion-2 (visual recognizability) and GEO-02's perf criterion — both are intrinsically manual (no "looks like a real comuna" or "feels smooth" assertion exists). This plan is `autonomous: false` because it ends on a blocking human-verify checkpoint.
Output: edited `site/src/pages/methodology.astro` + `site/src/pages/es/metodologia.astro`, new `10-HUMAN-UAT.md`, and a completed human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md
@.planning/phases/10-high-resolution-commune-geometry/10-VALIDATION.md
@.planning/phases/10-high-resolution-commune-geometry/10-02-SUMMARY.md

<interfaces>
<!-- methodology.astro (EN) and es/metodologia.astro (ES) are EditorialLayout pages built from <section><h2>…</h2><p>…</p></section> blocks. The EN page's first content section is "Data source: CEAD". The crime stats come from CEAD; the geometry boundaries are a SEPARATE source (chilemapas) — the credit is a new short sub-note, not a claim that CEAD provides the map shapes. -->
<!-- chilemapas attribution facts (RESEARCH + CONTEXT): source = chilemapas (github.com/pachadotdev / pacha.dev), license GPL-3, derived from INE/SUBDERE/BCN official boundaries. EPSG:4326. -->
<!-- ES page must mirror EN content/structure/tone (Phase-9 parity requirement). Use the existing <section> CSS pattern already in both files — no new component. -->
<!-- Phase-3 manual perf gate was Moto G4 + Fast 3G; reuse that device class for the throttled-mobile profile. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chilemapas boundary-source attribution to methodology (EN + ES)</name>
  <read_first>
    - site/src/pages/methodology.astro (the file being modified — "Data source: CEAD" section, lines 48-83)
    - site/src/pages/es/metodologia.astro (the file being modified — ES counterpart section)
    - .planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md ("Boundary Source (LOCKED)" → "visible credit in the site methodology/colophon")
    - .planning/phases/10-high-resolution-commune-geometry/10-02-SUMMARY.md (confirm the exact source name/URL/license used in the build-script header for consistency)
  </read_first>
  <action>
    Add a short boundary-source attribution to both methodology pages, mirroring each other. In site/src/pages/methodology.astro, add a concise paragraph (or a small "Map boundaries" sub-note within the "Data source: CEAD" section, or a dedicated short section right after it) crediting the commune boundary geometry to **chilemapas** (github.com/pachadotdev/chilemapas / pacha.dev), licensed **GPL-3**, derived from official **INE / SUBDERE / BCN** boundaries — and clarifying that the map shapes (geometry) come from this source while the crime statistics come from CEAD. Add the equivalent paragraph to site/src/pages/es/metodologia.astro in Spanish, matching content, structure, and tone (Phase-9 parity rule). Use the existing `<section>`/`<p>` markup and CSS already in each file — do NOT introduce a new component or touch any other section. Keep wording sober and factual (no "safe/dangerous" language). Match the exact source name/URL/license text to whatever the Plan-02 build-script header documents so the on-site credit and the script attribution agree.
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/map.mjs — build is green AND both dist/methodology/index.html and dist/es/metodologia/index.html contain the string "chilemapas"</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/pages/methodology.astro` and `site/src/pages/es/metodologia.astro` each contain a "chilemapas" + "GPL-3" attribution mentioning INE/SUBDERE/BCN
    - `cd site && npm run build` exits 0 (no Astro/TS errors)
    - Built `dist/methodology/index.html` and `dist/es/metodologia/index.html` both contain "chilemapas" (grep)
    - No "peligroso"/"seguro"/"dangerous"/"safe"-absolute phrasing introduced; no other methodology section altered
  </acceptance_criteria>
  <done>Both locales visibly credit the chilemapas boundary source with its GPL-3 license, satisfying attribution; site still builds green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the HUMAN-UAT gate document for the two manual checks</name>
  <read_first>
    - .planning/phases/10-high-resolution-commune-geometry/10-VALIDATION.md ("Manual-Only Verifications" table — the two gates + instructions)
    - .planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md ("Verification (LOCKED)" → manual HUMAN-UAT gates)
    - .planning/phases/10-high-resolution-commune-geometry/10-02-SUMMARY.md (measured raw/gzip sizes to record as the "what shipped" baseline)
    - $HOME/.claude/projects/.../memory/browseros-review-gotchas.md (BrowserOS review runs inline; mobile via 375px iframe — for the perf-check method)
  </read_first>
  <action>
    Create `.planning/phases/10-high-resolution-commune-geometry/10-HUMAN-UAT.md` documenting the two intrinsic manual gates. Gate 1 (GEO-01 visual recognizability): instruct the human to open `/map/` and `/es/mapa/` in a real browser, compare the rendered polygons to the prior blocky simplification, and confirm coastlines and inter-commune borders read as real comuna shapes; include a few spot-check comunas (e.g. coastal Valparaíso region, Santiago metro cluster, an island/oceanic comuna like Isla de Pascua or Juan Fernández) to confirm tiny shapes survived `keep-shapes`. Gate 2 (GEO-02 perf): instruct a throttled-mobile profile run (Moto G4 + Fast 3G, per the Phase-3 gate) — pan, zoom, and hover the map; confirm no jank and a clean console (no new errors) on both `/map/` and `/es/mapa/`. Record the measured raw/gzip asset sizes from the Plan-02 SUMMARY as the shipped baseline. Include a sign-off checklist with explicit pass/fail boxes for each gate and a "resume signal" instruction. Note (from MEMORY) that the browser review runs inline and mobile can be emulated via a 375px viewport/iframe.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-high-resolution-commune-geometry/10-HUMAN-UAT.md and it contains both "recogniz" (or "recognizable") and "Fast 3G" / "throttled" gate references</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/10-high-resolution-commune-geometry/10-HUMAN-UAT.md` exists
    - It documents BOTH gates with concrete steps (URLs `/map/` + `/es/mapa/`, spot-check comunas, throttled-mobile profile, console check)
    - It records the shipped asset's measured raw + gzip sizes (from Plan-02 SUMMARY)
    - It has a per-gate pass/fail sign-off checklist
  </acceptance_criteria>
  <done>The manual-gate doc exists with executable instructions for both intrinsic checks and a sign-off checklist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies recognizable geometry + smooth mobile perf</name>
  <what-built>
    The map now ships chilemapas-sourced commune boundaries (regenerated `communes.topo.json`, recognizable fidelity at visvalingam 10% / quantization 10000) rendered through the unchanged Phase-3 native `L.geoJSON()` + `L.canvas()` path. Methodology pages credit the source. `10-HUMAN-UAT.md` holds the step-by-step gate instructions.
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/10-high-resolution-commune-geometry/10-HUMAN-UAT.md`:
    1. Start the dev server (`cd site && npm run dev`) and open `/map/` and `/es/mapa/`.
    2. Gate 1 — Visual: confirm polygons read as real comuna shapes (coastline + inter-commune borders) vs the prior blocky render; spot-check the listed comunas including a tiny island/oceanic one.
    3. Gate 2 — Perf: with a throttled-mobile profile (Moto G4 + Fast 3G, or a 375px viewport with throttling), pan/zoom/hover the map; confirm no jank and zero new console errors on both pages.
    4. Confirm the choropleth still colours every comuna with CEAD data and the commune panel still opens on click (no regression to the join or render path).
  </how-to-verify>
  <resume-signal>Type "approved" if both gates pass, or describe the visual/perf/console issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editorial content → published site | Methodology attribution text is author-controlled static content |
| shipped asset → browser (runtime) | The new geometry rendered in the live map island (verified by the human gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-LICENSE | (Legal, not STRIDE) | methodology attribution | mitigate | Visible GPL-3 chilemapas credit added to both locales completes the attribution obligation begun in the Plan-02 script header — Task 1 |
| T-10-05 | Tampering | regenerated asset in the live render path | mitigate | Human-verify gate confirms the shipped geometry renders correctly and the choropleth join + commune panel still work (no silent mis-join past the automated 346/0/0/0 gate) — Task 3 |
| T-10-DoS | Denial of Service (client) | MapIsland.tsx fetch/decode | accept | Existing `safeFetch` + `loadError` graceful path unchanged; the human-verify console check confirms no new runtime errors |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan; `npm run build` uses existing deps only |
</threat_model>

<verification>
- `cd site && npm run build && node scripts/validate/map.mjs` green; both built methodology pages contain "chilemapas".
- `10-HUMAN-UAT.md` exists with both gates + sign-off.
- Human checkpoint: both gates approved (recognizable geometry + smooth throttled-mobile perf + clean console + no choropleth/panel regression).
</verification>

<success_criteria>
- Boundary source credited (GPL-3 chilemapas) in EN + ES methodology, with parity (GEO-01/GEO-02 attribution).
- Manual-gate doc executable; the two intrinsic checks documented and signed off (GEO-01 SC-2 visual, GEO-02 perf).
- Confirmed in browser: choropleth colours every CEAD comuna, commune panel opens, zero new console errors, smooth pan/zoom/hover (GEO-02 SC-3/SC-4).
</success_criteria>

<output>
Create `.planning/phases/10-high-resolution-commune-geometry/10-03-SUMMARY.md` when done.
</output>
