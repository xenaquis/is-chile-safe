---
phase: 02-astro-site-programmatic-pages
plan: 05
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - site/src/pages/crime/[family].astro
  - site/src/pages/es/delito/[family].astro
  - site/scripts/validate/crime.mjs
autonomous: true
requirements: [PAGES-03]
must_haves:
  truths:
    - "7 EN crime-type pages (/crime/{family}/) + 7 ES (/es/delito/{family}/) = 14 pages build, one per crime family (D-10/D-11 national-only)"
    - "Each crime-type page shows a national ranking of communes for that family by rate per 100k (low_population excluded)"
    - "Featured families (vida, propiedad) receive prioritized treatment (D-12)"
    - "Crime-type URL slugs are translated per locale (D-17) and pages are NOT batch-gated"
  artifacts:
    - path: "site/src/pages/crime/[family].astro"
      provides: "EN crime-type pages with national commune ranking"
      contains: "getStaticPaths"
    - path: "site/src/pages/es/delito/[family].astro"
      provides: "ES crime-type pages mirroring EN with translated slugs"
      contains: "getStaticPaths"
  key_links:
    - from: "site/src/pages/crime/[family].astro"
      to: "site/src/config/i18n.ts FAMILY_SLUGS"
      via: "getStaticPaths over 7 family keys -> EN slug params"
      pattern: "FAMILY_SLUGS|getFamilySlug"
    - from: "site/src/pages/crime/[family].astro"
      to: "all commune JSONs by_family"
      via: "build-time national family ranking computation"
      pattern: "by_family"
---

<objective>
Generate the bilingual crime-type pages (PAGES-03): 7 crime families × 2 locales = 14 pages, each a national ranking of communes for that family with a family trend and regional breakdown. National-only in Phase 2 (D-11) to avoid the family×region page explosion. URL slugs are translated per locale (D-17). Featured families (vida, propiedad) get prioritized treatment (D-12). Not batch-gated (D-26).

Purpose: Crime-type pages are the third SEO page tree, targeting "{crime type} en Chile" style queries, and reserve a map-placeholder slot for the Phase 3 filtered-map view (D-13).
Output: `/crime/{family}/` and `/es/delito/{family}/` pages + crime validation assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md
@.planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md
@.planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md
@.planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md
@site/src/lib/data.ts
@site/src/config/i18n.ts
@site/src/layouts/BaseLayout.astro

<interfaces>
<!-- From 02-01 / 02-02. -->
data/cead/meta/catalog.json: family_keys order ["vida","robos_violentos","vif","drogas","armas","propiedad","incivilidades"]; families[key].featured true only for vida + propiedad
site/src/config/i18n.ts FAMILY_SLUGS (D-17 translated URL slugs):
  vida -> {en:'homicide', es:'homicidios'}
  robos_violentos -> {en:'violent-robbery', es:'robos-violentos'}
  vif -> {en:'domestic-violence', es:'violencia-intrafamiliar'}
  drogas -> {en:'drug-crimes', es:'drogas'}
  armas -> {en:'weapons', es:'armas'}
  propiedad -> {en:'property', es:'propiedad'}
  incivilidades -> {en:'disorder', es:'incivilidades'}
site/src/lib/data.ts: loadIndex(), loadCommune(cut) (+ latestCompleteYear), loadRegion(id), loadNational(), getFamilySlug(key, locale)
Components: CommuneRankingTable (or NationalRankingTable variant), FamilyBreakdownBars (region breakdown reuse), MapPlaceholderSlot, MethodologyCaveat

Crime-type page section order (UI-SPEC Page Type 3, lines 150-160): Header, CrimeTypeHero (h1 localized family name + definition sentence), NationalRankingTable (all non-low-pop communes sorted by this family's rate per 100k; columns Commune, Region, Rate per 100k, Trend), RegionBreakdownBars (16 regions' aggregate rate for this family), MapPlaceholderSlot(crime-type-map), MethodologyCaveat, Footer.
</interfaces>

<derivations>
- National family ranking: iterate loadIndex(); for each !low_population commune, loadCommune(cut) and read by_family[familyKey] at latestCompleteYear; sort descending; that is the national ranking table (UI-SPEC notes ~346 rows ~40KB HTML is acceptable, all rows shown, no JS pagination). This is the only place a per-family national ranking is computed (national.json has no per-commune ranking).
- Region breakdown bars: for each of the 16 regions, loadRegion(id) and read by_family[familyKey] at the region's latestCompleteYear; render with the CSS-percentage bar pattern.
- featured family (vida, propiedad) prominence (D-12): e.g. a featured badge in the hero + ordering hint; the featured flag comes from catalog.families[key].featured.
- The "vida" family maps to the homicide URL slug — confirm h1 family name copy uses the localized family label from i18n, while the URL segment uses FAMILY_SLUGS.
</derivations>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EN + ES crime-type page routes with national family ranking</name>
  <files>site/src/pages/crime/[family].astro, site/src/pages/es/delito/[family].astro</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (Page Type 3 section order lines 150-160; crime-type h1 copy line 213; Schema.org Dataset-only for crime pages line 288)
    - .planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md (D-10 7 families, D-11 national-only 14 pages, D-12 featured, D-13 map slot, D-17 translated slugs)
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pattern 1 getStaticPaths lines 257-329; Pitfall 5 latest-complete-year)
    - data/cead/meta/catalog.json, data/cead/comunas/13101.json (by_family shape)
    - The <interfaces> and <derivations> blocks in this plan
  </read_first>
  <action>
    Create `site/src/pages/crime/[family].astro`. getStaticPaths(): for each of the 7 catalog family_keys, return { params: { family: getFamilySlug(key,'en') }, props: { familyKey: key } } (NO rollout gate — D-26, all 7 build). Body: compute the national family ranking per <derivations> (iterate loadIndex, exclude low_population, read by_family[familyKey] at each commune's latestCompleteYear, sort desc); compute the 16-region breakdown for this family; enPath=`/crime/${getFamilySlug(familyKey,'en')}/`, esPath=`/es/delito/${getFamilySlug(familyKey,'es')}/`; build Dataset-only JSON-LD (crime pages emit Dataset, NOT Place — UI-SPEC line 288). Render BaseLayout lang='en', EN title/description, mountType='crime-type-map'. Compose UI-SPEC Page Type 3 order: CrimeTypeHero (localized family name from EN_STRINGS family labels + a sober definition sentence; featured badge when catalog featured===true, D-12), NationalRankingTable (Commune, Region, Rate per 100k, Trend — all non-low-pop communes; commune name links to /commune/{slug}/), RegionBreakdownBars (16 regions), MapPlaceholderSlot, MethodologyCaveat. Zero client: directives.
    Create `site/src/pages/es/delito/[family].astro` mirroring it: getStaticPaths emits ES slug params via getFamilySlug(key,'es'); lang='es'; ES_STRINGS family labels + ES definition; commune links to /es/comuna/{slug}/; same enPath/esPath for reciprocity; ES Dataset JSON-LD.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <verify>
    <automated>cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - After build, dist/crime contains exactly 7 dirs and dist/es/delito contains exactly 7 (14 total)
    - EN dirs use the translated slugs (e.g. property, homicide, violent-robbery); ES dirs use propiedad, homicidios, robos-violentos
    - Each crime page renders a national ranking <table> sorted by that family's rate, excluding low_population communes
    - Featured families (vida, propiedad) render a featured marker in the hero
    - Each page has reciprocal hreflang + self-canonical + a Dataset JSON-LD block (no Place)
    - `cd site && npm run build` exits 0
  </acceptance_criteria>
  <done>14 crime-type pages build (un-gated) with translated slugs, national family ranking, region breakdown, featured prominence, and reciprocal hreflang.</done>
</task>

<task type="auto">
  <name>Task 2: Crime-type validator</name>
  <files>site/scripts/validate/crime.mjs</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-VALIDATION.md (crime task 2-03-01 line 48)
    - .planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md (D-11 14 pages, D-17 slugs)
    - site/src/config/i18n.ts (FAMILY_SLUGS), data/cead/meta/catalog.json
  </read_first>
  <action>
    Create `site/scripts/validate/crime.mjs`: assert dist/crime contains exactly 7 dirs and dist/es/delito contains exactly 7; assert the EN dir names exactly match the 7 FAMILY_SLUGS .en values and the ES dir names match the .es values (catches slug-mapping drift); for a sample page (e.g. crime/property), assert the index.html contains a ranking <table> with multiple data rows and a Dataset JSON-LD script (and assert it does NOT contain a Place @type, per UI-SPEC line 288). Exit non-zero on failure.
  </action>
  <verify>
    <automated>cd site && npm run build</automated>
  </verify>
  <verify>
    <automated>cd site && node scripts/validate/crime.mjs</automated>
  </verify>
  <acceptance_criteria>
    - crime.mjs asserts 7 EN + 7 ES crime dirs whose names match FAMILY_SLUGS en/es values
    - crime.mjs asserts a sample crime page has a ranking table + Dataset JSON-LD and no Place type
    - `node site/scripts/validate/crime.mjs` exits 0 against a build
  </acceptance_criteria>
  <done>Crime-type page count, translated-slug correctness, ranking table, and Dataset-only JSON-LD are machine-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-1 commune/region JSON -> rendered ranking HTML/JSON-LD | CEAD per-family aggregates iterated and rendered at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | crime-type Dataset JSON-LD | mitigate | Emitted via JSON.stringify in BaseLayout; family/commune names JSON-escaped |
| T-02-14 | Tampering | family slug param from FAMILY_SLUGS | mitigate | Slugs are a fixed config table (D-17), not user input; getStaticPaths enumerates only the 7 known keys |
| T-02-15 | Information disclosure | crime-type pages | accept | Render only public CEAD aggregates; no secrets/PII |
</threat_model>

<verification>
- `cd site && npm run build` exits 0
- `node site/scripts/validate/crime.mjs` exits 0
- dist/crime and dist/es/delito each contain 7 dirs with the correct translated slugs
</verification>

<success_criteria>
- 7×2 crime-type pages build (un-gated) with national family ranking + region breakdown + featured prominence (PAGES-03)
- Translated slugs (D-17), Dataset-only JSON-LD, reciprocal hreflang
- crime validator green
</success_criteria>

<output>
Create `.planning/phases/02-astro-site-programmatic-pages/02-05-SUMMARY.md` when done
</output>
