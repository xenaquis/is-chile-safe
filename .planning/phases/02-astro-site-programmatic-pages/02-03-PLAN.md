---
phase: 02-astro-site-programmatic-pages
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - site/src/lib/proseEngine.ts
  - site/src/pages/commune/[slug].astro
  - site/src/pages/es/comuna/[slug].astro
  - site/scripts/validate/commune.mjs
  - site/scripts/validate/rollout.mjs
autonomous: true
requirements: [PAGES-01, PAGES-04]
must_haves:
  truths:
    - "Each built commune page (EN and ES) contains 500+ words and all 5 canonical data dimensions (vs-national, vs-regional, trend, dominant family, comparable commune)"
    - "Default build produces exactly the 12 rollout communes per locale; ROLLOUT_ALL=true produces all 346 per locale (true 404 for non-rollout)"
    - "EN and ES commune routes use the SAME rollout CUT gate so hreflang partners always both exist"
    - "Prose is deterministic, combinatorially varied across trend/rank/comparison branches, and uses sober language (no forbidden terms)"
  artifacts:
    - path: "site/src/lib/proseEngine.ts"
      provides: "deterministic 5-dimension prose variation engine (EN + ES)"
      exports: ["buildCommuneProse"]
    - path: "site/src/pages/commune/[slug].astro"
      provides: "EN commune pages with full section layout"
      contains: "getStaticPaths"
    - path: "site/src/pages/es/comuna/[slug].astro"
      provides: "ES commune pages mirroring EN with same rollout gate"
      contains: "getStaticPaths"
  key_links:
    - from: "site/src/pages/commune/[slug].astro"
      to: "site/src/lib/data.ts loadRolloutCuts()"
      via: "getStaticPaths rollout gate"
      pattern: "loadRolloutCuts|rollout"
    - from: "site/src/pages/commune/[slug].astro"
      to: "site/src/lib/proseEngine.ts"
      via: "buildCommuneProse(data, 'en')"
      pattern: "buildCommuneProse"
---

<objective>
Generate the bilingual commune pages — the SEO core of the site (PAGES-01) — gated by the rollout batch allowlist (PAGES-04). Build the deterministic prose variation engine (D-01/D-02) that interpolates real Phase-1 data into combinatorially-varied 500+ word prose across both locales, then compose the full commune page layout for EN and ES.

Purpose: These pages capture programmatic SEO traffic. Thin/duplicate content risks Google deindexing (SEO risk #1) — the variation engine plus 5 unique data dimensions per commune mitigates this. The rollout gate ships 12 communes first (346 on env flag) without code changes.
Output: `/commune/{slug}/` and `/es/comuna/{slug}/` pages, plus commune + rollout validation assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md
@.planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md
@.planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md
@.planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md
@site/src/lib/data.ts
@site/src/config/i18n.ts
@site/src/layouts/BaseLayout.astro

<interfaces>
<!-- Available from plans 02-01 and 02-02. -->
site/src/lib/data.ts: loadIndex(), loadRolloutCuts(), loadCommune(cut) -> commune + latestCompleteYear + regionName, loadRegion(id), loadNational(), nearestComparable(cut)
site/src/config/i18n.ts: EN_STRINGS, ES_STRINGS (all Element-Level Copy rows), FAMILY_SLUGS
site/src/layouts/BaseLayout.astro props: { lang, title, description, enPath, esPath, jsonLd, mountType }
Components (02-02): LevelChip, TrendChip, StatCard, Sparkline, FamilyBreakdownBars, ComparisonCallout, ComparableCommune, MapPlaceholderSlot, MethodologyCaveat

Commune page section order (UI-SPEC Page Type 1, lines 119-135): Header, CommuneHero (h1 + region breadcrumb + LevelChip + population), KeyStatsRow (3 StatCards: latest-complete-year total rate, national rank N of 346, regional rank), SparklineSection, ComparisonCallouts (vs-national + vs-regional), CrimeFamilyBreakdown (7 families), ComparableCommune, MapPlaceholderSlot(commune-map), ProseSummary (500+ words), MethodologyCaveat, Footer.
</interfaces>

<derived_values>
<!-- The 5 PAGES-01 dimensions, all computed at build time from loaded data: -->
1. vs-national: commune latest-complete total rate vs loadNational() latest-complete total rate -> signed %
2. vs-regional: commune total rate vs loadRegion(region_id) latest-complete total rate -> signed %
3. trend: commune.trend ('up'|'down'|'stable')
4. dominant family: argmax of by_family at latestCompleteYear (and secondary = 2nd highest)
5. comparable commune: nearestComparable(cut)
Use latestCompleteYear (NOT the 2026 partial) for all rate/family/comparison figures. The 2026 partial bar appears only in the Sparkline at 50% opacity.
</derived_values>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic prose variation engine (EN + ES) + commune validator</name>
  <files>site/src/lib/proseEngine.ts, site/scripts/validate/commune.mjs</files>
  <behavior>
    - buildCommuneProse(commune, 'en') returns >= 500 words for a typical commune (e.g. Santiago 13101)
    - Output names the comparable commune, states vs-national and vs-regional comparisons with signs, the trend, and the dominant + secondary crime family
    - Two communes on different (trend x rankTier x vsNational) tiers produce materially different opening sentences (not one template with swapped numbers)
    - A low_population commune's output includes the statistical-volatility caveat sentence
    - Output contains none of the forbidden terms ("zona peligrosa", "ranking definitivo", "zona segura garantizada", unqualified "the safest")
  </behavior>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pattern 5 branch dimensions lines 456-469)
    - .planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md (proseEngine lines 375-402; Sober Language Contract lines 711-715)
    - .planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md (D-01..D-06 content generation; D-08 comparable)
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (Copywriting Contract voice + forbidden terms lines 187-234)
    - The <derived_values> block in this plan
  </read_first>
  <action>
    Create `site/src/lib/proseEngine.ts` exporting buildCommuneProse(commune, locale). Implement the RESEARCH Pattern 5 branch structure: derive trend (3 branches), nationalRankTier (top10/top25/mid/bottom25/bottom10 from national_rank vs 346 -> 5 branches), vsNationalSign+magnitudeBand (above/below x >50% / 20-50% / <20% -> 6 branches), vsRegionalSign+magnitudeBand (6 branches), dominantFamily (7 keys with secondary-family phrasing), and a lowPopulation caveat inserted/omitted. Maintain PARALLEL hand-written EN and ES sentence banks (D-03 — never machine-translate). Assemble 4-6 paragraphs naming: the commune + region, the latest-complete-year rate per 100k, national + regional rank, vs-national and vs-regional comparison, trend over 2005-latest, dominant + secondary family, the comparable commune by name with its rate, and (when low_population) the volatility caveat. Target 500+ words. Sober wording only (D-05). All figures from latestCompleteYear, never the 2026 partial.
    Fill `site/scripts/validate/commune.mjs` (replace the 02-01 stub): for each built commune index.html under dist/commune/ and dist/es/comuna/, strip tags, assert word count >= 500, and assert the 5 dimension markers are present (vs-national callout, vs-regional callout, trend chip, family-breakdown heading, comparable-commune heading). Exit non-zero on first failure. Support a --self-test mode that imports proseEngine and asserts buildCommuneProse(loadCommune('13101'),'en') and ('...,'es') both exceed 500 words and contain no forbidden term, so the engine can be checked before pages build.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <verify>
    <automated>cd site && node scripts/validate/commune.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - proseEngine.ts exports buildCommuneProse and contains separate EN and ES sentence banks (no translation call/library import)
    - commune.mjs --self-test confirms EN and ES prose for 13101 each exceed 500 words and contain no forbidden term
    - proseEngine.ts source does NOT contain "zona peligrosa", "ranking definitivo", or "zona segura garantizada"
    - site/scripts/validate/commune.mjs asserts >=500 words and 5 dimension markers and exits non-zero on failure
    - `cd site && npx astro check` exits 0
  </acceptance_criteria>
  <done>Prose engine emits 500+ word, combinatorially varied, sober bilingual prose from real data; commune validator enforces the contract.</done>
</task>

<task type="auto">
  <name>Task 2: EN + ES commune page routes with full layout composition</name>
  <files>site/src/pages/commune/[slug].astro, site/src/pages/es/comuna/[slug].astro</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pattern 1 getStaticPaths lines 257-329; Pitfall 2 hreflang reciprocity lines 513-521; Pitfall 5 latest-complete-year lines 535-541)
    - .planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md (commune route lines 432-468)
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (Page Type 1 section order lines 119-135; commune title/description meta lines 259-262; Schema.org JSON-LD lines 266-286)
    - The <interfaces> and <derived_values> blocks in this plan
  </read_first>
  <action>
    Create `site/src/pages/commune/[slug].astro`. getStaticPaths(): read loadRolloutCuts() (NOT the full index — the rollout gate, D-24/Pitfall 2) and map each CUT to { params: { slug }, props: { cut } }. In the body: data = loadCommune(cut); compute the 5 derived values per <derived_values> (vs-national via loadNational(), vs-regional via loadRegion(data.region_id), dominant+secondary family from by_family at latestCompleteYear, comparable via nearestComparable(cut)); enPath=`/commune/${data.slug}/`, esPath=`/es/comuna/${data.slug}/`; build the Dataset+Place JSON-LD per UI-SPEC lines 266-286 (name, description, url, spatialCoverage with regionName, temporalCoverage 2005/latestCompleteYear, measurementTechnique, creator CEAD). Render BaseLayout lang='en' with EN title/description (UI-SPEC 259-262), enPath/esPath/jsonLd/mountType='commune-map'. Compose the UI-SPEC Page Type 1 section order exactly: CommuneHero, KeyStatsRow (3 StatCards), Sparkline (full series incl partial 2026 bar), two ComparisonCallout pills, FamilyBreakdownBars (by_family at latestCompleteYear), ComparableCommune, MapPlaceholderSlot, buildCommuneProse(data,'en'), MethodologyCaveat. Labels from EN_STRINGS. Zero client: directives.
    Create `site/src/pages/es/comuna/[slug].astro` mirroring it: IDENTICAL getStaticPaths rollout gate (same loadRolloutCuts() CUT list — guarantees hreflang reciprocity, Pitfall 2), lang='es', ES_STRINGS labels, ES title/description meta, buildCommuneProse(data,'es'), the ES JSON-LD variant (Spanish name/description), same enPath/esPath so canonical+hreflang stay reciprocal.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <verify>
    <automated>cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Both route files contain getStaticPaths reading loadRolloutCuts() (not loadIndex directly for the gate)
    - After a default build, dist/commune has exactly 12 commune dirs and dist/es/comuna has exactly 12
    - Each built page renders all UI-SPEC Page Type 1 sections (hero, 3 stat cards, sparkline svg, 2 callouts, 7 family bars, comparable, map slot, prose, methodology)
    - Neither route file contains a client: directive
    - `cd site && npm run build` exits 0
  </acceptance_criteria>
  <done>EN and ES commune pages build under the rollout gate with full layout, real data, JSON-LD, and reciprocal hreflang.</done>
</task>

<task type="auto">
  <name>Task 3: Rollout gate validator (batch count + ROLLOUT_ALL parity)</name>
  <files>site/scripts/validate/rollout.mjs</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-VALIDATION.md (rollout task 2-01-02 line 46; Per-Task map)
    - .planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md (D-24, D-25, D-26 rollout gate)
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pitfall 2 hreflang reciprocity lines 513-521)
    - site/src/config/rollout.json, site/src/lib/data.ts
  </read_first>
  <action>
    Fill `site/scripts/validate/rollout.mjs` (replace 02-01 stub). It inspects the existing dist/ output (does not itself rebuild) and asserts: (a) when run in default mode, dist/commune contains exactly the rollout.json enabled count (12) of commune dirs AND dist/es/comuna contains the same count (parity — Pitfall 2: both locales gated symmetrically); (b) every EN commune slug under dist/commune has a matching ES dir under dist/es/comuna and vice versa (reciprocity); (c) regions and crime-type dirs are NOT gated (presence checked loosely — they are validated in their own plans). Provide an explicit message naming any missing partner. The script reads an env hint (e.g. EXPECT_ALL=true) to instead assert 346 per locale for the ROLLOUT_ALL build. Exit non-zero on any mismatch.
  </action>
  <verify>
    <automated>cd site && npm run build</automated>
  </verify>
  <verify>
    <automated>cd site && node scripts/validate/rollout.mjs</automated>
  </verify>
  <acceptance_criteria>
    - rollout.mjs asserts dist/commune count === dist/es/comuna count === rollout.json enabled length (12) in default mode
    - rollout.mjs asserts EN<->ES slug parity and names any missing partner
    - rollout.mjs supports an EXPECT_ALL mode asserting 346 per locale
    - `node site/scripts/validate/rollout.mjs` exits 0 against a default build
  </acceptance_criteria>
  <done>Rollout batch gate and EN/ES hreflang-partner parity are machine-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-1 commune JSON -> rendered HTML + JSON-LD | CEAD aggregate data interpolated into prose, SVG, and structured data at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | commune JSON-LD block | mitigate | JSON-LD emitted via JSON.stringify in BaseLayout (from plan 02-02); commune values are JSON-escaped, never string-concatenated |
| T-02-08 | Information disclosure | non-rollout commune pages | mitigate | D-26 — non-rollout communes are a true 404 (not built, not noindex); rollout.mjs verifies only the 12 are emitted, protecting crawl budget and avoiding accidental disclosure of unreviewed pages |
| T-02-09 | Tampering | prose forbidden-language drift | mitigate | commune.mjs --self-test asserts no forbidden term in generated prose; sober-language contract enforced at the engine source level (EDIT-05 enforcing build gate lands in Phase 4) |
</threat_model>

<verification>
- `cd site && npm run build` exits 0 (default rollout)
- `node site/scripts/validate/commune.mjs` and `rollout.mjs` exit 0
- dist/commune and dist/es/comuna each contain exactly 12 dirs; EN/ES partners reciprocal
- commune.mjs --self-test confirms 500+ word sober prose in both locales
</verification>

<success_criteria>
- proseEngine emits 500+ word combinatorially-varied bilingual prose with all 5 dimensions (PAGES-01)
- EN + ES commune pages build under the rollout gate; 12 default / 346 with ROLLOUT_ALL (PAGES-04)
- hreflang partners reciprocal; commune + rollout validators green
</success_criteria>

<output>
Create `.planning/phases/02-astro-site-programmatic-pages/02-03-SUMMARY.md` when done
</output>
