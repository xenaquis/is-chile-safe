---
phase: 04-editorial-pages-adsense
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-03]
files_modified:
  - site/src/pages/index.astro
  - site/src/pages/is-chile-safe.astro
  - site/src/pages/is-santiago-safe.astro
  - site/src/pages/valparaiso-safety.astro
  - site/src/pages/vina-del-mar-safety.astro
  - site/src/pages/concepcion-safety.astro
autonomous: true
requirements: [EDIT-01]
must_haves:
  truths:
    - "The EN home (/) and five EN non-map editorial pages render 800-1500 words of sober, sourced editorial prose with a single H1 and H2 sections"
    - "Question-style pages (/is-chile-safe/, /is-santiago-safe/) emit FAQPage JSON-LD and show a static FAQ block"
    - "Data callouts on city pages reflect real CEAD rates loaded via loadCommune/loadNationalAverage (never the SUM)"
    - "Every page passes the forbidden-language gate and has reciprocal hreflang"
  artifacts:
    - path: "site/src/pages/is-chile-safe.astro"
      provides: "EDIT-01 /is-chile-safe/ FAQ editorial page"
      min_lines: 40
    - path: "site/src/pages/is-santiago-safe.astro"
      provides: "EDIT-01 /is-santiago-safe/ FAQ editorial page with Santiago data callouts"
      min_lines: 40
    - path: "site/src/pages/valparaiso-safety.astro"
      provides: "EDIT-01 /valparaiso-safety/ city editorial page"
      min_lines: 40
  key_links:
    - from: "site/src/pages/is-santiago-safe.astro"
      to: "loadCommune('13101')"
      via: "build-time data callout"
      pattern: "loadCommune"
    - from: "site/src/pages/is-chile-safe.astro"
      to: "FAQPage jsonLd"
      via: "BaseLayout jsonLd prop"
      pattern: "FAQPage"
---

<objective>
Author the EN home page and five EN non-map editorial pages (EDIT-01) using EditorialLayout (Plan 03): `/` (extend existing), `/is-chile-safe/`, `/is-santiago-safe/`, `/valparaiso-safety/`, `/vina-del-mar-safety/`, `/concepcion-safety/`. Each is ~800-1500 words of sober, sourced editorial prose (D-01/D-02) with a single H1, H2 sections, DataCallouts backed by real CEAD data, and (for the two question-style pages) a FAQBlock + FAQPage JSON-LD. The map-bearing pages (`/chile-crime-map/`, `/santiago-safety-map/`), `/safest-cities-in-chile/`, and `/methodology/` are in Plan 05; all ES pages in Plan 06; legal pages in Plan 07.

Purpose: The high-value EN SEO hero pages, authored against the live forbidden-language gate so absolute-safety language can never ship.
Output: 1 extended page (index.astro) + 5 new EN editorial pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-editorial-pages-adsense/04-CONTEXT.md
@.planning/phases/04-editorial-pages-adsense/04-PATTERNS.md
@.planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md
@.planning/phases/04-editorial-pages-adsense/04-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 (consumed here): -->
EditorialLayout Props: { lang:'en'|'es'; title; description; enPath; esPath; jsonLd?:object|null }  // wraps BaseLayout, 720px column, gated ads+consent
DataCallout Props: { value:string; label:string; source:string; locale:'en'|'es' }
FAQBlock Props: { items:{ q:string; a:string }[]; locale:'en'|'es' }   // page builds FAQPage jsonLd and passes to layout

<!-- Data API (site/src/lib/data.ts) — CRITICAL: use loadNationalAverage(), NOT loadNational().series[].rate_per_100k (SUM): -->
loadCommune(cut:string): CommuneData            // .latestCompleteYear, .trend, .national_rank, .regionName, series[]
loadNationalAverage(): number                   // MEAN per-capita rate — use for the national comparison
latestCompleteYearRate(commune): number
// CUT codes: Santiago '13101', Valparaíso '5101', Viña del Mar '5109', Concepción '8101'  (verify against data/cead/meta/index.json before use)

<!-- hreflang pairing (REQUIREMENTS.md authoritative; UI-SPEC slug map): -->
// /  <-> /es/ ; /is-chile-safe/ <-> /es/delitos-por-comuna/ ;
// /is-santiago-safe/ has NO ES pair in EDIT-02 -> set esPath = enPath ('/is-santiago-safe/') so x-default points to EN (see FLAG below)
// /valparaiso-safety/ <-> /es/seguridad-valparaiso/ ; /vina-del-mar-safety/ <-> /es/seguridad-vina-del-mar/ ; /concepcion-safety/ <-> /es/seguridad-concepcion/
</interfaces>

<slug_flag>
FLAG (Research Open Question 2): REQUIREMENTS.md EDIT-02 does NOT contain an ES equivalent of `/is-santiago-safe/` (it lists `/es/delitos-por-region/` instead). Do NOT invent `/es/seguridad-santiago/`. For `/is-santiago-safe/`, set `esPath` equal to its own `enPath` so hreflang x-default resolves to the EN page; the ES Santiago topic is served by `/es/mapa-seguridad-santiago/` (Plan 06). Use REQUIREMENTS.md EDIT-01/EDIT-02 slugs verbatim — they are authoritative over the UI-SPEC illustrative map.
</slug_flag>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EN home (extend index.astro) + is-chile-safe.astro (FAQ)</name>
  <files>site/src/pages/index.astro, site/src/pages/is-chile-safe.astro</files>
  <read_first>
    - site/src/pages/index.astro (EXISTING placeholder — extend, do not discard the hero/links; replace the "coming soon" note with real editorial content)
    - site/src/layouts/EditorialLayout.astro (Plan 03 — wrapper Props + slots)
    - site/src/components/FAQBlock.astro + DataCallout.astro (Plan 03)
    - site/src/lib/data.ts (loadNationalAverage, loadCommune — SUM-vs-MEAN pitfall header)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (Template 1; meta tags table; FAQPage schema; Copywriting Contract — qualified superlatives)
    - .planning/REQUIREMENTS.md (EDIT-01 slugs)
  </read_first>
  <action>
    index.astro: switch the wrapper from BaseLayout to EditorialLayout (lang="en", enPath="/", esPath="/es/", title/description per UI-SPEC home row). Keep the existing hero + commune/region/crime quick-links; replace the "coming soon" placeholder with ~800-1000 words of sober editorial prose: what the site is, what CEAD data represents, how to read rates per 100k, links into the map and key pages. Add 1-2 DataCallouts for the national average rate via `loadNationalAverage()` (label "National average rate per 100k — CEAD {year}", source "Source: CEAD"). Single H1, H2 sections. No FAQ on home.

    is-chile-safe.astro (/is-chile-safe/): EditorialLayout, esPath="/es/delitos-por-comuna/". ~1000-1300 words answering the title question soberly using national-level CEAD framing (never an absolute "safe/dangerous" verdict). Include a FAQBlock with 4-6 plain-text Q+A items (e.g. "Is Chile safe for tourists?", "How is crime measured here?", "Which data source is used?"). Build the FAQPage JSON-LD object in the page frontmatter from those items and pass it as `jsonLd` to EditorialLayout. Add DataCallout(s) for the national average rate. Any superlative must carry the "according to CEAD data for {year}" qualifier (Copywriting Contract).
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/forbidden-language.mjs && node -e "const{readFileSync}=require('fs');const h=readFileSync('dist/is-chile-safe/index.html','utf8');if(/application\/ld\+json/.test(h)&&/FAQPage/.test(h)){console.log('PASS faq jsonld')}else{console.error('FAIL: FAQPage JSON-LD missing on is-chile-safe');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/index.html` and `dist/is-chile-safe/index.html` build successfully.
    - `node scripts/validate/forbidden-language.mjs` exits 0 for the whole dist/.
    - `dist/is-chile-safe/index.html` contains a `<script type="application/ld+json">` with `"@type":"FAQPage"`.
    - is-chile-safe.astro uses `loadNationalAverage()` (not `loadNational().series`) for the national rate callout.
    - is-chile-safe `esPath="/es/delitos-por-comuna/"` (verbatim EDIT-02 slug).
  </acceptance_criteria>
  <done>Home extended with editorial prose + national callout; is-chile-safe is a FAQ page emitting FAQPage JSON-LD; both pass the forbidden-language gate.</done>
</task>

<task type="auto">
  <name>Task 2: is-santiago-safe.astro (FAQ + Santiago data) + valparaiso-safety.astro</name>
  <files>site/src/pages/is-santiago-safe.astro, site/src/pages/valparaiso-safety.astro</files>
  <read_first>
    - site/src/pages/is-chile-safe.astro (Task 1 — FAQ + jsonLd pattern to mirror)
    - site/src/components/DataCallout.astro + FAQBlock.astro (Plan 03)
    - site/src/lib/data.ts (loadCommune CUT codes; latestCompleteYearRate; .trend; .national_rank)
    - data/cead/meta/index.json (confirm CUT codes for Santiago / Valparaíso before hardcoding)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (Template 1; meta tags; Copywriting Contract; slug map note)
    - .planning/REQUIREMENTS.md (EDIT-01)
  </read_first>
  <action>
    is-santiago-safe.astro (/is-santiago-safe/): EditorialLayout, lang="en". Per the slug_flag above, set `esPath="/is-santiago-safe/"` (self — no ES pair in EDIT-02). ~1000-1300 words. Load Santiago via `loadCommune('13101')`; surface DataCallouts for Santiago's latest rate (`latestCompleteYearRate`), its position vs `loadNationalAverage()`, and trend direction — all labeled "CEAD {year}" and sourced. FAQBlock with 4-6 plain-text Santiago-specific Q+A; build + pass FAQPage jsonLd. Sober framing — comparative claims qualified with "according to CEAD data for {year}".

    valparaiso-safety.astro (/valparaiso-safety/): EditorialLayout, esPath="/es/seguridad-valparaiso/". ~900-1200 words. Load Valparaíso via `loadCommune` (confirm CUT from index.json). DataCallouts for rate vs national + trend. No FAQ required (not a question-style page) — emit WebPage JSON-LD per UI-SPEC instead (about: CEAD Dataset). Single H1 + H2 sections.
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/forbidden-language.mjs && node -e "const{readFileSync}=require('fs');const s=readFileSync('dist/is-santiago-safe/index.html','utf8');const v=readFileSync('dist/valparaiso-safety/index.html','utf8');if(/FAQPage/.test(s)&&/CEAD/.test(v)){console.log('PASS')}else{console.error('FAIL: santiago FAQ or valpo CEAD callout missing');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/is-santiago-safe/index.html` and `dist/valparaiso-safety/index.html` build successfully and pass forbidden-language.
    - is-santiago-safe emits FAQPage JSON-LD and uses `loadCommune('13101')` + `loadNationalAverage()`.
    - is-santiago-safe `esPath` equals its own enPath (`/is-santiago-safe/`) — no invented ES slug.
    - valparaiso-safety surfaces a CEAD-sourced DataCallout and emits WebPage JSON-LD.
  </acceptance_criteria>
  <done>Santiago FAQ page with real data callouts; Valparaíso editorial page with sourced data; both sober and gate-passing.</done>
</task>

<task type="auto">
  <name>Task 3: vina-del-mar-safety.astro + concepcion-safety.astro</name>
  <files>site/src/pages/vina-del-mar-safety.astro, site/src/pages/concepcion-safety.astro</files>
  <read_first>
    - site/src/pages/valparaiso-safety.astro (Task 2 — non-FAQ city page pattern to mirror)
    - site/src/lib/data.ts (loadCommune; latestCompleteYearRate; loadNationalAverage)
    - data/cead/meta/index.json (confirm CUT codes for Viña del Mar / Concepción)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (Template 1; meta tags; slug map; Copywriting Contract)
    - .planning/REQUIREMENTS.md (EDIT-01)
  </read_first>
  <action>
    vina-del-mar-safety.astro (/vina-del-mar-safety/): EditorialLayout, esPath="/es/seguridad-vina-del-mar/". ~900-1200 words. Load Viña del Mar via `loadCommune` (confirm CUT). DataCallouts for rate vs national + trend, CEAD-sourced. WebPage JSON-LD. Single H1 + H2 sections.

    concepcion-safety.astro (/concepcion-safety/): EditorialLayout, esPath="/es/seguridad-concepcion/". ~900-1200 words. Load Concepción via `loadCommune` (confirm CUT). DataCallouts + WebPage JSON-LD. Sober framing; qualified superlatives only.
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/forbidden-language.mjs && node scripts/validate/hreflang.mjs && node -e "const{existsSync}=require('fs');if(existsSync('dist/vina-del-mar-safety/index.html')&&existsSync('dist/concepcion-safety/index.html')){console.log('PASS')}else{process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/vina-del-mar-safety/index.html` and `dist/concepcion-safety/index.html` exist and pass forbidden-language + hreflang validators.
    - Each uses `loadCommune` (correct CUT) + `loadNationalAverage()` for a CEAD-sourced DataCallout.
    - esPath values match EDIT-02 verbatim (`/es/seguridad-vina-del-mar/`, `/es/seguridad-concepcion/`).
  </acceptance_criteria>
  <done>Viña del Mar + Concepción editorial pages with sourced data, reciprocal hreflang, gate-passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build -> dist/ | Editorial prose + data callouts rendered at build time; forbidden-language gate scans output |
| page -> BaseLayout jsonLd | FAQPage/WebPage JSON-LD via stringify+set:html (XSS-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Repudiation | editorial prose makes an absolute safety claim | mitigate | EDIT-05 forbidden-language gate runs per build; qualified-superlative Copywriting Contract |
| T-04-10 | Tampering | wrong national rate (SUM vs MEAN) misleads readers | mitigate | Use `loadNationalAverage()` only; never `loadNational().series[].rate_per_100k` (data.ts header) |
| T-04-06 | Tampering | FAQPage JSON-LD XSS | mitigate | Plain-object + stringify via BaseLayout set:html; plain-text answers |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit empty) |
</threat_model>

<verification>
- `cd site && npm run build && node scripts/validate/all.mjs` — full 9-validator suite green.
- forbidden-language exits 0 across all dist/ pages including these 6.
- hreflang reciprocity holds (except the flagged `/is-santiago-safe/` which self-references esPath per the slug_flag).
- Single chained build+validate command (OneDrive desync — Pitfall 6).
- <human-check> (deferred to phase UAT per human_verify_mode=end-of-phase): spot-check 3 of these pages — prose authoritative + editorially sober (no absolute verdicts); each DataCallout rate/trend matches data/cead/; word count 800-1500.</human-check>
</verification>

<success_criteria>
- 6 EN pages (1 extended + 5 new) render 800-1500 words of sober, sourced prose.
- Two question-style pages emit FAQPage JSON-LD with static FAQ blocks.
- Data callouts use loadCommune/loadNationalAverage (MEAN), CEAD-sourced and year-labeled.
- All pass forbidden-language + hreflang.
</success_criteria>

<output>
Create `.planning/phases/04-editorial-pages-adsense/04-04-SUMMARY.md` when done
</output>
