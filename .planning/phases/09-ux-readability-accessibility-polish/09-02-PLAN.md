---
phase: 09-ux-readability-accessibility-polish
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - site/src/components/map/Legend.tsx
  - site/src/components/map/MapIsland.tsx
  - site/src/components/map/ResultPanel.tsx
  - site/src/components/map/PanelFamilyBars.tsx
  - site/src/pages/map.astro
  - site/src/pages/es/mapa.astro
autonomous: true
requirements: [UX-02, A11Y-01]
must_haves:
  truths:
    - "The map legend shows, per colour band, a real numeric rate range (e.g. '0 – 1,500') and a qualitative label (Very low → Very high) plus a per-100k note"
    - "The commune panel shows a '?' tooltip explaining 'rate per 100,000' next to the rate figure"
    - "The commune panel expresses standing as a neutral multiplier vs the national average ('1.8× the national average') with a CSS comparison bar — never 'safe/dangerous'"
    - "The map island receives the national average as a build-time prop (no fs call in the browser)"
  artifacts:
    - path: "site/src/components/map/Legend.tsx"
      provides: "Numeric bands + qualitative labels (D-03)"
      contains: "breaks"
    - path: "site/src/components/map/ResultPanel.tsx"
      provides: "Rate tooltip (D-01) + multiplier/comparison bar (D-04)"
      contains: "nationalAvg"
    - path: "site/src/pages/map.astro"
      provides: "Build-time loadNationalAverage threaded to the island"
      contains: "loadNationalAverage"
  key_links:
    - from: "site/src/pages/map.astro"
      to: "site/src/lib/data.ts"
      via: "loadNationalAverage() in frontmatter, passed as nationalAvg prop"
      pattern: "loadNationalAverage"
    - from: "site/src/components/map/MapIsland.tsx"
      to: "site/src/components/map/Legend.tsx"
      via: "<Legend lang breaks={breaks} />"
      pattern: "breaks"
    - from: "site/src/components/map/ResultPanel.tsx"
      to: "site/src/components/map/RateTooltipReact.tsx"
      via: "import + render next to rate"
      pattern: "RateTooltip"
---

<objective>
Make the interactive map readable for a non-expert: numeric+qualitative legend bands (D-03), an inline "?" tooltip explaining "rate per 100k" (D-01), and a neutral multiplier-vs-average comparison bar in the commune panel (D-04). All on the existing React island — no new `client:*` directives.

Purpose: This is the user-driven core of the phase ("the map is too hard to read"). It directly closes UX-02 (each surface communicates one clear idea with its CEAD source) and contributes to A11Y-01 (the tooltip is keyboard/touch accessible via <details>). Depends on Plan 01 for the RateTooltip React component and the i18n strings.
Output: extended Legend, ResultPanel, PanelFamilyBars, MapIsland; build-time prop threading in map.astro + es/mapa.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ux-readability-accessibility-polish/09-CONTEXT.md
@.planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md
@.planning/phases/09-ux-readability-accessibility-polish/09-PATTERNS.md
@.planning/phases/09-ux-readability-accessibility-polish/09-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (already built) and existing island code -->

RateTooltip React component (Plan 01): site/src/components/map/RateTooltipReact.tsx
  export function RateTooltip({ label, tip, lang }: { label: string; tip: string; lang: 'en'|'es' })

i18n keys available (Plan 01): rate_tooltip_title, rate_tooltip_body, comparison_national (contains {multiplier}),
  legend_qual_1..5, legend_per_100k_note. Import from site/src/config/i18n.ts (EN_STRINGS/ES_STRINGS).

colors.ts (existing):
  export const INCIDENCE_COLORS: string[]  (5 stops, level1→level5)
  export function computeQuantileBreaks(sorted: number[], classes = 5): number[]  // returns 4 thresholds

Legend.tsx current signature (to extend): interface Props { lang: 'en' | 'es'; }

MapIsland.tsx: function MapIsland({ lang }: Props) — line 74; holds payloadRef.current = payload.comunas
  (each has rate:number, level:1..5); renders <Legend lang={lang} /> at line ~410 and
  <ResultPanel cut lang year onClose /> at line ~417.

ResultPanel.tsx: function ResultPanel({ cut, lang, year, onClose }: Props) — line 98.
  Rate available as `rate` (line 166); displayed via toLocaleString('es-CL') (line 167);
  `level` computed lines 184-191; stat-unit block lines 235-239; national_rank at data.national_rank.

data.ts: export function loadNationalAverage(): number  // fs-backed, BUILD-TIME ONLY (Pitfall 1)

map.astro currently mounts <MapIsland client:only="react" lang="en" /> (line ~37);
es/mapa.astro is the ES mirror.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Legend numeric bands + qualitative labels (D-03) and thread breaks from MapIsland</name>
  <files>site/src/components/map/Legend.tsx, site/src/components/map/MapIsland.tsx</files>
  <read_first>
    - site/src/components/map/Legend.tsx (full — current 5-swatch render)
    - site/src/components/map/colors.ts (INCIDENCE_COLORS, computeQuantileBreaks)
    - site/src/components/map/MapIsland.tsx (lines 90-100 state block; lines 144-210 data load where payloadRef.current is set; line ~410 <Legend> render; the year-filter effect ~215-230)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (Pattern 2 + extended Legend example lines 561-611)
    - .planning/phases/09-ux-readability-accessibility-polish/09-PATTERNS.md (Legend.tsx assignment — note the threading correction: breaks are computed from the rate array, NOT pulled from ChoroplethLayer)
  </read_first>
  <behavior>
    - Legend accepts `breaks?: number[]` (4 thresholds). When breaks is provided, each of the 5 bands shows its numeric range: band 0 = "0 – {breaks[0]}", band i = "{breaks[i-1]+1} – {breaks[i]}", band 4 = "{breaks[3]+1}+", formatted with toLocaleString('es-CL'|'en-US').
    - Each band always shows a qualitative label from i18n legend_qual_1..5 (Very low…Very high / Muy baja…Muy alta).
    - Legend shows a per-100k unit note (legend_per_100k_note).
    - When breaks is undefined, the legend still renders qualitative labels (graceful degradation).
  </behavior>
  <action>
    Extend Legend.tsx Props to `{ lang: 'en' | 'es'; breaks?: number[] }`. Render 5 bands (one per INCIDENCE_COLORS entry): each band = a swatch (`aria-hidden="true"`) + qualitative label (from EN_STRINGS/ES_STRINGS legend_qual_1..5) + numeric range when breaks present. Compute the range with a `bandLabel(i)` helper exactly as RESEARCH lines 580-586: lo = i===0 ? 0 : breaks[i-1]+1, hi = breaks[i] ?? null, format via `Math.round(n).toLocaleString(lang==='es'?'es-CL':'en-US')`, last band shows "{lo}+". Add the per-100k note using legend_per_100k_note.
    In MapIsland.tsx: add `const [breaks, setBreaks] = useState<number[] | undefined>(undefined);` to the state block. After the initial payload loads (where `payloadRef.current = payload.comunas` is set) AND inside the year-filter effect after a new payload arrives, compute `setBreaks(computeQuantileBreaks(payload.comunas.map(c => c.rate).filter(r => r > 0), 5))` (import computeQuantileBreaks from './colors'). Change the render to `<Legend lang={lang} breaks={breaks} />`. NOTE per PATTERNS.md: do not try to read breaks out of ChoroplethLayer — compute them in MapIsland from the same rate array. The choropleth's initial colouring uses precomputed `level`, so the Legend breaks are a parallel display computation over `rate`.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Legend.tsx Props includes `breaks`: `grep -c "breaks" site/src/components/map/Legend.tsx` ≥ 2.
    - Legend renders 5 qualitative labels sourced from i18n (references legend_qual or QUAL_LABELS array of length 5).
    - MapIsland computes breaks via computeQuantileBreaks and passes them: `grep -c "computeQuantileBreaks" site/src/components/map/MapIsland.tsx` ≥ 1 and `grep -c "breaks={breaks}\|breaks=" site/src/components/map/MapIsland.tsx` ≥ 1.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The legend shows numeric ranges + qualitative labels + per-100k note; breaks are computed in MapIsland and threaded to Legend; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResultPanel rate tooltip (D-01) + multiplier/comparison bar (D-04); thread nationalAvg prop</name>
  <files>site/src/components/map/ResultPanel.tsx, site/src/components/map/MapIsland.tsx, site/src/pages/map.astro, site/src/pages/es/mapa.astro</files>
  <read_first>
    - site/src/components/map/ResultPanel.tsx (Props interface line 65-70; rate at 166; displayRate 167; level 184-191; stat-unit 235-239; national_rank usage ~251-253; existing aria-hidden conventions lines 223, 295)
    - site/src/components/map/RateTooltipReact.tsx (Plan 01 — the component to import)
    - site/src/components/map/MapIsland.tsx (Props interface line 54; function signature line 74; <ResultPanel> render line ~417)
    - site/src/pages/map.astro (frontmatter imports + <MapIsland> mount line ~37)
    - site/src/pages/is-santiago-safe.astro (lines 14-19 — canonical build-time loadNationalAverage usage to mirror)
    - site/src/lib/data.ts (loadNationalAverage lines ~182-191 — fs-backed, build-time only)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (Pattern 3 lines 301-340; Pitfall 1 lines 404-409; Pitfall 2 clipping lines 411-416)
  </read_first>
  <behavior>
    - ResultPanel gains a `nationalAvg: number` prop.
    - Next to the rate stat-unit, a RateTooltip renders with tip=rate_tooltip_body and an aria-labelled "?" trigger.
    - A multiplier is computed = (rate / nationalAvg) to 1 decimal; displayed via the comparison_national i18n string with {multiplier} substituted (e.g. "1.8× the national average" / "1,8× el promedio nacional"). Framing is strictly neutral — no "safe/dangerous/peligroso/seguro".
    - A CSS-only comparison bar shows the commune value vs an average marker (no chart library). The bar wrapper is aria-hidden; the text multiplier label is the readable signal.
    - nationalAvg is computed at build time in map.astro / es/mapa.astro and passed as a prop through MapIsland to ResultPanel. loadNationalAverage is NEVER called inside any React component (Pitfall 1).
  </behavior>
  <action>
    map.astro: add `import { loadNationalAverage } from '../lib/data.ts';` and `const nationalAvg = loadNationalAverage();` in frontmatter; change the mount to `<MapIsland client:only="react" lang="en" nationalAvg={nationalAvg} />`. Do the same in es/mapa.astro with `lang="es"` and the correct relative import depth (`../../lib/data.ts`).
    MapIsland.tsx: add `nationalAvg: number` to the Props interface (line ~54) and to the destructured signature (line 74); forward it to `<ResultPanel ... nationalAvg={nationalAvg} />` (line ~417).
    ResultPanel.tsx: add `nationalAvg: number` to Props (lines 65-70) and the destructure (line 98). Import RateTooltip from './RateTooltipReact'. In the stat-unit block (235-239) render `<RateTooltip lang={lang} label={EN_STRINGS/ES_STRINGS.rate_tooltip_title} tip={... rate_tooltip_body} />` after the unit text. Compute `const multiplier = (rate !== null && nationalAvg > 0) ? (rate / nationalAvg).toFixed(1) : null;` (use locale decimal: replace '.' with ',' for es display, or format via toLocaleString). When multiplier is non-null, render the comparison block from RESEARCH Pattern 3 lines 324-338: a `comparison-bar-wrapper` (aria-hidden="true") with an average marker at 33% and a commune fill of width `Math.min(rate/(nationalAvg*3),1)*100`% coloured `var(--s${level})`, plus a `comparison-bar-label` showing the comparison_national string with {multiplier} replaced. Add the comparison-bar CSS to ResultPanel's existing scoped style block (or global.css if that is where panel styles already live — check the file). Address Pitfall 2: if the tooltip body clips inside the panel, set `overflow: visible` on the `.stat-card` wrapper. Preserve all existing aria-hidden conventions on decorative markers.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - map.astro and es/mapa.astro both call loadNationalAverage and pass nationalAvg: `grep -c "loadNationalAverage" site/src/pages/map.astro` ≥ 1 and same for es/mapa.astro; `grep -c "nationalAvg" site/src/pages/map.astro` ≥ 1.
    - loadNationalAverage is NOT imported or called inside any map component: `grep -rn "loadNationalAverage" site/src/components/map/` returns nothing (Pitfall 1 — fs is build-time only).
    - ResultPanel imports RateTooltip and references nationalAvg + a multiplier: `grep -c "RateTooltip" site/src/components/map/ResultPanel.tsx` ≥ 1; `grep -c "nationalAvg" site/src/components/map/ResultPanel.tsx` ≥ 1; `grep -c "multiplier\|comparison_national" site/src/components/map/ResultPanel.tsx` ≥ 1.
    - No absolute-tone words introduced: `grep -ni "peligroso\|dangerous\|\\bsafe\b\|seguro" site/src/components/map/ResultPanel.tsx` returns nothing (the only allowed matches would be pre-existing neutral compounds; verify none are absolute safety labels).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The panel explains the rate via an accessible tooltip and shows a neutral multiplier + comparison bar fed by a build-time national average; no fs call in the browser; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Per-family glossary affordance in PanelFamilyBars (D-02)</name>
  <files>site/src/components/map/PanelFamilyBars.tsx</files>
  <read_first>
    - site/src/components/map/PanelFamilyBars.tsx (full — row labels lines 81-87; FAMILY_LABELS 43-51; aria-hidden accent-dot line 84)
    - site/src/lib/familyDefs.ts (Plan 01 — FAMILY_LABELS / FAMILY_DEFS / FAMILY_ORDER to dedupe against)
    - .planning/phases/09-ux-readability-accessibility-polish/09-PATTERNS.md (PanelFamilyBars assignment — per-family tooltip OR #anchor link to glossary, zero-JS)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (Bilingual Glossary Wiring — anchor cross-link lines 651-655)
  </read_first>
  <action>
    Add a per-family glossary affordance to each row in PanelFamilyBars. Two acceptable approaches (pick the one that fits the existing row markup cleanly): (a) render a RateTooltip (imported from './RateTooltipReact') next to each family label carrying that family's definition from familyDefs.ts FAMILY_DEFS_EN/ES; OR (b) make each family label an anchor link to the glossary page section, e.g. `/glossary/#${familyKey}` (en) / `/es/glosario/#${familyKey}` (es) — zero-JS anchor navigation (the glossary pages with matching `id` anchors are built in Plan 03). If choosing (b), the link must be a real `<a href>` so it works without JS. Keep the existing `aria-hidden="true"` accent-dot convention. Prefer importing FAMILY labels from familyDefs.ts to remove the local duplication if the row currently declares its own FAMILY_LABELS. Do not introduce a new client:* directive (this component already runs inside the island).
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - PanelFamilyBars references either RateTooltip or a glossary anchor: `grep -c "RateTooltip\|glosario\|glossary" site/src/components/map/PanelFamilyBars.tsx` ≥ 1.
    - If anchor approach: the href uses the per-family key and the correct locale path (`/glossary/#` for en, `/es/glosario/#` for es).
    - Existing aria-hidden accent-dot preserved: `grep -c "aria-hidden" site/src/components/map/PanelFamilyBars.tsx` ≥ 1.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Each family bar offers in-context help (definition tooltip or glossary anchor); build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time Astro frontmatter → client island props | loadNationalAverage() runs at build time; only a numeric prop crosses into the browser island. No user input crosses any boundary. |
| client island → /data/cead/*.json fetch | Existing same-origin static-data fetch (unchanged by this plan); no new endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | nationalAvg prop | accept | The national average is already-public aggregate CEAD data, no PII; injecting it as a prop exposes nothing new. |
| T-09-04 | Tampering | Legend/ResultPanel render of computed numbers | accept | All numbers are derived client-side from the existing static payload already served; no new injection path; no innerHTML / dangerouslySetInnerHTML used. |
| T-09-05 | Denial of Service | computeQuantileBreaks over rate array | accept | Bounded to ≤346 communes, O(n log n) sort already used by the choropleth; negligible. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §"Package Legitimacy Audit": none). No install task; no slopcheck checkpoint required. |
</threat_model>

<verification>
- `cd site && npm run build` exits 0 (single chained command — OneDrive desync note).
- No `loadNationalAverage` reference inside `site/src/components/map/` (Pitfall 1).
- No new `client:*` directive added (D-09): the island already uses `client:only="react"`; this plan adds none.
- Manual browser check (deferred to Plan 05 / human-verify): legend bands show numbers, panel tooltip opens via keyboard, multiplier text reads neutrally.
</verification>

<success_criteria>
- Legend shows numeric ranges + qualitative labels + per-100k note (D-03).
- Panel rate tooltip is keyboard/touch accessible and cites CEAD (D-01).
- Panel shows neutral multiplier + CSS comparison bar fed by a build-time national average (D-04); no fs in browser.
- Each family bar offers in-context glossary help (D-02 island surface).
- Site builds clean; no absolute safety language introduced.
</success_criteria>

<output>
Create `.planning/phases/09-ux-readability-accessibility-polish/09-02-SUMMARY.md` when done
</output>
