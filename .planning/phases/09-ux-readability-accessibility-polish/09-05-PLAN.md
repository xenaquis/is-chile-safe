---
phase: 09-ux-readability-accessibility-polish
plan: 05
type: execute
wave: 3
depends_on: ["09-02", "09-03", "09-04"]
files_modified:
  - .planning/phases/09-ux-readability-accessibility-polish/09-VALIDATION.md
autonomous: false
requirements: [A11Y-01, A11Y-02, READ-03, UX-01, UX-02]
must_haves:
  truths:
    - "A full production build succeeds with all Phase-9 routes emitted (including /glossary/ and /es/glosario/)"
    - "A site-wide grep confirms no absolute 'peligroso/seguro/dangerous/safe' tone in editorial pages"
    - "Sampled pages pass A11Y/SEO meta checks (title, meta description, canonical, hreflang) and a11y checks (focus states, alt/aria, map-control aria, contrast) in a real browser"
    - "FAQPage JSON-LD is present in a sampled FAQ money page build output (dist/is-santiago-safe/index.html)"
    - "The legend numeric bands, panel rate tooltip, and multiplier comparison render and operate by keyboard in the browser"
    - "No statistic is stated both in a DataCallout and verbatim in adjacent prose on the 3 sampled editorial pages (UX-02 spot-check)"
  artifacts:
    - path: ".planning/phases/09-ux-readability-accessibility-polish/09-VALIDATION.md"
      provides: "Filled validation ledger with per-requirement results"
      contains: "nyquist_compliant"
  key_links:
    - from: "build output"
      to: "phase requirements UX/READ/A11Y"
      via: "automated build + grep + manual browser/axe checks"
      pattern: "npm run build"
---

<objective>
Verify the whole phase end to end and record the result: a clean production build with all new routes, an automated sober-tone grep assertion, a meta-signal grep on built HTML (including FAQPage JSON-LD on a money page), and a single human-verify checkpoint covering the inherently visual/keyboard a11y items (focus rings, tooltip keyboard operation, map-control aria, legend numeric bands, multiplier bar, WCAG contrast via axe, and a UX-02 callout-redundancy spot-check).

Purpose: Per 09-RESEARCH §"Validation Architecture", this phase's validation is build-success + manual browser tabbing + axe scan + tone grep. This plan executes the automated layer and gates the manual layer behind one consolidated checkpoint, then re-confirms the phase VALIDATION.md ledger (authored pre-execution in the revision). Depends on Plans 02, 03, 04 (the work being verified).
Output: a confirmed/checked-off 09-VALIDATION.md and a recorded human verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md
@.planning/phases/09-ux-readability-accessibility-polish/09-VALIDATION.md
@.planning/phases/09-ux-readability-accessibility-polish/09-01-SUMMARY.md
@.planning/phases/09-ux-readability-accessibility-polish/09-02-SUMMARY.md
@.planning/phases/09-ux-readability-accessibility-polish/09-03-SUMMARY.md
@.planning/phases/09-ux-readability-accessibility-polish/09-04-SUMMARY.md

<interfaces>
<!-- The validation contract from RESEARCH §"Validation Architecture" and the pre-authored 09-VALIDATION.md ledger -->

09-VALIDATION.md (authored pre-execution in the revision): real framework (Astro build + grep +
  manual axe/keyboard), quick/full commands, per-requirement test-map rows for all 7 IDs, and a
  Manual-Only Verifications table. This plan flips the ⬜ pending status rows to ✅/❌ — it does NOT
  re-author the ledger structure.

Tone grep (READ-03): grep -rin "peligroso|seguro|dangerous|safe" over site/src/pages/ —
  matches must all be neutral compounds ("safety", "incidencia"), never absolute verdicts.

Meta signals (A11Y-02): every built page has <title>, <meta name="description">, <link rel="canonical">,
  hreflang en/es/x-default — all emitted by BaseLayout (verified Phase 7). Glossary pages intentionally
  ship NO JSON-LD (defensible per RESEARCH); the FAQPage JSON-LD assertion targets a FAQ money page
  (is-santiago-safe), NOT the glossary.

A11Y-01 (manual): focus-visible rings (Plan 01), tooltip keyboard operation, map-control aria
  (year <select> + crime-type chips — verify accessible names with axe), WCAG-AA contrast
  (#0f766e on #f5f8f8 = 4.71:1 — do not regress). Use the axe browser extension or axe-core.

UX-02 (manual): callout↔prose redundancy on is-santiago-safe / is-chile-safe / safest-cities —
  Plan 04 Task 2 removed duplications; this plan spot-checks the rendered pages (self-report is not enough).

Dev/preview: `cd site && npm run build && npm run preview` (manual tab-key walk afterward).
OneDrive note: always chain build + any dist check in ONE command (dist/ desyncs between processes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase verification — build, route emission, tone grep, meta grep, FAQPage JSON-LD</name>
  <files>.planning/phases/09-ux-readability-accessibility-polish/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-ux-readability-accessibility-polish/09-VALIDATION.md (pre-authored ledger — flip the automated-row statuses, do not re-author structure)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (Validation Architecture §, Requirements→Test Map lines 772-788; SEO/Meta §"Gap: FAQPage JSON-LD" lines 666-673)
    - site/src/pages/is-santiago-safe.astro (supplies a FAQPage jsonLd to BaseLayout — the page that SHOULD carry FAQPage JSON-LD)
    - all four prior SUMMARYs (09-01..09-04) for what changed
  </read_first>
  <action>
    Run the production build chained with all dist checks in a SINGLE command (OneDrive desync guard): `cd site && npm run build` then in the same command verify the new routes exist (`dist/glossary/index.html`, `dist/es/glosario/index.html`) and that a sampled built page carries the meta signals (grep the built HTML for `<title`, `name="description"`, `rel="canonical"`, `hreflang`).
    A11Y-02 FAQPage assertion (WARNING 3 fix): grep a sampled FAQ MONEY page build output — `dist/is-santiago-safe/index.html` — for `"@type":"FAQPage"` (or `"@type": "FAQPage"` with optional whitespace) to confirm the FAQPage JSON-LD is present on a page that SHOULD have it. Do NOT assert JSON-LD on the glossary pages — they intentionally ship none (defensible per RESEARCH §"Gap: FAQPage JSON-LD"); only title/description/canonical/hreflang are asserted there.
    Run the READ-03 sober-tone assertion over `site/src/pages/` — filter out comment lines and confirm every remaining match of `peligroso|seguro|dangerous|safe` is a neutral compound (e.g. "safety", "Chile Safety Map", "incidencia"), with NO absolute verdict ("is dangerous", "es peligroso", "muy seguro").
    Record each result (PASS/FAIL with the command output excerpt) in 09-VALIDATION.md: flip the automated test-map rows from ⬜ pending to ✅ green (UX-01 build/H1, READ-01 css grep, READ-02 byte count, READ-03 tone grep, A11Y-02 routes+meta+FAQPage). Leave the manual-only rows (A11Y-01 visual + map-control aria, UX-01 5-second scan, UX-02 callout spot-check, READ-02 depth) pending until Task 2's checkpoint.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build && ls dist/glossary/index.html dist/es/glosario/index.html dist/is-santiago-safe/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Build exits 0 and the new route files exist (the verify command lists `dist/glossary/index.html`, `dist/es/glosario/index.html`, and `dist/is-santiago-safe/index.html`).
    - Tone grep is documented in the SUMMARY/VALIDATION with the exact command and a verdict that every match is a neutral compound (no absolute safety verdict). Use a comment-filtered grep, e.g. `grep -rin --include=*.astro "es peligroso\|is dangerous\|muy seguro\|totalmente seguro" site/src/pages/` returns no absolute-verdict line.
    - Built sample page has all four meta signals: a grep over `dist/glossary/index.html` finds `<title`, `name="description"`, `rel="canonical"`, and `hreflang`.
    - FAQPage JSON-LD present on the FAQ money page (A11Y-02): `grep -c "FAQPage" dist/is-santiago-safe/index.html` ≥ 1 (the page supplies a FAQPage `jsonLd` to BaseLayout). The glossary pages are NOT expected to carry JSON-LD.
    - 09-VALIDATION.md automated rows flipped to ✅ green; the per-requirement test map (already authored) reflects the run; manual rows left pending for Task 2.
  </acceptance_criteria>
  <done>Production build is clean with new routes emitted; tone, meta, and FAQPage JSON-LD assertions pass and are recorded; VALIDATION.md automated rows marked green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — focus states, tooltip keyboard, map-control aria, legend bands, multiplier bar, contrast, callout redundancy</name>
  <files>(none — verification gate, no code changes)</files>
  <action>
    This is a blocking human-verify gate — no code changes. Build and start the preview server, then walk every check in <how-to-verify> in a real browser (BrowserOS MCP if connected). Record each result in 09-VALIDATION.md (flip the manual-only rows to green/red) and in the SUMMARY. Pause for the user's "approved" or a defect list; any defect becomes input for a gap-closure plan. Per memory note browseros-review-gotchas, run any BrowserOS steps inline (the executor agent lacks BrowserOS) and save screenshots to a no-spaces path.
  </action>
  <what-built>
    Phase 9 comprehension + a11y polish: a numeric+qualitative map legend (D-03), a keyboard-accessible rate "?" tooltip on the map panel and ranking tables (D-01), a neutral multiplier + comparison bar in the commune panel (D-04), a bilingual crime glossary at /glossary/ + /es/glosario/ (D-02), global :focus-visible focus rings + editorial prose rhythm, accessible names on the year select + crime-type chips, and an expanded ES methodology page. All automated checks (build, routes, tone grep, meta grep, FAQPage JSON-LD) already passed in Task 1.
  </what-built>
  <how-to-verify>
    Start the preview server in ONE chained command (OneDrive desync guard):
    `cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build && npm run preview`
    Then in a real browser (and BrowserOS MCP if connected):

    1. A11Y-01 focus states: open `/map/` and Tab through the page. CONFIRM a visible teal outline appears on every interactive element (search input, year select, crime chips, legend, map controls, the panel close button, and the "?" tooltip trigger).
    2. A11Y-01 map-control aria (WARNING 2): run the axe DevTools extension on `/map/` and CONFIRM the year `<select>` and every crime-type chip button report an accessible name (NO "Form element has no accessible name" / "Buttons must have discernible text" violation). If axe is unavailable, inspect each control's accessibility node and confirm an aria-label or associated label.
    3. D-01 tooltip (keyboard + touch): Tab to a "?" trigger next to a rate (open a commune panel via search → Santiago). Press Enter/Space → CONFIRM the definition tooltip opens, cites CEAD, and is not clipped by the panel. Press Enter again or Escape → it closes. Repeat on a ranking table at `/region/metropolitana/` (the static "?" in the rate column header).
    4. D-03 legend: on `/map/` CONFIRM the legend shows 5 bands, each with a numeric range (e.g. "0 – 1,500") AND a qualitative label (Very low → Very high), plus a "per 100,000 inhab." note. Change the year filter → CONFIRM the numeric ranges update. Check `/es/mapa/` shows "Muy baja … Muy alta" and "por 100.000 hab.".
    5. D-04 multiplier/bar: in the Santiago panel CONFIRM a neutral "X× the national average" label + a comparison bar render; the wording is NEVER "safe/dangerous/peligroso/seguro".
    6. A11Y-01 contrast: run axe on `/map/`, `/glossary/`, and `/is-santiago-safe/` → CONFIRM no new color-contrast violations (the #0f766e on #f5f8f8 = 4.71:1 pair must still pass AA; report any violation).
    7. A11Y-02 / READ-02 spot check: open `/glossary/` and `/es/glosario/` → CONFIRM substantive prose (not thin), per-family sections, and that the language switcher round-trips between the EN and ES glossary. Open `/methodology/` and `/es/metodologia/` side by side → CONFIRM the ES page now reads at comparable depth to EN.
    8. UX-02 callout-redundancy spot-check (WARNING 4): open `/is-santiago-safe/`, `/is-chile-safe/`, and `/safest-cities-in-chile/` → for each, scan the DataCallouts and the adjacent prose and CONFIRM no statistic appears BOTH inside a DataCallout (value+label+source) AND verbatim in the neighbouring paragraph. Note any remaining duplication (page + the repeated figure) as a defect.
    9. UX-01 5-second scan: glance at `/is-santiago-safe/` → CONFIRM the page purpose is clear within ~5 seconds (one H1, scannable H2s).

    Record any defect with the page + observation so a gap-closure plan can address it.
  </how-to-verify>
  <verify>
    <human-check>All 9 manual checks confirmed in a real browser; results recorded in 09-VALIDATION.md.</human-check>
  </verify>
  <resume-signal>Type "approved" if all checks pass, or list the specific failures (page + observation) to drive a gap-closure plan.</resume-signal>
  <done>Human has confirmed focus states, map-control aria, keyboard tooltip operation, legend bands, multiplier bar, contrast, ES/EN parity, and callout-redundancy removal in a real browser (or captured specific gaps); 09-VALIDATION.md manual rows updated and the sign-off latency item checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification only | This plan runs builds and manual checks; it modifies only a planning doc (VALIDATION.md). No production code or runtime boundary is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | VALIDATION.md ledger | accept | Internal planning artifact, not shipped; no runtime impact, no user input. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §"Package Legitimacy Audit": none). axe DevTools is a browser extension used manually, not an installed project dependency. No install task; no slopcheck checkpoint required. |
</threat_model>

<verification>
- `cd site && npm run build && ls dist/glossary/index.html dist/es/glosario/index.html dist/is-santiago-safe/index.html` succeeds (single chained command — OneDrive desync note).
- Tone grep, meta grep, and FAQPage JSON-LD grep documented PASS in 09-VALIDATION.md.
- Human checkpoint approved (or failures captured for gap closure), including map-control aria and callout-redundancy spot-checks.
</verification>

<success_criteria>
- Clean production build with all Phase-9 routes emitted.
- Automated tone + meta + FAQPage JSON-LD assertions pass and are recorded.
- Human verification confirms focus states, map-control aria, keyboard tooltip, legend bands, multiplier bar, contrast, ES/EN parity, and callout-redundancy removal — or captures specific gaps.
- 09-VALIDATION.md status rows flipped to reflect per-requirement results.
</success_criteria>

<output>
Create `.planning/phases/09-ux-readability-accessibility-polish/09-05-SUMMARY.md` when done
</output>
