---
phase: 09-ux-readability-accessibility-polish
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - site/src/pages/glossary.astro
  - site/src/pages/es/glosario.astro
  - site/src/components/CommuneRankingTable.astro
  - site/src/components/MethodologyCaveat.astro
autonomous: true
requirements: [READ-03, A11Y-02, UX-02]
must_haves:
  truths:
    - "A bilingual crime-type glossary exists at /glossary/ (EN) and /es/glosario/ (ES) defining all 7 crime families with substantive, CEAD-attributed prose"
    - "Each glossary entry has a stable #anchor id matching its family key and links to the crime-type page"
    - "Glossary pages emit valid <title>, meta description, canonical, and reciprocal hreflang via BaseLayout"
    - "Ranking tables surface an accessible '?' explanation of 'rate per 100k' and a cross-link to the glossary"
    - "No glossary or table link targets a non-rollout /commune/* page (no 404s)"
  artifacts:
    - path: "site/src/pages/glossary.astro"
      provides: "EN glossary page (/glossary/)"
      contains: "FAMILY_DEFS_EN"
    - path: "site/src/pages/es/glosario.astro"
      provides: "ES glossary page (/es/glosario/)"
      contains: "FAMILY_DEFS_ES"
    - path: "site/src/components/CommuneRankingTable.astro"
      provides: "Rate tooltip + glossary cross-link in ranking tables"
      contains: "RateTooltip"
  key_links:
    - from: "site/src/pages/glossary.astro"
      to: "site/src/lib/familyDefs.ts"
      via: "import FAMILY_ORDER + FAMILY_DEFS_EN + FAMILY_LABELS_EN"
      pattern: "familyDefs"
    - from: "site/src/components/CommuneRankingTable.astro"
      to: "site/src/components/RateTooltip.astro"
      via: "import + render in the rate column header"
      pattern: "RateTooltip"
---

<objective>
Ship the bilingual crime-type glossary (D-02 page surface) at `/glossary/` + `/es/glosario/`, and wire the zero-JS rate "?" tooltip + glossary cross-links into the static ranking tables.

Purpose: Captures long-tail SEO ("qué es robo con violencia"), gives every in-context tooltip a canonical link target, and makes static tables self-explanatory — closing READ-03 (jargon explained, sober tone), A11Y-02 (valid title/meta/canonical/hreflang on the new pages), and the table side of UX-02. Depends on Plan 01 for familyDefs.ts, RateTooltip.astro, and i18n strings. Disjoint files from Plan 02, so it runs in the same wave.
Output: two new glossary routes + tooltip/cross-link edits in CommuneRankingTable + MethodologyCaveat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ux-readability-accessibility-polish/09-CONTEXT.md
@.planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md
@.planning/phases/09-ux-readability-accessibility-polish/09-PATTERNS.md
@.planning/phases/09-ux-readability-accessibility-polish/09-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (built) + existing layout/components -->

familyDefs.ts (Plan 01): FAMILY_LABELS_EN, FAMILY_LABELS_ES, FAMILY_DEFS_EN, FAMILY_DEFS_ES,
  FAMILY_ORDER (['vida','propiedad','robos_violentos','incivilidades','vif','drogas','armas']).

RateTooltip.astro (Plan 01): <RateTooltip tip={string} lang={'en'|'es'} label?={string} /> — zero-JS.

i18n.ts (Plan 01): glossary_link, rate_tooltip_title, rate_tooltip_body in EN_STRINGS/ES_STRINGS.

EditorialLayout.astro: wraps BaseLayout in `.editorial-prose` (720px column); props
  { lang, title, description, enPath, esPath, jsonLd? }. Use this for the glossary pages so
  READ-01 prose rhythm + 720px column apply automatically.

BaseLayout.astro emits <title>, meta description, canonical, hreflang×3 from enPath/esPath — automatic.

FAMILY_SLUGS (i18n.ts): Record<key, { en: string; es: string }> — for building /crime/{slug}/ links.

CommuneRankingTable.astro: rateColLabel at line 33; <th scope="col" class="col-num">{rateColLabel}</th> at line 46.
  Used by commune/region/crime pages (both locales). Rows already gated to rollout CUTs (F-005 fixed in Phase 8).

MethodologyCaveat.astro: existing CEAD-attribution caveat block (a natural place for a glossary cross-link).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build /glossary/ (EN) and /es/glosario/ (ES) bilingual glossary pages</name>
  <files>site/src/pages/glossary.astro, site/src/pages/es/glosario.astro</files>
  <read_first>
    - site/src/pages/about.astro (static-page pattern with literal enPath/esPath)
    - site/src/layouts/EditorialLayout.astro (props + .editorial-prose 720px wrapper + jsonLd passthrough)
    - site/src/lib/familyDefs.ts (Plan 01 — labels/defs/order to iterate)
    - site/src/config/i18n.ts (FAMILY_SLUGS for /crime/{slug}/ links)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (Pattern 4 lines 342-377; Pitfall 3 thin-content lines 418-423; Pitfall 5 rollout 404 lines 432-436)
  </read_first>
  <action>
    Create site/src/pages/glossary.astro using EditorialLayout with lang="en", a descriptive title ("Crime Type Glossary — Chile Safety Map"), a meta description, `enPath="/glossary/"`, `esPath="/es/glosario/"`. Import FAMILY_ORDER, FAMILY_LABELS_EN, FAMILY_DEFS_EN from '../lib/familyDefs' and FAMILY_SLUGS from '../config/i18n'. Render an `<h1>` and an intro paragraph naming CEAD (Centro de Estudios y Análisis del Delito). Iterate FAMILY_ORDER → one `<section id={familyKey}>` per family with `<h2>{FAMILY_LABELS_EN[key]}</h2>`, the FAMILY_DEFS_EN[key] sentence, PLUS 2-3 additional sentences of context (what specific incidents CEAD records in this category, an under-reporting note where relevant) to reach 80-120 words per entry (~600+ words total) — Pitfall 3 thin-content guard. End each section with `<a href={`/crime/${FAMILY_SLUGS[key].en}/`}>` link text using the glossary_link/family label. The `id` on each section MUST equal the family key (vida, propiedad, …) so PanelFamilyBars anchors (#vida etc.) land correctly.
    Create site/src/pages/es/glosario.astro mirroring it: lang="es", Spanish title/description, SAME `enPath="/glossary/"` / `esPath="/es/glosario/"` (reciprocal hreflang), import FAMILY_DEFS_ES/FAMILY_LABELS_ES, link to `/es/delito/${FAMILY_SLUGS[key].es}/`, ES context prose.
    Editorial constraint (non-negotiable): never use "peligroso/seguro/dangerous/safe" in absolute terms; use "incidencia reportada" / "reported incidence"; attribute CEAD + that figures are police-reported. Link ONLY to /crime/* and /es/delito/* (and optionally /region/*) — NEVER to /commune/* or /es/comuna/* (rollout 404 risk, Pitfall 5).
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Both pages exist and import from familyDefs: `grep -l "familyDefs" site/src/pages/glossary.astro site/src/pages/es/glosario.astro` lists both.
    - Each page renders 7 family sections with key-matching ids: `grep -c "id=" site/src/pages/glossary.astro` ≥ 7.
    - No commune links: `grep -n "/commune/\|/es/comuna/" site/src/pages/glossary.astro site/src/pages/es/glosario.astro` returns nothing (Pitfall 5).
    - No absolute-tone words: `grep -ni "peligroso\|dangerous" site/src/pages/glossary.astro site/src/pages/es/glosario.astro` returns nothing; any "safe"/"seguro" match must be inside a neutral phrase (verify manually if matched).
    - Build emits both routes: after `npm run build`, `dist/glossary/index.html` and `dist/es/glosario/index.html` exist. (Chain the check into the build command to avoid OneDrive dist desync.)
    - Built EN page has reciprocal hreflang to /es/glosario/: `grep -c "es/glosario" dist/glossary/index.html` ≥ 1.
  </acceptance_criteria>
  <done>Two substantive (≥600-word) CEAD-attributed glossary pages exist with per-family anchors, crime-page links, and reciprocal hreflang; no rollout 404 links; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add rate "?" tooltip + glossary cross-link to ranking tables and methodology caveat</name>
  <files>site/src/components/CommuneRankingTable.astro, site/src/components/MethodologyCaveat.astro</files>
  <read_first>
    - site/src/components/CommuneRankingTable.astro (full — rateColLabel line 33; rate <th> line 46; locale handling)
    - site/src/components/MethodologyCaveat.astro (full — existing caveat text + locale handling)
    - site/src/components/RateTooltip.astro (Plan 01 — the zero-JS component to embed)
    - site/src/config/i18n.ts (rate_tooltip_body, glossary_link)
    - .planning/phases/09-ux-readability-accessibility-polish/09-PATTERNS.md (CommuneRankingTable assignment — RateTooltip next to rate <th>, glossary cross-link footnote)
  </read_first>
  <action>
    CommuneRankingTable.astro: import RateTooltip from '../components/RateTooltip.astro'. In the rate column header (`<th scope="col" class="col-num">{rateColLabel}</th>`, line 46), render `<RateTooltip lang={locale} tip={rate_tooltip_body for locale} />` next to the label (keep `scope="col"`). Add a one-line glossary cross-link footnote below the table (near the existing rollout-note / caption area) pointing to `/glossary/` (en) or `/es/glosario/` (es) using the glossary_link i18n string. Pull the tooltip/label strings from i18n (EN_STRINGS/ES_STRINGS) based on the existing `locale` prop. NO client:* directive — RateTooltip is zero-JS.
    MethodologyCaveat.astro: add a sentence/link cross-referencing the glossary (`/glossary/` or `/es/glosario/` per locale) so readers can look up crime-family definitions — using the glossary_link string. Keep the existing CEAD attribution intact.
    Editorial constraint: relative framing only; do not add absolute "safe/dangerous" wording.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - CommuneRankingTable imports and renders RateTooltip: `grep -c "RateTooltip" site/src/components/CommuneRankingTable.astro` ≥ 2 (import + usage).
    - CommuneRankingTable links to the glossary: `grep -c "glossary\|glosario" site/src/components/CommuneRankingTable.astro` ≥ 1.
    - MethodologyCaveat links to the glossary: `grep -c "glossary\|glosario" site/src/components/MethodologyCaveat.astro` ≥ 1.
    - Header keeps scope="col": `grep -c 'scope="col"' site/src/components/CommuneRankingTable.astro` ≥ 4 (unchanged count).
    - No client:* added: `grep -c "client:" site/src/components/CommuneRankingTable.astro` returns 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Ranking tables explain the rate metric inline (accessible, zero-JS) and cross-link the glossary; the methodology caveat links the glossary; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time source → static HTML | Glossary prose and tooltip text are authored at build time; rendered to static HTML. No runtime user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering (XSS) | glossary.astro / glosario.astro content | accept | All text is authored static content interpolated by Astro's auto-escaping JSX-like templating; no `set:html`, no user input. If a WebPage/DefinedTermSet JSON-LD is later added, it must go through BaseLayout's existing `JSON.stringify().replace(/<\//g,'<\\/')` escape — not hand-built. Initial scope adds no JSON-LD. |
| T-09-07 | Information Disclosure | glossary links | accept | Links target only public /crime/* and /region/* pages; never commune pages (also avoids 404s). No PII, no secrets. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §"Package Legitimacy Audit": none). No install task; no slopcheck checkpoint required. |
</threat_model>

<verification>
- `cd site && npm run build` exits 0; `dist/glossary/index.html` and `dist/es/glosario/index.html` exist (chained check — OneDrive desync note).
- No `/commune/` or `/es/comuna/` link anywhere in the glossary pages (rollout 404 guard).
- Reciprocal hreflang present on both glossary pages (automatic via BaseLayout enPath/esPath).
- Sober-tone grep passes (no absolute "peligroso/seguro/dangerous/safe").
</verification>

<success_criteria>
- Bilingual glossary pages exist with ≥600 words, per-family #anchors, and crime-page links; valid title/meta/canonical/hreflang (A11Y-02).
- Ranking tables and methodology caveat cross-link the glossary and expose the accessible rate tooltip.
- No rollout 404 links introduced; site builds clean.
</success_criteria>

<output>
Create `.planning/phases/09-ux-readability-accessibility-polish/09-03-SUMMARY.md` when done
</output>
