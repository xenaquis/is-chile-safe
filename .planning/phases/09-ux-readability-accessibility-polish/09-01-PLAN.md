---
phase: 09-ux-readability-accessibility-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/lib/familyDefs.ts
  - site/src/config/i18n.ts
  - site/src/components/RateTooltip.astro
  - site/src/components/map/RateTooltipReact.tsx
  - site/src/styles/global.css
  - site/src/pages/crime/[family].astro
  - site/src/pages/es/delito/[family].astro
autonomous: true
requirements: [UX-02, A11Y-01, READ-01]
must_haves:
  truths:
    - "Crime-family labels and definitions exist in a single shared module imported by both the EN and ES crime-type pages"
    - "A zero-JS '?' tooltip component renders an accessible <details>/<summary> disclosure in both Astro and React contexts"
    - "Keyboard focus is visibly indicated on every interactive element (outline ring) when tabbing"
    - "Editorial prose paragraphs have vertical rhythm (margin between paragraphs)"
    - "All Phase-9 bilingual strings (rate tooltip, legend qualitative labels, comparison multiplier, glossary link) exist in both EN_STRINGS and ES_STRINGS"
  artifacts:
    - path: "site/src/lib/familyDefs.ts"
      provides: "Hoisted FAMILY_LABELS_EN/ES, FAMILY_DEFS_EN/ES, FAMILY_ORDER"
      contains: "export const FAMILY_DEFS_EN"
    - path: "site/src/components/RateTooltip.astro"
      provides: "Zero-JS Astro tooltip (details/summary)"
      contains: "<details"
    - path: "site/src/components/map/RateTooltipReact.tsx"
      provides: "React twin of the tooltip with identical markup"
      exports: ["RateTooltip"]
    - path: "site/src/styles/global.css"
      provides: ":focus-visible rules, tooltip CSS, prose rhythm"
      contains: ":focus-visible"
    - path: "site/src/config/i18n.ts"
      provides: "Phase-9 i18n keys in both locales"
      contains: "rate_tooltip_body"
  key_links:
    - from: "site/src/pages/crime/[family].astro"
      to: "site/src/lib/familyDefs.ts"
      via: "import { FAMILY_LABELS_EN, FAMILY_DEFS_EN }"
      pattern: "import.*familyDefs"
    - from: "site/src/pages/es/delito/[family].astro"
      to: "site/src/lib/familyDefs.ts"
      via: "import { FAMILY_LABELS_ES, FAMILY_DEFS_ES }"
      pattern: "import.*familyDefs"
---

<objective>
Build the shared foundation that every other Phase-9 comprehension plan consumes: a single source-of-truth crime-family data module, all new bilingual strings, the dual-surface accessible "?" tooltip component (Astro + React twin), and the global CSS for focus-visible states, tooltip styling, and editorial prose rhythm.

Purpose: Plans 02 (map island) and 03 (glossary + tables) both depend on these contracts. Defining them first prevents a scavenger hunt and guarantees the tooltip markup/CSS are identical across React and Astro surfaces (one CSS block styles both). Covers the A11Y-01 focus-state gap (the highest-severity a11y finding — `global.css` currently has zero `:focus-visible` rules) and the READ-01 prose-rhythm gap.
Output: `familyDefs.ts`, `RateTooltip.astro`, `RateTooltipReact.tsx`, extended `i18n.ts`, extended `global.css`, and re-wired imports in both `[family].astro` pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ux-readability-accessibility-polish/09-CONTEXT.md
@.planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md
@.planning/phases/09-ux-readability-accessibility-polish/09-PATTERNS.md

<interfaces>
<!-- Existing source-of-truth constants to hoist VERBATIM. Do not rewrite the strings. -->

FAMILY_LABELS_EN + FAMILY_DEFS_EN live in site/src/pages/crime/[family].astro lines 35-54.
FAMILY_LABELS_ES + FAMILY_DEFS_ES live in site/src/pages/es/delito/[family].astro lines 28-47.

Display order (mirror PanelFamilyBars.tsx FAMILY_ORDER):
  ['vida', 'propiedad', 'robos_violentos', 'incivilidades', 'vif', 'drogas', 'armas']

Existing CSS tokens in global.css :root — reuse, never hardcode:
  --primary #0f766e, --bg #f5f8f8, --ink #1c2a2b, --muted #5d6c6d, --card #ffffff,
  --line, --radius, --text-label, spacing --xs/--sm/--md/--lg/--xl, --s1..--s5.

i18n.ts shape: `export interface I18nStrings` (line 7); `export const EN_STRINGS: I18nStrings` (line 87);
`export const ES_STRINGS: I18nStrings` (line 169). New keys must be added to all three.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hoist crime-family constants into shared lib/familyDefs.ts and re-wire both crime pages</name>
  <files>site/src/lib/familyDefs.ts, site/src/pages/crime/[family].astro, site/src/pages/es/delito/[family].astro</files>
  <read_first>
    - site/src/pages/crime/[family].astro (lines 35-54 = FAMILY_LABELS_EN + FAMILY_DEFS_EN; lines 166-167 = consumers)
    - site/src/pages/es/delito/[family].astro (lines 28-47 = FAMILY_LABELS_ES + FAMILY_DEFS_ES; lines 156-157 = consumers)
    - site/src/components/map/PanelFamilyBars.tsx (FAMILY_ORDER / FAMILY_LABELS for the canonical display order)
    - site/src/config/i18n.ts (record-of-strings export style to mirror)
  </read_first>
  <behavior>
    - familyDefs.ts exports FAMILY_LABELS_EN, FAMILY_LABELS_ES, FAMILY_DEFS_EN, FAMILY_DEFS_ES (7 keys each: vida, robos_violentos, vif, drogas, armas, propiedad, incivilidades) with the EXACT strings copied from the two source files.
    - familyDefs.ts exports FAMILY_ORDER = ['vida','propiedad','robos_violentos','incivilidades','vif','drogas','armas'] as const.
    - crime/[family].astro and es/delito/[family].astro import labels+defs from familyDefs.ts instead of declaring them locally; rendered HTML is byte-identical to before (zero behaviour change).
  </behavior>
  <action>
    Create site/src/lib/familyDefs.ts. Copy FAMILY_LABELS_EN and FAMILY_DEFS_EN VERBATIM from crime/[family].astro lines 35-54, and FAMILY_LABELS_ES and FAMILY_DEFS_ES VERBATIM from es/delito/[family].astro lines 28-47 — do NOT paraphrase, retranslate, or alter any CEAD attribution wording. Add `export const FAMILY_ORDER = ['vida','propiedad','robos_violentos','incivilidades','vif','drogas','armas'] as const;`. Each constant gets `export`.
    Then edit crime/[family].astro: remove the local `const FAMILY_LABELS_EN` (35-43) and `const FAMILY_DEFS_EN` (45-54) blocks and add `import { FAMILY_LABELS_EN, FAMILY_DEFS_EN } from '../../lib/familyDefs';` in the frontmatter import group. Leave consumers at 166-167 untouched.
    Edit es/delito/[family].astro: remove local `const FAMILY_LABELS_ES` (28-36) and `const FAMILY_DEFS_ES` (39-47) blocks and add `import { FAMILY_LABELS_ES, FAMILY_DEFS_ES } from '../../../lib/familyDefs';` (note the extra `../` — file is one directory deeper). Leave consumers at 156-157 untouched.
    Editorial constraint: definitions must keep relative/sober framing and CEAD attribution; never introduce "peligroso/seguro/dangerous/safe" in absolute terms.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/lib/familyDefs.ts` exists and `grep -c "export const" site/src/lib/familyDefs.ts` returns 5 (4 record constants + FAMILY_ORDER).
    - `grep -c "FAMILY_DEFS_EN" site/src/pages/crime/[family].astro` shows the local definition removed and replaced by an import (no `const FAMILY_DEFS_EN` declaration remains; an `import ... familyDefs` line is present).
    - Both crime pages import from familyDefs (`grep -l "familyDefs" site/src/pages/crime/[family].astro site/src/pages/es/delito/[family].astro` lists both).
    - `npm run build` completes with exit code 0 (Astro emits all /crime/* and /es/delito/* pages with no error).
  </acceptance_criteria>
  <done>familyDefs.ts is the single source of crime-family labels/defs/order; both crime-type pages import from it; site builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add all Phase-9 bilingual strings to i18n.ts (both locales)</name>
  <files>site/src/config/i18n.ts</files>
  <read_first>
    - site/src/config/i18n.ts (I18nStrings interface line 7; EN_STRINGS line 87; ES_STRINGS line 169 — see the exact append points)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (Code Examples §"Add i18n strings", lines 491-528 — the exact key names and EN/ES values to use verbatim)
  </read_first>
  <action>
    Add these keys to the `I18nStrings` interface and supply matching values in BOTH EN_STRINGS and ES_STRINGS, using the verbatim values specified in 09-RESEARCH.md lines 507-528: `rate_tooltip_title`, `rate_tooltip_body`, `glossary_link`, `comparison_national` (value contains the literal placeholder token `{multiplier}`), `legend_qual_1`, `legend_qual_2`, `legend_qual_3`, `legend_qual_4`, `legend_qual_5`, `legend_per_100k_note`. EN qualitative labels are Very low / Low / Medium / High / Very high; ES are Muy baja / Baja / Media / Alta / Muy alta. Keep the rate_tooltip_body CEAD attribution sentence exactly as written in RESEARCH (it ends with "Source: CEAD official police statistics." / "Fuente: estadísticas policiales oficiales CEAD."). Do not introduce absolute "peligroso/seguro" wording.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 10 new keys appear exactly 3 times each (interface + EN + ES): `grep -c "rate_tooltip_body" site/src/config/i18n.ts` returns 3; same for `legend_qual_3` and `comparison_national`.
    - `comparison_national` value contains the substring `{multiplier}` in both EN and ES.
    - TypeScript compiles (no missing-key error from the I18nStrings interface): `npm run build` exits 0. (A missing key in either locale object would fail the type check.)
  </acceptance_criteria>
  <done>Every Phase-9 string is available in both locales and the build type-checks both locale objects against the interface.</done>
</task>

<task type="auto">
  <name>Task 3: Create dual-surface RateTooltip (Astro + React twin) and add focus/tooltip/prose CSS to global.css</name>
  <files>site/src/components/RateTooltip.astro, site/src/components/map/RateTooltipReact.tsx, site/src/styles/global.css</files>
  <read_first>
    - site/src/components/DataCallout.astro (props interface + scoped style + zero client:* — the Astro analog to mirror)
    - site/src/components/map/PanelFamilyBars.tsx (props-only React presentational component; aria-hidden on decorative spans — the React analog to mirror)
    - site/src/styles/global.css (:root tokens lines 12-17; the `p` rule near line 124; confirm NO existing :focus-visible rule)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (Pattern 1 markup lines 177-226; tooltip CSS lines 230-267; focus block lines 473-483)
    - .planning/phases/09-ux-readability-accessibility-polish/09-PATTERNS.md (RateTooltip.astro + RateTooltipReact.tsx assignments)
  </read_first>
  <action>
    Create site/src/components/RateTooltip.astro: a zero-JS component (NO client:* anywhere) with `interface Props { tip: string; lang: 'en' | 'es'; label?: string; }`. Render a `<span class="rate-tooltip"><details class="tooltip-details"><summary class="tooltip-trigger" aria-label={label}><span aria-hidden="true">?</span></summary><div class="tooltip-body" role="tooltip">{tip}</div></details></span>`. Default `label` to "Más información" (es) / "More info" (en). Per PATTERNS, the tooltip CSS lives in global.css (shared with the React twin) — do NOT duplicate it in a scoped block here.
    Create site/src/components/map/RateTooltipReact.tsx exporting `function RateTooltip({ label, tip, lang }: { label: string; tip: string; lang: 'en' | 'es' })` that returns the IDENTICAL markup using `className` (React) and `aria-label`. No hooks, no fetch, no useState — props only.
    Edit site/src/styles/global.css: (a) add the tooltip CSS block from RESEARCH lines 230-267 using existing tokens (`--primary`, `--card`, `--line`, `--radius`, `--text-label`); include `details[open] .tooltip-body { display: block; }` and `.tooltip-trigger::-webkit-details-marker { display: none; }`. (b) Add the A11Y-01 focus block: `*:focus-visible { outline: 2px solid var(--primary); outline-offset: 2px; }` and `*:focus:not(:focus-visible) { outline: none; }` (blanket rule so it also covers `<summary>`, `.lang-btn`, the nav label — Pitfall 4). (c) Add READ-01 prose rhythm: `.editorial-prose p { margin-bottom: var(--md); }`. (d) Add the documenting Pitfall-6 comment `/* #0f766e on #f5f8f8 = 4.71:1 (AA min 4.5:1) — do not lighten --primary or darken --bg */` near the `--primary`/`--bg` token definitions. Do NOT change any color token value.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "client:" site/src/components/RateTooltip.astro` returns 0 (zero-JS, D-09 respected).
    - Both tooltip files render `<details` and `<summary` and an `aria-label` on the summary, plus `aria-hidden="true"` on the "?" glyph: `grep -l "aria-hidden" site/src/components/RateTooltip.astro site/src/components/map/RateTooltipReact.tsx` lists both.
    - `RateTooltipReact.tsx` exports `RateTooltip`: `grep -c "export function RateTooltip" site/src/components/map/RateTooltipReact.tsx` returns 1.
    - global.css now contains a `:focus-visible` rule and `.tooltip-body` and `.editorial-prose p`: `grep -c ":focus-visible" site/src/styles/global.css` ≥ 1, `grep -c ".tooltip-body" site/src/styles/global.css` ≥ 1, `grep -c ".editorial-prose p" site/src/styles/global.css` ≥ 1.
    - Color tokens unchanged: `grep -c "#0f766e" site/src/styles/global.css` still finds the original --primary value (no token edits).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Both tooltip surfaces share one CSS block; keyboard focus is globally visible; editorial paragraphs have rhythm; no color token changed; site builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time source → static HTML | All Phase-9 strings/components are authored content rendered to static HTML at build; no runtime user input crosses any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | i18n.ts / familyDefs.ts string content | accept | Authored static strings, no interpolation of untrusted data; tooltip `tip`/`label` are build-time constants, not user input. No new attack surface vs. existing pages. |
| T-09-02 | Information Disclosure | RateTooltip render | accept | Renders only authored definition text; no PII, no secrets. Reasoning: pure presentational static HTML. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (RESEARCH §"Package Legitimacy Audit": none). No install task present, so no slopcheck checkpoint required. |
</threat_model>

<verification>
- `cd site && npm run build` exits 0 (OneDrive note: build is run as a single chained command so dist/ does not desync between processes).
- `grep` assertions in each task's acceptance_criteria pass.
- No new `client:*` directive anywhere (D-09): `grep -rn "client:" site/src/components/RateTooltip.astro` returns nothing.
</verification>

<success_criteria>
- familyDefs.ts is the single source of crime-family labels/defs; both crime pages import from it; no behaviour change.
- All 10 Phase-9 i18n keys exist in interface + both locales.
- RateTooltip exists in both Astro and React form with identical accessible markup, styled by one shared global.css block.
- global.css has :focus-visible (A11Y-01) and editorial prose rhythm (READ-01); no color token altered.
- Site builds clean.
</success_criteria>

<output>
Create `.planning/phases/09-ux-readability-accessibility-polish/09-01-SUMMARY.md` when done
</output>
