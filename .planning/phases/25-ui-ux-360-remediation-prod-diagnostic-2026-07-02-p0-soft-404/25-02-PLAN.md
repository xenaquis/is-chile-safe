---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - site/src/lib/formatNumber.ts
  - site/src/components/map/ResultPanel.tsx
  - site/src/components/map/Legend.tsx
  - site/src/components/CommuneRankingTable.astro
  - site/src/components/ComparableCommune.astro
  - site/src/components/map/map.css
autonomous: true
requirements: [P1-1, P1-4, P2-3]
must_haves:
  truths:
    - "A single formatNumber helper produces locale-correct numbers (EN: 4,984 / ES: 4.984) everywhere"
    - "The EN map panel no longer shows the ES-formatted '~4.984'; the '~' prefix is gone entirely"
    - "The map result panel groups Composite Index (2024) and Rate (2025) blocks each with a year badge, and a '?' tooltip explains why the years differ"
    - "Singular/plural is correct: '1 case' not '1 cases' in the panel"
  artifacts:
    - path: "site/src/lib/formatNumber.ts"
      provides: "formatRate + formatDecimal locale helpers"
      exports: ["formatRate", "formatDecimal"]
    - path: "site/src/components/map/ResultPanel.tsx"
      provides: "locale-correct rate display, year badges, no tilde"
  key_links:
    - from: "site/src/components/map/ResultPanel.tsx"
      to: "site/src/lib/formatNumber.ts"
      via: "import { formatRate }"
      pattern: "formatRate\\("
    - from: "ResultPanel.tsx composite/rate blocks"
      to: ".year-badge"
      via: "year badge span"
      pattern: "year-badge"
---

<objective>
Fix P1-1 (inconsistent locale number formatting across islands and static components) and P1-4 (map panel mixes years 2024/2025/2005-2026 and two rank systems without explanation, plus an unexplained "~" prefix). Both center on the map ResultPanel, so they ship together.

Purpose: EN users currently see ES-formatted numbers ("~4.984") and ES pages show EN-formatted numbers in the "Similar Communes" table. The map panel is confusing because the Composite Index score (2024) and rate (2025) sit next to two different ranks with no year context. A single `formatNumber` helper plus year badges + a "?" tooltip resolve both.

Output: `site/src/lib/formatNumber.ts` (new), wired into ResultPanel, Legend, CommuneRankingTable, ComparableCommune; ResultPanel year-badge grouping + year-mismatch tooltip; tilde removed; plural fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-RESEARCH.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-PATTERNS.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

<interfaces>
New module contract (create exactly this — from RESEARCH.md Code Examples):
  formatRate(value: number, locale: 'en' | 'es'): string
    → Math.round(value).toLocaleString(locale === 'es' ? 'es-CL' : 'en-US')
  formatDecimal(value: number, locale: 'en' | 'es', decimals = 1): string
    → value.toLocaleString(locale==='es'?'es-CL':'en-US', {minimumFractionDigits: decimals, maximumFractionDigits: decimals})

Astro components (CommuneRankingTable, ComparableCommune) already receive a `locale: 'en'|'es'` prop.
React islands (ResultPanel) receive `lang` prop. COMPOSITE_YEAR const = 2024 (ResultPanel line 41).
RateTooltipReact is already imported in ResultPanel (line 21) — reuse for the year tooltip.
</interfaces>

Number Formatting Contract (UI-SPEC): rates always 1 decimal; homicide rate 1 decimal (not 2); rank integers no decimals; NEVER rescale (values already per-100k); NO "~" prefix anywhere.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create formatNumber helper + wire all number call sites (P1-1)</name>
  <files>site/src/lib/formatNumber.ts, site/src/components/map/ResultPanel.tsx, site/src/components/map/Legend.tsx, site/src/components/CommuneRankingTable.astro, site/src/components/ComparableCommune.astro</files>
  <read_first>
    - site/src/lib/data.ts lines 1-26 (module export pattern for new lib file)
    - site/src/components/map/ResultPanel.tsx lines 200, 305, 319, 393 (hardcoded 'es-CL', tilde, the already-correct locale-aware pattern at 319)
    - site/src/components/map/Legend.tsx line 37
    - site/src/components/CommuneRankingTable.astro line 78 (bare toLocaleString) and its Props (locale: 'en'|'es')
    - site/src/components/ComparableCommune.astro line 51
    - .planning/phases/25-.../25-RESEARCH.md "### P1-1" table and Code Examples
  </read_first>
  <behavior>
    - formatRate(4984.1, 'en') === '4,984'
    - formatRate(4984.1, 'es') === '4.984'
    - formatDecimal(2871.5, 'en') === '2,871.5'
    - formatDecimal(8.69, 'es', 1) === '8,7'
    - No call site passes a hardcoded 'es-CL' regardless of the active locale.
  </behavior>
  <action>
    Create `site/src/lib/formatNumber.ts` exporting `formatRate` and `formatDecimal` exactly per the interface contract above (no imports needed — pure Intl.NumberFormat/toLocaleString wrapper).
    Replace call sites: ResultPanel.tsx line 200 `Math.round(rate).toLocaleString('es-CL')` → `formatRate(rate, lang)`; line 393 `Math.round(homRate!).toLocaleString('es-CL')` → `formatRate(homRate!, lang)`; line 305 `~{displayRate}` → `{displayRate}` (drop the tilde entirely). Legend.tsx line 37 → `formatRate(val, lang)`. CommuneRankingTable.astro line 78 (bare `toLocaleString()`) → `formatRate(val, locale)` (import from `../lib/formatNumber.ts` in frontmatter). ComparableCommune.astro line 51 → `formatRate(val, locale)`. Import in React islands via `import { formatRate, formatDecimal } from '../../lib/formatNumber';`.
    Do NOT touch ComparatorIsland.tsx here — its number calls (lines 620/644) are owned by the compare plan (25-06) to keep single-file ownership.
  </action>
  <verify>
    <automated>cd site && grep -c "es-CL" src/components/map/ResultPanel.tsx | grep -qx 0 && ! grep -q "~{displayRate}" src/components/map/ResultPanel.tsx && grep -q "formatRate" src/components/CommuneRankingTable.astro && grep -q "formatRate" src/components/ComparableCommune.astro && npm run build && echo P1-1-OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/lib/formatNumber.ts` exists and exports `formatRate` and `formatDecimal`.
    - `grep -c "es-CL" src/components/map/ResultPanel.tsx` returns 0.
    - No `~{displayRate}` literal remains in ResultPanel.tsx.
    - CommuneRankingTable.astro and ComparableCommune.astro both call `formatRate(..., locale)`.
    - `npm run build` succeeds and `@astrojs/check` (npm run check) has no new type errors.
  </acceptance_criteria>
  <done>All rate/number displays are locale-correct via one helper; EN map panel shows "4,984" and ES "Similar Communes" table shows "4.991"; no tilde anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Map panel year badges + year-mismatch tooltip + plural fix (P1-4, P2-3 plural)</name>
  <files>site/src/components/map/ResultPanel.tsx, site/src/components/map/map.css</files>
  <read_first>
    - site/src/components/map/ResultPanel.tsx line 41 (COMPOSITE_YEAR=2024), the composite-index block, the rate block, the national_rank vs composite rank displays, line 21 (RateTooltipReact import), and any "(n cases)" / "casos" render
    - site/src/components/map/RateTooltipReact.tsx (tooltip "?" trigger + body pattern to reuse)
    - .planning/phases/25-.../25-UI-SPEC.md "### Year Badge (P1-4)" (exact .year-badge CSS + tooltip EN/ES copy) and "### Plural Fix (P2-3)"
  </read_first>
  <action>
    In `site/src/components/map/ResultPanel.tsx`, group the panel into two labeled blocks: "Composite Index" with a `<span class="year-badge">2024</span>` and "Rate" with `<span class="year-badge">2025</span>` (use the actual `year` prop for the rate block, `COMPOSITE_YEAR` for composite). Keep the two rank systems (data.national_rank rate-based; composite rank) but place each inside its year-badged block so the user sees which year each rank belongs to. Add a `RateTooltipReact` "?" trigger next to the year badges whose body is the exact UI-SPEC year-mismatch copy — EN: "The Composite Index uses 2024 as the latest complete CEAD year. The incident rate uses 2025 (partial data). Rankings may differ because they use two different calculation methods." / ES: "El Índice Compuesto usa 2024 como el último año completo de CEAD. La tasa de incidentes usa 2025 (datos parciales). Los rankings pueden diferir al usar dos métodos de cálculo distintos." — inline lang-conditional strings (no i18n.ts edit).
    Fix "(n cases)" pluralization: EN `n === 1 ? 'case' : 'cases'`, ES `n === 1 ? 'caso' : 'casos'` wherever the count label renders in the panel.
    In `site/src/components/map/map.css`, add the `.year-badge` class exactly per UI-SPEC lines 175-188 (inline-flex, height 20px, padding 0 var(--sm), border-radius 4px, background var(--line), color var(--muted), font-size var(--text-label), font-weight var(--weight-strong), margin-left var(--xs)). Reference tokens only — no new hex.
  </action>
  <verify>
    <automated>cd site && grep -q "year-badge" src/components/map/ResultPanel.tsx && grep -q ".year-badge" src/components/map/map.css && ! grep -q "~{" src/components/map/ResultPanel.tsx && npm run build && npm run validate && echo P1-4-OK</automated>
  </verify>
  <acceptance_criteria>
    - ResultPanel.tsx renders `.year-badge` spans for both the composite (2024) and rate (year) blocks.
    - A RateTooltipReact "?" with the exact year-mismatch copy (EN and ES branches) is present.
    - `.year-badge` CSS defined in map.css using only tokens (background var(--line), color var(--muted)).
    - Plural branch present: `'case'`/`'cases'` and `'caso'`/`'casos'`.
    - No `~` literal precedes any number in ResultPanel.tsx.
    - `npm run build && npm run validate` exits 0 (forbidden-language validator still green — "Very High"/composite labels remain attributed).
  </acceptance_criteria>
  <done>The map panel visually separates year-tagged blocks with a "?" explanation; the two ranks are no longer ambiguous; plural is correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commune JSON → panel render | Trusted build-time data; numbers formatted for display only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-EDIT | Information Disclosure (editorial) | ResultPanel band/index labels | mitigate | Keep every "Very High"/band label attributed to the named Composite Index; forbidden-language validator run in verify |
| T-25-02-NUM | Tampering | formatNumber output | accept | Pure numeric formatting; no user input; values already per-100k, never rescaled |
| T-25-SC | Tampering (supply chain) | npm installs | mitigate | No new packages; formatNumber is a stdlib Intl wrapper |
</threat_model>

<verification>
`cd site && npm run build && npm run validate && npm run check`. Confirm: 0 `es-CL` in ResultPanel; no tilde; `.year-badge` in map.css + ResultPanel; formatRate wired in the two Astro components; forbidden-language validator green.
</verification>

<success_criteria>
- Single `formatNumber` helper is the sole locale formatter for the wired call sites.
- EN panel shows comma-grouped numbers, ES pages show period-grouped, no tilde.
- Map panel year badges + "?" tooltip render; plural correct.
- Full build+validate+check exits 0.
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-02-SUMMARY.md` when done.
</output>
