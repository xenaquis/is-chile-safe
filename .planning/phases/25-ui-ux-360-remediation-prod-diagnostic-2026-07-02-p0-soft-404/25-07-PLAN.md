---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 07
type: execute
wave: 4
depends_on: [25-01, 25-04]
files_modified:
  - site/src/layouts/BaseLayout.astro
  - site/src/styles/global.css
  - site/src/components/map/map.css
  - site/src/pages/communes/index.astro
  - site/src/pages/es/comunas/index.astro
  - site/src/components/ContactForm.astro
autonomous: true
requirements: [P2-1]
must_haves:
  truths:
    - "A keyboard user can skip to main content via a visible-on-focus skip link on every page"
    - "Link focus shows a visible outline (no component-scoped unconditional outline:none defeats the global :focus-visible rule)"
    - "The <main> landmark carries id=main-content as the skip-link target"
  artifacts:
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "Skip link as first body child + id=main-content on <main>"
      contains: "skip-link"
    - path: "site/src/styles/global.css"
      provides: ".skip-link styles (visually hidden until focus)"
      contains: ".skip-link"
  key_links:
    - from: "BaseLayout skip link"
      to: "#main-content"
      via: "href anchor to <main id>"
      pattern: "main-content"
---

<objective>
Deliver the skip-link + focus-visibility portion of P2-1 (the incident-marker accessible-name portion shipped in Plan 25-04).

Purpose: There is no skip-to-main link, and component-scoped `outline: none` overrides defeat the correct global `:focus-visible` rule on some link-focus states, hurting keyboard accessibility.

Output: a `.skip-link` (visually hidden until focused) as the first `<body>` child in BaseLayout, `id="main-content"` on `<main>`, and removal of the unconditional `outline: none` overrides on focusable links found in the codebase — keeping the global `*:focus-visible { outline: 2px solid var(--primary) }` rule intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-RESEARCH.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-PATTERNS.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

<interfaces>
- `global.css` lines 166-173 ALREADY has the correct `:focus-visible { outline: 2px solid var(--primary) }` + `*:focus:not(:focus-visible){ outline: none }` (line 173 — this one is CORRECT, keep it).
- Candidate problematic overrides found via grep: `site/src/components/map/map.css:260`, `site/src/pages/communes/index.astro:359`, `site/src/pages/es/comunas/index.astro:355`, `site/src/components/ContactForm.astro:118`. Inspect each: only remove/guard `outline: none` that applies UNCONDITIONALLY to focusable links (`a`, buttons) — leave intentional non-focus decorative uses and the correct global `:focus:not(:focus-visible)` rule.
- BaseLayout: skip link goes as first child of `<body>` (line ~114); `<main class="page-content">` (line ~116) gets `id="main-content"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skip-to-main link + main landmark id + .skip-link CSS</name>
  <files>site/src/layouts/BaseLayout.astro, site/src/styles/global.css</files>
  <read_first>
    - site/src/layouts/BaseLayout.astro lines 114-116 (body open, PageHeader, main)
    - site/src/styles/global.css lines 166-173 (existing focus-visible block — insert .skip-link after it)
    - .planning/phases/25-.../25-UI-SPEC.md "### Skip link (P2-1)" (exact .skip-link CSS) and Copywriting Contract "Skip link" (EN/ES)
  </read_first>
  <action>
    In `site/src/layouts/BaseLayout.astro`, add as the FIRST child of `<body>` (before `<PageHeader>`): `<a href="#main-content" class="skip-link">{lang === 'es' ? 'Ir al contenido principal' : 'Skip to main content'}</a>`. Add `id="main-content"` to the `<main class="page-content">` element.
    In `site/src/styles/global.css`, after the existing `:focus-visible` block (~line 173), add the `.skip-link` rule exactly per UI-SPEC (position absolute; top -40px; left var(--md); background var(--primary); color #ffffff; padding var(--xs) var(--md); border-radius 4px; font-size var(--text-label); font-weight var(--weight-strong); z-index 9999; text-decoration none; transition top 0.1s) and `.skip-link:focus { top: var(--sm); }`.
  </action>
  <verify>
    <automated>cd site && npm run build && grep -q "skip-link" dist/index.html && grep -q 'id="main-content"' dist/index.html && grep -q ".skip-link" src/styles/global.css && echo P2-1-SKIP-OK</automated>
  </verify>
  <acceptance_criteria>
    - `dist/index.html` contains a `skip-link` anchor to `#main-content` and `<main id="main-content"`.
    - ES pages render "Ir al contenido principal".
    - `.skip-link` + `.skip-link:focus` defined in global.css using tokens.
    - `npm run build && npm run validate` exits 0.
  </acceptance_criteria>
  <done>Keyboard users can jump to main content on every page; link is hidden until focused.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unconditional outline:none overrides on focusable links (focus-visible sweep)</name>
  <files>site/src/components/map/map.css, site/src/pages/communes/index.astro, site/src/pages/es/comunas/index.astro, site/src/components/ContactForm.astro</files>
  <read_first>
    - Each candidate line: map.css:260, communes/index.astro:359, es/comunas/index.astro:355, ContactForm.astro:118 — read the full selector + surrounding rule to judge intent
    - site/src/styles/global.css lines 166-173 (the CORRECT global rule — do NOT change it)
    - .planning/phases/25-.../25-RESEARCH.md "### P2-1" (focus-visible sub-row) + Open Question 3
  </read_first>
  <action>
    Inspect each candidate `outline: none`. Where it unconditionally strips the focus outline from a focusable link/button (defeating keyboard focus visibility), either remove it or scope it behind `:focus:not(:focus-visible)` so the global focus-visible outline still shows on keyboard focus. Do NOT alter the correct global rule at global.css:173. Leave any `outline: none` that is not on a focusable interactive element or is already correctly guarded. If a candidate turns out to be intentional/harmless, note it in the SUMMARY rather than changing it.
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate && npm run check && echo P2-1-FOCUS-OK</automated>
  </verify>
  <acceptance_criteria>
    - No focusable link/button in the four files strips its keyboard focus outline unconditionally (each remaining `outline: none` is either guarded by `:not(:focus-visible)` or applies to a non-interactive/decorative element, documented in SUMMARY).
    - The global `:focus-visible` rule at global.css:173 is unchanged.
    - `npm run build && npm run validate && npm run check` all exit 0.
    - Keyboard-focus visibility confirmed visually in Plan 09.
  </acceptance_criteria>
  <done>Keyboard focus is visible on links; no component override defeats the global focus-visible outline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard user → focus management | A11y-only change; no data boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07-A11Y | Repudiation (accessibility gap) | missing skip link / hidden focus | mitigate | Skip link + restored focus-visible outlines |
| T-25-07-XSS | Injection | skip-link label | accept | Static literal copy, lang-conditional, no user input |
| T-25-SC | Tampering (supply chain) | npm installs | mitigate | No new packages |
</threat_model>

<verification>
`cd site && npm run build && npm run validate && npm run check`. Confirm skip-link + main-content id in dist; no unconditional outline:none on focusable links; global rule intact.
</verification>

<success_criteria>
- Skip link present + functional target; focus-visible outlines restored.
- Full build+validate+check exit 0.
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-07-SUMMARY.md` when done.
</output>
