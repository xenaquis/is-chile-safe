---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 04
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - site/src/components/map/MapIsland.tsx
  - site/src/components/map/IncidentPinLayer.ts
  - site/src/components/map/Legend.tsx
  - site/src/components/map/MapTopbar.tsx
  - site/src/components/map/map.css
  - site/src/styles/global.css
autonomous: true
requirements: [P1-3, P2-1]
must_haves:
  truths:
    - "The map opens framed on continental Chile (fitBounds + maxBounds), not as a thin strip on the left with 60% ocean"
    - "Incident pins are a distinct purple (--pin #6d28d9) with a white border, clearly different from the orange choropleth, and the legend shows a pin row when the incident layer is active"
    - "On mobile (≤480px) the legend is collapsible and the Index/By-crime pills stack in their own row (no overlap with the search box)"
    - "An unknown ?cut= value never fires a network fetch (validated before selection)"
    - "Each incident marker has an accessible name (aria-label/title) with the incident title"
  artifacts:
    - path: "site/src/components/map/MapIsland.tsx"
      provides: "fitBounds framing + maxBounds + validated ?cut= guard"
      contains: "fitBounds"
    - path: "site/src/styles/global.css"
      provides: "--pin token"
      contains: "--pin"
  key_links:
    - from: "MapIsland.tsx"
      to: "?cut= param"
      via: "5-digit regex guard before selectCommune"
      pattern: "test\\(focusCut\\)|\\\\d\\{5\\}"
    - from: "IncidentPinLayer.ts marker"
      to: "escHtml(incident title)"
      via: "aria-label on divIcon html"
      pattern: "aria-label"
---

<objective>
Fix P1-3 (map initial framing, mobile legend, filter-pill overlap, indistinct incident-pin color, unguarded ?cut= fetch) and deliver the incident-marker accessible-name portion of P2-1.

Purpose: The map currently opens with Chile as a thin left strip (60%+ ocean/Argentina), the mobile legend eats 40% of the map height, the Index/By-crime pills overlap the search box at ≤480px, incident pins are nearly the same orange as the "High" choropleth level, an invalid ?cut= fires a silent 404 fetch, and ~130 incident markers have no accessible name.

Output: fitBounds+maxBounds framing, `--pin #6d28d9` pin color with white border + legend pin row, collapsible mobile legend, pills stacked at ≤480px, regex-guarded ?cut=, and aria-label/title on incident markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-RESEARCH.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-PATTERNS.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

<interfaces>
- MapIsland.tsx line 125: `map.setView([-35.7, -71.8], 5)` (replace).
- MapIsland.tsx lines 225-229: `?cut=` read + setTimeout(selectCommune) with NO validation.
- IncidentPinLayer.ts line 154-160: `L.marker(...)` with `L.divIcon({ html: '<div class="ev-dot"></div>' })`, no title/aria. `escHtml()` already defined in this file — reuse it.
- map.css: `.ev-dot` at line 424, `.ev-dot-mini` at line 113. Pills/mode-toggle live in MapTopbar.tsx + map.css.
- Chile continental bbox (RESEARCH A2): fitBounds([[-55.9,-75.7],[-17.5,-66.4]], {padding:[20,20]}); setMaxBounds([[-60,-80],[-14,-60]]); setMinZoom(4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map framing + ?cut= guard (P1-3 framing + guard)</name>
  <files>site/src/components/map/MapIsland.tsx</files>
  <read_first>
    - site/src/components/map/MapIsland.tsx lines 96 (year useState default 2025), 125 (setView), 181 (communeIndex set after fetch), 225-229 (focusCut read)
    - .planning/phases/25-.../25-RESEARCH.md "### P1-3" table + Pitfall 3 (?cut= race) + Code Examples bbox
  </read_first>
  <action>
    In `site/src/components/map/MapIsland.tsx` replace line 125 `map.setView([-35.7, -71.8], 5)` with `map.fitBounds([[-55.9, -75.7], [-17.5, -66.4]], { padding: [20, 20] })`, then `map.setMaxBounds([[-60, -80], [-14, -60]])` and `map.setMinZoom(4)` so panning cannot leave Chile framing and the initial view centers continental Chile.
    Guard the `?cut=` deep link (lines 225-229): only call `selectCommune(focusCut)` when `focusCut` matches `/^\d{5}$/` (all Chilean CUTs are 5-digit integers) — this avoids the silent `/data/cead/comunas/99999.json` 404 for invalid params without waiting on the communeIndex race. Keep the existing setTimeout.
  </action>
  <verify>
    <automated>cd site && grep -q "fitBounds" src/components/map/MapIsland.tsx && grep -q "setMaxBounds" src/components/map/MapIsland.tsx && grep -Eq "\\\\d\\{5\\}|test\\(focusCut\\)" src/components/map/MapIsland.tsx && ! grep -q "setView(\[-35.7" src/components/map/MapIsland.tsx && npm run build && echo P1-3-MAP-OK</automated>
  </verify>
  <acceptance_criteria>
    - MapIsland.tsx calls `fitBounds([[-55.9, -75.7], [-17.5, -66.4]]...)`, `setMaxBounds(...)`, `setMinZoom(4)`.
    - The old `setView([-35.7, -71.8], 5)` is gone.
    - `?cut=` is only used to select when it matches `/^\d{5}$/`.
    - `npm run build` succeeds.
    - Visual framing confirmation deferred to Plan 09 BrowserOS review.
  </acceptance_criteria>
  <done>Map opens centered on Chile; invalid ?cut= no longer triggers a 404 fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Distinct incident pins + marker accessible name + legend pin row (P1-3 pins, P2-1 markers)</name>
  <files>site/src/components/map/IncidentPinLayer.ts, site/src/components/map/Legend.tsx, site/src/components/map/map.css, site/src/styles/global.css</files>
  <read_first>
    - site/src/components/map/IncidentPinLayer.ts (escHtml helper, L.divIcon/L.marker at lines 154-160, incident fields title_es/title_en, lang)
    - site/src/components/map/map.css lines 113 (.ev-dot-mini) and 424 (.ev-dot — current color)
    - site/src/components/map/Legend.tsx (band rows; where to append the incident-layer row + how it knows the incidents layer is active)
    - .planning/phases/25-.../25-UI-SPEC.md "### Map Legend — Incident Layer Entry", "Incident pin color — NEW", Copywriting Contract "Incident legend row"
  </read_first>
  <action>
    Add `--pin: #6d28d9;` to `:root` in `site/src/styles/global.css`. In `site/src/components/map/map.css`, change `.ev-dot` background from the current orange (`#e8a05c`) to `var(--pin)` and add `border: 2px solid #ffffff;` (keep existing 14×14 size/border-radius); update `.ev-dot-mini` to the same `var(--pin)`.
    In `IncidentPinLayer.ts`, compute `const escapedTitle = escHtml(lang === 'es' ? incident.title_es : incident.title_en);` and set the marker's `L.divIcon` html to `<div class="ev-dot" role="img" aria-label="${escapedTitle}"></div>` AND add `title: escapedTitle` to the marker options (Leaflet renders it as a DOM title). Always escape via the existing `escHtml` (titles come from external RSS — XSS boundary).
    In `Legend.tsx`, when the incidents layer is active, append one legend row below the 5 choropleth bands: a round swatch `background: var(--pin)` + label "Reported incidents" (EN) / "Incidentes reportados" (ES), inline lang-conditional (no i18n.ts edit).
  </action>
  <verify>
    <automated>cd site && grep -q "\-\-pin" src/styles/global.css && grep -q "var(--pin)" src/components/map/map.css && grep -q "aria-label" src/components/map/IncidentPinLayer.ts && grep -q "escHtml" src/components/map/IncidentPinLayer.ts && grep -Eq "Reported incidents|Incidentes reportados" src/components/map/Legend.tsx && npm run build && echo P1-3-PIN-OK</automated>
  </verify>
  <acceptance_criteria>
    - `--pin: #6d28d9` present in global.css `:root`; `.ev-dot`/`.ev-dot-mini` use `var(--pin)` with a white border on `.ev-dot`.
    - IncidentPinLayer markers set both `aria-label` (in divIcon html) and `title`, each escaped via `escHtml`.
    - Legend shows a `var(--pin)` incident row with "Reported incidents"/"Incidentes reportados" when the incidents layer is active.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Incident pins are visually distinct from the choropleth, appear in the legend, and are announced by screen readers.</done>
</task>

<task type="auto">
  <name>Task 3: Collapsible mobile legend + pill stacking ≤480px (P1-3 mobile)</name>
  <files>site/src/components/map/Legend.tsx, site/src/components/map/MapTopbar.tsx, site/src/components/map/map.css</files>
  <read_first>
    - site/src/components/map/Legend.tsx (current legend markup + any existing min-width)
    - site/src/components/map/MapTopbar.tsx (the Index/By-crime mode-toggle pills + search box row structure)
    - site/src/components/map/map.css (existing legend + topbar rules; `.tooltip-body`/`details` pattern to mirror)
    - .planning/phases/25-.../25-UI-SPEC.md "### Collapsible Mobile Legend", "### Breakpoints" (Map filter pills stacking), Copywriting Contract "Mobile legend toggle"
  </read_first>
  <action>
    Collapsible legend at ≤480px: wrap the legend body so that at ≤480px it renders inside a `<details>` with a `<summary class="legend-title">` toggle (reuse the existing tooltip/details pattern); default collapsed; summary label "Crime Index ▸"/"Índice delictivo ▸" (closed) and "Crime Index ▾"/"Índice delictivo ▾" (open) via CSS `details[open]`. At ≤480px set min-width 100px / max-width 160px (drop the existing `min-width: 140px`). On desktop the legend stays fully expanded (media query).
    Pill stacking: at ≤480px make `.map-topbar-row1` wrap to a column and change `.mode-toggle` from `position: absolute; top: 8px; right: 16px` to `position: static` so the Index/By-crime pills appear as their own second row and no longer overlap the search input. Implement in map.css (and MapTopbar.tsx markup only if the current structure prevents the CSS-only stack).
  </action>
  <verify>
    <automated>cd site && grep -Eq "max-width: 480px|480px" src/components/map/map.css && grep -Eq "Crime Index|Índice delictivo" src/components/map/Legend.tsx && npm run build && npm run validate && echo P1-3-MOBILE-OK</automated>
  </verify>
  <acceptance_criteria>
    - map.css has a ≤480px media query stacking `.map-topbar-row1` to column and setting `.mode-toggle` to `position: static`.
    - Legend uses a `<details>`/`<summary>` collapse at ≤480px with the exact toggle labels (EN/ES).
    - `npm run build && npm run validate` exits 0.
    - Mobile legend/pill behavior confirmed visually in Plan 09 (375px iframe).
  </acceptance_criteria>
  <done>Mobile legend is compact/collapsible and the filter pills no longer overlap the search box.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL ?cut= param → data fetch | Untrusted param used to build a JSON fetch path |
| external RSS incident title → marker DOM | Untrusted title injected into divIcon html + title attr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-URL | Tampering | MapIsland ?cut= | mitigate | `/^\d{5}$/` guard before selectCommune/fetch — invalid CUT never fetches |
| T-25-04-XSS | Injection (XSS) | IncidentPinLayer divIcon html/title | mitigate | escHtml() on incident title before interpolation into aria-label/title |
| T-25-SC | Tampering (supply chain) | npm installs | mitigate | No new packages; Leaflet already present |
</threat_model>

<verification>
`cd site && npm run build && npm run validate`. Confirm fitBounds/setMaxBounds/setMinZoom + 5-digit guard in MapIsland; `--pin` token + `var(--pin)` in map.css; aria-label + escHtml in IncidentPinLayer; legend incident row copy; ≤480px pill/legend rules. Full validator suite green.
</verification>

<success_criteria>
- Map framed on Chile with maxBounds; invalid ?cut= guarded.
- Purple bordered incident pins + legend row; markers have accessible names.
- Mobile legend collapsible; pills stacked ≤480px.
- Build+validate exit 0.
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-04-SUMMARY.md` when done.
</output>
