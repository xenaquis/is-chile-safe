---
phase: 31-docs-methodology-refresh
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/pages/news.astro
  - site/src/pages/es/noticias.astro
  - site/src/config/i18n.ts
autonomous: true
requirements: [DOCS-04, DOCS-01]

# AMENDED at Fable plan review (2026-08-03): added the stale-header correction below.
# `site/src/pages/news.astro:5` reads "incidents sourced from RSS feeds + DeepSeek geolocation".
# Both halves are stale: the default classifier is Granite 4.1 8B via OpenRouter, and geolocation
# is done by the deterministic name->CUT resolver, not by the LLM. It is a source comment, not
# reader-facing prose, so it is a one-line DOCS-01 drift fix with zero rendering impact — replace
# it with "incidents sourced from Google News RSS search, classified by the news pipeline's LLM
# classifier and geolocated by the deterministic commune name->CUT resolver". The ES twin's
# header (es/noticias.astro:5) makes no pipeline claim and must NOT gain one.
#
# ACCEPTED, not changed (recorded so it reads as a decision, not an oversight): the reader-facing
# sentence at news.astro:187 / es/noticias.astro:187 ("Incidents are extracted from RSS feeds" /
# "se extraen de feeds RSS") stays as-is. Google News RSS *is* RSS, so the claim is still true,
# and F-63 forbids rewriting prose that is not wrong.

must_haves:
  truths:
    - "A first-time visitor to /news/ or /es/noticias/ reads a short note explaining that facet counts are unfiltered, that 'latest' is anchored to the newest incident's date (not wall-clock today), and that news incident counts are not comparable to CEAD per-100k rates."
    - "No reader-facing clustering explainer is added anywhere — DOCS-04's clustering half stays N/A per Phase 26's NO-GO, recorded internally only (Plan 31-04's STATE.md task), never on a reader-facing page."
    - "EN and ES pages carry the same note via the same i18n.ts key pair, so parity cannot half-land."
  artifacts:
    - path: "site/src/pages/news.astro"
      provides: "Facet-semantics note rendered in the page intro, before the filter bar"
    - path: "site/src/pages/es/noticias.astro"
      provides: "Facet-semantics note (ES) rendered in the page intro, before the filter bar"
    - path: "site/src/config/i18n.ts"
      provides: "news_facet_semantics_note key in both EN_STRINGS and ES_STRINGS"
  key_links:
    - from: "site/src/pages/news.astro"
      to: "site/src/config/i18n.ts"
      via: "t.news_facet_semantics_note"
      pattern: "news_facet_semantics_note"
---

**Fable review: APPROVED 2026-08-03** — with amendments applied inline: the `astro check` criterion no longer pins the hint count (Plan 31-04 changes it by design, which would have reddened this gate for an unrelated reason), and the stale `news.astro:5` header comment crediting "DeepSeek geolocation" is folded in as a one-line DOCS-01 drift fix. This plan satisfies DOCS-04 via a facet-semantics note rather than a clustering explainer, per **F-61** (Phase 26 returned NO-GO — there is no clustering feature for a reader to encounter). Binding decisions: F-59..F-69 in STATE.md.

<objective>
Ship the reader-facing facet-semantics note DOCS-04 requires in place of a clustering
explainer (Phase 26 returned NO-GO; there is no shipped clustering feature to document).
Covers: what the time-window facets mean and that "latest" is anchored to the newest
incident's date, not wall-clock; that facet counts are unfiltered per-option marginals;
and that the news layer is qualitative and not comparable to CEAD per-100k rates.

Purpose: Phase 28 shipped a facet UI with zero reader-facing explanation of what a count
or a time window means (`newsFacets.ts`'s documented contract, F-20).
Output: A short bilingual note on both news pages, added via shared i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-docs-methodology-refresh/31-RESEARCH.md
@.planning/STATE.md

<interfaces>
<!-- Ground truth from Phase 27/28 SUMMARYs, do not re-derive. -->
site/src/lib/newsFacets.ts contract (F-20): facet counts are unfiltered marginal totals
over the window; "today"/latest window is anchored to the newest incident's date, not
wall-clock (deliberate — a stalled cron would otherwise empty the bucket).
site/src/config/i18n.ts: EN_STRINGS/ES_STRINGS are plain object literals; existing
news_* keys live around lines 141-147 (type interface) and 369-375 / 602-608 (values).
Follow that exact key-naming and placement convention for the new key.
site/src/pages/news.astro (lines 182-196) / site/src/pages/es/noticias.astro (lines
182-196): both pages share an identical opening `<section>` structure — `<h1>`, an intro
`<p>`, then a conditional `{generated && (<p class="freshness">...)}` block. The new note
goes inside that same `<section>`, immediately after the freshness block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the news_facet_semantics_note i18n key pair</name>
  <files>site/src/config/i18n.ts</files>
  <read_first>site/src/config/i18n.ts (lines 135-150 for the type interface block, lines 360-380 for EN_STRINGS news_* values, lines 595-615 for ES_STRINGS news_* values)</read_first>
  <action>
  Add `news_facet_semantics_note: string;` to the shared Strings type interface, in the
  same news_* block as the existing `news_window_all` / `news_clear_filters` entries.
  Add to EN_STRINGS: `news_facet_semantics_note: 'Filters show unfiltered per-option
  counts. "Latest" is anchored to the newest incident\'s date, not to today\'s calendar
  date — if the news feed pauses, "Latest" reflects the last successful update, not
  necessarily today. Incident counts are a qualitative, editorially-selected news sample
  and are not comparable to the CEAD per-100k statistical rates shown elsewhere on this
  site.'` Add to ES_STRINGS the Spanish equivalent: `news_facet_semantics_note: 'Los
  filtros muestran conteos por opción sin filtrar. "Más reciente" está anclado a la fecha
  del incidente más nuevo registrado, no a la fecha calendario de hoy — si el feed de
  noticias se detiene, "Más reciente" refleja la última actualización exitosa, no
  necesariamente hoy. Los conteos de incidentes son una muestra cualitativa y de
  selección editorial de noticias, y no son comparables con las tasas estadísticas del
  CEAD por 100.000 habitantes que se muestran en el resto del sitio.'` Match the existing
  quoting style used by neighboring entries in the file exactly (single-quoted string
  literals with escaped apostrophes) so `@astrojs/check`/TypeScript parsing is unaffected.
  </action>
  <acceptance_criteria>
  `grep -c "news_facet_semantics_note" site/src/config/i18n.ts` equals 3 (type interface + EN_STRINGS + ES_STRINGS).
  `npx astro check` from `site/` reports exactly **4 errors / 0 warnings**, all four in `ComparatorPairsLinks.astro:28` — zero new errors introduced by this key addition. Do NOT assert the hint count: `tsconfig.json`'s `include` covers `scripts/**/*.mjs`, so the 43 hints move whenever any validator file gains an untyped function — which Plan 31-04 does by design. Pinning hints would make this criterion go RED for a reason unrelated to what it is measuring.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render the note on both news pages, before the filter bar</name>
  <files>site/src/pages/news.astro, site/src/pages/es/noticias.astro</files>
  <read_first>site/src/pages/news.astro (lines 182-198), site/src/pages/es/noticias.astro (lines 182-198)</read_first>
  <action>
  In site/src/pages/news.astro, immediately after the closing of the `{generated && (
  <p class="freshness">...</p>
  )}` block and before the `</section>` closing tag (i.e., still inside the first
  `<section>`, after the freshness paragraph), add:
  `<p class="facet-semantics-note" id="news-facet-semantics">{t.news_facet_semantics_note}</p>`.
  Apply the identical markup at the identical structural position in
  site/src/pages/es/noticias.astro. Do not add any new client-side script, hydration
  directive, or CSS class definition beyond what already exists for `.freshness` — reuse
  the existing typography; a bare paragraph with the new class is sufficient (no new CSS
  rule is required by this task; if the class needs any styling it must be a trivial
  addition to the page's existing `<style>` block matching `.freshness`'s treatment, not a
  new stylesheet).
  </action>
  <acceptance_criteria>
  `grep -c 'id="news-facet-semantics"' site/src/pages/news.astro` equals 1.
  `grep -c 'id="news-facet-semantics"' site/src/pages/es/noticias.astro` equals 1.
  After `cd site && npm run build`: `grep -c 'id="news-facet-semantics"' dist/news/index.html` equals 1 and `grep -c 'id="news-facet-semantics"' dist/es/noticias/index.html` equals 1.
  No `client:` directive appears in either page (grep confirms zero occurrences of `client:load|client:idle|client:visible|client:only` in both files) — the note is static, matching NEWSUI-06's zero-hydration invariant.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. `t.news_facet_semantics_note` is a static, developer-authored string rendered
via Astro's default (escaped) `{expression}` interpolation — not `set:html` — so no new
XSS surface is introduced. No new input, no new dependency.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering | news.astro / es/noticias.astro static prose | accept | Developer-authored string, standard escaped Astro interpolation, no data-derived content. |
</threat_model>

<verification>
`cd site && npm run build && npm run validate` (chained). `facets.mjs` (validator #16) is
extended in Plan 31-04 to assert this note's presence in the built pages — that assertion
is written and run there, not in this plan, since it depends on this plan's exact output
existing first.
</verification>

<success_criteria>
Both news pages render the facet-semantics note via shared i18n keys, statically, with no
new hydration and no clustering language.
</success_criteria>

<output>
Create `.planning/phases/31-docs-methodology-refresh/31-03-SUMMARY.md` when done
</output>
