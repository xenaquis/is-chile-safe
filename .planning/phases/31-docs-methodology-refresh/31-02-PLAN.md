---
phase: 31-docs-methodology-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/SOURCES.md
autonomous: true
requirements: [DOCS-03, DOCS-02]

must_haves:
  truths:
    - "data/SOURCES.md's News feeds section describes the pipeline that actually runs today (Google News RSS search queries + gnews_decoder outlet resolution + Granite-4.1-8b/OpenRouter default classifier), not the retired direct-RSS/DeepSeek architecture."
    - "data/SOURCES.md names the R2 full-text research archive as an internal, non-reader-displayed corpus."
    - "data/SOURCES.md no longer cites the non-existent data/news/current.json path; it cites the real data/incidents/current.json artifact."
    - "The ENUSC [verify edition] marker is resolved to an explicit, honest 'intentionally generic — no edition claimed' statement; no year is written anywhere."
  artifacts:
    - path: "data/SOURCES.md"
      provides: "Current, canonical News feeds section + resolved ENUSC vintage marker"
      contains: "news.google.com"
  key_links:
    - from: "data/SOURCES.md"
      to: "pipeline/news/feeds.py google_news_url() / gnews_decoder.py / classifier.py"
      via: "prose describing the ingestion mechanism"
      pattern: "news.google.com"
---

**Fable review: APPROVED 2026-08-03** — with amendments applied inline. Both Opus passes found that this plan shipped TWO acceptance gates that could not fail: `grep -c "Emol (https"` already returns 0 on the untouched tree (a backtick sits after the paren), and the `grep -A3` window stops 12 lines short of the `**Vintage:**` line it claims to check. Both verified by execution and replaced with falsifiable equivalents (each new check confirmed to return a non-zero count on today's tree, so it can actually go red). Also added: the F15 token dependency (`RSS` + `news` must survive the section rewrite, or an unrelated figure orphans). `data/SOURCES.md` is authorized for editing per **F-66** — the directive's `data/` read-only rule covers generated pipeline artifacts, not this hand-authored registry, which DOCS-03 requires be brought current. Binding decisions: F-59..F-69 in STATE.md.

<objective>
Bring `data/SOURCES.md`'s "News feeds" section current with the pipeline that actually
ships today (Google News RSS search + outlet-decoding + Granite/OpenRouter default
classifier), add the previously-undocumented R2 research archive, fix a dead file-path
citation, and resolve the standing `[verify edition]` marker on the ENUSC underreporting
citation the only way the evidence supports: explicitly and honestly generic, with no
year ever written.

Purpose: SOURCES.md is the canonical attribution registry `figure-registry.mjs` reads
against; it must describe what the code does, not a retired architecture.
Output: data/SOURCES.md with a rewritten News feeds section and a resolved ENUSC vintage line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-docs-methodology-refresh/31-RESEARCH.md
@.planning/STATE.md

<interfaces>
<!-- Ground truth. Do not re-derive. -->
pipeline/news/feeds.py:22-32 — `google_news_url(query)` builds
`https://news.google.com/rss/search?q=...&hl=es-419&gl=CL&ceid=CL:es-419`; a
`_GOOGLE_NEWS_QUERIES` registry (e.g. `GoogleNews-Nacional`) drives searches, not fixed
per-outlet feeds.
pipeline/news/gnews_decoder.py — decodes `news.google.com` redirect URLs to the real
outlet.
pipeline/news/feeds.py:145-159 — `_extract_outlet_from_item()`: outlet is read per-item
from the RSS `<source>` tag; falls back to the literal string "Google News" when that tag
is empty or absent.
pipeline/news/classifier.py:8,73-79 — default classifier is `ibm-granite/granite-4.1-8b`
via OpenRouter; DeepSeek v4-flash is a fallback path only.
pipeline/tests/test_archive_r2.py — a live, tested R2-backed (bucket `ischilesafe`)
full-text archival pipeline: full-text corpus, APA citations, provenance ledger, daily
cron; internal/research-only, never reader-displayed.
Real storage path: data/incidents/current.json (rolling) + data/incidents/archive/
(monthly) — NOT data/news/current.json, which does not exist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite the News feeds section and add the R2 archive entry</name>
  <files>data/SOURCES.md</files>
  <read_first>data/SOURCES.md (lines 386-411, the full "## News feeds — qualitative incident layer (RSS)" section)</read_first>
  <action>
  Replace the entire "## News feeds — qualitative incident layer (RSS)" section (lines
  386-411) with a rewritten section retitled "## News feeds — qualitative incident layer
  (Google News RSS)" that covers, as separate labeled sub-points under the existing
  **Role:** / **Licence:** style used elsewhere in this file: (1) an **Ingestion
  mechanism:** point stating incidents are sourced via Google News RSS search queries
  (`pipeline/news/feeds.py`, `google_news_url()`) driven by a named query registry
  (`_GOOGLE_NEWS_QUERIES`, e.g. `GoogleNews-Nacional`), not fixed per-outlet feeds, and
  that `pipeline/news/gnews_decoder.py` resolves each Google News redirect URL back to
  the real publishing outlet; (2) an **Outlet attribution:** point stating the true
  outlet name is extracted per item from the RSS `<source>` tag
  (`_extract_outlet_from_item()`), falling back to the literal label "Google News" when
  that tag is empty, so readers may occasionally see "Google News" cited as the source
  for that honest reason; (3) a **Classification:** point naming
  `ibm-granite/granite-4.1-8b` via OpenRouter (`pipeline/news/classifier.py`) as the
  default model this milestone, with DeepSeek v4-flash retained as a fallback path only;
  (4) a **Full-text research archive (internal, not reader-displayed):** point describing
  the daily-cron R2-backed archive (bucket `ischilesafe`) of full incident text, APA
  citations, and a provenance ledger for audit/research purposes
  (`pipeline/tests/test_archive_r2.py`), explicitly stating it is not surfaced to site
  readers and is not part of the qualitative incident layer; (5) a **Storage:** point
  replacing the dead `data/news/current.json` citation with the real
  `data/incidents/current.json` (rolling window) plus `data/incidents/archive/` (monthly
  archives). Keep the existing **Attribution requirement:**, **Measure semantics:**,
  **Vintage:**, and **Licence:** points, updating only their wording if it still refers to
  the retired per-outlet-feed framing (e.g. "Coverage is subject to editorial selection by
  each news outlet" should become "...reflected in Google News' indexing of Chilean press
  coverage").
  </action>
  <acceptance_criteria>
  `grep -c "news.google.com" data/SOURCES.md` >= 1.
  `grep -c "granite" data/SOURCES.md` (case-insensitive, i.e. `grep -ic`) >= 1.
  `grep -c "R2" data/SOURCES.md` >= 1 and `grep -c "ischilesafe" data/SOURCES.md` >= 1.
  `grep -c "data/news/current.json" data/SOURCES.md` equals 0 (dead path removed).
  `grep -c "data/incidents/current.json" data/SOURCES.md` >= 1.
  RETIRED-FEED REMOVAL — the planner's original criterion here was `grep -c "Emol (https"` equals 0,
  which is VACUOUS: a backtick sits between `(` and `https` in the real file, so it already returns 0
  on the untouched tree and an executor who deleted nothing would still pass. Verified by execution.
  Use these five instead; each returns >= 1 on the untouched tree today (verified), so each is
  genuinely falsifiable:
  `grep -c "www.emol.com/rss" data/SOURCES.md` equals 0.
  `grep -c "biobiochile.cl/feed" data/SOURCES.md` equals 0.
  `grep -c "t13.cl/rss.xml" data/SOURCES.md` equals 0.
  `grep -c "cooperativa.cl/rss" data/SOURCES.md` equals 0.
  `grep -c "DeepSeek v4-flash" data/SOURCES.md` equals 0.
  F15 DEPENDENCY (do not break it): `figure-registry.mjs:178-190`'s F15 passes only if BOTH the
  literal `RSS` and the literal `news` still appear somewhere in `data/SOURCES.md`. The rewrite
  describes "Google News RSS search", which satisfies both — but assert it explicitly:
  `grep -c "RSS" data/SOURCES.md` >= 1 and `grep -c "news" data/SOURCES.md` >= 1, and
  `cd site && node scripts/validate/figure-registry.mjs` exits 0 with F15 and F16 both PASS.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Resolve the ENUSC [verify edition] marker as intentionally generic</name>
  <files>data/SOURCES.md</files>
  <read_first>data/SOURCES.md (lines 292-315, "## ENUSC — underreporting / cifra negra" section)</read_first>
  <action>
  In the "## ENUSC — underreporting / cifra negra" section, replace the two-line
  **Vintage:** entry ("Cite the exact ENUSC edition year used for the on-page claims.
  [verify edition]") with: "**Vintage:** Resolved: intentionally generic — no specific
  ENUSC edition year is claimed. The three underreporting bullets on the methodology
  pages cite the ENUSC programme generically (property-crime, intra-family-violence, and
  homicide reporting-rate patterns), not a dated statistic; no code path in this
  repository ties those bullets to a specific survey year, and naming one without human
  confirmation would replace an honest gap with an unverifiable claim. Distinct from the
  ENUSC 2024 SAE VHDV snapshot below, which is dated and code-verified." Never write a
  specific year (e.g. "2024", "2025") anywhere in this replacement text as the edition of
  the underreporting survey itself — the only permitted year references are the ones
  already present for the separate, distinct SAE VHDV dataset, which this task does not
  touch.
  </action>
  <acceptance_criteria>
  `grep -c "\[verify edition\]" data/SOURCES.md` equals 0.
  `grep -c "intentionally generic" data/SOURCES.md` equals 1.
  SECTION-SCOPED YEAR CHECK — the planner's original criterion used `grep -A3`, which is VACUOUS:
  the heading is at line 292 and the `**Vintage:**` line it claims to check is at line 307, so the
  3-line window never reaches it and the check returns 0 even if the executor writes "ENUSC 2024"
  straight into the Vintage line. Verified by execution. Use a real section extraction instead
  (`awk`, no pipe into the exit-status-bearing command):
  `awk '/^## ENUSC — underreporting/{f=1;next} /^## /{f=0} f' data/SOURCES.md > "$TMPDIR/enusc-sec.txt"`
  then, against that extracted section file:
    `grep -c "ENUSC 20" "$TMPDIR/enusc-sec.txt"` equals 0 (no edition year claimed for the underreporting survey).
    `grep -c "intentionally generic" "$TMPDIR/enusc-sec.txt"` equals 1 (proves the resolution landed INSIDE this section, not merely somewhere in the file).
  Falsifiability check, run once and reported: re-run the same two commands against a scratch copy of
  the section with "ENUSC 2024" inserted into the Vintage line — the first must return >= 1. Do not
  write that scratch copy back to `data/SOURCES.md`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan edits only `data/SOURCES.md`, a static Markdown documentation registry
read by build-time tooling (`figure-registry.mjs`) and rendered nowhere as executable
code. No new dependency, no data mutation to the pipeline's scraped datasets (`data/`'s
READ-ONLY constraint applies to scraped/computed data artifacts, not to this
hand-maintained attribution document, which DOCS-03 explicitly requires editing).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | data/SOURCES.md | accept | Static doc, no runtime input; `figure-registry.mjs` (extended in Plan 31-04) is the only consumer and only reads it as plain text. |
</threat_model>

<verification>
`grep -c "news.google.com" data/SOURCES.md` and `grep -ic "granite" data/SOURCES.md` and
`grep -c "R2" data/SOURCES.md` all >= 1. `grep -c "\[verify edition\]" data/SOURCES.md`
equals 0. `cd site && node scripts/validate/figure-registry.mjs` still exits 0 (F11/F16
unaffected — this plan does not touch the ENUSC SAE VHDV section's own F16 tokens).
</verification>

<success_criteria>
data/SOURCES.md's News feeds section matches the live pipeline; R2 archive documented;
dead file-path citation fixed; ENUSC `[verify edition]` resolved without ever writing a
year for the underreporting citation.
</success_criteria>

<output>
Create `.planning/phases/31-docs-methodology-refresh/31-02-SUMMARY.md` when done
</output>
