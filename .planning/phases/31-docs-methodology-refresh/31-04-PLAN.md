---
phase: 31-docs-methodology-refresh
plan: 04
type: execute
wave: 2
depends_on: ["31-01", "31-02", "31-03"]
files_modified:
  - site/scripts/validate/figure-registry.mjs
  - site/scripts/validate/figure-registry.test.ts
  - site/scripts/validate/coverage.mjs
  - site/scripts/validate/facets.mjs
  - .planning/STATE.md
autonomous: true
requirements: [DOCS-05, DOCS-01, DOCS-03, DOCS-04, DOCS-06]

must_haves:
  truths:
    - "figure-registry.mjs's F16 check can no longer report PASS against a stub '## INE ENUSC SAE' section containing only its heading — a real, executable mutation test proves this, run against a hardcoded fixture string, never against data/SOURCES.md itself."
    - "The real, fully-populated '## INE ENUSC SAE' section in data/SOURCES.md still passes the hardened check, unchanged."
    - "A build-time assertion fails if the national_rank direction claim ever regresses back to 'lowest reported rate' in either methodology page or the safest-cities page."
    - "A build-time assertion fails if data/SOURCES.md's News section ever loses its Google News / Granite / R2 tokens."
    - "A build-time assertion fails if the facet-semantics note (Plan 31-03) is ever removed from either news page's built HTML."
    - "The four actively-linked methodology anchors are asserted present in the built dist/ HTML, closing DOCS-06's previously-nonexistent fragment-level check."
    - "The two pre-existing map defects (legend bands never vary by family; AVAILABLE_YEARS offers a yearless 2026) and DOCS-04's clustering N/A status are recorded in STATE.md as internal backlog notes, never as reader-facing prose."
    - "The full validator suite is 16/16, or 15/16 with only freshness excluded per the pre-authorized F-19 branch — this exact disjunction is stated in the close-out, never encoded into a gate command."
  artifacts:
    - path: "site/scripts/validate/figure-registry.mjs"
      provides: "Section-bounded, minimum-length-guarded F16 check + companion DOCS-01/DOCS-03 content assertions, exported pure functions for testing"
    - path: "site/scripts/validate/figure-registry.test.ts"
      provides: "Real vitest mutation test: stub section fails, real section passes"
    - path: "site/scripts/validate/coverage.mjs"
      provides: "New assertion F: anchor-presence check for the four linked methodology anchors"
    - path: "site/scripts/validate/facets.mjs"
      provides: "New assertion 23: facet-semantics note present in both built news pages"
    - path: ".planning/STATE.md"
      provides: "Phase 31 backlog notes for the two map defects + DOCS-04 clustering N/A"
  key_links:
    - from: "site/scripts/validate/figure-registry.test.ts"
      to: "site/scripts/validate/figure-registry.mjs"
      via: "import { extractSection, checkF16 }"
      pattern: "extractSection"
---

<objective>
Harden `figure-registry.mjs`'s F16 check so a stub `## INE ENUSC SAE` section can no
longer report green (DOCS-05), encode a real mutation test proving it, and add the
build-time regression guards that make Plans 31-01/31-02/31-03's fixes durable: a
national_rank-direction assertion (DOCS-01), a SOURCES.md content assertion (DOCS-03),
a facet-semantics-note assertion (DOCS-04), and an anchor-presence assertion (DOCS-06).
Close with the STATE.md backlog notes for the two out-of-scope map defects and the
DOCS-04 clustering N/A status, then run and report the full phase-close gate.

Purpose: every prose fix landed in Waves 1 needs a durable, falsifiable guard so it
cannot silently regress — per this run's repeated finding (Phases 27-30) that green
gates measured what they were written to measure, not what mattered.
Output: hardened figure-registry.mjs + its mutation test, extended coverage.mjs and
facets.mjs, STATE.md backlog entries, and a reported chained-gate result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-docs-methodology-refresh/31-RESEARCH.md
@.planning/STATE.md
@.planning/v2.1-AUTONOMOUS-DIRECTIVE.md

<interfaces>
<!-- Current figure-registry.mjs shape (site/scripts/validate/figure-registry.mjs),
     read in full during research: top-level script, no exports, executes on import
     (readFileSync + FIGURE_REGISTRY loop + process.exit at module-load time). This
     plan refactors it to export pure functions while preserving its CLI behavior. -->

Existing F16 entry (site/scripts/validate/figure-registry.mjs:192-200):
  id: 'F16', tokens: [['## INE ENUSC SAE', 'VHDV'], ['ENUSC', 'Victimizacion en Hogares', 'SAE']]
  — currently checked via `figure.tokens.some((group) => group.every((token) =>
  sourcesContent.includes(token)))`, a whole-file substring check with no section
  boundary and no minimum length (the exploitable hole).

Real, populated section text in data/SOURCES.md (lines 318-343, "## INE ENUSC SAE —
communal VHDV victimization (experimental)" through the line before "## SPD —
institutional authority") is ~26 lines / 900+ characters — use this as the TEST
FIXTURE's "real section" input (copy the text into the test file; do not read
data/SOURCES.md at test-run time).

site/scripts/validate/coverage.mjs: uses a `failures` counter, `process.exit(1)` at end
if `failures > 0`, assertions labeled A-E already exist (see lines 1-317); the next label
is F. `DIST_DIR` = `path.join(SITE_ROOT, 'dist')` is already defined at file top.

site/scripts/validate/facets.mjs: 22 assertions already exist, ending with `console.log(
PASS facets: ... 22 assertions passed)` at the bottom. `DIST_PAGES` (EN/ES dist path
pairs for /news/ and /es/noticias/) is already defined mid-file and reused across
assertions 7/11/12/etc. — reuse it, do not redefine.

Anchor inventory (DOCS-06, from research, verified stable): #trend-formula
(methodology.astro), #comparison-criteria (both locales, id NOT translated),
#national-mean (methodology.astro), #media-nacional (metodologia.astro, ES-translated
id). Built pages: dist/methodology/index.html, dist/es/metodologia/index.html.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden figure-registry.mjs's F16 check + encode the mutation test</name>
  <files>site/scripts/validate/figure-registry.mjs, site/scripts/validate/figure-registry.test.ts</files>
  <read_first>site/scripts/validate/figure-registry.mjs (full file, 253 lines)</read_first>
  <behavior>
    - Test 1 (RED before hardening, GREEN after): a fixture string containing only the
      heading line "## INE ENUSC SAE — communal VHDV household victimization rate
      (experimental, 2024)" followed immediately by the next section's heading "## SPD —
      institutional authority" (i.e. zero body content between them) must make
      `checkF16(fixture)` return false, even though the heading text itself already
      contains the literal substring "VHDV".
    - Test 2: a fixture string containing the real, fully-populated section text (copy
      the actual ~26-line / 900+-character body from data/SOURCES.md lines 318-343,
      hardcoded into the test file, never read from disk at test time) must make
      `checkF16(fixture)` return true.
    - Test 3: `extractSection(content, heading)` bounds correctly — given a fixture with
      three `## ` sections, extracting the middle one must include only that section's
      text (heading through the byte before the next `## ` heading), not spill into the
      third section.
  </behavior>
  <action>
  Refactor site/scripts/validate/figure-registry.mjs to export two pure functions before
  any file-reading side effects: `extractSection(content, heading)` — returns the
  substring from `heading`'s first occurrence in `content` up to (but not including) the
  next line starting with `## ` or EOF if none exists — and `checkF16(content)` — calls
  `extractSection(content, '## INE ENUSC SAE')`, returns false if the result is empty or
  its length is <= 200 characters, otherwise applies the existing two-token-group OR
  check (`['## INE ENUSC SAE', 'VHDV']` / `['ENUSC', 'Victimizacion en Hogares', 'SAE']`)
  against the bounded section text only (not the whole file). Wrap all module-level
  side-effecting code (the `readFileSync(SOURCES_PATH)` call, the FIGURE_REGISTRY loop,
  the `process.exit` calls) inside a guard so it runs only when the file is executed
  directly (compare `process.argv[1]` against the module's own resolved path via
  `fileURLToPath(import.meta.url)`), not when imported by a test. Inside the existing
  FIGURE_REGISTRY loop, special-case the `id === 'F16'` entry to call `checkF16(sourcesContent)`
  instead of the generic whole-file `tokens.some(...)` check; leave every other figure
  (F1-F15) using the existing unchanged whole-file logic — do not generalize the
  section-bounding to figures whose token groups legitimately span multiple `## `
  sections (e.g. F1's `['## CEAD', '## INE']`), per Research Pitfall 3.

  Create site/scripts/validate/figure-registry.test.ts as a real vitest spec importing
  `{ extractSection, checkF16 }` from `./figure-registry.mjs`, encoding exactly the three
  behaviors above as `expect(...)` assertions against hardcoded fixture strings (never
  against `data/SOURCES.md` itself — data/ is read-only for this milestone and the guard
  above ensures importing the module performs no file IO).
  </action>
  <acceptance_criteria>
  `cd site && npx vitest run scripts/validate/figure-registry.test.ts` exits 0 with all
  three tests passing. Manually confirmed once during this task (not part of the
  permanent suite): temporarily reverting `checkF16` to the pre-hardening whole-file
  `.includes()` check makes Test 1 fail (RED), proving the test is falsifiable; the
  reversion is then undone before commit. `cd site && node scripts/validate/figure-registry.mjs`
  still exits 0 against the real data/SOURCES.md (F16 and all other figures PASS,
  matching the pre-hardening baseline of "all 16 in-scope figures").
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add durable regression guards for DOCS-01, DOCS-03, DOCS-04, DOCS-06</name>
  <files>site/scripts/validate/figure-registry.mjs, site/scripts/validate/coverage.mjs, site/scripts/validate/facets.mjs</files>
  <read_first>site/scripts/validate/coverage.mjs (lines 240-318, existing assertion D/E pattern), site/scripts/validate/facets.mjs (lines 900-933, existing assertion 22 + final PASS log)</read_first>
  <action>
  In site/scripts/validate/figure-registry.mjs, after the existing FIGURE_REGISTRY loop
  (inside the same CLI-execution guard from Task 1, so this still only runs when the
  script is executed directly), add two content-correctness checks that push into the
  same `orphans`-style failure list (or a new counter that also drives `process.exit(1)`
  on the same final summary): (a) read
  site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro, and
  site/src/pages/safest-cities-in-chile.astro from disk and fail if any contains "lowest
  reported rate in Chile", "menor tasa reportada en Chile", or "lowest reported rate
  among all non-low-population" (the three exact inverted phrases fixed in Plan 31-01) —
  and separately fail if methodology.astro is missing "highest reported rate in Chile" or
  metodologia.astro is missing "mayor tasa reportada en Chile" (proving the corrected
  phrasing, not just the absence of the wrong one, actually landed); (b) read
  data/SOURCES.md and fail unless it contains at least one of "news.google.com" or
  "Google News", contains "granite" case-insensitively, and contains "R2" — proving
  Plan 31-02's rewrite is present. Print PASS/FAIL lines for each in the same style as
  the existing figure checks, and ensure a failure in either sets the script's final exit
  code to 1 alongside any figure orphans.

  In site/scripts/validate/coverage.mjs, add a new labeled assertion F (anchor-presence,
  DOCS-06) after the existing assertion E: read `dist/methodology/index.html` and assert
  it contains `id="trend-formula"`, `id="comparison-criteria"`, and `id="national-mean"`;
  read `dist/es/metodologia/index.html` and assert it contains `id="comparison-criteria"`
  and `id="media-nacional"`. Missing any of these five id occurrences increments
  `failures` with a clear per-anchor error message (which page, which anchor) — reuse the
  existing `failures`/`process.exit(1)` pattern already in this file, do not introduce a
  second exit mechanism.

  In site/scripts/validate/facets.mjs, add assertion 23 (facet-semantics note presence,
  DOCS-04) after the existing assertion 22, before the final summary `console.log`: for
  each entry in the existing `DIST_PAGES` array, read the built HTML and assert it
  contains `id="news-facet-semantics"` with non-empty text content inside that element
  (a simple regex capturing the element's inner text and checking `.trim().length > 0`
  is sufficient — do not require exact wording match, since that would re-couple this
  validator to Plan 31-03's exact prose). Update the final summary log's "22 assertions
  passed" to "23 assertions passed".
  </action>
  <acceptance_criteria>
  `cd site && node scripts/validate/figure-registry.mjs` exits 0 against the post-31-01/31-02 tree.
  `cd site && npm run build && node scripts/validate/coverage.mjs` exits 0 and prints a `PASS [F]` line.
  `cd site && node scripts/validate/facets.mjs` exits 0 and its final PASS line reads "23 assertions passed".
  Manually confirmed once (not part of the permanent suite, reverted before commit): temporarily
  re-inserting "lowest reported rate in Chile" into a scratch copy of methodology.astro's text
  and re-running figure-registry.mjs's new check against that scratch content shows it would fail —
  demonstrating the assertion is falsifiable, not vacuous.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: STATE.md backlog notes + full chained phase-close gate</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md (§ Critical Pitfalls to Avoid, § Phase 30 Outcome, around lines 124-260)</read_first>
  <action>
  Append two backlog entries to STATE.md's "### Critical Pitfalls to Avoid" list (or an
  adjacent "### Backlog" subsection if one already exists by the time this task runs):
  (1) the map legend's numeric bands never vary by selected family or index mode
  (`MapIsland.tsx:201,290-293`) — confirmed pre-existing, out of Phase 31's DOCS scope,
  no code fix and no reader-facing caveat per F-62, recorded for a future map-code phase;
  (2) `AVAILABLE_YEARS` in `mapFilterDefs.ts:45-50` offers 2026 with no payload on disk —
  same disposition, self-resolving once a 2026 map-payload exists, no code fix and no
  reader-facing caveat per F-62. Also add a one-line internal note (not reader-facing)
  recording that DOCS-04's clustering-documentation clause is N/A because Phase 26
  returned NO-GO (per F-61), and that the facet-semantics note shipped in its place on
  `/news/` and `/es/noticias/` (Plan 31-03).

  Then run the full chained gate from repo root in ONE command each (OneDrive
  requirement — never split build and validate across separate commands):
  `cd site && npm run build && npm run validate`, then `cd site && npm test`, then from
  repo root `python -m pytest pipeline/tests -q`. Record the exact pass/fail counts for
  each in this task's completion summary. If `freshness` is the sole failure, confirm
  both F-19 preconditions still hold (the failure output is exactly the age assertion;
  `git log -1 --format=%ci origin/master -- data/incidents/current.json` is within 3
  days) and state the close bar as "16/16, or 15/16 with freshness excluded per F-19" —
  do not remediate freshness by any of the banned actions (no `MAX_AGE_DAYS` change, no
  editing/skipping/deleting/unregistering the validator, no touching `data/`, no
  `git pull`/`rebase`/`push`).
  </action>
  <acceptance_criteria>
  STATE.md contains both new backlog entries and the DOCS-04 N/A note, verifiable via
  `grep -c "AVAILABLE_YEARS" .planning/STATE.md` >= 2 (pre-existing Phase 30 mention plus
  this new one) and `grep -c "clustering-documentation clause is N/A"` >= 1 (or
  equivalent wording actually written).
  `npx astro check` from `site/` still reports exactly 4 errors / 0 warnings (the
  pre-existing ComparatorPairsLinks.astro baseline) — zero new errors.
  `npm test` (vitest) passes with a count >= 51 + 3 (the new figure-registry.test.ts
  cases) = >= 54.
  `npm run validate` reports 16/16, or exactly 15/16 with `freshness` as the sole
  failure and both F-19 preconditions confirmed in the completion summary.
  `python -m pytest pipeline/tests -q` from repo root still reports 344 passed / 1
  skipped / 1 xfailed — unchanged, since this plan touches no Python.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time validator scripts → repo source files | Validators read `.astro`/`.md` files as trusted, developer-authored input from the same repo; no external or user-supplied input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | figure-registry.mjs / coverage.mjs / facets.mjs | mitigate | Each new assertion is designed with a stated, demonstrated falsifiable counterexample (per gate_quality_requirements) rather than a self-referential check that cannot fail. |
| T-31-06 | Denial of Service (build) | Mutation test suite | accept | Adds ~3 fast, offline vitest cases; no measurable build-time cost. |
| T-31-07 | Elevation of Privilege | N/A | accept | No new dependency, no new execution surface — pure string/regex assertions over already-trusted repo content. |
</threat_model>

<verification>
Full chained gate as specified in Task 3, with exact pass/fail counts reported. The
close bar is stated explicitly as "16/16, or 15/16 with freshness excluded per F-19" —
never encoded into a single gate command per F-34 (gates stay binary; judgement stays
with the orchestrator).
</verification>

<success_criteria>
figure-registry.mjs's F16 hole is closed and proven by an executable mutation test;
four new durable regression guards exist for DOCS-01/03/04/06; STATE.md carries the
required backlog notes; the full suite is reported at 16/16 or the pre-authorized
15/16-with-freshness-excluded state, with pytest unchanged at 344/1/1.
</success_criteria>

<output>
Create `.planning/phases/31-docs-methodology-refresh/31-04-SUMMARY.md` when done
</output>
