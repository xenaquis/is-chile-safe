---
phase: 31-docs-methodology-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
  - site/src/pages/safest-cities-in-chile.astro
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-06]

must_haves:
  truths:
    - "A reader of the EN methodology page sees 'Rank 1 = highest reported rate' (most reported crime, not safest), not the inverted 'lowest reported rate' claim."
    - "A reader of the ES methodology page sees the equivalent corrected claim in Spanish, landing in the same commit as the EN fix so parity never lapses even transiently."
    - "A reader of the EN safest-cities-in-chile page sees a corrected national-rank footnote that does not contradict the page's own ascending (lowest-first) sort — its ES twin is left untouched (no equivalent footnote exists there)."
    - "A reader of either EN or ES methodology page learns that the news layer's 8th family, sexuales, has no CEAD quantitative counterpart and is never merged into CEAD-derived statistics."
    - "None of the four actively-linked methodology anchors (#trend-formula, #comparison-criteria, #national-mean, #media-nacional) is renamed, renumbered, or removed by this edit."
  artifacts:
    - path: "site/src/pages/methodology.astro"
      provides: "Corrected national_rank direction + new sexuales section"
    - path: "site/src/pages/es/metodologia.astro"
      provides: "Corrected national_rank direction (ES) + new sexuales section (ES)"
    - path: "site/src/pages/safest-cities-in-chile.astro"
      provides: "Corrected national-rank footnote, EN only"
  key_links:
    - from: "site/src/pages/methodology.astro"
      to: "pipeline/cead/normalizer.py compute_ranks()"
      via: "prose describing national_rank direction"
      pattern: "highest reported rate"
---

<objective>
Fix the site's single highest-priority documentation defect — the EN and ES methodology
pages both claim "Rank 1 = lowest reported rate," which is the exact inverse of what
`pipeline/cead/normalizer.py`'s `compute_ranks()` computes (rank 1 = highest rate,
descending sort). Fix the same inversion's third live instance on the EN
safest-cities-in-chile editorial SEO page. Add the missing `sexuales` news-only family
explainer to both methodology pages (DOCS-01's other named gap).

Purpose: A reader relying on "rank" language anywhere on the site must get the direction
right — this is the site's primary credibility page and the claim is provably, not just
staleness-ly, wrong.
Output: methodology.astro, es/metodologia.astro, safest-cities-in-chile.astro with
corrected prose; zero remaining instances of the inverted claim anywhere in `site/src`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-docs-methodology-refresh/31-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Ground truth this plan must match. Do not re-derive — use directly. -->

From pipeline/cead/normalizer.py:111-141 (compute_ranks docstring, confirmed unchanged):
"Rank by rate_per_100k descending (highest rate = rank 1)" — `eligible_sorted = sorted(..., reverse=True)`,
rank 1 assigned to the highest-rate commune.

From site/src/pages/commune/[slug].astro:100 (the one place already correct):
"national_rank/regional_rank are DESCENDING (rank 1 = highest incidence)".

From pipeline/shared/schema.py:20-22:
`VALID_FAMILIES: set[str] = set(FAMILY_KEYS) | {"sexuales"}` — "NEWS-ONLY extension:
'sexuales' is not a CEAD quantitative family (no scraped rate)." `FAMILY_KEYS` (7 keys)
is the CEAD-quantitative contract and must never be extended.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix the national_rank inversion in EN + ES methodology pages, same commit</name>
  <files>site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro</files>
  <read_first>site/src/pages/methodology.astro (lines 300-334), site/src/pages/es/metodologia.astro (lines 309-343)</read_first>
  <action>
  In site/src/pages/methodology.astro, inside the "Comparison criteria" section (`id="comparison-criteria"`),
  locate the "Ranking basis:" list item whose text ends "...Rank 1 = lowest reported rate in Chile
  for that year." Replace only that sentence's final clause so the item reads: "...Rank 1 = highest
  reported rate in Chile for that year — the commune with the most reported crime, not the safest."
  Do not touch any other sentence in that section (population-threshold wording, national-average
  definition, etc. are correct today per Pitfall 4 — leave them byte-identical).

  In site/src/pages/es/metodologia.astro, inside the equivalent "Criterios de comparación" section
  (`id="comparison-criteria"`, note this id is NOT translated — leave it as-is, this is a pre-existing
  convention, not a defect to fix here), locate the "Base del ranking:" list item ending "...Ranking 1
  = menor tasa reportada en Chile para ese año." Replace only that final clause so the item reads:
  "...Ranking 1 = mayor tasa reportada en Chile para ese año — la comuna con más delincuencia
  reportada, no la más segura." Do not touch any other sentence in that section.

  Both edits MUST land in this single task/commit — do not split EN and ES across tasks or waves,
  per Research Pitfall 1 (EN/ES parity lapse).
  </action>
  <acceptance_criteria>
  `grep -rn "lowest reported rate in Chile" site/src/pages/methodology.astro` returns zero matches.
  `grep -rn "menor tasa reportada en Chile" site/src/pages/es/metodologia.astro` returns zero matches.
  `grep -c "highest reported rate in Chile" site/src/pages/methodology.astro` equals 1.
  `grep -c "mayor tasa reportada en Chile" site/src/pages/es/metodologia.astro` equals 1.
  The `id="comparison-criteria"` heading is still present, unchanged, in both files (anchor preserved).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix the third inverted-rank instance (EN safest-cities-in-chile, EN-only) and add the sexuales explainer to both methodology pages</name>
  <files>site/src/pages/safest-cities-in-chile.astro, site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro</files>
  <read_first>site/src/pages/safest-cities-in-chile.astro (lines 54, 92-176), site/src/pages/es/comunas-mas-seguras-chile.astro (grep confirms no equivalent footnote exists — do not add one there)</read_first>
  <action>
  In site/src/pages/safest-cities-in-chile.astro, locate the `<p class="table-note">` whose final
  sentence reads "National rank 1 = lowest reported rate among all non-low-population communes in
  Chile." Careful: the page itself is correct (it sorts communes ascending by rate at line 55 and
  legitimately lists the lowest-rate communes first) — only this footnote sentence about what
  "National Rank" (the table's last column, `c.nationalRank`) means is wrong. Replace that sentence
  with: "The National Rank column (a separate sitewide measure, not this table's own sort order)
  follows rank 1 = highest reported rate nationwide — most reported crime, not safest." Do NOT touch
  site/src/pages/es/comunas-mas-seguras-chile.astro — it has no equivalent claim and must not gain one
  (per F-60/orchestrator instruction: no forced parity where none existed).

  In site/src/pages/methodology.astro, insert a new `<section>` immediately after the existing
  "Underreporting (cifra negra)" section and before the "Household Victimization Indicator (ENUSC
  2024 SAE)" section (`id="enusc-vhdv"`). New section: `<h2 id="sexuales-family">News-only crime
  category: sexuales</h2>` followed by one paragraph stating that the news incident layer classifies
  incidents into 8 crime families — one more than the 7 CEAD statistical families used elsewhere on
  the site — the 8th being `sexuales` (sexual offenses); that this news-only family has no CEAD
  quantitative counterpart and therefore no per-100k rate, no choropleth representation, and no
  composite-index input; and that it exists only as a qualitative classification label on the
  recent-incidents news layer, never merged into the CEAD-derived statistics described elsewhere on
  the page.

  In site/src/pages/es/metodologia.astro, insert the Spanish twin at the equivalent position (after
  "Subregistro (cifra negra)", before `id="enusc-vhdv"`): `<h2 id="familia-sexuales">Categoría
  exclusiva de noticias: sexuales</h2>` with a paragraph translating the same three facts (8 vs 7
  families, `sexuales` = delitos sexuales, no tasa/coroplético/índice compuesto, nunca se fusiona con
  las estadísticas del CEAD).
  </action>
  <acceptance_criteria>
  `grep -c "lowest reported rate among all non-low-population" site/src/pages/safest-cities-in-chile.astro` equals 0.
  `grep -c "highest reported rate nationwide" site/src/pages/safest-cities-in-chile.astro` equals 1.
  `grep -c "menor tasa reportada" site/src/pages/es/comunas-mas-seguras-chile.astro` equals 0 (unchanged, no new claim added).
  `grep -c 'id="sexuales-family"' site/src/pages/methodology.astro` equals 1.
  `grep -c 'id="familia-sexuales"' site/src/pages/es/metodologia.astro` equals 1.
  `grep -c "sexuales" site/src/pages/methodology.astro` is >= 1 and `site/src/pages/es/metodologia.astro` is >= 1.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan edits only static, pre-rendered `.astro` prose files with no new user
input, no new dependency, and no data-mutation path — content is authored by the
executor, not derived from any untrusted external source at build time.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | methodology.astro / metodologia.astro prose | accept | Static content, no runtime input; `forbidden-language.mjs` (validator #9) already guards against absolute-verdict language regressions in this exact file class. |
| T-31-02 | Information Disclosure | N/A | accept | No new data surfaced; only correcting an existing public claim about an already-public computed field (`national_rank`). |
</threat_model>

<verification>
After both tasks: `grep -rn "lowest reported rate" site/src/pages/methodology.astro site/src/pages/es/metodologia.astro site/src/pages/safest-cities-in-chile.astro` returns zero matches. `grep -rn "menor tasa reportada en Chile" site/src` returns zero matches. Both methodology pages contain their new sexuales section anchors. `cd site && npm run build && npm run validate` (chained, one command) exits 0 on `forbidden-language.mjs` and `hreflang.mjs` at minimum — full 16/16 (or 15/16-with-freshness-excluded per F-19) is confirmed at Plan 31-04's phase-close gate, not required standalone here.
</verification>

<success_criteria>
Zero remaining instances of the inverted national_rank claim anywhere in `site/src`. Both
methodology pages document the `sexuales` news-only family. The four actively-linked
anchors are unchanged. No forbidden-language regression.
</success_criteria>

<output>
Create `.planning/phases/31-docs-methodology-refresh/31-01-SUMMARY.md` when done
</output>
