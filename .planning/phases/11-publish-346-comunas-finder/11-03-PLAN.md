---
phase: 11-publish-346-comunas-finder
plan: 03
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - site/src/config/i18n.ts
  - site/src/pages/communes/index.astro
  - site/src/pages/es/comunas/index.astro
  - site/src/components/PageHeader.astro
  - site/src/pages/index.astro
  - site/src/pages/es/index.astro
autonomous: true
requirements: [COMU-02]
must_haves:
  truths:
    - "A user can open /communes/ (EN) and /es/comunas/ (ES) and see ALL 346 comunas as real links in the server-rendered HTML (crawlable, visible with JS off)."
    - "A user can filter the list accent-insensitively (e.g. 'vina' matches 'Viña del Mar', 'nunoa' matches 'Ñuñoa') and toggle A–Z ↔ by-region (16 regions) views — and with JS disabled the full A–Z list remains visible (graceful degradation)."
    - "The directory is reachable from the primary nav (Comunas/Communes) and from the home page, in both locales; any comuna is reachable in ≤2 interactions."
    - "Low-population comunas appear in the directory tagged (low pop. / baja pob.)."
  artifacts:
    - path: "site/src/pages/communes/index.astro"
      provides: "EN directory: SSR full 346-link list (A–Z + region blocks) + inline filter/toggle script"
      min_lines: 80
    - path: "site/src/pages/es/comunas/index.astro"
      provides: "ES directory (mirror, hardcoded /es/comuna/{slug}/ links)"
      min_lines: 80
    - path: "site/src/components/PageHeader.astro"
      provides: "Hardcoded per-locale Comunas nav link (/communes/ | /es/comunas/)"
      contains: "communesHref"
  key_links:
    - from: "site/src/pages/communes/index.astro"
      to: "/commune/{slug}/ pages"
      via: "hardcoded EN comuna href per row (346 links in static HTML)"
      pattern: "/commune/"
    - from: "site/src/pages/es/comunas/index.astro"
      to: "/es/comuna/{slug}/ pages"
      via: "hardcoded ES comuna href per row"
      pattern: "/es/comuna/"
    - from: "site/src/components/PageHeader.astro"
      to: "directory pages"
      via: "communesHref = locale==='en' ? '/communes/' : '/es/comunas/'"
      pattern: "communesHref"
---

<objective>
Build the SEO-safe comuna directory (COMU-02) and link it from nav + home.

1. New `/communes/` (EN) + `/es/comunas/` (ES) pages: server-render the COMPLETE list of all 346 comunas as real `<a href>` links, grouped into BOTH an A–Z structure and a by-region (16) structure in the static HTML, then layer a vanilla inline `<script>` (NOT a React island — per CLAUDE.md island discipline) that filters accent-insensitively and toggles which grouping is visible. Crawlers and no-JS users see all 346 links. Port the validated spike-002 UX (`finder.html`).
2. Add an i18n nav string + directory page strings (EN+ES), a hardcoded per-locale "Comunas/Communes" nav link in PageHeader, and a minimal link from the existing home pages (no home redesign — that is Phase 12).

Purpose: Eliminates "material perdido" findability — any of the 346 comunas reachable in ≤2 interactions. Depends on Plan 02 so the linked comuna pages actually exist.
Output: Two directory pages, i18n strings, nav link, two home links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-publish-346-comunas-finder/11-CONTEXT.md
@.planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md
@.planning/spikes/002-comuna-finder/finder.html
@.planning/spikes/002-comuna-finder/README.md
@site/src/components/PageHeader.astro
@site/src/config/i18n.ts

<interfaces>
<!-- Build-time data + helpers for the SSR directory (from site/src/lib/data.ts + colorScale.ts) -->
From site/src/lib/data.ts:
```typescript
export function loadIndex(): CommuneMeta[];            // {cut,name,slug,region_id,population,low_population}
export function loadCommune(cut: string): CommuneData; // enriched; memoized in Plan 01
export function latestCompleteYearRate(commune: CommuneData): number;
```
From site/src/lib/colorScale.ts:
```typescript
export function levelForRate(rate: number, sortedRates: number[]): 1|2|3|4|5; // sortedRates = ascending non-low-pop rates
```
Layout (use BaseLayout, full-width — NOT EditorialLayout's 720px prose column):
```
BaseLayout props: lang, title, description, enPath, esPath, jsonLd?, mountType?   // injects canonical+hreflang+PageHeader+PageFooter
```
Region grouping: derive canonically as `Math.floor(parseInt(cut,10)/1000)` → 1..16 (spike convention; sidesteps the CEAD region_id quirk). Region display names: reuse the 16 region files via loadRegion(String(i)).name, or a static 1..16 name map — either is acceptable; prefer loadRegion for the official names.
</interfaces>

<!-- Spike UX contract to port (accent-fold + highlight + A–Z/region toggle) -->
<!-- finder.html line 46: const norm=s=>s.normalize('NFD').replace(/[̀-ͯ]/g,'').toLowerCase(); -->
<!-- finder.html line 47: highlight by slicing the TRUSTED name around the match index (NOT raw query) — see threat T-11-05 -->
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add directory + nav i18n strings (EN + ES)</name>
  <read_first>
    - site/src/config/i18n.ts (full read — I18nStrings interface + EN_STRINGS + ES_STRINGS)
  </read_first>
  <files>site/src/config/i18n.ts</files>
  <behavior>
    - I18nStrings gains a `nav_communes` field plus directory page strings (page title, intro/lead, search placeholder, A–Z toggle label, by-region toggle label, count template, low-pop tag, empty-results template).
    - EN_STRINGS and ES_STRINGS both provide every new key (no missing key in either locale → typecheck enforces parity).
    - Tone is sober per D-05 (no "peligroso/seguro" absolutes; "incidencia reportada").
  </behavior>
  <action>
    Extend the `I18nStrings` interface and BOTH string maps with: `nav_communes` (EN "Communes" / ES "Comunas"); `dir_title` (EN "All 346 communes of Chile" / ES "Las 346 comunas de Chile"); `dir_lead` (sober one-liner: every comuna with CEAD data has a page — search or browse by region); `dir_search_placeholder` (EN "Filter by name… (e.g. 'la ', 'puerto', 'vina')" / ES "Filtra por nombre… (ej. 'la ', 'puerto', 'viña')"); `dir_view_az` (EN "A–Z" / ES "A–Z"); `dir_view_region` (EN "By region" / ES "Por región"); `dir_count` (template with {n}, EN "{n} of 346 communes" / ES "{n} de 346 comunas"); `dir_lowpop_tag` (EN "low pop." / ES "baja pob."); `dir_empty` (template with {q}, EN 'No results for "{q}".' / ES 'Sin resultados para "{q}".'). Keep the existing NAV_STRINGS export consistent if you add communes there (optional). Do NOT alter any existing string value.
  </action>
  <verify>
    <automated>cd site && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "i18n\.ts|I18nStrings" || echo "i18n strings parity OK"</automated>
  </verify>
  <acceptance_criteria>
    - `nav_communes` and all `dir_*` keys exist in the I18nStrings interface and in BOTH EN_STRINGS and ES_STRINGS.
    - `tsc --noEmit` reports no missing-property errors on EN_STRINGS/ES_STRINGS (parity enforced).
  </acceptance_criteria>
  <done>i18n.ts carries nav_communes + directory strings in both locales with type-enforced parity.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create the EN + ES directory pages (SSR full 346-link list + vanilla enhancement script)</name>
  <read_first>
    - .planning/spikes/002-comuna-finder/finder.html (the validated UX + accent-fold + highlight + A–Z/region toggle — the design contract)
    - site/src/lib/data.ts (loadIndex, loadCommune, latestCompleteYearRate)
    - site/src/lib/colorScale.ts (levelForRate)
    - site/src/layouts/BaseLayout.astro (props: lang/title/description/enPath/esPath)
    - site/src/components/CommuneRankingTable.astro (how existing code hardcodes comunaPrefix per locale — mirror the link convention)
  </read_first>
  <files>site/src/pages/communes/index.astro, site/src/pages/es/comunas/index.astro</files>
  <behavior>
    - SSR computes 346 rows from loadIndex(): {name, slug, rate, low_population, region(1..16), level}. allRates = ascending non-low-pop rates for levelForRate.
    - The static HTML contains TWO server-rendered blocks: an A–Z block (default visible) and a by-region block (16 sections, hidden by default) — EACH containing all 346 links. With JS off, the A–Z block (all 346 links) is fully visible.
    - Each row is an `<a>` to the locale-correct comuna page: EN /commune/{slug}/, ES /es/comuna/{slug}/ (hardcoded prefix — getRelativeLocaleUrl does NOT translate ES slugs, Pitfall 3). Shows a level dot + rate/100k; low-pop rows carry the low-pop tag.
    - The inline <script> (no island): (1) accent-folded substring filter on the search input, (2) A–Z ↔ by-region toggle, (3) <mark> highlight by slicing the trusted name around the match index, (4) live count. JS-off: input/toggle inert, full A–Z list shown.
    - Both pages declare enPath="/communes/" + esPath="/es/comunas/" to BaseLayout (reciprocal hreflang — Pitfall 4); single H1; unique title/meta/canonical.
  </behavior>
  <action>
    Create `site/src/pages/communes/index.astro` (EN) and `site/src/pages/es/comunas/index.astro` (ES) using `BaseLayout` (full-width; NOT EditorialLayout). In frontmatter, build `rows` from `loadIndex()` enriching each with `latestCompleteYearRate(loadCommune(c.cut))`, `region = Math.floor(parseInt(c.cut,10)/1000)`, and `level = levelForRate(rate, allRates)` where `allRates` = the sorted ascending rates of non-low-pop comunas. Render the FULL 346-link list server-side in BOTH an A–Z grouping (group by first letter of the accent-folded name; include an A–Z jump index) and a by-region grouping (16 sections ordered 1..16, using loadRegion(String(i)).name for headings, with per-section counts) — both blocks present in HTML, region block `hidden` by default. Port the spike's inline `<script>` for filter/toggle/highlight/count, using `s.normalize('NFD').replace(/[̀-ͯ]/g,'').toLowerCase()` for accent-folding and slicing the trusted name around the match index for `<mark>` (do NOT inject the raw query — threat T-11-05). Hardcode comuna hrefs per locale (EN `/commune/${slug}/`, ES `/es/comuna/${slug}/`). Render the low-pop tag from `dir_lowpop_tag` when `low_population`. Use the `dir_*`/`nav_communes` i18n strings from Task 1. Single `<h1>` = `dir_title`. Reference: this is the SEO-safe version of finder.html — the spike renders the list client-only; here the 346 links MUST be in server HTML. Keep styles scoped in the page `<style>` (you may adapt the spike's `.citem/.clist/.section/mark` CSS to the site's CSS variables).
  </action>
  <verify>
    <automated>cd site && npx astro check 2>&1 | grep -iE "communes/index|comunas/index" || echo "directory pages typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and use BaseLayout with enPath="/communes/" + esPath="/es/comunas/".
    - Each page's frontmatter maps over the FULL loadIndex() (346 rows) — no rollout filter, no slice.
    - The A–Z block and the region block are BOTH server-rendered (present in the .astro markup, not built only in the script).
    - Comuna hrefs are hardcoded per locale (/commune/{slug}/ in EN, /es/comuna/{slug}/ in ES); the accent-fold + name-slice highlight from the spike is used (no raw-query innerHTML).
    - `astro check` reports no errors for the two new pages.
    - (Full-build assertion that dist contains ≥346 hrefs + graceful no-JS degradation is verified by coverage.mjs in Plan 04.)
  </acceptance_criteria>
  <done>EN + ES directory pages server-render all 346 comuna links in A–Z and by-region groupings with a vanilla filter/toggle script; SEO-safe and no-JS-degradable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Link the directory from nav (PageHeader) and from both home pages</name>
  <read_first>
    - site/src/components/PageHeader.astro (full read — the mapHref/methodHref hardcoding pattern at L24-25)
    - site/src/pages/index.astro (EN home — find the quick-links section)
    - site/src/pages/es/index.astro (ES home — quick-links section, e.g. the "346 comunas, usa el mapa o busca por…" line ~L149)
  </read_first>
  <files>site/src/components/PageHeader.astro, site/src/pages/index.astro, site/src/pages/es/index.astro</files>
  <behavior>
    - The primary nav shows a "Communes"/"Comunas" link pointing to /communes/ (EN) resp. /es/comunas/ (ES), in both desktop and the zero-JS mobile dropdown.
    - Both home pages contain a visible link to the directory (minimal — a sentence/CTA in the existing quick-links area; NO home redesign).
  </behavior>
  <action>
    In `PageHeader.astro`: add `const communesHref = locale === 'en' ? '/communes/' : '/es/comunas/';` next to the existing `mapHref`/`methodHref` lines (hardcode per locale — Pitfall 3; do NOT use getRelativeLocaleUrl which would emit a broken /es/communes/). Add a `<a href={communesHref} class="nav-link">{t.nav_communes}</a>` into the `<nav class="csm-nav">` block (it already renders in both the desktop row and the checked mobile dropdown via the same markup) — place it after the Map link. In `site/src/pages/index.astro` (EN) and `site/src/pages/es/index.astro` (ES): add ONE minimal directory link in the existing quick-links/explore area (e.g. EN "Browse all 346 communes in the directory →" linking `/communes/`; ES "Explora las 346 comunas en el directorio →" linking `/es/comunas/`). Keep it unobtrusive — no hero, no search box, no layout restructuring (that is Phase 12, Open Question 1). Do NOT change the home H1 or any existing prose.
  </action>
  <verify>
    <automated>cd site && grep -q "communesHref" src/components/PageHeader.astro && grep -q "nav_communes" src/components/PageHeader.astro && grep -q "/communes/" src/pages/index.astro && grep -q "/es/comunas/" src/pages/es/index.astro && echo "nav + home links present" || (echo "FAIL: missing link" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - PageHeader has `communesHref` hardcoded per locale and renders `{t.nav_communes}` linking to it.
    - EN home links `/communes/`; ES home links `/es/comunas/`.
    - No getRelativeLocaleUrl is used for the directory link; no home H1/prose restructured.
  </acceptance_criteria>
  <done>The directory is linked from the primary nav (both locales, desktop + mobile dropdown) and from both home pages, with hardcoded per-locale paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → DOM (directory search) | The search input value is client-side only, never sent to a server, but it is interpolated into the DOM by the highlight logic. |
| repo data → static HTML | Comuna names/slugs are trusted CEAD index data rendered server-side as text/links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | directory search-highlight `<mark>` injection | mitigate | Highlight by slicing the TRUSTED comuna name around the matched index (spike finder.html line 47 pattern), NEVER by injecting the raw user query into innerHTML. Comuna names come from trusted build data; the query is only used to locate the match offset. (ASVS V5.) |
| T-11-06 | Information Disclosure | directory exposes all 346 comuna links | accept | Intended — public CEAD data; the directory is the findability hub. No PII. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (vanilla script, zero deps). slopcheck N/A. |
</threat_model>

<verification>
- `astro check` clean for the two new directory pages and i18n.ts.
- `grep` confirms nav + home links and hardcoded per-locale paths.
- Full dist assertions (≥346 hrefs in each directory HTML, sitemap includes the directory URLs, reciprocal hreflang, no-JS full-list visibility) are verified by coverage.mjs + the manual reachability check in Plan 04.
</verification>

<success_criteria>
- /communes/ and /es/comunas/ server-render all 346 comuna links in A–Z + by-region groupings with a vanilla accent-insensitive filter/toggle that degrades gracefully without JS.
- Comuna + directory links hardcoded per locale; low-pop comunas tagged; single H1 + reciprocal hreflang.
- Directory linked from primary nav (both locales) and both home pages without any home redesign.
</success_criteria>

<output>
Create `.planning/phases/11-publish-346-comunas-finder/11-03-SUMMARY.md` when done.
</output>
