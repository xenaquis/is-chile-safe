---
phase: 34-news-classification-restore
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - pipeline/news/store.py
  - pipeline/news/schema.py
  - pipeline/news_evidence.py
  - pipeline/news_health_gate.py
  - pipeline/tests/test_store.py
  - pipeline/tests/test_news_evidence.py
  - pipeline/tests/test_news_health_gate.py
  - pipeline/tests/test_workflow_guards.py
  - site/scripts/validate/freshness.mjs
  - site/scripts/validate/freshness.test.ts
  - site/scripts/validate/all.mjs
  - .github/workflows/news-pipeline.yml
  - .github/workflows/heartbeat.yml
  - .github/scripts/check-heartbeat.sh
  - DEPLOYMENT.md
autonomous: true
requirements: [NREC-07, NREC-08]
executor: sonnet

must_haves:
  truths:
    - "When classification health fails (G-04), the news job still runs 'Commit data if changed' and the deploy trigger, then fails at the post-commit 'Health gate' step. The existing if: failure() label + issue steps then open or comment the pipeline-failure-news issue."
    - "current.json carries last_new_incident_at (UTC ISO). It is set only when a run adds ≥1 new id to the current window, and carried forward otherwise (G-05)."
    - "The heartbeat and freshness.mjs fail when the newest-incident evidence (last_new_incident_at, else max(date)+1 day) is older than 48 h, independently of `generated`."
    - "The G-05 fixture (generated=now, evidence now−3d) fails both instruments. A fresh fixture passes both."
    - "With 100% injected API errors, scrape_news.main() returns 0 (commit path reached) and the health gate on its summary exits 1."
  artifacts:
    - path: pipeline/news_health_gate.py
      provides: "G-04 evaluator + CLI (stdlib only)"
      contains: "def evaluate"
    - path: pipeline/news_evidence.py
      provides: "G-05 evidence extractor for the heartbeat (stdlib only, runs on system python3)"
      contains: "last_new_incident_at"
    - path: site/scripts/validate/freshness.test.ts
      provides: "vitest fixtures proving freshness.mjs fails on the G-05 fixture"
  key_links:
    - from: .github/workflows/news-pipeline.yml "Run news scraper"
      to: .github/workflows/news-pipeline.yml "Health gate"
      via: "same NEWS_RUN_SUMMARY_PATH (${{ runner.temp }}/news-run-summary.json) in both steps' env"
      pattern: "NEWS_RUN_SUMMARY_PATH"
    - from: .github/workflows/heartbeat.yml
      to: pipeline/news_evidence.py
      via: "LAST_NEWS=$(python3 pipeline/news_evidence.py data/incidents/current.json); check-heartbeat.sh news 2"
      pattern: "news_evidence.py"
    - from: pipeline/news/store.py
      to: data/incidents/current.json
      via: "payload['last_new_incident_at']"
      pattern: "last_new_incident_at"
---

<objective>
Make total or partial classification failure loud (NREC-07 / G-04), and make the freshness instruments measure real news, not file rewrites (NREC-08 / G-05). Closes V-03.

Purpose: 20/20 green heartbeats ran through an 18-day outage. `store.py:194-195` rewrites `generated` every run, `heartbeat.yml:73` and `freshness.mjs:56-91` read only that field, and the job exits 0 by design (WR-03).

Output: a post-commit health gate, `last_new_incident_at`, G-05 evidence in heartbeat.yml and freshness.mjs, a harness test, and a runbook update.

**Deploy-build question (resolved, with evidence):** freshness.mjs does NOT run in any deploy path.
- Cloudflare Pages runs `npm run build` (DEPLOYMENT.md:62). That is prebuild (generate-pairs + sync-data) plus `astro build` (site/package.json:10-11), and validators are a separate script (`validate`, :13).
- deploy-on-code.yml:66 and deploy-manual.yml:53 only POST the deploy hook.
- ci.yml runs `node scripts/validate/all.mjs` (:37-41) only on pull_request / workflow_dispatch (:3-5).

So pushing this plan cannot fail a deploy, even though freshness.mjs will be honestly RED on the current data. Its evidence is max(date)=2026-09-04 → 2026-09-05T00:00Z, far over 48 h, until live classification adds a new id. 34-04 treats that red as an expected, named signal and 34-06 requires it green. It is never excluded (directive close-gate rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md  (G-04, G-05 verbatim)
@.planning/phases/34-news-classification-restore/34-02-SUMMARY.md  (run-summary schema 1 as shipped)
@pipeline/news/store.py
@.github/workflows/news-pipeline.yml
@.github/workflows/heartbeat.yml
@site/scripts/validate/freshness.mjs
@pipeline/tests/test_workflow_guards.py

<interfaces>
Existing code (HEAD e565ce8), read this session:
- store.py:118-209 `merge_and_write(new_incidents, current_path, archive_dir, window_days=30, today=None) -> None`:
  - loads existing (:142), merges by id with existing winning (:148)
  - partitions `date >= today-30d` (:150-163), archives aged-out items (:166-191)
  - payload {generated: now Z, window_days, incidents} (:194-198), validate (:199-206), atomic write (:209)
  - Callers ignore the return value (scrape_news.py:379; tests test_store.py:68,:89,:253,:271).
- schema.py:126-131 `IncidentsFile(generated: str, window_days: int, incidents: list[IncidentRecord])`. Pydantic's default extra="ignore".
- news-pipeline.yml step order:
  1. Guard required secrets (:52-55)
  2. setup-python (:57)
  3. pip install (:61-62)
  4. Run news scraper (:64-68, env OPENROUTER_API_KEY + DEEPSEEK_API_KEY)
  5. Commit data if changed (id commit, :70-82)
  6. Guard deploy hook (:90-91)
  7. Trigger Cloudflare Pages deploy (if changed, :96-98)
  8. Ensure alert label exists (if failure(), continue-on-error, :107-114)
  9. Alert via GitHub Issue on failure (if failure(), dedup via `gh issue list --label pipeline-failure-news --state open`, :116-131)
- heartbeat.yml:70-74: `LAST_NEWS=$(python3 -c "...['generated'])")`; `bash .github/scripts/check-heartbeat.sh news 3 "$LAST_NEWS"`. The heartbeat job has NO setup-python. Its python is system python3 inside a `run: |` block.
- check-heartbeat.sh:69-117 news mode: `news <max_age_days> <ts> [as_of]`. It fails when age_s > max_age_days*86400, so exactly N days is inclusive PASS, per F-96. It refuses evidence >24 h in the future (F-100) and an empty ts ("no timestamp evidence"). `news 2` is exactly the G-05 48 h rule.
- freshness.mjs:25-27 CURRENT_JSON_PATH is fixed, MAX_AGE_DAYS=3. :77 uses Date.now(). There is no injection point today.

Workflow invariants the edits must keep (cite, re-run):
- test_workflow_guards.py:652-661 (H-02): every line that starts with `run: python ` needs actions/setup-python + `pip install -r` earlier in the same job. news-pipeline.yml has both (:57, :62). heartbeat.yml does not, so its python call must stay inside a `run: |` block and stay stdlib-only.
- test_workflow_guards.py:513-561 (F-123): news-pipeline.yml must carry NO live `persist-credentials:` key, and heartbeat.yml exactly 1 `persist-credentials: false`. Do not touch the checkouts.
- ci.yml:88-98: lint-workflows.sh (actionlint + shellcheck, canary-armed), check-sha-pins.sh (every `uses:` SHA-pinned; this plan adds no `uses:`), zizmor==1.10.0 --persona=regular (no `${{ }}` expressions inside `run:` bodies; pass values through step `env:`), check-secret-hygiene.sh.
- `runner.temp` is valid in step-level `env:` but NOT in job-level `env:`. Set NEWS_RUN_SUMMARY_PATH per step.
- The run-summary producer writes to NEWS_RUN_SUMMARY_PATH only when the env var is set (34-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: last_new_incident_at in store.py (G-05) + G-05 evidence extractor</name>
  <files>pipeline/news/store.py, pipeline/news/schema.py, pipeline/news_evidence.py, pipeline/tests/test_store.py, pipeline/tests/test_news_evidence.py</files>
  <read_first>pipeline/news/store.py (whole), pipeline/tests/test_store.py</read_first>
  <behavior>
    - merge_and_write([inc_new_in_window], ..., now=T) on a file without the field → payload last_new_incident_at == T (ISO Z); returns 1.
    - A no-op merge (new_incidents=[] or only existing ids) carries the previous last_new_incident_at forward verbatim and returns 0. If the field was absent before, it stays absent (no key).
    - A new id whose date is older than the cutoff (goes straight to archive) does NOT set the field (not "in the current window") and returns 0.
    - validate_incidents_file accepts payloads with and without the field (schema field `last_new_incident_at: str | None = None`).
    - All existing test_store.py tests pass unchanged.
    - evidence_iso({"last_new_incident_at": "2026-06-01T10:00:00Z", ...}) == "2026-06-01T10:00:00Z". When the field is absent or unparseable and incidents max(date)=="2026-05-29" → "2026-05-30T00:00:00Z". With no field and no incidents → None.
    - CLI: `python3 pipeline/news_evidence.py <file>` prints the evidence and exits 0. With a missing/unparseable file, or no evidence, it prints an empty line and exits 0, so check-heartbeat.sh fails with its own "no timestamp evidence" error.
  </behavior>
  <action>
**`merge_and_write`:**
- Add the keyword `now: datetime.datetime | None = None` (default `datetime.now(timezone.utc)`, used for BOTH `generated` and the new field) and return `int`: the count of ids in `current_incidents` that were not in `existing`.
- Read the previous `last_new_incident_at` from the raw existing JSON dict. Add a small `_load_payload` helper next to `_load_incidents_list`; do not change `_load_incidents_list`'s return type.
- If the count is > 0, set `payload["last_new_incident_at"] = now` ISO with "Z". Else, if the previous value is a str, carry it; else omit the key.
- `generated` behaviour is UNCHANGED here. FRESH-04 (Phase 35) will make no-op runs byte-identical. The only note needed for that later phase: a carried-forward field keeps a no-op payload's only diff at `generated`, so FRESH-04 must still solve `generated` itself. Record that in the SUMMARY.

**schema.py:** add the optional field to IncidentsFile.

**`pipeline/news_evidence.py`:** stdlib only (json, sys, datetime), with no import from the `pipeline` package, because the heartbeat runs it with system python3 and no deps. It exposes `evidence_iso(payload) -> str | None` and a `__main__` CLI per `<behavior>`.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_store.py pipeline/tests/test_news_evidence.py -q -p no:cacheprovider → exit 0</automated>
  </verify>
  <done>The field is set, carried and omitted exactly per G-05. The evidence CLI works on system python3 with no third-party imports (`grep -E "^(from|import) " pipeline/news_evidence.py` shows only stdlib modules).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: freshness.mjs + heartbeat.yml on G-05 evidence (48 h), with fixtures</name>
  <files>site/scripts/validate/freshness.mjs, site/scripts/validate/freshness.test.ts, site/scripts/validate/all.mjs, .github/workflows/heartbeat.yml, .github/scripts/check-heartbeat.sh, pipeline/tests/test_workflow_guards.py</files>
  <read_first>site/scripts/validate/freshness.mjs (whole), site/scripts/validate/figure-registry.test.ts (vitest conventions in this dir), .github/workflows/heartbeat.yml:70-74, pipeline/tests/test_workflow_guards.py:390-455</read_first>
  <behavior>
    Fixtures, all with an injected as-of (never the wall clock):
    - A (G-05 fixture): generated=as_of=2026-06-01T12:00:00Z, last_new_incident_at=2026-05-29T12:00:00Z (−3 d) → freshness.mjs exit 1; `news_evidence.py` + `check-heartbeat.sh news 2 <ev> <as_of>` exit 1
    - B (G-05 fallback fixture): generated=as_of=2026-06-01T12:00:00Z, no field, max(date)=2026-05-29 → evidence 2026-05-30T00:00:00Z (60 h) → both exit 1
    - C: last_new_incident_at = as_of−47 h → both exit 0
    - D: exactly 48 h → both exit 0 (PASS-inclusive, F-96 semantics); E: 48 h + 1 s → both exit 1
    - F: evidence 25 h in the future → both exit 1 (F-100 parity)
    - G: no field, no incidents → both exit 1
    - freshness.mjs output names the evidence source ("last_new_incident_at" or "max(date)+1d") and the age in hours
  </behavior>
  <action>
**freshness.mjs:**
- Replace the `generated` check with the G-05 evidence rule, MAX_AGE_HOURS = 48 and a >24 h-future refusal.
- Add two test-only env injections: FRESHNESS_CURRENT_JSON (path; default unchanged) and FRESHNESS_NOW (ISO; if set but unparseable → FAIL).
- Keep exit codes 0/1 and the "PASS freshness:" / "FAIL freshness:" prefixes that all.mjs parses.
- Update the header docblock and the all.mjs line-15 description to "newest-incident evidence ≤ 48h (G-05)".

**freshness.test.ts** (vitest; the directory is already in the vitest run, see figure-registry.test.ts): write fixtures A–G to a tmp dir and spawn `process.execPath` on freshness.mjs with the env injections.

**heartbeat.yml news step (:70-74):**
- Rename it to "Check news pipeline heartbeat (newest-incident evidence per G-05, 48h threshold, matches freshness.mjs)".
- Body:
  - `LAST_NEWS=$(python3 pipeline/news_evidence.py data/incidents/current.json)`
  - `bash .github/scripts/check-heartbeat.sh news 2 "$LAST_NEWS"`
- Keep `if: '!cancelled()'`. The body must remain a `run: |` block (H-02 guard) and use no `${{ }}` (zizmor).

**check-heartbeat.sh:** comment-only update of `:12-17`, which now describes the evidence field. The logic is unchanged.

**test_workflow_guards.py:** add class TestNewsHeartbeatG05. It runs fixtures A–G through `python pipeline/news_evidence.py` (sys.executable) and then `run_script(CHECK_HEARTBEAT, ["news","2",ev,as_of])` using the existing `_find_bash` F-86 resolution. It also asserts that heartbeat.yml contains "news_evidence.py" and "check-heartbeat.sh news 2" and no longer contains "['generated']".
  </action>
  <verify>
    <automated>cd site && npx vitest run scripts/validate/freshness.test.ts → exit 0; cd .. && python -m pytest pipeline/tests/test_workflow_guards.py -q -p no:cacheprovider → exit 0; FRESHNESS_NOW=2026-09-22T21:00:00Z node site/scripts/validate/freshness.mjs; echo EXIT=$? → "FAIL freshness" naming max(date)+1d = 2026-09-05T00:00:00Z, EXIT=1 (expected RED on current data)</automated>
  </verify>
  <done>Both instruments implement G-05 identically and are proven by the same fixture set in both harnesses. On today's data freshness.mjs is RED, which is the true state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: post-commit Health gate (G-04) + 100%-injected-error harness + runbook</name>
  <files>pipeline/news_health_gate.py, pipeline/tests/test_news_health_gate.py, .github/workflows/news-pipeline.yml, DEPLOYMENT.md</files>
  <read_first>.github/workflows/news-pipeline.yml (whole), pipeline/tests/test_workflow_order.py:36-66 (YAML step helpers), DEPLOYMENT.md lines 110-130 and 440-455, 34-02-SUMMARY.md (summary schema)</read_first>
  <behavior>
    G-04 boundaries (evaluate() returns the list of failure reasons; the CLI exits 1 iff the list is non-empty):
    - (a) attempted=1, responded=0 → fail. attempted=0, responded=0 → pass.
    - (b) attempted=10, api_errors=5 → fail (5/10 ≥ 0.5). attempted=10, api_errors=4, responded=6 → pass. attempted=9, api_errors=5, responded=4 → pass: (b) is inapplicable below 10 and (a) is not triggered.
    - (c) backup_exhausted=true → fail, even with responded>0.
    - failovers=1 alone → pass, and stdout has "::warning::" naming failover_reason.
    - Fail-closed on missing evidence: a missing file, invalid JSON or schema != 1 → exit 1 with "::error::". This is a planner addition, see Knob objections.
    Harness (real scrape_news.main(), fake feeds with 12 unseen crime entries, preflight patched to "ok", `pipeline.news.classifier._request_completion` patched to raise openai.NotFoundError(404) for EVERY call, both keys set):
    - main() returns 0, so the commit step would run.
    - pending.json has 12 items. seen.json has none of the 12 URLs. rejected/*.json has no classifier_none row. The summary shows attempted=10, api_errors=10, responded=0, failovers=1, backup_exhausted=true, not_attempted=2.
    - `python pipeline/news_health_gate.py <summary>` exits 1 and stdout contains "::error::Health gate".
    Harness variant (primary 404, backup answers OK) → main() 0, summary failovers=1, gate exit 0 with "::warning::".
    Workflow structure (YAML parse, same approach as test_workflow_order.py):
    - In scrape job step order: "Run news scraper" < "Commit data if changed" < "Trigger Cloudflare Pages deploy" < "Health gate (NREC-07 / G-04)" < "Ensure alert label exists" < "Alert via GitHub Issue on failure".
    - The Health gate step has no `if:` and no `continue-on-error`, and its run is `python pipeline/news_health_gate.py "$NEWS_RUN_SUMMARY_PATH"`.
    - The scrape step and the gate step declare the identical NEWS_RUN_SUMMARY_PATH value.
    - Both alert steps keep `if: failure()`.
    - No `${{` appears inside the `run:` of the scrape or gate steps.
  </behavior>
  <action>
**`pipeline/news_health_gate.py`:** stdlib only. It implements `evaluate(summary) -> list[str]` with the exact G-04 predicates:
- (a) attempted > 0 and responded == 0
- (b) attempted ≥ 10 and api_errors / attempted ≥ 0.5
- (c) backup_exhausted

It also has `main(argv)`: fail-closed on missing or invalid input; print one `::error::Health gate (G-04): <reason>` per reason; print `::warning::` for failovers > 0; print a final `Health gate OK: attempted=… responded=… api_errors=… failovers=…` line on pass.

**news-pipeline.yml:**
- Scrape step (:64-68): add `id: scrape` and `NEWS_RUN_SUMMARY_PATH: ${{ runner.temp }}/news-run-summary.json` to its `env:`.
- Insert the new step immediately after "Trigger Cloudflare Pages deploy" (:96-98) and before "Ensure alert label exists" (:107):
  - name "Health gate (NREC-07 / G-04)"
  - env NEWS_RUN_SUMMARY_PATH (same value)
  - run `python pipeline/news_health_gate.py "$NEWS_RUN_SUMMARY_PATH"`
  - comment block: why post-commit (the V-03 design constraint: a non-zero scrape exit would skip the commit), and that the existing failure() steps provide the issue alert with dedup.
- Update the M-01 comment (:44-51): DEEPSEEK_API_KEY is now the NREC-04 backup and is still deliberately unguarded. A missing backup key only matters when the primary fails, and then G-04(c) fails the job loudly.
- Do not touch the checkout, uses: pins, permissions or the alert steps.

**DEPLOYMENT.md:** update the rows at :122 and :449 (they name Granite 4.1 as the default classifier) and add a "News classification health" runbook subsection covering:
- NEWS_MODEL / NEWS_BACKUP_MODEL / NEWS_CLUSTERING_MODEL and their defaults
- the DeepSeek backup
- data/incidents/pending.json semantics (G-03)
- the Health gate predicates (G-04)
- the heartbeat/freshness evidence (G-05, 48 h)
- how to read `Run summary:` in a run log

Do NOT edit data/SOURCES.md:417 (it also names Granite 4.1). It lives under data/, where directive edits are forbidden outside the backfill, and it is V-25 docs drift for Phase 38. Record that in the SUMMARY.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_news_health_gate.py pipeline/tests/test_workflow_guards.py pipeline/tests/test_workflow_order.py -q -p no:cacheprovider → exit 0; bash .github/scripts/lint-workflows.sh; echo LINT=$? → LINT=0; bash .github/scripts/check-sha-pins.sh; echo PINS=$? → PINS=0; bash .github/scripts/check-secret-hygiene.sh; echo HYG=$? → HYG=0; python -m pytest pipeline/tests/ -q -p no:cacheprovider; echo EXIT=$? → EXIT=0, "2 xfailed", 0 failed</automated>
  </verify>
  <done>The gate step is in place post-commit. The harness proves the 100%-error path is committed and failed. The workflow lint, pins and secret-hygiene instruments pass locally. zizmor is verified by the ci.yml dispatch in 34-04; this plan introduces no `${{ }}` in run bodies and no new `uses:`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| workflow → GitHub Issues API | health gate failure opens issues (issues: write already granted at news-pipeline.yml:18) |
| run summary file (runner.temp) → gate | produced by the same job; not user input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-09 | Denial of Service (maintainer attention) | issue alert | mitigate | Reuse the existing dedup (`gh issue list --label pipeline-failure-news --state open` → comment, not a new issue); no parallel issue stream |
| T-34-10 | Tampering / injection | new workflow steps | mitigate | No `${{ }}` in run bodies (zizmor template-injection); values via step env; no new `uses:` (SHA-pin invariant) |
| T-34-11 | Repudiation (silent green) | gate input missing | mitigate | Fail-closed on a missing/invalid summary; the heartbeat evidence extractor prints empty → check-heartbeat errors |
| T-34-12 | Elevation of Privilege | permissions | accept | No permission changes (contents: write, issues: write unchanged) |
</threat_model>

<verification>
- Task verify blocks pass.
- `cd site && npx vitest run` → all files pass (baseline 86/86 in 9 files, plus the freshness.test.ts cases in 10 files).
- `node site/scripts/validate/freshness.mjs` on the current data exits 1 with the evidence named. This is expected and resolved in 34-04/34-06.
</verification>

<success_criteria>
- NREC-07: post-commit gate plus automatic issue, proven by the 100%-injected harness and the structure test.
- NREC-08: heartbeat and freshness.mjs run on G-05 ≤ 48 h; the G-05 fixture fails both.
</success_criteria>

<output>
Create `.planning/phases/34-news-classification-restore/34-03-SUMMARY.md` when done.
</output>
