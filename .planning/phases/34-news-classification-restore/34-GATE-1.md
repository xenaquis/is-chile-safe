---
phase: 34
artifact: adversarial gate
round: 1
plans_evaluated: 34-01..34-06-PLAN.md @ 8efb557
verdict: NO-GO
blockers: 9
non_blockers: 14
agents: 5 (haiku verifier, 3 opus lenses high, fresh opus arbiter xhigh) + deterministic citation script
subagent_tokens: 1004664
cycle: 1 of 2
---

# Phase 34 — Adversarial Gate, Round 1

## Verdict: NO-GO

NO-GO para 34-01..34-06 @ 8efb557, ronda 1. Firmo 9 bloqueantes CONFIRMADOS con evidencia propia.

- BF-01. Los gemelos de G-08 no se barrieron: 34-01:343 exige WINNER; el verify de 34-02:281 y el behavior :204 fallan por construcción en DEEPSEEK_DIRECT.
- BF-02. La aserción absoluta del harness R-13 (34-03:229) rompe el gate post-apply de 34-05 (:254) aunque el sistema esté sano.
- BF-03. Con la definición de 34-05:133, el estrato gnews_title_only de G-11 tiene 0 miembros (medido: 0/1.089). Eso deja inerte la mitigación R-06 e invierte su rama de falla.
- BF-04. El backfill hereda el presupuesto de 1200 s por corrida de R-11, sin código de salida propio. Cubre ~325-860 ítems por invocación y puede quedar parcial en silencio.
- BF-05. El runner A/B trata finish_length (y el vacío en el tramo G-08) distinto que prod según G-09. Puede elegir un modelo que en prod expire ítems y deje G-09(f) en rojo.
- BF-06. Tres planes citan un 'fast-path inmediato' que el G-08 registrado no contiene. Implica un push a prod sin autorización registrada.
- BF-07. Faltan los curl a prod después de los deploys de 34-04 y 34-05, que la directiva exige (:76). La disclosure de metodología nunca se verifica servida.
- BF-08. 34-06 marca NREC-01..10 como Complete sin condición, también en las ramas G-08 y backfill parcial.
- BF-09. El done de 34-05 omite 'withheld' del residual.

La mayoría son ediciones mecánicas. BF-03, BF-05 y BF-06 piden además una redefinición o un G-NN nuevo, así que no alcanza para GO-CONDICIONADO.

Hay 14 no-bloqueantes confirmados:
- anclaje y filo de navaja en G-10;
- reasoning_unfixable sin destino y '|| break';
- la regla por estrato de G-11 y el caso de re-auditoría fallida;
- conteos de tests (son 10, no 9);
- pop_redispatched sin drenar en el backfill;
- aritmética del spend guard (0,49+0,0025 ≤ 0,50);
- .pyc que ensucian el grep;
- el issue de news-heartbeat de la transición, que nadie cierra;
- fidelidad de titulares en la auditoría y regex de parentesco estrecha;
- redacción de la disclosure;
- regex de 34-06 sin ampliar;
- higiene de git (autoStash no configurado, pull.rebase=false);
- 34-02 sin check de data/.

Descarté 7 afirmaciones PLAUSIBLES o refutadas, con su razón.

Cierres verificados por mí: población 2.073, baseline pytest 395/1/1 con exit 0 y data/ intacto, aritmética de costos del A/B y del backfill, insumos de G-10, citas muestreadas, y las 3 ESCALADAS y el PROBLEMA mecánico resueltos.

Observaciones del orquestador: (o1) y (o4) descartadas; (o2) confirmada, es BF-06; (o3) no hay dependencia de setear vars ni secrets, y el calendario es solo una estimación.

No hice escrituras en el repo ni en data/, ni llamadas LLM, ni deploys. El único archivo que creé es el script de medición en el scratchpad.

## Final blockers (required edits)

| id | origin | title | evidence | required edit |
|---|---|---|---|---|
| BF-01 | A-R1-04 + B-R1-02 + C-R1-01 + C-R1-02 (dedup) | Gemelos de R-01/G-08 sin barrer: tres instrumentos de verificación fallan en las ramas FALLBACK_WINNER y DEEPSEEK_DIRECT | Todo leído por mí @ 8efb557: - 34-01-PLAN.md:343: '`34-AB-RESULTS.json` exists with decision.status == "WINNER"'. Es la única aparición del literal en los 6 planes (grep). - 34-01-PLAN.md:214, :319-320. - 34-02-PLAN.md:204, :216-220 y :281 (verify con los dos asserts). - 34-04-PLAN.md:96, :135. | - 34-01:343 → 'decision.status ∈ {WINNER, FALLBACK_WINNER, DEEPSEEK_DIRECT} and spend_total_usd ≤ 0.50'. - 34-02:281, ramificar por decision.status:   - WINNER/FALLBACK_WINNER: los asserts actuales más DEFAULT_PROVIDER=='openrouter'.   - DEEPSEEK_DIRECT: DEFAULT_PROVIDER=='deepseek', DEFAULT_OPENROUTER_MODEL=='deepseek/deepseek-v4-flash', REASONING_EXTRA_BODY_DEEPSEEK==winner.reasoning_extra_body y REASONING_EXTRA_BODY_OPENROUTER == la variante elegida en probe-deepseek-deepseek-v4-flash.json. - 34-02:204: expresar el behavior contra model_config.DEFAULT_PROVIDER, no contra el literal 'openrouter'. - 34-04:135 → 'on the 34-01 decision (WINNER or G-08 fallback)'. - 34-04:96: en DEEPSEEK_DIRECT la prueba de DEEPSEEK_API_KEY es la primera corrida (primario) y el forced-backup prueba OpenRouter. |
| BF-02 | B-R1-01 | La aserción R-13 del harness ('git status data/ vacío') rompe el gate post-apply de 34-05 aunque el sistema esté sano | Leído: - 34-03-PLAN.md:229: 'The real repo data/incidents is unchanged: git status --porcelain data/ is asserted empty via subprocess at the end of the test.' - 34-05-PLAN.md:246 (apply) → :249 (data/ sucio esperado) → :254 (pytest completo en el gate) → :256 (compara git diff --stat data/ antes y después del gate, o sea que ya asume data/ sucio) → :261 (commit). | En 34-03 Task 3 hay dos opciones: - el harness toma un snapshot de `git status --porcelain data/` (o el sha256 de data/incidents/*) al inicio del test y exige que sea idéntico al final; - o verifica solo que no se escribió nada fuera de tmp_path. Registrar el gemelo en 34-05 Task 3 paso 4: el gate post-apply debe pasar con data/ sucio. |
| BF-03 | A-R1-03 | G-11: el estrato gnews_title_only, tal como está definido, tiene 0 miembros; la mitigación R-06 queda inerte y su rama de falla invertida | Mi medición en python de solo lectura (scratchpad arb_strat.py) sobre data/incidents/rejected/2026-09.json @ HEAD: - selected=2073, gnews=1089; - len(description) min/mediana/máx = 44/118/272; - vacías 0, iguales al título normalizado (unescape + espacios + casefold) 0, <40 caracteres 0 → miembros según la definición del plan = 0; - '&nbsp;&nbsp;' presente en 1089/1089; - sacando el sufijo ' - medio' del título y '&nbsp;&nbsp;medio' de la descripción, 1077/1089 coinciden. Ejemplo: 'Seguridad, crimen organizado y cárceles: Gobierno recibe a ministros y fiscal de El Salvador - BioBioChile' \|\| '…El Salvador&nbsp;&nbsp;BioBioChile'. Plan leído: 34-05-PLAN.md:133, :213, :217. | 34-05 Task 1: - redefinir gnews_title_only = host news.google.com (la descripción es solo título por construcción, medido); - agregar un test con una fila real de ese formato. 34-05 Task 2 paso 7b: - un estrato definido con 0 candidatos es un STOP del sampler (definición rota), no 'take all it has'; - definir la retención por estrato sobre conjuntos disjuntos (random = el resto que no pertenece a ningún estrato de riesgo); - pre-declarar qué se publica cuando falla 'random'. |
| BF-04 | A-R1-02 + B-R1-03 (dedup; B lo calificó no-bloqueante, lo subo porque el corte es seguro y no existe ninguna salida que exija completitud) | El backfill hereda el presupuesto de 1200 s por corrida (R-11) y el código de salida al agotarse no está definido: la cache queda parcial y en silencio | Leído: 34-02-PLAN.md:35, :145, :267; 34-05-PLAN.md:117, :151, :206-209 (grep -i 'budget' en 34-05 solo encuentra la fila T-34-18 del threat model, que trata del gasto). Latencias leídas por mí: pipeline/results/ab_deepseek.json mean_latency_ms=3695.3; .planning/spikes/008-granite-openrouter-classifier/results_granite_golden.json mean_latency_ms=1398.1. Aritmética propia: 1200/3,695 ≈ 325 ítems; 1200/1,398 ≈ 858 ítems. | 34-05 Task 1: - el backfill construye su router con budget_s=float('inf') (o fija NEWS_RUN_BUDGET_S alto en su propio proceso) y lo documenta; - códigos de salida distintos: 0 = cache completa, 3 = cap, 4 = router agotado (backup_exhausted); - test: router falso agotado → exit 4 y cache parcial intacta. 34-05 Task 2 paso 5: 'Exit 4 → re-invocar (cache reanuda); nunca pasar a 7b/apply con exit 4'. Agregar la estimación de duración, N × latencia medida en 34-01. |
| BF-05 | A-R1-01 (acotado a finish_length y al tramo G-08; se descarta la parte 'vacío=parse_error' en el tramo estricto, ver descartados) | El runner A/B no clasifica finish_reason=length como lo hace prod (G-09); el fallback G-08 admite ítems que prod expiraría | Leído: - 34-01-PLAN.md:29, :174, :190, :210, :218, :239-241, :267, :271; - 34-02-PLAN.md:33, :194, :245-246; - directiva:98 (G-02: 'reasoning_tokens are 0 or the answer is never truncated'), :105 (G-09). El escenario del proveedor no está medido, porque las llamadas LLM están prohibidas. La divergencia de texto está confirmada. | Registrar un G-NN que ajuste G-02/G-08 a G-09 y aplicarlo en 34-01 Task 2: - el runner cuenta finish_length y 'vacío tras la re-llamada' como 'transient' (subconjunto de api_errors), sin pasarlos por _parse_content; - select_winner descalifica en ambos tramos (estricto y G-08) a todo candidato con finish_length_count>0 o empty_content_count>0, cualquiera sea reasoning_tokens; - tests: finish_reason='length' con JSON válido seguido de prosa → transient, no OK; - decir contra qué contador se mide parse_errors ≤1/47. |
| BF-06 | C-R1-06 + B-R1-08 (parte de los commits arrastrados); respuesta a la obs (o2) | Los planes citan G-08 para un 'fast-path INMEDIATO antes del 09-30' que el G-08 registrado no contiene: es un push a prod sin autorización registrada | Leído: - directiva:51, :75, :103-104; - 34-PREMORTEM.md:40 (R-01 edición 1, con la frase 'Si una pausa 1 o 2 ocurre antes del 09-30, el fast-path de 34-02 se ejecuta de inmediato') y :65 (I-01 descartado apoyándose en esa cláusula); - 34-01-PLAN.md:307, :314; 34-02-PLAN.md:393-398; 34-04-PLAN.md:105. | Elegir una de dos: (a) anexar a la directiva un G-NN fechado que agregue la cláusula del disparo inmediato, diga qué pasa con los commits locales no pusheados (pushearlos tras el gate de 34-01, o hacer el fast-path desde un branch basado en origin/master) y redefina el disparo como 'decision.status ∈ {FALLBACK_WINNER, DEEPSEEK_DIRECT}' en vez de 'pausa 2'; (b) quitar 'IMMEDIATELY' de 34-01:307, 34-02:393 y 34-04:105 y dejar solo el disparo del 09-30 de la directiva. En los dos casos, el fast-path documenta su variante post-34-02 (NEWS_PROVIDER/NEWS_MODEL vía model_config). |
| BF-07 | C-R1-03 | La regla dura 'después de cada deploy, curl a prod y verificar' no está implementada en los deploys de 34-04 y 34-05, y la línea de disclosure de metodología nunca se verifica servida | Leído: - directiva:76 ('After every deploy, curl prod and verify') y :41 (Validation against the served route); - 34-04-PLAN.md:76; 34-05-PLAN.md:265; - grep 'curl\|ischilesafe.com' en 34-04/34-05: el único curl es el del catálogo OpenRouter (34-04:73); - deploy-on-code.yml:64-66 y deploy-manual.yml:51-53: solo `curl -X POST … "$CF_HOOK"`; - 34-06-PLAN.md:84-101. | - 34-04 Task 1 paso 10 y Task 2 (tras el deploy de la primera corrida clasificadora): hacer poll con curl de https://ischilesafe.com/data/incidents/current.json hasta que el conteo y max(date) coincidan con master, y contar news-date en /news/ y /es/noticias/. - 34-05 Task 3 paso 6: el mismo poll, y además `curl -s https://ischilesafe.com/methodology/ \| grep -c '<fragmento EN>'` → ≥1 y su equivalente en /es/metodologia/. |
| BF-08 | C-R1-04 | 34-06 marca NREC-01..10 como Complete sin condición, aunque las ramas G-08 y G-11/cap dejan NREC-01 y NREC-09 sin cumplir literalmente | Leído: - REQUIREMENTS.md:17 (umbrales de NREC-01), :25 (NREC-09 'exactly the 2,073'); - 34-01-PLAN.md:213; 34-05-PLAN.md:208, :217, :250; - 34-06-PLAN.md:125, :127. | 34-06 paso 5: - NREC-01 → Complete solo si decision.status == WINNER; si no, 'Partial (G-08 <status>, owner question in deferred-live)'. - NREC-09 → Complete solo si el residual == 0; si no, 'Partial (<n> not_attempted / <w> withheld / <a> api_error)', con el archivo de ids en deferred-live. - Replicar esa redacción en RUN STATUS. |
| BF-09 | A-R1-05 | Gemelo de R-06 sin barrer: el done/resume de 34-05 Task 3 omite 'withheld' del residual | Leído: 34-05-PLAN.md:28 (la truth incluye withheld), :250, :276-277. | 34-05:276-277 → 'residual classifier_none == api_error_remaining + not_attempted + withheld'. |

## Non-blockers

| id | title | detail |
|---|---|---|
| NB-01 | G-10: los candidatos a re-etiquetar salen de las predicciones, y FAMILY_MIN_V2 queda en el filo de navaja | Los 9 candidatos de 34-01:140 son un subconjunto de las predicciones robos_violentos de DeepSeek y de Granite. Lo medí: DeepSeek tiene 12 y Granite 14. El revisor recibe el sesgo de forma indirecta. Si se re-etiquetan los 9, FAMILY_MIN_V2 = 38/44, exactamente la marca de DeepSeek (medido: 29 → 38). El tramo estricto queda en el filo de navaja por diseño de G-02/G-10, y G-08 pasa a ser la ruta esperada. Edición sugerida: - re-etiquetado ciego de los 44 ítems sin la lista de candidatos, reportando los cambios fuera de ese conjunto; - que el G-NN registre que se espera G-08. |
| NB-02 | 34-01: 'reasoning_unfixable' sin destino y '\|\| break' que corta a los candidatos restantes | reasoning_unfixable (34-01:219) no tiene mapeo en --reasoning auto (:231 solo define exit 2 cuando falta el probe JSON), ni en select_winner, ni código de salida. Los loops de Task 3 (:296, :300) cortan ante cualquier exit≠0, aunque solo exit 3 (cap) justifica cortar. Eso puede dejar menos de 3 candidatos puntuados, cuando NREC-01 y :348 exigen al menos 3. Edición sugerida: - romper solo con exit 3 (`rc=$?; [ $rc -eq 3 ] && break`); - que reasoning_unfixable corra con la variante sin empty y select_winner lo descalifique. |
| NB-03 | G-11: la regla por estrato solo aplica si falla el total, y no hay salida declarada si falla la re-auditoría | Por 34-05:216-217, si el total pasa (≥43/50) se aplica todo, aunque un estrato falle su propia tasa. Ejemplo: 20/20 + 18/20 + 5/10 = 43. Si la re-auditoría 26/30 falla, 'the backfill does not publish', sin pausa ni vínculo con el check por día de 34-06 (:92, :133). Edición sugerida: - umbral por estrato, ceil(0,86·n_s), además del total; - pre-declarar que la no-publicación deja NREC-09/10 en Partial, con pregunta al owner en deferred-live y sin edición de datos. |
| NB-04 | El costo por ítem puede ser un techo, y 34-01 todavía tiene una salida que detiene la corrida | c = spend_usd/47 (34-05:194) se calcula a precio máximo cuando falta usage.cost (34-01:254). Es conservador, pero hay que rotularlo como 'ceiling-priced' cuando usage_cost_present=false. 34-01:291 dice 'If the cap aborts the run (exit 3), stop and report', una salida que detiene sin pasar por G-08. Sugerido: aplicar --decide sobre lo ya corrido y seguir con G-08. |
| NB-05 | Conteos de tests equivocados y modelo del fast-path en la rama DEEPSEEK_DIRECT | test_classifier.py tiene 7 tests y test_classifier_traffic.py tiene 3 (medido). 34-01:178, :199 y :201 dicen '9' y 'both', y 34-02:108 dice 'both'. Corregir a 10 (7+3) y a 'the 3'. 34-02:394 elige deepseek/deepseek-v4-flash en OpenRouter si no hay FALLBACK_WINNER. Eso respeta el texto de la directiva :51, pero en DEEPSEEK_DIRECT ese modelo ya falló el tramo G-08. Conviene que el paso 1 sea NEWS_PROVIDER=deepseek en ese caso. |
| NB-06 | El backfill no drena pop_redispatched() (gemelo de R-04) | El loop de 34-05 (:148-152) no drena pop_redispatched(), cosa que 34-02 exige (:146, :266, :323). Tras un disparo del breaker, las respuestas del backup para la racha se descartan: los ítems quedan como API_ERROR en la cache, se reintentan y se gastan dos veces. Ese gasto de re-despacho no se mide, y api_error_remaining sale inflado. Edición sugerida: drenar después de cada classify() y escribir una línea de cache por (key, resultado). |
| NB-07 | Aritmética del test del spend guard en 34-01 | El behavior de 34-01:220 espera exit 3 con ledger 0,49 y techo 0,0025. Pero 0,4925 ≤ 0,50, y la regla de :255 (abortar si total + techo > cap) permite la llamada, así que el test TDD contradice la implementación correcta. Usar 0,499 + 0,0025 → exit 3, y agregar el borde 0,4975 + 0,0025 = 0,50 → llama. |
| NB-08 | El verify de 34-02 Task 3 cuenta .pyc de __pycache__ | Hoy `grep -rln granite-4.1-8b pipeline` devuelve 8 archivos, 4 de ellos .pyc (lo ejecuté). Entre ellos está test_classifier.cpython-313-pytest-8.4.2.pyc, que no se regenera porque la versión actual es pytest 9.1.1. El verify 'exactly 2 fixtures' (34-02:384) falla aunque el código esté limpio. Usar `--exclude-dir=__pycache__` o `git grep -l`. |
| NB-09 | El issue de news-heartbeat que se abra en la transición no se cierra nunca, y 34-06 3b exige cero abiertos | heartbeat.yml corre a diario a las 12:00Z (:23) y no tiene ningún paso que cierre issues (grep, leído). Entre el push de 34-04 y la primera corrida que agrega un id nuevo, la evidencia de fallback es 09-05, o sea roja. Si ese hueco cruza las 12:00Z, o si 34-04:91 espera por U==0, se abre pipeline-failure-news-heartbeat, y 34-06:106 exige []. El mecanismo está verificado; que se dispare depende del horario. Edición sugerida: aceptar un issue anterior a la primera corrida clasificadora si el último step news está en success, y cerrarlo con un comentario. |
| NB-10 | La auditoría G-11 no pregunta por la fidelidad del titular, y la regex de parentesco es estrecha | Se publican ~1.000 titulares reescritos por el LLM (34-05:122). La auditoría de 7b no pregunta si title_es/title_en agregan hechos, personas o parentescos que no están en la fuente (clase V-06, hotfix t59). La regex de --review (:131) no incluye hijo\|hija\|padre\|madre\|herman\|espos\|pareja\|yerno\|nuera. El ROADMAP deja la política de titular literal para FID-01 (Fase 36), por eso no lo trato como bloqueante. Sugerido: agregar la pregunta 4 de fidelidad, ampliar la regex y registrar en deferred-live que las cards llevan titular del LLM hasta FID-01. |
| NB-11 | Redacción de la disclosure (34-05:234-237) | Problemas del texto: - 'Between 4 and <D> September' se rompe si la primera corrida clasificadora cae en octubre; - 'with the same method' no es exacto, porque el modelo cambió; - 'were reclassified' exagera si hay ítems withheld o not_attempted; - se commitea aunque 7b termine sin publicar. Usar un rango de fechas completo, 'with our current classifier' y 'items that could not be verified were not published', y omitir el commit si no hay publicación. |
| NB-12 | La regex del barrido servido de 34-06 no se amplió como la de 34-05 (gemelo de R-06) | 34-06:101 no tiene términos de parentesco en inglés ni la ampliación de 34-05:131. Reusar el patrón de 34-05 y agregar grandfather\|grandmother\|father-in-law\|mother-in-law\|nephew\|niece\|brother-in-law\|sister-in-law\|prison\|inmate. |
| NB-13 | Higiene de git en 34-05 Task 3 | Task 2 modifica la directiva (RUN STATUS y G-NN, :210-218), un archivo trackeado, y no lo commitea. rebase.autoStash no está configurado y pull.rebase=false (medido con git config), así que `git pull --rebase` (:242) rechaza el árbol sucio. El `git reset --hard HEAD~1` de :262 descartaría registros de decisiones que no estén commiteados. Antes del paso 2: `git add .planning && git commit`, y verificar que `git status --porcelain` esté vacío. |
| NB-14 | Gemelo de R-13 sin barrer en 34-02 | 34-02 agrega tests de main() que escriben pending.json. El fixture autouse NEWS_DATA_DIR de conftest recién llega en 34-03 (:267), y los verify de 34-02 no revisan data/. Lo atrapa 34-04 paso 2, así que cuesta tiempo pero no correctitud. Mover el fixture a 34-02 Task 2, o agregar `git status --porcelain data/` → vacío a sus verify. |

## Discarded

| id | title | reason |
|---|---|---|
| A-R1-01(parte 1) | 'Vacío tras la re-llamada = parse_error en el runner' como divergencia de prod | Coincide con el texto de G-08 ('parse_errors ≤1/47 (after the prod-identical empty-content re-call)', directiva:104). En el tramo estricto (parse 0/47) es conservador: descalifica. El riesgo que queda está en el tramo G-08 (1 vacío admitido, que en prod expira) y quedó absorbido en BF-05. |
| A-R1-07(parte 1) | El probe acepta reasoning_tokens=0 cuando el proveedor no reporta el campo | PLAUSIBLE y no verificable sin una completion (están prohibidas). La regla del probe exige además empty==0 y api_errors==0 (34-01:219), y la corrida completa cuenta empty y finish_length. Con BF-05, cualquier truncado descalifica sin importar reasoning_tokens, así que el daño queda cubierto. Las partes 2 y 3 del hallazgo se confirmaron (NB-02). |
| A-R1-08(a,d) | La muestra G-11 no es representativa y la conjunción de 3 preguntas es más estricta que FID-03 | (a) Es el diseño de G-11, una decisión registrada, y no hay evidencia que lo invalide. (d) Que la conjunción vuelva probable la rama de falla es PLAUSIBLE: no se puede medir sin LLM. Las partes (b) y (c) se confirmaron en NB-03. |
| A-R1-09(aritmética 0,509>0,50) | El A/B podría superar el cap de USD 0,50 en el caso techo | El techo de 4.500 tokens de entrada no es lo que se cobra. El ledger cobra los tokens reales (34-01:254), y el SYSTEM_PROMPT medido es de 3.580 tokens (34-01:121), así que el gasto real queda bajo el techo. Solo se mantiene la parte de rotular el costo como 'ceiling-priced' y la salida por exit 3 (NB-04). |
| B-R1-08(freshness) | Un fast-path ya shippeado haría PASS el 'rojo esperado' de freshness en 34-03/34-04 | Ningún paso hace pull entre el fast-path y 34-03 Task 2 o 34-04 Task 1 paso 3. El pull del fast-path (34-02:396) ocurre antes del dispatch, y 34-04 hace pull recién en el paso 8, después de validar. Los datos locales siguen con max(date)=2026-09-04 y el rojo esperado se mantiene. La parte de los commits de 34-01 arrastrados por el push del fast-path quedó en BF-06. |
| B-R1-10 | 34-02 es demasiado grande para un ejecutor de ~40 min, y el autouse no 'falla fuerte' | El tamaño es una estimación que no se puede verificar antes de ejecutar, y GSD commitea por tarea, así que un corte no pierde lo ya hecho. Recomendación para el orquestador (no firma): un ejecutor por tarea. La afirmación de 'no falla fuerte' está indeterminada. El autouse parchea también preflight_openrouter para que lance, y scrape_news.main() convierte cualquier Exception en return 1 (scrape_news.py:389-391, leído). Un sitio sin re-apuntar con primario openrouter falla ruidosamente, salvo que router.preflight() atrape la excepción, algo que el plan no especifica. |
| C-R1-18 | El calendario cabe antes del 10-05 con 4-6 días de margen | Es una estimación de duraciones, no una medición. No firma. Se trata en la observación (o3). |

## Orchestrator observations

| obs | status | detail |
|---|---|---|
| (o1) load_dotenv de pipeline/.env frente al .env en la raíz | DESCARTADA | Los planes no asumen nada falso. Verificado por mí: - pipeline/.env no existe (ls) y el .env de la raíz sí existe. - ab_score.py no llama a load_dotenv (solo lo menciona en mensajes de :30 y :301), y el plan no lo usa. - archive_r2.py:59-60 carga solo pipeline/.env. El camino primario de R2 en 34-05 es el dispatch de r2-archive.yml, con secrets de Actions, y el fallback local (34-05:269) hace load_dotenv('.env') de la raíz antes de runpy. - El runner de 34-01 (:226) y el backfill de 34-05 (:149) cargan pipeline/.env y después el .env de la raíz, con override=False. El backfill importa classifier después de load_dotenv (R-12), y el runner construye su propio cliente OpenAI por corrida, así que no depende del cliente creado al importar. También lo cubrieron A-R1-15, B-R1-15 y C-R1-16. |
| (o2) Coherencia de G-08 con la directiva (pausa 2, fast-path, pausa 6) | CONFIRMADA-POR-ARBITRO | El G-08 registrado es coherente en sí mismo: - convierte la pausa 2 en fallback determinista hasta el 10-03; - mantiene la pausa 1 con el fast-path primero si prod no clasifica; - deja la pausa 6 sin cambios. La contradicción está en los planes. Tres de ellos (34-01:307, 34-02:393, 34-04:105) le atribuyen a G-08 un 'fast-path INMEDIATO ante pausa 1/2 antes del 09-30' que el G-08 registrado no tiene. El premortem R-01 lo había propuesto y se perdió al registrar la decisión. Un ejecutor que sigue la directiva no hace push; uno que sigue los planes sí. Además 'ocurre la pausa 2' queda sin definir después de G-08. Todo esto está en BF-06. |
| (o3) Camino crítico hacia ~09-26 y pasos que dependan de setear vars o secrets | CUBIERTA-POR-LENTE | Ningún paso exige setear repo vars ni secrets. Verificado por mí: - 34-03:238 y :258 solo leen vars.* en el env y prohíben `gh variable set` y `gh secret set`; - force_backup es un input de dispatch; - news-pipeline.yml:54 y :66-67 ya inyectan OPENROUTER_API_KEY y DEEPSEEK_API_KEY, y :26 CF_DEPLOY_HOOK_URL; - el grep 'secret set\|variable set\|gh api' en los planes solo encuentra la prohibición de 34-03:238. El calendario (listo hacia 09-27..09-29) es una estimación de C-R1-18, no una medición. BF-04 agrega de 3 a 7 re-invocaciones de backfill de ≤20 min cada una, o sea horas y no días. Los ciclos de gate por las ediciones de esta ronda (máximo 2) consumen parte del margen. |
| (o4) Los LITERAL_AUSENTE del script de citas son falsos positivos | DESCARTADA | Muestreé por mi cuenta las citas de código existente que sostienen afirmaciones y todas se sostienen: - classifier.py: 52, 58, 60-65, 72-79, 159, 162-166; - scrape_news.py: 101-103, 178, 215, 280-283, 299-304, 314, 359-362, 389-391; - store.py: 139, 194-198; - clustering.py: 52-60; - test_scrape_news.py: las 10 líneas de patch (143 … 496); - test_classifier.py: los patch en 45/58/72/82/92/107 y :115-122; - test_clustering.py: 976 y 991; - freshness.mjs: 25; - heartbeat.yml: 23, 73-74, 84-85, 100; - deploy-on-code.yml y deploy-manual.yml: solo el POST al hook; - archive_r2.py: 59-60; - site/package.json: 10-13; - ab_score.py: 54-58. Los LITERAL_AUSENTE restantes describen el estado objetivo, no código existente. No encontré ninguna cita desplazada que cargue peso. |

## Closures verified by the arbiter

- Población NREC-09: medí selected=2073 con fecha parseada (glob 2026-*, mes ≥ 2026-09, classifier_none, first_seen ≥ 2026-09-04T20:13:49Z). Coincide con 34-05:80-81 y G-06.
- Baseline: `python -m pytest pipeline/tests/ -q -p no:cacheprovider` → 395 passed, 1 skipped, 1 xfailed, EXIT=0 (lo ejecuté yo). `git status --porcelain data/` vacío antes y después. El último commit de data/ es 45c2650.
- ESCALADA 1 (pipeline/.env en 34-01:226): no es un error. ab_score.py:54-58 es el bootstrap de sys.path, citado solo como patrón. pipeline/.env no existe y el .env de la raíz sí, tal como dice el plan.
- ESCALADA 2 (LITERAL_AUSENTE): mi muestra confirma que son descripciones del estado objetivo. Ver (o4).
- ESCALADA 3 (R-07 '$2.3'): 34-05:199-201 dice ahora v4-flash ≈ $1.30 típico / $2.09 techo y v4.1-flash ≈ $3.00 / $3.94. Recalculado con P=3.700, C=170, N=2.400: 2,996 / 3,942 / 2,093.
- PROBLEMA mecánico site/package.json:10-11: refutado. Las líneas 10-11 son prebuild y build, y validate está en :13, que es exactamente lo que dice 34-03:71 (leído).
- Aritmética del A/B en 34-01:286-289 recalculada: $0.00123 / $0.00246 / $0.00058 / $0.00089 / $0.00196 por llamada; probe de 42 llamadas ≤ $0.058; full de 235 ≤ $0.335; total $0.393.
- Insumos de G-10: ab_deepseek.json (deepseek-v4-flash sobre golden_set.json, 47 per_item, commune_match=42). Los 9 candidatos de 34-01:140 son un subconjunto de las predicciones robos_violentos de DeepSeek (12) y de Granite (14). DeepSeek da family 29 en v1 y 38 en v2 si se re-etiquetan los 9 (lo medí yo).
- Premortem aplicado en el cuerpo (leído): R-02 (34-03:133/:143; 34-05:101/:165/:243/:247; 34-06:86); R-03 (34-02:194/:245-246/:297; 34-03:219-220); R-04 re-dispatch (34-02:196/:266/:323-325; 34-03:228); R-08 (34-03:169-174/:192-198; 34-06:105-108); R-09 (34-02:198/:263; 34-03:238/:254-258; 34-04:94-97); R-11 (34-02:197/:267; 34-03:222); R-12 (34-02:199/:270; 34-05:120/:149/:205); R-13 --theirs eliminado (34-04:74); R-14 (34-05:259-260). Los gemelos faltantes están en BF-01/02/03/04/09 y en NB-06/NB-12/NB-14.
- Instrumento G-05/G-07(a): freshness.mjs:25 lee data/incidents/current.json de la raíz; current.json no tiene last_new_incident_at y max(date)=2026-09-04, así que el rojo esperado de 34-03:205 y 34-04:68 es correcto con los datos locales; bump_last_new=False está encadenado en 34-05 y 34-06.
- Tests existentes: test_classifier.py tiene 7 y test_classifier_traffic.py 3 (grep -c 'def test_'). Los 10 sitios de patch de test_scrape_news.py y los 6+2 patch de client coinciden con 34-02:107-110.

## Measurements reserved to the orchestrator

- Después de redefinir gnews_title_only, volver a medir sobre la cache real los candidatos por estrato: gnews ≥20 y non_chile_cue ≥10 entre los ítems OK. Hoy hay 1.089 filas de Google News y 154 filas con indicios no chilenos antes de clasificar (medido por mí); los OK serán un subconjunto.
- Medir en el A/B real finish_length_count, empty_content_count y usage_cost_present por candidato, y registrar el G-NN que ajusta G-02/G-08 a G-09 (BF-05).
- Medir la latencia por ítem del ganador en 34-01 para estimar la duración del backfill (N × latencia) y fijar NEWS_RUN_BUDGET_S o budget infinito en el proceso de backfill (BF-04).
- Decidir y registrar como G-NN fechado si el fast-path se dispara de inmediato ante FALLBACK_WINNER/DEEPSEEK_DIRECT o solo el 09-30 (BF-06).

## Lens verdicts

- C — producto, ruta servida y coherencia con el pedido del owner (ronda 1, 34-01.: NO-GO (18 findings)
- B — Ejecutabilidad de gates, tareas e invariantes (ronda 1, 34-01..34-06 @ 8efb5: NO-GO (17 findings)
- A — Mediciones, estadística e instrumentos (ronda 1, objeto 34-01..34-06 @ 8efb5: NO-GO (15 findings)

## Condition closure log (append)

