---
phase: 34-news-classification-restore
plan: 05
type: execute
wave: 5
depends_on: ["34-02", "34-04"]
files_modified:
  - pipeline/backfill_classifier_outage.py
  - pipeline/tests/test_backfill_classifier_outage.py
  - data/incidents/current.json
  - data/incidents/rejected/2026-09.json
  - data/incidents/rejected/2026-10.json
  - data/incidents/archive/
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
autonomous: false
requirements: [NREC-09]
executor: opus (Task 1 backfill logic); orchestrator (Tasks 2-3: spend, data mutation, push, dispatch)

must_haves:
  truths:
    - "Selection = rows with rejection_stage 'classifier_none' AND datetime(first_seen) ≥ 2026-09-04T20:13:49Z across rejected/2026-09.json and rejected/2026-10.json (if present), deduped by id. It contains all 2,073 rows measured 2026-09-22 (the frozen subset with first_seen ≤ 2026-09-22T20:49:36.848245+00:00)."
    - "The dry-run performs zero writes (every data/incidents file hash unchanged) and reports counts only."
    - "After apply: no selected row is left classifier_none except those whose last outcome was API_ERROR (reported). Accepted rows are gone from rejected/ and present in current.json or archive/. Every other row carries its real stage. seen.json is byte-identical."
    - "Accepted items are deduped against the existing incidents with the 0.82 rule (dedup.py:21, bucket (cut,date), title_es) before merge_and_write."
    - "The measured split (accepted in-window / accepted archived / per stage / api_error remaining / excluded) and the spend (≤ USD 3.00) are reported as numbers."
    - "R2 rejected.jsonl and corpus-state.json are regenerated from the corrected files."
  artifacts:
    - path: pipeline/backfill_classifier_outage.py
      provides: "Two-phase backfill: --classify (cache, no repo writes) then --apply (fast, idempotent); --dry-run; --limit; --exclude-ids; spend cap"
      contains: "def select_outage_rows"
    - path: pipeline/tests/test_backfill_classifier_outage.py
      provides: "Offline tests: selection, dry-run zero writes, idempotency, dedup, seen untouched, cap"
  key_links:
    - from: pipeline/backfill_classifier_outage.py
      to: pipeline/news/classifier.py
      via: "build_router_from_env() → router.preflight() → router.classify() (the hardened 34-02 path)"
      pattern: "build_router_from_env"
    - from: pipeline/backfill_classifier_outage.py
      to: pipeline/news/store.py
      via: "merge_and_write(accepted, current_path, archive_dir)"
      pattern: "merge_and_write"
---

<objective>
Reclassify the outage items lost to the Granite delisting (V-04 / NREC-09) from the inputs `record_rejected` already stored, without refetching. This follows the `pipeline/repair_sexual_family.py` pattern:
- argparse CLI
- local repair first
- an R2 rebuild after the local repair. repair_sexual_family.py:197-227 does it by running archive_r2; here it runs as an r2-archive.yml dispatch, so credentials stay in Actions
- idempotent re-runs

Purpose: `current.json` empties on about 2026-10-05, because the newest incident is 2026-09-04 and the window is 30 days (store.py:139). The ~2,073+ lost items hold the missing September.

Output: the backfill tool with tests, corrected data/incidents files, the methodology disclosure line (owner-decision default), and regenerated R2 outputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md  (backfill rules: dry-run + backup first; data/ mutation allowed only here; spend caps)
@.planning/phases/34-news-classification-restore/34-RESEARCH.md  (§Pitfalls 3, 4, 5)
@.planning/phases/34-news-classification-restore/34-AB-RESULTS.json  (winner mean_prompt_tokens / mean_completion_tokens / prices for the estimate)
@.planning/phases/34-news-classification-restore/34-04-SUMMARY.md  (date live classification resumed = end date of the disclosure)
@pipeline/repair_sexual_family.py
@pipeline/news/store.py
@pipeline/news/dedup.py

<interfaces>
Measured this session (2026-09-22, HEAD e565ce8):
- rejected/2026-09.json: 2,308 rows (classifier_none 2,296, commune_null 7, resolver_fail 5). Row keys: id, url, outlet, date (YYYY-MM-DD), title, description (≤500, HTML-stripped), rejection_stage, first_seen (ISO with microseconds and "+00:00").
- Outage rows (classifier_none, datetime(first_seen) ≥ 2026-09-04T20:13:49Z): **2,073**. 34-RESEARCH.md reported 2,057 because it STRING-compared against "…20:13:49Z". The 16 rows of the first failed run share first_seen "2026-09-04T20:13:49.524591+00:00", and "." sorts before "Z", so they were dropped. The tool MUST parse datetimes.
- max first_seen = 2026-09-22T20:49:36.848245+00:00. 2,072 of the 2,073 are in seen.json. 0 are in current.json. Dup ids: 0. Publication dates run 2021-09-16..2026-09-22. Per-day count for 2026-09-05..09-22 is between 75 and 148, so every day has ≥ 75 rows. 2 rows are dated < 2026-09-03 and 62 are dated < 2026-09-05.
- Pre-outage acceptance (August 2026): 1,877 incidents vs 1,656 rejected → ~53%. This is an estimate for planning only; the real split is measured.
- Hotfix quick-260922-t59 (commit 45c2650) dropped ids c3db293fd1316ad1, d468d43eeda0316d, f9031873f12b9479, 8f18cd275b4a6692, 836e500e4e4b1f30, 60fa7e16887af782, efa0fae52180e3e2, c53a579a6c972705, and retitled 514872e1d06db7f2. None of those ids is in the outage set (0 overlap). Outage rows matching /Fierro/ = 0, /Carahue/ = 1 (f3edc22bd1168826, dated 2026-09-04), /suicid/ = 2 (2899498ea6b50be0 — a US case; bcacb259d20d6d43 — Spain).
- The repo-root `.env` has non-empty OPENROUTER_API_KEY, DEEPSEEK_API_KEY and R2_* (names checked, values never read). `pipeline/.env` does not exist, and archive_r2.py:59-60 loads only pipeline/.env.
- archive_r2.py consolidates all rejected/*.json (:172-201), drops rejected ids that collide with incident ids (:495-504), and uploads rejected.jsonl (:659-663) and corpus-state.json (:621-632). r2-archive.yml has workflow_dispatch (:6) and concurrency group r2-archive (:8-9).
- store.merge_and_write (after 34-03) returns the count of new ids in the window and sets last_new_incident_at when > 0.
- dedup.deduplicate(incidents) keeps the first occurrence: canonical URL first, then title_es SequenceMatcher ≥ 0.82 within the (cut, date) bucket (dedup.py:21, 65-106).
- scrape_news.record_rejected SKIPS existing ids (:101-103). A later cron run therefore never overwrites the stages this tool rewrites.

Tool contract (this plan creates):
    python pipeline/backfill_classifier_outage.py [--data-dir data/incidents] [--since 2026-09-04T20:13:49Z]
        --dry-run | --classify | --apply | --review
        --cache PATH (required; JSONL, outside data/)  --limit N  --exclude-ids ID[,ID]
        --spend-cap 3.00  --spend-ledger PATH  --report PATH
    def select_outage_rows(data_dir, since) -> list[dict]      # datetime-parsed, deduped by id, month files ≥ 2026-09
    stages written: accepted rows removed; "low_confidence" | "parse_error" | "commune_null" | "resolver_fail"
                    | "centroid_fail" | "url_rejected" | "dedup_duplicate" | "manual_exclusion";
                    API_ERROR / never-attempted rows untouched (stay classifier_none → next run retries them)
    rewritten rows gain: "reclassified_at" (ISO Z), "reclassified_model" ("<provider>:<model>")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backfill_classifier_outage.py (two-phase, idempotent) + offline tests</name>
  <files>pipeline/backfill_classifier_outage.py, pipeline/tests/test_backfill_classifier_outage.py</files>
  <read_first>pipeline/repair_sexual_family.py (whole), pipeline/scrape_news.py:317-356 (the OK→incident path to mirror), pipeline/news/classifier.py (router API from 34-02), pipeline/news/store.py (post 34-03)</read_first>
  <behavior>
    - A row with first_seen "2026-09-04T20:13:49.524591+00:00" IS selected (a string compare would drop it). A row at 20:13:48 is not. Rows with other stages are never selected.
    - A row present in both 2026-09.json and 2026-10.json is selected once (dedup by id). 2026-10.json is optional.
    - --dry-run on a tmp data dir: the sha256 of every file under it is unchanged, the cache is not created, and stdout has selected=N, by_file, frozen_subset=<count with first_seen ≤ 2026-09-22T20:49:36.848245+00:00>, would_archive=<count with date < today−30d>, already_cached=K, planned_calls=N−K.
    - --classify with a fake router (OK/NOT_CRIME/PARSE_ERROR/API_ERROR mix) writes one cache line per attempted id and NO file under data dir. On re-run, ids whose cached outcome is final are skipped and API_ERROR ids are attempted again. When router.classify returns None (exhausted), the loop stops and the remaining ids stay uncached.
    - The spend guard stops before exceeding --spend-cap (fake meter). Exit code 3, and the cache is intact.
    - --apply:
      - OK + resolvable → the incident appears in current.json (date = row date, url/outlet = row, title_es/title_en/family from output) and the row is removed from its rejected file.
      - NOT_CRIME → stage low_confidence. PARSE_ERROR → parse_error. Resolver None → commune_null/resolver_fail as in scrape_news. Centroid None → centroid_fail. build_incident None → url_rejected.
      - A near-duplicate (ratio ≥ 0.82, same cut+date) of an EXISTING current.json incident → stage dedup_duplicate, not merged.
      - A row in --exclude-ids → manual_exclusion, not merged.
      - An API_ERROR row → untouched (byte-level row equality).
      - seen.json is byte-identical. An accepted item dated < today−30d lands in archive/YYYY-MM.json and the report counts it as accepted_archived.
    - Running --apply twice: the second run changes no file (hashes equal). Idempotent.
    - --review prints the cached OK outcomes whose title/description/title_es match /Fierro|Carahue|suicid|quit\w* la vida/i, with id, date, family and title_es.
    - --r2 is NOT implemented. R2 regeneration is done by dispatching r2-archive.yml (Task 3), which avoids handling credentials locally.
  </behavior>
  <action>
Implement the contract in `<interfaces>`.

**Selection (`select_outage_rows`):**
- Iterate `sorted((data_dir/"rejected").glob("2026-*.json"))` with the month ≥ "2026-09".
- Keep rows where stage == "classifier_none" and `datetime.fromisoformat(first_seen) >= since`. Normalize a trailing "Z" before parsing; treat a naive datetime as UTC.
- Dedup by id, first occurrence wins.

**Classify phase:**
- Load keys from pipeline/.env and then the repo-root .env (python-dotenv, override=False).
- `router = build_router_from_env()`, then `router.preflight()` once, then `router.classify(row["title"], row["description"])`. The inputs are exactly what classify() read originally: title + description[:500] (classifier.py:159, and record_rejected stored description[:500]).
- Append a JSON line per attempt: id, outcome, status_code, error, output (model_dump) or null, provider, model, ts. Flush every line so a crash loses at most one item.

**Spend meter:**
- Before the first call and every 50 calls, GET `https://openrouter.ai/api/v1/key` with the bearer key and read `data.usage` (USD). The delta since start is the measured spend. If the field is absent or the call fails, fall back to Σ(prompt_tokens×max_in + completion_tokens×max_out) using the catalog max prices (conservative).
- Between checkpoints, add per-call estimates.
- Backup (DeepSeek) calls are always charged conservatively at the peak price (in 0.30, out 1.20 per 1M).
- Stop (exit 3) when measured + estimate + next-call ceiling > cap.
- Persist the ledger JSON after every call.

**Apply phase:**
- Load the cache (last line per id wins). Build candidates via the same resolve_cut → get_centroid → build_incident path as scrape_news.py:317-356.
- Dedup: `deduplicate(existing_current + existing archive incidents for every month a candidate would archive into + candidates)`, with existing first so they win. Survivors whose id is a candidate are accepted; dropped candidates get dedup_duplicate.
- Call `merge_and_write(accepted, current_path, archive_dir)` ONCE.
- Rewrite each touched rejected month file in place:
  - remove accepted ids
  - set stage + reclassified_at + reclassified_model on the others
  - leave API_ERROR / uncached rows untouched
  - preserve key order
  - refresh the envelope's "generated"
  - write via atomic_write_json, keeping `indent=2, ensure_ascii=False` to match record_rejected's format (scrape_news.py:123)
- Never open seen.json for writing.
- Write the `--report` JSON with: selected, frozen_subset, frozen_subset_resolved, accepted_in_window, accepted_archived, by_stage, api_error_remaining, not_attempted, excluded, spend_usd, window_cutoff.

**Tests:** write the `<behavior>` tests against a tmp data dir built from small fixtures. Mock the router and the spend GET; nothing may reach the network (autouse guard patching requests.get and classifier._request_completion to raise).
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_backfill_classifier_outage.py -q -p no:cacheprovider → exit 0; python -m pytest pipeline/tests/ -q -p no:cacheprovider; echo EXIT=$? → EXIT=0, 2 xfailed, 0 failed</automated>
  </verify>
  <done>The tool exists with offline tests proving selection, zero-write dry-run, idempotency, dedup against existing incidents, seen untouched and the cap. It is committed locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ORCHESTRATOR — backup, dry-run, estimate, classify phase (external spend, hard cap USD 3.00)</name>
  <what-built>Offline-tested backfill tool; live classification restored (34-04 green).</what-built>
  <action>
SCR = the session scratchpad (no spaces in the file names below).

1. Precondition: `git status --porcelain` is empty, then `git pull --rebase origin master`.
2. Backup: `mkdir -p "$SCR/bf-backup" && cp data/incidents/current.json data/incidents/seen.json "$SCR/bf-backup/" && cp -r data/incidents/rejected data/incidents/archive "$SCR/bf-backup/" && (cd data/incidents && find . -type f -name '*.json' -exec sha256sum {} +) > "$SCR/bf-backup/SHA256SUMS"` gives a non-empty SHA256SUMS. The seen.json line is the reference for the later "untouched" check.
3. Dry-run: `python pipeline/backfill_classifier_outage.py --dry-run --cache "$SCR/bf-cache.jsonl"` → prints selected=N with N ≥ 2073, frozen_subset=2073 (a mismatch → STOP; the selection is wrong), and would_archive=A. Then `git status --porcelain data/` → empty (zero writes).
4. Pre-run estimate. Read the winner's mean_prompt_tokens (P), mean_completion_tokens (C), median and max prices from 34-AB-RESULTS.json and print both:
   - typical = N×(P×median_in + C×median_out)/1e6
   - ceiling = N×(P×max_in + C×max_out)/1e6

   Planning figures from this session's measurements, with P ≈ 3,700 and C ≈ 170 assumed until 34-01 measures them, and N ≈ 2,073 + ~110/day until 34-04. The spot prices below are the planner's approximations of the median rows (median_in/median_out = the catalog medians printed by 34-01); the exact medians come from the 34-01 catalog:
   - N ≈ 2,400, winner deepseek/deepseek-v4-flash: typical ≈ 2,400×(3,700×0.10 + 170×0.40)/1e6 ≈ $1.05, ceiling ≈ $2.09.
   - winner deepseek/deepseek-v4.1-flash: typical ≈ $2.3, ceiling ≈ $3.94, so the cap binds at the ceiling.

   If the typical estimate > $3.00, STOP and apply pause point 4 (ask the owner). Otherwise proceed. The cap enforces the rest.
5. Classify phase: it writes nothing under data/, so it cannot race the cron. Run it in the background: `python pipeline/backfill_classifier_outage.py --classify --cache "$SCR/bf-cache.jsonl" --spend-cap 3.00 --spend-ledger "$SCR/bf-spend.json"`. Exit 0 → done. Exit 3 → the cap was hit: record the spend and stop at pause point 4. Router exhausted → re-invoke later; the cache resumes.
6. `python -c "import json;print(json.load(open('$SCR/bf-spend.json'))['total_usd'])"` gives ≤ 3.00. Add it to RUN STATUS "LLM spend". If phase spend (34-01 + this) > $5, apply pause point 4.
7. Review: `python pipeline/backfill_classifier_outage.py --review --cache "$SCR/bf-cache.jsonl"`. For each printed row, a fresh-context opus check compares title_es against the source title/description. The goal is to catch a kinship claim about a named person not in the source (the V-06 class), or a suicide / non-crime labelled as `vida`. Any row that fails goes into the `--exclude-ids` list, recorded as the next G-NN with the reason.
  </action>
  <verify>
    <automated>Each numbered step in the action carries its exact command and its expected output; a mismatch at any step is a FAIL of this task.</automated>
  </verify>
  <done>Cache complete (or intentionally partial with the reason recorded), spend ≤ 3.00, exclusion list decided</done>
  <resume-signal>Cache complete (or intentionally partial with the reason recorded), spend ≤ 3.00, exclusion list decided</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ORCHESTRATOR — methodology disclosure, race-safe apply, gate, push, deploy, R2 regeneration</name>
  <what-built>Classification cache for the whole outage population.</what-built>
  <action>
**1. Disclosure (owner-decision default "Backfilled incidents keep original dates + one bilingual line"; sonnet-sized edit).**

Add one sentence to the news-layer section of `site/src/pages/methodology.astro` (next to :242-247) and its counterpart in `site/src/pages/es/metodologia.astro`:
- EN: "Between 4 and <D> September 2026 our news classifier was unavailable; news items collected in that period were reclassified on <apply date> with the same method and keep their original publication dates."
- ES: "Entre el 4 y el <D> de septiembre de 2026 nuestro clasificador de noticias no estuvo disponible; las noticias recopiladas en ese período se reclasificaron el <fecha> con el mismo método y conservan su fecha de publicación original."

<D> = the day of the 34-04 first classifying run. Use no absolute safe/dangerous wording (the forbidden-language validator enforces this). Commit it separately first: `git commit -m "docs(34-05): disclose Sept 2026 news reclassification"`.

**2. Race check (Research Pitfall 5).** `gh run list --workflow=news-pipeline.yml --limit 1 --json status,createdAt` must show status "completed" AND a createdAt less than 3 h ago. Measured minimum gap between cron runs is 3h55m, so this leaves ≥ ~55 min, and apply + commit + push takes ≲ 2 min. Otherwise wait. Then `git pull --rebase origin master`.

**3. Apply.**
- Run `python pipeline/backfill_classifier_outage.py --apply --cache "$SCR/bf-cache.jsonl" --exclude-ids "<list or empty>" --report "$SCR/bf-report.json"` → exit 0.
- Seen untouched: `sha256sum data/incidents/seen.json` equals the line in `$SCR/bf-backup/SHA256SUMS`, or equals the post-pull seen.json hash if a cron commit changed it in between. Capture that hash right before apply.
- `git status --porcelain data/` lists only current.json, rejected/2026-09.json (and 2026-10.json if present) and archive/*.json.
- Residual check: `python -c "import json,datetime as d;T=d.datetime(2026,9,4,20,13,49,tzinfo=d.timezone.utc);import glob;r=[i for f in sorted(glob.glob('data/incidents/rejected/2026-*.json')) if f[-12:-5]>='2026-09' for i in json.load(open(f,encoding='utf-8'))['items']];print(sum(1 for i in r if i['rejection_stage']=='classifier_none' and d.datetime.fromisoformat(i['first_seen'])>=T))"` → equals the report's api_error_remaining + not_attempted. Target 0.
- Hotfix intact: none of the 8 hotfix ids is in current.json, and 514872e1d06db7f2's title_es equals `git show 45c2650:data/incidents/current.json`'s value (python one-liner).

**4. Gate: ONE chained command.**
- `python -m pytest pipeline/tests/ -q -p no:cacheprovider; echo PY=$?; cd site && npm run build && node scripts/validate/all.mjs; echo V=$?; npx vitest run; echo VT=$?; npx astro check; cd ..` → PY=0, V=0 (16/16; freshness is green since 34-04), VT=0, "0 errors".
- Then `bash .github/scripts/lint-workflows.sh` → 0.

**5. Commit and push.**
- `git add data/incidents && git commit -m "data(34-05): backfill Sept 2026 classifier-outage items (NREC-09)"`, then `git pull --rebase origin master`.
- If the rebase conflicts in data/: `git rebase --abort`, then `git reset --hard HEAD~1`. This drops ONLY the local, unpushed data commit, which is reproducible from the cache; the disclosure commit is kept. Then `git pull --rebase origin master`, then repeat steps 2-5 (apply is idempotent from the cache).
- `git push origin master` (never --force).

**6. Deploy.** The disclosure changes site/**, so deploy-on-code triggers. Check `gh run list --workflow=deploy-on-code.yml --limit 1 --json conclusion,headSha` → success on the pushed SHA. If no deploy ran for the data commit's SHA, run `gh workflow run deploy-manual.yml --ref master` (authorized by the directive for served data).

**7. R2.**
- `gh workflow run r2-archive.yml --ref master`, then `gh run watch <id> --exit-status` → success. The log shows "Consolidated N rejected candidates" with N = the rejected row count after apply, and the corpus-state and rejected.jsonl uploads.
- Local fallback only if the dispatch fails for a non-R2 reason: `python -c "from dotenv import load_dotenv; load_dotenv('.env'); import runpy,sys; sys.argv=['archive_r2.py']; runpy.run_path('pipeline/archive_r2.py', run_name='__main__')"`.

**8. Report** (RUN STATUS + SUMMARY): the full `$SCR/bf-report.json` split as numbers, the Pitfall-4 count accepted_archived (items that aged straight into archive/), the spend, and the exclusions G-NN.
  </action>
  <verify>
    <automated>Each numbered step in the action carries its exact command and its expected output; a mismatch at any step is a FAIL of this task.</automated>
  </verify>
  <done>Report recorded; residual classifier_none == api_error_remaining + not_attempted; gate green; push + deploy + R2 run succeeded</done>
  <resume-signal>Report recorded; residual classifier_none == api_error_remaining + not_attempted; gate green; push + deploy + R2 run succeeded</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| stored third-party text (rejected rows) → LLM | untrusted input, re-sent to the classifier |
| local data mutation → origin/master | concurrent cron commits |
| orchestrator → R2 bucket | via the workflow's secrets |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-15 | Tampering | LLM output → published incidents | mitigate | Same closed-set path (Pydantic → resolve_cut → centroid → build_incident/is_safe_url) as the live pipeline; `--review` + opus check for V-06-class errors before apply |
| T-34-16 | Tampering | data/ race with cron | mitigate | Classify phase never writes data/. Apply only in a verified idle cron window after a pull. Pull --rebase before push; abort + re-apply from cache on conflict; never --force |
| T-34-17 | Repudiation | data loss during rewrite | mitigate | Scratchpad backup + SHA256SUMS before any write; seen.json hash asserted unchanged; idempotent apply |
| T-34-18 | Denial of Service (budget) | classify phase | mitigate | Provider-reported spend meter (OpenRouter /api/v1/key usage delta) with a conservative fallback; hard cap USD 3.00; exit 3 |
| T-34-19 | Information Disclosure | R2 credentials | mitigate | Prefer the r2-archive.yml dispatch (secrets stay in Actions); the local fallback loads .env without printing values |
</threat_model>

<verification>
- Task 1's pytest is green. The dry-run shows frozen_subset=2073 and zero writes. The apply report and residual check agree. seen.json is unchanged. The gate is 16/16. The R2 run succeeded.
</verification>

<success_criteria>
NREC-09 holds:
- The whole outage population (≥ 2,073, including all 2,073 measured rows) is reclassified without refetching.
- Accepted rows are merged with dedup. Rejected rows carry their real stage. seen.json is untouched.
- R2 is regenerated.
- The measured split and spend (≤ $3.00) are reported.
</success_criteria>

<output>
Create `.planning/phases/34-news-classification-restore/34-05-SUMMARY.md` when done.
</output>
