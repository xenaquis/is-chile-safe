---
phase: 34-news-classification-restore
plan: 04
type: execute
wave: 4
depends_on: ["34-01", "34-02", "34-03"]
files_modified:
  - .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
  - .planning/STATE.md
autonomous: false
requirements: [NREC-10, NREC-01, NREC-04]
executor: orchestrator (gate, push, dispatch, log verification)

must_haves:
  truths:
    - "The code from 34-01..34-03 reaches master only after the ship-gate suite is at or above the directive baseline plus the new tests, with exactly one named, expected red (freshness on 2026-09-04 data)."
    - "After every deploy in this plan, prod is curled and verified: the served current.json count and max(date) equal origin/master's, and the /news/ and /es/noticias/ news-date card counts equal the served incident count (directive :76, BF-07)."
    - "The first post-push news-pipeline run classifies: attempted>0, responded>0, classified>0, 0 'HTTP/1.1 404' lines, failovers == 0 (or a diagnosed reason, G-09), and the Health gate step (G-04 a-c + G-09 d-f) concludes success."
    - "A second dispatch with force_backup=true proves the Actions-secret DeepSeek backup live: failover_reason 'forced', responded ≥ 1, api_errors 0, backup_exhausted false (R-09, NREC-04)."
    - "The OpenRouter model chosen by 34-01 (WINNER or G-08 FALLBACK_WINNER) still has ≥2 live endpoints at switch time (G-02). For G-08 DEEPSEEK_DIRECT this check is replaced by the forced-backup/primary DeepSeek evidence."
    - "No gate pytest run leaves changes in data/ (R-13), and a rebase conflict is never resolved with --theirs (R-13)."
  artifacts:
    - path: .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
      provides: "RUN STATUS updated with push SHA, first live run id + measured counts"
  key_links:
    - from: master push
      to: news-pipeline.yml workflow_dispatch run
      via: "gh workflow run news-pipeline.yml --ref master"
      pattern: "classified=[1-9]"
---

<objective>
Ship the hardened classifier, alarm and honest heartbeat to master and prove live classification from the run log. This covers directive Phase-34 step (4) and the first leg of NREC-10.

Purpose: nothing counts until prod classifies. 34-05's backfill needs the hardened path live and the classifier_none population frozen (no new classifier_none rows are written after this push).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md  (Hard safety rules: pull --rebase, never --force; RUN STATUS baseline)
@.planning/phases/34-news-classification-restore/34-01-SUMMARY.md
@.planning/phases/34-news-classification-restore/34-02-SUMMARY.md
@.planning/phases/34-news-classification-restore/34-03-SUMMARY.md
@.planning/phases/34-news-classification-restore/34-AB-RESULTS.json

<interfaces>
- Directive baseline (RUN STATUS, HEAD e565ce8): pytest 395 passed / 1 skipped / 1 xfailed; vitest 86/86 (9 files); validators 16/16; astro check 0 errors / 0 warnings; lint-workflows exit 0; build 834 pages.
- Expected post-34-03 arithmetic: pytest passed ≥ 394 + N_new(34-01..34-03), 1 skipped, 2 xfailed (34-02 Task 3 moves the clustering staleness test to xfail), 0 failed; vitest ≥ 86 + freshness cases, 10 files, 0 failed.
- Expected-red rule (freshness): at this point current.json has no last_new_incident_at, and max(date)=2026-09-04 (measured 2026-09-22, 799 incidents 2026-08-23..2026-09-04). freshness.mjs therefore reports FAIL with evidence 2026-09-05T00:00:00Z. This is the true signal the phase exists to fix. It does not block the push because freshness is not in any deploy path (34-03 objective: DEPLOYMENT.md:62, site/package.json:10-13, deploy-on-code.yml:66, deploy-manual.yml:53, ci.yml:3-5). It MUST turn green after the first classifying run (check at step 7), and 34-06 requires 16/16.
- Log instruments (formats fixed by 34-02): "Fetch summary: fetched=%d, keyword_passed=%d, unseen_candidates=%d" (scrape_news.py:280-283); "Classification summary: classified=%d, rejected=%d"; "Run summary: {json}". httpx logs "HTTP Request: POST … \"HTTP/1.1 <code> …\"".
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: ORCHESTRATOR — ship gate, switch-time endpoint check, pull --rebase, push</name>
  <what-built>34-01..34-03 committed locally (not pushed).</what-built>
  <action>
Run from the repo root in bash. Each command's expected output is given, and anything else STOPS the push.

1. `git status --porcelain` → empty. Commit any .planning edits first.
2. `python -m pytest pipeline/tests/ -q -p no:cacheprovider; echo EXIT=$?` → EXIT=0. The final line is "P passed, 1 skipped, 2 xfailed" with P ≥ 394 + N_new. Count by exit code and the summary line; a `failed` grep also matches `xfailed`. Then `git status --porcelain data/` → empty (R-13: no test wrote into the real data dir).
3. ONE chained command (OneDrive desync rule): `cd site && npm run build && node scripts/validate/all.mjs; echo VALIDATE=$?; npx vitest run; echo VITEST=$?; cd ..`
   - The build succeeds (≈834 pages).
   - The all.mjs summary shows 15 PASS and exactly one FAIL, `freshness`, whose message names `max(date)+1d` = `2026-09-05T00:00:00Z`. So VALIDATE=1 is expected ONLY in that shape. Any other failing validator → STOP.
   - VITEST=0 with ≥ 86 + new tests.
4. `cd site && npx astro check; cd ..` → "0 errors".
5. `bash .github/scripts/lint-workflows.sh; echo L=$?; bash .github/scripts/check-sha-pins.sh; echo P=$?; bash .github/scripts/check-secret-hygiene.sh; echo H=$?` → L=0 P=0 H=0.
6. `grep -rn "granite-4.1-8b" pipeline --include=*.py | wc -l` → 0.
7. Switch-time endpoints (G-02): `W=$(python -c "import json;print(json.load(open('.planning/phases/34-news-classification-restore/34-AB-RESULTS.json'))['decision']['winner']['model'])"); curl -s "https://openrouter.ai/api/v1/models/$W/endpoints" | python -c "import json,sys;print(len(json.load(sys.stdin)['data']['endpoints']))"` → ≥ 2. This applies to WINNER and FALLBACK_WINNER (G-08 also requires ≥ 2 endpoints). If < 2, re-run `eval_classifier.py --decide` (it refetches endpoint counts). If the decision changes, record a new G-NN and revise 34-02 Task 1's model_config before pushing. For DEEPSEEK_DIRECT (G-08) the primary is not on OpenRouter; skip this step, and step 5b of Task 2 plus the first run become the evidence.
8. `git pull --rebase origin master` → success. The cron commits touch data/ only, and no 34-01..34-03 commit touches data/, so conflicts are not expected. On a conflict: `git rebase --abort` and investigate which local commit touched data/ (R-13). If a resolution is ever needed, use `git checkout --ours data/...`: during a rebase, `--ours` is the UPSTREAM side and `--theirs` is the local commit being replayed. Never use `--theirs` on data/. Then re-run step 2.
9. `git push origin master` → accepted. NEVER `--force`. If rejected: repeat step 8, then push.
10. `gh run list --workflow=deploy-on-code.yml --limit 1 --json conclusion,headSha` → the push SHA with conclusion success. site/scripts changed, so it triggers per deploy-on-code.yml:30-32.
11. Served-route check after this deploy (BF-07): `L=$(git show origin/master:data/incidents/current.json | python -c "import json,sys;d=json.load(sys.stdin);print(len(d['incidents']),max(i['date'] for i in d['incidents']))"); for i in $(seq 1 30); do R=$(curl -s https://ischilesafe.com/data/incidents/current.json | python -c "import json,sys;d=json.load(sys.stdin);print(len(d['incidents']),max(i['date'] for i in d['incidents']))"); [ "$R" = "$L" ] && break; sleep 60; done; echo "served=$R master=$L"` → served == master within 30 min (after `git fetch origin`). Then `for u in https://ischilesafe.com/news/ https://ischilesafe.com/es/noticias/; do curl -s "$u" | grep -cE 'class="news-date" datetime="[0-9]{4}-[0-9]{2}-[0-9]{2}"'; done` → both counts equal the served incident count (directive :76, "after every deploy, curl prod and verify").
  </action>
  <verify>
    <automated>Each numbered step in the action carries its exact command and its expected output; a mismatch at any step is a FAIL of this task.</automated>
  </verify>
  <done>Push SHA recorded; every step matched its expected output</done>
  <resume-signal>Push SHA recorded; every step matched its expected output</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ORCHESTRATOR — dispatch one news run and verify live classification from the log</name>
  <what-built>Hardened pipeline on master.</what-built>
  <action>
1. `gh workflow run news-pipeline.yml --ref master`. Wait about 10 s, then `ID=$(gh run list --workflow=news-pipeline.yml --event workflow_dispatch --limit 1 --json databaseId --jq '.[0].databaseId')`, then `gh run watch "$ID" --exit-status; echo RUN=$?` → RUN=0.
2. `gh run view "$ID" --log > "$SCRATCH/news-$ID.log"` (SCRATCH = session scratchpad).
3. `grep -oE 'unseen_candidates=[0-9]+' "$SCRATCH/news-$ID.log"` gives U. If U == 0, there is nothing to prove on this run. Wait for the next scheduled run (`gh run list --workflow=news-pipeline.yml --event schedule --limit 1`) and apply steps 2-6 to it instead.
4. `grep -oE 'Classification summary: classified=[0-9]+' "$SCRATCH/news-$ID.log"` → a value ≥ 1. `grep -o 'Run summary: .*' "$SCRATCH/news-$ID.log"` → attempted ≥ 1, responded ≥ 1, backup_exhausted false. api_errors / attempted < 0.5 by construction if the gate passed.
5. `grep -cE 'HTTP/1\.1 404' "$SCRATCH/news-$ID.log"` → 0. Preflight: `grep -oE '"preflight": "[a-z_]+"' "$SCRATCH/news-$ID.log"` → `"preflight": "ok"` (`"skipped"` for G-08 DEEPSEEK_DIRECT). `grep -oE '"failovers": [0-9]+' "$SCRATCH/news-$ID.log"` → `"failovers": 0` is REQUIRED for this run to count as the live proof (G-09, R-04). failovers > 0 → diagnose from failover_reason before counting; the gate may be green, but the run does not prove the primary.
5b. R-09 / NREC-04 live backup check. BF-01 twin, by 34-01 decision.status:
   - WINNER / FALLBACK_WINNER: the primary is OpenRouter, so the forced-backup run below is the proof for DEEPSEEK_API_KEY.
   - DEEPSEEK_DIRECT (G-08): the primary IS DeepSeek direct, so the first run (steps 1-6) is already the DEEPSEEK_API_KEY proof. The forced-backup run below instead proves the OpenRouter backup (deepseek/deepseek-v4-flash with OPENROUTER_API_KEY). Its expected values are the same.

   Dispatch: `gh workflow run news-pipeline.yml --ref master -f force_backup=true`, then take the new dispatch ID, `gh run watch "$ID2" --exit-status` (RUN=0), and `gh run view "$ID2" --log > "$SCRATCH/news-$ID2.log"`.
   - `grep -o 'Run summary: .*' "$SCRATCH/news-$ID2.log"` → `"failover_reason": "forced"`, responded ≥ 1, `"api_errors": 0`, `"backup_exhausted": false`. If U == 0 on this run, repeat the dispatch after the next scheduled run.
   - In the WINNER/FALLBACK_WINNER branch this is the first proof that the Actions secret DEEPSEEK_API_KEY (set 2026-06-19) is funded and reachable from Actions. In the DEEPSEEK_DIRECT branch it is the proof for OPENROUTER_API_KEY.
   - A failure here (401/402/timeouts) is directive pause point 3 only if the primary is also failing. Otherwise record it as a blocker finding and continue with the primary. The backup is then known-dead, so the owner question goes to RUN STATUS.
6. `gh run view "$ID" --json jobs --jq '.jobs[].steps[] | select(.name=="Health gate (NREC-07 / G-04)") | .conclusion'` → `success`. The gate now evaluates G-04 (a)(b)(c) and G-09 (d) accepted==0 at attempted≥10, (e) parse_errors/attempted ≥ 0.20, and (f) expired > 0. Also check `"budget_exhausted": false`; true is a warning to record, not a fail.
6b. Served-route check after the deploy triggered by the first classifying run's data commit (BF-07): the run's "Trigger Cloudflare Pages deploy" step concluded success, then `L=$(git show origin/master:data/incidents/current.json | python -c "import json,sys;d=json.load(sys.stdin);print(len(d['incidents']),max(i['date'] for i in d['incidents']))"); for i in $(seq 1 30); do R=$(curl -s https://ischilesafe.com/data/incidents/current.json | python -c "import json,sys;d=json.load(sys.stdin);print(len(d['incidents']),max(i['date'] for i in d['incidents']))"); [ "$R" = "$L" ] && break; sleep 60; done; echo "served=$R master=$L"` → served == master within 30 min (after `git fetch origin`). Then `for u in https://ischilesafe.com/news/ https://ischilesafe.com/es/noticias/; do curl -s "$u" | grep -cE 'class="news-date" datetime="[0-9]{4}-[0-9]{2}-[0-9]{2}"'; done` → both counts equal the served incident count (directive :76, "after every deploy, curl prod and verify"). The served max(date) must now be ≥ (UTC today − 1 day).
7. `git pull --rebase origin master && node site/scripts/validate/freshness.mjs; echo F=$?` → F=0: "PASS freshness" citing last_new_incident_at from this run. If the run added no new in-window id (every accepted item was a duplicate), wait for the next scheduled run and re-check.
8. `gh workflow run ci.yml --ref master`, then `gh run watch <ci id> --exit-status` → all 3 jobs (frontend, pipeline, lint-workflows including zizmor 1.10.0) succeed. If only `frontend` fails and its log shows only the freshness FAIL, go back to step 7. Any zizmor finding → fix-forward commit (gate steps 2-9 again).
9. Record in RUN STATUS: push SHA, run ID, U/attempted/responded/classified, 404 count, preflight, failovers, and a note that live cron spend is production operations, not run-cap spend. Estimated ≈ 110 items/day × $0.0004-0.0009 ≈ $0.05-0.10/day at the 34-01 measured tokens. Update STATE.md.

**Failure handling:**
- classified == 0 with U > 0, 404 > 0, or the gate red: do NOT start 34-05. Diagnose from the Run summary. If `failover_reason == preflight_zero_endpoints`, the winner was delisted: rerun the 34-01 decide step and revise.
- Fast path (G-12, BF-06): only if prod is still not classifying on 2026-09-30, or on pause point 1. Then execute the Fast-path section of 34-02-PLAN.md through /gsd:quick, built from a branch on origin/master. A G-08 A/B outcome is never a fast-path trigger.
- Pause point 6 applies on 2026-10-03.
  </action>
  <verify>
    <automated>Each numbered step in the action carries its exact command and its expected output; a mismatch at any step is a FAIL of this task.</automated>
  </verify>
  <done>classified ≥ 1, 0 HTTP 404 lines, failovers == 0, Health gate success, forced-backup run responded ≥ 1 with api_errors 0, freshness PASS locally → proceed to 34-05</done>
  <resume-signal>classified ≥ 1, 0 HTTP 404 lines, failovers == 0, Health gate success, forced-backup run OK, freshness PASS locally → proceed to 34-05</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| local → origin/master | shared history; cron commits concurrently |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-13 | Tampering | git history | mitigate | `git pull --rebase` before every push, never `--force`, conflicts in data/ resolved to upstream (the cron's data is authoritative for data/) |
| T-34-14 | Information Disclosure | run logs saved to scratchpad | mitigate | Logs are public Actions logs already; check-secret-hygiene.sh passed before push |
</threat_model>

<verification>
- Every step in Tasks 1-2 matched its expected output, including the served-route checks (Task 1 step 11, Task 2 step 6b). RUN STATUS carries the measured numbers.
</verification>

<success_criteria>
Prod classifies again on the 34-01 decision (WINNER or G-08 fallback) (BF-01). The first live run proves classified > 0, 0 × 404 and a green Health gate. Freshness turns green through a real new incident.
</success_criteria>

<output>
Create `.planning/phases/34-news-classification-restore/34-04-SUMMARY.md` when done.
</output>

## Revision R1 (premortem)

- **R-01:** Task 1 step 7 covers FALLBACK_WINNER / DEEPSEEK_DIRECT (G-08); Task 2 failure handling fast-path wording (superseded by G-12, see R2 BF-06); must_haves truth 3.
- **R-04:** Task 2 step 5 requires failovers == 0 for the run to count (G-09); step 6 names the G-09 predicates; done/resume-signal; must_haves truth 1.
- **R-09:** new Task 2 step 5b dispatches `-f force_backup=true` and requires failover_reason "forced", responded ≥ 1, api_errors 0, backup_exhausted false; must_haves truth 2.
- **R-11:** Task 2 step 6 records budget_exhausted (warning only, G-09).
- **R-13:** Task 1 step 2 adds `git status --porcelain data/` → empty after pytest; step 8 replaces `--theirs` with `git rebase --abort` + investigate / `--ours` (= upstream during a rebase); must_haves truth 4.

## Revision R2 (gate R1)

- **BF-01:** success_criteria ("on the 34-01 decision (WINNER or G-08 fallback)"); Task 2 step 5b branches by decision.status (in DEEPSEEK_DIRECT the first run proves DEEPSEEK_API_KEY and the forced-backup run proves OpenRouter).
- **BF-06 (G-12):** Task 2 Failure handling: "immediately" and the pause-1/2 trigger are removed; the fast path fires only on 09-30 or pause point 1, from an origin/master branch.
- **BF-07:** new Task 1 step 11 and Task 2 step 6b: curl polls of the served current.json (count + max(date) == origin/master) and news-date counts on /news/ and /es/noticias/; must_haves truth 1; verification.
