---
phase: 34-news-classification-restore
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - pipeline/news/model_config.py
  - pipeline/news/classifier.py
  - pipeline/news/clustering.py
  - pipeline/news/pending.py
  - pipeline/scrape_news.py
  - pipeline/tests/test_classifier.py
  - pipeline/tests/test_classifier_router.py
  - pipeline/tests/test_pending_queue.py
  - pipeline/tests/test_scrape_news.py
  - pipeline/tests/test_clustering.py
  - pipeline/tests/fixtures/openrouter_endpoints_granite_4_1_8b.json
  - pipeline/tests/fixtures/openrouter_key_no_credit.json
autonomous: true
requirements: [NREC-02, NREC-03, NREC-04, NREC-05, NREC-06]
executor: opus (pipeline kernel)

must_haves:
  truths:
    - "With no env overrides, the classifier uses the 34-01 decision (WINNER / FALLBACK_WINNER model on OpenRouter, or DeepSeek direct as primary when decision.status == DEEPSEEK_DIRECT, per G-08). NEWS_MODEL / NEWS_PROVIDER / NEWS_BACKUP_MODEL override it without any code edit, and an empty string (unset repo variable) falls back to the default (R-09)."
    - "`grep -rn 'granite-4.1-8b' pipeline --include=*.py` returns 0 lines."
    - "A preflight fed the verbatim granite-4.1-8b 0-endpoint JSON reports zero_endpoints and hands the run to the DeepSeek-direct backup before the first completion call."
    - "An HTTP 404/401 is not retried. It yields API_ERROR: the URL is NOT added to seen.json, NOT written to rejected/ as classifier_none, and IS persisted to data/incidents/pending.json."
    - "A genuine low-confidence answer is still marked seen and recorded in rejected/ (CR-02 kept), with stage low_confidence."
    - "429/5xx/connection errors are retried by tenacity: 3 attempts, 2 s then 4 s waits, and the SDK's own retries are disabled (max_retries=0)."
    - "After 5 consecutive API_ERROR outcomes, the primary is abandoned for the rest of the run in favour of the backup, AND the 5 items of the tripping streak are re-dispatched to the backup in the same run; if the backup answers, their attempts are not incremented (G-09, R-04). After 5 on the backup, the remaining items are queued unattempted and backup_exhausted=true."
    - "Empty content after the one re-call, and finish_reason == 'length', are transient: API_ERROR with error 'empty_content' / 'finish_length', never seen, queued to pending (G-09, R-03)."
    - "NEWS_FORCE_BACKUP=='true' starts the run on the backup (failover_reason 'forced'), so the Actions-secret DeepSeek backup can be exercised on demand (R-09)."
    - "A run time budget NEWS_RUN_BUDGET_S (default 1200 s, G-09) stops classification early. The remaining items are queued without an attempt increment, budget_exhausted=true is set, and the run still commits (R-11)."
    - "ClassifyResult carries served_by, finish_reason and usage. build_router_from_env() builds fresh clients from os.environ at call time (R-10, R-12)."
    - "Every run emits a machine-readable summary (attempted/responded/accepted/genuine_rejects/api_errors/failovers/queued/...) to NEWS_RUN_SUMMARY_PATH and to GITHUB_OUTPUT when those env vars are set."
    - "The log line 'Classification summary: classified=N, rejected=M' is preserved byte-for-byte in format (it is the 34-04/34-06 instrument)."
  artifacts:
    - path: pipeline/news/model_config.py
      provides: "Single source of model ids + measured reasoning-disable extra_body (no side effects on import)"
      contains: "DEFAULT_OPENROUTER_MODEL"
    - path: pipeline/news/pending.py
      provides: "Retry-queue load/save/upsert/expire per G-03"
      contains: "PENDING_MAX_ATTEMPTS = 5"
    - path: pipeline/tests/fixtures/openrouter_endpoints_granite_4_1_8b.json
      provides: "NREC-03 fixture — verbatim body from 34-RESEARCH.md §Endpoints Census"
      contains: "\"endpoints\":[]"
  key_links:
    - from: pipeline/scrape_news.py
      to: pipeline/news/classifier.py
      via: "build_router_from_env() → router.preflight() → router.classify()"
      pattern: "build_router_from_env"
    - from: pipeline/scrape_news.py
      to: data/incidents/pending.json
      via: "pending.load_pending / save_pending"
      pattern: "pending\\.json"
    - from: pipeline/news/clustering.py
      to: pipeline/news/model_config.py
      via: "MODEL = resolve('NEWS_CLUSTERING_MODEL', DEFAULT_OPENROUTER_MODEL)"
      pattern: "model_config"
---

<objective>
Harden the classifier kernel so a provider or model failure is loud and non-destructive (V-01, V-02, V-09, V-26):
- config/env model id defaulting to the A/B winner
- OpenRouter preflight
- DeepSeek-direct failover
- typed outcomes
- tenacity retry, circuit breaker and a persisted retry queue, all per G-03
- the reasoning flag measured in 34-01
- a run summary that the 34-03 health gate consumes

Purpose: this closes the exact outage shape. A delisted model currently produces the same `None` as "not a crime", so the URL is burned into seen.json and the lost item is filed as `classifier_none`.

Output: model_config.py, pending.py, a refactored classifier.py and scrape_news.py, updated and new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md  (G-03 knobs verbatim; G-04 consumes this plan's summary)
@.planning/phases/34-news-classification-restore/34-RESEARCH.md  (§Classifier Code Map, §Endpoints Census fixture JSON, §Pitfall 1)
@.planning/phases/34-news-classification-restore/34-AB-RESULTS.json  (34-01 output — the winner + reasoning variants)
@.planning/phases/34-news-classification-restore/34-01-SUMMARY.md
@pipeline/news/classifier.py
@pipeline/scrape_news.py
@pipeline/news/clustering.py

<interfaces>
Existing code (HEAD e565ce8), read this session:
- scrape_news.py:178-191 is the provider→key check (default openrouter → OPENROUTER_API_KEY). A missing key gives exit 0 and no writes. The test `test_main_no_api_key_returns_0` (test_scrape_news.py:219-236) asserts no *.json is written.
- scrape_news.py:215 imports `classify` inside main(). This is why the tests' patch("pipeline.news.classifier.classify") intercepts it.
- scrape_news.py:299-315 marks the URL seen (CR-02, :304) BEFORE classify. A None result becomes rejection_stage "classifier_none" (:314).
- scrape_news.py:317-340 has the downstream rejects commune_null / resolver_fail / centroid_fail. :359-362 is the log line "Classification summary: classified=%d, rejected=%d". :366 calls record_rejected. :371 runs deduplicate. :379 runs merge_and_write. :383 runs save_seen.
- scrape_news.py:66-123 `record_rejected(items, data_dir)` writes to rejected/<UTC now YYYY-MM>.json and dedups by id, skipping an existing id so that first_seen is preserved.
- scrape_news.py:389-391: any exception → return 1. In the workflow, the commit step (default if: success()) is then skipped.
- clustering.py:56-60: its own OpenRouter client, and `MODEL = "ibm-granite/granite-4.1-8b"`. The module "intentionally does NOT import from pipeline.news.classifier" (:52-53). That import would load index.json and build clients.
- test_clustering.py:991-1006 `test_cached_verdicts_match_current_model_config` asserts `fixture meta["model"] == MODEL`. The fixture pipeline/tests/fixtures/clustering_golden_set.json records "ibm-granite/granite-4.1-8b" as the model that produced the cached verdicts.
- pipeline/shared/atomic_write.py:13 `atomic_write_json(path, data, compact=False)`.
- 34-01 seam: `_request_completion(client, model, provider, user_content, extra_body=None)` (raises) and `_parse_content(raw, title) -> ("ok"|"low_conf"|"parse_error", out|None)`.

Mock surfaces that MUST stay green (enumerated this session):
- test_classifier.py: 7 original tests + the 34-01 seam tests patch `pipeline.news.classifier.client` (:45,:58,:72,:82,:92,:107). Kept green because `classify()` keeps using the module-level `client` read at call time, and a returned mock answer never triggers a retry.
- test_classifier_traffic.py: the 3 tests there (measured: `grep -c 'def test_'` = 3; the client patch sites are :42 and :67) are kept green the same way (confidence 0.0 → classify() returns None).
- test_classifier.py:115-122 `test_default_provider_is_openrouter` asserts the dead literal. It is REWRITTEN (see Task 1); the assertion of a broken value is not preserved.
- test_scrape_news.py: 10 sites patch `pipeline.news.classifier.classify` (:143, :260, :287, :310, :336, :373, :396, :442, :477, :496). After this plan, scrape_news no longer calls `classify`. Every site is retargeted to a fake router (Task 2). Unretargeted, the real router would make live network calls with the "sk-fake" key.
- test_scrape_news.py:219 `test_main_no_api_key_returns_0` keeps asserting zero *.json written. The summary is written only when NEWS_RUN_SUMMARY_PATH is set, and pending.json only when the queue is non-empty or changed.
- test_scrape_news.py:412-484: the courtesy-delay tests assert the exact time.sleep call list. The fake router must never sleep, and the preflight must not run (the fake is patched in at build_router_from_env).
- test_rejected_capture.py: record_rejected is unchanged, so all of these stay green.
- test_clustering.py:991 goes to xfail(strict=True). See Task 3.

Contracts this plan creates (34-03 and 34-05 consume them):

    # pipeline/news/model_config.py — stdlib only, no side effects
    DEFAULT_OPENROUTER_MODEL: str       # = 34-AB-RESULTS.json decision.winner.model
    DEFAULT_BACKUP_MODEL: str = "deepseek-v4-flash"
    REASONING_EXTRA_BODY_OPENROUTER: dict | None   # = decision.winner.reasoning_extra_body
    REASONING_EXTRA_BODY_DEEPSEEK: dict | None     # = backup.reasoning_extra_body (34-01 probe)
    DEFAULT_PROVIDER: str = "openrouter"            # "deepseek" iff decision.status == "DEEPSEEK_DIRECT" (G-08)
    def resolve(env_name: str, default: str) -> str   # os.environ.get(env_name, "").strip() or default  ('' from an unset repo variable → default, R-09)

    # pipeline/news/classifier.py (additions; client/_MODEL/_PROVIDER/classify() kept)
    BREAKER_THRESHOLD = 5; RETRY_MAX_ATTEMPTS = 3; RETRY_WAIT_FIRST_S = 2; RETRY_WAIT_MAX_S = 20
    REQUEST_TIMEOUT_S = 30.0; RUN_BUDGET_DEFAULT_S = 1200   # G-09; env NEWS_RUN_BUDGET_S overrides
    class Outcome(str, Enum): OK="ok"; NOT_CRIME="not_crime"; PARSE_ERROR="parse_error"; API_ERROR="api_error"
    @dataclass(frozen=True) class ClassifyResult:
        outcome: Outcome; output: ClassifierOutput | None; provider: str; model: str
        status_code: int | None = None; error: str | None = None   # error ∈ exception type | "empty_content" | "finish_length"
        served_by: str | None = None      # OpenRouter response `provider` attr if present, else resp.model (R-10)
        finish_reason: str | None = None
        usage: dict | None = None         # {prompt_tokens, completion_tokens, reasoning_tokens, cost?} (R-12)
    @dataclass(frozen=True) class PreflightResult:
        status: Literal["ok","zero_endpoints","model_unknown","no_credit","unknown","skipped"]; endpoints: int | None
    def preflight_openrouter(model: str, *, api_key: str | None = None, http_get=requests.get, timeout: float = 15.0) -> PreflightResult
    @dataclass class ProviderSpec: provider: str; model: str; client_getter: Callable[[], OpenAI]; extra_body: dict | None; key_present: bool
    def classify_outcome(title: str, description: str, spec: ProviderSpec | None = None) -> ClassifyResult
    class ProviderRouter:
        def __init__(self, primary: ProviderSpec, backup: ProviderSpec | None): ...
        def preflight(self) -> PreflightResult
        def __init__(..., clock: Callable[[], float] = time.monotonic, budget_s: float = RUN_BUDGET_DEFAULT_S)
        def classify(self, title: str, description: str, key: str | None = None) -> ClassifyResult | None   # None = not attempted (exhausted or budget)
        def pop_redispatched(self) -> list[tuple[str, ClassifyResult]]   # R-04: backup results for the tripping streak's keys
        failovers: int; failover_reason: str | None   # "preflight_zero_endpoints"|"preflight_model_unknown"|"preflight_no_credit"|"breaker"|"primary_key_absent"|"forced"|"backup_unavailable"
        backup_exhausted: bool; budget_exhausted: bool; active: Literal["primary","backup","none"]
        preflight_status: str
    def build_router_from_env() -> ProviderRouter   # NEW OpenAI clients built from os.environ at CALL time (never the import-time globals, R-12)
    def classify(title, description) -> ClassifierOutput | None   # compat: output iff OK, else None

    # Run summary JSON (NEWS_RUN_SUMMARY_PATH) — schema 1; 34-03 health gate contract
    {"schema": 1, "attempted": int, "responded": int, "accepted": int, "genuine_rejects": int,
     "parse_errors": int, "api_errors": int, "failovers": int, "failover_reason": str|null,
     "backup_exhausted": bool, "queued": int, "expired": int, "not_attempted": int,
     "downstream_rejects": int, "preflight": "ok|zero_endpoints|model_unknown|no_credit|unknown|skipped",
     "primary": "<provider>:<model>", "backup": "<provider>:<model>"|null, "skipped_reason": null|"no_api_key",
     "empty_content": int, "finish_length": int, "redispatched": int, "recovered_by_redispatch": int,
     "budget_exhausted": bool, "served_by_counts": {"<served_by>": int}}      # G-09 / R-03 / R-04 / R-10 / R-11 additions
    Definitions:
    - attempted = items for which router.classify returned a ClassifyResult
    - responded = OK + NOT_CRIME (G-04)
    - accepted = incidents built pre-dedup (== the existing `classified` counter)
    - genuine_rejects = NOT_CRIME
    - downstream_rejects = commune_null + resolver_fail + centroid_fail
    - queued = pending.json entries created or kept by this run
    - not_attempted = items skipped because the router was exhausted OR the run budget ran out
    - api_errors counts final per-item outcomes AFTER re-dispatch (an item recovered by the backup is not an api_error)
    - empty_content / finish_length are subsets of api_errors (G-09)

    # pipeline/news/pending.py — G-03 queue; file data/incidents/pending.json
    PENDING_MAX_ATTEMPTS = 5; PENDING_MAX_AGE_DAYS = 14
    envelope: {"items": [{"id","url","title","description","date","outlet","first_queued","attempts","last_error"}]}  (sorted by first_queued then id; no volatile "generated" field)
    def load_pending(path) -> list[dict]; def save_pending(path, items) -> bool  # writes only if content changed; returns changed
    def upsert_failure(items, cand, now, error, attempted: bool) -> list[dict]
    def split_expired(items, now) -> tuple[list[dict], list[dict]]   # (keep, expired): attempts >= 5 or now-first_queued > 14d
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: model_config + typed outcomes + tenacity retry + preflight + router/breaker in classifier.py (NREC-02..06)</name>
  <files>pipeline/news/model_config.py, pipeline/news/classifier.py, pipeline/tests/test_classifier.py, pipeline/tests/test_classifier_router.py, pipeline/tests/fixtures/openrouter_endpoints_granite_4_1_8b.json</files>
  <read_first>.planning/phases/34-news-classification-restore/34-AB-RESULTS.json, pipeline/news/classifier.py (post-34-01), 34-RESEARCH.md lines 131-136 (fixture JSON)</read_first>
  <behavior>
    - NREC-03: preflight_openrouter("ibm-granite/granite-4.1-8b", http_get=<returns 200 + fixture file bytes>) → status "zero_endpoints", endpoints 0. A fixture with 2 endpoints gives "ok". HTTP 404 on the endpoints URL gives "model_unknown". requests.ConnectionError, a 5xx or invalid JSON gives "unknown".
    - Router with preflight zero_endpoints: failovers==1, failover_reason=="preflight_zero_endpoints". The first classify() call hits ONLY the backup client (the primary create is called 0 times).
    - R-04 authenticated credit preflight: `http_get` for `https://openrouter.ai/api/v1/key` returns the fixture openrouter_key_no_credit.json (data.limit_remaining 0) → status "no_credit" → failover reason "preflight_no_credit". A limit_remaining of null (unlimited) or > 0 → no failover. A failure of this metadata GET → "unknown" (stay on primary). This is metadata, not a completion call.
    - Preflight "unknown": the router stays on the primary and logs a warning.
    - 404 (openai.NotFoundError) and 401 (AuthenticationError): create called exactly once. Outcome API_ERROR with status_code 404/401.
    - 429 (RateLimitError): create called 3 times, the injected _sleep is called with [2, 4], then API_ERROR 429. 500 followed by success: create is called 2 times and the outcome is OK. APIConnectionError is retried like 429.
    - Empty content: one immediate re-call, then API_ERROR with error "empty_content" and status_code None (G-09 / R-03, transient). finish_reason == "length" → API_ERROR "finish_length" (no re-call). Prose-wrapped JSON → OK via the seam's balanced-object recovery. Bad JSON or an invalid family gives PARSE_ERROR. Confidence 0.3 gives NOT_CRIME with the output attached.
    - Breaker: 5 consecutive primary API_ERRORs → failovers==1, reason "breaker", and the 6th item goes to the backup. The sequence 4 errors, 1 OK, 4 errors trips nothing. After a trip, 5 consecutive backup API_ERRORs → backup_exhausted True, and the next classify() returns None with no create call.
    - R-04 re-dispatch: primary 404 ×5 (keys k1..k5) with the backup answering OK. After the 5th classify(), pop_redispatched() returns 5 (key, OK) pairs from the backup, and a second pop returns []. With the backup also failing, the pairs carry API_ERROR. Re-dispatched calls count toward the backup breaker.
    - R-11 budget: with a fake clock advancing 100 s per call and budget_s=1200, classify() returns None from the call that starts after 1200 s and sets budget_exhausted True. No create call happens after that point.
    - R-09: NEWS_FORCE_BACKUP="true" → build_router_from_env() starts on the backup with failovers==1 and failover_reason "forced". NEWS_PROVIDER="" and NEWS_MODEL="" (unset repo variables arrive as empty strings) → the model_config defaults.
    - R-12: after import, set OPENROUTER_API_KEY="k-new" and call build_router_from_env(); the primary client's api_key is "k-new", not the import-time "placeholder". ClassifyResult.usage has prompt_tokens/completion_tokens/reasoning_tokens from the mocked response.usage, plus cost when present.
    - R-10: served_by comes from the mocked response's `provider` attribute when set, otherwise resp.model. finish_reason is copied from choices[0].
    - Primary key absent (OPENROUTER_API_KEY "" or whitespace) → start on the backup, failover_reason "primary_key_absent". Backup key absent when the primary trips → backup_exhausted True, reason "backup_unavailable".
    - create kwargs: the openrouter primary carries extra_body == REASONING_EXTRA_BODY_OPENROUTER and no response_format. The deepseek backup carries response_format json_object and extra_body == REASONING_EXTRA_BODY_DEEPSEEK.
    - Clients are constructed with max_retries=0 and timeout=30.0 (patch openai.OpenAI in a subprocess import test, or inspect client.max_retries / client.timeout).
    - NREC-02 (BF-01: expressed against model_config, not the literal "openrouter"): with NEWS_PROVIDER and NEWS_MODEL unset, _PROVIDER == model_config.DEFAULT_PROVIDER. If DEFAULT_PROVIDER == "openrouter", _MODEL == model_config.DEFAULT_OPENROUTER_MODEL. If DEFAULT_PROVIDER == "deepseek" (G-08 DEEPSEEK_DIRECT), _MODEL == "deepseek-v4-flash" (the deepseek branch default). In both cases "granite-4.1-8b" is not in _MODEL. In a subprocess with NEWS_MODEL="x/y", importing prints _MODEL=="x/y". The rewritten test_default_provider_is_openrouter follows the same branching; rename it to test_default_provider_matches_model_config.
    - classify() compat: returns ClassifierOutput iff OK, otherwise None. All pre-existing test_classifier.py and test_classifier_traffic.py tests stay green.
    - DEEPSEEK_DIRECT (G-08): with model_config.DEFAULT_PROVIDER == "deepseek" (patched in a subprocess), the primary is DeepSeek direct and the backup is OpenRouter `deepseek/deepseek-v4-flash` with REASONING_EXTRA_BODY_OPENROUTER.
  </behavior>
  <action>
**1. `pipeline/news/model_config.py` (stdlib only).** Copy the values VERBATIM from `34-AB-RESULTS.json`. This is how the default gets set without a re-plan. Never STOP here (G-08, R-01):
- If decision.status ∈ {WINNER, FALLBACK_WINNER}:
  - DEFAULT_PROVIDER = "openrouter"
  - DEFAULT_OPENROUTER_MODEL = decision.winner.model
  - REASONING_EXTRA_BODY_OPENROUTER = decision.winner.reasoning_extra_body
  - REASONING_EXTRA_BODY_DEEPSEEK = backup.reasoning_extra_body
  - DEFAULT_BACKUP_MODEL = "deepseek-v4-flash"
- If decision.status == DEEPSEEK_DIRECT:
  - DEFAULT_PROVIDER = "deepseek" (the existing DeepSeek branch, classifier.py:60-65)
  - DEFAULT_OPENROUTER_MODEL = "deepseek/deepseek-v4-flash" (the directive's pre-authorized fast-path model). It serves as the backup when the primary is DeepSeek direct, and as clustering's default.
  - REASONING_EXTRA_BODY_OPENROUTER = the v4-flash probe variant from 34-01's `ab/probe-deepseek-deepseek-v4-flash.json`. Per the 34-01 slug rule (D-R2-08) that is the OpenRouter probe; the DeepSeek-direct probe is `probe-direct-deepseek-v4-flash.json` and must NOT be used here.
  - REASONING_EXTRA_BODY_DEEPSEEK = backup.reasoning_extra_body
- If decision.status == INCOMPLETE: 34-01 is not finished. Go back to 34-01 Task 3; do not proceed.

Put a comment citing the G-NN recorded at 34-01 Task 3 and, when it applies, G-08. Do not pick a model yourself.

**2. `classifier.py`:**
- `_PROVIDER = model_config.resolve("NEWS_PROVIDER", model_config.DEFAULT_PROVIDER).lower()`, replacing `:58` (an empty string from an unset repo variable → default, R-09).
- openrouter branch: `_MODEL = model_config.resolve("NEWS_MODEL", DEFAULT_OPENROUTER_MODEL)`
- deepseek branch: `resolve("NEWS_MODEL", "deepseek-v4-flash")`
- minimax branch: unchanged
- Every OpenAI(...) gets max_retries=0 and timeout=REQUEST_TIMEOUT_S (G-03: tenacity is the only retry layer; the SDK default max_retries=2 would silently turn 3 attempts into 9).
- Add a backup client: DeepSeek direct, `base_url https://api.deepseek.com`, key `DEEPSEEK_API_KEY`, model `resolve("NEWS_BACKUP_MODEL", DEFAULT_BACKUP_MODEL)`.
- Replace the docstring model name (`:6-10`) and the comment at `:73` so no literal `granite-4.1-8b` remains. Keep the historical note as "the previous Granite 4.1 default (delisted 2026-09-04)" without the id string.

**3. Retry and outcome mapping.**
- `_retrying_create(spec, user_content)` wraps `_request_completion` in `tenacity.Retrying` with:
  - `retry=retry_if_exception(_is_retryable)`
  - `stop=stop_after_attempt(RETRY_MAX_ATTEMPTS)`
  - `wait=wait_exponential(multiplier=2, exp_base=2, max=RETRY_WAIT_MAX_S)` (gives 2 s then 4 s)
  - `sleep=lambda s: _sleep(s)`, where `_sleep = time.sleep` is a patchable module attribute
  - `reraise=True`
- `_is_retryable` is True for RateLimitError, InternalServerError, APIConnectionError (includes APITimeoutError), and any APIStatusError with status_code == 429 or >= 500. It is False for everything else: 400/401/403/404/409/422.
- `classify_outcome(title, description, spec=None)`:
  - spec None means the primary spec with `client_getter=lambda: client`, which reads the module global at call time and keeps the 8 existing patches working.
  - Calls `_retrying_create`. Any openai exception → `ClassifyResult(API_ERROR, status_code=getattr(exc,"status_code",None), error=type(exc).__name__)`. Any other exception → API_ERROR with error=type name; it is not retried, and G-04 will surface it.
  - Empty content → exactly one immediate re-call (the existing Pitfall-4 semantics, not a tenacity attempt), then API_ERROR error="empty_content", status_code None (G-09: transient → pending, never seen).
  - `choices[0].finish_reason == "length"` → API_ERROR error="finish_length" (G-09), no re-call.
  - Otherwise, `_parse_content` (which includes the 34-01 balanced-object recovery): ok→OK, low_conf→NOT_CRIME, parse_error→PARSE_ERROR.
  - Fill served_by (`getattr(resp, "provider", None) or resp.model`), finish_reason and usage (prompt_tokens, completion_tokens, completion_tokens_details.reasoning_tokens or 0, and `cost` if present) on every result that got a response (R-10, R-12).
  - Log status class and exception type, never keys.
- `classify()` = `classify_outcome(...)`: output if OK, else None.

**4. `preflight_openrouter`:** `http_get(f"https://openrouter.ai/api/v1/models/{model}/endpoints", timeout=timeout)`.
- 200 with `len(data["endpoints"]) == 0` → "zero_endpoints"
- 200 with at least one endpoint → "ok"
- 404 → "model_unknown"
- anything else, or an exception → "unknown"
- Then, when api_key is given (R-04, optional in the premortem, applied): GET `https://openrouter.ai/api/v1/key` with `Authorization: Bearer <key>` (timeout 15). If `data.limit_remaining` is a number ≤ 0 → "no_credit". If it is null, > 0 or the call fails → no change. Log the status only, never the key or the response body.

The preflight is NEVER run inside classify() or classify_outcome(), so the tests do no network I/O.

**5. `ProviderRouter` (per-run state):**
- `preflight()` runs only when the primary provider is openrouter; otherwise status "skipped". zero_endpoints, model_unknown or no_credit → failover (reason "preflight_<status>").
- R-09: env NEWS_FORCE_BACKUP == "true" (case-insensitive) → start on the backup, failovers=1, reason "forced" (the preflight is skipped).
- Consecutive-API_ERROR counters, one per provider, reset by any non-API_ERROR outcome.
- The primary counter reaching BREAKER_THRESHOLD → failover (reason "breaker"), or backup_exhausted ("backup_unavailable") when the backup key is absent.
- R-04 / G-09 re-dispatch: the router remembers (key, title, description) for each item in the current primary API_ERROR streak. On the trip it immediately re-classifies those items on the backup, in the same run, and stores the results for `pop_redispatched()`. Re-dispatched calls count toward the backup breaker. If the backup exhausts mid re-dispatch, the remaining streak items are returned as their original API_ERROR.
- R-11 / G-09 budget: `budget_s = float(os.environ.get("NEWS_RUN_BUDGET_S") or 1200)`, measured from router construction with the injectable `clock`. `classify()` returns None once `clock() − start ≥ budget_s` and sets budget_exhausted.
- The backup counter reaching the threshold → backup_exhausted, active "none".
- `classify()` returns None when active == "none".
- `build_router_from_env()` builds both specs from env. It constructs NEW OpenAI clients reading os.environ at call time (R-12); only the compat path classify()/classify_outcome(spec=None) uses the import-time module `client`. key_present means a non-empty stripped key; GH Actions injects unset secrets as "".
- When DEFAULT_PROVIDER (or NEWS_PROVIDER) is "deepseek", the primary is DeepSeek direct and the backup is OpenRouter DEFAULT_OPENROUTER_MODEL (G-08 DEEPSEEK_DIRECT case).

**6. Tests.**
- Fixture file `openrouter_endpoints_granite_4_1_8b.json`: copy the verbatim JSON from 34-RESEARCH.md line 135. This is the only permitted file containing the dead id besides clustering_golden_set.json; see Task 3.
- Rewrite `test_default_provider_is_openrouter` per `<behavior>`, asserting against the model_config constant.
- Put the router, retry and breaker tests in the new `test_classifier_router.py`. Build fake OpenAI clients whose `chat.completions.create` has `side_effect` lists, and build openai exceptions with `httpx.Response(code, request=httpx.Request("POST","https://x"))`.
- Add an autouse fixture that patches `classifier._sleep` to a recorder and `requests.get` to raise.
- Fixture `openrouter_key_no_credit.json`: `{"data": {"label": "test", "usage": 1.0, "limit": 1.0, "limit_remaining": 0}}` (synthetic, R-04).
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_classifier.py pipeline/tests/test_classifier_router.py pipeline/tests/test_classifier_traffic.py -q -p no:cacheprovider → exit 0; python -c "import json,pipeline.news.model_config as m;P='.planning/phases/34-news-classification-restore/';d=json.load(open(P+'34-AB-RESULTS.json'));s=d['decision']['status'];w=d['decision']['winner'];
assert s in ('WINNER','FALLBACK_WINNER','DEEPSEEK_DIRECT'),s
if s in ('WINNER','FALLBACK_WINNER'): assert m.DEFAULT_PROVIDER=='openrouter';assert m.DEFAULT_OPENROUTER_MODEL==w['model'];assert m.REASONING_EXTRA_BODY_OPENROUTER==w['reasoning_extra_body']
else: pr=json.load(open(P+'ab/probe-deepseek-deepseek-v4-flash.json'));assert m.DEFAULT_PROVIDER=='deepseek';assert m.DEFAULT_OPENROUTER_MODEL=='deepseek/deepseek-v4-flash';assert m.REASONING_EXTRA_BODY_DEEPSEEK==w['reasoning_extra_body'];assert m.REASONING_EXTRA_BODY_OPENROUTER==pr['chosen_extra_body']
print('OK',s)" → OK <status> (BF-01: branches by decision.status; the probe JSON key for the chosen variant's extra_body is `chosen_extra_body`, as written by 34-01's probe mode); git status --porcelain data/ → empty (NB-14)</automated>
  </verify>
  <done>Every `<behavior>` case passes. The model_config constants equal the A/B JSON. No module-import network I/O occurs (the tests run with requests.get patched to raise).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scrape_news typed-outcome loop + pending queue + run summary (NREC-04/05, G-03 queue, G-04 input)</name>
  <files>pipeline/news/pending.py, pipeline/scrape_news.py, pipeline/tests/test_pending_queue.py, pipeline/tests/test_scrape_news.py</files>
  <read_first>pipeline/scrape_news.py (whole), pipeline/tests/test_scrape_news.py (whole), pipeline/news/classifier.py (post Task 1)</read_first>
  <behavior>
    - 404 on every item (fake router returns API_ERROR 404): main() returns 0. seen.json lacks those URLs. rejected/*.json has NO row for them (in particular no classifier_none). pending.json lists them with attempts==1. The summary has api_errors==N and responded==0.
    - NOT_CRIME: main() returns 0. URL present in seen.json (CR-02 kept). rejected row stage "low_confidence". Not in pending.
    - PARSE_ERROR: seen marked, rejected stage "parse_error".
    - OK + resolve None: seen marked, stage "commune_null"/"resolver_fail" as today. OK + centroid None gives "centroid_fail".
    - Run 1 is API_ERROR (item queued). In run 2 the same item is not in the feed any more and the fake router answers OK: the incident lands in current.json, the item leaves pending.json, and its URL enters seen.json.
    - An item already pending that ALSO appears in the feed is classified once per run (no duplicate attempt).
    - G-09 / R-03: an API_ERROR "empty_content" or "finish_length" item is NOT in seen.json, IS in pending.json, and has no rejected row. The summary counts empty_content / finish_length.
    - G-09 / R-04: primary 404 ×5 with the backup OK (FakeRouter emitting pop_redispatched) → all 5 end as incidents or genuine rejects, pending.json is empty, attempts are never incremented, and the summary has redispatched=5, recovered_by_redispatch=5, api_errors=0, failovers=1.
    - G-09 / R-11: a real ProviderRouter with a fake clock and intermittent 30 s timeouts on 30 candidates stops at budget_s=1200. main() returns 0, writes current.json/seen.json/pending.json, the remaining items are in pending with attempts unchanged, and the summary has budget_exhausted=true.
    - Expiry: a pending item at attempts 4 that fails again (attempts→5) is removed from pending, recorded in rejected with stage "api_error_expired" and marked seen. A pending item whose first_queued is older than 14 days is expired the same way BEFORE classification (not attempted).
    - Router exhausted (classify returns None): the items are queued with attempts unchanged (new items attempts 0), not_attempted counts them, and backup_exhausted is true in the summary.
    - Pending items are classified before fresh candidates, and the combined list is capped at MAX_CLASSIFICATIONS_PER_RUN.
    - With NEWS_RUN_SUMMARY_PATH set, the summary JSON matches schema 1 with exact counts for a mixed run (1 OK accepted, 1 NOT_CRIME, 1 PARSE_ERROR, 1 API_ERROR, 1 resolver_fail). With GITHUB_OUTPUT set, the file gains `attempted=5`, `responded=3`, `api_errors=1` lines (one key=value per summary scalar).
    - caplog contains "Classification summary: classified=1, rejected=" (format preserved).
    - pending.json is NOT rewritten when unchanged (mtime/bytes identical across a no-op run). This keeps Phase-35 FRESH-04 byte-identity reachable.
    - test_main_no_api_key_returns_0 (both keys absent): still exit 0, zero *.json written.
  </behavior>
  <action>
**1. `pipeline/news/pending.py`** implements the pending.py contract in `<interfaces>` with the G-03 knobs (5 attempts, 14 days). Write through `atomic_write_json` with deterministic ordering, and only when the serialized content differs from what is on disk.

**2. `scrape_news.main()`:**

*Key check (replaces `:178-191`):*
- Build `router = build_router_from_env()`.
- If neither the primary nor the backup key is present → log the existing warning (naming the env var names), write the summary with skipped_reason "no_api_key" only if NEWS_RUN_SUMMARY_PATH is set, and return 0.
- Otherwise call `router.preflight()` once before classification.

*Pending and candidates:*
- Load pending from `data_dir / "pending.json"`.
- Run `split_expired(now)`. Expired items go to rejected_items with stage "api_error_expired" and are marked seen (`seen[url] = date`) so the feed cannot re-queue them forever.
- Candidates = kept pending items first, in first_queued order, then fresh unseen feed candidates whose URL is not already pending. Cap at MAX_CLASSIFICATIONS_PER_RUN, keeping the existing cap log.

*Classification loop* (replaces `:299-315`). Call `res = router.classify(title, description, key=item_id)`. After EVERY call, drain `router.pop_redispatched()` and re-apply each (key, result) to its earlier item (R-04 / G-09):
- If the backup answered, reverse that item's pending upsert (attempts back to the previous value; a brand-new entry is removed) and handle it like any non-API_ERROR outcome below. Count recovered_by_redispatch; api_errors is decremented.
- If the backup failed too, the single attempt increment stays.

Then, for the current result:
- `res is None` (router exhausted or run budget spent) → `upsert_failure(attempted=False)`; not_attempted += 1; continue.
- API_ERROR (including G-09 "empty_content" / "finish_length") → do NOT touch seen; `upsert_failure(attempted=True, error=f"{res.error}:{res.status_code}")`. Defer the ≥ 5 expiry check to after the loop, so a re-dispatch recovery can still reverse it. If the item now has attempts ≥ 5, move it to expired, following the expiry path above. api_errors += 1.
- Otherwise: remove the item from pending if present, then mark it seen (CR-02 moved to right after a provider answered — same guarantee for every reject path).
  - NOT_CRIME → stage "low_confidence"
  - PARSE_ERROR → stage "parse_error"
  - OK → the existing resolve/centroid/build path (`:317-356`), unchanged.
- Update the CR-02 comment accordingly.

*After the loop:*
- Apply the deferred expiry: items at attempts ≥ 5 → api_error_expired + seen.
- Keep the log line format at `:359-362`.
- Summary additions (G-09): empty_content, finish_length, redispatched, recovered_by_redispatch, budget_exhausted, served_by_counts (accumulated from ClassifyResult.served_by).
- `save_pending`.
- Log `"Run summary: %s"` with `json.dumps(summary, sort_keys=True)` (default separators, so `"preflight": "ok"` is greppable; 34-04/34-06 instrument).
- Write the summary to NEWS_RUN_SUMMARY_PATH (mkdir parents) and append key=value lines to GITHUB_OUTPUT (scalars only; bool as true/false; null as empty), each only if the env var is set.
- A failure writing the summary must NOT be swallowed: let it raise, so main() returns 1 and the workflow fails loudly. Missing evidence must never look healthy.

The genuine-reject stage "classifier_none" is no longer written by scrape_news (grep instrument in verify).

**3. `test_scrape_news.py`:**
- Add the helper `_patch_router(results)`. It patches `pipeline.news.classifier.build_router_from_env` to return a FakeRouter:
  - preflight() → PreflightResult("skipped", None)
  - classify() pops from a list, or maps a ClassifierOutput to OK and None to NOT_CRIME
  - failovers 0, backup_exhausted False
- Replace all 10 `patch("pipeline.news.classifier.classify", ...)` sites (:143, :260, :287, :310, :336, :373, :396, :442, :477, :496) with `_patch_router(...)`, keeping each test's intent.
- Rename `test_classifier_none_items_skipped` → `test_genuine_rejections_skipped_and_marked_seen` and add the seen.json + stage "low_confidence" assertions.
- Add an autouse fixture that patches `pipeline.news.classifier.preflight_openrouter` and `pipeline.news.classifier._request_completion` to raise AssertionError("network call in test"), so a missed retarget fails loudly instead of calling the network.
- Add the `<behavior>` tests, some here and the pure queue ones in test_pending_queue.py.
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/test_scrape_news.py pipeline/tests/test_pending_queue.py pipeline/tests/test_rejected_capture.py -q -p no:cacheprovider → exit 0; grep -n '"classifier_none"' pipeline/scrape_news.py | grep -v '^\s*#' | wc -l → 0; grep -c 'Classification summary: classified=%d, rejected=%d' pipeline/scrape_news.py → 1; git status --porcelain data/ → empty (NB-14: the main() tests set NEWS_DATA_DIR per test; the conftest autouse fixture arrives in 34-03)</automated>
  </verify>
  <done>Typed-outcome loop, retry queue and run summary are in place, with CR-02 kept for genuine answers. All 10 retargeted scrape tests plus the new V-02 tests pass, and no test reaches the network.</done>
</task>

<task type="auto">
  <name>Task 3: clustering model off the dead literal (V-26) + repo-wide literal purge + full suite</name>
  <files>pipeline/news/clustering.py, pipeline/tests/test_clustering.py</files>
  <read_first>pipeline/news/clustering.py:40-62 and :205-220, pipeline/tests/test_clustering.py:985-1010</read_first>
  <action>
**clustering.py:**
- `:60` becomes `MODEL = model_config.resolve("NEWS_CLUSTERING_MODEL", model_config.DEFAULT_OPENROUTER_MODEL)`. Import model_config (side-effect-free), NOT classifier; this preserves the `:52-53` design note.
- Add `max_retries=0, timeout=30.0` to its client.
- Do NOT run, enable or evaluate clustering: it is NO-GO, locked in v2.1.

**test_clustering.py:991:** mark `test_cached_verdicts_match_current_model_config` `@pytest.mark.xfail(strict=True, reason="NREC-02: clustering MODEL moved off delisted ibm-granite/granite-4.1-8b; cached golden-set verdicts (fixture meta.model) are stale by design; clustering is NO-GO (v2.1 lock) — rebuild verdicts only if clustering is ever reopened")`. The id inside the reason string is in a .py file, so write it as "granite 4.1 8B" to keep the grep instrument clean.

This follows the repo's existing quarantine pattern (the GO gate is already xfail strict, test_clustering.py:~980). It is the honest outcome, because the staleness guard was built to be loud exactly on a model swap. Do NOT edit the fixture's meta.model: it is provenance of how the cached verdicts were produced.

**Close gate for this plan:** run the full pytest suite. Expected arithmetic against the directive baseline (395 passed / 1 skipped / 1 xfailed):
- passed = 395 − 1 (the staleness test moves to xfail) + N_new (all new tests from 34-01 and 34-02)
- skipped = 1
- xfailed = 2
- failed = 0
  </action>
  <verify>
    <automated>grep -rn "granite-4.1-8b" pipeline --include=*.py | wc -l → 0; git grep -l "granite-4.1-8b" -- pipeline | sort → exactly pipeline/tests/fixtures/clustering_golden_set.json and pipeline/tests/fixtures/openrouter_endpoints_granite_4_1_8b.json (NB-08: git grep only sees tracked files, so stale __pycache__/*.pyc cannot pollute the count; stage new files first with `git add -N pipeline` so they are included); python -m pytest pipeline/tests/ -q -p no:cacheprovider; echo EXIT=$? → EXIT=0 and summary "P passed, 1 skipped, 2 xfailed" with P ≥ 394 + N_new (N_new = count of test functions added in 34-01+34-02, reported in SUMMARY)</automated>
  </verify>
  <done>No `.py` file under `pipeline/` contains the dead id. The two allow-listed fixtures are the only hits. The full suite is green at the stated arithmetic.</done>
</task>

</tasks>

## Fast-path fallback (documentation only — NOT the main path; directive "Fast-path rule")

Trigger (G-12, BF-06): ONLY the directive's own triggers. Either prod is still not classifying on 2026-09-30, or pause point 1 is reached (NO-GO after 2 gate cycles) while prod is not classifying; in that case the fast path ships first, then the run pauses (G-08/G-12). A G-08 A/B outcome (FALLBACK_WINNER / DEEPSEEK_DIRECT) is NOT a trigger; it flows through the normal 34-02 → 34-04 path.

Build it from a branch based on origin/master, so unpushed and ungated local commits are not dragged to prod (G-12):
- `git fetch origin && git switch -c fastpath-3402 origin/master`
- apply the change and run pytest
- `git push origin fastpath-3402:master`, a fast-forward only, never --force; on rejection, `git fetch` and rebase the branch
- Explicit sequence (D-R2-05):
  1. `git fetch origin`
  2. `git switch -c fastpath-3402 origin/master`
  3. edit, then run pytest
  4. `git commit`
  5. `git push origin fastpath-3402:master` (fast-forward only, never --force)
  6. `git switch master`
- Do NOT `git pull --rebase` local master right away. The local 34-01/34-02 commits rewrite classifier.py:58-79/_call_api and test_classifier.py:115-122, so they clash with the literal hotfix. Defer the pull to the 34-04 Task 1 step 8 ship gate.
- If that rebase conflicts in classifier.py or test_classifier.py, keep the LOCAL side (during a rebase that is `--theirs`, the replayed local commit), because the 34-02 code supersedes the hotfix. Then verify that `model_config.DEFAULT_PROVIDER` / `DEFAULT_OPENROUTER_MODEL` equal the fast-path model now live, and re-run the full 34-04 gate before pushing.

Pick the variant from what origin/master contains (G-12):
- If 34-02 has landed on origin/master, the fast path is config-only: change `model_config.DEFAULT_PROVIDER` / `DEFAULT_OPENROUTER_MODEL` and the reasoning constants. NEWS_PROVIDER / NEWS_MODEL resolve through model_config.
- Otherwise use the directive's literal minimal hotfix, steps 1-3 below. Instrument: `gh run list --workflow=news-pipeline.yml --limit 3 --json databaseId` shows 3 runs, and for each, `gh run view <id> --log | grep -oE 'classified=[0-9]+'` gives 0 while `unseen_candidates>0`. When that holds, the orchestrator runs `/gsd:quick` with a single opus validation, records the next G-NN, and ships only this:
1. `pipeline/news/classifier.py:79` becomes `_MODEL = os.environ.get("NEWS_MODEL", "").strip() or "<34-01 winner, or deepseek/deepseek-v4-flash if 34-01 has no result>"`, and `_call_api`'s kwargs get the measured reasoning extra_body (or `{"reasoning": {"enabled": False}}` if 34-01 never ran), passed as `extra_body=`. The model id is the 34-01 decision when one exists: WINNER or FALLBACK_WINNER. If 34-01 ended DEEPSEEK_DIRECT, do NOT use `deepseek/deepseek-v4-flash` on OpenRouter, because it already failed the G-08 tier. Step 1 becomes the NEWS_PROVIDER default flip to "deepseek" of step 2 (NB-05). If 34-01 has no result at all, use `deepseek/deepseek-v4-flash`, the directive text.
2. If the OpenRouter model also fails in a dispatch run, flip the default of NEWS_PROVIDER at `classifier.py:58` and `scrape_news.py:178` to "deepseek". This is the existing DeepSeek branch at `classifier.py:60-65`; DEEPSEEK_API_KEY already reaches the scrape step at news-pipeline.yml:67, and the "Guard required secrets" step (:52-55) needs no change.
3. Update test_classifier.py:115-122 to the new default. Run pytest, pull --rebase, push, dispatch news-pipeline, and verify `classified>0`.

Then continue 34-02..34-06 normally. The fast path does NOT replace typed outcomes, the queue or the alarm.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| pipeline → OpenRouter/DeepSeek | keys sent; responses untrusted |
| RSS item text → LLM prompt | untrusted third-party text (prompt injection) |
| pending.json (repo, served publicly via sync-data like rejected/) | third-party snippets at rest |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-04 | Tampering | LLM output → incidents | mitigate | Unchanged closed-set path: Pydantic ClassifierOutput → resolve_cut → get_centroid → build_incident (is_safe_url) |
| T-34-05 | Information Disclosure | retry/failover logging | mitigate | Log exception type + status code + env var NAMES only; check-secret-hygiene.sh at 34-04 |
| T-34-06 | Denial of Service | outage → unbounded retries/cost | mitigate | tenacity max 3 per item, breaker 5 consecutive per provider, SDK max_retries=0, timeout 30 s, queue caps 5 attempts / 14 days (G-03) |
| T-34-07 | Repudiation | silent loss of items | mitigate | API_ERROR never marks seen / never classifier_none; the queue plus the run summary make every item accounted for |
| T-34-08a | Denial of Service | 'poison' items with a per-item 400 (provider moderation) at the head of pending | accept | R-16 ACEPTAR: loud, not silent (G-04(c) issue); bounded by 5 attempts / 14 days (G-03); R-04 re-dispatch reduces it. Excluding 4xx from the breaker would hide a systemic 400. Re-evaluate if the A/B backup row or the first runs show per-item 400s |
| T-34-08 | Information Disclosure | pending.json served on prod | accept | Same class and content as the already-served rejected/ (V-17); ledger de-serving is Phase 38 — add pending.json to that list |
</threat_model>

<verification>
- The three verify blocks above pass.
- `python -m pytest pipeline/tests/ -q -p no:cacheprovider` exits 0 with the Task 3 arithmetic.
- `git diff --stat` touches only files_modified.
</verification>

<success_criteria>
- NREC-02: the model comes from config/env, and there is no dead literal in pipeline/*.py.
- NREC-03: the preflight is tested against the verbatim 0-endpoint fixture.
- NREC-04: DeepSeek-direct failover happens on preflight failure or a breaker trip, and is counted.
- NREC-05: typed outcomes plus the persisted queue, with CR-02 kept.
- NREC-06: retries happen only on 429/5xx/connection errors, and the breaker trips at 5, with same-run re-dispatch of the tripping streak (G-09).
</success_criteria>

<output>
Create `.planning/phases/34-news-classification-restore/34-02-SUMMARY.md` when done. Include N_new, the final pytest line, and the model_config values.
</output>

## Revision R1 (premortem)

- **R-01 (P0):** Task 1 §1 no longer STOPs. It covers WINNER / FALLBACK_WINNER / DEEPSEEK_DIRECT (DEFAULT_PROVIDER) per G-08, with a DEEPSEEK_DIRECT behavior test and must_haves truth 1. (The R1 fast-path trigger change was superseded by G-12 — see R2 BF-06.)
- **R-03:** Task 1 behavior and §3 (empty → API_ERROR "empty_content", finish_length → API_ERROR, balanced-object JSON recovery via the 34-01 seam twin); interfaces summary schema (empty_content, finish_length); Task 2 behavior (pending, not seen) and loop action; must_haves truth.
- **R-04:** Task 1 §4 authenticated /api/v1/key "no_credit" preflight plus fixture (the premortem's optional item, applied), §5 same-run re-dispatch with pop_redispatched; Task 2 loop reversal of attempts, deferred expiry, and behavior test (404×5 + backup OK → pending empty); must_haves truth; success_criteria NREC-06.
- **R-09:** model_config DEFAULT_PROVIDER + `_PROVIDER` via resolve() ('' → default); NEWS_FORCE_BACKUP "forced" in §5 and behavior tests. The workflow wiring is in 34-03 (twin).
- **R-10:** ClassifyResult served_by / finish_reason in interfaces, behavior and §3; summary served_by_counts.
- **R-11:** RUN_BUDGET_DEFAULT_S=1200 / NEWS_RUN_BUDGET_S with an injectable clock (§5); Task 2 budget test; summary budget_exhausted.
- **R-12:** ClassifyResult.usage; build_router_from_env builds fresh clients at call time; behavior test for a post-import key change.
- **R-16 (ACEPTAR):** one-line note in the threat register (T-34-08a).

## Revision R2 (gate R1)

- **BF-01:** Task 1 `<behavior>` NREC-02 bullet is expressed against model_config.DEFAULT_PROVIDER, with both branches and the renamed test. Task 1 `<verify>` branches by decision.status: WINNER/FALLBACK_WINNER asserts DEFAULT_PROVIDER == 'openrouter' plus winner model and extra_body; DEEPSEEK_DIRECT asserts DEFAULT_PROVIDER == 'deepseek', DEFAULT_OPENROUTER_MODEL == 'deepseek/deepseek-v4-flash', REASONING_EXTRA_BODY_DEEPSEEK == winner extra_body, and REASONING_EXTRA_BODY_OPENROUTER == the probe's chosen_extra_body.
- **BF-06 (G-12):** § Fast-path trigger rewritten: "IMMEDIATELY" removed; it fires only on 09-30 or pause point 1; it is built from branch fastpath-3402 on origin/master and pushed fast-forward; it uses the model_config variant if 34-02 has landed, else the literal hotfix.
- **NB-05:** interfaces: "the 3" test_classifier_traffic tests; Fast-path step 1 uses the NEWS_PROVIDER=deepseek flip when 34-01 ended DEEPSEEK_DIRECT.
- **NB-08:** Task 3 `<verify>` uses `git grep -l` (tracked files only, so no .pyc).
- **NB-14:** Task 1 and Task 2 `<verify>` add `git status --porcelain data/` → empty.

## Revision R3 (gate R2)

- **D-R2-05:** § Fast-path gives an explicit branch-from-origin/master sequence; the local-master pull is deferred to 34-04 step 8, with the conflict rule (keep local = `--theirs` during the rebase; verify model_config equals the fast-path model; re-run the gate).
- **D-R2-08:** Task 1 §1 names the probe file via the 34-01 slug rule and excludes the direct-* probe.
