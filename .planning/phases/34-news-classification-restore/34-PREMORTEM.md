---
phase: 34
artifact: premortem
plans_evaluated: 34-01..34-06 @ 6c80b6c
method: corrida-autonoma §3.4 — 3 opus perspectives (technical, methodological, integration) + fresh opus arbiter (high)
risks: 17 (P0 1, P1 7, P2 9) — MITIGAR 14, ACEPTAR 3
---

# Phase 34 — Premortem

> "It is 2026-10-06 and Phase 34 failed. Why?"

## Arbiter summary

Arbitré 26 riesgos de 3 perspectivas (T-01..08, M-01..07, I-01..11) contra el repo en 6c80b6c, sin escrituras ni llamadas LLM. Quedan 17 riesgos finales: 1 P0, 7 P1, 9 P2; 14 MITIGAR y 3 ACEPTAR. Descarté 5 sub-afirmaciones sin verificar.

P0 (R-01): la ruta más probable a un /news/ vacío el 10-05 no es un bug de código.
- Los umbrales de G-06 son exactamente la marca histórica de DeepSeek v4-flash (42/44 commune y 29/44 family; lo verifiqué en ab_deepseek.json). Un solo ítem de variación deja el A/B en NO_QUALIFIER.
- Además, el runner es más estricto que prod: no hace la re-llamada por contenido vacío.
- Con NO_QUALIFIER, 34-01 y 34-02 hacen STOP y dejan una pregunta al owner, que está ausente. Nadie ejecuta el fast-path del 09-30.
- Mitigación: registrar un G-08 antes del A/B con un fallback determinista y sin pausa.

P1 confirmados:
- **R-02:** el backfill viola G-07(a), porque merge_and_write sube last_new_incident_at sin opt-out.
- **R-03:** las pérdidas parciales son invisibles. PARSE_ERROR (vacío o truncado) queda en seen y es terminal. G-04 no implementa el literal de NREC-07 ('classified==0 with candidates>0').
- **R-04:** con un primario persistentemente roto que el preflight público no ve, los 5 ítems que disparan el breaker expiran en silencio cada 5 corridas.
- **R-05:** el golden set no tiene ninguna etiqueta robos_violentos. Los 13 fallos de familia de DeepSeek son predicciones robos_violentos, así que el criterio de victoria premia el defecto V-07.
- **R-06:** el backfill publica ~1,000 ítems sin auditar precisión, y el 52.5% viene de Google News con solo el título.
- **R-07:** el costo 'typical' de v4.1-flash es $3.00 (el plan dice $2.3), lo que dispararía una pausa 4 espuria.
- **R-08:** el heartbeat CEAD falla el 10-01 (lo ejecuté: EXIT=1) y enmascara el canal de alerta de news G-05 justo en la ventana del deadline.

P2: backup y NEWS_MODEL inertes en Actions, telemetría de proveedor, presupuesto de tiempo por corrida, contrato de usage y orden de imports, harness que escribe en data/ real más el `--theirs` invertido, y un segundo chequeo de carrera. ACEPTAR: pending.json servido (ya decidido en G-06), ítems 'veneno' 4xx y el rango de fechas de 34-06.

Archivos de plan que se editan: .planning/phases/34-news-classification-restore/34-01..34-06-PLAN.md y .planning/v2.2-AUTONOMOUS-DIRECTIVE.md, con los nuevos G-08, G-09 (enmienda G-04/G-03) y G-10 (re-etiquetado del golden set).

## Final risks

| id | sev | decision | origin | scenario | plan edit / acceptance reason |
|---|---|---|---|---|---|
| R-01 | P0 | MITIGAR | T-03 + M-01 + I-01 | El A/B de 34-01 termina en NO_QUALIFIER o en INCOMPLETE persistente. El plan se detiene en la pausa 2 y termina con una pregunta al owner, que está ausente, y 34-02 Task 1 también hace STOP si no hay WINNER. El fast-path del 09-30 lo tendría que ejecutar el mismo orquestador que ya terminó en pausa, así que nadie lo corre. El 10-05 /news/ queda vacío. | (1) Directiva § Decision log: registrar G-08 ANTES de 34-01 Task 3. Texto: 'Antes del 2026-10-03, las pausas 1 y 2 NO terminan la corrida. Se informan en RUN STATUS y deferred-live y se aplica este fallback. FALLBACK_WINNER = el candidato con endpoints≥2, parse_errors≤1/47 (contados después de la re-llamada por contenido vacío) y commune≥41/44, ordenado por parse asc, commune desc, family desc (sobre el set re-etiquetado de R-05) y endpoints desc. Si ninguno cumple, el primario pasa a ser DeepSeek directo (default NEWS_PROVIDER=deepseek, rama existente classifier.py:60-65). Si una pausa 1 o 2 ocurre antes del 09-30, el fast-path de 34-02 se ejecuta de inmediato, sin esperar al 09-30.' (2) 34-01 Task 2: select_winner devuelve status FALLBACK_WINNER según G-08, con tests (sin calificados y con un candidato 41/44 + 1/47 → FALLBACK_WINNER; sin nada → DEEPSEEK_DIRECT). En 'Client', agregar: 'on empty content make exactly one immediate re-call before _parse_content (same as classifier.py:162-166); record empty_first_try separately'. (3) 34-01 Task 3 'Expected': reemplazar 'NO_QUALIFIER → directive pause point 2: stop and ask the owner' por 'NO_QUALIFIER → apply G-08 (FALLBACK_WINNER or DEEPSEEK_DIRECT), record it, continue to 34-02'. (4) 34-02 Task 1 §1: reemplazar 'If decision.status != "WINNER", STOP' por 'If status ∈ {WINNER, FALLBACK_WINNER} copy winner; if DEEPSEEK_DIRECT set the NEWS_PROVIDER default to deepseek; never STOP here'. |
| R-02 | P1 | MITIGAR | T-02 + M-05 + I-03 | El --apply del backfill llama a merge_and_write, que desde 34-03 fija last_new_incident_at=now cuando entra ≥1 id nuevo. Eso deja fresca la evidencia G-05 durante 48 h aunque la clasificación viva haya muerto, y el check 1 de 34-06 queda satisfecho por el backfill y no por el cron. Es la violación literal de G-07(a). | 34-03 Task 1: agregar a merge_and_write el keyword-only `bump_last_new: bool = True`. Con False, se arrastra el valor previo o se omite la clave. Behavior nuevo: 'bump_last_new=False with count>0 leaves last_new_incident_at byte-identical (or absent)'. 34-05-PLAN:80 y :143: pasar a 'merge_and_write(accepted, current_path, archive_dir, bump_last_new=False)  # G-07(a)'. 34-05 Task 1 behavior: 'apply leaves last_new_incident_at byte-identical'. 34-05 Task 3 paso 3: comparar el campo antes y después del apply. 34-06 Task 2 paso 1: exigir además last_new_incident_at > `git log -1 --format=%cI --grep='data(34-05)'`, es decir, que lo haya fijado una corrida del cron posterior al backfill. |
| R-03 | P1 | MITIGAR | T-01 + M-04 + I-04 (parte parse) | Los proveedores de OpenRouter devuelven contenido vacío, truncado (finish_reason=length) o JSON con prosa alrededor en una fracción de los ítems, o el modelo responde pero rechaza todo. El plan marca esos PARSE_ERROR y NOT_CRIME como seen, así que se pierden de forma permanente sin pasar por pending. G-04 solo falla con responded==0, con api_errors≥50% o con el backup agotado, así que una corrida con classified==0 y ~20 candidatos que 'respondieron' queda en verde. Resultado: /news/ incompleto o congelado, con el gate en verde. | (1) Directiva: registrar G-09, que enmienda G-04 y G-03: 'empty_content (tras la re-llamada) y finish_reason==length son transitorios, se tratan como API_ERROR (van a pending y no se marcan seen). G-04 agrega (d) attempted≥10 y accepted==0 → fail (literal de NREC-07) y (e) attempted≥10 y parse_errors/attempted ≥0.2 → fail.' (2) 34-02 Task 1 behavior: cambiar 'then PARSE_ERROR with error "empty_content"' por 'then API_ERROR with error "empty_content" (status_code None)'. Agregar 'finish_reason=="length" → API_ERROR "finish_length"'. Si json.loads falla, reintentar sobre el primer objeto {...} balanceado antes de declarar PARSE_ERROR. (3) 34-02 Task 2 y schema del resumen: agregar los contadores empty_content y finish_length. (4) 34-03 Task 3: evaluate() implementa (d) y (e), con tests de borde (attempted=10, accepted=0 → fail; attempted=9 → pass; parse 2/10 → fail; 1/10 → pass). (5) 34-05 Task 1: los resultados empty_content/finish_length quedan intactos como API_ERROR (siguen en classifier_none y se reintentan). |
| R-04 | P1 | MITIGAR | T-04 + M-07 + I-04 (parte failover) | El primario queda roto de forma persistente con un fallo que el preflight público no ve: 402 sin créditos en OpenRouter, 401 por key revocada o 404 por data policy. En cada corrida los 5 ítems más viejos de pending (que se procesan primero) fallan en el primario, disparan el breaker, se re-encolan con attempts+1 y nunca pasan al backup en esa corrida. A la 5ª corrida expiran como api_error_expired y quedan en seen. G-04 queda en verde porque 5 de ~21 es menos del 50%, y el failover solo emite ::warning::. El resultado es una pérdida silenciosa y sostenida, y prod corre sobre el backup sin que nadie lo sepa. | (1) 34-02 Task 1 §5 y Task 2: 'When the primary breaker trips, the items whose API_ERROR formed the tripping streak are re-dispatched to the backup in the same run; if the backup answers, their attempts are not incremented'. Test: primario 404 ×5 y backup OK → los 5 terminan OK y pending queda vacío. (2) 34-03 Task 3 evaluate(): agregar (f) expired>0 → fail ('items permanently lost'), registrado en G-09. (3) 34-02 Task 1 §4, opcional: preflight autenticado con GET https://openrouter.ai/api/v1/key; limit_remaining ≤0 → status 'no_credit' → failover (es metadata, no completion). (4) 34-04 Task 2 paso 5 y 34-06 Task 1 paso 2: 'failovers==0 is REQUIRED for a run to count toward NREC-10; failovers>0 → diagnose before counting'. |
| R-05 | P1 | MITIGAR | M-02 | El golden set no tiene ninguna etiqueta robos_violentos. Portonazos y asaltos están etiquetados como vida o propiedad. Así, el criterio 'gana el mayor family_correct' y el umbral de family premian al modelo que menos usa robos_violentos, que es el defecto V-07 (vida como cajón de sastre), y descalifican a los modelos que siguen la taxonomía del prompt. El backfill publica ~1,000 cards con familia sesgada. | 34-01: agregar un Task 0 offline, sin LLM API, antes de Task 3. Un revisor opus fresco re-etiqueta ítem por ítem los robos con violencia o intimidación. Candidatos: gs-001/002/003/026/033 (vida) y gs-013/021/029/036 (propiedad), con una razón por ítem. El resultado va a un archivo NUEVO pipeline/tests/fixtures/golden_set_v2.json; no se muta golden_set.json, que es la procedencia del número de Phase 16. Después se re-puntúan las predicciones por ítem ya guardadas (pipeline/results/ab_deepseek.json y .planning/spikes/008-granite-openrouter-classifier/results_granite_golden.json) para fijar el nuevo baseline de family. Registrar G-10 con los ids re-etiquetados y el nuevo umbral de family (= baseline Phase-16 re-puntuado, como exige NREC-01). 34-01 Task 3: correr con --golden golden_set_v2.json. 34-AB-RESULTS.md imprime la matriz de confusión de familias por candidato. |
| R-06 | P1 | MITIGAR | M-03 | El backfill publica de una vez ~1,000 aceptados sin ninguna medición de precisión sobre no-crimen. El 52.5% de la población son ítems de Google News cuyo único contenido es el título. Hay ítems institucionales y de medios extranjeros. El --review solo busca /Fierro\|Carahue\|suicid/, y el check de 34-06 (≥1 card por día) pasa aunque los datos sean incorrectos. | 34-05 Task 2, nuevo paso 7b antes de Task 3: auditoría estratificada de 50 ítems OK de la cache (≥20 de Google News con solo título, ≥10 con indicios de ubicación no chilena, el resto al azar). Un opus fresco compara cada ítem con su título y descripción fuente (¿crimen en Chile?, ¿comuna?, ¿familia?). Umbral pre-declarado ≥43/50 (85%, alineado con FID-03). Si pasa, se aplica todo. Si no, el estrato que falla queda fuera del apply (sus ids se quedan en classifier_none, no se publican) y se re-auditan 30 ítems del resto; registrar G-NN. Ampliar el regex de --review (34-05-PLAN:117) con abuel\|suegr\|sobrin\|cuñad, accidente\|choque\|colisi\|atropell, cárcel\|recinto penitenciario\|interno, y México\|Argentina\|España\|Venezuela\|Estados Unidos\|EE\.UU. |
| R-07 | P1 | MITIGAR | M-06 (+ T-08 parte meter) | Si el desempate por endpoints elige deepseek-v4.1-flash (24 endpoints), la fórmula del propio 34-05 da un costo 'typical' ≥ $3.00. La regla 'typical > $3.00 → STOP, pausa 4' detiene el backfill a días del 10-05, y /news/ no muestra 09-05..09-22. El plan afirma '≈ $2.3', lo que no cuadra con su fórmula. | 34-05 Task 2 paso 4: calcular typical = N × (spend_usd/47 del run-<winner>.json medido en 34-01), y el ceiling con los precios máximos. Corregir :176-177 con los valores del catálogo (v4-flash ≈ $1.30, v4.1-flash ≈ $3.00). Cambiar la regla de STOP a 'typical > (5.00 − gasto de fase ya incurrido)'. Paso 5: --spend-cap = 5.00 − gasto del A/B − 0.25. Con exit 3, NO pausar: hacer --apply de la cache parcial, con la selección ordenada para cubrir primero las fechas 2026-09-05..09-22 (agregar orden por date en select_outage_rows/classify en Task 1) y reportar el resto como not_attempted. |
| R-08 | P1 | MITIGAR | I-02 | El 2026-10-01 a las 12:00Z el heartbeat CEAD falla con razón (2 fechas trimestrales vencidas) y abre el issue pipeline-failure-heartbeat. De ahí en adelante, toda falla del check news G-05 solo comenta 'Pipeline Heartbeat failed again' en ese issue abierto. La alarma de muerte silenciosa que construye 34-03 queda enmascarada justo en la ventana 10-01..10-05. | 34-03 Task 2 (heartbeat.yml): darle `id: news_hb` al step news y agregar un par de steps de alerta propio. 'Ensure news-heartbeat label' con continue-on-error, y 'Alert news heartbeat' con `if: failure() && steps.news_hb.outcome == 'failure'`, label pipeline-failure-news-heartbeat, dedup sobre ese label y ${{ }} solo vía env. El alert existente conserva R2 y CEAD. Test en test_workflow_guards: los dos alert steps usan labels distintos y el nuevo referencia steps.news_hb.outcome. 34-06: registrar el rojo CEAD del 10-01 como señal esperada con nombre (HYG-06 pendiente). |
| R-09 | P2 | MITIGAR | T-06 + I-05 + I-06 | El backup DeepSeek y la perilla de modelo nacen inertes en Actions. Ningún paso ejercita el backup con el secret de Actions: la fila backup del A/B usa el .env local y 34-04 exige preflight ok sobre el primario. NEWS_MODEL, NEWS_BACKUP_MODEL y NEWS_PROVIDER nunca llegan al step. El primer uso real del backup ocurre en el próximo outage, y si el secret de 06-19 es otro o no tiene fondos, la clasificación vuelve a cero. Cambiar de modelo en prod exige un push. | 34-03 Task 3 (news-pipeline.yml): agregar `workflow_dispatch: inputs: force_backup: {type: boolean, default: false}` y, en el env del step scrape, `NEWS_FORCE_BACKUP: ${{ inputs.force_backup }}`, `NEWS_MODEL: ${{ vars.NEWS_MODEL }}`, `NEWS_BACKUP_MODEL: ${{ vars.NEWS_BACKUP_MODEL }}` y `NEWS_PROVIDER: ${{ vars.NEWS_PROVIDER }}`. Solo en env, así que zizmor no se queja. Un vacío cae al default vía resolve() y classifier.py:58, y hay un test de que '' da openrouter. 34-02 Task 1 §5: NEWS_FORCE_BACKUP=='true' hace que el router arranque en el backup, con failover_reason 'forced' y test. 34-04 Task 2, nuevo paso 5b: `gh workflow run news-pipeline.yml --ref master -f force_backup=true`, y en el Run summary exigir failover_reason 'forced', responded ≥1, api_errors 0 y backup_exhausted false. DEPLOYMENT.md documenta que el owner cambia de modelo con una repo variable, sin push. |
| R-10 | P2 | MITIGAR | T-05 + M-04(c) | El modelo ganador en OpenRouter se enruta entre 15 y 24 proveedores heterogéneos (fp4/fp8/unknown), y lo que midió el A/B no se transfiere por completo a producción. Nadie registra qué proveedor atendió cada llamada ni su finish_reason, así que una degradación de precisión o un truncamiento no se puede diagnosticar. | 34-02 Task 1: ClassifyResult agrega served_by (el campo `provider` de la respuesta de OpenRouter si existe, si no resp.model) y finish_reason. El resumen de corrida agrega served_by_counts. 34-01 Task 2: per_item registra served_by y finish_reason, y 34-AB-RESULTS.md lista los proveedores vistos por candidato. Opcional, con kwargs idénticos en A/B y prod: si todos los endpoints del ganador soportan response_format (catálogo), enviar response_format json_object también a OpenRouter, decidido en 34-01 y copiado a model_config. |
| R-11 | P2 | MITIGAR | T-07 | Un proveedor se cuelga de forma intermitente y cada ítem consume 3×30 s más las esperas (~96 s). Un solo éxito resetea el breaker, así que nunca se dispara. Con ~20 ítems así se superan los 30 minutos de timeout del job. scrape_news escribe todo al final, así que el job cancelado no commitea nada, la siguiente corrida repite el mismo trabajo (livelock) y solo G-05 lo detecta a las 48 h. | 34-02 Task 1 §5: agregar NEWS_RUN_BUDGET_S (default 1200) con un reloj inyectable. Pasado el presupuesto, router.classify() devuelve None: los ítems quedan not_attempted, se encolan sin incrementar attempts y el resumen marca budget_exhausted=true. Test con reloj falso y 30 timeouts intermitentes: main() termina dentro del presupuesto y escribe los archivos. 34-03 evaluate: budget_exhausted → ::warning:: y no fail. |
| R-12 | P2 | MITIGAR | T-08 | El medidor del backfill no se puede implementar contra el contrato de 34-02 porque ClassifyResult no trae usage, y /api/v1/key no mide el gasto en DeepSeek directo. Además, si el backfill importa pipeline.news.classifier antes de load_dotenv, el client del módulo se construye con 'placeholder'. Si build_router_from_env reutiliza ese client, todas las llamadas dan 401 y se pierden horas diagnosticando cerca del deadline. | 34-02 contrato: agregar a ClassifyResult `usage: dict \| None` (prompt, completion y reasoning tokens, y cost si viene), con test. build_router_from_env() construye clients nuevos leyendo os.environ en el momento de la llamada; solo la ruta compat classify() usa el client global. 34-05 Task 1: importar pipeline.news.classifier dentro de main() DESPUÉS de load_dotenv, con un test en subprocess (env vacío y luego seteado). El medidor suma usage por proveedor (DeepSeek a precio peak). Task 2 paso 5: antes de --classify, un --classify --limit 1 real debe dar outcome OK/NOT_CRIME. |
| R-13 | P2 | MITIGAR | I-08 | (a) El harness de 34-03 ejecuta el scrape_news.main() real sin NEWS_DATA_DIR. Un pytest del gate escribe pending.json y current.json falsos en el data/ real, y eso puede terminar commiteado. (b) 34-04 paso 8 resuelve los conflictos del rebase con `git checkout --theirs data/...`, que dentro de un rebase es el commit LOCAL: pisa los datos del cron con datos viejos. | 34-03 Task 3: el harness hace monkeypatch de NEWS_DATA_DIR y NEWS_RUN_SUMMARY_PATH a tmp_path. Agregar a pipeline/tests/conftest.py una fixture autouse que fije NEWS_DATA_DIR=tmp_path si no está seteada. En 34-04 paso 2, 34-05 paso 4 (antes del apply) y 34-06 paso 4, agregar después de pytest `git status --porcelain data/` → vacío. 34-04-PLAN:72: reemplazar la resolución por '`git rebase --abort` and investigate (no 34-01..03 commit touches data/); if a resolution is ever needed use `git checkout --ours data/...` (= upstream during a rebase)'. |
| R-14 | P2 | MITIGAR | I-10 | El chequeo de carrera de 34-05 se hace solo antes del apply, pero entre el apply y el push corre el gate completo (más de 10 minutos, más posibles ciclos de arreglo). Un cron que arranca en ese intervalo choca en current.json. Si el backfill empuja primero, el cron falla en push-with-rebase, pierde el seen/pending de esa corrida y abre un issue falso. | 34-05 Task 3 paso 5: justo antes de `git commit`, repetir el chequeo `gh run list --workflow=news-pipeline.yml --limit 1 --json status,createdAt` (completed y hace menos de 3 h). Si la ventana se cerró: `git checkout -- data/incidents`, esperar a que termine el cron, `git pull --rebase` y re-ejecutar --apply desde la cache (idempotente) y el gate. |
| R-15 | P2 | ACEPTAR | I-07 | Desde 34-04, data/incidents/pending.json (snippets de terceros sin clasificar) se sirve públicamente vía sync-data hasta la Fase 38. | Ya está decidido en G-06 (directive:102: 'pending.json public exposure deferred to Phase 38 (HYG-01/HYG-05)'). Es la misma clase de contenido que rejected/, que ya se sirve. No hace fallar la Fase 34 (ni /news/ vacío ni datos incorrectos) y es reversible. Hay que asegurarse de que 34-06 lo agregue a deferred-live, como ya dice 34-06-PLAN:117. |
| R-16 | P2 | ACEPTAR | I-09 | Ítems 'veneno' con un 400 específico (moderación del proveedor, por ejemplo el 'Content Exists Risk' de DeepSeek) quedan al frente de pending, cuentan para el breaker y generan failover o backup_exhausted repetidos, con issues falsos. | La falla es ruidosa, no silenciosa: G-04(c) abre un issue. Está acotada por la expiración de 5 intentos o 14 días (G-03), y la mitigación R-04 (re-despacho al backup) reduce el efecto. Sacar los 4xx del breaker haría que un 400 sistémico (parámetro de reasoning no soportado) nunca disparara el failover. Se re-evalúa si el A/B (fila backup, 47 ítems) o las primeras corridas muestran 400 por ítem. |
| R-17 | P2 | ACEPTAR | I-11 | Si el cierre de 34-06 se corre al 10-06 o después, el 09-05 ya salió de la ventana de 30 días y el check '≥1 card para cada fecha 09-05..09-22' da un FAIL falso. | 34-06 está definido para correr antes del 2026-10-05, y ese día el 09-05 todavía está en la ventana. Si corre después, el deadline de NREC-10 ya se incumplió y el FAIL refleja un incumplimiento real del plazo, no un defecto de datos. No hace falta editar. |

## Discarded

| id | reason |
|---|---|
| T-01 (sub-afirmación empírica) | La parte 'DeepSeek v4-flash devolvió contenido vacío o JSON inválido en gs-011 y gs-039' no es verificable: ab_score.py:151-158 cuenta como parse_failure cualquier None de classify(), sea API error, low_conf (<0.6) o JSON inválido. El riesgo estructural se mantiene en R-03 sin apoyarse en ese dato. |
| M-01 (cifras binomiales P≈0.05 / 0.41) | Salen de supuestos de tasa de error, no de una medición. R-01 se sostiene en un hecho aritmético verificado: los umbrales G-06 son exactamente 42/44 y 29/44, la marca de DeepSeek en ab_deepseek.json. Las probabilidades concretas no entran como evidencia. |
| T-03 (no-determinismo con temperature 0) | Queda debilitado: el README del spike 008 (línea 87) reporta 3 corridas de Granite vía OpenRouter 'fully deterministic'. La variación entre proveedores sigue siendo plausible y está cubierta como observabilidad en R-10. No es base de R-01. |
| I-01 (hotfix fast-path inmediato el 09-23 como quick independiente) | Contradice la regla de fast-path de la directiva (directive:51, que lo dispara el 09-30) sin necesidad. La mitigación adoptada (R-01, G-08) consigue lo mismo, ya que el fast-path corre de inmediato si se dispara la pausa 1 o 2, y conserva el protocolo A/B como ruta principal. |
| T-07 (afirmación 'if: failure() no corre tras timeout') | No la verifiqué y no es necesaria para la mitigación R-11 (presupuesto de tiempo), que se sostiene en hechos leídos: timeout de 30 minutos, escrituras al final del loop y 96 s por ítem colgado. |

## Evidence per risk

- **R-01**: Los umbrales G-06 (directive:102; 34-01-PLAN:208: commune ≥42, family ≥29, parse 0/47) coinciden exactamente con la marca histórica del candidato más probable. pipeline/results/ab_deepseek.json da DeepSeek v4-flash commune 0.9545 (42/44), family 0.6591 (29/44) y parse_failure 0.0426. gs-011 y gs-039 devolvieron None; ab_score.py:151-158 cuenta como parse_failure cualquier None, sea low_conf, API o JSON. Basta un ítem de variación para descalificarlo. El runner no replica la re-llamada por contenido vacío que prod sí hace: 34-01-PLAN:178 contra classifier.py:162-166 y 34-02-PLAN:214. A/B es más estricto que prod. ibm-granite/granite-4.2-8b tiene hoy exactamente 2 endpoints (GET catálogo 2026-09-22, bf16 ×2), el mínimo de G-02. 34-01-PLAN:241 dice 'NO_QUALIFIER → pause point 2: stop and ask the owner', 34-02-PLAN:192 'If decision.status != WINNER, STOP' y directive:51 el fast-path al 09-30. Ninguna regla dice qué prevalece.
- **R-02**: directive:103 G-07(a) 'The backfill must NOT bump last_new_incident_at'. 34-03-PLAN:137 'If the count is > 0, set payload["last_new_incident_at"] = now', sin forma de desactivarlo. 34-05-PLAN:80 reconoce el efecto y 34-05-PLAN:143 llama 'merge_and_write(accepted, current_path, archive_dir) ONCE' sin flag. Un grep de last_new_incident_at/G-07 en los 6 planes no encuentra ningún test ni acción que implemente G-07(a). 34-06-PLAN:82 solo exige 'present'.
- **R-03**: 34-02-PLAN:177 y :214 (vacío → una re-llamada → PARSE_ERROR 'empty_content') y :286 (PARSE_ERROR → seen + stage parse_error). 34-02-PLAN:149: responded = OK + NOT_CRIME. 34-03-PLAN:214-217 implementa solo (a), (b) y (c). REQUIREMENTS.md:23 (NREC-07) define la falla como 'classified==0 with candidates>0', pero G-04(a) la reemplazó por responded==0, así que el literal de NREC-07 no se implementa. 34-05-PLAN:125 selecciona solo classifier_none, así que ningún tool recupera los parse_error. classifier.py:137: el regex de fence exige que el fence cubra todo el contenido. El resumen de corrida (34-02-PLAN:142-146) no registra finish_reason. NO verificado: que los None de gs-011 y gs-039 hayan sido parse errors, porque ab_score los mezcla con low_conf.
- **R-04**: 34-02-PLAN:178 dice que el 6º ítem va al backup; los 5 que dispararon el breaker no se re-despachan. 34-02-PLAN:279 procesa pending primero. :283 marca expired al llegar a attempts≥5. :278 marca seen a los expirados. :228 hace failover del preflight solo con zero_endpoints o model_unknown, vía GET sin auth (:219). El estado del breaker es por corrida (:227). directive:100 'Failover to the backup alone does NOT fail the job'. 34-04-PLAN:91 y 34-06-PLAN:66 tratan los failovers como no bloqueantes. 34-RESEARCH.md:496 marca OPENROUTER_API_KEY como 'Not verified'.
- **R-05**: golden_set.json (python): familias vida 12, propiedad 12, drogas 8, vif 4, armas 4, incivilidades 4, null 3, robos_violentos 0. En ab_deepseek.json per_item, los 13 fallos de familia son (propiedad→robos_violentos) ×7, (vida→robos_violentos) ×5 y (propiedad→armas) ×1. robos_violentos es una familia válida (shared/schema.py:22-30, ofrecida en el prompt en classifier.py:103-118). Spike 008 README:121-124: 'gs-001..003 are portonazos/asaltos labelled vida … the 65.91% ceiling is partly a label problem'.
- **R-06**: Python sobre rejected/2026-09.json: 2,073 filas de outage, 1,089 (52.5%) con url news.google, mediana de description 118 chars contra 320 del resto. Ejemplo: 'Gobierno recibe a ministros y fiscal de El Salvador'. El golden set tiene solo 3 nulls triviales (spike 008 README:92: los bloquea el pre-filtro). 34-01-PLAN:200-201: null_correct es informativo y low_conf sobre un crimen cuenta como fallo. Regex de review en 34-05-PLAN:117. El gate de rechazo no-crimen de V-08 quedó diferido a FID-02 (Fase 36). Atenuante: el pipeline vivo pre-outage procesaba la misma distribución con ~53% de aceptación, así que el riesgo nuevo es la escala y la falta de revisión, no el método.
- **R-07**: GET catálogo 2026-09-22, v4.1-flash: mediana in $0.285, out $1.14, máximos 0.375 y 1.50. Con la fórmula de 34-05-PLAN:172 (P=3,700, C=170, N=2,400): typical $2.996 y ceiling $3.94; N crece ~84-148 por día. v4-flash: typical $1.30 (el plan dice $1.05). 34-05-PLAN:177 dice '≈ $2.3'. La STOP está en :179. El tope de la directiva es $5 por fase (directive:78), no $3.
- **R-08**: git log -1 --format=%cI -- data/cead/ = 2026-06-19T17:24:23-04:00. Ejecuté check-heartbeat.sh cead con as-of 2026-10-01T12:00:00Z: '2 quarterly due dates have elapsed', EXIT=1. Con as-of 09-30: EXIT=0. heartbeat.yml:95-110 deduplica las 3 checks con un solo label pipeline-failure-heartbeat. 34-03-PLAN:173-178 solo cambia el cuerpo del step news. HYG-06 queda en la Fase 38 (directive:56). gh issue list --state open = [] hoy.
- **R-09**: news-pipeline.yml:6 tiene workflow_dispatch sin inputs, y :64-68 el env del scrape solo con OPENROUTER_API_KEY y DEEPSEEK_API_KEY. 34-03-PLAN:222 agrega solo id y NEWS_RUN_SUMMARY_PATH. gh variable list está vacío. gh secret list: DEEPSEEK_API_KEY 2026-06-19T22:38:05Z. 34-01-PLAN:165 carga el .env local. 34-04-PLAN:91 exige preflight ok. La prueba de NREC-02 (34-02-PLAN:182) es un subprocess local. NO verificado: que la key del secret difiera de la local.
- **R-10**: GET catálogo 2026-09-22: v4-flash tiene fp8 ×11, unknown ×3 y fp4 ×1, con response_format en 15 de 15. v4.1-flash tiene fp4 ×3, con response_format en 21 de 24. 34-01-PLAN:124 y 34-02-PLAN:180 no mandan response_format ni routing de proveedor a OpenRouter. El schema del resumen (34-02-PLAN:142-146) no tiene proveedor ni finish_reason. El probe usa 3 ítems (34-01-PLAN:195). NO verificado que fp4 degrade la precisión; spike 008 README:87 da Granite determinista en 3 corridas.
- **R-11**: news-pipeline.yml:15 tiene timeout-minutes: 30. 34-02-PLAN:121-122 fija RETRY_MAX_ATTEMPTS=3 y REQUEST_TIMEOUT_S=30.0, y :229 dice que cualquier resultado que no sea API_ERROR resetea el breaker. scrape_news.py:366-383 hace record_rejected, merge_and_write y save_seen después del loop. scrape_news.py:145 fija un tope de 200. NO verificado que los steps if: failure() no corran tras un timeout del job.
- **R-12**: 34-02-PLAN:124-126: ClassifyResult no tiene usage. 34-05-PLAN:134-136: el fallback del medidor usa prompt_tokens y completion_tokens, y el backup se cobra a precio peak. classifier.py:61-79 construye OpenAI(api_key=os.environ.get(..., 'placeholder')) al importar. 34-02-PLAN:212 define client_getter=lambda: client. 34-05-PLAN:129 dice 'Load keys … then build_router_from_env()' sin fijar el orden respecto al import.
- **R-13**: pipeline/tests/conftest.py no tiene fixture autouse de NEWS_DATA_DIR (grep: solo aparece en el docstring, :23). scrape_news.py:126-128 usa por defecto _DEFAULT_DATA_DIR = data/incidents. Los tests existentes lo fijan uno por uno (test_scrape_news.py:113). 34-03-PLAN:201 describe el harness sin mencionar NEWS_DATA_DIR. 34-04-PLAN:72 dice 'keeping the upstream data/ files (`git checkout --theirs data/...` inside the rebase)', y en git rebase --ours es upstream y --theirs es el commit re-aplicado.
- **R-14**: 34-05-PLAN:203 hace un chequeo único ('apply + commit + push takes ≲ 2 min') que ignora el gate del paso 4 (:212-214). push-with-rebase.sh aborta con exit 1 ante un conflicto. El cron es '0 */6 * * *' (news-pipeline.yml:5) y arranca con retrasos irregulares.
- **R-15**: sync-data.mjs:44-48 hace cpSync de todo data/incidents sin filtro. news-pipeline.yml:75 hace git add data/. 34-02-PLAN:367 (T-34-08) lo acepta.
- **R-16**: 34-02-PLAN:210 dice que 400/422 no se reintentan pero sí son API_ERROR que cuentan para el breaker (:229-231). Pending va primero (:279). No hay evidencia medida de 400 por ítem en los candidatos.
- **R-17**: store.py:138-160: cutoff = today − 30d, date ≥ cutoff. El 10-05 el cutoff es 09-05 (incluido) y el 10-06 pasa a 09-06. 34-06-PLAN:88 usa un rango fijo.
