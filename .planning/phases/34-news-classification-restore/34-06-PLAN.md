---
phase: 34-news-classification-restore
plan: 06
type: execute
wave: 6
depends_on: ["34-04", "34-05"]
files_modified:
  - .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [NREC-10]
executor: orchestrator (gh/curl polling); fresh opus for the served-route validation verdict

must_haves:
  truths:
    - "3 consecutive scheduled news-pipeline runs after the 34-04 push each show classified ≥ 1, 0 'HTTP/1.1 404' lines and a green Health gate."
    - "Before 2026-10-05, prod /news/ and /es/noticias/ each render ≥ 1 card (class news-date) for EVERY date 2026-09-05..2026-09-22, and the newest card date is ≥ (UTC today − 1 day)."
    - "The hotfixed cards (quick-260922-t59) are not reintroduced: the 8 dropped ids are absent from the served current.json and 514872e1d06db7f2 keeps its hotfixed title."
    - "The phase closes at the full gate: pytest ≥ baseline + new, vitest ≥ 86 + new, validators 16/16 (freshness green), astro check 0 errors, lint-workflows 0, and ci.yml dispatch all green."
  artifacts:
    - path: .planning/v2.2-AUTONOMOUS-DIRECTIVE.md
      provides: "RUN STATUS: Phase 34 ☑, measured prod numbers, spend, G-NNs"
  key_links:
    - from: https://ischilesafe.com/news/
      to: data/incidents/current.json (served copy)
      via: "Cloudflare Pages build from master (sync-data copies data/incidents → public/data/incidents)"
      pattern: "class=\"news-date\" datetime="
---

<objective>
Prove NREC-10 on the served routes and close Phase 34. Validation is against prod (directive Roles: "Validation … against the served route"), not only the harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v2.2-AUTONOMOUS-DIRECTIVE.md
@.planning/phases/34-news-classification-restore/34-04-SUMMARY.md  (push SHA + timestamp = start of the scheduled-run window)
@.planning/phases/34-news-classification-restore/34-05-SUMMARY.md  (backfill split)

<interfaces>
Served-route facts measured this session (2026-09-22):
- /news/ and /es/noticias/ render one `<time class="news-date" datetime="YYYY-MM-DD">` per incident. There are 799 on each page, equal to the served current.json (https://ischilesafe.com/data/incidents/current.json → 200, 799 incidents). The "Updated" stamp is a separate `<time datetime="<ISO generated>">` WITHOUT class news-date, so the instrument below does not count it.
- Card rendering: news.astro:413 / es/noticias.astro:412.
- Scheduled cadence measured: every 4-8 h (research §Pitfall 5), so 3 scheduled runs take about 12-24 h.
- Hotfix commit 45c2650 lists the 8 dropped ids and the retitled 514872e1d06db7f2.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: ORCHESTRATOR — 3 consecutive scheduled runs classify with 0 × 404</name>
  <what-built>Hardened pipeline live since the 34-04 push (timestamp PUSH_TS).</what-built>
  <action>
1. `gh run list --workflow=news-pipeline.yml --event schedule --limit 10 --json databaseId,createdAt,conclusion` → take the earliest 3 consecutive scheduled runs with createdAt > PUSH_TS.
2. For each run ID:
   - `gh run view "$ID" --log > "$SCR/sched-$ID.log"`.
   - `grep -oE 'Classification summary: classified=[0-9]+' "$SCR/sched-$ID.log"` → ≥ 1.
   - `grep -cE 'HTTP/1\.1 404' "$SCR/sched-$ID.log"` → 0.
   - `gh run view "$ID" --json conclusion,jobs --jq '[.conclusion, (.jobs[].steps[] | select(.name=="Health gate (NREC-07 / G-04)") | .conclusion)]'` → ["success","success"].
   - Record `"failovers"` from the Run summary line. It is non-blocking per G-04 but reported.
3. Literal consecutiveness (NREC-10): a scheduled run with classified=0, even one with 0 unseen candidates, RESETS the count. Continue with the next 3. This is pre-declared so a quiet run cannot be hand-waved.
4. If any run fails the gate or shows 404: that is a NREC-10 FAIL. Diagnose from the Run summary and fix-forward through a gated push (34-04 Task 1 steps). If 2026-10-03 arrives without 3 green runs → pause point 6: say loudly that the pages go empty on 10-05.
  </action>
  <verify>
    <automated>Each numbered step in the action carries its exact command and its expected output; a mismatch at any step is a FAIL of this task.</automated>
  </verify>
  <done>3 consecutive scheduled run IDs recorded with classified values, 404=0, gate success</done>
  <resume-signal>3 consecutive scheduled run IDs recorded with classified values, 404=0, gate success</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ORCHESTRATOR + fresh opus — served-route verification, hotfix survival, phase-close gate</name>
  <what-built>Backfilled data deployed (34-05) + live runs.</what-built>
  <action>
**1. Freshness of the served build.** `curl -s https://ischilesafe.com/data/incidents/current.json | python -c "import json,sys;d=json.load(sys.stdin);print(d['generated'],d.get('last_new_incident_at'),len(d['incidents']),max(i['date'] for i in d['incidents']))"`
- last_new_incident_at is present, and max(date) ≥ (UTC today − 1 day).
- If the served data lags master (for example max(date) is older than the local `data/incidents/current.json` after `git pull`), run `gh workflow run deploy-manual.yml --ref master`, wait for success (`gh run watch <id> --exit-status`), then re-curl. This is the only allowed deploy dispatch.

**2. Card coverage for each locale.** `for u in https://ischilesafe.com/news/ https://ischilesafe.com/es/noticias/; do curl -s "$u" | grep -oE 'class="news-date" datetime="[0-9]{4}-[0-9]{2}-[0-9]{2}"' | grep -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}' | sort | uniq -c > "$SCR/days-$(basename $u).txt"; done`

Then a python check on each file:
- every date D in 2026-09-05..2026-09-22 (18 dates) has count ≥ 1
- max date ≥ (UTC today − 1 day)
- the EN and ES totals are equal

Print the per-day counts. The threshold of ≥ 1 per day is defensible because each of those 18 days had ≥ 75 outage rows (measured), against a ~53% pre-outage acceptance rate. A zero day is therefore a real defect, not noise.

**3. Hotfix survival (cheap FID-07 pre-check; FID-07 itself is Phase 36).** On the served current.json:
- none of c3db293fd1316ad1, d468d43eeda0316d, f9031873f12b9479, 8f18cd275b4a6692, 836e500e4e4b1f30, 60fa7e16887af782, efa0fae52180e3e2, c53a579a6c972705 is present
- 514872e1d06db7f2's title_es == its value in `git show 45c2650:data/incidents/current.json`
- list every served incident whose title_es/title_en matches /Fierro|kinship|abuelo|suegro|sobrino político|suicid|quit\w* la vida/i

A fresh opus reviewer checks each listed incident against its source outlet URL and verdicts whether it repeats a V-06 kinship error or a suicide mislabelled as a crime. Any hit → STOP. Hand it to Phase 36 (FID-07) as a blocker finding. A data edit outside the backfill is NOT allowed here (directive data/ rule → pause point 5).

**4. Phase-close gate.** `git pull --rebase origin master`, then ONE chained command:
`python -m pytest pipeline/tests/ -q -p no:cacheprovider; echo PY=$?; cd site && npm run build && node scripts/validate/all.mjs; echo V=$?; npx vitest run; echo VT=$?; npx astro check; cd ..; bash .github/scripts/lint-workflows.sh; echo L=$?`

Expected:
- PY=0: "P passed, 1 skipped, 2 xfailed", P ≥ 394 + N_new.
- V=0: 16/16 with `freshness` PASS.
- VT=0: ≥ 86 + new.
- astro check: 0 errors.
- L=0.

Then `gh workflow run ci.yml --ref master` → all 3 jobs success.

**5. Close-out.**
- Update RUN STATUS (Phase 34 ☑, measured numbers, spend, G-NNs).
- Update STATE.md, ROADMAP.md (Phase 34 plans ticked, Plans: 6 plans), and REQUIREMENTS.md (NREC-01..10 → Complete).
- Update memory entries v2.2-milestone-news-recovery and granite-default-classifier (replacement live).
- Deferred-live additions: Granite-era re-classification (owner default); data/SOURCES.md:417 model-name drift and pending.json public exposure → Phase 38.
- `git pull --rebase origin master && git push origin master`.
  </action>
  <verify>
    <automated>Each numbered step in the action carries its exact command and its expected output; a mismatch at any step is a FAIL of this task.</automated>
  </verify>
  <done>All four checks green before 2026-10-05; Phase 34 closed and pushed</done>
  <resume-signal>All four checks green before 2026-10-05; Phase 34 closed and pushed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| prod site → public | published incidents about real people |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-20 | Tampering (reputational/legal) | backfilled cards | mitigate | Regex sweep + fresh-opus source check for V-06-class errors; stop-and-escalate, no ad-hoc data edits |
| T-34-21 | Denial of Service | empty /news/ on 10-05 | mitigate | Deadline checks; pause point 6 on 10-03 |
</threat_model>

<verification>
- Task 1 and Task 2 checks all green, with numbers recorded in RUN STATUS and 34-06-SUMMARY.md.
</verification>

<success_criteria>
NREC-10 is proven on prod before 2026-10-05, and Phase 34 closes at the full gate with 16/16 validators, including an honest, green freshness check.
</success_criteria>

<output>
Create `.planning/phases/34-news-classification-restore/34-06-SUMMARY.md` when done.
</output>
