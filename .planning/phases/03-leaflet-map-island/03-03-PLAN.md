---
phase: 03-leaflet-map-island
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - site/src/components/map/MapIsland.tsx
  - site/src/components/map/UserLocationMarker.ts
  - site/src/components/map/IncidentPinLayer.ts
  - site/src/components/map/FiltersRow.tsx
  - site/src/components/map/map.css
autonomous: false
requirements: [MAP-04, MAP-05]
must_haves:
  truths:
    - "Tapping 'mostrar mi ubicación' identifies and highlights the user's commune (or degrades gracefully on denial)"
    - "The events toggle fetches incidents/current.json and shows pins with source + date when present"
    - "When incidents/current.json is absent, the events toggle shows a graceful 'coming soon' empty state with no console error"
  artifacts:
    - path: "site/src/components/map/UserLocationMarker.ts"
      provides: "Geolocation API + ray-cast point-in-polygon -> highlight commune; graceful denial"
      min_lines: 40
    - path: "site/src/components/map/IncidentPinLayer.ts"
      provides: "NEWS layer: fetch incidents/current.json, divIcon pins, graceful empty state"
      min_lines: 30
  key_links:
    - from: "site/src/components/map/UserLocationMarker.ts"
      to: "navigator.geolocation"
      via: "getCurrentPosition + point-in-polygon"
      pattern: "getCurrentPosition"
    - from: "site/src/components/map/IncidentPinLayer.ts"
      to: "/data/incidents/current.json"
      via: "graceful fetch (404 = empty state)"
      pattern: "incidents/current\\.json"
---

<objective>
Add geolocation ("mostrar mi ubicación") and the NEWS incident-pin layer to the island, then run the manual interaction/device UAT. Geolocation uses the browser Geolocation API plus a hand-rolled ray-cast point-in-polygon against the loaded GeoJSON to identify and highlight the user's commune (no external reverse-geocode), degrading gracefully on permission denial. The events toggle fetches `incidents/current.json`; when present it shows `divIcon` pins with source outlet + date + link; when absent (Phase 5 not yet built) it shows a graceful "coming soon" toast with no console error.

Purpose: Delivers MAP-05 (locate + highlight) and MAP-04 (incident-pin layer with graceful empty state) — the last two requirements — and gates the phase on the manual checks that static assertions cannot cover (choropleth render, filter recolor, panel, geolocation, jank on 3G mobile).
Output: UserLocationMarker.ts + IncidentPinLayer.ts; MapIsland + FiltersRow + map.css wired; manual UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-leaflet-map-island/03-RESEARCH.md
@.planning/phases/03-leaflet-map-island/03-PATTERNS.md
@.planning/phases/03-leaflet-map-island/03-UI-SPEC.md
@.planning/phases/03-leaflet-map-island/03-VALIDATION.md
@.planning/phases/03-leaflet-map-island/03-02-SUMMARY.md

<interfaces>
Loaded GeoJSON features (from MapIsland after TopoJSON decode): Array<GeoJSON.Feature> with properties.id = CUT, geometry Polygon|MultiPolygon. Used for ray-cast PiP.
pointInPolygon(lat, lng, features): string|null -> returns the CUT of the containing feature (GeoJSON coords are [lng, lat]).
Chile bounds guard: lat in (-56.5, -17), lng in (-76.5, -66); outside -> simulate Santiago [-33.44, -70.65].
incidents/current.json (schema this phase DEFINES for Phase 5 to produce): { generated: string; window_days: number; incidents: Array<{ id; cut; lat; lng; title_es; title_en; date (YYYY-MM-DD); outlet; url; family }> }.
divIcon markers (Pitfall 7): use L.divIcon for user-dot (.user-dot 18x18) and ev-dot (.ev-dot 14x14) — never default L.marker icon (avoids marker-icon.png 404). L.marker uses DOM, not the canvas renderer (Pitfall 6 — keep canvas scoped to the choropleth).
Toast copy (UI-SPEC): geolocation success "Tu comuna: {nombre}"/"Your commune: {name}"; denied "No se pudo obtener tu ubicación"/"Location access denied"; simulated "Ubicación aproximada: Santiago"/"Approx. location: Santiago"; news absent "Capa de noticias próximamente"/"News layer coming soon".
selectCommune + highlightSelected available from Waves 1-2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UserLocationMarker.ts (geolocation + ray-cast point-in-polygon) and wire locate into MapIsland</name>
  <read_first>
    - ischilesafe.com/map-view.jsx (locate() fn lines 136-162 — geolocation flow, Chile bounds check, user-dot divIcon, flyTo)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md section UserLocationMarker.ts (locate + pointInPolygon + ringContains)
    - .planning/phases/03-leaflet-map-island/03-RESEARCH.md Pattern 5 (ray-cast PiP, why not turf), Pitfall 7 (default marker icon 404), A2/A5 assumptions
    - .planning/phases/03-leaflet-map-island/03-UI-SPEC.md section Geolocation (flow states, user-dot, highlighted commune)
    - site/src/components/map/MapIsland.tsx (selectCommune, highlightSelected, geoFeatures ref, onToast)
  </read_first>
  <action>
    Create UserLocationMarker.ts exporting locate(map, geoFeatures, userRef, onSelectCut, onToast, lang) and the ray-cast helpers pointInPolygon(lat, lng, features), containsPoint(geom, pt) (Polygon + MultiPolygon), ringContains(ring, pt) — replacing the prototype's centroid-distance nearestComuna with true PiP per D-14. Flow: if navigator.geolocation present, getCurrentPosition with timeout 5000; on success inside Chile bounds -> place .user-dot L.divIcon (remove previous), run pointInPolygon to get the CUT, call onSelectCut(cut) (which highlights + opens panel), flyTo zoom 12, success toast "Tu comuna: {name}"; coordinates outside Chile -> simulate Santiago + approximate toast; permission denied / error callback -> denied toast (no crash, no retry loop); geolocation unavailable -> simulate silently. Wire into MapIsland: pass the locate handler down to MapTopbar/SearchBox LocateButton (replacing the Wave-2 placeholder onLocate), expose the loaded GeoJSON features array + userRef. Ensure the LocateButton carries aria-label "Show my location"/"Mostrar mi ubicación" (the assertion deferred from Wave 1 map.mjs now becomes satisfiable). Highlighted-commune visual = same as click-selected (primary border 2.5px, fillOpacity 0.78, bringToFront).
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - UserLocationMarker.ts exports locate + pointInPolygon (handles Polygon and MultiPolygon via ring ray-cast); user marker uses L.divIcon (.user-dot), never the default marker icon.
    - Geolocation denial path calls onToast with the denied copy and does not throw (graceful — MAP-05 fallback).
    - The locate button in the rendered island carries aria-label "Show my location" (EN) / "Mostrar mi ubicación" (ES).
    - npx astro check exits 0.
  </acceptance_criteria>
  <done>Locate identifies + highlights the user's commune via PiP; denial degrades gracefully; locate aria-label present.</done>
</task>

<task type="auto">
  <name>Task 2: IncidentPinLayer.ts (NEWS layer + graceful empty state) wired to the events toggle; build + validators green</name>
  <read_first>
    - ischilesafe.com/map-view.jsx (events useEffect lines 256-278 — ev-dot divIcon, bindPopup)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md section IncidentPinLayer.ts (graceful empty-state pattern, ev-pop popup, security note)
    - .planning/phases/03-leaflet-map-island/03-RESEARCH.md Pattern 7 (incidents schema contract for Phase 5), Security Domain (XSS via incident popup, external link)
    - .planning/phases/03-leaflet-map-island/03-UI-SPEC.md section Incident Pins Layer (present vs absent states, popup content)
    - site/src/components/map/FiltersRow.tsx (events toggle chip — replace Wave-2 placeholder handler)
    - site/scripts/validate/map.mjs (locate aria-label assertion now satisfiable)
  </read_first>
  <action>
    Create IncidentPinLayer.ts exporting fetchAndMountIncidents(map, lang, onToast): graceful fetch of /data/incidents/current.json — if res.ok is false (404, the current state since Phase 5 is not built) or the fetch throws, call onToast("Capa de noticias próximamente"/"News layer coming soon") and return null with NO console error (D-15). When present, build an L.layerGroup of L.divIcon .ev-dot markers (Pitfall 7: divIcon, not default marker), each bindPopup with badge row (CEAD + approx-location), title (localized), date + outlet, and a source link target=_blank rel="noopener noreferrer". SECURITY: the popup HTML embeds incident strings — since bindPopup takes an HTML string, escape title/outlet before interpolation (or build the popup via DOM/textContent); never trust raw incident data (note for Phase-5 downstream). Wire into MapIsland: the events toggle (FiltersRow) now calls fetchAndMountIncidents when turned on and removes the layer group when turned off; store the layer in eventsRef. Define and document the incidents/current.json schema (Pattern 7) in a comment so Phase 5 produces a matching file. Run full build + validators.
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/map.mjs && node scripts/validate/all.mjs</automated>
  </verify>
  <acceptance_criteria>
    - IncidentPinLayer.ts fetches /data/incidents/current.json gracefully: absent/404 -> localized "coming soon" toast, returns null, no console error (MAP-04 empty state).
    - When the file is present its incidents render as .ev-dot divIcon pins with localized title, date + outlet, and a source link using rel="noopener noreferrer"; incident strings are escaped (no dangerouslySetInnerHTML / unescaped innerHTML).
    - node scripts/validate/all.mjs exits 0 including map.mjs (now asserting locate aria-label); content pages remain React/Leaflet-free.
  </acceptance_criteria>
  <done>Events toggle shows incident pins when data exists and a graceful empty state when absent (MAP-04); full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual interaction + mid-range mobile UAT (phase sign-off)</name>
  <what-built>
    The complete interactive map island on /map/ and /es/mapa/: 346-commune canvas choropleth (MAP-01), year + crime-type filters with in-place recolor (MAP-02), commune detail panel with tasa/trend/rank/sparkline/family breakdown (MAP-03), incident-pin layer with graceful empty state (MAP-04), and "mostrar mi ubicación" geolocation with point-in-polygon highlight (MAP-05) — all on the <100 KB TopoJSON with canvas rendering (MAP-06).
  </what-built>
  <how-to-verify>
    Run `cd site && npm run dev` and open http://localhost:4321/map/ (and /es/mapa/), then verify each (these are the manual-only checks from 03-VALIDATION.md):
    1. MAP-01: All 346 communes render colored by incidence; at country zoom the overview dots appear; no white/transparent polygons (CUT join sanity).
    2. MAP-02: Change the year selector and a crime-type chip — the map recolors with NO page reload/navigation.
    3. MAP-03: Click 3 communes (a high-incidence, a low-incidence, and a low-population one) — the panel shows rate, trend, national rank, a 2005-2025 sparkline, and the 7-family breakdown matching that commune's JSON.
    4. MAP-04: Toggle the "Recent incidents" chip — since incidents/current.json is absent, confirm a graceful "News layer coming soon"/"Capa de noticias próximamente" toast and NO console error.
    5. MAP-05: Click locate, grant permission -> correct commune highlighted + user-dot + success toast; then deny permission -> graceful denied toast, no crash.
    6. MAP-06 (SC3): In Chrome DevTools, emulate Moto G4 + Fast 3G (or CPU 4x throttle) and confirm the choropleth renders within ~4s on cold load and pan/zoom/filter stay responsive (no jank).
    Also spot-check a Phase-2 content page (e.g. /commune/santiago/) in DevTools Network/Sources and confirm NO leaflet/react JS loads there (zero-React regression).
  </how-to-verify>
  <resume-signal>Type "approved" to sign off Phase 3, or describe any issue (which requirement, what you saw) so it can be fixed before sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser Geolocation API to client | lat/lng stays client-side; PiP computed locally; never sent to any server |
| browser to incidents JSON | same-origin fetch only; may be absent (graceful) |
| incident strings to popup DOM | future Phase-5 data rendered into Leaflet popups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | geolocation coordinates | mitigate | Coordinates used only for client-side PiP + flyTo; no network transmission; permission denial handled gracefully (no silent retry/tracking) |
| T-03-03-02 | Tampering (XSS) | incident popup HTML (Phase-5 data) | mitigate | Escape title/outlet before interpolating into bindPopup HTML (or build via textContent); never dangerouslySetInnerHTML; downstream note for Phase 5 |
| T-03-03-03 | Tampering | external incident source links | mitigate | All incident URLs open with target="_blank" rel="noopener noreferrer" |
| T-03-03-04 | Denial of Service | ray-cast PiP over 346 features | accept | O(features x ring) is < 2ms in browser (RESEARCH A2); negligible on locate only |
</threat_model>

<verification>
- `cd site && npx astro check` exits 0 after each code task.
- `cd site && npm run build && node scripts/validate/all.mjs` exits 0 (map.mjs included).
- Manual UAT (Task 3) covers the six interaction/device checks that static HTML cannot assert.
</verification>

<success_criteria>
- "Mostrar mi ubicación" identifies + highlights the user's commune via Geolocation API + point-in-polygon; denial degrades gracefully (MAP-05).
- The events toggle shows incident pins with source + date when incidents/current.json is present, and a graceful empty state when absent (MAP-04).
- Manual UAT confirms MAP-01..06 including mid-range mobile / 3G performance (SC3); content pages remain React/Leaflet-free.
</success_criteria>

<output>
Create `.planning/phases/03-leaflet-map-island/03-03-SUMMARY.md` when done, including the manual UAT outcome and the documented incidents/current.json schema for Phase 5.
</output>
