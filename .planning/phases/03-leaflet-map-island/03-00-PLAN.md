---
phase: 03-leaflet-map-island
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - site/package.json
  - site/astro.config.mjs
  - data/cead/geo/communes.topo.json
  - scripts/build-topojson.mjs
  - site/scripts/validate/map.mjs
  - site/scripts/validate/all.mjs
autonomous: true
requirements: [MAP-06]
must_haves:
  truths:
    - "React/Leaflet toolchain installs cleanly on React 19 (no react-leaflet)"
    - "A 346-commune boundary TopoJSON exists under 100 KB, keyed by CUT"
    - "Content pages (commune/region/crime) still ship zero React/Leaflet JS after adding react()"
    - "map.mjs validator runs in the suite and asserts the TopoJSON budget"
  artifacts:
    - path: "data/cead/geo/communes.topo.json"
      provides: "346-commune boundary geometry, CUT-keyed, <100 KB TopoJSON"
      contains: "Topology"
    - path: "scripts/build-topojson.mjs"
      provides: "Repeatable offline geometry simplify+convert+CUT-rekey pipeline"
      min_lines: 30
    - path: "site/scripts/validate/map.mjs"
      provides: "Build-output assertions for map pages + TopoJSON budget + zero-React regression"
      min_lines: 30
  key_links:
    - from: "site/astro.config.mjs"
      to: "@astrojs/react"
      via: "integrations array"
      pattern: "react\\(\\)"
    - from: "site/scripts/validate/all.mjs"
      to: "map.mjs"
      via: "VALIDATORS array"
      pattern: "map\\.mjs"
---

<objective>
Install the React + Leaflet toolchain into the Phase-2 Astro project (without react-leaflet — incompatible with React 19), produce the committed 346-commune boundary TopoJSON under 100 KB keyed by CUT, and add the `map.mjs` build-output validator (including a zero-React-JS regression guard for the 740 content pages).

Purpose: Every feature plan in Waves 1–3 depends on the integration, the geometry asset, and the validator existing first. This is the Wave-0 infra gate from 03-VALIDATION.md.
Output: Updated `package.json` + `astro.config.mjs`, `scripts/build-topojson.mjs`, `data/cead/geo/communes.topo.json`, `site/scripts/validate/map.mjs`, and `map.mjs` registered in `all.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-leaflet-map-island/03-RESEARCH.md
@.planning/phases/03-leaflet-map-island/03-PATTERNS.md
@.planning/phases/03-leaflet-map-island/03-VALIDATION.md

<critical_finding>
RESEARCH/CONTEXT state that `ischilesafe.com/geo-data.js` features are keyed by CUT and can be simplified to a 346-commune <100 KB TopoJSON (D-02). This is FALSE on inspection:
- `geo-data.js` (window.CSM_GEO) contains only 56 features, keyed by slug (`properties.id = "santiago"`), NOT CUT (`"13101"`).
- `map-payload.json` comunas are keyed by CUT (`id = "13101"`). SC1 requires the choropleth to render all 346 communes.
The prototype geometry is therefore PROVABLY UNUSABLE for the 346-commune requirement. D-02's own escape hatch applies: "Do NOT re-download a fresh dataset unless the prototype geometry proves unusable." It is unusable. This plan sources a real 346-commune boundary dataset and keys it by CUT. Record this as a planned deviation from D-02 in the SUMMARY.
</critical_finding>

<interfaces>
From data/cead/meta/index.json (346 entries — authoritative slug/name/region to CUT map):
  Array of { cut: string; name: string; slug: string; region_id: string; population: number; low_population: boolean }
  e.g. { "cut":"5602", "name":"Algarrobo", "slug":"algarrobo", "region_id":"56" }

From data/cead/map-payload.json (choropleth join target — keyed by CUT):
  { generated: string; year: number; comunas: Array of { id: string (CUT); rate: number; level: 1|2|3|4|5; by_family: number[7] } }

Each final TopoJSON feature MUST carry properties.id = CUT (string) so the island can join feature to payload by feature.properties.id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React/Leaflet toolchain and add react() integration (no react-leaflet)</name>
  <read_first>
    - site/package.json (current deps: astro, @astrojs/sitemap, chroma-js, topojson-client)
    - site/astro.config.mjs (existing integrations array + sitemap filter — extend, do NOT replace)
    - .planning/phases/03-leaflet-map-island/03-RESEARCH.md section Standard Stack + Critical Version Correction (react-leaflet) + Pattern 1
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md section site/astro.config.mjs
  </read_first>
  <action>
    In site/: install runtime deps @astrojs/react@5.0.7 react@19 react-dom@19 leaflet@1.9.4, and dev types @types/leaflet @types/react @types/react-dom. Do NOT install react-leaflet — react-leaflet@4 peerDep is React ^18 (breaks on React 19) and D-05 mandates native L.geoJSON anyway; use pure L.map(). Record "react-leaflet intentionally omitted (React 19 incompatibility); CLAUDE.md 'pin to v4' superseded by RESEARCH" as a deviation in the SUMMARY. Edit astro.config.mjs: add import react from '@astrojs/react' and place react() first in the integrations array, before the existing sitemap(...) call (leave all sitemap config untouched). In the existing sitemap filter function, add /map/ and /es/mapa/ to the always-included paths before the default fallback (Open Question 2 in RESEARCH resolved: include map pages in sitemap).
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - npx astro check exits 0.
    - site/package.json dependencies include @astrojs/react, react, react-dom, leaflet; devDependencies include @types/leaflet, @types/react, @types/react-dom; react-leaflet is absent.
    - site/astro.config.mjs integrations array contains react() before sitemap(; sitemap filter returns true for /map/ and /es/mapa/.
  </acceptance_criteria>
  <done>React 19 + Leaflet toolchain installed, react() wired, astro check green, no react-leaflet.</done>
</task>

<task type="auto">
  <name>Task 2: Build 346-commune CUT-keyed TopoJSON under 100 KB via offline pipeline</name>
  <read_first>
    - data/cead/meta/index.json (346-entry slug/name/region to CUT map — the rekey source of truth)
    - data/cead/map-payload.json (CUT join target; confirm "13101" = Santiago resolves)
    - ischilesafe.com/geo-data.js (prototype geometry — 56 features, slug-keyed; reference only, INSUFFICIENT for 346)
    - .planning/phases/03-leaflet-map-island/03-RESEARCH.md Pattern 4 (mapshaper command) + Pitfall 4 (CUT join mismatch)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md section data/cead/geo/communes.topo.json
  </read_first>
  <action>
    Create scripts/build-topojson.mjs (repo root, ES module) as the repeatable offline geometry pipeline. It must: (a) obtain a 346-commune Chilean comuna boundary GeoJSON — preferred source the public BCN / chilemapas comunas dataset (e.g. a comunas.geojson with a cod_comuna / COD_COMUNA property); if such a file is already present in the repo or can be fetched once, use it, otherwise document the exact download URL used in the script header and commit only the simplified output. (b) Re-key each feature so properties.id = CUT (string, no zero-padding, matching index.json cut): map the source dataset commune code to CUT directly when it already equals the CEAD CUT, else join via index.json by name/slug; also set properties.name from index.json. Drop all other properties to keep size down. (c) Run mapshaper simplification: mapshaper input.geojson -simplify visvalingam percentage=12% keep-shapes -o format=topojson data/cead/geo/communes.topo.json. Tune the percentage downward only if needed to stay under 100 KB while keeping shapes recognizable (Claude's discretion per D-02). (d) Assert feature count is 346 and that properties.id for Santiago equals "13101" before writing. Commit data/cead/geo/communes.topo.json. If a 346-commune source genuinely cannot be obtained in the execution environment, STOP and surface a checkpoint — do NOT ship the 56-feature prototype geometry as a substitute (it fails SC1).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='data/cead/geo/communes.topo.json';const s=fs.statSync(p).size;if(s>=102400)throw new Error('TopoJSON '+s+' bytes >= 100KB');const t=JSON.parse(fs.readFileSync(p,'utf8'));if(t.type!=='Topology')throw new Error('not TopoJSON');const obj=t.objects[Object.keys(t.objects)[0]];const feats=obj.geometries||[];if(feats.length!==346)throw new Error('feature count '+feats.length+' != 346');const sgo=feats.find(f=>f.properties&&f.properties.id==='13101');if(!sgo)throw new Error('CUT 13101 not found');console.log('OK',s,'bytes,',feats.length,'communes')"</automated>
  </verify>
  <acceptance_criteria>
    - data/cead/geo/communes.topo.json exists, is valid TopoJSON (type Topology), and is < 102400 bytes.
    - The geometry object contains exactly 346 features.
    - At least Santiago (CUT 13101) is present with properties.id === "13101" (string), confirming the CUT join key survives simplification.
    - scripts/build-topojson.mjs documents its geometry source URL and is re-runnable.
  </acceptance_criteria>
  <done>346-commune CUT-keyed TopoJSON committed under 100 KB; pipeline script repeatable and documented.</done>
</task>

<task type="auto">
  <name>Task 3: Add map.mjs validator (map pages + TopoJSON budget + zero-React regression guard) and register it</name>
  <read_first>
    - site/scripts/validate/structure.mjs (validator pattern: REPO_ROOT/SITE_ROOT/DIST_DIR resolution, early-exit if dist absent, node:assert/strict)
    - site/scripts/validate/all.mjs (VALIDATORS array registration pattern)
    - .planning/phases/03-leaflet-map-island/03-VALIDATION.md section Wave 0 Requirements (validator named map.mjs)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md section site/scripts/validate/map-pages.mjs (assertion list — use filename map.mjs to match VALIDATION.md)
  </read_first>
  <action>
    Create site/scripts/validate/map.mjs following the structure.mjs pattern (resolve REPO_ROOT/SITE_ROOT/DIST_DIR via fileURLToPath; node:assert/strict; print a PASS line per check; early-exit 0 if dist/ absent). Assertions: (1) TopoJSON budget — data/cead/geo/communes.topo.json exists and size < 102400 bytes (this check runs even without dist/, since it is a committed asset). (2) When dist/ exists: dist/map/index.html and dist/es/mapa/index.html exist; each contains an island mount marker (astro-island element or MapIsland reference); EN page HTML contains the locate aria-label "Show my location" and ES page contains "Mostrar mi ubicaci". (3) Zero-React regression guard: pick a sample content page (e.g. dist/commune/santiago/index.html or first commune dir found) and assert its HTML does NOT contain "astro-island" and does NOT reference leaflet (no leaflet css/js URL) — proving content pages stay React/Leaflet-free after adding react(). Register the validator by appending 'map.mjs' to the VALIDATORS array in all.mjs and updating its header comment.
  </action>
  <verify>
    <automated>cd site && node scripts/validate/map.mjs</automated>
  </verify>
  <acceptance_criteria>
    - site/scripts/validate/map.mjs exists and exits 0 (TopoJSON budget check passes; HTML checks skipped gracefully if dist/ absent).
    - 'map.mjs' appears in the VALIDATORS array of site/scripts/validate/all.mjs.
    - The validator includes an explicit zero-React-JS assertion against a content page (no astro-island, no leaflet reference).
  </acceptance_criteria>
  <done>map.mjs validates TopoJSON budget, map-page presence, locate aria-labels, and content-page React-freeness; registered in the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling to repo | mapshaper/topojson output is committed as a static asset; no runtime trust crossing |
| external geometry source to repo | one-time fetch of a public boundary dataset; verified by 346-count + CUT join assertion before commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Tampering | npm installs (react/leaflet/@astrojs/react) | accept | All packages registry-verified in RESEARCH Package Legitimacy Audit (no [SLOP]/[SUS]); high-trust org sources |
| T-03-00-02 | Tampering | external boundary geometry dataset | mitigate | Re-key only id+name from index.json; drop all other source properties; assert 346 count + Santiago CUT before commit |
| T-03-00-SC | Tampering | npm/pip installs | mitigate | RESEARCH audit table present and clean; no [ASSUMED]/[SUS] packages requiring a blocking checkpoint |
</threat_model>

<verification>
- `cd site && npx astro check` exits 0 after install + integration.
- TopoJSON: 346 features, CUT-keyed, < 100 KB (Task 2 verify command).
- `cd site && node scripts/validate/map.mjs` exits 0.
- After a full `npm run build`, content pages contain no astro-island/leaflet (regression guard inside map.mjs).
</verification>

<success_criteria>
- React 19 + Leaflet 1.9.x + @astrojs/react installed; react-leaflet absent; react() wired into astro.config.mjs.
- `data/cead/geo/communes.topo.json` committed: 346 communes, CUT-keyed, < 100 KB (satisfies MAP-06 boundary budget).
- `map.mjs` validator registered in all.mjs, asserting the TopoJSON budget and the zero-React content-page regression guard.
</success_criteria>

<output>
Create `.planning/phases/03-leaflet-map-island/03-00-SUMMARY.md` when done. Record the D-02 deviation (prototype geometry unusable; sourced a 346-commune dataset) and the react-leaflet omission.
</output>
