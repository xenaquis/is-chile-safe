---
phase: 09-ux-readability-accessibility-polish
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - site/src/pages/es/metodologia.astro
  - site/src/pages/is-santiago-safe.astro
  - site/src/pages/is-chile-safe.astro
  - site/src/pages/safest-cities-in-chile.astro
autonomous: true
requirements: [READ-02, READ-03, UX-01, UX-02, READ-01]
must_haves:
  truths:
    - "The ES methodology page has content depth equivalent to the EN methodology page across all 6 shared sections"
    - "The top editorial money pages have exactly one H1 with a scannable H2/H3 hierarchy"
    - "No statistic is stated both in a DataCallout and verbatim in the adjacent prose paragraph on the audited pages"
    - "No audited editorial page uses absolute 'peligroso/seguro/dangerous/safe' framing; comparisons use relative wording and cite CEAD + year"
  artifacts:
    - path: "site/src/pages/es/metodologia.astro"
      provides: "ES methodology at content parity with EN"
      min_lines: 130
  key_links:
    - from: "site/src/pages/es/metodologia.astro"
      to: "site/src/pages/methodology.astro"
      via: "section-for-section content parity (same 6 H2 topics, equivalent depth)"
      pattern: "<h2"
---

<objective>
Close the editorial readability gaps the Phase 7 review surfaced: bring the ES methodology to content parity with EN (READ-02 / F-003), and run a heading + callout-redundancy + sober-tone audit pass over the top editorial money pages (UX-01, UX-02, READ-03), with prose-rhythm already handled globally in Plan 01 (READ-01).

Purpose: READ-02 is the one confirmed structural parity gap (ES methodology ~6.9KB vs EN ~13.4KB — roughly half the prose per section). The audit pass enforces single-H1 scannability, removes any callout↔prose duplication, and verifies relative-only framing. Disjoint files from the comprehension plans (02/03), so it runs in the same wave with zero file conflict.
Output: an expanded ES methodology page and audited/cleaned top editorial pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ux-readability-accessibility-polish/09-CONTEXT.md
@.planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md
@.planning/phases/09-ux-readability-accessibility-polish/09-PATTERNS.md
@.planning/REVIEW-E2E-FINDINGS.md

<interfaces>
<!-- The parity target and audited pages -->

EN methodology (parity target) site/src/pages/methodology.astro — 6 H2 sections:
  "Data source: CEAD", "How the rate is calculated", "Underreporting (cifra negra)",
  "Trend formula", "Comparison criteria", "What this site does NOT say".

ES methodology site/src/pages/es/metodologia.astro — SAME 6 H2 sections already present
  but each is roughly half the EN depth. The gap is per-section content depth, not missing sections.

Top editorial money pages to audit (heading + callout redundancy + tone):
  is-santiago-safe.astro, is-chile-safe.astro, safest-cities-in-chile.astro.

DataCallout.astro renders value + label + source (CEAD). Redundancy = the same figure repeated
  verbatim in an adjacent <p>.

Editorial constraint (site-wide, non-negotiable): no absolute "peligroso/seguro/dangerous/safe";
  use "incidencia reportada" / "reported incidence" / "X× the average"; always cite CEAD + year.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring ES methodology to content parity with EN (READ-02 / F-003)</name>
  <files>site/src/pages/es/metodologia.astro</files>
  <read_first>
    - site/src/pages/methodology.astro (EN canonical — read all 6 sections in full to mirror depth/structure)
    - site/src/pages/es/metodologia.astro (current ES — the 6 thinner sections to expand)
    - .planning/REVIEW-E2E-FINDINGS.md (F-003 — ES ~4,174 chars vs EN ~8,849 body chars)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (READ-02 lines 691-693)
  </read_first>
  <action>
    For each of the 6 shared H2 sections in es/metodologia.astro ("Fuente de datos: CEAD", "Cómo se calcula la tasa", "Subregistro (cifra negra)", "Fórmula de tendencia", "Criterios de comparación", "Lo que este sitio NO dice"), expand the Spanish prose so each section conveys the SAME substance as its EN counterpart in methodology.astro — translate/port the missing explanatory content (formulas, caveats, examples, the rate-per-100k definition, the cifra-negra discussion, the trend-formula description, the comparison criteria, and the explicit "what we do NOT say" disclaimers). Keep it natural Spanish, not a literal machine translation. Preserve all existing CEAD attributions. Maintain exactly one H1 and the existing 6 H2 structure (add H3s only if the EN page uses them within a section). Do NOT introduce absolute "peligroso/seguro" framing — keep the relative/sober tone the EN page uses ("incidencia reportada", "tasa por 100.000", "comparación relativa"). Target body content depth within ~15% of the EN page (EN body ≈ 8,849 chars / file ≈ 13.4KB).
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ES methodology file size is within ~15% of EN: after edit, `wc -c site/src/pages/es/metodologia.astro` returns ≥ 11,000 bytes (EN is ~13,419; ES was ~6,917). This is a proxy for content parity.
    - Exactly one H1: `grep -c "<h1" site/src/pages/es/metodologia.astro` returns 1.
    - All 6 H2 sections retained: `grep -c "<h2" site/src/pages/es/metodologia.astro` returns 6.
    - No absolute-tone words: `grep -ni "peligroso\|dangerous" site/src/pages/es/metodologia.astro` returns nothing; any "seguro"/"safe" match is inside a neutral phrase (verify manually).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>ES methodology covers the same six topics at equivalent depth to EN; one H1; relative tone preserved; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Heading hierarchy + callout-redundancy + sober-tone audit on top editorial pages</name>
  <files>site/src/pages/is-santiago-safe.astro, site/src/pages/is-chile-safe.astro, site/src/pages/safest-cities-in-chile.astro</files>
  <read_first>
    - site/src/pages/is-santiago-safe.astro (full)
    - site/src/pages/is-chile-safe.astro (full)
    - site/src/pages/safest-cities-in-chile.astro (full)
    - site/src/components/DataCallout.astro (to recognise callout markup and its value/label/source fields)
    - .planning/REVIEW-E2E-FINDINGS.md (UX-01 single-H1 confirmed; F-005/F-001 already fixed in Phase 8 — do not redo)
    - .planning/phases/09-ux-readability-accessibility-polish/09-RESEARCH.md (UX-01 lines 702-704; UX-02 redundancy lines 706-708; READ-03 tone lines 695-700)
  </read_first>
  <action>
    For each of the three editorial pages perform a content-quality audit and apply fixes only where a defect exists (this is a polish pass, not a rewrite):
    (a) UX-01: confirm exactly one `<h1>` and that H2/H3 form a scannable outline (no skipped levels, no H3 before an H2). Fix any level mis-nesting; do not add/remove sections gratuitously.
    (b) UX-02: find any statistic that appears BOTH inside a DataCallout (value+label+source) AND verbatim in the adjacent prose paragraph; remove the redundant restatement from the prose (keep the callout, which carries the CEAD source) OR consolidate so each figure is stated once. Each callout must still communicate one clear idea with its CEAD + year source.
    (c) READ-03: verify no absolute "peligroso/seguro/dangerous/safe" framing; every cited stat has CEAD + year; comparisons use relative wording. Fix any violation found.
    Make minimal, surgical edits. Do not touch the F-001 (ES "Comuna") or F-005 (rollout gating) items — those were resolved in Phase 8.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && cd site && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Each audited page has exactly one H1: `grep -c "<h1" site/src/pages/is-santiago-safe.astro` returns 1 (same for the other two).
    - No absolute-safety phrasing introduced or remaining in the audited pages: `grep -ni "is dangerous\|is safe\|es peligroso\|es seguro\|muy peligroso\|totalmente seguro" site/src/pages/is-santiago-safe.astro site/src/pages/is-chile-safe.astro site/src/pages/safest-cities-in-chile.astro` returns nothing.
    - The SUMMARY documents, per page, the audit result: H1 count, any callout redundancy removed (with the figure), any tone fix applied, or "no change needed — already compliant".
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Top editorial pages have one H1 each, a clean heading outline, no callout↔prose stat duplication, and verified sober/relative tone; build clean; audit results recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time source → static HTML | Editorial prose is authored content rendered to static HTML; no user input, no runtime data path changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering | editorial page prose | accept | Pure authored static content; no `set:html`, no user input, no new JSON-LD. No new attack surface vs. existing editorial pages. |
| T-09-09 | Repudiation | editorial claims | accept | Editorial constraint requires CEAD + year attribution on every stat — this plan enforces it, reducing (not adding) misattribution risk. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §"Package Legitimacy Audit": none). No install task; no slopcheck checkpoint required. |
</threat_model>

<verification>
- `cd site && npm run build` exits 0 (single chained command — OneDrive desync note).
- ES methodology byte size ≥ 11,000 (parity proxy); one H1; 6 H2s.
- Audited editorial pages: one H1 each; no absolute-tone phrasing.
- READ-01 prose rhythm is delivered globally by Plan 01's `.editorial-prose p { margin-bottom }` — no per-page CSS needed here.
</verification>

<success_criteria>
- ES methodology at content parity with EN across all 6 sections (READ-02).
- Top editorial pages: single H1, scannable headings (UX-01), no callout↔prose stat duplication (UX-02), sober relative tone (READ-03).
- Site builds clean.
</success_criteria>

<output>
Create `.planning/phases/09-ux-readability-accessibility-polish/09-04-SUMMARY.md` when done
</output>
