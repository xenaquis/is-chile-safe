---
phase: 08-bug-fixes-data-correctness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/PageHeader.astro
  - site/src/config/i18n.ts
autonomous: true
requirements: [BUGFIX-05]
must_haves:
  truths:
    - "At mobile width (<640px) a hamburger toggle is visible in the header"
    - "Toggling the hamburger reveals Home / Map / Methodology links"
    - "The Map (/map/, /es/mapa/) is reachable from header chrome on mobile in both locales"
    - "Desktop (>=640px) header layout is unchanged"
    - "The toggle uses zero client:* directives (CSS-only checkbox pattern, D-09)"
  artifacts:
    - path: "site/src/components/PageHeader.astro"
      provides: "Zero-JS hamburger mobile nav (checkbox + :checked sibling CSS)"
      contains: "nav-toggle"
    - path: "site/src/config/i18n.ts"
      provides: "nav_menu_open string in EN_STRINGS and ES_STRINGS"
      contains: "nav_menu_open"
  key_links:
    - from: "PageHeader.astro nav-toggle checkbox"
      to: ".csm-nav dropdown"
      via: ":checked ~ .csm-nav CSS sibling selector"
      pattern: "nav-toggle-input:checked ~ \\.csm-nav"
---

<objective>
Fix F-009 (D-04): at mobile width the header nav (Home / Map / Methodology) is `display:none` with no toggle, so the core Map is unreachable from site chrome on a phone. Add a zero-JS hamburger (hidden `<input type="checkbox">` + CSS `:checked` sibling selector) to PageHeader.astro — no `client:*` directive (D-09 forbids it). Desktop layout (>=640px) stays unchanged. Add the required `nav_menu_open` i18n string to both locales.

Purpose: the Map is the product's core value; a missing mobile path to it is the highest-impact remaining mobile UX gap. The lightest pattern consistent with the codebase's zero-client-directive rule.
Output: PageHeader.astro with a working mobile hamburger; i18n.ts with one new string in both dictionaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes-data-correctness/08-CONTEXT.md
@.planning/phases/08-bug-fixes-data-correctness/08-PATTERNS.md
@.planning/REVIEW-E2E-FINDINGS.md

<interfaces>
<!-- PageHeader.astro: t = locale === 'en' ? EN_STRINGS : ES_STRINGS; uses t.nav_home, t.nav_map, t.nav_methodology -->
<!-- .csm-nav is display:none by default, display:flex only at @media (min-width: 640px) -->
<!-- i18n.ts: I18nStrings interface + EN_STRINGS + ES_STRINGS exports -->
<!-- The mobile breakpoint across the codebase is 640px. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nav_menu_open i18n string (EN + ES)</name>
  <read_first>
    - site/src/config/i18n.ts (I18nStrings interface — locate `nav_methodology`; EN_STRINGS and ES_STRINGS objects)
    - site/src/components/PageHeader.astro lines 17-20 (the `const t = locale === 'en' ? EN_STRINGS : ES_STRINGS` usage)
  </read_first>
  <action>
    In site/src/config/i18n.ts: add a new member `nav_menu_open: string;` to the `I18nStrings` interface (place it immediately after `nav_methodology`). Add `nav_menu_open: 'Open navigation menu',` to `EN_STRINGS` and `nav_menu_open: 'Abrir menú de navegación',` to `ES_STRINGS`, each next to the existing `nav_methodology` entry so both dictionaries stay structurally aligned. Do not rename or reorder existing keys.
  </action>
  <verify>
    <automated>cd site && node -e "const fs=require('fs');const s=fs.readFileSync('src/config/i18n.ts','utf8');if(!/nav_menu_open\\s*:\\s*string/.test(s))throw new Error('I18nStrings missing nav_menu_open');const en=(s.match(/nav_menu_open:\\s*'Open navigation menu'/g)||[]).length;const es=(s.match(/nav_menu_open:\\s*'Abrir menú de navegación'/g)||[]).length;if(en<1||es<1)throw new Error('missing EN or ES nav_menu_open value (en='+en+' es='+es+')');console.log('nav_menu_open OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `I18nStrings` interface declares `nav_menu_open: string`.
    - `EN_STRINGS.nav_menu_open === 'Open navigation menu'` and `ES_STRINGS.nav_menu_open === 'Abrir menú de navegación'`.
    - No existing i18n key removed or reordered.
  </acceptance_criteria>
  <done>nav_menu_open exists in the interface and both dictionaries with correct locale values.</done>
</task>

<task type="auto">
  <name>Task 2: Zero-JS hamburger mobile nav in PageHeader.astro (F-009)</name>
  <read_first>
    - site/src/components/PageHeader.astro (full file — header structure lines 29-63, `.csm-nav` CSS lines 96-106, the 640px media queries)
    - site/src/config/i18n.ts (confirm nav_menu_open from Task 1 is available on `t`)
    - .planning/phases/08-bug-fixes-data-correctness/08-PATTERNS.md §F-009 (exact checkbox + label markup and the full CSS block to add)
    - .planning/REVIEW-E2E-FINDINGS.md §F-009 (resolution: Home/Map/Methodology reachable on mobile)
  </read_first>
  <action>
    In site/src/components/PageHeader.astro, implement the zero-JS hamburger per 08-PATTERNS.md §F-009:
    (1) Insert, between the wordmark `<a>` and the `<nav class="csm-nav">`, a hidden checkbox `<input type="checkbox" id="nav-toggle" class="nav-toggle-input" aria-hidden="true" />` and a `<label for="nav-toggle" class="nav-toggle-btn" aria-label={t.nav_menu_open}>` containing three `<span class="burger-bar"></span>` elements.
    (2) Add the CSS from 08-PATTERNS.md §F-009 to the component's `<style>`: hide `.nav-toggle-input`; show `.nav-toggle-btn` as a 24×18 flex column only below 640px and `display:none` at `@media (min-width:640px)`; style `.burger-bar` (2px bars, `var(--ink)`); the `.nav-toggle-input:checked ~ .csm-nav` rule that drops the nav as an absolute-positioned dropdown below the header (`top: var(--3xl)`, `background: var(--card)`, `border-bottom: 1px solid var(--line)`, `z-index: 99`); and the `@media (min-width:640px)` override that resets `.nav-toggle-input:checked ~ .csm-nav` to `position:static; flex-direction:row; border-bottom:none; padding:0` so DESKTOP layout is unchanged.
    Use the `nav_menu_open` string from `t` (Task 1). The label's `aria-label={t.nav_menu_open}` is a trusted dictionary literal — Astro auto-escapes the attribute; do not use `set:html`. Do NOT add any `client:*` directive (D-09). Do NOT change the existing nav links, wordmark, or lang-toggle. Verify the breakpoint is 640px (the codebase standard) in every media query you add.
  </action>
  <verify>
    <automated>cd site && npm run build && node -e "const fs=require('fs');const s=fs.readFileSync('src/components/PageHeader.astro','utf8');for(const t of ['nav-toggle-input','nav-toggle-btn','burger-bar','t.nav_menu_open']){if(!s.includes(t))throw new Error('PageHeader missing '+t);}if(!/nav-toggle-input:checked\\s*~\\s*\\.csm-nav/.test(s))throw new Error('missing :checked sibling rule');if(/client:/.test(s))throw new Error('client:* directive present — D-09 violation');if(!/min-width:\\s*640px/.test(s))throw new Error('640px breakpoint missing');const html=fs.readFileSync('dist/index.html','utf8');if(!/nav-toggle-btn/.test(html))throw new Error('hamburger label not in built home HTML');console.log('F-009 OK');"</automated>
  </verify>
  <acceptance_criteria>
    - PageHeader.astro contains the `.nav-toggle-input` checkbox, `.nav-toggle-btn` label (with `aria-label={t.nav_menu_open}`), and three `.burger-bar` spans.
    - CSS includes a `.nav-toggle-input:checked ~ .csm-nav` rule (mobile dropdown) AND a `@media (min-width: 640px)` override restoring static/row desktop layout.
    - Zero `client:` directives in PageHeader.astro (D-09 honored).
    - Built `dist/index.html` contains `nav-toggle-btn` (the toggle ships in static HTML) and the three nav links (Home/Map/Methodology) remain present in the nav.
    - `npm run build` succeeds (chained — OneDrive dist/ desync).
    - (Optional, not required) inline BrowserOS spot-check at 375px: `.nav-toggle-btn` has `offsetParent !== null`; if run, use a no-spaces screenshot path and run inline (not in executor).
  </acceptance_criteria>
  <done>A CSS-only hamburger reveals Home/Map/Methodology at mobile width; desktop unchanged; no client directives; build passes with the toggle in static HTML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static header markup | New nav markup is pure HTML/CSS; the only dynamic value is `t.nav_menu_open` (trusted dictionary literal). No JS, no user input, no network input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering (XSS) | hamburger label aria-label interpolation | mitigate | `aria-label={t.nav_menu_open}` interpolates a static EN/ES dictionary literal; Astro auto-escapes attribute values. No `set:html`, no user/network input introduced by the new markup. |
| T-08-05 | Denial of Service | CSS-only toggle | accept | Pure CSS `:checked` sibling toggle; no JS, no event handlers, no resource amplification. |
</threat_model>

<verification>
Chained build+grep (OneDrive dist/ desync — one command):
- Source has the checkbox/label/burger markup, the `:checked ~ .csm-nav` rule, a 640px desktop override, and zero `client:` directives.
- Built `dist/index.html` contains `nav-toggle-btn` and still lists the three nav links.
- Optional inline BrowserOS 375px check (no-spaces screenshot path) — not required for pass.
</verification>

<success_criteria>
F-009 resolved: a zero-JS hamburger exposes Home/Map/Methodology (and thus /map/ + /es/mapa/) at mobile width in both locales; desktop layout unchanged; no client directives; build passes.
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes-data-correctness/08-03-SUMMARY.md` when done.
</output>
