---
phase: 08-bug-fixes-data-correctness
plan: 05
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - .planning/phases/08-bug-fixes-data-correctness/08-BUGFIX-CLOSEOUT.md
autonomous: true
requirements: [BUGFIX-01, BUGFIX-03, BUGFIX-05]
must_haves:
  truths:
    - "Console is confirmed clean on the 10 EN editorial money pages + /map/ (build clean; any remaining warning triaged in writing)"
    - "Internal links resolve without 404 after F-005 gating; hreflang reciprocal pairs confirmed present"
    - "Commune/region rates use loadNationalAverage (per-100k MEAN, not a raw sum); ranking is by rate/100k; >=3 communes spot-checked against data/cead/"
    - "A per-finding ledger records F-001/F-007/F-009 resolved (with re-verification notes) and F-002/F-003/F-004/F-006/F-008 deferred to Phase 9 each with a one-line reason"
  artifacts:
    - path: ".planning/phases/08-bug-fixes-data-correctness/08-BUGFIX-CLOSEOUT.md"
      provides: "Console re-check, data spot-check, and the BUGFIX-05 finding-resolution/deferral ledger"
      min_lines: 40
  key_links:
    - from: "rate display path"
      to: "loadNationalAverage (mean of per-100k rates)"
      via: "data.ts lines 182-190 sum/count = mean"
      pattern: "loadNationalAverage"
---

<objective>
Close out Phase 8 verification: re-confirm console is clean on money pages (BUGFIX-01), verify rate methodology + spot-check >=3 communes against `data/cead/` (BUGFIX-03), and record the BUGFIX-05 per-finding ledger (F-001/F-007/F-009 resolved with re-verification notes; F-002/F-003/F-004/F-006/F-008 deferred to Phase 9 each with a one-line reason — D-05). Verification is build + grep + spot-check, NO BrowserOS dependency (D-08/D-09).

This plan depends on 08-01..04 (it verifies their output). It writes a single closeout document and makes no source-code changes.

Purpose: prove every Phase-8 success criterion holds and leave an auditable ledger of what was fixed vs deferred.
Output: 08-BUGFIX-CLOSEOUT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes-data-correctness/08-CONTEXT.md
@.planning/REVIEW-E2E-FINDINGS.md
@.planning/phases/08-bug-fixes-data-correctness/08-01-SUMMARY.md
@.planning/phases/08-bug-fixes-data-correctness/08-02-SUMMARY.md
@.planning/phases/08-bug-fixes-data-correctness/08-03-SUMMARY.md
@.planning/phases/08-bug-fixes-data-correctness/08-04-SUMMARY.md

<interfaces>
<!-- site/src/lib/data.ts loadNationalAverage() lines 182-190: -->
<!--   rates = eligible communes' latestCompleteYearRate (per-100k); sum/count = MEAN. NOT a raw count sum. -->
<!-- REVIEW-02 already confirmed rate sanity PASS: national mean 5,808/100k; Santiago 9,309/100k #11 of 346. -->
<!-- data/cead/ holds the per-commune source JSON to spot-check against. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify console-clean money pages + internal-link/hreflang integrity (BUGFIX-01, BUGFIX-02 link check)</name>
  <read_first>
    - .planning/REVIEW-E2E-FINDINGS.md §REVIEW-01 (the 10 EN editorial pages + /map/ were already console-clean; this is a re-check after fixes)
    - .planning/phases/08-bug-fixes-data-correctness/08-01-SUMMARY.md (F-005 gating outcome — which links were removed)
    - site/package.json (build + any existing validator scripts, e.g. validate/check scripts)
  </read_first>
  <action>
    Build once and run the existing validators in ONE chained command (OneDrive dist/ desync). Then, against the freshly built `dist/`:
    (1) BUGFIX-01 console re-check: there is no live browser here (D-08). Assert the build emitted no errors and that the existing Astro check / validators pass. For the 10 EN editorial money pages + /map/, confirm each built index.html exists and renders (non-empty `<main>`/`<h1>`). REVIEW-01 already recorded zero console errors/warnings on these pages; record that as the baseline and note that no new client-side code was added by 08-02/08-03/08-04 (PageHeader hamburger is CSS-only, AdSlot change is CSS-only, ResultPanel change is an aria-label literal) — so no new console surface was introduced. Triage in writing any validator warning that appears.
    (2) BUGFIX-02 link integrity: grep the built region + crime HTML to confirm the previously-404 examples from F-005 (`/commune/cerrillos/`, `/commune/recoleta/`, `/es/comuna/cerrillos/`) no longer appear as `href`s in displayed ranking rows. Confirm hreflang reciprocal pairs are present on a sample money page (each page has hreflang en/es/x-default per REVIEW-01) — grep the built HTML for 3 `hreflang=` link tags.
    Record all command outputs and the pass/fail verdict into 08-BUGFIX-CLOSEOUT.md (created in Task 3).
  </action>
  <verify>
    <automated>cd site && npm run build && node -e "const fs=require('fs');const pages=['dist/index.html','dist/is-chile-safe/index.html','dist/is-santiago-safe/index.html','dist/map/index.html'];for(const p of pages){if(!fs.existsSync(p))throw new Error('missing built page '+p);const h=fs.readFileSync(p,'utf8');if(h.length<500)throw new Error('suspiciously empty '+p);}const home=fs.readFileSync('dist/index.html','utf8');if((home.match(/hreflang=/g)||[]).length<3)throw new Error('home missing 3 hreflang tags');console.log('console-baseline + hreflang OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` + existing validators pass in one chained command (no build errors).
    - The 10 EN editorial money pages + `/map/` built index.html files exist and are non-trivial; no new client-side code was added by Phase 8 (recorded with justification).
    - Built region/crime HTML contains zero `href="/commune/cerrillos/"`, `href="/commune/recoleta/"`, `href="/es/comuna/cerrillos/"` in displayed rows.
    - A sampled money page has 3 `hreflang=` link tags (en/es/x-default reciprocal).
    - Any remaining warning is triaged with a written justification in the closeout doc.
  </acceptance_criteria>
  <done>Console baseline re-confirmed (no new client surface), link integrity + hreflang verified; outputs captured for the closeout ledger.</done>
</task>

<task type="auto">
  <name>Task 2: Verify rate methodology + spot-check >=3 communes vs data/cead/ (BUGFIX-03)</name>
  <read_first>
    - site/src/lib/data.ts lines 178-211 (loadNationalAverage = MEAN of per-100k rates lines 182-190; loadRegionalAverage lines 200-211; latestCompleteYearRate)
    - .planning/REVIEW-E2E-FINDINGS.md §REVIEW-02 "Rate-vs-raw-count sanity" (national mean 5,808/100k; Santiago 9,309 #11/346; Concepción 7,256 #40; Punta Arenas 3,512 #250 — PASS baseline)
    - data/cead/ (per-commune source JSON — pick Santiago CUT 13101, Concepción 8101, Punta Arenas 12101 for spot-check)
  </read_first>
  <action>
    BUGFIX-03 is largely a verification (the review already passed rate sanity). (1) Confirm the rate display path uses `loadNationalAverage` and that it computes a MEAN (sum of per-100k rates / count), NOT a raw summation — assert via reading data.ts lines 182-190. (2) Confirm ranking is by rate per 100k (not raw frequency). (3) Spot-check >=3 communes (Santiago 13101, Concepción 8101, Punta Arenas 12101) by reading the matching `data/cead/` source JSON and confirming the latest-complete-year per-100k rate matches the figure rendered on the built commune page (e.g. Santiago ~9,309/100k) within rounding. Record each commune's CUT, source rate, displayed rate, and PASS/FAIL into the closeout doc. This task makes NO code changes — if a real discrepancy is found, record it as an escalation in the closeout (do NOT silently fix; flag for a follow-up plan).
  </action>
  <verify>
    <automated>cd site && node -e "const fs=require('fs');const d=fs.readFileSync('src/lib/data.ts','utf8');const fn=(d.match(/export function loadNationalAverage[\\s\\S]*?\\n\\}/)||[''])[0];if(!/sum\\s*\\/\\s*rates\\.length|rates\\.length\\s*>\\s*0\\s*\\?\\s*sum\\s*\\/\\s*rates\\.length/.test(fn))throw new Error('loadNationalAverage is not a mean (sum/count)');if(!/latestCompleteYearRate/.test(fn))throw new Error('loadNationalAverage does not use per-100k rates');console.log('rate methodology = MEAN of per-100k rates: OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `loadNationalAverage` proven to return `sum(rates)/count` (mean), where `rates` are per-100k `latestCompleteYearRate` values — not a raw count sum (asserted against data.ts source).
    - Ranking is by rate per 100k (confirmed in the ranking templates / data path).
    - >=3 communes (13101, 8101, 12101) spot-checked: source `data/cead/` per-100k rate matches the displayed rate within rounding; results tabulated in the closeout.
    - Any discrepancy is escalated in writing (not silently fixed).
  </acceptance_criteria>
  <done>Rate methodology verified as per-100k mean via loadNationalAverage; >=3 communes spot-checked against data/cead/ with a results table in the closeout.</done>
</task>

<task type="auto">
  <name>Task 3: Write the BUGFIX-05 finding ledger + closeout (resolved vs deferred)</name>
  <read_first>
    - .planning/REVIEW-E2E-FINDINGS.md (all findings F-001..F-009 with severity/recommendation)
    - .planning/phases/08-bug-fixes-data-correctness/08-CONTEXT.md §D-05 (the 5 Polish deferrals and their Phase-9 targets)
    - .planning/phases/08-bug-fixes-data-correctness/08-01-SUMMARY.md, 08-02-SUMMARY.md, 08-03-SUMMARY.md, 08-04-SUMMARY.md (resolution evidence)
  </read_first>
  <action>
    Create .planning/phases/08-bug-fixes-data-correctness/08-BUGFIX-CLOSEOUT.md. Include:
    (1) A header noting verification method = build + grep + spot-check, no BrowserOS (D-08/D-09).
    (2) The Task 1 console/link/hreflang results and the Task 2 rate spot-check table (paste the captured outputs).
    (3) A per-finding ledger table with columns Finding | Severity | Disposition | Evidence/Reason:
        - F-001 → Warning → RESOLVED → "ES H1 now 'Comuna'; built H1 verified (08-02)."
        - F-005 → Warning → RESOLVED → "Region/crime ranking rows gated to loadRolloutCuts(); 0 non-rollout 404 links (08-01)."
        - F-007 → Warning → RESOLVED → "ResultPanel close aria-label locale-aware EN 'Close'/ES 'Cerrar', 0 hardcoded 'Cerrar' (08-02)."
        - F-009 → Warning → RESOLVED → "CSS-only hamburger exposes Home/Map/Methodology at <640px; desktop unchanged (08-03)."
        - BUGFIX-04 (AdSlot) → RESOLVED → "Blank reserved-height placeholder, gate + zero adsbygoogle intact (08-04)."
        - F-002 → Polish → DEFERRED to Phase 9 → "cookie/accessibility legal pages = new pages + go-live gate, not a bug (D-05)."
        - F-003 → Polish → DEFERRED to Phase 9 → "ES methodology content parity = READ-02 (D-05)."
        - F-004 → Polish → DEFERRED to Phase 9 → "thin contact pages = content task (D-05)."
        - F-006 → Polish → DEFERRED to Phase 9 → "ES region display-name grammar = READ (D-05)."
        - F-008 → Polish → DEFERRED to Phase 9 → "incident 'coming soon' toast = UX-01 (D-05)."
    (4) A success-criteria checklist mapping all 5 Phase-8 ROADMAP criteria to PASS with evidence pointers.
    All 4 Warnings resolved; all 5 Polish deferred-with-reason; zero Critical (per REVIEW-E2E-FINDINGS).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/08-bug-fixes-data-correctness/08-BUGFIX-CLOSEOUT.md';const s=fs.readFileSync(p,'utf8');for(const id of ['F-001','F-005','F-007','F-009','F-002','F-003','F-004','F-006','F-008']){if(!s.includes(id))throw new Error('ledger missing '+id);}if(!/RESOLVED/.test(s)||!/DEFERRED/.test(s))throw new Error('ledger missing RESOLVED/DEFERRED dispositions');if(s.split(/\\r?\\n/).length<40)throw new Error('closeout too thin');console.log('closeout ledger OK');"</automated>
  </verify>
  <acceptance_criteria>
    - 08-BUGFIX-CLOSEOUT.md exists and references all 9 findings (F-001..F-009).
    - Each of F-001/F-005/F-007/F-009 marked RESOLVED with an evidence pointer to the relevant SUMMARY.
    - Each of F-002/F-003/F-004/F-006/F-008 marked DEFERRED to Phase 9 with a one-line reason (D-05).
    - All 5 Phase-8 ROADMAP success criteria mapped to PASS with evidence.
    - Document is >=40 lines (substantive, not a stub).
  </acceptance_criteria>
  <done>The closeout ledger records every finding's disposition, the console/link/rate verification results, and PASS for all 5 Phase-8 success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (read-only verification) | This plan reads built artifacts + source and writes one planning markdown doc. No code changes, no runtime, no user/network input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Repudiation | verification ledger | mitigate | The closeout records exact commands, outputs, and per-finding dispositions, giving an auditable trail of what was verified vs deferred (no silent passes). |
| T-08-08 | Tampering | (verification only) | accept | No source/markup changes; nothing rendered to users. If a rate discrepancy is found it is escalated in writing, never silently patched. |
</threat_model>

<verification>
Chained build+validate+grep (OneDrive dist/ desync — one command):
- Build + validators pass; money pages + /map/ built and non-trivial; hreflang ×3 present.
- Built region/crime HTML free of the previously-404 commune hrefs.
- loadNationalAverage asserted to be a per-100k mean; >=3 communes spot-checked vs data/cead/.
- Closeout ledger references all 9 findings with RESOLVED/DEFERRED dispositions and maps the 5 success criteria to PASS.
</verification>

<success_criteria>
BUGFIX-01/03/05 closed: console-clean baseline + link/hreflang integrity confirmed; rate methodology verified as per-100k mean with >=3 commune spot-checks; the per-finding ledger records all 4 Warnings resolved and all 5 Polish findings deferred-with-reason, with the 5 Phase-8 success criteria mapped to PASS.
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes-data-correctness/08-05-SUMMARY.md` when done (in addition to 08-BUGFIX-CLOSEOUT.md).
</output>
