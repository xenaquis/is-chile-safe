---
phase: 08-bug-fixes-data-correctness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/AdSlot.astro
autonomous: true
requirements: [BUGFIX-04]
must_haves:
  truths:
    - "With ADSENSE_ENABLED != 'true', the AdSlot reserved space renders BLANK (no dashed border/box)"
    - "The AdSlot still reserves its height (90px desktop / 50px mobile) — no CLS"
    - "With AdSense OFF, zero adsbygoogle elements appear in the DOM"
    - "The env gate remains an exact string compare ADSENSE_ENABLED === 'true'"
  artifacts:
    - path: "site/src/components/AdSlot.astro"
      provides: "Blank reserved-height placeholder (dashed border removed), env gate intact"
      contains: "ADSENSE_ENABLED === 'true'"
  key_links:
    - from: "AdSlot.astro enabled flag"
      to: "adsbygoogle render branch"
      via: "enabled && publisherId guard"
      pattern: "ADSENSE_ENABLED === 'true'"
---

<objective>
BUGFIX-04 / D-06: AdSlot already reserves height and renders no `adsbygoogle` when disabled — no real CLS exists (D-06). The only change: with AdSense OFF, render the reserved space as BLANK by removing the dashed `.ad-placeholder` border/box, while keeping the reserved height (clean pre-AdSense look, still zero CLS). Preserve the env-gating contract exactly (exact string compare `=== 'true'`, zero `adsbygoogle` in DOM when off) — verified, not redesigned (D-07).

Purpose: a finished/clean appearance before AdSense go-live without re-introducing layout shift.
Output: AdSlot.astro with the dashed-border styles removed and all gating invariants intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes-data-correctness/08-CONTEXT.md
@.planning/phases/08-bug-fixes-data-correctness/08-PATTERNS.md
@.planning/REVIEW-E2E-FINDINGS.md

<interfaces>
<!-- AdSlot.astro invariants to PRESERVE: -->
<!-- line ~18: const enabled = import.meta.env.ADSENSE_ENABLED === 'true';  (exact string compare) -->
<!-- lines ~23-30: <ins class="adsbygoogle" ...> renders only when (enabled && publisherId) -->
<!-- lines ~31-33: the off-branch <div class="ad-placeholder" aria-hidden="true"></div> -->
<!-- lines ~44-49: .ad-slot--content / .ad-slot--bottom height reservations (90px desktop / 50px mobile) -->
<!-- lines ~52-58: .ad-placeholder styles (the dashed border to remove) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dashed-border placeholder, keep reserved height + env gate (D-06/D-07)</name>
  <read_first>
    - site/src/components/AdSlot.astro (full file — `enabled` flag line ~18; adsbygoogle branch lines ~23-30; off-branch placeholder div lines ~31-33; height reservations lines ~44-49; `.ad-placeholder` style block lines ~52-58)
    - .planning/phases/08-bug-fixes-data-correctness/08-PATTERNS.md §D-06 (exact before/after CSS and preserved invariants)
    - .planning/REVIEW-E2E-FINDINGS.md (Severity Summary — no Critical; BUGFIX-04 is the cosmetic delta + verification)
  </read_first>
  <action>
    In site/src/components/AdSlot.astro, edit the `.ad-placeholder` CSS rule (lines ~52-58): REMOVE the `border: 2px dashed var(--line);` and `border-radius: var(--radius);` declarations. KEEP `width: 100%; height: 100%; background: var(--bg);` so the div still fills the reserved height blankly. Leave the `<div class="ad-placeholder" aria-hidden="true"></div>` markup in place (it is the structural element that fills reserved height).
    Do NOT change: the `const enabled = import.meta.env.ADSENSE_ENABLED === 'true';` exact string compare (line ~18); the `enabled && publisherId` guard around `<ins class="adsbygoogle">` (lines ~23-30); the `.ad-slot--content` / `.ad-slot--bottom` height reservations of 90px desktop / 50px mobile (lines ~44-49). These are the CLS-safe + DOM-clean invariants verified in BUGFIX-04 and must remain byte-for-byte intact.
  </action>
  <verify>
    <automated>cd site && npm run build && node -e "const fs=require('fs');const s=fs.readFileSync('src/components/AdSlot.astro','utf8');const ph=(s.match(/\\.ad-placeholder\\s*\\{[^}]*\\}/m)||[''])[0];if(/dashed/.test(ph)||/border-radius/.test(ph))throw new Error('.ad-placeholder still has dashed border / border-radius');if(!/ADSENSE_ENABLED === 'true'/.test(s))throw new Error('env gate exact-compare changed/removed');if(!/(90px|--ad-h|height)/.test(s))throw new Error('height reservation appears removed');const html=fs.readFileSync('dist/index.html','utf8');if(/adsbygoogle/.test(html))throw new Error('adsbygoogle present in DOM with AdSense OFF');console.log('BUGFIX-04 OK: blank placeholder, gate intact, no adsbygoogle');"</automated>
  </verify>
  <acceptance_criteria>
    - The `.ad-placeholder` CSS rule contains NO `dashed` border and NO `border-radius` (blank box).
    - `.ad-placeholder` still has `height: 100%` and `width: 100%` (fills reserved space).
    - Source still contains `import.meta.env.ADSENSE_ENABLED === 'true'` (exact string compare preserved, D-07).
    - The 90px/50px height reservation rules are unchanged (no CLS).
    - Built `dist/index.html` (AdSlot present on the homepage) contains ZERO `adsbygoogle` occurrences with AdSense OFF.
    - `npm run build` succeeds (chained — OneDrive dist/ desync).
  </acceptance_criteria>
  <done>AdSlot renders a blank reserved-height placeholder with AdSense OFF; dashed border removed; env gate + height reservation + zero-adsbygoogle invariants intact; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → render branch | `ADSENSE_ENABLED` (build-time env) gates whether the third-party `adsbygoogle` script tag is emitted. The cosmetic change must not weaken this gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering (third-party script injection) | AdSlot env gate | mitigate | Change is CSS-only (remove border declarations). The `=== 'true'` exact-compare gate and the `enabled && publisherId` guard around `<ins class="adsbygoogle">` are left unchanged and re-asserted by the verify command (zero `adsbygoogle` in built DOM when OFF). No new third-party surface introduced. |
</threat_model>

<verification>
Chained build+grep (OneDrive dist/ desync — one command):
- `.ad-placeholder` has no dashed border / border-radius but keeps height/width.
- Source retains `ADSENSE_ENABLED === 'true'` and the height reservations.
- Built `dist/index.html` contains zero `adsbygoogle` with AdSense OFF.
</verification>

<success_criteria>
BUGFIX-04 resolved: AdSlot reserves height (no CLS) and renders a blank (border-free) placeholder when AdSense is OFF, with zero `adsbygoogle` in the DOM and the exact-compare env gate intact.
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes-data-correctness/08-04-SUMMARY.md` when done.
</output>
