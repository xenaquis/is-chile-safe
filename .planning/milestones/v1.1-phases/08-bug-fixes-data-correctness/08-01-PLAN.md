---
phase: 08-bug-fixes-data-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/pages/region/[slug].astro
  - site/src/pages/es/region/[slug].astro
  - site/src/pages/crime/[family].astro
  - site/src/pages/es/delito/[family].astro
autonomous: true
requirements: [BUGFIX-02]
must_haves:
  truths:
    - "Region ranking tables (EN + ES) display only communes whose CUT is in loadRolloutCuts()"
    - "Crime ranking tables (EN + ES) display only communes whose CUT is in loadRolloutCuts()"
    - "A user clicking any displayed commune row reaches a built page (no 404)"
    - "The national rank value (#N of 346) shown anywhere is unchanged by the row gating"
    - "A minimal 'showing N of M communes' affordance appears when rows are hidden"
  artifacts:
    - path: "site/src/pages/region/[slug].astro"
      provides: "Rollout-gated region ranking rows (EN)"
      contains: "loadRolloutCuts"
    - path: "site/src/pages/es/region/[slug].astro"
      provides: "Rollout-gated region ranking rows (ES)"
      contains: "loadRolloutCuts"
    - path: "site/src/pages/crime/[family].astro"
      provides: "Rollout-gated crime ranking rows (EN)"
      contains: "loadRolloutCuts"
    - path: "site/src/pages/es/delito/[family].astro"
      provides: "Rollout-gated crime ranking rows (ES)"
      contains: "loadRolloutCuts"
  key_links:
    - from: "region/[slug].astro"
      to: "loadRolloutCuts() / rollout.json"
      via: "new Set(loadRolloutCuts()) filter over rankingRows"
      pattern: "loadRolloutCuts"
    - from: "crime/[family].astro"
      to: "index.find(slug) → CUT membership test"
      via: "displayedRows filter"
      pattern: "rolloutCuts\\.has"
---

<objective>
Eliminate F-005: region and crime ranking tables link to thousands of non-rollout commune pages that 404. Gate the *displayed* rows in all 4 ranking templates (region EN/ES, crime EN/ES) to rollout slugs only, using `loadRolloutCuts()` as the single source of truth (D-01/D-02). Keep rank denominators (`#N of 346`, region totals) accurate — only displayed rows are gated, never the rank computation (D-03). Add a minimal "showing N of M communes — more coming" affordance (D-03).

Purpose: 404-free, crawl-clean internal links across 16 regions + 7 crime families × 2 locales. Highest SEO/UX impact of the phase.
Output: 4 modified ranking templates that auto-expand as rollout grows (and show all under ROLLOUT_ALL=true).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes-data-correctness/08-CONTEXT.md
@.planning/phases/08-bug-fixes-data-correctness/08-PATTERNS.md

<interfaces>
<!-- From site/src/lib/data.ts — use directly, no exploration needed -->
loadRolloutCuts(): string[]   // returns enabled CUT codes from src/config/rollout.json; ROLLOUT_ALL=true → all 346 CUTs. Wrap in new Set() for O(1) membership.
loadIndex(): IndexEntry[]      // each entry has { cut, slug, name, region_id, low_population, ... }; slug is unique.

<!-- From site/src/components/CommuneRankingTable.astro — RankingRow shape -->
interface RankingRow { name: string; slug: string; rate: number; national_rank: number; trend: 'up'|'down'|'stable'; low_population: boolean; }
<!-- slug is the bridge from a ranking row back to the index entry's CUT. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate region ranking rows to rollout CUTs (EN + ES)</name>
  <read_first>
    - site/src/pages/region/[slug].astro (full file — frontmatter import block ~lines 14-27, rankingRows build ~lines 73-87, sort ~line 87, table render ~lines 200-205)
    - site/src/pages/es/region/[slug].astro (ES mirror — rankingRows build ~lines 66-80; locale strings differ)
    - site/src/lib/data.ts lines 217-230 (loadRolloutCuts implementation — source of truth)
    - site/src/components/CommuneRankingTable.astro lines 15-28 (RankingRow interface, locale prop)
  </read_first>
  <action>
    In BOTH site/src/pages/region/[slug].astro and site/src/pages/es/region/[slug].astro:
    (1) Add `loadRolloutCuts` to the existing named import from the data module (`'../../lib/data.ts'` in EN; verify the correct relative depth in the ES file and use whatever the other loaders already import from).
    (2) Immediately AFTER the existing `rankingRows.sort(...)` line, build the gate: `const rolloutCuts = new Set(loadRolloutCuts());` then `const displayedRows = rankingRows.filter((row) => { const meta = regionCommunes.find((c) => c.slug === row.slug); return meta ? rolloutCuts.has(meta.cut) : false; });` and `const hiddenCount = rankingRows.length - displayedRows.length;`.
    (3) Change the `<CommuneRankingTable rows={rankingRows} ... />` prop to `rows={displayedRows}`.
    (4) After the table, add the D-03 affordance gated on `hiddenCount > 0`: EN text "Showing {displayedRows.length} of {rankingRows.length} communes — more coming soon."; ES text "Mostrando {displayedRows.length} de {rankingRows.length} comunas — más próximamente." Use a `<p class="rollout-note">` element.
    Do NOT touch `national_rank` / `data.national_rank` or any `TOTAL_COMMUNES`/346 constant — the rank denominator stays intact (D-03). Do NOT hardcode any slug list. `regionCommunes` is the region's commune list already in scope (NOT the 346 total), so `rankingRows.length` is the region's commune count — correct for the affordance. Inject `displayedRows.length` / `rankingRows.length` only as Astro expression interpolation (`{...}`) of numbers — no user input, no `set:html`.
  </action>
  <verify>
    <automated>cd site && npm run build && node -e "const fs=require('fs');for(const f of ['src/pages/region/[slug].astro','src/pages/es/region/[slug].astro']){const s=fs.readFileSync(f,'utf8');if(!s.includes('loadRolloutCuts'))throw new Error(f+': missing loadRolloutCuts import/use');if(!s.includes('displayedRows'))throw new Error(f+': missing displayedRows gate');if(/rows=\{rankingRows\}/.test(s))throw new Error(f+': table still renders ungated rankingRows');}console.log('region gating OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with both region templates modified (chained in ONE command — OneDrive dist/ desync).
    - Both region templates contain `loadRolloutCuts` and `displayedRows`; neither passes `rows={rankingRows}` to CommuneRankingTable.
    - Build output `dist/region/metropolitana/index.html` contains zero `href="/commune/cerrillos/"` and zero `href="/commune/recoleta/"` (previously-404 non-rollout communes), confirmed by grep against the built HTML in the same chained command if region/metropolitana is built.
    - `data.national_rank` / 346 totals are not referenced in the diff (rank denominator untouched).
    - When `hiddenCount > 0`, the built region HTML contains the substring "more coming" (EN) / "más próximamente" (ES).
  </acceptance_criteria>
  <done>Both region ranking templates render only rollout-CUT rows plus a "showing N of M" note; rank denominator unchanged; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Gate crime ranking rows to rollout CUTs (EN + ES)</name>
  <read_first>
    - site/src/pages/crime/[family].astro (full file — import ~line 69 `const index = loadIndex()`, rankingRows assembly ~lines 82-109/132, inline tbody map ~lines 209-246)
    - site/src/pages/es/delito/[family].astro (ES mirror — same structure; locale strings differ)
    - site/src/lib/data.ts lines 217-230 (loadRolloutCuts)
    - site/src/pages/region/[slug].astro (reference for the affordance markup just added in Task 1, for consistency)
  </read_first>
  <action>
    In BOTH site/src/pages/crime/[family].astro and site/src/pages/es/delito/[family].astro:
    (1) Add `loadRolloutCuts` to the existing named import from `'../../lib/data.ts'` (verify ES relative depth).
    (2) AFTER `rankingRows` is fully assembled (the `[...eligibleRows, ...lowPopRows]` concat), build the gate using the in-scope `index`: `const rolloutCuts = new Set(loadRolloutCuts());` then `const displayedRows = rankingRows.filter((row) => { const meta = index.find((c) => c.slug === row.slug); return meta ? rolloutCuts.has(meta.cut) : false; });` and `const hiddenCount = rankingRows.length - displayedRows.length;`.
    (3) Change the inline `<tbody>` map from `rankingRows.map((row, i) => ...)` to `displayedRows.map((row, i) => ...)`. The displayed rank column `#${i + 1}` is the position within the gated list — this is expected and correct (the national `#N of 346` lives only on commune pages / ResultPanel, not in this table).
    (4) Add the affordance below the table gated on `hiddenCount > 0`: EN "Showing {displayedRows.length} of {rankingRows.length} communes — more coming soon."; ES "Mostrando {displayedRows.length} de {rankingRows.length} comunas — más próximamente." `<p class="rollout-note">`.
    Do NOT hardcode slugs. Do NOT alter `latestYear`, sort order, or low-population handling. Numbers interpolated via Astro `{...}` only — no `set:html`, no untrusted input.
  </action>
  <verify>
    <automated>cd site && npm run build && node -e "const fs=require('fs');for(const f of ['src/pages/crime/[family].astro','src/pages/es/delito/[family].astro']){const s=fs.readFileSync(f,'utf8');if(!s.includes('loadRolloutCuts'))throw new Error(f+': missing loadRolloutCuts');if(!s.includes('displayedRows'))throw new Error(f+': missing displayedRows gate');if(/rankingRows\.map\(/.test(s))throw new Error(f+': tbody still maps ungated rankingRows');}console.log('crime gating OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with both crime templates modified (chained in ONE command).
    - Both crime templates contain `loadRolloutCuts` and `displayedRows`; neither tbody maps `rankingRows` directly.
    - Built `dist/crime/homicide/index.html` (or equivalent built crime family) contains only `href="/commune/{slug}/"` links whose slug is a rollout commune — spot-check: zero `href="/commune/cerrillos/"`.
    - The affordance string ("more coming" EN / "más próximamente" ES) appears in the built crime HTML when rows are hidden.
    - No change to rank-denominator logic; the per-table `#${i+1}` rank is over displayed rows (intended).
  </acceptance_criteria>
  <done>Both crime ranking templates render only rollout-CUT rows plus the affordance; build passes; no 404-bound commune links remain in displayed rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data → static HTML | rollout.json + index.json + commune JSON read at build; rendered to HTML. No runtime/client trust boundary, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering (XSS) | region/crime row-filter affordance interpolation | mitigate | Only numeric `displayedRows.length`/`rankingRows.length` and static literals are interpolated via Astro `{...}` (auto-escaped). No `set:html`, no user/network input. Slugs/names already rendered by existing CommuneRankingTable were pre-existing trusted build data — gating reduces, never adds, rendered rows. |
| T-08-02 | Information disclosure | rollout gating | accept | Gating hides non-rollout rows; index.json/rollout.json are already public build inputs. No secret exposed. |
</threat_model>

<verification>
Chained build+grep (OneDrive dist/ desync — one command):
- `cd site && npm run build` then assert all 4 templates import/use `loadRolloutCuts` and render `displayedRows`.
- Grep built region/crime HTML: zero `href="/commune/cerrillos/"`, zero `href="/commune/recoleta/"` (previously-404 examples from F-005).
- Confirm rank denominator strings (#N of 346 on commune/panel pages) are untouched — no edits to data.national_rank or TOTAL_COMMUNES.
</verification>

<success_criteria>
F-005 resolved: all 4 ranking templates display only rollout-CUT commune rows; clicking any displayed row reaches a built page; the "showing N of M" affordance renders when rows are hidden; the national rank denominator is unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes-data-correctness/08-01-SUMMARY.md` when done.
</output>
