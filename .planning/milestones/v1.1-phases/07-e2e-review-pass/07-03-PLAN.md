---
phase: 07-e2e-review-pass
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - .planning/REVIEW-E2E-FINDINGS.md
  - .planning/ui-reviews/
autonomous: true
requirements: [REVIEW-03]
must_haves:
  truths:
    - "Year filter changes choropleth colours without unhandled error"
    - "Crime-type chip filter recolours choropleth and resets correctly"
    - "Clicking a commune polygon opens the panel with rate/trend/ranking"
    - "Panel sparkline and 7-family bars render"
    - "Search by commune name zooms map and opens panel"
    - "Geolocation locate control behaves (marker or graceful Toast)"
    - "Incident layer toggle shows graceful empty-state Toast with NO console.error"
    - "REVIEW-03 section appended to REVIEW-E2E-FINDINGS.md"
  artifacts:
    - path: ".planning/REVIEW-E2E-FINDINGS.md"
      provides: "REVIEW-03 (map interactions) section with per-interaction evidence"
      contains: "## REVIEW-03"
    - path: ".planning/ui-reviews/"
      provides: "Before/after screenshots for each map interaction"
  key_links:
    - from: ".planning/REVIEW-E2E-FINDINGS.md"
      to: ".planning/ui-reviews/r03-*.png"
      via: "screenshot path per interaction step"
      pattern: "ui-reviews/r03-"
---

<objective>
Drive the Leaflet map island through every dynamic interaction in a real browser
and confirm each behaves without unhandled errors, capturing before/after
screenshots and a console read per interaction.

Purpose: REVIEW-03 — the map's dynamic behaviour (year filter, crime-type
filter, commune panel with rate/trend/ranking/sparkline/families, search,
geolocation, and the incident layer empty state) is verified live and clean.
Output: REVIEW-03 section appended to REVIEW-E2E-FINDINGS.md; r03- screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-e2e-review-pass/07-RESEARCH.md
@.planning/phases/07-e2e-review-pass/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 3): Exercise all map interactions on /map/</name>
  <files>.planning/REVIEW-E2E-FINDINGS.md, .planning/ui-reviews/</files>
  <read_first>
    - .planning/phases/07-e2e-review-pass/07-RESEARCH.md (§ Map Island: Interaction Inventory — the full table; § Common Pitfalls 4 — incident empty state is expected)
    - site/src/components/map/MapIsland.tsx (map init, fetch URLs, setSelected state)
    - site/src/components/map/FiltersRow.tsx (year select, CHIP_DEFS chip order, incidents toggle)
    - site/src/components/map/ResultPanel.tsx (panel data shape, rate/trend/ranking, CATALOG_KEY_ORDER)
    - site/src/components/map/SearchBox.tsx (search + locate button)
    - site/src/components/map/IncidentPinLayer.ts (graceful empty-state behaviour)
    - .planning/phases/07-e2e-review-pass/07-01-SUMMARY.md (BrowserOS tool names + base URL)
  </read_first>
  <action>
    Using the BrowserOS tool names and base URL from 07-01-SUMMARY.md, navigate
    to /map/ (EN) at desktop viewport (1280×800). The map island is
    client:only="react" — wait for hydration (polygons visible) before
    interacting. Perform these interactions IN ORDER; for each, screenshot to
    .planning/ui-reviews/ (e.g. r03-01-year-2010.png) and read the console:

    1. Year filter: select a different year (e.g. 2010) in the year dropdown;
       confirm choropleth recolours. (r03-01)
    2. Crime-type chip: click the "Property crimes" chip; confirm choropleth
       recolours by that family; then click "All types" and confirm reset.
       (r03-02-property, r03-03-alltypes)
    3. Commune panel: click the Santiago polygon (or use search to target it);
       confirm the ResultPanel opens and fetches /data/cead/comunas/13101.json.
       (r03-04-panel)
    4. Panel contents: confirm rate per 100k, trend arrow, national ranking
       (rank / 346), sparkline SVG (≥2 points), and the 7-family bar chart all
       render. (r03-05-panel-detail)
    5. Search: type "Temuco" in the SearchBox, select from the prefix dropdown;
       confirm the map zooms and the panel opens for Temuco. (r03-06-search)
    6. Geolocation: click the locate control. A browser permission prompt or a
       graceful Toast (if denied/unavailable in the automation context) is
       acceptable — record which occurred. (r03-07-locate)
    7. Incident layer: toggle the events/incidents layer ON. The fetch of
       /data/incidents/current.json returns 404 — this is EXPECTED. Confirm a
       graceful "coming soon"/empty-state Toast appears and that the console
       shows NO uncaught error / no console.error for this (Pitfall 4).
       (r03-08-incidents-empty)

    After the full sequence, do one final console read and record the aggregate
    console state. Tag any interaction that errors or misbehaves as a finding
    (Critical if it throws/breaks the map, Warning for degraded behaviour, Polish
    for cosmetic). A graceful incident-layer Toast with a clean console is a PASS,
    not a finding.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readdirSync('.planning/ui-reviews').filter(f=>f.startsWith('r03-'));if(d.length<8){console.error('Expected >=8 r03- interaction screenshots, got '+d.length);process.exit(1)}console.log('r03 screenshots: '+d.length)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ui-reviews/ contains >=8 r03- screenshots covering year filter, chip filter+reset, panel open, panel detail, search, locate, and incident empty state
    - REVIEW-E2E-FINDINGS.md has a "## REVIEW-03" section with a per-interaction evidence row
    - The console read for the incident-layer toggle is recorded and explicitly states whether console.error was present (expected: none)
    - Each interaction is marked PASS or carries a severity-tagged finding
  </acceptance_criteria>
  <done>All map interactions exercised on /map/ with before/after screenshots and console reads; REVIEW-03 section appended; incident empty state confirmed graceful with no console.error.</done>
</task>

</tasks>

<threat_model>
Read-only QA review; no code, no data mutation, no new attack surface — no
applicable threats. Browser interactions fire client-side JS only against a
local dev server; no secrets are read, no production code is written.
</threat_model>

<verification>
- >=8 r03- interaction screenshots exist
- REVIEW-E2E-FINDINGS.md contains a "## REVIEW-03" section with per-interaction evidence
- Incident-layer toggle recorded as graceful (no console.error)
</verification>

<success_criteria>
ROADMAP Phase 7 success criterion 3 is satisfied: map dynamic interactions
(year filter, crime-type filter, commune panel with
rate/trend/ranking/sparkline/families, search, geolocation, incident empty
state) are verified in-browser without unhandled errors.
</success_criteria>

<output>
Create `.planning/phases/07-e2e-review-pass/07-03-SUMMARY.md` when done.
Record: per-interaction PASS/finding outcome and the incident-layer console result.
</output>
