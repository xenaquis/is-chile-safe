---
phase: 07-e2e-review-pass
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/REVIEW-E2E-FINDINGS.md
  - .planning/ui-reviews/
autonomous: true
requirements: [REVIEW-02]
must_haves:
  truths:
    - "3 commune pages (Santiago/Concepción/Punta Arenas) reviewed in both locales against the template"
    - "2 region pages (Metropolitana/Biobío) reviewed in both locales"
    - "2 crime-type pages (homicide/property) reviewed in both locales"
    - "Each sampled page checked against its 10-component template; data/rate sanity noted"
    - "REVIEW-02 section appended to REVIEW-E2E-FINDINGS.md"
  artifacts:
    - path: ".planning/REVIEW-E2E-FINDINGS.md"
      provides: "REVIEW-02 (programmatic sampling) section with declared sample + template checklist"
      contains: "## REVIEW-02"
    - path: ".planning/ui-reviews/"
      provides: "Screenshots for all 14 sampled programmatic URLs"
  key_links:
    - from: ".planning/REVIEW-E2E-FINDINGS.md"
      to: ".planning/ui-reviews/r02-*.png"
      via: "relative screenshot path per sampled URL"
      pattern: "ui-reviews/r02-"
---

<objective>
Review the declared sample of programmatic pages (communes / regions / crime
types, both locales) against the page template, capturing screenshots and
console reads and ticking each template component.

Purpose: REVIEW-02 — a declared, representative sample of programmatic pages
(≥3 communes high/mid/low, ≥2 regions, ≥2 crime types, both locales) has been
verified against the template, so we have confidence the 740 generated pages are
structurally sound without walking each one.
Output: REVIEW-02 section appended to REVIEW-E2E-FINDINGS.md; r02- screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-e2e-review-pass/07-RESEARCH.md
@.planning/phases/07-e2e-review-pass/07-01-SUMMARY.md

# 07-01-SUMMARY.md carries the confirmed BrowserOS tool names + dev-server base URL.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 2): Review declared programmatic sample against template</name>
  <files>.planning/REVIEW-E2E-FINDINGS.md, .planning/ui-reviews/</files>
  <read_first>
    - .planning/phases/07-e2e-review-pass/07-RESEARCH.md (§ Programmatic Page Patterns — the declared-sample tables; § Commune Page Template Structure — the 10 components)
    - site/src/pages/commune/[slug].astro (defines correct commune-page structure)
    - site/src/pages/region/[slug].astro (defines correct region-page structure)
    - site/src/pages/crime/[family].astro (defines correct crime-page structure)
    - .planning/phases/07-e2e-review-pass/07-01-SUMMARY.md (BrowserOS tool names + base URL)
  </read_first>
  <action>
    Using the BrowserOS tool names and base URL from 07-01-SUMMARY.md, navigate
    each declared-sample URL at desktop viewport (1280×800). For each: navigate,
    screenshot to .planning/ui-reviews/ (kebab name with locale, e.g.
    r02-en-commune-santiago.png, r02-es-region-biobio.png), and read the console.

    Communes (3 tiers × 2 locales = 6 URLs):
    EN /commune/santiago/, /commune/concepcion/, /commune/punta-arenas/
    ES /es/comuna/santiago/, /es/comuna/concepcion/, /es/comuna/punta-arenas/

    Regions (2 × 2 locales = 4 URLs):
    EN /region/metropolitana/, /region/biobio/
    ES /es/region/metropolitana/, /es/region/biobio/

    Crime types (2 × 2 locales = 4 URLs):
    EN /crime/homicide/, /crime/property/
    ES /es/delito/homicidios/, /es/delito/propiedad/

    For each COMMUNE page, tick the 10-component template checklist from RESEARCH
    (BaseLayout meta, LevelChip, TrendChip, StatCard headline rate, Sparkline,
    ComparisonCallout, FamilyBreakdownBars, ComparableCommune cross-link,
    MapPlaceholderSlot linking /map/, MethodologyCaveat with CEAD attribution).
    Sanity-check the displayed rate is a per-100k rate (not a raw sum) and that
    the comparable-commune link resolves. For region and crime pages, verify the
    page renders its template sections, shows CEAD attribution + year, and the
    hreflang/locale counterpart is reachable. Record one checklist table per page
    category. Tag every defect Critical/Warning/Polish per the schema; treat any
    data figure that looks like a raw count where a rate is expected as a Warning
    (do not deep-debug here — Phase 8 fixes; this phase records).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readdirSync('.planning/ui-reviews').filter(f=>f.startsWith('r02-'));if(d.length<14){console.error('Expected >=14 r02- screenshots (6 commune + 4 region + 4 crime), got '+d.length);process.exit(1)}console.log('r02 screenshots: '+d.length)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ui-reviews/ contains a screenshot for all 14 declared-sample URLs (>=14 r02- files)
    - REVIEW-E2E-FINDINGS.md has a "## REVIEW-02" section naming the declared sample explicitly
    - Each of the 3 sampled commune pages has a 10-component template checklist recorded
    - Rate-vs-raw-count sanity result is recorded for at least the 3 commune pages
    - A console read is recorded for each sampled URL (errors logged as findings)
  </acceptance_criteria>
  <done>All 14 sampled programmatic URLs screenshotted + console-read; REVIEW-02 section appended with declared sample, template checklists, and severity-tagged findings.</done>
</task>

</tasks>

<threat_model>
Read-only QA review; no code, no data mutation, no new attack surface — no
applicable threats. Navigation + console/DOM read of a local dev server only.
</threat_model>

<verification>
- >=14 r02- screenshots exist
- REVIEW-E2E-FINDINGS.md contains a "## REVIEW-02" section with the declared sample and per-page template checklists
</verification>

<success_criteria>
ROADMAP Phase 7 success criterion 2 is satisfied: a declared sample of
programmatic pages (≥3 communes across population tiers, ≥2 regions, ≥2 crime
types, both locales) has been reviewed against the page template with evidence.
</success_criteria>

<output>
Create `.planning/phases/07-e2e-review-pass/07-02-SUMMARY.md` when done.
Record: sampled URLs, template-checklist outcomes, and any data/rate findings.
</output>
