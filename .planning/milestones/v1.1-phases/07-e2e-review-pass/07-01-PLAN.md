---
phase: 07-e2e-review-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REVIEW-E2E-FINDINGS.md
  - .planning/ui-reviews/
autonomous: false
requirements: [REVIEW-01]
must_haves:
  truths:
    - "BrowserOS MCP tools are confirmed available and their real names recorded"
    - "The Astro dev server is running and its actual port is captured"
    - "All 12 EN + 12 ES editorial pages navigated, screenshotted, console-checked"
    - "/map/ and /es/mapa/ navigated, screenshotted, console-checked"
    - "All 4 legal URLs (terms + privacy, each locale) navigated, screenshotted, console-checked"
    - "REVIEW-E2E-FINDINGS.md header + REVIEW-01 section exist with findings"
  artifacts:
    - path: ".planning/REVIEW-E2E-FINDINGS.md"
      provides: "Findings header + REVIEW-01 (editorial/legal inventory) section"
      contains: "## REVIEW-01"
    - path: ".planning/ui-reviews/"
      provides: "Screenshot evidence for every editorial, map, and legal URL"
  key_links:
    - from: ".planning/REVIEW-E2E-FINDINGS.md"
      to: ".planning/ui-reviews/*.png"
      via: "relative screenshot path reference per finding/URL"
      pattern: "ui-reviews/"
---

<objective>
Pre-flight the review environment, then walk the entire fixed-page inventory
(editorial + map + legal, both locales) in a real browser via BrowserOS MCP,
capturing a screenshot and a console read for every URL. Begin
REVIEW-E2E-FINDINGS.md.

Purpose: REVIEW-01 — every page in the fixed inventory has been seen in a real
browser (ES and EN) with evidence, and the findings document is initialised so
later waves can append crash-resiliently.
Output: REVIEW-E2E-FINDINGS.md (header + REVIEW-01 section); screenshots in
.planning/ui-reviews/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-review-pass/07-RESEARCH.md
@.mcp.json

# Use the VERIFIED facts in 07-RESEARCH.md, NOT the figures in REQUIREMENTS.md
# where they differ (editorial = 12+12, legal = 4 URLs, not 10+10 / 8).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (Wave 0): Pre-flight — confirm dev server port + BrowserOS MCP tools</name>
  <read_first>
    - .planning/phases/07-e2e-review-pass/07-RESEARCH.md (§ BrowserOS MCP Tooling, § Common Pitfalls 1–3, § Open Questions 1–2)
    - .mcp.json
    - .planning/STATE.md (Research Flags for Planning — BrowserOS port 9200, dev server requirement)
  </read_first>
  <what-built>
    Nothing is built. This is an availability gate before any browser work.
    The executor must, in order:
    1. List the MCP tools currently surfaced and identify every tool whose name
       matches `mcp__browseros__*` (or any tool clearly from the `browseros`
       server). Record the REAL tool names — RESEARCH assumes navigate /
       screenshot / get_console_logs / set_viewport / click / type / evaluate
       but these are UNVERIFIED. Map each assumed capability to its actual tool
       name in the SUMMARY so later waves use correct names.
    2. Confirm the Astro dev server is running. Start it if needed with
       `cd site && npm run dev` (predev runs sync-data.mjs; the incidents layer
       showing an empty state is EXPECTED — not a bug). Capture the ACTUAL base
       URL/port from the dev-server output (default http://localhost:4321; may
       be 4322 if 4321 is occupied). Record the confirmed base URL in the
       SUMMARY — all later waves use it.
    3. Use the discovered navigate + screenshot tools to load the confirmed base
       URL and save a smoke screenshot to .planning/ui-reviews/00-preflight-home.png.
       A blank page or net::ERR_CONNECTION_REFUSED means the server/port is wrong
       — fix before continuing.
  </what-built>
  <how-to-verify>
    1. The SUMMARY lists the real BrowserOS tool names (capability → tool name table).
    2. The SUMMARY states the confirmed dev-server base URL (e.g. http://localhost:4321).
    3. .planning/ui-reviews/00-preflight-home.png exists and shows the rendered
       home page (not blank / not connection error).
    If BrowserOS tools are NOT found: STOP. Report that the user must ensure
    BrowserOS is running at 127.0.0.1:9200 and may need to restart Claude Code so
    its tools surface, then re-run this plan. Do not proceed to Task 2.
  </how-to-verify>
  <acceptance_criteria>
    - .planning/ui-reviews/00-preflight-home.png exists and is non-blank
    - SUMMARY records the actual BrowserOS tool name for each capability used downstream
    - SUMMARY records the confirmed dev-server base URL/port
  </acceptance_criteria>
  <resume-signal>Type "approved" once dev server + BrowserOS are confirmed, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2 (Wave 1): Walk editorial + map + legal inventory — screenshot + console each</name>
  <files>.planning/REVIEW-E2E-FINDINGS.md, .planning/ui-reviews/</files>
  <read_first>
    - .planning/phases/07-e2e-review-pass/07-RESEARCH.md (§ Page Inventory — the verified 12 EN + 12 ES editorial table, the 2 map URLs, the 4 legal URLs; § REVIEW-E2E-FINDINGS.md Schema; § Common Pitfalls 4 & 5)
  </read_first>
  <action>
    Using the BrowserOS tool names and base URL confirmed in Task 1, navigate to
    EVERY URL below at desktop viewport (1280×800). For each URL, in order:
    (a) navigate, (b) take a screenshot saved to .planning/ui-reviews/ with a
    descriptive kebab-case name including locale (e.g. r01-en-is-santiago-safe.png,
    r01-es-seguridad-concepcion.png, r01-en-map.png, r01-en-terms.png),
    (c) read the browser console and note any error/warning verbatim.

    EN editorial (12): /, /is-chile-safe/, /is-santiago-safe/,
    /safest-cities-in-chile/, /chile-crime-map/, /santiago-safety-map/,
    /valparaiso-safety/, /vina-del-mar-safety/, /concepcion-safety/, /about/,
    /contact/, /methodology/

    ES editorial (12): /es/, /es/mapa-delito-chile/, /es/mapa-seguridad-santiago/,
    /es/comunas-mas-seguras-chile/, /es/delitos-por-comuna/, /es/delitos-por-region/,
    /es/seguridad-concepcion/, /es/seguridad-valparaiso/, /es/seguridad-vina-del-mar/,
    /es/acerca-de/, /es/contacto/, /es/metodologia/

    Map (2): /map/, /es/mapa/

    Legal (4): /terms/, /privacy-policy/, /es/terminos/, /es/politica-privacidad/

    For each URL record at minimum: page renders, no obvious layout breakage,
    console state. Tag any defect as a finding (Critical/Warning/Polish) per the
    schema; a clean page needs only an evidence row (screenshot path + "console
    clean"). Do NOT treat the incidents-layer 404 on /map/ or /es/mapa/ as a bug
    — it is the expected empty state (Pitfall 4). The 4 legal URLs are the only
    legal pages that exist — log the absence of cookie-policy / accessibility
    statement (REQUIREMENTS.md anticipated 8) as a single Polish finding
    (Pitfall 5), do NOT gate success on 8.

    NOTE: the verify screenshot floor (>=28) assumes exactly 4 legal URLs per
    07-RESEARCH.md (12 EN + 12 ES editorial + 2 map + 4 legal). If pre-flight or
    this walk discovers additional legal pages (e.g. cookie policy, accessibility
    statement) actually shipped, screenshot them too and raise the count
    accordingly — the floor is a minimum, not a cap.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readdirSync('.planning/ui-reviews').filter(f=>f.startsWith('r01-'));if(d.length<28){console.error('Expected >=28 r01- screenshots (12+12+2+4), got '+d.length);process.exit(1)}console.log('r01 screenshots: '+d.length)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ui-reviews/ contains a screenshot for each of the 12 EN + 12 ES editorial URLs, /map/, /es/mapa/, and all 4 legal URLs (>=28 r01- files)
    - A console read is recorded in REVIEW-E2E-FINDINGS.md for /map/ and /es/mapa/ (errors logged as findings; incidents 404 explicitly noted as expected, not a defect)
    - The missing-legal-pages gap (4 of 8 anticipated) is recorded as exactly one Polish finding
    - REVIEW-E2E-FINDINGS.md has a header block and a "## REVIEW-01" section with >=1 finding entry per the schema
  </acceptance_criteria>
  <done>All 28 inventory URLs have a screenshot + console read; REVIEW-E2E-FINDINGS.md header and REVIEW-01 section written with severity-tagged findings and screenshot references.</done>
</task>

</tasks>

<threat_model>
Read-only QA review; no code, no data mutation, no new attack surface — no
applicable threats. The phase only navigates a local dev server and reads the
browser console/DOM; it writes no production code and touches no secrets.
</threat_model>

<verification>
- .planning/ui-reviews/00-preflight-home.png renders the home page (preflight passed)
- >=28 r01- screenshots exist (full editorial + map + legal inventory)
- REVIEW-E2E-FINDINGS.md exists with header + "## REVIEW-01" section
- Incidents-layer 404 noted as expected; missing-legal-pages logged as Polish
</verification>

<success_criteria>
ROADMAP Phase 7 success criterion 1 is satisfied: all editorial pages (12 EN +
12 ES per verified count), /map/ + /es/mapa/, and the legal pages have been
navigated, screenshotted, and console-checked, with findings recorded.
</success_criteria>

<output>
Create `.planning/phases/07-e2e-review-pass/07-01-SUMMARY.md` when done.
Record: confirmed BrowserOS tool names, confirmed dev-server base URL, count of
screenshots captured, and any Critical/Warning findings surfaced.
</output>
