---
phase: 07-e2e-review-pass
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - .planning/REVIEW-E2E-FINDINGS.md
autonomous: true
requirements: [REVIEW-05]
must_haves:
  truths:
    - "REVIEW-E2E-FINDINGS.md is consolidated into the canonical schema"
    - "Every finding is tagged Critical / Warning / Polish"
    - "Every finding identifies the affected page + locale"
    - "Every finding references a screenshot in ui-reviews/"
    - "Every finding has a concrete recommendation"
    - "A severity summary count is present at the top"
  artifacts:
    - path: ".planning/REVIEW-E2E-FINDINGS.md"
      provides: "Consolidated, schema-conformant findings document with severity summary"
      contains: "# E2E Review Findings"
  key_links:
    - from: ".planning/REVIEW-E2E-FINDINGS.md"
      to: ".planning/ui-reviews/"
      via: "every finding cites a relative screenshot path"
      pattern: "ui-reviews/"
---

<objective>
Consolidate the per-wave findings (REVIEW-01..04) into a single, schema-conformant
REVIEW-E2E-FINDINGS.md: normalise every finding to Critical/Warning/Polish with
page, locale, screenshot reference, and a concrete recommendation, and add a
severity summary at the top so Phase 8 can triage.

Purpose: REVIEW-05 — produce the single prioritised findings document that is the
sole deliverable of this phase and the input to Phases 8 and 9.
Output: finalised .planning/REVIEW-E2E-FINDINGS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-e2e-review-pass/07-RESEARCH.md
@.planning/REVIEW-E2E-FINDINGS.md

# RESEARCH § REVIEW-E2E-FINDINGS.md Schema is the canonical format.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 5): Consolidate and finalise REVIEW-E2E-FINDINGS.md</name>
  <files>.planning/REVIEW-E2E-FINDINGS.md</files>
  <read_first>
    - .planning/phases/07-e2e-review-pass/07-RESEARCH.md (§ REVIEW-E2E-FINDINGS.md Schema)
    - .planning/REVIEW-E2E-FINDINGS.md (the accumulated wave sections to normalise)
    - .planning/phases/07-e2e-review-pass/07-01-SUMMARY.md, 07-02-SUMMARY.md, 07-03-SUMMARY.md, 07-04-SUMMARY.md (per-wave outcomes to fold in if any finding is only in a SUMMARY)
  </read_first>
  <action>
    Re-read the full REVIEW-E2E-FINDINGS.md accumulated across Waves 1–4 and the
    four wave SUMMARYs. Produce the final document conforming to the RESEARCH
    schema:
    - Header block: title, date, reviewer (Claude Code / BrowserOS MCP), confirmed
      dev-server base URL, desktop viewport (1280×800), mobile viewport (375×812).
    - A "Severity Summary" near the top: counts of Critical / Warning / Polish.
    - Findings numbered F-001.. each with: Severity, Page (route), Locale
      (EN|ES|Both), Screenshot (relative ui-reviews/ path that exists),
      Observation, Recommendation (concrete and actionable for Phase 8/9).
    - Group or order findings by severity (Critical first) so Phase 8 triage is
      direct.
    Ensure the known recorded facts appear as findings where applicable: the
    missing-legal-pages gap (Polish), the incidents 404 explicitly noted as
    EXPECTED (not a defect — either omit as a finding or mark as an
    informational/PASS note, never Critical), and any data/rate, console, link,
    overflow, or interaction defects surfaced in Waves 1–4. Every finding MUST
    cite a screenshot path that actually exists in .planning/ui-reviews/. If a
    finding lacks a screenshot, point it at the closest captured evidence from
    its wave. Do NOT invent findings — only consolidate what was observed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/REVIEW-E2E-FINDINGS.md','utf8');const checks=[/# E2E Review Findings/,/Severity Summary/i,/F-001/,/Critical|Warning|Polish/,/ui-reviews\//,/Recommendation/i];const fail=checks.filter(r=>!r.test(t));if(fail.length){console.error('FINDINGS missing: '+fail.map(r=>r.source).join(' | '));process.exit(1)}console.log('FINDINGS schema OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REVIEW-E2E-FINDINGS.md exists, is non-empty, and starts with "# E2E Review Findings"
    - A Severity Summary with Critical/Warning/Polish counts is present near the top
    - At least one finding entry (F-001..) exists, each with Severity, Page, Locale, Screenshot (ui-reviews/ path), Observation, Recommendation
    - Every cited screenshot path resolves to a file in .planning/ui-reviews/
    - The missing-legal-pages gap is present as a Polish finding; the incidents 404 is recorded as EXPECTED (never Critical)
  </acceptance_criteria>
  <done>REVIEW-E2E-FINDINGS.md is finalised in the canonical schema with a severity summary and every finding fully specified and screenshot-backed; ready as input to Phases 8 and 9.</done>
</task>

</tasks>

<threat_model>
Read-only QA review; no code, no data mutation, no new attack surface — no
applicable threats. This wave only edits a planning markdown document.
</threat_model>

<verification>
- .planning/REVIEW-E2E-FINDINGS.md passes the schema check (title, severity summary, F-001, severity tags, screenshot refs, recommendations)
- Every cited screenshot path exists in .planning/ui-reviews/
</verification>

<success_criteria>
ROADMAP Phase 7 success criterion 5 is satisfied: .planning/REVIEW-E2E-FINDINGS.md
exists with every finding tagged Critical/Warning/Polish, the affected page
identified, a screenshot reference, and a concrete recommendation.
</success_criteria>

<output>
Create `.planning/phases/07-e2e-review-pass/07-05-SUMMARY.md` when done.
Record: total findings by severity and confirmation the document is schema-conformant.
</output>
