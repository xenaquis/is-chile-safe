---
phase: 05-rss-news-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - pipeline/scrape_news.py
  - pipeline/tests/test_scrape_news.py
autonomous: false
requirements: [NEWS-01, NEWS-02, NEWS-03, NEWS-04, NEWS-05]
tags: [python, orchestrator, pipeline, integration, uat]

must_haves:
  truths:
    - "Running scrape_news.py wires fetch -> pre-filter -> seen-ledger -> classify -> centroid -> dedup -> store end to end"
    - "main() returns 0 on success and 1 on failure (mirrors scrape_cead.py); sys.exit guard present"
    - "A per-run classification cap bounds DeepSeek API cost"
    - "If DEEPSEEK_API_KEY is absent the pipeline exits gracefully (no crash, clear log)"
    - "After a real run, audit_incidents.py dumps 50 incidents and a human confirms no commune hallucination"
  artifacts:
    - path: "pipeline/scrape_news.py"
      provides: "End-to-end orchestrator mirroring scrape_cead.py (main()->int, sys.exit guard, dotenv, per-feed try/except)"
      contains: "def main"
    - path: "pipeline/tests/test_scrape_news.py"
      provides: "Integration test: full pipeline wired with mocked feeds + mocked DeepSeek -> validated current.json"
      contains: "def test_"
  key_links:
    - from: "pipeline/scrape_news.py"
      to: "pipeline/news/{feeds,classifier,centroids,dedup,store}.py"
      via: "orchestration imports"
      pattern: "from pipeline\\.news"
    - from: "pipeline/scrape_news.py"
      to: "pipeline/news/store.py"
      via: "merge_and_write final step"
      pattern: "merge_and_write"
---

<objective>
Wire the full RSS news pipeline orchestrator (scrape_news.py) mirroring scrape_cead.py, add an integration test (mocked feeds + mocked DeepSeek), and run the D-16 50-incident human-audit gate. This is the phase's last wave: it connects Wave-1 modules into a runnable pipeline and proves the success criteria end to end.

Purpose: Make the pipeline runnable by Phase 6's cron with a single entrypoint, bound API cost (D-17), fail gracefully without a key, and validate classification quality through the human audit before declaring production-ready.
Output: pipeline/scrape_news.py, pipeline/tests/test_scrape_news.py, and a human-verified 50-incident audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-rss-news-pipeline/05-RESEARCH.md
@.planning/phases/05-rss-news-pipeline/05-PATTERNS.md
@.planning/phases/05-rss-news-pipeline/05-VALIDATION.md
@.planning/phases/05-rss-news-pipeline/05-01-SUMMARY.md
@.planning/phases/05-rss-news-pipeline/05-02-SUMMARY.md
@.planning/phases/05-rss-news-pipeline/05-03-SUMMARY.md

<interfaces>
<!-- From Waves 0-1 — orchestrate these. -->
pipeline/news/feeds.py: FEEDS, fetch_feed(name,url), canonical_url(entry,feed), is_crime_item(title,desc), strip_html(t), parse_pub_date(entry), load_seen()/save_seen()/filter_seen()
pipeline/news/classifier.py: classify(title, description, cut_list) -> ClassifierOutput|None ; CONFIDENCE_THRESHOLD
pipeline/news/centroids.py: get_centroid(cut) -> (lat,lng)|None
pipeline/news/dedup.py: deduplicate(incidents) -> list[dict]
pipeline/news/store.py: make_id(url), build_incident(...), merge_and_write(new, current_path, archive_dir, window_days=30, today=None)
pipeline/news/schema.py: VALID_CUTS, IncidentRecord, IncidentsFile

scrape_cead.py orchestrator skeleton: main()->int, sys.exit(main()), _get_data_dir() with NEWS_DATA_DIR override, logging.basicConfig at top.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scrape_news.py orchestrator + integration test</name>
  <files>pipeline/scrape_news.py, pipeline/tests/test_scrape_news.py</files>
  <read_first>
    - pipeline/scrape_cead.py (exact analog — file header, logging.basicConfig at top, _get_data_dir + env override, main()->int try/except returning 0/1, sys.exit(main()) guard, local imports inside functions)
    - 05-PATTERNS.md "pipeline/scrape_news.py" section (full orchestrator skeleton; MAX_CLASSIFICATIONS_PER_RUN env constant; dotenv load_dotenv())
    - 05-RESEARCH.md System Architecture Diagram (fetch -> filter -> seen -> classify -> centroid -> dedup -> store sequence) + Open Question 1/3 (threshold logging; no git in this script)
    - pipeline/news/* (Wave 1 interfaces above)
    - 05-VALIDATION.md (Wave-2 integration row: orchestrator wires full pipeline; atomic write)
  </read_first>
  <action>
    Create pipeline/scrape_news.py (mirror scrape_cead.py exactly). File header docstring listing the 7 pipeline steps; logging.basicConfig(level=INFO, ...) at module top; load_dotenv() (no-op in CI). _get_data_dir() honoring NEWS_DATA_DIR override (default data/incidents). MAX_CLASSIFICATIONS_PER_RUN = int(os.environ.get("NEWS_MAX_CLASSIFY","200")) (D-17 cost cap). Graceful key check: if not os.environ.get("DEEPSEEK_API_KEY"): log clear error and return 1 (no crash) — UNLESS a future dry-run flag is set (keep simple: require key). main()->int with try/except returning 0/1 (exc_info on failure). Orchestration (local imports inside main per analog): load VALID_CUTS / cut_list; for each feed in FEEDS: fetch_feed (per-feed try/except already inside feeds), resolve canonical_url, parse_pub_date, strip_html(description for biobio), is_crime_item filter (D-02), filter_seen (D-03); cap candidates at MAX_CLASSIFICATIONS_PER_RUN (D-17, log when capped); for each candidate: classify(...) -> skip if None (rejected: CUT∉346 / low confidence / parse error — log with reason where available, RESEARCH Open Q1); get_centroid(cut) -> skip + log if None; build_incident(...) with outlet/url/date attribution (NEWS-05); collect into new_incidents; deduplicate(new_incidents) (NEWS-03); merge_and_write(new_incidents, current_path, archive_dir) (NEWS-04, D-15 validate-then-write inside store); update + save_seen ledger; log counts (fetched/filtered/classified/rejected/written). sys.exit(main()) guard. scrape_news.py does NOT perform git operations (Phase 6 owns commit — RESEARCH Open Q3).
    Create pipeline/tests/test_scrape_news.py: integration test using tmp NEWS_DATA_DIR; monkeypatch FEEDS to point at the Wave-0 fixture XML files (or monkeypatch fetch_feed to return parsed fixture entries); MOCK classifier.classify to return deterministic ClassifierOutput objects (NEVER live DeepSeek); assert main() returns 0 and the written current.json validates via IncidentsFile.model_validate and contains the expected de-duplicated incident count with correct attribution + centroid lat/lng. Also assert a missing DEEPSEEK_API_KEY path returns 1 without raising.
  </action>
  <verify>
    <automated>cd "$REPO" && pytest pipeline/tests/test_scrape_news.py -x -q && pytest pipeline/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - scrape_news.py has main()->int, sys.exit(main()) guard, logging.basicConfig at top, NEWS_DATA_DIR + NEWS_MAX_CLASSIFY env overrides, load_dotenv()
    - scrape_news.py contains NO deepseek-chat/reasoner/v4-pro and performs NO git operations
    - test_scrape_news.py: end-to-end with mocked feeds + mocked DeepSeek produces a current.json that passes IncidentsFile.model_validate; dedup applied; attribution + centroid present
    - missing-DEEPSEEK_API_KEY path returns 1 without raising
    - pytest pipeline/tests/ -q: FULL suite green
  </acceptance_criteria>
  <done>scrape_news.py runs the full fetch->filter->seen->classify->centroid->dedup->store pipeline (main()->int, sys.exit guard, cost cap, graceful no-key), produces a schema-valid current.json; integration test + full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: D-16 50-incident manual audit (phase human-UAT gate)</name>
  <what-built>The full pipeline is wired and unit/integration-tested with mocked DeepSeek. Now run it ONCE against the live feeds with a real DEEPSEEK_API_KEY to produce real incidents, then audit 50 of them for commune-hallucination (success criterion #5, D-16). This is the phase's production-readiness gate.</what-built>
  <how-to-verify>
    1. Ensure DEEPSEEK_API_KEY is set in pipeline/.env (dev). Then run: `python pipeline/scrape_news.py`
    2. Confirm the run log shows: at least one feed returned items; any down feed was skipped+logged (NEWS-01); counts of classified/rejected/written.
    3. Confirm data/incidents/current.json exists and validates: `python -c "import json; from pipeline.news.schema import validate_incidents_file; validate_incidents_file(json.load(open('data/incidents/current.json')))"`
    4. Run the audit: `python pipeline/scripts/audit_incidents.py` — it dumps up to 50 random incidents (title, outlet, source URL, assigned commune).
    5. For each sampled incident, open the source URL and confirm the assigned commune matches the article location. Note any hallucinations (wrong/invented commune).
    6. If hallucinations appear: adjust CONFIDENCE_THRESHOLD (currently 0.6) and/or the keyword pre-filter, re-run, re-audit (RESEARCH A3 — thresholds are starting guesses requiring empirical tuning).
  </how-to-verify>
  <resume-signal>Type "approved" if the 50-incident audit shows no commune hallucinations (pipeline production-ready), or describe the errors + which threshold to tune.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> external feeds + DeepSeek | Combines all upstream untrusted inputs into the final public JSON |
| operator/CI env -> DEEPSEEK_API_KEY | Secret supplied via env (dotenv dev / repo secret CI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Denial of Service / cost | scrape_news.py classification loop | mitigate | MAX_CLASSIFICATIONS_PER_RUN cap (D-17) + keyword pre-filter + seen-ledger bound DeepSeek calls per run |
| T-05-04-02 | Information Disclosure | DEEPSEEK_API_KEY handling | mitigate | Key read from env only; absent-key path logs a clear message and returns 1 (never prints the key) (V2) |
| T-05-04-03 | Tampering | commune hallucination reaching production | mitigate | D-16 blocking-human 50-incident audit gate before production-ready; reinforces classifier CUT/confidence rejection (defense in depth) |
| T-05-04-04 | Tampering | end-to-end output integrity | mitigate | merge_and_write runs the IncidentsFile gate before atomic write; integration test asserts current.json validates |
</threat_model>

<verification>
- `pytest pipeline/tests/ -q` full suite green (DeepSeek mocked throughout)
- One live run produces a schema-valid data/incidents/current.json with attributed incidents
- D-16: human confirms 50-incident audit shows no commune hallucination
</verification>

<success_criteria>
- NEWS-01..05 wired end to end in a single runnable entrypoint (main()->int, sys.exit guard)
- Cost-bounded (D-17 cap) and key-graceful
- Phase success criterion #5 satisfied via the D-16 human audit gate
</success_criteria>

<output>
Create `.planning/phases/05-rss-news-pipeline/05-04-SUMMARY.md` when done
</output>
