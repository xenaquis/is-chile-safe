---
phase: 03-leaflet-map-island
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - site/src/pages/map.astro
  - site/src/pages/es/mapa.astro
  - site/src/components/map/MapIsland.tsx
  - site/src/components/map/colors.ts
  - site/src/components/map/ChoroplethLayer.ts
  - site/src/components/map/LowZoomDotLayer.ts
  - site/src/components/map/Legend.tsx
  - site/src/components/map/ZoomControl.tsx
  - site/src/components/map/map.css
autonomous: true
requirements: [MAP-01, MAP-06]
must_haves:
  truths:
    - "Visiting /map/ and /es/mapa/ loads an interactive Leaflet map below the header"
    - "All 346 commune polygons render colored by precomputed incidence level"
    - "At country zoom (<8) communes are also represented as overview dots"
    - "A 5-swatch legend explains the incidence scale; desktop zoom buttons work"
  artifacts:
    - path: "site/src/components/map/MapIsland.tsx"
      provides: "Root client:only React island: L.map init, CARTO tiles, TopoJSON+payload fetch, choropleth mount"
      min_lines: 60
    - path: "site/src/components/map/ChoroplethLayer.ts"
      provides: "Native L.geoJSON + L.canvas layer factory, styleMap-from-level, setStyle-in-place"
      min_lines: 40
    - path: "site/src/components/map/colors.ts"
      provides: "Island-internal INCIDENCE_COLORS (chroma-js) + quantile helpers; NOT imported from Node-only colorScale.ts"
      min_lines: 10
    - path: "site/src/pages/map.astro"
      provides: "EN map page mounting MapIsland client:only"
      contains: "client:only"
  key_links:
    - from: "site/src/pages/map.astro"
      to: "MapIsland"
      via: "client:only=react"
      pattern: "client:only=\"react\""
    - from: "site/src/components/map/MapIsland.tsx"
      to: "/data/cead/geo/communes.topo.json"
      via: "runtime fetch on mount"
      pattern: "communes\\.topo\\.json"
    - from: "site/src/components/map/ChoroplethLayer.ts"
      to: "feature.properties.id"
      via: "CUT join with map-payload"
      pattern: "properties\\.id"
---

<objective>
Build the dedicated map pages (`/map/`, `/es/mapa/`) and the core choropleth island: a `client:only="react"` MapIsland that instantiates `L.map()`, adds CARTO Positron tiles, fetches the Wave-0 TopoJSON + latest `map-payload.json` at runtime, and renders all 346 commune polygons with the native `L.geoJSON()` + `L.canvas()` renderer colored by the precomputed `level`. Includes the low-zoom overview dots, the legend, and desktop zoom controls.

Purpose: This is the MAP-01 hero deliverable and the performance foundation (MAP-06) — canvas renderer, memoized styleMap ref, <100 KB geometry. Filters and the commune panel build on this island in later waves.
Output: map pages + MapIsland.tsx + ChoroplethLayer.ts + LowZoomDotLayer.ts + colors.ts + Legend.tsx + ZoomControl.tsx + map.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-leaflet-map-island/03-RESEARCH.md
@.planning/phases/03-leaflet-map-island/03-PATTERNS.md
@.planning/phases/03-leaflet-map-island/03-UI-SPEC.md
@site/src/components/Sparkline.astro
@site/src/lib/colorScale.ts

<interfaces>
map-payload.json (fetched on mount): { generated, year, comunas: Array<{ id: string /*CUT*/; rate: number; level: 1|2|3|4|5; by_family: number[7] }> }
communes.topo.json (Wave-0 asset): TopoJSON; decode with topojson.feature(topo, topo.objects[<key>]) -> FeatureCollection; each feature.properties = { id: CUT string, name: string }.
index.json (for low-zoom dots commune list + name lookup): Array<{ cut, name, slug, region_id }>.
INCIDENCE_COLORS scale (define in colors.ts, do NOT import colorScale.ts): chroma.scale(['#dbeef0','#9fd0cd','#f4d58d','#e8a05c','#c96b5a']).classes(5).colors(5).
Default polygon style: { fillColor: INCIDENCE_COLORS[level-1], fillOpacity: 0.55, color: '#ffffff', weight: 1.2 }.
Low-zoom dot radius: 4.5 + level * 1.1 (UI-SPEC).
Initial view: L.map.setView([-35.7, -71.8], 5); lowZoom threshold zoom < 8.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: colors.ts + ChoroplethLayer.ts + LowZoomDotLayer.ts (choropleth contracts and layer factories)</name>
  <read_first>
    - site/src/lib/colorScale.ts (the Node-only color scale to mirror — do NOT import it in the island)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md sections ChoroplethLayer.ts, LowZoomDotLayer.ts, Shared Pattern 1 (Incidence Color Constants)
    - .planning/phases/03-leaflet-map-island/03-RESEARCH.md Pattern 3, Pattern 8, Pitfall 6 (L.canvas + non-path layers)
    - .planning/phases/03-leaflet-map-island/03-UI-SPEC.md section Choropleth Layer — Interaction States
  </read_first>
  <action>
    Create site/src/components/map/colors.ts exporting INCIDENCE_COLORS (chroma-js 5-class teal->red scale, imported directly from chroma-js per RESEARCH Pitfall 3 — never re-export colorScale.ts), DEFAULT_STYLE, plus computeQuantileBreaks(sorted, classes) and levelFromBreaks(rate, breaks) helpers (used in Wave 2 for per-family coloring; define now as the shared contract). Create site/src/components/map/ChoroplethLayer.ts exporting: mountChoroplethLayer(map, geoData, styleMapRef, polyIdxRef, onClickCut) which builds the layer with renderer: L.canvas() (declared ONLY on this layer per Pitfall 6), style reads from styleMapRef, onEachFeature binds tooltip(feature.properties.name), click -> onClickCut(feature.properties.id), mouseover/mouseout hover restyle, and populates polyIdxRef keyed by CUT; applyStyleMap(layer, styleMapRef) calling layer.setStyle in place (D-12, never re-mount); buildStyleMapFromLevel(comunas) producing the CUT->PathOptions map from the precomputed level (D-11); and highlightSelected(layer, polyIdxRef, cut, prevCut) for selected-state styling (per-04 wiring). Create site/src/components/map/LowZoomDotLayer.ts exporting mountLowZoomDots(map, comunas, onClickCut) building an L.layerGroup of L.circleMarker (radius 4.5 + level*1.1) — do NOT set a global canvas renderer (Pitfall 6). Join: each commune dot needs lat/lng; derive centroid from the loaded GeoJSON feature for that CUT (compute a simple ring centroid) since index.json has no coordinates.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - colors.ts exports INCIDENCE_COLORS (length 5), DEFAULT_STYLE, computeQuantileBreaks, levelFromBreaks; no import from ../../lib/colorScale.
    - ChoroplethLayer.ts exports mountChoroplethLayer, applyStyleMap, buildStyleMapFromLevel, highlightSelected; L.canvas() appears only inside L.geoJSON options.
    - LowZoomDotLayer.ts exports mountLowZoomDots using L.circleMarker with radius formula 4.5 + level*1.1.
    - npx astro check exits 0.
  </acceptance_criteria>
  <done>Choropleth + low-zoom layer factories and color contracts compile; canvas renderer scoped to the geoJSON layer only.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MapIsland.tsx (root island: L.map init, CARTO tiles, TopoJSON+payload fetch, mount choropleth) + Legend + ZoomControl + map.css</name>
  <read_first>
    - ischilesafe.com/map-view.jsx (MapView fn lines 100-322 — primary analog: refs, map init, CARTO tile, flyTo, JSX structure)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md sections MapIsland.tsx, Legend.tsx, ZoomControl.tsx, Shared Patterns 2 (CSS tokens) + 4 (Leaflet CSS import) + 5 (graceful fetch)
    - .planning/phases/03-leaflet-map-island/03-RESEARCH.md Pattern 2, Pitfall 1 (Leaflet CSS in SSR), Pitfall 5 (double-init guard)
    - .planning/phases/03-leaflet-map-island/03-UI-SPEC.md sections Page Layout, Choropleth interaction states, Empty/Error States (map load failure), Performance Constraints
    - site/src/components/map/colors.ts and ChoroplethLayer.ts and LowZoomDotLayer.ts (Task 1 outputs)
  </read_first>
  <action>
    Create site/src/components/map/MapIsland.tsx as the client:only root: import 'leaflet/dist/leaflet.css' inside this TSX (Pitfall 1). Set up refs (divRef, mapRef, tileRef, layerRef, dotsRef, polyIdxRef, styleMapRef) and state (year=2025, lowZoom, crimeFamily=null, selected=null) per PATTERNS. Map-init useEffect with the double-init guard (if mapRef.current return; cleanup map.remove()), zoomControl:false, setView([-35.7,-71.8],5), CARTO Positron tile layer (D-08, crossOrigin true), and a zoomend handler setting lowZoom = map.getZoom() < 8. A data useEffect that fetches /data/cead/geo/communes.topo.json (decode via topojson.feature) and /data/cead/map-payload.json once on mount using the graceful safeFetch pattern; on TopoJSON success build styleMapRef via buildStyleMapFromLevel(payload.comunas) and call mountChoroplethLayer; on fetch failure render the centered map-load-error overlay (UI-SPEC copy "Map failed to load" / "No se pudo cargar el mapa" + Reload). A lowZoom useEffect that adds/removes the LowZoomDotLayer (centroids from the loaded features). Render JSX: map-stage > map-canvas(divRef) + Legend + ZoomControl. Accept a lang: 'en'|'es' prop. Create Legend.tsx (5 swatches from INCIDENCE_COLORS + Baja/Alta + unit label, UI-SPEC copy) and ZoomControl.tsx (desktop +/- buttons calling mapRef.current.zoomIn/zoomOut, aria-labels). Create site/src/components/map/map.css referencing Phase-2 tokens via var() (do NOT redefine tokens): .map-stage height calc(100vh - 64px), .map-canvas, .legend, .zoom-ctl (hidden under 640px), chip base styles, .user-dot/.ev-dot for later waves. Read map.css via an import in MapIsland.tsx.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - MapIsland.tsx imports leaflet/dist/leaflet.css, guards double-init, sets CARTO Positron tiles, fetches communes.topo.json + map-payload.json on mount, and mounts the choropleth via mountChoroplethLayer.
    - Legend.tsx renders exactly 5 swatches mapped from INCIDENCE_COLORS with localized Baja/Alta + unit label.
    - ZoomControl.tsx renders +/- buttons with aria-labels; map.css hides .zoom-ctl below 640px.
    - npx astro check exits 0.
  </acceptance_criteria>
  <done>Island mounts a CARTO basemap and the 346-polygon canvas choropleth; legend + zoom controls present; graceful map-load error overlay defined.</done>
</task>

<task type="auto">
  <name>Task 3: map.astro + es/mapa.astro pages mounting the island; build + map.mjs green</name>
  <read_first>
    - site/src/pages/commune/[slug].astro (analog: BaseLayout usage, head/meta/hreflang convention)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md section map.astro / es/mapa.astro
    - .planning/phases/03-leaflet-map-island/03-UI-SPEC.md Copywriting Contract (page title + meta description EN/ES)
    - site/src/components/map/MapIsland.tsx (Task 2 output)
    - site/scripts/validate/map.mjs (Wave-0 validator — its dist/ assertions activate now)
  </read_first>
  <action>
    Create site/src/pages/map.astro (EN) and site/src/pages/es/mapa.astro (ES). Each wraps BaseLayout with the UI-SPEC page title + meta description (EN: "Chile Crime Map — Interactive"; ES: "Mapa de Incidencia en Chile — Interactivo") and mounts MapIsland with client:only="react" (D-06) passing lang="en" / lang="es". No getStaticPaths (single static routes). Ensure reciprocal hreflang for /map/ <-> /es/mapa/ <-> x-default per the BaseLayout/commune convention (inject the three alternates if BaseLayout does not auto-derive them for these routes). Then run a full build and the map validator. Declare the focal point of /map/ in a brief comment as the choropleth canvas (UI checker FLAG).
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/map.mjs && node scripts/validate/all.mjs</automated>
  </verify>
  <acceptance_criteria>
    - dist/map/index.html and dist/es/mapa/index.html exist after build.
    - Each map page HTML contains the island mount (astro-island) and the locate aria-label text is present once Wave 3 adds it — for this wave, map.mjs island-mount + TopoJSON assertions pass; the locate aria-label assertion may be deferred to Wave 3 (note in SUMMARY if temporarily skipped).
    - node scripts/validate/all.mjs exits 0 (existing 7 validators + map.mjs); content pages remain React/Leaflet-free (regression guard inside map.mjs).
  </acceptance_criteria>
  <done>/map/ and /es/mapa/ build and serve the interactive choropleth island; full validator suite green; content pages unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to static JSON | island fetches only same-origin /data/cead/* files (no arbitrary URLs) |
| browser to CARTO CDN | basemap tiles; keyless, public; no secret to leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | CARTO Positron tile requests | accept | Public keyless tile CDN (D-08); no API key present to leak |
| T-03-01-02 | Tampering | TopoJSON/payload fetch | mitigate | Fetch only hardcoded same-origin /data/cead/* paths; graceful map-load-error overlay on failure, never eval'd |
| T-03-01-03 | Denial of Service | 346-polygon render on mobile | mitigate | L.canvas() renderer + <100 KB geometry + memoized styleMap ref (MAP-06) |
</threat_model>

<verification>
- `cd site && npx astro check` exits 0 after each task.
- `cd site && npm run build && node scripts/validate/all.mjs` exits 0 (map.mjs included).
- Manual (deferred to phase gate): open /map/, confirm 346 polygons render colored; pan/zoom smooth; low-zoom dots appear under zoom 8.
</verification>

<success_criteria>
- /map/ (EN) and /es/mapa/ (ES) load the interactive Leaflet island below the header.
- All 346 commune polygons render via native L.geoJSON + L.canvas, colored by precomputed level (MAP-01).
- Geometry is the <100 KB Wave-0 TopoJSON; styles memoized in a ref (MAP-06).
- Legend, low-zoom dots, and desktop zoom controls present; content pages still React/Leaflet-free.
</success_criteria>

<output>
Create `.planning/phases/03-leaflet-map-island/03-01-SUMMARY.md` when done.
</output>
