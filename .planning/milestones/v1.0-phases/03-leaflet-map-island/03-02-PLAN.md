---
phase: 03-leaflet-map-island
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - site/src/components/map/MapIsland.tsx
  - site/src/components/map/MapTopbar.tsx
  - site/src/components/map/FiltersRow.tsx
  - site/src/components/map/SearchBox.tsx
  - site/src/components/map/ResultPanel.tsx
  - site/src/components/map/PanelSparkline.tsx
  - site/src/components/map/PanelFamilyBars.tsx
  - site/src/components/map/IncidentsList.tsx
  - site/src/components/map/Toast.tsx
  - site/src/components/map/map.css
autonomous: true
requirements: [MAP-02, MAP-03]
must_haves:
  truths:
    - "Changing the year selector recolors the map from map-payload-{year}.json without a page reload"
    - "Selecting a crime-type chip recolors the choropleth from by_family rates via client-side quantile breaks"
    - "Clicking a commune opens a panel with tasa, trend, national rank, a 2005-present sparkline, and a crime-family breakdown"
    - "Searching a commune name selects and flies to it"
  artifacts:
    - path: "site/src/components/map/FiltersRow.tsx"
      provides: "Year select + 8 crime-type chips (catalog-indexed) + events toggle"
      min_lines: 30
    - path: "site/src/components/map/ResultPanel.tsx"
      provides: "Commune detail panel fetching comunas/{cut}.json with skeleton loading"
      min_lines: 50
    - path: "site/src/components/map/PanelSparkline.tsx"
      provides: "Inline SVG sparkline mirroring Sparkline.astro contract"
      min_lines: 20
  key_links:
    - from: "site/src/components/map/MapIsland.tsx"
      to: "/data/cead/map-payload-{year}.json"
      via: "fetch on year change"
      pattern: "map-payload-"
    - from: "site/src/components/map/ResultPanel.tsx"
      to: "/data/cead/comunas/{cut}.json"
      via: "fetch on panel open"
      pattern: "comunas/"
    - from: "site/src/components/map/FiltersRow.tsx"
      to: "by_family index"
      via: "catalog family_keys order"
      pattern: "by_family"
---

<objective>
Add the filter controls (year selector + crime-type chips + search) and the commune detail panel to the island. Year change loads `map-payload-{year}.json` and recolors via `setStyle` in place; crime-type chips recolor from `by_family[]` using client-side quantile breaks; clicking/searching a commune opens a `ResultPanel` that fetches `comunas/{cut}.json` and renders tasa, trend, national rank, an inline-SVG sparkline (2005–present), and the 7-family breakdown.

Purpose: Delivers MAP-02 (filter by year + crime type, no reload) and MAP-03 (commune territorial panel) on top of the Wave-1 choropleth, reusing the Phase-2 visual language (Sparkline.astro, FamilyBreakdownBars.astro contracts).
Output: MapTopbar, FiltersRow, SearchBox, ResultPanel, PanelSparkline, PanelFamilyBars, IncidentsList, Toast; MapIsland + map.css extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-leaflet-map-island/03-RESEARCH.md
@.planning/phases/03-leaflet-map-island/03-PATTERNS.md
@.planning/phases/03-leaflet-map-island/03-UI-SPEC.md
@site/src/components/Sparkline.astro
@site/src/components/FamilyBreakdownBars.astro
@.planning/phases/03-leaflet-map-island/03-01-SUMMARY.md

<interfaces>
map-payload-{year}.json (year filter, 2005-2025 available): same shape as map-payload.json — { year, comunas: Array<{ id (CUT); rate; level; by_family: number[7] }> }.
comunas/{cut}.json (panel fetch): { id, name, slug, region_id, population, low_population, national_rank, regional_rank, trend, featured_rates, series: Array<{ year: number; rate_per_100k: number; by_family: Record<familyKey, number> }>, last_updated }.
  NOTE: per-commune series.by_family is a RECORD keyed by family name; map-payload.by_family is an ARRAY indexed by catalog order. Do not confuse them.
catalog.json family_keys order (by_family ARRAY index -> key): 0 vida, 1 robos_violentos, 2 vif, 3 drogas, 4 armas, 5 propiedad, 6 incivilidades.
UI-SPEC chip DISPLAY order (after Todos): vida, propiedad, robos_violentos, incivilidades, vif, drogas, armas. Map chip -> by_family index via catalog order, NOT chip order.
PanelSparkline contract (mirror Sparkline.astro): series Array<{year, rate_per_100k, partial?}>, activeYear, primaryColor='#0f766e'.
PanelFamilyBars props: { byFamily: number[7] (catalog-indexed), locale, level } — reorder to FAMILY_ORDER (vida, propiedad, robos_violentos, incivilidades, vif, drogas, armas) after index->key mapping.
Quantile helpers available from colors.ts (Wave 1): computeQuantileBreaks, levelFromBreaks, INCIDENCE_COLORS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FiltersRow + SearchBox + MapTopbar + year/crime-type recolor wiring in MapIsland</name>
  <read_first>
    - ischilesafe.com/map-view.jsx (.filters-row + .map-topbar blocks lines 284-299; choropleth setStyle on filter change)
    - ischilesafe.com/components.jsx (SearchBox lines 63-101; SearchIcon lines 41-61)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md sections FiltersRow.tsx, SearchBox.tsx, MapTopbar
    - .planning/phases/03-leaflet-map-island/03-UI-SPEC.md sections Filter Controls (chip order, catalog mapping), Copywriting Contract (chip + search labels)
    - data/cead/meta/catalog.json (family_keys authoritative index order)
    - data/cead/meta/index.json (search commune list: cut/name/slug/region_id)
    - site/src/components/map/ChoroplethLayer.ts + colors.ts (applyStyleMap, buildStyleMapFromLevel, buildStyleMapFromFamily, quantile helpers)
  </read_first>
  <action>
    Create FiltersRow.tsx: year <select> (AVAILABLE_YEARS 2005-2025 newest-first, default 2025, aria-label per UI-SPEC); 8 chips = Todos + 7 families in UI-SPEC display order with featured accent dots on vida/propiedad; events toggle chip (wired in Wave 3 — render now, no-op handler for this wave or pass through). CHIP_DEFS must carry both the display order and the catalog by_family index for each family (map chip -> index via catalog.json family_keys, NOT chip order). Create SearchBox.tsx: port the prototype's prefix-filter dropdown driven by an index array (from index.json), inline SearchIcon SVG, onSelect(cut), onLocate() (locate wired Wave 3). Create MapTopbar.tsx composing SearchBox + LocateButton + FiltersRow. Wire into MapIsland: on year change, fetch /data/cead/map-payload-{year}.json (graceful safeFetch; on null show Toast "Datos no disponibles para este año"), then rebuild styleMapRef — if crimeFamily is null use buildStyleMapFromLevel, else buildStyleMapFromFamily(comunas, catalogIndex) — and call applyStyleMap (D-12: setStyle in place, never re-mount). On crime-type chip change, recompute styleMapRef from the current payload and applyStyleMap (no extra fetch, D-10). Add a Toast state + render <Toast> in MapIsland.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - FiltersRow.tsx renders a year select (21 options 2005-2025) and 8 chips in UI-SPEC order with vida/propiedad accent dots; each family chip maps to its catalog by_family index.
    - SearchBox.tsx filters index.json commune names by prefix and calls onSelect(cut).
    - MapIsland recolors via applyStyleMap on both year change (after fetching map-payload-{year}.json) and crime-type change (no extra fetch); never re-mounts the L.geoJSON layer.
    - npx astro check exits 0.
  </acceptance_criteria>
  <done>Year + crime-type filters recolor the choropleth in place without reload; commune search selects communes.</done>
</task>

<task type="auto">
  <name>Task 2: PanelSparkline + PanelFamilyBars + IncidentsList + Toast (panel sub-components)</name>
  <read_first>
    - site/src/components/Sparkline.astro (full file — exact contract to mirror, including the width/step WR-05 fix)
    - site/src/components/FamilyBreakdownBars.astro (full file — FAMILY_ORDER, FEATURED, FAMILY_LABELS, bar-width % pattern, accent dot)
    - ischilesafe.com/components.jsx (Toast lines 152-155)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md sections PanelSparkline.tsx, PanelFamilyBars.tsx, IncidentsList.tsx, Toast.tsx
    - .planning/phases/03-leaflet-map-island/03-UI-SPEC.md Commune Detail Panel (sparkline + family sections), Toast spec (padding 10px not 8px — fold checker FLAG: use padding 10px 20px)
  </read_first>
  <action>
    Create PanelSparkline.tsx mirroring Sparkline.astro exactly (inline SVG <rect> bars, width/step derived so bars never overflow per WR-05, active-year bar uses primaryColor prop, partial-year opacity 0.5, aria-hidden). Create PanelFamilyBars.tsx porting FamilyBreakdownBars.astro: FAMILY_ORDER (vida, propiedad, robos_violentos, incivilidades, vif, drogas, armas), FEATURED set {vida, propiedad}, FAMILY_LABELS EN/ES; props { byFamily: number[7] (catalog-indexed), locale, level } — map index->key via catalog order then reorder to FAMILY_ORDER; bar fill var(--s{level}); bar-width is a CSS % string (round(val/max*100)+'%'); accent dot on featured. Create IncidentsList.tsx (event rows: title, date + outlet, source link with rel="noopener noreferrer"; empty state em-dash). Create Toast.tsx with 3000ms auto-dismiss, role="status", aria-live="polite". Apply the checker FLAG: Toast CSS padding 10px 20px (UI-SPEC), and add the named typography exception comment for the 12px mobile legend label in map.css.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - PanelSparkline.tsx produces inline SVG bars with no charting library; active-year bar uses primaryColor; partial-year bars at 0.5 opacity.
    - PanelFamilyBars.tsx renders 7 bars in FAMILY_ORDER with % widths and featured accent dots, correctly mapping the catalog-indexed byFamily array.
    - IncidentsList external links use rel="noopener noreferrer"; Toast auto-dismisses with aria-live="polite".
    - npx astro check exits 0.
  </acceptance_criteria>
  <done>Panel sub-components compile and mirror the Phase-2 visual contracts; security-safe external links; checker FLAGs folded.</done>
</task>

<task type="auto">
  <name>Task 3: ResultPanel (fetch comunas/{cut}.json, render full panel) + wire into MapIsland; build + validators green</name>
  <read_first>
    - ischilesafe.com/map-view.jsx (ResultPanel fn lines 2-98 — head, stat card, mini-stats, sparkline, family bars, CTA, official row)
    - .planning/phases/03-leaflet-map-island/03-PATTERNS.md section ResultPanel.tsx (fetch-on-open, skeleton loading, stat-card, CTA, family-bars wiring)
    - .planning/phases/03-leaflet-map-island/03-UI-SPEC.md Commune Detail Panel (full content order + skeleton + close behavior + copy)
    - data/cead/comunas/13101.json (real shape: series with by_family RECORD, national_rank, trend, slug)
    - site/src/components/map/PanelSparkline.tsx + PanelFamilyBars.tsx + IncidentsList.tsx (Task 2 outputs)
    - site/src/components/map/MapIsland.tsx (selected state + selectCommune wiring from Wave 1)
  </read_first>
  <action>
    Create ResultPanel.tsx: props { cut, lang, year, onClose }. On cut change, fetch /data/cead/comunas/{cut}.json (graceful) showing the skeleton (aria-busy, 3 placeholder bars) while loading. Render top-to-bottom per UI-SPEC: panel head (name h2 + region name + 44px close button), level chip + "Datos CEAD" attribution, stat card (rate 28px/700 + "Tasa por 100.000 hab. ({year})" with es-CL thousands separator), mini-stats row (TrendChip from data.trend + "Posición nacional #{national_rank} de {N}"), sparkline section (PanelSparkline from series mapped to {year, rate_per_100k, partial?}, activeYear=year, endpoints 2005 and latest), family breakdown (PanelFamilyBars — derive the catalog-indexed by_family ARRAY for the selected year from the series entry's by_family RECORD using catalog family_keys order; pass commune level), IncidentsList (from any incidents in commune JSON, else em-dash), CTA "Ver ficha completa"/"View full profile" linking /commune/{slug}/ or /es/comuna/{slug}/, and the official CEAD source row. Wire into MapIsland: render <ResultPanel> when selected is set; selectCommune(cut) sets selected + flyToBounds (Wave-1 helper) + highlightSelected; close clears selected and resetStyle. Add .has-panel layout class so the map pans for the desktop right panel / mobile bottom sheet (UI-SPEC). Run full build + validators.
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/all.mjs</automated>
  </verify>
  <acceptance_criteria>
    - ResultPanel.tsx fetches /data/cead/comunas/{cut}.json on open, shows a skeleton while loading, and renders rate/trend/national rank/sparkline/family breakdown + localized CTA to the Phase-2 commune page.
    - The family breakdown correctly converts the commune series by_family RECORD into the catalog-indexed ARRAY PanelFamilyBars expects.
    - MapIsland renders ResultPanel on commune select and clears it on close; node scripts/validate/all.mjs exits 0; content pages remain React/Leaflet-free.
  </acceptance_criteria>
  <done>Clicking or searching a commune opens a full territorial panel (MAP-03); year + crime filters recolor without reload (MAP-02); suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to static JSON | year payloads + commune JSON fetched from same-origin /data/cead/* only |
| rendered commune/incident strings | data rendered via React JSX (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering (XSS) | ResultPanel / IncidentsList rendering commune + future incident strings | mitigate | Render all data via React JSX (auto-escaped); never dangerouslySetInnerHTML; external links rel="noopener noreferrer" (note for Phase-5 incident data downstream) |
| T-03-02-02 | Tampering | year payload + commune fetch | mitigate | Fetch only hardcoded same-origin /data/cead/* paths derived from numeric year + CUT; safeFetch try/catch, graceful toast on failure |
| T-03-02-03 | Input Validation | commune search box | mitigate | Client-side Array.filter over the known 346-commune index only; no eval, no server query |
</threat_model>

<verification>
- `cd site && npx astro check` exits 0 after each task.
- `cd site && npm run build && node scripts/validate/all.mjs` exits 0.
- Manual (deferred to phase gate): change year + crime chip and confirm recolor without navigation; click 3 communes (high/low/low-pop) and confirm panel matches commune JSON.
</verification>

<success_criteria>
- Year selector recolors the map from map-payload-{year}.json via setStyle in place, no reload (MAP-02).
- Crime-type chips recolor from by_family rates via client-side quantile breaks, no extra fetch (MAP-02).
- Commune click/search opens a panel with tasa, trend, national rank, 2005-present sparkline, and 7-family breakdown (MAP-03).
- Validator suite green; content pages still React/Leaflet-free.
</success_criteria>

<output>
Create `.planning/phases/03-leaflet-map-island/03-02-SUMMARY.md` when done.
</output>
