---
phase: 02-astro-site-programmatic-pages
plan: 06
type: execute
wave: 4
depends_on: [02-03, 02-04, 02-05]
files_modified:
  - site/astro.config.mjs
  - site/public/manifest.json
  - site/public/icon-192.png
  - site/public/icon-512.png
  - site/src/pages/index.astro
  - site/src/pages/es/index.astro
  - site/scripts/validate/hreflang.mjs
  - site/scripts/validate/schema.mjs
  - site/scripts/validate/all.mjs
autonomous: false
requirements: [SEO-01, SEO-02, SEO-03, SEO-04]
must_haves:
  truths:
    - "sitemap-index.xml + per-locale sitemap build, listing only rollout-enabled commune URLs plus all region/crime URLs, with i18n hreflang alternates"
    - "Every built page has a self-canonical and reciprocal en/es/x-default hreflang pointing at pages that actually exist in dist/"
    - "Territorial pages embed valid Dataset/Place (commune+region) or Dataset (crime) JSON-LD crediting CEAD"
    - "manifest.json + theme-color ship; no SEO-critical content depends on client-side rendering"
    - "Full ROLLOUT_ALL build produces 346×2 + 16×2 + 7×2 pages and all validators pass"
  artifacts:
    - path: "site/astro.config.mjs"
      provides: "sitemap filter gated on rollout allowlist + i18n hreflang"
      contains: "filter"
    - path: "site/public/manifest.json"
      provides: "PWA manifest (name, theme_color, icons)"
      contains: "theme_color"
    - path: "site/scripts/validate/all.mjs"
      provides: "aggregate validator running structure/commune/rollout/region/crime/hreflang/schema"
      contains: "hreflang"
  key_links:
    - from: "site/astro.config.mjs sitemap filter()"
      to: "site/src/config/rollout.json"
      via: "rollout-gated URL inclusion + ROLLOUT_ALL override"
      pattern: "rollout|ROLLOUT_ALL"
    - from: "site/scripts/validate/hreflang.mjs"
      to: "dist/ built files"
      via: "reciprocity check that alternate URLs resolve to real files"
      pattern: "hreflang"
---

<objective>
Finish the SEO + PWA layer and machine-verify the whole phase: tighten the sitemap filter to the rollout gate with i18n hreflang (SEO-02), add the PWA manifest + icons (SEO-04), add the EN/ES home placeholders so root URLs resolve, then fill the hreflang-reciprocity (SEO-01), Schema.org (SEO-03), and aggregate validators and run the full ROLLOUT_ALL build as the phase gate.

Purpose: This wave proves the four SEO requirements hold across the entire built site — reciprocal hreflang, sitemap + canonical, structured data, and PWA meta — and confirms the full 346-commune build succeeds before phase sign-off. A human visual check closes the end-of-phase UAT items (palette, prose readability) that HTML assertions cannot cover.
Output: sitemap, manifest, home placeholders, complete validator suite, and a green full build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md
@.planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md
@.planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md
@.planning/phases/02-astro-site-programmatic-pages/02-VALIDATION.md
@site/astro.config.mjs
@site/src/config/rollout.json
@site/src/lib/data.ts

<interfaces>
<!-- From prior plans. -->
site/src/config/rollout.json: { enabled: [12 CUT codes] }
site/src/lib/data.ts: loadIndex(), loadRolloutCuts(), buildCommuneSlugPairMap() (via slugMaps.ts)
Built dist tree (default rollout): dist/commune/{slug}/, dist/es/comuna/{slug}/ (12 each), dist/region + dist/es/region (16 each), dist/crime + dist/es/delito (7 each)
@astrojs/sitemap filter(url) receives full URL strings; i18n config auto-generates xhtml:link hreflang alternates (RESEARCH Pattern 3 lines 382-416)
UI-SPEC PWA manifest shape (lines 294-309): name, short_name, start_url, display, background_color #f5f8f8, theme_color #0f766e, icons 192+512
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rollout-gated sitemap + PWA manifest + home placeholders</name>
  <files>site/astro.config.mjs, site/public/manifest.json, site/public/icon-192.png, site/public/icon-512.png, site/src/pages/index.astro, site/src/pages/es/index.astro</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pattern 3 rollout sitemap lines 382-416; Pitfall 3 sitemap i18n mismatch lines 523-527; A4 rollout CUT-vs-slug lines 689; Open Q3 lines 706-709)
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (PWA manifest lines 292-311; theme-color lines 263)
    - .planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md (D-19 sitemap, D-20 PWA, D-24/D-26 rollout gate scope)
    - site/astro.config.mjs (current permissive filter from 02-01), site/src/config/rollout.json
  </read_first>
  <action>
    Update `site/astro.config.mjs` sitemap filter to the real rollout gate (RESEARCH Pattern 3): build an ENABLED_SLUGS set at config time by joining rollout.json CUT codes with index.json slugs (A4 — rollout stores CUT codes; derive slugs); the filter includes all /region/, /es/region/, /crime/, /es/delito/, and home URLs unconditionally, includes /commune/{slug}/ and /es/comuna/{slug}/ only when the slug is in ENABLED_SLUGS, and short-circuits to include everything when process.env.ROLLOUT_ALL==='true'. Keep the sitemap i18n config exactly matching the astro i18n config (Pitfall 3). Confirm the filter receives slug-bearing URLs and the region/crime guards do not accidentally drop pages.
    Create `site/public/manifest.json` per UI-SPEC lines 294-309 (name "Chile Safety Map", short_name "ChileSafe", start_url "/", display standalone, background_color #f5f8f8, theme_color #0f766e, icons 192 + 512). Add `site/public/icon-192.png` and `site/public/icon-512.png` — generate simple solid-teal PNG placeholder icons (the PinGlyph or a teal square) at the two sizes so the manifest references resolve; note in the SUMMARY that final brand icons can be swapped later.
    Create `site/src/pages/index.astro` (EN root) and `site/src/pages/es/index.astro` (ES root) as minimal placeholder home pages using BaseLayout (so root URLs resolve, carry hreflang/canonical/manifest, and the sitemap home entry is valid). Real editorial home content lands in Phase 4 — keep these to a hero + links to a few territorial pages; note the Phase-4 boundary in a comment.
  </action>
  <verify>
    <automated>cd site && npm run build</automated>
  </verify>
  <verify>
    <automated>cd site && node scripts/validate/structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - astro.config.mjs sitemap filter references the rollout allowlist and the ROLLOUT_ALL override
    - After a default build, the generated sitemap lists the 12 rollout commune URLs (×2 locales) and all 16×2 region + 7×2 crime URLs, and does NOT list non-rollout communes
    - site/public/manifest.json contains theme_color #0f766e and references icon-192.png + icon-512.png, both of which exist in site/public/
    - dist/index.html and dist/es/index.html exist with canonical + hreflang
    - `cd site && npm run build` exits 0
  </acceptance_criteria>
  <done>Sitemap is rollout-gated with i18n hreflang; PWA manifest + icons ship; EN/ES home placeholders resolve.</done>
</task>

<task type="auto">
  <name>Task 2: hreflang reciprocity + Schema.org validators + aggregate runner</name>
  <files>site/scripts/validate/hreflang.mjs, site/scripts/validate/schema.mjs, site/scripts/validate/all.mjs</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-VALIDATION.md (hreflang 2-04-01, canonical/sitemap 2-04-02, schema 2-04-03, pwa 2-04-04 lines 49-52; full suite line 23)
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pitfall 2 reciprocity lines 513-521; Schema.org example lines 613-642; SEO-01..04 test map lines 746-757)
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (hreflang pattern lines 244-253; Schema.org lines 266-288)
    - site/scripts/validate/ (existing commune/rollout/region/crime/structure from prior plans)
  </read_first>
  <action>
    Fill `site/scripts/validate/hreflang.mjs` (replace 02-01 stub): walk every index.html in dist/, parse the three hreflang alternates (en, es, x-default) and the canonical; assert each page has all three alternates + a self-canonical; assert the en and es alternate URLs each resolve to an actually-existing dist/ file (reciprocity — Pitfall 2: no dangling partner); assert x-default points at the EN path. Also assert each page carries a theme-color meta and a manifest link (folds SEO-04 page-level checks in). Exit non-zero naming the first offending page.
    Fill `site/scripts/validate/schema.mjs` (replace 02-01 stub): for every commune + region page, assert an application/ld+json script exists, parses as valid JSON, has @type including both "Dataset" and "Place", and a creator naming CEAD; for every crime page, assert a Dataset JSON-LD with no Place type. Assert the temporalCoverage ends at a complete year (not 2026 partial). Exit non-zero on failure.
    Update `site/scripts/validate/all.mjs`: run structure, commune, rollout, region, crime, hreflang, schema in sequence; any non-zero exit fails the suite; print a per-check PASS/FAIL summary. This is the phase-gate command (`npm run validate`).
  </action>
  <verify>
    <automated>cd site && npm run build</automated>
  </verify>
  <verify>
    <automated>cd site && node scripts/validate/all.mjs</automated>
  </verify>
  <acceptance_criteria>
    - hreflang.mjs asserts 3 alternates + self-canonical per page AND that alternate URLs resolve to existing dist files; exits non-zero on a dangling partner
    - schema.mjs asserts Dataset+Place JSON-LD on commune/region pages, Dataset-only on crime pages, and CEAD as creator
    - all.mjs runs all seven validators and fails if any fails
    - `cd site && node scripts/validate/all.mjs` exits 0 against a default build
  </acceptance_criteria>
  <done>SEO-01/02/03/04 are machine-verified across the full built site by the aggregate validator.</done>
</task>

<task type="auto">
  <name>Task 3: Full ROLLOUT_ALL build gate</name>
  <files>site/scripts/validate/all.mjs</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-VALIDATION.md (phase gate lines 33, 763; File Count Math)
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (File Count Math lines 661-678)
    - site/scripts/validate/rollout.mjs (EXPECT_ALL mode from 02-03)
  </read_first>
  <action>
    Run the full-site build with ROLLOUT_ALL=true and verify the complete output. Ensure all.mjs (or rollout.mjs EXPECT_ALL mode) asserts 346 commune dirs ×2 locales + 16 region ×2 + 7 crime ×2 (= 743 HTML pages per RESEARCH File Count Math) when EXPECT_ALL/ROLLOUT_ALL is set, and that the validator suite still passes at full scale (hreflang reciprocity across all 346, schema on all territorial pages). Confirm total file count is well under the 20k Cloudflare limit. Do NOT commit a 346-page dist — this is a build-gate verification; the committed rollout default stays at 12. Record the full-build counts in the SUMMARY.
  </action>
  <verify>
    <automated>cd site && npx cross-env ROLLOUT_ALL=true npm run build</automated>
  </verify>
  <verify>
    <automated>cd site && npx cross-env EXPECT_ALL=true node scripts/validate/all.mjs</automated>
  </verify>
  <acceptance_criteria>
    - ROLLOUT_ALL=true build produces 346 dirs under dist/commune and 346 under dist/es/comuna
    - region (16×2) and crime (7×2) counts unchanged at full scale
    - the full validator suite (EXPECT_ALL) exits 0 — hreflang reciprocity + schema hold across all 346 communes
    - ROLLOUT_ALL build exits 0
  </acceptance_criteria>
  <done>The full 346-commune bilingual build succeeds and passes every validator; default committed rollout remains 12.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-of-phase visual + prose UAT</name>
  <what-built>
    The complete Phase 2 static site: 12 rollout commune pages (EN+ES), 16 region pages (EN+ES), 7 crime-type pages (EN+ES), home placeholders, sitemap, PWA manifest — all server-rendered with the prototype teal palette and zero client JS.
  </what-built>
  <how-to-verify>
    1. Run `cd site && npm run dev` and open http://localhost:4321/commune/santiago/ and http://localhost:4321/es/comuna/santiago/ — confirm the teal palette (#0f766e), the 5-level incidence colors, the SVG sparkline (with the faint 2026 partial bar), the 7 family bars (vida + propiedad first with accent dots), and the comparable-commune card all render correctly.
    2. Open one small/low-population commune and one region page (e.g. /region/aysen/ or its real slug) — confirm the low-population caveat + the `*` ranking marks render.
    3. Spot-read the prose on 3 communes across different trend/rank tiers in both locales — confirm it reads naturally, names real data, and uses sober language (no "zona peligrosa", "ranking definitivo", "zona segura garantizada").
    4. View source on any page — confirm reciprocal hreflang + canonical in <head> and no client:* hydration on content.
  </how-to-verify>
  <resume-signal>Type "approved" or describe rendering/prose issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-1 JSON + config -> sitemap/manifest/JSON-LD output | Build-time generation of SEO artifacts from trusted data |
| npm (cross-env, build deps) | Dev dependency used for env flag in build commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | sitemap filter dropping/adding wrong URLs | mitigate | hreflang.mjs + rollout.mjs assert the sitemap/dist match the rollout allowlist exactly; Pitfall 3 i18n-config-match enforced |
| T-02-17 | Information disclosure | non-rollout communes leaking via sitemap | mitigate | Filter excludes non-rollout commune URLs; validator asserts only the 12 appear (crawl-budget + unreviewed-page protection, D-26) |
| T-02-18 | Tampering | manifest/JSON-LD escaping | accept | manifest.json is static; JSON-LD via JSON.stringify (02-02); values are public CEAD aggregates |
| T-02-SC | Tampering | npm supply chain (cross-env if added) | mitigate | cross-env is a widely-used dev dep; pin exact version; alternatively use an inline cross-platform env approach — no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- `cd site && npm run build` (default) exits 0; `node scripts/validate/all.mjs` exits 0
- ROLLOUT_ALL build produces 346×2 commune pages and passes the full validator suite
- sitemap lists only rollout communes + all regions/crime; manifest + theme-color present on all pages
- Human UAT approved (palette, prose, hreflang source check)
</verification>

<success_criteria>
- SEO-02: sitemap-index + canonical on every page, rollout-gated
- SEO-03: Dataset/Place (territorial) + Dataset (crime) JSON-LD crediting CEAD
- SEO-04: manifest + theme-color, no SEO-critical client rendering
- SEO-01: reciprocal hreflang verified across the full site
- Full ROLLOUT_ALL build green; all validators pass; human UAT approved
</success_criteria>

<output>
Create `.planning/phases/02-astro-site-programmatic-pages/02-06-SUMMARY.md` when done
</output>
