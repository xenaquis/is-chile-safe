---
phase: 02-astro-site-programmatic-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/package.json
  - site/astro.config.mjs
  - site/tsconfig.json
  - site/.nvmrc
  - site/src/env.d.ts
  - site/src/lib/data.ts
  - site/src/lib/slugMaps.ts
  - site/src/lib/colorScale.ts
  - site/src/config/rollout.json
  - site/src/config/i18n.ts
  - site/scripts/validate/structure.mjs
  - site/scripts/validate/commune.mjs
  - site/scripts/validate/rollout.mjs
  - site/scripts/validate/hreflang.mjs
  - site/scripts/validate/schema.mjs
  - site/scripts/validate/all.mjs
autonomous: true
requirements: [PAGES-04]
must_haves:
  truths:
    - "An Astro 6.4.x project at site/ installs and `astro check` exits 0"
    - "data.ts reads data/cead/ JSON at build time via fileURLToPath parent traversal (not process.cwd, not import.meta.glob, NO symlink) and resolves region name + latestCompleteYear"
    - "data.ts exports loadNationalAverage() that returns the MEAN of non-low-population communes' latest-complete-year total rate (hundreds-to-low-thousands), NOT the national.json sum (millions)"
    - "rollout.json lists the 12 priority commune CUT codes; loadRolloutCuts honours ROLLOUT_ALL=true"
    - "i18n.ts exposes EN+ES UI strings and the 7 translated crime-family URL slugs per locale"
    - "colorScale.ts emits the 5-stop incidence color array at build time only"
    - "validate/*.mjs scripts exist (stubs that exit 0) so later plans can fill assertions"
  artifacts:
    - path: "site/astro.config.mjs"
      provides: "i18n (prefixDefaultLocale:false), react+sitemap integrations, @data vite alias"
      contains: "prefixDefaultLocale"
    - path: "site/src/lib/data.ts"
      provides: "loadIndex/loadCommune/loadRegion/loadNational/loadCatalog + latestCompleteYear + regionName + nearestComparable + loadNationalAverage + loadRegionalAverage"
      exports: ["loadIndex", "loadCommune", "loadRegion", "loadNational", "loadCatalog", "loadRolloutCuts", "nearestComparable", "loadNationalAverage", "loadRegionalAverage"]
    - path: "site/src/config/rollout.json"
      provides: "12 priority commune CUT allowlist"
      contains: "13101"
    - path: "site/src/config/i18n.ts"
      provides: "EN/ES strings + FAMILY_SLUGS translation table"
      exports: ["EN_STRINGS", "ES_STRINGS", "FAMILY_SLUGS"]
    - path: "site/src/lib/slugMaps.ts"
      provides: "central slug-pair maps for commune/region/family hreflang"
      exports: ["buildCommuneSlugPairMap", "buildRegionSlugPairMap", "buildFamilySlugPairMap", "getFamilySlug"]
  key_links:
    - from: "site/src/lib/data.ts"
      to: "data/cead/meta/index.json"
      via: "fs.readFileSync from fileURLToPath-derived DATA_ROOT (parent traversal, no symlink)"
      pattern: "fileURLToPath"
    - from: "site/src/lib/data.ts loadNationalAverage()"
      to: "non-low-pop commune latest-complete-year rates"
      via: "mean over loadIndex filtered !low_population"
      pattern: "loadNationalAverage"
    - from: "site/astro.config.mjs"
      to: "site/src/config/rollout.json"
      via: "sitemap filter() + ROLLOUT_ALL gate"
      pattern: "ROLLOUT_ALL"
---

<objective>
Scaffold the greenfield Astro 6.4.x static site at `site/` and build the shared build-time foundation every downstream plan consumes: the `node:fs` data loader, the i18n string + crime-family-slug config, the central slug-pair maps for hreflang, the build-time chroma-js color scale, the rollout batch-gate config (PAGES-04), and stub validation scripts.

Purpose: Everything in later waves (layout, components, pages, SEO finishing) imports from this plan. Getting the data contract, CWD-safe path resolution, the correct national/regional AVERAGE helpers (RESEARCH Pitfall 1 — national.json is a SUM, not an average), and the i18n slug table correct here prevents arithmetically-wrong output and rework across 700+ pages.
Output: A buildable Astro project with a typechecking-clean lib/ and config/ layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md
@.planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md
@.planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md
@.planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md

<interfaces>
<!-- Phase 1 data shapes — VERIFIED against the committed files. Executor uses these directly. -->

data/cead/meta/index.json — JSON ARRAY of 346 objects:
  { "cut": "5602", "name": "Algarrobo", "slug": "algarrobo", "region_id": "56", "population": 16095, "low_population": false }

data/cead/meta/catalog.json:
  family_keys order: ["vida","robos_violentos","vif","drogas","armas","propiedad","incivilidades"];
  families[key].featured === true ONLY for "vida" and "propiedad";
  featured_subgroups: { "homicidios": 101, "secuestros": null }

data/cead/comunas/{cut}.json:
  { id, name, slug, region_id, population, low_population, national_rank, regional_rank, trend ("up"|"down"|"stable"),
    featured_rates: { propiedad:{year:rate}, homicidios:{}, secuestros:{} },
    series: [ { year, rate_per_100k, by_family:{vida,robos_violentos,vif,drogas,armas,propiedad,incivilidades}, partial }, ... ] }
  NOTE: series[].rate_per_100k EQUALS the sum of its by_family values. 2026 entry has partial:true. NO region_name field.

data/cead/regions/{region_id}.json  (filename is the bare region_id, e.g. 13.json, 5.json, 11.json):
  { id, name ("Metropolitana", no "Región de" prefix), slug, series:[{year,rate_per_100k,by_family,partial?}],
    comuna_ranking:[ {cut, name, rank}, ... ] }
  NOTE: region series[].rate_per_100k is ALSO a SUM of that region's commune rates (same pipeline behaviour as national.json) — NOT a per-capita regional average. See Pitfall 1.

data/cead/national.json:
  { name, series:[{year,rate_per_100k,by_family,partial?}] }  — NO per-commune ranking.
  NOTE: series[].rate_per_100k is a SUM of all 346 commune rates (~millions), NOT a national per-capita average. See RESEARCH Pitfall 1.
</interfaces>

<rollout_cut_codes>
<!-- VERIFIED against data/cead/meta/index.json at plan time. These 12 CUT codes ARE the D-25 priority batch. -->
Santiago 13101 · Providencia 13123 · Las Condes 13114 · Maipú 13119 · Valparaíso 5101 · Viña del Mar 5109 · Concepción 8101 · La Serena 4101 · Antofagasta 2101 · Temuco 9101 · Puerto Montt 10101 · Punta Arenas 12101
</rollout_cut_codes>

<family_slugs>
<!-- D-17 translated URL slugs. family key (data) -> EN slug -> ES slug. ORDER per family_keys. -->
vida -> homicide -> homicidios
robos_violentos -> violent-robbery -> robos-violentos
vif -> domestic-violence -> violencia-intrafamiliar
drogas -> drug-crimes -> drogas
armas -> weapons -> armas
propiedad -> property -> propiedad
incivilidades -> disorder -> incivilidades
<!-- NOTE: family "vida" maps to the homicide URL slug (vida = crimes against life, featured). -->
</family_slugs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro project + pinned deps + config</name>
  <files>site/package.json, site/astro.config.mjs, site/tsconfig.json, site/.nvmrc, site/src/env.d.ts</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Standard Stack table lines 104-137; astro.config.mjs scaffold lines 554-595; Pitfall 1 lines 497-512)
    - .planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md (astro.config.mjs section lines 604-643)
    - CLAUDE.md (Recommended Stack table + "What NOT to Use": Astro 6 NOT v5, @astrojs/react 5.x, prefixDefaultLocale:false)
  </read_first>
  <action>
    Create the Astro project under `site/` with the locked pinned versions from RESEARCH.md Standard Stack: astro@6.4.6, @astrojs/react@5.0.7, react@19.2.7, react-dom@19.2.7, @astrojs/sitemap@3.7.3, chroma-js@3.2.0, topojson-client@3.1.0; devDeps @astrojs/check (current) and typescript. Add npm scripts dev/build/check plus `"validate": "node scripts/validate/all.mjs"`. Pin Node in `site/.nvmrc` to 20.
    Write `site/astro.config.mjs` per RESEARCH.md lines 554-595: site 'https://ischilesafe.com'; i18n locales ['en','es'] defaultLocale 'en' routing prefixDefaultLocale false (D-15); integrations react() and sitemap(). CRITICAL per Pitfall 1 (path resolution, A1/Q3): compute the data root from a fileURLToPath(import.meta.url)-derived __dirname, never process.cwd(); expose a @data vite alias to path.resolve(__dirname, '..', 'data'). Do NOT rely on a symlink anywhere — parent-directory traversal from the config/lib file location reaches repo-root/data and works cross-platform at build time (Q3 RESOLVED — no symlink). The rollout sitemap filter and Schema.org wiring are finished in plan 02-06 — for now configure sitemap with i18n defaultLocale 'en' locales {en:'en',es:'es'} and a permissive filter returning true. Do NOT add Tailwind. Do NOT use client:load anywhere.
    Write `site/tsconfig.json` extending astro/tsconfigs/strict; `site/src/env.d.ts` referencing astro/client with a comment noting the ROLLOUT_ALL env flag.
  </action>
  <verify>
    <automated>cd site && npm install && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - site/package.json lists astro@6.4.6, @astrojs/react@5.0.7, @astrojs/sitemap@3.7.3, chroma-js@3.2.0 at exact pinned versions
    - grep -v '^#' site/astro.config.mjs contains the strings prefixDefaultLocale, fileURLToPath, and @data
    - site/astro.config.mjs does NOT contain process.cwd for the data root and does NOT contain tailwind
    - `cd site && npx astro check` exits 0
  </acceptance_criteria>
  <done>Astro project installs, typechecks clean, and config encodes i18n + data alias + CWD-safe path.</done>
</task>

<task type="auto">
  <name>Task 2: Build-time data loader (incl. national/regional averages), color scale, slug maps</name>
  <files>site/src/lib/data.ts, site/src/lib/colorScale.ts, site/src/lib/slugMaps.ts</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pattern 1 companion lines 307-329; Pitfall 1 national.json is a SUM lines 621-626; Pitfall 5 latest-complete-year lines 535-541; Pattern 2 slugMaps lines 331-364; chroma-js example lines 597-611; Pitfall 6 comparable fallback lines 543-547)
    - .planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md (data.ts section lines 472-521; slugMaps.ts lines 525-555; colorScale.ts lines 405-428)
    - data/cead/meta/index.json, data/cead/comunas/13101.json, data/cead/regions/13.json, data/cead/national.json
    - The <interfaces>, <rollout_cut_codes>, and <family_slugs> blocks in this plan
  </read_first>
  <action>
    Create `site/src/lib/data.ts`. Define DATA_ROOT explicitly as `path.resolve(fileURLToPath(import.meta.url), '../../..', 'data', 'cead')` — i.e. parent-traversal from site/src/lib to repo-root/data/cead (the file is at site/src/lib/data.ts, so '../../..' from the file URL reaches site/, then 'data','cead' would be wrong; confirm the exact segment count so that loadIndex() resolves data/cead/meta/index.json — from site/src/lib/ you must climb lib/ -> src/ -> site/ -> repo-root, then descend into data/cead). Do NOT use process.cwd and do NOT depend on a symlink (Q3 RESOLVED — parent traversal only). Export loadIndex() (array of 346), loadCatalog(), loadCommune(cut), loadRegion(regionId), loadNational(). loadCommune must attach latestCompleteYear = max series[].year where partial===false (Pitfall 5 — never use the 2026 partial year for rate/family figures) and attach regionName resolved by reading the region JSON name for the commune's region_id.
    Add a build-time helper latestCompleteYearRate(commune) returning that commune's series rate_per_100k at latestCompleteYear.
    Add `loadNationalAverage()`: compute mean(loadIndex().filter(c => !c.low_population).map(c => latestCompleteYearRate(loadCommune(c.cut)))). This is the TRUE national average (RESEARCH Pitfall 1 — do NOT use loadNational().series[].rate_per_100k, which is a SUM in the millions). Returns a single number in the hundreds-to-low-thousands range.
    Add `loadRegionalAverage(regionId)`: same mean approach over that region's non-low-pop communes (loadIndex filtered by region_id === regionId && !low_population) — region.json's rate field is ALSO a SUM (Pitfall 1), so never use loadRegion(regionId).series rate for the average. Returns a single number.
    Add loadRolloutCuts() returning ROLLOUT_ALL==='true' ? loadIndex().map(c=>c.cut) : rollout.enabled (import ../config/rollout.json). Add nearestComparable(cut) per D-08: filter same-region communes by !low_population, exclude self, pick nearest by latest-complete-year total rate_per_100k; if fewer than 2 qualify, fall back to nearest nationally among !low_population (Pitfall 6). Return {cut,name,slug,rate,national_rank,trend,regionName,fallback:boolean}.
    Create `site/src/lib/colorScale.ts`: export INCIDENCE_COLORS = chroma.scale(['#dbeef0','#9fd0cd','#f4d58d','#e8a05c','#c96b5a']).classes(5).colors(5); add levelForRate(rate, sortedRates) returning 1-5 by quintile. chroma-js is build-time only — never re-export to a client component.
    Create `site/src/lib/slugMaps.ts` per Pattern 2: buildCommuneSlugPairMap() (cut -> {en:`/commune/{slug}/`, es:`/es/comuna/{slug}/`}), buildRegionSlugPairMap() (region_id -> {en:`/region/{slug}/`, es:`/es/region/{slug}/`}), buildFamilySlugPairMap() (family key -> {en:`/crime/{enSlug}/`, es:`/es/delito/{esSlug}/`} using FAMILY_SLUGS), getFamilySlug(key, locale).
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
    <automated>node scripts/validate/structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - site/src/lib/data.ts contains fileURLToPath and does NOT contain process.cwd or import.meta.glob, and does NOT reference a symlink path
    - DATA_ROOT resolves data/cead/meta/index.json successfully: a quick assertion (e.g. `node -e "import('./site/src/lib/data.js').then(m=>{if(m.loadIndex().length!==346)process.exit(1)})"` or the structure.mjs stub) confirms loadIndex() parses 346 entries — NO symlink present
    - data.ts exports loadIndex, loadCommune, loadRegion, loadNational, loadCatalog, loadRolloutCuts, nearestComparable, loadNationalAverage, loadRegionalAverage
    - loadNationalAverage() returns a plausible per-capita average (asserted to be > 50 and < 50000 — i.e. hundreds-to-low-thousands, NOT the national.json sum in the millions)
    - colorScale.ts exports INCIDENCE_COLORS (5 entries) and levelForRate
    - slugMaps.ts exports buildCommuneSlugPairMap, buildRegionSlugPairMap, buildFamilySlugPairMap, getFamilySlug
    - `cd site && npx astro check` exits 0
  </acceptance_criteria>
  <done>data.ts resolves all Phase-1 JSON at build time via parent traversal (no symlink), derives latestCompleteYear + regionName + comparable + national/regional AVERAGES (means, not sums); color scale and slug maps export cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: i18n strings, family slugs, rollout config, validation stubs</name>
  <files>site/src/config/i18n.ts, site/src/config/rollout.json, site/scripts/validate/structure.mjs, site/scripts/validate/commune.mjs, site/scripts/validate/rollout.mjs, site/scripts/validate/hreflang.mjs, site/scripts/validate/schema.mjs, site/scripts/validate/all.mjs</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md (i18n.ts section lines 341-372; rollout.json lines 647-657; validate-build section lines 690-695)
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (Copywriting Contract / Element-Level Copy lines 207-234; incidence labels lines 95-101; trend semantic lines 108-111)
    - .planning/phases/02-astro-site-programmatic-pages/02-VALIDATION.md (Per-Task Verification Map lines 41-52; Wave 0 Requirements lines 56-63)
    - ischilesafe.com/data.js (I18N object — UI string source to port)
    - The <rollout_cut_codes> and <family_slugs> blocks in this plan
  </read_first>
  <action>
    Create `site/src/config/i18n.ts` exporting EN_STRINGS and ES_STRINGS objects covering every UI-SPEC Element-Level Copy row (page h1 patterns for commune/region/crime-type, rate stat label, national/regional rank labels, trend up/down/stable, sparkline heading, partial-year badge, vs-national/vs-regional above/below callouts, family breakdown heading, comparable-commune same-region + national-fallback headings, map placeholder text, methodology caveat link text, low-population caveat full text, level_1..level_5 incidence labels). Port wording from ischilesafe.com/data.js I18N but apply the sober tone from UI-SPEC (e.g. ES rate label "Tasa por 100.000 hab. ({año})"). Also export FAMILY_SLUGS: a record keyed by the 7 family keys, each {en, es} per the <family_slugs> block. Export NAV_STRINGS (Home/Map/Methodology) per locale. No forbidden terms (D-05): never "zona peligrosa", "ranking definitivo", "zona segura garantizada".
    Create `site/src/config/rollout.json` with shape { "enabled": ["13101","13123","13114","13119","5101","5109","8101","4101","2101","9101","10101","12101"] } — exactly the 12 CUT codes from <rollout_cut_codes>.
    Create the six `site/scripts/validate/*.mjs` scripts as runnable stubs. Each is an ESM script using node:assert and node:fs that, for now, exits 0 with a console note like "structure: stub (filled in plan 02-NN)". all.mjs imports/runs the other five in sequence. These stubs are filled with real assertions in plans 02-03..02-06; creating them now satisfies the Nyquist Wave 0 requirement that every later task has an automated command to call.
  </action>
  <verify>
    <automated>node site/scripts/validate/all.mjs</automated>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - site/src/config/rollout.json enabled array has exactly 12 entries and contains "13101" and "13119"
    - site/src/config/i18n.ts exports EN_STRINGS, ES_STRINGS, FAMILY_SLUGS; FAMILY_SLUGS has 7 keys with both en and es slugs
    - i18n.ts does NOT contain the substrings "zona peligrosa", "ranking definitivo", or "zona segura garantizada"
    - All six site/scripts/validate/*.mjs files exist; `node site/scripts/validate/all.mjs` exits 0
    - `cd site && npx astro check` exits 0
  </acceptance_criteria>
  <done>i18n + family slugs + rollout allowlist locked; runnable validation harness exists for downstream plans to extend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-1 JSON → build process | Trusted pipeline output read from data/cead/ at build time; no runtime/user input in this phase |
| npm registry → build | Third-party packages pulled at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | data.ts file-path resolution | mitigate | DATA_ROOT is a fixed fileURLToPath-derived constant (parent traversal, no symlink); no user input constructs paths (path traversal impossible in a static build) |
| T-02-02 | Tampering | npm install (astro, react, sitemap, chroma-js, topojson-client) | accept | All packages first-party/long-standing per RESEARCH Package Legitimacy Audit (all Approved); pinned exact versions in package.json; low severity |
| T-02-03 | Information disclosure | committed config | accept | No secrets in this phase — rollout.json + i18n.ts contain only public slugs/labels; static build has no env secrets |
| T-02-SC | Tampering | npm package supply chain | mitigate | Versions pinned exactly; RESEARCH audit table present and all packages Approved (no ASSUMED/SUS/SLOP) — no blocking checkpoint required |
</threat_model>

<verification>
- `cd site && npm install` succeeds; `cd site && npx astro check` exits 0
- `node site/scripts/validate/all.mjs` exits 0
- rollout.json has 12 CUT codes; data.ts uses fileURLToPath parent traversal (not process.cwd / import.meta.glob / symlink)
- loadNationalAverage() returns a per-capita mean in the hundreds-to-low-thousands range (NOT the national.json sum)
</verification>

<success_criteria>
- Astro 6.4.x project scaffolded at site/ with pinned deps and i18n/data-alias config
- data.ts loads all Phase-1 JSON via parent traversal (no symlink), derives latestCompleteYear + regionName + nearestComparable + loadNationalAverage + loadRegionalAverage
- i18n.ts (EN/ES + FAMILY_SLUGS), colorScale.ts, slugMaps.ts, rollout.json all present and typecheck clean
- Six validation stub scripts runnable; all.mjs exits 0
</success_criteria>

<output>
Create `.planning/phases/02-astro-site-programmatic-pages/02-01-SUMMARY.md` when done
</output>
</content>
</invoke>
