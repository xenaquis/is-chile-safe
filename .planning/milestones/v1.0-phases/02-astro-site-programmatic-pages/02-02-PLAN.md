---
phase: 02-astro-site-programmatic-pages
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - site/src/styles/global.css
  - site/src/layouts/BaseLayout.astro
  - site/src/components/PageHeader.astro
  - site/src/components/PageFooter.astro
  - site/src/components/LevelChip.astro
  - site/src/components/TrendChip.astro
  - site/src/components/StatCard.astro
  - site/src/components/Sparkline.astro
  - site/src/components/FamilyBreakdownBars.astro
  - site/src/components/ComparisonCallout.astro
  - site/src/components/ComparableCommune.astro
  - site/src/components/MapPlaceholderSlot.astro
  - site/src/components/MethodologyCaveat.astro
  - site/src/components/CommuneRankingTable.astro
autonomous: true
requirements: [SEO-01]
must_haves:
  truths:
    - "BaseLayout injects reciprocal hreflang (en/es/x-default) + self-canonical + theme-color + manifest link on every page from enPath/esPath props"
    - "global.css declares the locked CSS custom properties (--primary, --bg, --s1..--s5, spacing, typography)"
    - "All 11 content components render server-side with zero client:* directives (zero JS on content pages)"
    - "Sparkline emits inline SVG with partial-year bars at 50% opacity; FamilyBreakdownBars uses CSS percentage widths with vida+propiedad featured first"
  artifacts:
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "head meta + hreflang + canonical + JSON-LD slot + PageHeader/PageFooter shell"
      contains: "hreflang"
    - path: "site/src/styles/global.css"
      provides: "design tokens + reset + responsive base"
      contains: "--primary"
    - path: "site/src/components/Sparkline.astro"
      provides: "server-rendered inline SVG bar sparkline"
      contains: "svg"
    - path: "site/src/components/MapPlaceholderSlot.astro"
      provides: "Phase 3 hydration mount point"
      contains: "data-phase3-mount"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro"
      to: "central slug-pair map (enPath/esPath props)"
      via: "link rel=alternate hreflang"
      pattern: "hreflang"
    - from: "site/src/components/FamilyBreakdownBars.astro"
      to: "CSS width percentage"
      via: "Math.round(val/max*100)+'%'"
      pattern: "%"
---

<objective>
Build the visual shell and the 11 server-rendered content components ported from the prototype, plus the BaseLayout that injects the reciprocal hreflang + canonical + PWA meta on every page (SEO-01 mechanism). This wave produces no pages yet — it produces the component library that all three page-type plans (commune/region/crime-type) compose.

Purpose: One authoritative BaseLayout and one component set means hreflang correctness and the teal visual language are guaranteed identically across all 700+ pages. Zero React/JS ships on content pages (D-09).
Output: A typecheck-clean component + layout library ready to be composed into pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md
@.planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md
@.planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md
@site/src/lib/data.ts
@site/src/lib/colorScale.ts
@site/src/config/i18n.ts

<interfaces>
<!-- From plan 02-01 (already built). Components consume these. -->
site/src/lib/colorScale.ts: export INCIDENCE_COLORS: string[5]; levelForRate(rate, sortedRates): 1..5
site/src/config/i18n.ts: EN_STRINGS, ES_STRINGS, NAV_STRINGS, FAMILY_SLUGS
site/src/lib/data.ts: loadCommune(cut) returns commune + latestCompleteYear + regionName; nearestComparable(cut)

Commune series entry shape (props into Sparkline + FamilyBreakdownBars):
  { year:number, rate_per_100k:number, by_family:{vida,robos_violentos,vif,drogas,armas,propiedad,incivilidades}, partial:boolean }
Trend prop values: "up" | "down" | "stable" (string, NOT numeric -1/0/1 — prototype used numeric)
Family order for bars (UI-SPEC §CrimeFamilyBreakdown): vida, propiedad (both featured, accent dot), then robos_violentos, incivilidades, vif, drogas, armas
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: global.css tokens + BaseLayout with hreflang/canonical/PWA meta</name>
  <files>site/src/styles/global.css, site/src/layouts/BaseLayout.astro, site/src/components/PageHeader.astro, site/src/components/PageFooter.astro</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (Design System lines 17-27; Spacing lines 31-50; Typography lines 54-66; Color lines 68-114; Responsive Breakpoints lines 345-353; Meta Tags lines 255-264)
    - .planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md (global.css lines 661-687; BaseLayout hreflang/JSON-LD lines 559-600; PageHeader port lines 49-111; PageFooter lines 18)
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pattern 2 hreflang lines 331-380; XSS/JSON-LD note lines 794-798)
    - ischilesafe.com/components.jsx (Header lines 21-39, PinGlyph lines 4-10), ischilesafe.com/home-view.jsx (footer lines 134-138)
  </read_first>
  <action>
    Create `site/src/styles/global.css` with the :root custom properties from PATTERNS.md lines 666-685 (--primary #0f766e, --bg #f5f8f8, --ink #1c2a2b, --muted #5d6c6d, --line #e2e9e8, --card #ffffff, --s1..--s5 incidence scale, --radius 14px, spacing xs..3xl, --font Nunito Sans stack), a minimal CSS reset, the Nunito Sans @import or font <link> (wght@400;700), and the mobile-first base typography (body 16/400/1.5; the 4-size 2-weight scale from UI-SPEC Typography). Add the three responsive breakpoints (640px, 1024px; max content width 860px, page padding 0 24px desktop).
    Create `site/src/layouts/BaseLayout.astro`. Props: lang ('en'|'es'), title, description, enPath, esPath, jsonLd (object|null), mountType?. In <head> inject (RESEARCH Pattern 2 lines 367-380): meta charset/viewport, <title>, <meta name=description>, the three hreflang alternates (en->enPath, es->esPath, x-default->enPath) and the self-canonical (lang==='en'?enPath:esPath), all prefixed with https://ischilesafe.com; <meta name=theme-color content="#0f766e">; <link rel=manifest href=/manifest.json>; <meta name=robots content="index, follow">; import global.css. When jsonLd is non-null, emit <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} /> — use JSON.stringify (RESEARCH XSS note) so values are safely serialized. Render <PageHeader> at top of <body> and <PageFooter> at bottom, with a <slot/> between.
    Create `site/src/components/PageHeader.astro` per PATTERNS.md port (lines 49-111): static links via getRelativeLocaleUrl(), wordmark + PinGlyph inline SVG (port components.jsx lines 4-10), nav (Home/Map/Methodology from NAV_STRINGS) and a lang-toggle that links to the alternate locale path. No onClick/state. Map link points to /map placeholder href for now.
    Create `site/src/components/PageFooter.astro`: small wordmark + "Fuente: CEAD / Source: CEAD" attribution (locale-aware) + a locale-switch link.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - global.css contains --primary, --bg, --s1, --s5, --radius and the Nunito Sans font reference
    - BaseLayout.astro contains the strings hreflang (>=3 occurrences for en/es/x-default), canonical, theme-color, and application/ld+json
    - BaseLayout.astro uses JSON.stringify for the JSON-LD block (not raw interpolation)
    - PageHeader.astro uses getRelativeLocaleUrl and contains NO client: directive
    - `cd site && npx astro check` exits 0
  </acceptance_criteria>
  <done>Tokens + layout shell + header/footer exist; hreflang/canonical/PWA meta inject from props; zero client JS.</done>
</task>

<task type="auto">
  <name>Task 2: Data-viz components (Sparkline, FamilyBreakdownBars, chips, StatCard)</name>
  <files>site/src/components/Sparkline.astro, site/src/components/FamilyBreakdownBars.astro, site/src/components/LevelChip.astro, site/src/components/TrendChip.astro, site/src/components/StatCard.astro</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md (Sparkline lines 153-206; FamilyBreakdownBars lines 210-234; LevelChip lines 115-131; TrendChip lines 135-149; StatCard lines 237-259)
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (SparklineSection lines 128; CrimeFamilyBreakdown lines 130; trend semantic colors lines 108-111; partial-year badge lines 112,221)
    - .planning/phases/02-astro-site-programmatic-pages/02-RESEARCH.md (Pattern 4 Sparkline lines 418-454; Pitfall 5 latest-complete-year lines 535-541; CSS-width anti-pattern line 478)
    - ischilesafe.com/components.jsx (Sparkline lines 135-150, LevelChip 119-126, TrendLabel 128-133), ischilesafe.com/map-view.jsx (type-bars 52-60)
  </read_first>
  <action>
    Create `site/src/components/Sparkline.astro` per PATTERNS.md/RESEARCH Pattern 4: props series (array of {year,rate_per_100k,partial}), activeYear?, width?, height? (default 44 per UI-SPEC). Compute maxRate over series, render one <rect> per year; bar height proportional to rate_per_100k; partial-year bars at opacity 0.5; active-year bar fill var(--primary), others var(--muted). aria-hidden. Pure inline SVG — no JS, no charting lib.
    Create `site/src/components/FamilyBreakdownBars.astro`: props byFamily (the by_family object from the latest COMPLETE year — never the 2026 partial, Pitfall 5), locale, level (1-5 for fill color). Render the 7 families in the fixed UI-SPEC order: vida, propiedad (both featured — render an accent dot via var(--primary) on the label), then robos_violentos, incivilidades, vif, drogas, armas. Each row: label (from i18n family labels), a track, and a fill whose width is the CSS percentage string `Math.round((val/max)*100) + '%'` (RESEARCH anti-pattern line 478 — must be a percentage string with unit, never a bare number), plus the numeric value. Fill color uses var(--s{level}).
    Create `site/src/components/LevelChip.astro` (props level 1-5, locale): color dot via var(--s{level}) + the incidence label from i18n level_{n}.
    Create `site/src/components/TrendChip.astro` (props trend 'up'|'down'|'stable', locale): derive arrow (↑/↓/→) and class; colors per UI-SPEC semantic (up #c96b5a, down #0f766e, stable #5d6c6d). NOTE the Phase-1 trend is a STRING, not numeric — derive from the string (PATTERNS line 149).
    Create `site/src/components/StatCard.astro` (props label, value, sublabel?): 28px/700 value + 14px/700 label per UI-SPEC Typography.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - Sparkline.astro emits an <svg> with <rect> elements and applies opacity 0.5 for partial entries; contains NO client: directive
    - FamilyBreakdownBars.astro computes width as a percentage string (contains the literal + '%') and orders vida and propiedad first
    - TrendChip.astro branches on the string values 'up'/'down'/'stable' (not numeric comparison)
    - LevelChip.astro references var(--s in the dot styling
    - `cd site && npx astro check` exits 0
  </acceptance_criteria>
  <done>All data-viz components render server-side from Phase-1 data shapes with correct ordering, units, and partial-year handling.</done>
</task>

<task type="auto">
  <name>Task 3: Composite components (callouts, comparable, map slot, methodology, ranking table)</name>
  <files>site/src/components/ComparisonCallout.astro, site/src/components/ComparableCommune.astro, site/src/components/MapPlaceholderSlot.astro, site/src/components/MethodologyCaveat.astro, site/src/components/CommuneRankingTable.astro</files>
  <read_first>
    - .planning/phases/02-astro-site-programmatic-pages/02-UI-SPEC.md (ComparisonCallouts lines 129; ComparableCommune lines 131; MapPlaceholderSlot spec lines 315-341; MethodologyCaveat lines 134; CommuneRankingTable lines 145; copy rows lines 222-231)
    - .planning/phases/02-astro-site-programmatic-pages/02-PATTERNS.md (ComparableCommune lines 262-291; MapPlaceholderSlot lines 293-320; MethodologyCaveat lines 323-337; CommuneRankingTable lines 28; PinGlyph SVG lines 102-111)
    - .planning/phases/02-astro-site-programmatic-pages/02-CONTEXT.md (D-08 comparable algorithm, D-07 map slot)
    - The <interfaces> block in this plan
  </read_first>
  <action>
    Create `site/src/components/ComparisonCallout.astro`: props pct (signed number), direction ('above'|'below'), scope ('national'|'regional'), locale. Render the pill copy from i18n (UI-SPEC rows: "{N}% above/below national/regional average"); above -> level-4/5 warm tint background, below -> level-1/2 teal tint. Display |pct| rounded.
    Create `site/src/components/ComparableCommune.astro`: props comparable (the nearestComparable() return: {name, slug, rate, national_rank, trend, regionName, fallback}), locale. Card layout: heading from i18n — same-region heading when fallback===false, national-fallback heading when fallback===true (UI-SPEC rows 227-228); show rate, national rank, a TrendChip, and an <a href> to the comparable's commune page (locale-aware path /commune/{slug}/ or /es/comuna/{slug}/). No button/onClick.
    Create `site/src/components/MapPlaceholderSlot.astro` per UI-SPEC lines 315-341: outer div class map-placeholder-slot with data-phase3-mount set from a mountType prop ('commune-map'|'region-map'|'crime-type-map'), role=img, aria-label; inner centered PinGlyph SVG at 40px (port components.jsx lines 4-10) + locale-aware placeholder text from i18n. Scoped CSS: min-height 320px mobile / 480px desktop (region/crime variants may use 280/400 — accept the prop-driven min-heights from UI-SPEC), border 2px dashed var(--line), border-radius var(--radius), background var(--bg), flex center. The outer div dimensions + data attribute are the Phase-3 contract — do not omit them.
    Create `site/src/components/MethodologyCaveat.astro`: grey box (bg var(--bg), border var(--line), radius var(--radius)); 2-3 sober sentences on subregistro + CEAD attribution + rate-per-100k definition (14px/400); link to /methodology/ (EN) or /es/metodologia/ (ES) using i18n "About this data" copy. NOTE: those methodology pages are built in Phase 4 — the link target is fine (will 404 until then; acceptable per phase boundary).
    Create `site/src/components/CommuneRankingTable.astro`: props rows (array of {name, slug, rate, national_rank, trend, low_population}), locale, columns config. Server-rendered <table> with header row (Commune, Rate per 100k, National Rank, Trend per UI-SPEC region layout); low_population rows get a `*` marker and are excluded from rank numbering; each commune name links to its page. Mobile: wrapper allows horizontal scroll. No JS sorting.
  </action>
  <verify>
    <automated>cd site && npx astro check</automated>
    <automated>node scripts/validate/structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - MapPlaceholderSlot.astro contains data-phase3-mount and role="img" and the 320px/480px min-heights
    - ComparableCommune.astro selects heading by the fallback flag and renders an <a href> (no onClick/button)
    - CommuneRankingTable.astro renders a server-side <table> and marks low_population rows with *
    - MethodologyCaveat.astro links to /methodology/ and /es/metodologia/
    - `cd site && npx astro check` exits 0
  </acceptance_criteria>
  <done>All composite components render server-side, honour D-08 comparable + D-07 map slot, and link locale-aware with zero JS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-1 JSON values → HTML/SVG/JSON-LD output | CEAD-sourced strings (commune/region names) rendered into templates at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | JSON-LD injection in BaseLayout | mitigate | Emit JSON-LD via set:html={JSON.stringify(jsonLd)} so all values are JSON-escaped; never string-concatenate data into the script block (RESEARCH XSS note lines 794-798) |
| T-02-05 | Tampering | XSS via unescaped commune names in .astro templates | mitigate | Astro auto-escapes {expression} interpolations; no set:html used with raw data anywhere except the JSON.stringify'd JSON-LD; commune names are Spanish ASCII (low risk) |
| T-02-06 | Information disclosure | committed component templates | accept | Components render only public CEAD aggregate statistics + UI labels; no secrets, no PII |
</threat_model>

<verification>
- `cd site && npx astro check` exits 0
- BaseLayout injects 3 hreflang alternates + self-canonical + theme-color + manifest
- No component file contains a `client:` directive (zero JS on content pages)
- FamilyBreakdownBars uses CSS percentage-string widths; Sparkline dims partial-year bars
</verification>

<success_criteria>
- global.css declares the locked token set; BaseLayout injects SEO-01 hreflang + canonical + PWA meta from props
- 11 content components ported, all server-rendered, zero client:* directives
- Component library typechecks clean and is ready for page composition
</success_criteria>

<output>
Create `.planning/phases/02-astro-site-programmatic-pages/02-02-SUMMARY.md` when done
</output>
