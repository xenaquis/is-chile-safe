---
phase: 01-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/ine/poblacion_comunal.json
  - pipeline/shared/population.py
  - pipeline/tests/test_population.py
  - pipeline/scripts/fetch_ine_population.py
autonomous: false
requirements: [DATA-04]
user_setup: []
must_haves:
  truths:
    - "A committed static file maps all 346 CUT codes to 2024 INE population"
    - "population lookup returns the population for a CUT code"
    - "is_low_population returns True for a commune under 10,000 inhabitants and False above"
    - "5 sampled commune populations match official INE figures (human-verified)"
  artifacts:
    - path: "data/ine/poblacion_comunal.json"
      provides: "Static INE 2024 communal population per CUT code (346 entries)"
      contains: "cut"
    - path: "pipeline/shared/population.py"
      provides: "load_population + is_low_population with 10000 threshold"
      contains: "LOW_POPULATION_THRESHOLD"
    - path: "pipeline/scripts/fetch_ine_population.py"
      provides: "One-time CSV to JSON converter from the INE projections dataset"
      contains: "population_2024"
  key_links:
    - from: "pipeline/shared/population.py"
      to: "data/ine/poblacion_comunal.json"
      via: "json.loads file read"
      pattern: "poblacion_comunal.json"
    - from: "pipeline/tests/test_population.py"
      to: "pipeline/shared/population.py"
      via: "import is_low_population"
      pattern: "is_low_population"
---

<objective>
Source, convert, and commit the official INE communal population dataset as a static JSON asset, and build the population lookup module the pipeline uses for the ranking filter (D-04, D-11, D-12).

Purpose: DATA-04 requires national/regional rankings to exclude communes under 10,000 inhabitants. CEAD serves rates directly (medida=2, D-10) so we never compute rates from population — population is used ONLY for the low_population flag and page context (D-11). This is a one-time data sourcing task with no ongoing scraping pipeline.

Output: data/ine/poblacion_comunal.json (346 CUT→population entries), a population lookup module with the 10,000 threshold, a one-time fetch/convert script, and a human checkpoint verifying 5 sampled values against official INE (Assumption A6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES. Plan 04 (normalizer) consumes load_population + is_low_population. -->

pipeline/shared/population.py (canonical source: 01-PATTERNS.md lines 397-424):
- load_population() -> dict[str, int]   # {cut_code: population_2024}, lru_cached
- is_low_population(cut_code: str) -> bool   # True if population < 10000
- LOW_POPULATION_THRESHOLD = 10_000   # D-12

data/ine/poblacion_comunal.json shape — a JSON array of objects:
  {"cut": "13101", "name": "Santiago", "population_2024": <int>}
(load_population reads entry['cut'] and entry['population_2024'])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch INE projections CSV and convert to committed JSON asset (D-11)</name>
  <files>pipeline/scripts/fetch_ine_population.py, data/ine/poblacion_comunal.json</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (INE Population Data section lines 557-567 — the bastianolea CSV source URL and the year-2024 field requirement; Assumption A6 line 594)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-11 static reference file, D-12 10000 threshold, D-17 CUT as canonical id)
    - .planning/phases/01-data-foundation/01-PATTERNS.md (population.py lines 397-424 for the consuming module's expected JSON shape)
  </read_first>
  <action>
    Write pipeline/scripts/fetch_ine_population.py: a one-time, idempotent script (NOT part of the quarterly scrape) that downloads the processed INE projections CSV from the bastianolea repo URL given in RESEARCH line 561 (datos_procesados/censo_proyecciones_año.csv), filters to year 2024, and emits one entry per commune with keys `cut` (CUT code as string), `name` (commune name), and `population_2024` (int). Write the result via the existing pipeline.shared.atomic_write.atomic_write_json (from Plan 01) to data/ine/poblacion_comunal.json.

    The CSV uses CUT codes; coerce them to string form (zero-padding is not applied — keep the raw CUT digits as the catalog uses, e.g. "13101", "2101"). If upstream column names differ from expected, the script must raise with the actual column names printed — do NOT silently emit partial data. Assert the output contains exactly 346 entries before writing; raise if not (consistency with D-14 count discipline).

    Run the script to produce data/ine/poblacion_comunal.json and commit it (versioned static asset per D-11). The script remains in the repo for reproducibility.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && python -c "import json,pathlib; d=json.loads(pathlib.Path('data/ine/poblacion_comunal.json').read_text(encoding='utf-8')); assert len(d)==346, len(d); assert all('cut' in e and 'population_2024' in e for e in d); print('346 INE entries OK')"</automated>
  </verify>
  <acceptance_criteria>
    - data/ine/poblacion_comunal.json is a JSON array of exactly 346 objects
    - Every object has string key `cut`, string key `name`, and integer key `population_2024`
    - Santiago (cut "13101") has a population_2024 value greater than 100000 (sanity floor)
    - pipeline/scripts/fetch_ine_population.py raises (non-zero exit) if the converted count is not 346
  </acceptance_criteria>
  <done>The 346-entry INE population JSON exists, is committed, and the fetch script is reproducible and count-guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Population lookup module + tests (DATA-04 filter substrate)</name>
  <files>pipeline/shared/population.py, pipeline/tests/test_population.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-PATTERNS.md (population.py section lines 397-424 — load_population + is_low_population + LOW_POPULATION_THRESHOLD are the canonical implementation)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-12 — communes under 10000 get rank null + low_population flag)
    - data/ine/poblacion_comunal.json (produced by Task 1 — read to confirm key names load_population must parse)
  </read_first>
  <behavior>
    - Test: load_population() returns a dict whose keys are CUT strings and values are ints
    - Test: load_population() contains exactly 346 entries
    - Test: is_low_population for a commune with population >= 10000 returns False
    - Test: is_low_population for a commune with population < 10000 returns True
    - Test: is_low_population for an unknown CUT returns True (default 0 < threshold — safer to exclude unknowns from rankings)
  </behavior>
  <action>
    Implement pipeline/shared/population.py exactly per 01-PATTERNS.md lines 397-424: DATA_DIR resolves to the repo-root /data via pathlib parents; load_population() is lru_cached, reads data/ine/poblacion_comunal.json, returns {str(entry['cut']): int(entry['population_2024'])}; LOW_POPULATION_THRESHOLD = 10_000 (D-12); is_low_population(cut_code) returns pop.get(cut_code, 0) < LOW_POPULATION_THRESHOLD.

    Write pipeline/tests/test_population.py covering all <behavior> cases. The tests read the real committed data/ine/poblacion_comunal.json from Task 1 (no mocking needed since it is a committed static asset). For the low/high population assertions, pick two real CUT codes by inspecting the file at test time rather than hardcoding magic numbers.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && python -m pytest pipeline/tests/test_population.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/shared/population.py contains `LOW_POPULATION_THRESHOLD = 10_000` and an `lru_cache` decorator on load_population
    - load_population() returns a dict of length 346
    - is_low_population returns True for an under-10000 commune and False for an over-10000 commune (asserted against real data)
    - python -m pytest pipeline/tests/test_population.py -q reports all tests passed
  </acceptance_criteria>
  <done>Population lookup loads the static asset, applies the 10000 threshold, and tests are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A one-time INE population fetch script and the committed data/ine/poblacion_comunal.json with 2024 population for all 346 communes, plus the population lookup module. The data comes from a third-party processed mirror of INE projections (Assumption A6 — must be spot-checked against the official source before downstream ranking depends on it).</what-built>
  <how-to-verify>
    1. Open data/ine/poblacion_comunal.json.
    2. For these 5 communes, compare population_2024 against the official INE projections page (https://www.ine.gob.cl/estadisticas/sociales/demografia-y-vitales/proyecciones-de-poblacion): Santiago (cut 13101), Puente Alto (13201), Antofagasta (2101), Valparaíso (5101), and one small commune such as Ollagüe (2102) or Torres del Paine (12104).
    3. Confirm each of the 5 values matches the official 2024 projection within rounding.
    4. Confirm at least one of the small sampled communes is below 10,000 (so the low_population path is exercised by real data).
  </how-to-verify>
  <resume-signal>Type "approved" if the 5 sampled values match official INE, or describe the discrepancies (which CUT, expected vs. found) so the fetch script can be corrected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bastianolea GitHub CSV → committed JSON | Third-party processed mirror of INE data crosses into our repo as authoritative population |
| data/ine JSON → pipeline ranking | Population values drive the low_population ranking filter (DATA-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Third-party population CSV | mitigate | Blocking human checkpoint spot-checks 5 communes against official INE (A6); script asserts exactly 346 entries before write |
| T-02-02 | Tampering | Wrong/partial conversion silently committed | mitigate | fetch script raises (non-zero) on column-name mismatch and on count != 346; no silent partial output |
| T-02-03 | Repudiation | Source provenance | accept | Provenance recorded in script header + RESEARCH citation; official INE page named in checkpoint for audit |
</threat_model>

<verification>
- data/ine/poblacion_comunal.json has exactly 346 entries with cut + population_2024
- python -m pytest pipeline/tests/test_population.py -q is green
- Human checkpoint confirms 5 sampled populations match official INE
</verification>

<success_criteria>
- All 346 communes have a committed 2024 population value keyed by CUT (DATA-04 substrate)
- is_low_population applies the 10,000 threshold (D-12) and is unit-tested
- Source values verified against official INE for 5 sampled communes (A6 closed)
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-02-SUMMARY.md` when done.
</output>
