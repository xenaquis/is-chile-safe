---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/requirements.txt
  - pipeline/pytest.ini
  - pipeline/__init__.py
  - pipeline/shared/__init__.py
  - pipeline/shared/schema.py
  - pipeline/shared/atomic_write.py
  - pipeline/tests/__init__.py
  - pipeline/tests/conftest.py
  - pipeline/tests/fixtures/.gitkeep
  - pipeline/tests/test_schema.py
  - pipeline/tests/test_atomic_write.py
  - pipeline/cache/.gitkeep
  - .gitignore
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "Pydantic v2 models validate a commune record and reject missing keys"
    - "Validation gate rejects when commune count is not exactly 346"
    - "Validation gate rejects negative or implausible (>20000) rates"
    - "Atomic write produces no partial file on crash and works on Windows"
    - "pytest runs and the schema + atomic-write tests pass"
  artifacts:
    - path: "pipeline/shared/schema.py"
      provides: "Pydantic v2 models (ComunaRecord, RegionRecord, NationalRecord, YearRecord, MapPayloadEntry, MapPayload, MetaIndexEntry) and validate_all_communes gate"
      contains: "class ComunaRecord"
    - path: "pipeline/shared/atomic_write.py"
      provides: "atomic_write_json using os.replace"
      contains: "os.replace"
    - path: "pipeline/tests/conftest.py"
      provides: "Shared fixtures: sample_commune_dict, sample_cead_html"
      contains: "def sample_commune_dict"
    - path: "pipeline/requirements.txt"
      provides: "Pinned Python dependency versions"
      contains: "pydantic"
    - path: "pipeline/pytest.ini"
      provides: "pytest config with rootdir and pythonpath"
      contains: "[pytest]"
  key_links:
    - from: "pipeline/tests/test_schema.py"
      to: "pipeline/shared/schema.py"
      via: "import validate_all_communes"
      pattern: "from pipeline.shared.schema import"
    - from: "pipeline/tests/test_atomic_write.py"
      to: "pipeline/shared/atomic_write.py"
      via: "import atomic_write_json"
      pattern: "atomic_write_json"
---

<objective>
Establish the Phase 1 pipeline scaffold and the data contracts everything else builds against: the Pydantic v2 schema, the atomic write utility, the dependency manifest, and the pytest test infrastructure (closing all RESEARCH Wave 0 gaps for schema + atomic write).

Purpose: This is the interface-first foundation. The schema is the single all-or-nothing validation gate (D-14) that protects all 346 output files. Every downstream plan (client, parser, normalizer, orchestrator) imports from `pipeline/shared/schema.py`. Defining it first prevents executors from inventing field names.

Output: A runnable Python package skeleton, a green test suite for schema validation and atomic writes, pinned dependencies, and git-ignored cache/raw-response directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-PATTERNS.md
@.planning/phases/01-data-foundation/01-VALIDATION.md

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans (02, 03, 04) consume these. -->
<!-- Field names and model names below are the canonical target — do not rename. -->

Schema models to define in pipeline/shared/schema.py (Pydantic v2 syntax only — @.planning/phases/01-data-foundation/01-PATTERNS.md lines 302-368 are the source of truth):
- YearRecord: year:int, rate_per_100k:float, by_family:dict[str,float|None], partial:bool=False
- ComunaRecord: id:str (CUT), name:str, slug:str, region_id:str, population:int, low_population:bool, national_rank:int|None, regional_rank:int|None, trend:Literal["up","down","stable"], featured_rates:dict, series:list[YearRecord], last_updated:str
- RegionRecord: id:str (region code "13"), name:str, slug:str, series:list[YearRecord], comuna_ranking:list[dict], last_updated:str
- NationalRecord: name:str, series:list[YearRecord], last_updated:str
- MapPayloadEntry: id:str (CUT), rate:float, level:int (1-5 quintile), by_family:dict[str,float|None]
- MapPayload: generated:str, year:int, comunas:list[MapPayloadEntry]
- MetaIndexEntry: cut:str, name:str, slug:str, region_id:str, population:int, low_population:bool

Validation gate function signature:
- validate_all_communes(all_communes: list[dict]) -> list[ComunaRecord]
  Raises ValueError("Expected 346 communes, got N") if len != 346.
  Raises on negative rate and on rate > 20000 (first-run absolute limits, Open Question 4).

FAMILIA_IDS family key names (by_family dict keys): vida, robos_violentos, vif, drogas, armas, propiedad, incivilidades
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pipeline package scaffold, dependencies, and git-ignore</name>
  <files>pipeline/requirements.txt, pipeline/pytest.ini, pipeline/__init__.py, pipeline/shared/__init__.py, pipeline/tests/__init__.py, pipeline/tests/fixtures/.gitkeep, pipeline/cache/.gitkeep, .gitignore</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Standard Stack section lines 86-115 for exact pinned versions; Recommended Project Structure lines 184-218 for directory layout)
    - .planning/phases/01-data-foundation/01-PATTERNS.md (Pydantic v2 Syntax shared pattern lines 581-587)
    - .gitignore (if it exists — read current state before editing; do not clobber existing entries)
  </read_first>
  <behavior>
    - `python -m pytest --version` works after `pip install -r pipeline/requirements.txt`
    - Importing `pipeline.shared` and `pipeline.tests` as packages does not error
    - `pipeline/cache/` and the raw-response cache are git-ignored; `pipeline/cache/.gitkeep` is tracked
  </behavior>
  <action>
    Create the Python package skeleton under `pipeline/` with empty `__init__.py` files at `pipeline/__init__.py`, `pipeline/shared/__init__.py`, `pipeline/tests/__init__.py`.

    Write `pipeline/requirements.txt` pinning exactly: requests==2.34.2, beautifulsoup4==4.15.0, lxml==6.1.1, pydantic==2.13.4, tenacity==9.1.4, python-dotenv==1.2.2, openpyxl==3.1.5, and pytest (add pytest==8.* — RESEARCH Validation Architecture names pytest 8.x as the framework, not yet in the stack list).

    Write `pipeline/pytest.ini` with a `[pytest]` section that sets `pythonpath = ..` (so `from pipeline.shared.schema import ...` resolves with the repo root on the path) and `testpaths = tests`. Use `pythonpath` (pytest 7+ ini option) so tests import the package as `pipeline.shared.*` per the import patterns in 01-PATTERNS.md.

    Create `pipeline/cache/.gitkeep` and `pipeline/tests/fixtures/.gitkeep` so the empty dirs are tracked.

    Update `.gitignore` (create if absent) to ignore `pipeline/cache/*` except `.gitkeep`, plus `__pycache__/`, `*.pyc`, `.pytest_cache/`, `*.tmp`, and `.env` (D-04: raw responses cached locally, never committed; secrets out of repo).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && pip install -r pipeline/requirements.txt && python -m pytest --version</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/requirements.txt` contains the literal lines `pydantic==2.13.4`, `beautifulsoup4==4.15.0`, `lxml==6.1.1`, `tenacity==9.1.4`, `requests==2.34.2`, `openpyxl==3.1.5`, `python-dotenv==1.2.2`, and a `pytest==8.*` line
    - `pipeline/pytest.ini` contains `[pytest]` and a `pythonpath` entry pointing at the repo root (`..`)
    - `python -m pytest --version` exits 0
    - `.gitignore` contains an entry that ignores `pipeline/cache/` contents and an entry for `.env`
    - Files `pipeline/__init__.py`, `pipeline/shared/__init__.py`, `pipeline/tests/__init__.py` all exist
  </acceptance_criteria>
  <done>Dependencies install, pytest runs, package imports resolve, cache and secrets are git-ignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic v2 schema models + all-or-nothing validation gate (DATA-02, DATA-03)</name>
  <files>pipeline/shared/schema.py, pipeline/tests/conftest.py, pipeline/tests/test_schema.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-PATTERNS.md (schema.py section lines 302-368 — Pydantic v2 model definitions and validate_all_communes gate are the canonical source; conftest fixture lines 487-509; test_schema patterns lines 512-525)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 3 lines 279-342 for the validation gate; Assumptions A4 for featured subgroup uncertainty; Open Question 4 for the 0-20000 first-run absolute limits)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-14 all-or-nothing, D-16 plausibility, D-17 CUT id rules, D-06 featured flag)
  </read_first>
  <behavior>
    - Test: validate_all_communes with exactly 346 valid commune dicts returns 346 ComunaRecord objects
    - Test: validate_all_communes with 200 dicts raises ValueError matching "Expected 346"
    - Test: a commune dict missing the `name` key raises pydantic ValidationError through the gate
    - Test: an `id` of "ABC" (non-numeric) is rejected by the id_must_be_cut field validator
    - Test: a series entry with rate_per_100k = -5 raises ValueError ("Negative rate")
    - Test: a series entry with rate_per_100k = 25000 raises ValueError ("Implausible")
    - Test: a valid MapPayload with one MapPayloadEntry (level 1-5) validates successfully
  </behavior>
  <action>
    Implement `pipeline/shared/schema.py` using the exact model names, field names, and types from the `<interfaces>` block and 01-PATTERNS.md lines 302-368. Use Pydantic v2 syntax exclusively: `model_validate()`, `@field_validator` with `@classmethod`, `model_config` — never v1 `@validator`/`parse_obj`/`class Config` (RESEARCH State of the Art).

    Define models: YearRecord, ComunaRecord (with `id_must_be_cut` field validator requiring `v.isdigit() and 4 <= len(v) <= 5`), RegionRecord, NationalRecord, MapPayloadEntry (with a validator asserting `1 <= level <= 5`), MapPayload, MetaIndexEntry.

    Implement `validate_all_communes(all_communes: list[dict]) -> list[ComunaRecord]`: raise `ValueError("Expected 346 communes, got {n}")` if count != 346 (D-14, Pitfall 4); run `ComunaRecord.model_validate` on each; then plausibility-loop each YearRecord raising `ValueError` on negative rate ("Negative rate for {id} in {year}") and on rate > 20000 ("Implausible rate ..." — Open Question 4 first-run absolute upper bound, D-16). Add a module constant `PLAUSIBILITY_MAX_RATE = 20000` so the threshold is a single named value to tighten later.

    Add a module constant list `FAMILY_KEYS = ["vida","robos_violentos","vif","drogas","armas","propiedad","incivilidades"]` for downstream by_family key consistency, and `FEATURED_FAMILIES = {"propiedad","vida"}` (D-06).

    Write `pipeline/tests/conftest.py` with `sample_commune_dict` and `sample_cead_html` fixtures per 01-PATTERNS.md lines 487-509 (sample_cead_html reads `fixtures/cead_response_sample.html` — guard with a skip if the fixture file is absent so this task's tests do not require live data).

    Write `pipeline/tests/test_schema.py` covering every case in `<behavior>`.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && python -m pytest pipeline/tests/test_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/shared/schema.py` defines classes `YearRecord`, `ComunaRecord`, `RegionRecord`, `NationalRecord`, `MapPayloadEntry`, `MapPayload`, `MetaIndexEntry`
    - Source contains `@field_validator` and contains no `@validator(` and no `parse_obj(` (v2-only assertion)
    - `validate_all_communes([sample]*200)` raises ValueError with message containing "Expected 346"
    - `validate_all_communes` with a 346-length list containing one negative rate raises ValueError containing "Negative"
    - `pipeline/shared/schema.py` contains the literal `PLAUSIBILITY_MAX_RATE = 20000`
    - `python -m pytest pipeline/tests/test_schema.py -q` reports all tests passed
  </acceptance_criteria>
  <done>Schema models validate correct records, reject count/key/range violations, and the test suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Atomic write utility + test (DATA-03)</name>
  <files>pipeline/shared/atomic_write.py, pipeline/tests/test_atomic_write.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-PATTERNS.md (atomic_write.py section lines 372-393 — the os.replace pattern is the canonical source; Windows compatibility note)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 4 lines 344-357; Don't Hand-Roll row for atomic write line 436)
  </read_first>
  <behavior>
    - Test: atomic_write_json writes a dict to a path; the file content round-trips via json.loads to the same dict
    - Test: parent directories are created if missing (mkdir parents=True)
    - Test: no `.tmp` file remains in the directory after a successful write
    - Test: UTF-8 content with accented commune name (e.g. "Ñuñoa") round-trips without corruption (ensure_ascii=False)
  </behavior>
  <action>
    Implement `pipeline/shared/atomic_write.py` with `atomic_write_json(path: pathlib.Path, data: dict | list) -> None` exactly per 01-PATTERNS.md lines 372-393: `path.parent.mkdir(parents=True, exist_ok=True)`, write to `path.with_suffix('.tmp')` using `json.dumps(data, ensure_ascii=False, indent=2)` encoded utf-8, then `os.replace(tmp, path)` (atomic on Windows and POSIX — this repo runs on Windows locally per environment).

    Write `pipeline/tests/test_atomic_write.py` covering all `<behavior>` cases using pytest's `tmp_path` fixture. For the accented-name test, write `{"name": "Ñuñoa"}` and assert the re-read file's `name` equals `"Ñuñoa"`.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe" && python -m pytest pipeline/tests/test_atomic_write.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/shared/atomic_write.py` contains `os.replace(` and `ensure_ascii=False`
    - Test asserts no file matching `*.tmp` remains in `tmp_path` after a successful write
    - Test asserts `{"name": "Ñuñoa"}` round-trips with the accented character intact
    - `python -m pytest pipeline/tests/test_atomic_write.py -q` reports all tests passed
  </acceptance_criteria>
  <done>Atomic write round-trips UTF-8 JSON, leaves no temp files, and the test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CEAD server → pipeline parser | Untrusted government endpoint data crosses into our normalized JSON (handled in Plan 03/04; schema is the gate) |
| pipeline → repo `/data/` | Validated JSON committed to version control and served read-only to the static site |
| PyPI → pipeline runtime | Third-party Python packages installed into the build/runtime environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Parsed CEAD data written to repo | mitigate | `validate_all_communes` is the single all-or-nothing gate (D-14); rejects count != 346, missing keys, and rates outside 0-20000 before any write |
| T-01-02 | Denial of Service | Unbounded file write | mitigate | Count validated == 346 before write; bounded, known file set |
| T-01-03 | Information Disclosure | Secrets in repo | mitigate | `.gitignore` excludes `.env`; no secrets used in this phase (V6 N/A) |
| T-01-SC | Tampering | pip installs (supply chain) | mitigate | All 7 packages passed slopcheck in RESEARCH Package Legitimacy Audit (all [OK], zero [SLOP]/[SUS]); versions pinned exactly in requirements.txt. No blocking checkpoint required — audit table present and clean |
</threat_model>

<verification>
- `pip install -r pipeline/requirements.txt` succeeds with pinned versions
- `python -m pytest pipeline/tests/ -q` is green (schema + atomic write)
- `from pipeline.shared.schema import validate_all_communes` and `from pipeline.shared.atomic_write import atomic_write_json` both resolve
</verification>

<success_criteria>
- Pydantic v2 schema enforces 346-count, required keys, CUT-id format, and 0-20000 rate range (DATA-02)
- Atomic write utility prevents partial files and preserves UTF-8 accents (DATA-03 substrate)
- Test infrastructure (pytest.ini, conftest fixtures) is in place and green — closes RESEARCH Wave 0 gaps for schema and atomic write
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` when done.
</output>
