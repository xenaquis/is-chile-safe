---
phase: 04-editorial-pages-adsense
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/AdSlot.astro
  - site/src/components/CookieConsent.astro
  - site/src/layouts/BaseLayout.astro
  - site/src/config/i18n.ts
  - site/src/components/PageFooter.astro
autonomous: true
requirements: [MON-01, EDIT-04]
user_setup:
  - service: google-adsense
    why: "Publisher ID for the gated AdSense loader (activation deferred to Phase 6)"
    env_vars:
      - name: PUBLIC_ADSENSE_PUBLISHER_ID
        source: "Google AdSense dashboard -> Account -> Settings (ca-pub-XXXXXXXXXXXXXXXX). Leave UNSET in Phase 4 — slots render placeholders by default."
      - name: ADSENSE_ENABLED
        source: "Set to the string 'true' only in Phase 6 after first GSC indexing wave. Default unset = OFF."
must_haves:
  truths:
    - "With ADSENSE_ENABLED unset, no dist/ page contains an <ins class=\"adsbygoogle\"> tag or the pagead2.googlesyndication script — AdSlot renders a reserved-dimension placeholder only"
    - "The footer on every page links to Privacy Policy, Terms, About, and Contact in the correct locale"
    - "The cookie-consent banner exists as a component injected only when AdSense is enabled"
  artifacts:
    - path: "site/src/components/AdSlot.astro"
      provides: "CLS-safe ad slot, env-gated by ADSENSE_ENABLED (MON-01, D-09/D-10)"
      contains: "ADSENSE_ENABLED"
    - path: "site/src/components/CookieConsent.astro"
      provides: "Sticky-bottom consent banner, localStorage gate (D-11)"
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "Gated async AdSense loader in head (D-12)"
      contains: "ADSENSE_ENABLED"
    - path: "site/src/components/PageFooter.astro"
      provides: "Footer legal-page links (EDIT-04 footer requirement, D-13)"
    - path: "site/src/config/i18n.ts"
      provides: "Footer + cookie + data-callout EN/ES strings"
  key_links:
    - from: "site/src/components/PageFooter.astro"
      to: "site/src/config/i18n.ts"
      via: "footer_* string keys"
      pattern: "footer_privacy|footer_terms|footer_about|footer_contact"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "import.meta.env.ADSENSE_ENABLED"
      via: "build-time env gate around the async loader script"
      pattern: "ADSENSE_ENABLED"
---

<objective>
Build the AdSense subsystem (MON-01) gated OFF by default, plus the footer legal links (EDIT-04 footer requirement). The `<AdSlot>` component reserves fixed CLS-safe dimensions and emits the adsbygoogle `<ins>` ONLY when `ADSENSE_ENABLED === 'true'`; otherwise an `aria-hidden` placeholder fills the same reserved height. `<CookieConsent>` is the consent banner (injected only when AdSense is enabled, by EditorialLayout in Plan 03). BaseLayout gains the single async AdSense loader in `<head>`, gated by the same flag. Extend `i18n.ts` with footer/cookie/callout strings and add the four legal links to `PageFooter.astro`.

Purpose: Monetization scaffolding that is fully present at build time but inert until Phase 6 flips the flag — no ad markup, no Google script, no third-party request ships in Phase 4 (D-09).
Output: AdSlot.astro, CookieConsent.astro, BaseLayout AdSense loader, i18n strings, footer legal links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-editorial-pages-adsense/04-CONTEXT.md
@.planning/phases/04-editorial-pages-adsense/04-RESEARCH.md
@.planning/phases/04-editorial-pages-adsense/04-PATTERNS.md
@.planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md

<interfaces>
<!-- Existing BaseLayout head + props (BaseLayout.astro). Loader injection goes after the manifest link, before the JSON-LD block. -->
BaseLayout Props: { lang:'en'|'es'; title; description; enPath; esPath; jsonLd?:object|null; mountType?:string }
head currently emits: charset, viewport, title, description, robots, hreflang(en/es/x-default), canonical, theme-color, manifest, conditional JSON-LD.

<!-- PageFooter (PageFooter.astro). footer-inner is a flex row; add a SECOND nav row below it, do not alter the first row. -->
PageFooter Props: { locale:'en'|'es'; enPath; esPath }
existing scoped styles: .csm-footer, .footer-inner, .footer-wordmark, .footer-attribution(flex:1), .footer-lang

<!-- i18n (i18n.ts). Add keys to I18nStrings interface, then provide values in BOTH EN_STRINGS and ES_STRINGS. -->
export interface I18nStrings { ... }   export const EN_STRINGS / ES_STRINGS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdSlot.astro (env-gated, CLS-safe)</name>
  <files>site/src/components/AdSlot.astro</files>
  <read_first>
    - site/src/components/MapPlaceholderSlot.astro (env-gated reserved-space placeholder analog)
    - site/src/components/StatCard.astro (scoped-style + CSS token convention)
    - .planning/phases/04-editorial-pages-adsense/04-RESEARCH.md (Pattern 6 AdSlot, Pitfall 4 env evaluation)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (## AdSlot Component Specification — reserved dims table, placement rules)
    - .planning/phases/04-editorial-pages-adsense/04-PATTERNS.md (AdSlot.astro section — Props + gate + style)
  </read_first>
  <action>
    Create AdSlot.astro with Props `slot: 'content' | 'bottom'` and optional `publisherId?: string`. Default publisherId to `import.meta.env.PUBLIC_ADSENSE_PUBLISHER_ID ?? ''`. Compute `enabled` via explicit string compare `import.meta.env.ADSENSE_ENABLED === 'true'` (never `!!env` — Pitfall 4). Render a wrapper div with classes `ad-slot ad-slot--{slot}` that ALWAYS reserves the dimensions. Inside: when `enabled && publisherId`, render the adsbygoogle `<ins>` with `data-ad-client={publisherId}`, `data-ad-slot` = `CONTENT_SLOT_ID` for content / `BOTTOM_SLOT_ID` for bottom, `data-ad-format="auto"`, `data-full-width-responsive="true"`; otherwise render `<div class="ad-placeholder" aria-hidden="true">`. Scoped styles from UI-SPEC: width 100%, max-width 728px, margin `var(--lg)` auto; both content/bottom 90px desktop, 50px at `max-width:639px`; placeholder `var(--bg)` bg + `2px dashed var(--line)` + `var(--radius)`. No `client:*` directive. Do not inline the pagead loader here (D-12: loader lives once in BaseLayout). Page-type exclusion is the templates' job — this component does not self-restrict.
  </action>
  <verify>
    <automated>cd site; if (((Get-Content src/components/AdSlot.astro -Raw) -match "ADSENSE_ENABLED === 'true'") -and ((Get-Content src/components/AdSlot.astro -Raw) -match "adsbygoogle")) { Write-Host PASS } else { Write-Error FAIL }</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/components/AdSlot.astro` exists with Props `slot` and optional `publisherId`.
    - Gate uses exact string compare `import.meta.env.ADSENSE_ENABLED === 'true'`.
    - The reserved-dimension wrapper renders in both states (placeholder when disabled).
    - No `client:` directive present in the file.
  </acceptance_criteria>
  <done>AdSlot renders a 90px/50px reserved placeholder by default; emits adsbygoogle ins only when enabled+publisherId present.</done>
</task>

<task type="auto">
  <name>Task 2: Create CookieConsent.astro + gated AdSense loader in BaseLayout</name>
  <files>site/src/components/CookieConsent.astro, site/src/layouts/BaseLayout.astro</files>
  <read_first>
    - site/src/layouts/BaseLayout.astro (head structure; insert loader after manifest link, before JSON-LD block; do not change Props or hreflang logic)
    - site/src/components/PageFooter.astro (sticky Astro component + scoped style + zero client:* pattern — analog for CookieConsent)
    - .planning/phases/04-editorial-pages-adsense/04-RESEARCH.md (Pattern 7 CookieConsent, Pitfall 4 loader env, State of the Art — single async loader)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (## Cookie-Consent Banner Specification; ## AdSense Loader D-12)
    - .planning/phases/04-editorial-pages-adsense/04-PATTERNS.md (CookieConsent.astro section)
  </read_first>
  <action>
    CookieConsent.astro: Props `locale: 'en' | 'es'`. Render a banner div `id="csm-consent-banner" class="consent-banner" role="dialog"` with EN/ES copy from i18n keys `cookie_banner_text`, `cookie_banner_accept`, `cookie_banner_reject` plus a Privacy Policy link (`/privacy-policy/` EN, `/es/politica-privacidad/` ES). Add two scripts: an inline `is:inline` script that hides the banner synchronously before first paint when `localStorage.getItem('csm_consent')` is set; and a regular script wiring buttons `csm-consent-accept` (set key 'accepted' + hide) and `csm-consent-reject` (set key 'rejected' + hide). Scoped styles per UI-SPEC: fixed bottom full-width z-index 200, `var(--ink)` bg, `var(--card)` text, padding `var(--md) var(--lg)`, flex wrap; Accept button `var(--primary)` fill / `var(--card)` text; Reject transparent + `1px solid var(--card)`; both 14px/700, padding 8px 16px, `calc(var(--radius)/2)` radius, min-height 44px. No `client:*`.

    BaseLayout.astro: add to frontmatter `const adsenseEnabled = import.meta.env.ADSENSE_ENABLED === 'true'` and `const adsenseClient = import.meta.env.PUBLIC_ADSENSE_PUBLISHER_ID ?? ''`. In `<head>`, after the manifest link and before the JSON-LD conditional block, emit the single async loader ONLY when `adsenseEnabled && adsenseClient`: an `is:inline async` script whose src is `https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client={adsenseClient}` with `crossorigin="anonymous"`. Default (flag unset) emits nothing. Do NOT add CookieConsent into BaseLayout (EditorialLayout injects it in Plan 03). Preserve all existing BaseLayout props/behavior.
  </action>
  <verify>
    <automated>cd site && npm run build && node -e "const{readdirSync,readFileSync,statSync}=require('fs');const p=require('path');let hit=0;(function w(d){for(const e of readdirSync(d)){const f=p.join(d,e);if(statSync(f).isDirectory())w(f);else if(f.endsWith('.html')&&/pagead2\.googlesyndication|adsbygoogle/.test(readFileSync(f,'utf8')))hit++;}})('dist');if(hit>0){console.error('FAIL: adsense markup leaked in',hit,'pages');process.exit(1)}console.log('PASS: no adsense markup when disabled')"</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/components/CookieConsent.astro` exists with Props `locale`, banner ids `csm-consent-banner`/`csm-consent-accept`/`csm-consent-reject`, and the localStorage `csm_consent` logic.
    - BaseLayout.astro contains `ADSENSE_ENABLED` gate and the conditional pagead2 loader; existing Props unchanged.
    - After `npm run build`, recursive scan of `dist/` finds ZERO occurrences of `pagead2.googlesyndication` or `adsbygoogle` (flag unset).
  </acceptance_criteria>
  <done>Consent banner component built; AdSense loader gated in BaseLayout; no ad/Google markup ships with the flag off.</done>
</task>

<task type="auto">
  <name>Task 3: Extend i18n.ts strings + add legal links to PageFooter.astro</name>
  <files>site/src/config/i18n.ts, site/src/components/PageFooter.astro</files>
  <read_first>
    - site/src/config/i18n.ts (I18nStrings interface; EN_STRINGS + ES_STRINGS; naming convention noun_verb / context_role)
    - site/src/components/PageFooter.astro (footer-inner flex row + scoped styles — add a second nav row, do not alter the first)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (## Footer Extension table; ## Cookie-Consent copy table)
    - .planning/phases/04-editorial-pages-adsense/04-PATTERNS.md (PageFooter + i18n.ts modify sections — exact keys + link paths)
  </read_first>
  <action>
    i18n.ts: add to the `I18nStrings` interface and provide EN+ES values for: `footer_privacy`, `footer_terms`, `footer_about`, `footer_contact`, `cookie_banner_text`, `cookie_banner_accept`, `cookie_banner_reject`, `data_callout_source_prefix`. Use the exact UI-SPEC copy — EN footer: "Privacy Policy"/"Terms of Use"/"About"/"Contact"; ES: "Política de Privacidad"/"Términos de Uso"/"Acerca de"/"Contacto". Cookie EN: "We use cookies to serve ads and analyze traffic. See our Privacy Policy." (Accept/Reject); ES: "Usamos cookies para mostrar anuncios y analizar el tráfico. Ver nuestra Política de Privacidad." (Aceptar/Rechazar). callout prefix: "Source: CEAD" / "Fuente: CEAD". Keep sober editorial tone (the forbidden-language gate scans rendered output of these strings).

    PageFooter.astro: below the existing `.footer-inner` flex row (do NOT modify it), add a second `<nav class="footer-legal page-content">` row of four links using locale-correct labels + paths: EN `/privacy-policy/`, `/terms/`, `/about/`, `/contact/`; ES `/es/politica-privacidad/`, `/es/terminos/`, `/es/acerca-de/`, `/es/contacto/`. Style links `font-size: var(--text-label); font-weight: var(--weight-body); color: var(--muted); text-decoration:none` with `var(--md)` gap; no new design tokens. Import strings from i18n.ts keyed by `locale`.
  </action>
  <verify>
    <automated>cd site && npm run build && node -e "const{readFileSync}=require('fs');const h=readFileSync('dist/index.html','utf8');const es=readFileSync('dist/es/index.html','utf8');if(/\/privacy-policy\//.test(h)&&/\/terms\//.test(h)&&/\/about\//.test(h)&&/\/contact\//.test(h)&&/\/es\/politica-privacidad\//.test(es)&&/\/es\/terminos\//.test(es)){console.log('PASS')}else{console.error('FAIL: footer legal links missing');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - i18n.ts I18nStrings interface and both EN_STRINGS/ES_STRINGS contain all 8 new keys; TypeScript build does not error on missing keys.
    - `dist/index.html` footer contains links to `/privacy-policy/`, `/terms/`, `/about/`, `/contact/`.
    - `dist/es/index.html` footer contains links to `/es/politica-privacidad/`, `/es/terminos/`, `/es/acerca-de/`, `/es/contacto/`.
    - The original `.footer-inner` row (wordmark/attribution/lang) is unchanged.
  </acceptance_criteria>
  <done>i18n carries footer/cookie/callout strings; footer renders four locale-correct legal links on every page without disrupting the existing row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Google AdSense CDN | Async loader script (only when ADSENSE_ENABLED=true); deferred to Phase 6 |
| browser -> localStorage | Cookie-consent key (`csm_consent`); non-sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Info Disclosure | AdSense publisher ID in HTML | accept | Publisher ID is public-by-design (in every served page); `PUBLIC_` env prefix is the correct Astro convention (RESEARCH Security Domain) |
| T-04-04 | Info Disclosure | AdSense script / cookies set without disclosure | mitigate | CookieConsent banner + Privacy Policy (EDIT-04) disclose ads/cookies; loader gated OFF until Phase 6 (V8) |
| T-04-05 | Repudiation | consent bypass via cleared localStorage | accept | Static site, no EU establishment; soft-compliance appropriate (RESEARCH Security Domain) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit empty) |
</threat_model>

<verification>
- `cd site && npm run build && node scripts/validate/all.mjs` — full suite passes; forbidden-language validator (Plan 01) scans the new footer/cookie strings and passes (sober copy).
- Recursive dist/ scan: zero `pagead2.googlesyndication` / `adsbygoogle` occurrences with the flag unset.
- Footer legal links present on EN root and ES root.
- Single chained build+validate command (OneDrive desync — Pitfall 6).
</verification>

<success_criteria>
- AdSlot + CookieConsent components exist; BaseLayout has the gated loader.
- With ADSENSE_ENABLED unset, no ad markup or Google script ships.
- Footer links to all four legal pages per locale on every page.
- i18n.ts carries all new strings; TypeScript build clean.
</success_criteria>

<output>
Create `.planning/phases/04-editorial-pages-adsense/04-02-SUMMARY.md` when done
</output>
