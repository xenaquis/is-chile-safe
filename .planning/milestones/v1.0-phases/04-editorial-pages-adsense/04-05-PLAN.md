---
phase: 04-editorial-pages-adsense
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-03]
files_modified:
  - site/src/pages/chile-crime-map.astro
  - site/src/pages/santiago-safety-map.astro
  - site/src/pages/safest-cities-in-chile.astro
  - site/src/pages/methodology.astro
autonomous: true
requirements: [EDIT-01, EDIT-03]
must_haves:
  truths:
    - "Two EN map-bearing editorial pages embed the Phase-3 MapIsland (client:only=react) with editorial prose around it"
    - "The safest-cities page presents a CEAD-sourced ranking with qualified comparative language"
    - "The methodology page documents CEAD sources, rate-per-100k calc, subregistro caveat, trend formula, comparison criteria, and a What-this-site-does-NOT-say section"
    - "All four pages pass the forbidden-language gate"
  artifacts:
    - path: "site/src/pages/chile-crime-map.astro"
      provides: "EDIT-01 map-bearing editorial page (D-03)"
      contains: "MapIsland"
    - path: "site/src/pages/methodology.astro"
      provides: "EDIT-03 methodology page"
      min_lines: 40
    - path: "site/src/pages/safest-cities-in-chile.astro"
      provides: "EDIT-01 safest-cities ranking editorial page"
      min_lines: 40
  key_links:
    - from: "site/src/pages/chile-crime-map.astro"
      to: "site/src/components/map/MapIsland"
      via: "client:only react embed"
      pattern: "client:only"
    - from: "site/src/pages/safest-cities-in-chile.astro"
      to: "loadIndex / loadNationalAverage"
      via: "build-time ranking data"
      pattern: "loadIndex|loadRolloutCuts"
---

<objective>
Author the two EN map-bearing editorial pages (D-03), the safest-cities ranking page, and the methodology page (EDIT-03). Map-bearing pages (`/chile-crime-map/`, `/santiago-safety-map/`) embed the Phase-3 `MapIsland` (`client:only="react"`) inside a full-width block that breaks out of the 720px prose column, with editorial prose before and after; no AdSlot adjacent to the map (D-10). `/safest-cities-in-chile/` presents a CEAD-sourced ranking with strictly qualified comparatives. `/methodology/` documents CEAD sources, rate calculation, subregistro/cifra negra, the trend formula, comparison criteria, and an explicit "What this site does NOT say" section (EDIT-03, D-15).

Purpose: The remaining EN hero pages, including the credibility-critical methodology page AdSense approval depends on.
Output: 4 new EN pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-editorial-pages-adsense/04-CONTEXT.md
@.planning/phases/04-editorial-pages-adsense/04-PATTERNS.md
@.planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/04-editorial-pages-adsense/04-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 + Phase 3 (consumed here): -->
EditorialLayout Props: { lang:'en'|'es'; title; description; enPath; esPath; jsonLd?:object|null }
MapIsland (site/src/components/map/MapIsland): React island — embed as <MapIsland client:only="react" lang="en" />  (exact wiring in site/src/pages/map.astro)

<!-- Data API for the ranking page (site/src/lib/data.ts): -->
loadIndex(): CommuneMeta[]                       // all communes (cut/name/slug/region_id/population/low_population)
loadRolloutCuts(): string[]                      // currently published commune CUTs (rollout gate)
loadCommune(cut): CommuneData                    // .national_rank, latestCompleteYear, rate via latestCompleteYearRate
loadNationalAverage(): number                    // MEAN — never the SUM
// Rankings exclude low_population communes (DATA-04 / 10k filter); use national_rank already computed in commune data.

<!-- Methodology source-of-truth (Phase 1 CONTEXT): rate per 100k = canonical metric; trend formula = % threshold (Phase-1 D-13); low_population default; subregistro/cifra negra caveat. -->
</interfaces>

<map_page_note>
Map-bearing editorial uses EditorialLayout (NOT raw BaseLayout). The MapIsland block must break out of the 720px prose column (`width:100%; max-width:none`). AdSlot is FORBIDDEN inside or adjacent to the MapIslandBlock (D-10 exclusion). Reuse the exact MapIsland mount from map.astro/es/mapa.astro including the visually-hidden locate-button span. hreflang: /chile-crime-map/ <-> /es/mapa-delito-chile/ ; /santiago-safety-map/ <-> /es/mapa-seguridad-santiago/ (verbatim EDIT-02).
</map_page_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: chile-crime-map.astro + santiago-safety-map.astro (map-bearing)</name>
  <files>site/src/pages/chile-crime-map.astro, site/src/pages/santiago-safety-map.astro</files>
  <read_first>
    - site/src/pages/map.astro (exact MapIsland mount: import path, client:only="react", visually-hidden locate span)
    - site/src/layouts/EditorialLayout.astro (Plan 03 — wrapper + prose column; map block breaks out)
    - site/src/lib/data.ts (loadCommune('13101') for Santiago callouts)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (Template 2 map-bearing section order; AdSlot exclusion near map; responsive breakout margins; meta tags)
    - .planning/phases/04-editorial-pages-adsense/04-PATTERNS.md (Map-bearing editorial pages section)
  </read_first>
  <action>
    chile-crime-map.astro (/chile-crime-map/): EditorialLayout, lang="en", esPath="/es/mapa-delito-chile/", title/description per UI-SPEC. Section order (Template 2): EditorialHero → EditorialIntro (1-3 prose paras) → MapIslandBlock (full-width breakout `width:100%; max-width:none`, min-height 480px desktop/320px mobile) containing the visually-hidden locate span + `<MapIsland client:only="react" lang="en" />` exactly as in map.astro → EditorialBody (continues below, 720px) → DataCallout(s) for national average via loadNationalAverage → bottom AdSlot via EditorialLayout (NOT adjacent to map). ~800-1100 words total prose. Emit WebPage JSON-LD.

    santiago-safety-map.astro (/santiago-safety-map/): EditorialLayout, esPath="/es/mapa-seguridad-santiago/". Same Template-2 structure focused on Santiago/RM. Embed MapIsland identically. Load `loadCommune('13101')` for Santiago DataCallouts (rate vs national, trend). Sober framing, qualified comparatives.
    Both: NO AdSlot inside/adjacent to the MapIslandBlock (D-10).
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/forbidden-language.mjs && node scripts/validate/map.mjs && node -e "const{readFileSync}=require('fs');for(const p of ['dist/chile-crime-map/index.html','dist/santiago-safety-map/index.html']){const h=readFileSync(p,'utf8');if(!/astro-island|data-astro-cid|client/i.test(h)){console.error('FAIL: map island not mounted on '+p);process.exit(1)}}console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/chile-crime-map/index.html` and `dist/santiago-safety-map/index.html` build and pass forbidden-language + map.mjs validators.
    - Each page source embeds `<MapIsland client:only="react" lang="en" />` and the visually-hidden locate span (matching map.astro).
    - No AdSlot markup appears inside the MapIslandBlock container.
    - hreflang esPath values match EDIT-02 verbatim.
  </acceptance_criteria>
  <done>Both map-bearing editorial pages embed MapIsland with surrounding prose; no ad adjacent to map; gate + map validators pass.</done>
</task>

<task type="auto">
  <name>Task 2: safest-cities-in-chile.astro (CEAD ranking)</name>
  <files>site/src/pages/safest-cities-in-chile.astro</files>
  <read_first>
    - site/src/pages/commune/[slug].astro (data-loading + ranking pattern analog)
    - site/src/lib/data.ts (loadIndex, loadRolloutCuts, loadCommune, loadNationalAverage, latestCompleteYearRate; low_population exclusion)
    - site/src/components/DataCallout.astro (Plan 03)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (Template 1; Copywriting Contract — qualified superlatives MANDATORY here; meta tags)
    - .planning/REQUIREMENTS.md (EDIT-01; DATA-04 ranking rules)
  </read_first>
  <action>
    safest-cities-in-chile.astro (/safest-cities-in-chile/): EditorialLayout, lang="en", esPath="/es/comunas-mas-seguras-chile/". ~900-1300 words. Build a lowest-reported-incidence ranking at build time: load published communes (loadRolloutCuts → loadCommune, or loadIndex filtered to non-low_population), sort by latest-year rate ascending, take a top N (e.g. 10-15). Render as a list/table with commune name, region, rate per 100k, CEAD {year}. CRITICAL editorial constraint: this page is the highest-risk for forbidden language — every comparative ("safest", "lowest", "least") MUST be qualified ("...according to CEAD data for {year}, by reported incidence per 100k") and the page must NOT imply absolute safety or a "definitive ranking". Exclude low_population communes (DATA-04) and state that exclusion in prose. DataCallout for the national average reference. Emit WebPage JSON-LD.
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/forbidden-language.mjs && node -e "const{readFileSync}=require('fs');const h=readFileSync('dist/safest-cities-in-chile/index.html','utf8');if(/CEAD/.test(h)&&/per 100/i.test(h)){console.log('PASS')}else{console.error('FAIL: ranking/source missing');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/safest-cities-in-chile/index.html` builds and passes forbidden-language (no bare "the safest" / "definitive ranking").
    - Ranking is built from loadIndex/loadRolloutCuts + loadCommune (not hardcoded), excludes low_population communes, sorted by rate ascending.
    - Every comparative is qualified with the CEAD-year phrase; national average shown via loadNationalAverage().
  </acceptance_criteria>
  <done>Safest-cities ranking page built from real CEAD data with strictly qualified comparative language; gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: methodology.astro (EDIT-03)</name>
  <files>site/src/pages/methodology.astro</files>
  <read_first>
    - site/src/layouts/EditorialLayout.astro (Plan 03 — Template 3 uses bottom AdSlot only)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (rate-per-100k calc, trend % threshold D-13, low_population default, subregistro caveat — source of truth)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (Template 3 required H2 sections in order; AdSlot bottom-only; meta tags)
    - .planning/REQUIREMENTS.md (EDIT-03)
  </read_first>
  <action>
    methodology.astro (/methodology/): EditorialLayout, lang="en", esPath="/es/metodologia/", title "Methodology — How We Calculate Crime Rates". ~1200 words, first-person institutional "we" (Copywriting Contract). Required H2 sections IN ORDER (UI-SPEC Template 3): "Data source: CEAD", "How the rate is calculated", "Underreporting (cifra negra)", "Trend formula", "Comparison criteria", "What this site does NOT say". The trend-formula section states the Phase-1 % threshold (from 01-CONTEXT). The "What this site does NOT say" section explicitly disclaims absolute safe/dangerous verdicts and probability-of-victimization (Out of Scope). Only a bottom AdSlot (Template 3 — methodology is a credibility page; EditorialLayout default bottom slot suffices; do NOT add a content-position ad). Emit WebPage JSON-LD.
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/forbidden-language.mjs && node -e "const{readFileSync}=require('fs');const h=readFileSync('dist/methodology/index.html','utf8').toLowerCase();for(const s of ['cead','rate is calculated','cifra negra','trend','comparison','does not say']){if(!h.includes(s)){console.error('FAIL: methodology missing section '+s);process.exit(1)}}console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/methodology/index.html` builds and passes forbidden-language.
    - All six required H2 sections present (CEAD source, rate calc, cifra negra/underreporting, trend formula, comparison criteria, what-it-does-NOT-say).
    - Trend-formula section references the Phase-1 % threshold; esPath="/es/metodologia/".
  </acceptance_criteria>
  <done>Methodology page documents all required sections in order, including the explicit non-claims section; gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> MapIsland | Client-only React/Leaflet island (Phase 3); fetches static /data at runtime |
| build -> dist/ | Ranking + methodology prose rendered at build; forbidden-language gate scans output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Repudiation | safest-cities implies absolute safety / definitive ranking | mitigate | Mandatory CEAD-year qualifier on all comparatives; forbidden-language gate catches bare superlatives + "ranking definitivo"/"definitive ranking" |
| T-04-12 | Tampering | AdSlot rendered adjacent to map (D-10 violation) | mitigate | Map-bearing template places ads only in the prose column, never in/around MapIslandBlock |
| T-04-10 | Tampering | SUM-vs-MEAN national rate | mitigate | loadNationalAverage() only |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit empty) |
</threat_model>

<verification>
- `cd site && npm run build && node scripts/validate/all.mjs` — full 9-validator suite green (map.mjs still passes; zero-React regression guard unaffected since editorial map pages are intentionally client:only).
- forbidden-language exits 0 (safest-cities is the critical case).
- Single chained build+validate command (OneDrive desync — Pitfall 6).
- <human-check> (deferred to phase UAT per human_verify_mode=end-of-phase): open /chile-crime-map/ + /santiago-safety-map/ — MapIsland renders, prose surrounds without overlap; /safest-cities-in-chile/ ranking matches data/cead/ with qualified comparatives; /methodology/ accurate vs Phase-1 rules.</human-check>
</verification>

<success_criteria>
- Two map-bearing pages embed MapIsland with surrounding prose, no adjacent ads.
- safest-cities ranking is data-driven, low_population-excluded, strictly qualified.
- methodology has all six required H2 sections including the non-claims section.
- All four pages pass forbidden-language + hreflang + map validators.
</success_criteria>

<output>
Create `.planning/phases/04-editorial-pages-adsense/04-05-SUMMARY.md` when done
</output>
