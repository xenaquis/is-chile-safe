---
phase: 04-editorial-pages-adsense
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - site/src/layouts/EditorialLayout.astro
  - site/src/layouts/LegalLayout.astro
  - site/src/components/DataCallout.astro
  - site/src/components/FAQBlock.astro
  - site/src/components/ContactForm.astro
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, MON-01]
must_haves:
  truths:
    - "Editorial pages can wrap their content in EditorialLayout, which renders the 720px prose column, injects AdSlots, and (only when AdSense is enabled) the CookieConsent banner"
    - "Legal pages can wrap content in LegalLayout, which renders the prose column with NO AdSlot and NO CookieConsent"
    - "A DataCallout renders a sourced value/label/source box; a FAQBlock renders a static Q+A list; a ContactForm renders a static mailto form"
  artifacts:
    - path: "site/src/layouts/EditorialLayout.astro"
      provides: "Editorial wrapper around BaseLayout with prose column + AdSlot + consent (D-16, D-10)"
      contains: "BaseLayout"
    - path: "site/src/layouts/LegalLayout.astro"
      provides: "Legal wrapper around BaseLayout, no ads (D-10, D-16)"
      contains: "BaseLayout"
    - path: "site/src/components/DataCallout.astro"
      provides: "Sourced data callout box (D-01)"
    - path: "site/src/components/FAQBlock.astro"
      provides: "Static FAQ Q+A list for FAQPage pages (D-02)"
    - path: "site/src/components/ContactForm.astro"
      provides: "Static mailto contact form (D-14, EDIT-04)"
      contains: "mailto:"
  key_links:
    - from: "site/src/layouts/EditorialLayout.astro"
      to: "site/src/components/AdSlot.astro"
      via: "AdSlot injection at content + bottom positions"
      pattern: "AdSlot"
    - from: "site/src/layouts/EditorialLayout.astro"
      to: "site/src/layouts/BaseLayout.astro"
      via: "wraps BaseLayout, passes jsonLd through"
      pattern: "BaseLayout"
---

<objective>
Build the layout + content components every editorial and legal page consumes (D-16: WRAP BaseLayout, never replace it). `EditorialLayout` wraps `<BaseLayout>` with a 720px prose column, injects `<AdSlot>` at the content + bottom positions, and renders `<CookieConsent>` only when AdSense is enabled. `LegalLayout` is the same wrapper minus AdSlot and CookieConsent (legal pages are an ad exclusion zone, D-10). `DataCallout` renders a sourced value/label/source box (D-01). `FAQBlock` renders a static open Q+A list (D-02) and exposes a helper to build FAQPage JSON-LD (injected by the page via BaseLayout's `jsonLd` prop). `ContactForm` is the static mailto form (D-14).

Purpose: The contracts every page in Waves 3 builds against — defined first so page executors receive the interfaces, not a scavenger hunt.
Output: EditorialLayout.astro, LegalLayout.astro, DataCallout.astro, FAQBlock.astro, ContactForm.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-editorial-pages-adsense/04-CONTEXT.md
@.planning/phases/04-editorial-pages-adsense/04-RESEARCH.md
@.planning/phases/04-editorial-pages-adsense/04-PATTERNS.md
@.planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md
@.planning/phases/04-editorial-pages-adsense/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (consumed here): -->
AdSlot.astro Props: { slot: 'content' | 'bottom'; publisherId?: string }   // env-gated, CLS-safe
CookieConsent.astro Props: { locale: 'en' | 'es' }                         // render only when AdSense enabled

<!-- BaseLayout.astro (wrapped here): -->
BaseLayout Props: { lang:'en'|'es'; title; description; enPath; esPath; jsonLd?:object|null }
Renders: <PageHeader/> <main class="page-content"><slot/></main> <PageFooter/>

<!-- StatCard.astro (DataCallout analog): -->
StatCard Props: { label; value:string|number; sublabel? }  // .stat-card / .stat-value / .stat-label tokens

<!-- BaseLayout JSON-LD injection (FAQBlock target — page passes jsonLd up): -->
{jsonLd !== null && (<script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />)}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EditorialLayout.astro + LegalLayout.astro</name>
  <files>site/src/layouts/EditorialLayout.astro, site/src/layouts/LegalLayout.astro</files>
  <read_first>
    - site/src/layouts/BaseLayout.astro (Props + slot structure — these layouts WRAP it, do not duplicate the html/head)
    - site/src/components/AdSlot.astro (Plan 02 output — Props slot:'content'|'bottom')
    - site/src/components/CookieConsent.astro (Plan 02 output — Props locale)
    - .planning/phases/04-editorial-pages-adsense/04-PATTERNS.md (EditorialLayout/LegalLayout section — wraps BaseLayout, 720px column)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (Template 1/2/3 section order; AdSlot placement rules; editorial prose 720px exception)
  </read_first>
  <action>
    EditorialLayout.astro: Props `{ lang:'en'|'es'; title; description; enPath; esPath; jsonLd?:object|null }`. Import BaseLayout, AdSlot, CookieConsent. Render `<BaseLayout {...props} jsonLd={jsonLd}>` and inside it a `<div class="editorial-prose">` (max-width 720px, centered) wrapping a default `<slot />` for the page body. Provide TWO named slots so pages control AdSlot placement: a `content` AdSlot rendered after a `<slot name="after-first-section" />` boundary and a `bottom` AdSlot before the prose column closes. Simplest contract: render the page body via the default `<slot />`, then a `<AdSlot slot="bottom" />` at the end of the prose column; expose a `<slot name="ad-content" />` marker the page can ignore OR place a single `<AdSlot slot="content" />` after the default slot — pick the named-slot approach so pages with no content-position ad (methodology) can omit it. Compute `const adsenseEnabled = import.meta.env.ADSENSE_ENABLED === 'true'` and render `{adsenseEnabled && <CookieConsent locale={lang} />}` once at the end. Scoped style: `.editorial-prose { max-width:720px; margin:0 auto; }` plus mobile `<640px` full-width with `var(--md)` side padding. Pass `jsonLd` straight through (FAQ pages supply it).

    LegalLayout.astro: identical wrapper but NO AdSlot import/render and NO CookieConsent (legal pages are an ad+consent exclusion zone, D-10). Same Props and `.editorial-prose` column. (Contact page uses LegalLayout per UI-SPEC Template 5.)
  </action>
  <verify>
    <automated>cd site; foreach($f in 'src/layouts/EditorialLayout.astro','src/layouts/LegalLayout.astro'){ if(-not (Select-String -Path $f -Pattern 'BaseLayout' -Quiet)){ Write-Error "FAIL $f missing BaseLayout"; exit 1 } }; if(Select-String -Path src/layouts/LegalLayout.astro -Pattern 'AdSlot' -Quiet){ Write-Error 'FAIL LegalLayout must not import AdSlot'; exit 1 }; Write-Host PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both layouts import and wrap `BaseLayout` (contain `BaseLayout`); neither declares its own `<html>`/`<head>`.
    - EditorialLayout imports AdSlot and renders a `bottom` AdSlot; renders CookieConsent only under the `ADSENSE_ENABLED === 'true'` gate.
    - LegalLayout contains NO `AdSlot` and NO `CookieConsent` references.
    - Both expose a `.editorial-prose` 720px column and pass `jsonLd` through to BaseLayout.
  </acceptance_criteria>
  <done>EditorialLayout + LegalLayout wrap BaseLayout; editorial injects ads+consent (gated), legal injects neither.</done>
</task>

<task type="auto">
  <name>Task 2: Create DataCallout.astro + FAQBlock.astro</name>
  <files>site/src/components/DataCallout.astro, site/src/components/FAQBlock.astro</files>
  <read_first>
    - site/src/components/StatCard.astro (value/label/sublabel + CSS-token style — DataCallout analog)
    - site/src/layouts/BaseLayout.astro (jsonLd prop injection pattern — FAQBlock target)
    - .planning/phases/04-editorial-pages-adsense/04-PATTERNS.md (DataCallout + FAQBlock sections — Props, JSON-LD ownership at page level)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (Template 1 DataCallout + FAQBlock specs; Pitfall 8 plain-text answers)
    - .planning/phases/04-editorial-pages-adsense/04-RESEARCH.md (Pattern 2 FAQPage JSON-LD; Pitfall 8)
  </read_first>
  <action>
    DataCallout.astro: Props `{ value:string; label:string; source:string; locale:'en'|'es' }`. Render `.data-callout` box: `.callout-value` (28px/700, `var(--primary)`), `.callout-label` (14px/700, `var(--muted)`), `.callout-source` (14px/400, `var(--muted)`). Style with `border-left: 4px solid var(--primary)`, `var(--card)` bg, `var(--radius)`, `var(--md)` padding, `var(--lg) 0` margin. No client directive.

    FAQBlock.astro: Props `{ items: { q:string; a:string }[]; locale:'en'|'es' }`. Render each item as `<div class="faq-item"><h2 class="faq-q">{q}</h2><p class="faq-a">{a}</p></div>` separated by `1px solid var(--line)`; questions 20px/700, answers 16px/400. Static open list — no JS accordion. FAQBlock does NOT inject JSON-LD itself (single injection point is BaseLayout). Export a small helper (e.g. `buildFaqJsonLd(items)`) from the component frontmatter OR document in a leading comment that the page constructs `{ "@context":"https://schema.org","@type":"FAQPage","mainEntity": items.map(...) }` and passes it to the layout's `jsonLd` prop. Answer text MUST be plain text — no HTML in the JSON-LD `text` field (Pitfall 8).
  </action>
  <verify>
    <automated>cd site; if((Test-Path src/components/DataCallout.astro) -and (Test-Path src/components/FAQBlock.astro) -and (Select-String -Path src/components/DataCallout.astro -Pattern 'callout-source' -Quiet)){Write-Host PASS}else{Write-Error FAIL;exit 1}</automated>
  </verify>
  <acceptance_criteria>
    - DataCallout.astro renders value/label/source with a `4px solid var(--primary)` left border; Props are `value`,`label`,`source`,`locale`.
    - FAQBlock.astro renders a static `<h2>`/`<p>` Q+A list from `items`; contains no `client:` directive and does not inject its own `<script type="application/ld+json">`.
    - FAQBlock documents (comment or exported helper) the FAQPage JSON-LD shape the page must pass to `jsonLd`.
  </acceptance_criteria>
  <done>DataCallout + FAQBlock built; JSON-LD ownership stays at the page→BaseLayout boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Create ContactForm.astro (static mailto)</name>
  <files>site/src/components/ContactForm.astro</files>
  <read_first>
    - site/src/components/StatCard.astro (Astro Props + scoped CSS-token style analog)
    - .planning/phases/04-editorial-pages-adsense/04-UI-SPEC.md (## Contact Form Specification — fields, labels, styles, mechanism)
    - .planning/phases/04-editorial-pages-adsense/04-PATTERNS.md (ContactForm.astro section — mailto mechanism, v2 comment)
    - .planning/phases/04-editorial-pages-adsense/04-RESEARCH.md (Security Domain V5 — required attrs, email-harvesting tradeoff)
  </read_first>
  <action>
    ContactForm.astro: Props `{ locale:'en'|'es' }`. Render a static form `action="mailto:xenaquis@gmail.com" method="POST" enctype="text/plain"` (D-14). Fields per UI-SPEC: Name (`<input type="text">`, required), Email (`<input type="email">`, required), Message (`<textarea rows="5">`, required), Submit button. Locale-correct labels: EN "Your name"/"Email address"/"Message"/"Send message"; ES "Tu nombre"/"Correo electrónico"/"Mensaje"/"Enviar mensaje". All three inputs carry the `required` attribute (V5 client-side validation — no backend). Scoped styles per UI-SPEC: label 14px/700 `var(--ink)` block + `var(--xs)` bottom margin; inputs 16px/400, `var(--card)` bg, `1px solid var(--line)`, `calc(var(--radius)/2)`, `var(--sm) var(--md)` padding, width 100% max 480px; focus `2px solid var(--primary)` + `outline:none`; submit `var(--primary)` bg / `var(--card)` text, padding `var(--sm) var(--xl)`, min-height 44px. Add a leading HTML comment: `<!-- Phase-4 MVP: mailto link. Replace with a form service endpoint in v2. -->` documenting the email-harvesting tradeoff and v2 upgrade path.
  </action>
  <verify>
    <automated>cd site; if((Select-String -Path src/components/ContactForm.astro -Pattern 'mailto:xenaquis@gmail.com' -Quiet) -and ((Select-String -Path src/components/ContactForm.astro -Pattern 'required' -AllMatches).Matches.Count -ge 3)){Write-Host PASS}else{Write-Error FAIL;exit 1}</automated>
  </verify>
  <acceptance_criteria>
    - ContactForm.astro form uses `action="mailto:xenaquis@gmail.com"` with `enctype="text/plain"`.
    - Name, Email, Message fields each carry `required`; submit button present.
    - Leading comment documents the mailto MVP + v2 upgrade path.
    - No `client:` directive; no backend endpoint.
  </acceptance_criteria>
  <done>Static mailto ContactForm built with required fields and documented MVP tradeoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> mail client | ContactForm mailto submission handled entirely by the browser; no server processing |
| page -> BaseLayout jsonLd | FAQ JSON-LD object stringified via set:html (XSS-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | FAQPage JSON-LD injection | mitigate | JSON-LD constructed as a plain object + `JSON.stringify` via `set:html` in BaseLayout (existing XSS-safe path); plain-text answers only (Pitfall 8) |
| T-04-07 | Info Disclosure | contact email visible in mailto HTML | accept | mailto exposes the address by design; acceptable for MVP volume; documented v2 mitigation (RESEARCH Security Domain V5) |
| T-04-08 | Tampering | unvalidated contact input | mitigate | `required` attributes on all fields; no server-side processing means no injection sink (V5) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit empty) |
</threat_model>

<verification>
- `cd site && npm run build && node scripts/validate/all.mjs` — full suite green; new components compile (used by Wave 3 pages but importing them must not break the build).
- LegalLayout contains no AdSlot/CookieConsent references.
- Single chained build+validate command (OneDrive desync — Pitfall 6).
</verification>

<success_criteria>
- EditorialLayout + LegalLayout wrap BaseLayout; editorial injects gated ads+consent, legal injects neither.
- DataCallout, FAQBlock, ContactForm built per UI-SPEC with correct tokens and Props.
- FAQPage JSON-LD ownership stays at page→BaseLayout; ContactForm uses mailto with required fields.
</success_criteria>

<output>
Create `.planning/phases/04-editorial-pages-adsense/04-03-SUMMARY.md` when done
</output>
