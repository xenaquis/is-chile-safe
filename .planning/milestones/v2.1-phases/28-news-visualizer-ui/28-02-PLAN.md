---
phase: 28-news-visualizer-ui
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - site/src/pages/news.astro
  - site/src/pages/es/noticias.astro
  - site/.gitignore
autonomous: true
requirements: [NEWSUI-01, NEWSUI-02, NEWSUI-03, NEWSUI-04, NEWSUI-05, NEWSUI-06, NEWSUI-07]

must_haves:
  truths:
    - "A user on /news/ or /es/noticias/ sees a filter bar (time/region/family + comuna search) with per-option counts, and can filter without a page reload"
    - "With JavaScript disabled, every incident in current.json remains visible in the static HTML on both pages (NEWSUI-02) — filtering is additive, never a rendering precondition"
    - "Filter state round-trips through ?family=&region=&window=&q= and survives reload via history.replaceState"
    - "A zero-result filter combination shows the bilingual empty state and collapses empty month sections, all via the hidden attribute exclusively — never style.display"
    - "Every classifier/RSS-derived string (family, title, outlet) is written via textContent, never innerHTML; the #news-facets payload (now including anchorDate) stays routed through escapeForInlineScript"
    - "No cluster/source-count badge markup is added anywhere (NEWSUI-05 NO-GO branch — faceting only)"
    - "The result-count and window-option copy is fully substituted server-side (F-32) — the indexed HTML never contains a literal {n} or {date}"
    - "The anchor-date label is formatted with timeZone: 'UTC' so Chilean readers see the correct calendar date (F-32)"
  artifacts:
    - path: "site/src/pages/news.astro"
      provides: "data-* attributes on .news-card/.news-month-section, UI-SPEC filter-bar markup (count in a dedicated span, never the label), facetsProjection.anchorDate, inline <script> importing newsFilterLogic"
    - path: "site/src/pages/es/noticias.astro"
      provides: "identical structure to news.astro, ES copy sourced from i18n.ts, same hide/show mechanism"
  key_links:
    - from: "site/src/pages/news.astro <script>"
      to: "site/src/lib/newsFilterLogic.ts"
      via: "ES module import (bare, non-is:inline Astro <script>)"
      pattern: "import \\{[^}]*\\} from '\\.\\./lib/newsFilterLogic'"
    - from: "site/src/pages/es/noticias.astro <script>"
      to: "site/src/lib/newsFilterLogic.ts"
      via: "ES module import (bare, non-is:inline Astro <script>)"
      pattern: "import \\{[^}]*\\} from '\\.\\./\\.\\./lib/newsFilterLogic'"
    - from: ".news-card visibility"
      to: "#news-result-count / .news-month-section collapse"
      via: "the single `!card.hidden` predicate, hidden attribute only"
      pattern: "\\.hidden\\s*=\\s*!"
---
> **Fable review: APPROVED 2026-07-30.** Gates passed: Opus plan-check (6 CRITICAL / 6 HIGH / 6 MEDIUM / 3 LOW) → Opus premortem (11 failure modes, 16 amendments) → Sonnet fix cycle 1 (A1..A19) → Fable inline fix cycle 2 (F-34: cycle 1 reintroduced exit-code laundering in the phase-close gate) → independent Opus re-review → Fable inline fix of its 2 remaining blockers (both "cannot pass against a correct implementation": the `{n}` token scan vs. the required `data-result-count-template` carrier, and a bare-token `set:html` count that is RED today because of a Phase 27 comment).
>
> Every `<automated>` gate in this plan set was executed or synthetically exercised by the orchestrator: the astro-check assertion exits 0 on the current tree and 1 against a 5-error log; the 28-03 chain propagates a failure from every stage downstream of its single deliberate `;`; the i18n parity script is correctly RED today; `facets.mjs` exits 0; pytest is 344 passed / 1 skipped / 1 xfailed.
>
> Accepted and recorded rather than fixed (fix cycles exhausted; see STATE.md F-35): N1 `?region=` namespace note for Phase 30, N2 freshness may redden the gate mid-phase (F-19/F-33/F-34 govern — stop and report, never remediate), N3 the manual browser pass runs at verification, N4 the "All dates" count span is not recomputed client-side (cosmetic), N5..N9 minor.



<objective>
Wire the UI-SPEC's filter bar onto both `/news/` and `/es/noticias/`: add `data-*` attributes to already-rendered `.news-card`/`.news-month-section` elements, render the `<details>`/`<fieldset>` filter markup (count in its own `<span class="filter-count">`, never written into the option `<label>`'s `textContent` — see Element Structure amendment), publish `anchorDate` through the existing `escapeForInlineScript` call, and add each page's own inline `<script>` (no `client:` directive) that `import`s `newsFilterLogic.ts` (Plan 28-01) to drive filtering, per-option F-28 self-exclusion counts, the empty state, and `history.replaceState` URL sync. Cross-filtered counts are read from each card's `data-*` attributes (F-29 — `facetKeys` is deliberately NOT serialized).

Purpose: deliver NEWSUI-01 through -04, -06, -07 as progressive enhancement over the already-shipped, already-indexable zero-JS pages — the filter script must never be a precondition for content visibility.
Output: both `.astro` pages updated with markup + wired client script; zero new pages, zero new dependencies, zero React island.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-news-visualizer-ui/28-UI-SPEC.md
@.planning/phases/28-news-visualizer-ui/28-RESEARCH.md
@.planning/phases/28-news-visualizer-ui/28-PATTERNS.md
@.planning/phases/28-news-visualizer-ui/28-01-SUMMARY.md
@site/src/pages/news.astro
@site/src/pages/es/noticias.astro
@site/src/lib/newsFilterLogic.ts
@site/src/pages/communes/index.astro
</context>

<interfaces>
<!-- What Plan 28-01 already shipped — use these exact names, do not re-derive. -->

From `site/src/lib/newsFilterLogic.ts` (Plan 28-01):
`WINDOW_7D_WIDTH_DAYS`, `WINDOW_30D_WIDTH_DAYS`, `FILTER_LOGIC_MARKER`, `WindowValue`, `FilterParams`, `norm(s)`, `parseFilterParams(search)`, `serializeFilterParams(params)`, `computeNewestDate(dates)`, `matchesWindow(dateStr, newestDate, window)`, `CardFilterState`, `cardMatchesFilters(card, active, newestDate, excludeDimension?)`, `computeFacetCounts(cards, active, newestDate, dimension, keys)`.

From `site/src/lib/newsFacets.ts` (Plan 28-01 addition):
`NewsFacetIndex.anchorDate: string | null` — already returned by `computeNewsFacets()`.

From `site/src/config/i18n.ts` (Plan 28-01 addition): 15 `news_*` keys — exact names in `28-01-PLAN.md`'s Task 3. Note `news_window_7d_label`/`news_window_30d_label` (renamed, no `{n}` token — the count lives in a separate `<span class="filter-count">`).

From `site/src/lib/data.ts` (existing, already imported by both pages): `loadIndex()` returns `CommuneMeta[]` with `{cut, name, slug, region_id, population, low_population}` — **no region name field**. `loadRegion(regionId)` (`data.ts:171`) returns `RegionData` with a `.name` field, reading from `data/regions/{regionId}.json`; `regionFileId(regionId)` (`data.ts:133`) normalizes the id before that lookup. Region names come from `loadRegion(regionFileId(regionId)).name`, NOT from `CommuneMeta`.

From `site/src/config/i18n.ts` (existing): `regionNameEs(name: string): string` (`i18n.ts:666`) — wraps a bare region name ("Metropolitana") into the ES-conventional form ("Región Metropolitana"). Used by every other ES page; the ES news page must use it too.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire /news/ (news.astro) — data attributes, filter-bar markup, anchorDate, client script</name>
  <files>site/src/pages/news.astro, site/.gitignore</files>
  <read_first>
    site/src/pages/news.astro (full file, current state — frontmatter lines 1-144, template 145-211, style 213-302)
    site/src/lib/newsFilterLogic.ts (Plan 28-01 output — exact export names/signatures, including `FILTER_LOGIC_MARKER`)
    .planning/phases/28-news-visualizer-ui/28-UI-SPEC.md § Element Structure & Interaction Contract, including the "AMENDED 2026-07-30" block (the count slot is a dedicated `<span class="filter-count">`, never the option `<label>`'s own text; the `hidden`-only rule; F-29's data-* count source; F-32's server-side substitution + UTC formatting)
    site/src/pages/communes/index.astro lines 170-307 (structural pieces to port: read `URLSearchParams` synchronously before first paint, `?q=` pre-population via `input.value` only never innerHTML, highlight via TRUSTED attribute slicing — do NOT copy `style.display`, do NOT copy the inline `norm()` re-declaration)
    site/src/lib/familyDefs.ts (FAMILY_LABELS_EN keys, for family-filter checkbox labels)
    site/src/lib/data.ts lines 125-179 (`regionFileId`, `loadRegion` — the only region-name lookup path; `CommuneMeta` has no region name)
  </read_first>
  <action>
    Frontmatter changes: (1) build `const cutToRegion = new Map(loadIndex().map(c => [c.cut, c.region_id]))` (mirrors newsFacets.ts's internal map — bake the result, not the logic, into markup per RESEARCH.md Don't-Hand-Roll); (2) build `const regionNames = new Map(facets.byRegion.map(r => [r.regionId, loadRegion(regionFileId(r.regionId)).name]))` (at most 16 lookups — `loadIndex()`/`CommuneMeta` has no region name field, so this is the only correct source; import `loadRegion`, `regionFileId` alongside the existing `loadIndex` import); (3) add `anchorDate: facets.anchorDate` as a new key in the existing `facetsProjection` object literal (line ~127-136) — do NOT add a second `set:html` call, it is automatically covered by the existing `escapeForInlineScript(JSON.stringify(facetsProjection))` call; per F-29, do NOT add `facetKeys` to this object (the UI-SPEC line mandating it is superseded — cross-filtered counts come from card `data-*` attributes instead, see below); (4) import `FAMILY_LABELS_EN` (already present), `norm` and `FILTER_LOGIC_MARKER` from `../lib/newsFilterLogic`, and `EN_STRINGS as t` from `../config/i18n.ts` matching `RankingTableEnhancer.astro:38`'s exact import convention (`import { EN_STRINGS, ES_STRINGS } from '../config/i18n.ts'`, using only `EN_STRINGS` here) — this page currently has no `i18n.ts` import.

    Compute server-side substitution values in frontmatter (F-32 — these MUST be baked into the rendered HTML, never left as `{n}`/`{date}` for the client to fill in): `const anchorLabel = facets.anchorDate ? new Date(facets.anchorDate + 'T00:00:00Z').toLocaleDateString('en-US', { timeZone: 'UTC', month: 'short', day: 'numeric' }) : '—'` (mandatory `timeZone: 'UTC'` — without it the label is off by one day for any Chilean reader, VERIFIED); `const resultCountText = t.news_result_count.replace('{n}', String(incidents.length))`; `const windowLatestText = t.news_window_latest.replace('{date}', anchorLabel)`. Never reuse the existing `formattedDate` (it formats `generated`, the pipeline timestamp, not the newest incident's date — UI-SPEC explicit correction).

    Template changes: on each `.news-card` `<article>` add `data-family={incident.family ?? ''}`, `data-region-id={cutToRegion.get(String(incident.cut)) ?? ''}`, `data-year-month={yearMonth}` (reuse the existing loop's `yearMonth`, do not recompute), `data-commune-norm={communeInfo ? norm(communeInfo.name) : ''}` (server-side call to the imported pure `norm()` — safe at build time), `data-date={incident.date}` — keep `class="news-card"` present in the tag but do not assume its attribute position (Plan 28-03's card-count assertion is order-independent, so any position is fine). On the wrapping `<section class="news-month-section">` add `id={'month-' + yearMonth}`.

    Insert the UI-SPEC's filter-bar `<details id="news-filters" class="news-filter-bar" open>` block between the existing intro `<section>` and the month-grouped list, using the AMENDED markup shape: every option is `<label class="filter-chip"><input …> <span class="filter-opt-label">TEXT</span> <span class="filter-count">N</span></label>` — the count is ALWAYS its own `<span class="filter-count">`, never appended into the label text and never written via `textContent`/`innerHTML` on the `<label>`, `<fieldset>`, or `.filter-chip` itself (destroys the nested `<input>`). Window group: four options — `value="today"` labeled `{windowLatestText}` (no separate count span content needed beyond the marginal `today` count from `facets.byWindow.today.length`), `value="7d"` labeled `{t.news_window_7d_label}` with count `{facets.byWindow.sevenDay.length}` (297 today), `value="30d"` labeled `{t.news_window_30d_label}` with count `{facets.byWindow.thirtyDay.length}` (1215 today), `value="" checked` labeled `{t.news_window_all}` with count `{incidents.length}` (1215 today) — **all four options carry a count span**. Region group: one checkbox per `facets.byRegion` entry, label text `regionNames.get(r.regionId)`, count span `{r.count}`. Family group: one checkbox per `facets.byFamily` entry, label text via `familyLabel()`/`FAMILY_LABELS_EN`, count span `{f.count}`. Comuna search `<input type="search" id="news-comuna-q">`. Result-count span: `<span id="news-result-count" role="status" aria-live="polite">{resultCountText}</span>` (already substituted, never a raw `{n}` token). Clear-filters button `id="news-clear-filters"`. Add the `<div id="news-empty" hidden>` block (heading text + body text, NOT an `<h2>`/`<h1>`) directly after the filter bar, containing `news_empty_heading`/`news_empty_body` as plain text.

    Add a second inline `<script>` (no `client:` directive, no `is:inline`) that: imports `{ parseFilterParams, serializeFilterParams, norm, cardMatchesFilters, computeFacetCounts, matchesWindow, FILTER_LOGIC_MARKER }` from `'../lib/newsFilterLogic'`; on load, parses `#news-facets`'s JSON (already inert, now including `anchorDate`) to get `anchorDate`; parses `location.search` via `parseFilterParams`; queries all `.news-card` and reads their `data-*` into `CardFilterState` objects (this — not a serialized `facetKeys` array — is the F-29 count source); pre-populates the filter-bar controls (checked radios/checkboxes, `input.value` for comuna search — value assignment only, never innerHTML, per T-12-01) from the parsed params; sets `document.getElementById('news-filters')?.setAttribute('data-filter-logic', FILTER_LOGIC_MARKER)` on first run (a real runtime reference to the marker so tree-shaking cannot drop it, and a browser-inspectable signal the module loaded — Plan 28-03's assertion 17 depends on this literal surviving minification); defines `applyFilters()` which, for each card, sets `card.hidden = !cardMatchesFilters(...)` (never `style.display`), then for each `.news-month-section` sets `section.hidden = (visible child count === 0)` using the `!card.hidden` predicate exclusively, sets `#news-empty`'s `hidden` based on total visible count, recomputes each filter-group's option counts via `computeFacetCounts` (self-exclusion, F-28) and writes them into each option's `span.filter-count` via `textContent` (never the `<label>`'s own `textContent`/`innerHTML` — family/region labels are build-time-known strings, not classifier-derived, but keep the discipline uniform regardless), updates `#news-result-count`'s `textContent` from a freshly-computed string (`t`'s `news_result_count` template with `{n}` replaced by the current visible count — this is a CLIENT-SIDE re-substitution on every filter change; it does not affect the server-rendered initial value which is already substituted per F-32), and calls `history.replaceState(null, '', '?' + serializeFilterParams(active))` (never `pushState`); wires `input`/`change` listeners on every control and a `click` listener on `#news-clear-filters` that resets `active` to the empty `FilterParams` and re-applies; calls `applyFilters()` once synchronously on load (before first paint content is already correct because `<details open>` and unfiltered visibility are the server-rendered default — this call only narrows based on any `?`-params already in the URL). The `t.news_result_count` object is not directly available client-side (it's an Astro frontmatter import) — pass the two template strings needed (`news_result_count`, and nothing else requiring `{date}` re-formatting client-side, since window labels no longer carry `{n}`) into the script via a small `data-*` attribute or an inline JSON constant assigned in the script tag itself (e.g. `const RESULT_COUNT_TEMPLATE = ${JSON.stringify(t.news_result_count)};` emitted via Astro's `{expr}` interpolation is NOT safe inside `<script>` per the known Astro literal-interpolation pitfall — instead read it from a `data-result-count-template` attribute on `#news-result-count`, set server-side, and read via `.dataset.resultCountTemplate` client-side).

    Do not add a "view all {family}" link anywhere in this markup (Pitfall 2 — `vida`/`homicide` slug collision; filtering here is query-param only, never a route).

    Add `site/.astro-check.log` to `site/.gitignore` (used by this plan's astro-check verify step below).
  </action>
  <verify>
    <automated>cd site && npx astro check > .astro-check.log 2>&1; node -e "const s=require('fs').readFileSync('.astro-check.log','utf8').replace(/\[[0-9;]*m/g,''); const m=s.match(/^- (\d+) errors?$/m); if(!m) { console.error('FAIL: astro check summary line not found'); process.exit(1); } const n=Number(m[1]); if(n!==4){ console.error('FAIL: astro check reports '+n+' errors, baseline is 4'); process.exit(1); } console.log('astro check at 4-error baseline');" && npm run build && npm test</automated>
  </verify>
  <done>news.astro type-checks at the pre-existing 4-error baseline (all 4 in `src/components/ComparatorPairsLinks.astro:28`, `ts(7031)` implicit-any on `cutA`/`cutB`/`nameA`/`nameB` — VERIFIED; NOT in `scripts/validate/*.mjs`, which produces only `ts(6133)` hints; a new error anywhere else is NOT baseline); `npm run build` and `npm test` (vitest) both succeed; the file contains the exact import string `from '../lib/newsFilterLogic'`; every `.news-card` carries all 5 new data-* attributes; the filter-bar (count-in-its-own-span shape), empty-state, and result-count markup exist with the exact i18n key references from Plan 28-01, and the result count / window-latest label are server-substituted (no literal `{n}`/`{date}` in the rendered template).</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the wiring onto /es/noticias/ (es/noticias.astro) — ES copy, identical mechanism</name>
  <files>site/src/pages/es/noticias.astro</files>
  <read_first>
    site/src/pages/es/noticias.astro (full file, current state)
    site/src/pages/news.astro (post-Task-1 state, in this same plan — the structural template to mirror, located by SEMANTIC ANCHOR, not line number — see the drift note below)
    site/src/lib/newsFilterLogic.ts (Plan 28-01 — same exports, imported via `'../../lib/newsFilterLogic'` relative path from this file's location)
    .planning/phases/28-news-visualizer-ui/28-UI-SPEC.md § Copywriting Contract (ES column) and § Element Structure
    site/src/config/i18n.ts line 666 (`regionNameEs(name: string): string` — wraps a bare region name into the ES form, e.g. "Región Metropolitana", "Región del Maule")
  </read_first>
  <action>
    **Structural drift note (verified today):** `es/noticias.astro` is NOT byte-identical to `news.astro` pre-Task-1 — the `formattedDate` block, the `<style>` block's rule ordering, and a `slugToCommune` comment all sit at different line numbers between the two files. Locate every insertion point by its SEMANTIC ANCHOR (the `facetsProjection` object literal; the intro `<section>`'s closing tag; the first `<section class="news-month-section">`; the `<article class="news-card">` opening tag), never by copying line numbers from Task 1.

    Apply every change from Task 1 to this file with FOUR differences (not three): (1) import path is `'../../lib/newsFilterLogic'` and `'../../lib/data'`/`'../../lib/familyDefs'` (one extra `../` level, matching this file's existing imports); the i18n import is `ES_STRINGS as t` from `'../../config/i18n.ts'`. (2) all visible copy comes from `ES_STRINGS` (`t.news_*` keys, ES values) and `FAMILY_LABELS_ES`. (3) the anchor-date format call uses `toLocaleDateString('es-CL', { timeZone: 'UTC', month: 'short', day: 'numeric' })` per UI-SPEC — `timeZone: 'UTC'` is mandatory here too (without it, `new Date('2026-07-27T00:00:00Z').toLocaleDateString('es-CL',{month:'short',day:'numeric'})` renders "26 jul" in Chile, VERIFIED). (4) region checkbox labels are wrapped in `regionNameEs(loadRegion(regionFileId(r.regionId)).name)` (`site/src/config/i18n.ts:666`), yielding "Región Metropolitana" / "Región del Maule" / "Región de Valparaíso" — matching every other ES page's region-name convention; without this the checkbox would read bare "Metropolitana" while the rest of the ES site says "Región Metropolitana".

    The `?family=`/`?region=`/`?window=`/`?q=` param NAMES and VALUES (family keys, region ids) stay identical to the EN page — only the rendered labels/copy differ, never the query contract (a shared link between locales must filter identically). The `data-*` attribute names, the `hidden`-only mechanism, `history.replaceState` (never `pushState`), the `escapeForInlineScript`-wrapped `facetsProjection.anchorDate` addition (no `facetKeys`, per F-29), and the `FILTER_LOGIC_MARKER` runtime reference are structurally identical to Task 1. After completing this task, diff `news.astro` and `es/noticias.astro` and confirm the only remaining differences are import depth, locale strings, `FAMILY_LABELS_ES`, `regionNameEs`, the `'es-CL'` formatter, and pre-existing comment/order drift (not new structural divergence) — record that diff summary in `28-02-SUMMARY.md`.
  </action>
  <verify>
    <automated>cd site && npx astro check > .astro-check.log 2>&1; node -e "const s=require('fs').readFileSync('.astro-check.log','utf8').replace(/\[[0-9;]*m/g,''); const m=s.match(/^- (\d+) errors?$/m); if(!m) { console.error('FAIL: astro check summary line not found'); process.exit(1); } const n=Number(m[1]); if(n!==4){ console.error('FAIL: astro check reports '+n+' errors, baseline is 4'); process.exit(1); } console.log('astro check at 4-error baseline');" && npm run build && npm test</automated>
  </verify>
  <done>es/noticias.astro mirrors news.astro's structure exactly (same data-* attributes, same hidden mechanism, same import target adjusted for path depth, same FILTER_LOGIC_MARKER reference); all filter-bar copy is ES via ES_STRINGS/FAMILY_LABELS_ES/regionNameEs; astro check baseline unchanged at 4 errors; `npm run build` and `npm test` succeed; the EN/ES structural diff is recorded in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → client `<script>` on both pages | Untrusted, user/attacker-editable input parsed by `parseFilterParams` (Plan 28-01) then used only for DOM attribute comparison, never string interpolation into HTML |
| `#news-facets` JSON node → client `<script>` | Build-time data, LLM-classifier-derived strings inside it (`byFamily[].key`) already escaped at source via `escapeForInlineScript`; this plan adds `anchorDate` to the SAME object, automatically covered — `facetKeys` is deliberately NOT added (F-29) |
| Comuna search input (`?q=`, `#news-comuna-q`) → DOM | User-typed/URL-supplied text; assigned to `input.value` only (never innerHTML); matched via `norm()`/`.includes()` against a TRUSTED build-time `data-commune-norm` attribute, never used to construct HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering / Elevation of Privilege | `#news-facets` `set:html` node, now including `anchorDate` | mitigate | `anchorDate` is a plain build-time date string (not LLM-derived), but folds into the same `facetsProjection` object already passed through `escapeForInlineScript(JSON.stringify(...))` — no second unescaped `set:html` call is introduced (Pitfall 4); `facetKeys` is deliberately excluded (F-29), further shrinking this payload's surface |
| T-28-05 | Tampering | Reflected `?q=` content in the comuna typeahead | mitigate | `input.value = initialQ` assignment only (T-12-01); match/highlight logic (if any) slices the TRUSTED `data-commune-norm`/`data-name`-equivalent attribute, never injects the raw query string into `innerHTML` |
| T-28-06 | Tampering | Hostile/malformed `family`/`region`/`window` query-param values | mitigate | `parseFilterParams` (Plan 28-01) degrades invalid `window` to `''`; `cardMatchesFilters` compares against `.includes()` on a fixed set of DOM-known values — an unrecognized family/region key simply matches zero cards (empty result → empty state), never throws, never produces a broken page |
| T-28-07 | Tampering / XSS | `family`/`title_en`/`title_es`/`outlet` (classifier/RSS-derived) rendered anywhere new by this plan's script | mitigate | This plan's client script never writes these values via `innerHTML` — filter-option labels for family use build-time-known `FAMILY_LABELS_EN`/`ES` strings (not the raw classifier output), and the result-count/empty-state text never echoes incident content; per-option counts are written into `span.filter-count`'s `textContent` only, never a `<label>`'s `textContent`/`innerHTML`. Existing card markup (title/outlet) is untouched by this plan. |
</threat_model>

<verification>
`cd site && npm run build && npm test` — build succeeds (both pages render), vitest suite passes. `npx astro check` stays at the pre-existing 4-error/0-warning baseline (verified via the log-file + node-assertion pattern in each task's verify block, never a piped `grep` on colorized live output). Manual interactive verification (typeahead, filter combinations, URL round-trip, empty state, JS-disabled superset) is deferred to Plan 28-03's phase-close gate and is explicitly manual per 28-VALIDATION.md — this plan's automated verification covers markup/wiring presence, type-check cleanliness, build success, and vitest passing, not the live interaction.
</verification>

<success_criteria>
- Both `.astro` pages carry the UI-SPEC filter-bar markup (count in its own `<span class="filter-count">`), the 5 new `data-*` attributes per card, the `id`-tagged month sections, and the empty-state node — structurally identical to each other modulo locale.
- Both pages' inline `<script>` blocks contain a real `import { ... } from '../(../)lib/newsFilterLogic'` statement — no re-declared filter logic, no copy-pasted `norm()` — and set `data-filter-logic` to `FILTER_LOGIC_MARKER` at runtime.
- `facetsProjection.anchorDate` is present on both pages, routed through the existing `escapeForInlineScript` call, no second `set:html` site added, `facetKeys` deliberately absent (F-29).
- The result-count and window-latest copy are fully server-substituted — no literal `{n}`/`{date}` ships in the indexed HTML (F-32) — and every anchor-date `toLocaleDateString` call passes `timeZone: 'UTC'` (F-32).
- ES region labels render through `regionNameEs()`.
- `astro check` stays at the 4-error/0-warning baseline; `npm run build` and `npm test` both succeed.
- Zero `client:` directives introduced anywhere.
</success_criteria>

<output>
Create `.planning/phases/28-news-visualizer-ui/28-02-SUMMARY.md` when done
</output>
