---
phase: 28-news-visualizer-ui
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - site/scripts/validate/facets.mjs
autonomous: true
requirements: [NEWSUI-01, NEWSUI-02, NEWSUI-03, NEWSUI-04, NEWSUI-05, NEWSUI-06, NEWSUI-07]

must_haves:
  truths:
    - "facets.mjs (still validator #16 — extended, not replaced, so no hardcoded validator count anywhere needs correcting) proves, against the real built dist/ output, every falsifiable claim in 28-VALIDATION.md's Wave 0 table, including the 4 rows Plan 28-01/02's earlier draft left uncovered (filter-control presence, canonical/route-count unchanged, no-innerHTML, no-client-directive)"
    - "The F-27 bundling assumption (Astro's Vite pipeline bundles a bare <script>'s ES import) is proven by build against a string-literal marker that survives minification, in whichever emission shape (inline or src=) Astro actually produces — not asserted in prose"
    - "The full phase-close gate (build + vitest + validate, chained in one command, no exit-code laundering) is green at 16/16 validators, or 15/16 with freshness excluded strictly under F-19 and recorded in STATE.md, astro check stays at the 4-error/0-warning baseline, and the Python pytest suite (344 passed / 1 skipped / 1 xfailed) is confirmed unaffected by actually running it"
  artifacts:
    - path: "site/scripts/validate/facets.mjs"
      provides: "new assertions 11-21: anchorDate correctness, card-count==incident-count, i18n key parity (digit-safe), empty-state node presence, no-island, no-cluster-markup, F-27 bundled-import proof (string-literal, shape-agnostic), filter-control presence + no unsubstituted tokens, canonical/route-count unchanged, no-innerHTML, no-client-directive — each both-locales, source+dist where applicable"
  key_links:
    - from: "facets.mjs new assertions"
      to: "dist/news/index.html and dist/es/noticias/index.html"
      via: "existsSync + readFileSync against real build output, per-locale iteration (RR-1 pattern)"
      pattern: "DIST_PAGES|SERIALIZATION_SITES"
---
> **Fable review: APPROVED 2026-07-30.** Gates passed: Opus plan-check (6 CRITICAL / 6 HIGH / 6 MEDIUM / 3 LOW) → Opus premortem (11 failure modes, 16 amendments) → Sonnet fix cycle 1 (A1..A19) → Fable inline fix cycle 2 (F-34: cycle 1 reintroduced exit-code laundering in the phase-close gate) → independent Opus re-review → Fable inline fix of its 2 remaining blockers (both "cannot pass against a correct implementation": the `{n}` token scan vs. the required `data-result-count-template` carrier, and a bare-token `set:html` count that is RED today because of a Phase 27 comment).
>
> Every `<automated>` gate in this plan set was executed or synthetically exercised by the orchestrator: the astro-check assertion exits 0 on the current tree and 1 against a 5-error log; the 28-03 chain propagates a failure from every stage downstream of its single deliberate `;`; the i18n parity script is correctly RED today; `facets.mjs` exits 0; pytest is 344 passed / 1 skipped / 1 xfailed.
>
> Accepted and recorded rather than fixed (fix cycles exhausted; see STATE.md F-35): N1 `?region=` namespace note for Phase 30, N2 freshness may redden the gate mid-phase (F-19/F-33/F-34 govern — stop and report, never remediate), N3 the manual browser pass runs at verification, N4 the "All dates" count span is not recomputed client-side (cosmetic), N5..N9 minor.



<objective>
Close out Phase 28 with the falsifiable build-time proof VALIDATION.md's Wave 0 table requires: extend `facets.mjs` (stay at validator #16 — do not add a sibling file and trigger the F-21 stale-count hazard) with 11 new assertions (11-21) covering `anchorDate` correctness, the NEWSUI-02 static-superset card count, digit-safe i18n key parity, the empty-state node, the no-island / no-cluster-markup guards, filter-control presence with no unsubstituted `{n}`/`{date}` tokens, canonical/route-count stability, the no-`innerHTML` source guard, the no-`client:`-directive source guard, and — the plan's single highest-risk item — proof that Astro actually bundles `newsFilterLogic.ts`'s import into both pages' shipped JS via a string-literal marker (F-27's load-bearing assumption, now shape-agnostic per F-31). Then run and confirm the full chained phase-close gate, including the Python pytest suite and the F-19 freshness branch.

Purpose: convert every prose claim in Plans 28-01/02 into a build-time-falsifiable check, per the plan-quality requirement that no assertion ships without a demonstrated RED counterexample, and close every row 28-VALIDATION.md marks ❌.
Output: `facets.mjs` extended with 11 new numbered assertions (11-21); a green, chained `npm run build && npm test && npm run validate` phase-close gate (16/16 or 15/16-with-F-19-exclusion); `npm run check` confirmed at the 4-error/0-warning baseline; `python -m pytest -q` confirmed at 344 passed / 1 skipped / 1 xfailed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-news-visualizer-ui/28-UI-SPEC.md
@.planning/phases/28-news-visualizer-ui/28-VALIDATION.md
@.planning/phases/28-news-visualizer-ui/28-01-SUMMARY.md
@.planning/phases/28-news-visualizer-ui/28-02-SUMMARY.md
@site/scripts/validate/facets.mjs
@site/src/pages/news.astro
@site/src/pages/es/noticias.astro
</context>

<interfaces>
<!-- facets.mjs's own established idioms (Plan 28-02's SUMMARY confirms exact final markup this plan reads). -->
- `fail(reason)` — logs `FAIL facets: ${reason}` and `process.exit(1)`. Every new assertion uses this, never a bare `throw`.
- `SERIALIZATION_SITES` (source-level, both `.astro` files) and `DIST_PAGES` (built HTML, both locales) arrays — the existing two-locale-iteration pattern (assertion 7, RR-1). Every new assertion that touches page content MUST iterate both entries, never check only EN (the exact defect RR-1 fixed).
- Existing regex idiom for extracting the `#news-facets` node: `distHtml.match(/<script type="application\/json" id="news-facets">([\s\S]*?)<\/script>/)`.
- `SKIP_DIRS = new Set(['node_modules','.astro','dist'])` (facets.mjs:443) — if reusing/adapting the existing `walk()` helper for `dist/_astro/`, the traversal must START at `dist/_astro` directly (a `walk('dist')` call recurses into `dist` first, whose name is not itself skipped, but a blind reuse risks matching `dist` as a skip-test target depending on call convention — verify the helper's actual entry-point semantics before relying on it, and prefer starting the walk at `dist/_astro` explicitly to avoid the ambiguity entirely).
- Note (F-24, STATE.md:203): `facets.mjs`'s existing 10 assertions include 3 that are true by construction for every input (assertion 1's count-sum, assertion 8's three containments — confirmed by a 20,000-input fuzz) and 1 unreachable behind assertion 3. Do not read the assertion count as coverage; every assertion added here must be demonstrated RED before it is claimed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend facets.mjs with assertions 11-16 (anchorDate, card-count, i18n parity, empty-state, no-island, no-cluster-markup)</name>
  <files>site/scripts/validate/facets.mjs</files>
  <read_first>
    site/scripts/validate/facets.mjs (full file — assertion 7's SERIALIZATION_SITES/DIST_PAGES pattern at lines 244-332 is the exact template; the "assertion N — ${locale} ..." message convention; the `fail()` helper)
    .planning/phases/28-news-visualizer-ui/28-VALIDATION.md § Wave 0 Requirements falsifiability table (the exact counterexample each assertion must catch)
    site/src/pages/news.astro and site/src/pages/es/noticias.astro (post-28-02 state, via their SUMMARY — exact rendered markup: filter-bar IDs, `#news-empty` structure, `data-*` attribute names, `class="news-card"` opening-tag shape, now with additional `data-*` attributes on the same tag)
    site/src/config/i18n.ts (post-28-01 state — the 15 `news_*` keys, including the renamed `news_window_7d_label`/`news_window_30d_label`, and their exact EN_STRINGS/ES_STRINGS locations)
  </read_first>
  <action>
    Add six new numbered assertion blocks (11-16) after the existing assertion 10, each following assertion 7's per-locale iteration template exactly:
    (11) **anchorDate correctness** — independently recompute the max `date` string across raw `incidents` (same lexicographic-max logic already used for assertion 8's `newestDate`, do not duplicate a third time — reuse the existing `newestDate` local already computed above assertion 8, or hoist that computation earlier if assertion ordering requires it) and assert both dist pages' parsed `#news-facets.anchorDate` equals it exactly. Also assert, for each dist page, that every `toLocaleDateString(` call site with a `month:` option in the corresponding `.astro` source also passes `timeZone: 'UTC'` — regex over the page source, both locales (`facets.mjs` assertion 9's existing TZ scan is hardcoded to `newsFacets.ts` only and never reads the `.astro` pages — this closes that gap, per F-32). Falsifiable counterexample (state in a comment, mirroring assertion 7's style): construct a tmp-fixture current.json whose max incident date is moved back one day and confirm this assertion — run via `FACETS_CURRENT_JSON=<tmp>` — fails; this file need not execute that fixture run itself (that is a manual RED/GREEN demonstration performed once during this task, per the plan-quality rule), but the assertion logic itself must be capable of catching it. Second counterexample: remove `timeZone: 'UTC'` from one `toLocaleDateString` call in either page → RED.
    (12) **card-count == incident-count** — for each dist page, count occurrences of `/<article\b[^>]*\bclass="[^"]*\bnews-card\b[^"]*"/g` (order-independent over whatever attributes precede or follow `class` on the tag — NOT the literal `/<article class="news-card"/g`, which silently returns 0 if `class` is not the first attribute, and NOT a bare substring count, which also matches attribute/CSS text per VALIDATION.md's explicit warning) and assert it equals `incidents.length`. VERIFIED today: both forms return 1215 on both dist pages (= `incidents.length`) while the bare substring `news-card` returns 1216 (the extra is the inlined `.news-card` CSS rule). This directly proves NEWSUI-02 (full static superset, filtering never removes server-rendered content).
    (13) **i18n key parity** — read `site/src/config/i18n.ts` source, extract the `EN_STRINGS` and `ES_STRINGS` object bodies (string-split on the unique anchors `export const EN_STRINGS` / `export const ES_STRINGS`, matching this validator's existing style of parsing TS source with regex/string ops rather than importing the module — consistent with assertion 3's `schema.py` parsing approach), and using `matchAll(/^\s*(news_[a-z0-9_]+)\s*:/gm)` (note: `g` flag required — a bare `m`-flag `String.match()` returns only the first match, silently covering exactly one key; `[a-z0-9_]+` required — `[a-z_]+` alone does NOT match `news_window_7d_label`/`news_window_30d_label`, VERIFIED) collect the key sets from both blocks. Assert set equality in BOTH directions (a key present in ES but missing from EN is equally a defect) and additionally assert the harvested count from each block is ≥ 15, so a regex that silently stops matching cannot read as coverage. Fail with the specific missing key name(s).
    (14) **empty-state node presence** — for each dist page, assert the HTML contains an element with `id="news-empty"` and the `hidden` attribute present on it in the initial (unfiltered) render, and that it is NOT an `<h1>`/`<h2>` tag (regex-check the tag name immediately preceding `id="news-empty"` is not `h1`/`h2`).
    (15) **no-island** — for each dist page, assert the HTML does NOT contain the string `astro-island` (Astro's hydration marker) — proves NEWSUI-06's "no measurable... hydration regression" structurally, not just by inspection.
    (16) **no cluster markup** — for each dist page AND each `.astro` source file, assert the HTML/source does not contain a source-count badge class or string tied to `news_cluster_source_count` being rendered: assert absence of the class tokens `news-source-count` / `news-cluster` / `source-count` and of the literal substrings `sources</` / `fuentes</` (the key may exist in `i18n.ts` per Plan 28-01's reserved-key decision — that is expected and required by assertion 13 — but must never be consumed by either page's Astro template; grep the two `.astro` sources specifically, not `i18n.ts`).
    Every new assertion message starts `assertion N — ${locale} ...` matching the existing numbering convention. Do not scan this validator's own source for any of these patterns (self-scan tautology, per F-24's lesson).
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/facets.mjs</automated>
  </verify>
  <done>facets.mjs exits 0 against the real build; each of the six new assertions was manually broken once (edit the corresponding source/dist condition or point FACETS_CURRENT_JSON at a mutated tmp fixture), confirmed to produce a non-zero exit with the expected `FAIL facets: assertion N` message, then restored and reconfirmed exit 0 — before moving to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: F-27 bundling proof (assertion 17) + assertions 18-21 (Wave-0 coverage completion) + full chained phase-close gate</name>
  <files>site/scripts/validate/facets.mjs</files>
  <read_first>
    site/src/lib/newsFilterLogic.ts (Plan 28-01 — the `FILTER_LOGIC_MARKER = 'newsFilterLogic/v1'` string-literal export, chosen specifically because it survives minification, unlike identifiers)
    28-01-SUMMARY.md's Task 0 probe findings (which emission shape — inline `<script type="module">` or `<script type="module" src="/_astro/…">` — Astro actually produced for a script with a static import; do not assume, read the recorded result)
    site/dist/_astro/ directory structure after a build (`RankingTableEnhancer`'s chunk shows the file-naming convention and the `src=` emission shape ONLY — its script contains no `import` at all, so it does NOT prove import-bundling; Plan 28-01 Task 0 is the actual proof for that)
    .planning/STATE.md Fable Decisions F-27 and F-31 (the exact assumption this assertion exists to prove, that identifiers are mangled by minification while string literals survive, and what "fails loudly, never falls back silently" means operationally for a validator)
  </read_first>
  <action>
    Add assertion 17. For each of the two dist news pages, collect **every** shipped module script the page owns, in both emission shapes Astro can produce: (a) the bodies of all inline `<script type="module">…</script>` blocks in the page HTML, and (b) the contents of every `dist/_astro/*.js` file referenced by a `<script type="module" src="/_astro/…">` tag in that page, **plus one level of that chunk's own static `import`/`from "./…js"` specifiers** (Rollup may hoist `newsFilterLogic` into a shared chunk). Assert that the concatenation of (a)+(b) for that page contains the literal string `newsFilterLogic/v1` (the `FILTER_LOGIC_MARKER` value). Fail with the locale name and the list of script sources searched.

    **Both shapes are legitimate and the assertion MUST accept either** — do not require the `src=` shape specifically. VERIFIED: Astro inlines a page script iff its chunk has `imports.length === 0 && dynamicImports.length === 0` AND `Buffer.byteLength(code) < assetsInlineLimit` (Vite default 4096; no override in `astro.config.mjs`). Whether Phase 28's script crosses 4096 bytes is a property of code this plan writes, not knowable in advance — an assertion requiring the `src=` shape would be a coin flip.

    **Do NOT grep for `parseFilterParams` or `WINDOW_7D_WIDTH_DAYS` or any other identifier.** esbuild mangles every local/module-scope identifier in the production build (VERIFIED: `dist/_astro/RankingTableEnhancer.astro_astro_type_script_index_0_lang.BXsR19UC.js` opens `(function(){const q=document.getElementById("rte-strings")…function P(a,c,i)…` — every name is a single letter). Only string literals survive minification (VERIFIED: `"NFD"` survives verbatim in `dist/communes/index.html`'s inlined script) — this is exactly why `FILTER_LOGIC_MARKER` exists as a dedicated string-literal export.

    Falsifiable counterexample to demonstrate once: delete the `import` line from `news.astro`'s script (leaving the rest), rebuild, confirm assertion 17 fails for the `en` locale with a clear message, restore, reconfirm green. Record in the comment whether the build itself failed first or assertion 17 caught it.

    Then add four assertions (18-21) closing the remaining Wave-0 rows 28-VALIDATION.md marks ❌ that assertions 11-17 do not cover, each iterating both `DIST_PAGES` entries (RR-1 pattern) and each demonstrated RED once:
    - **(18) filter-control presence + no unsubstituted tokens** (covers NEWSUI-01 "controls rendered on BOTH pages" and NEWSUI-03 "`#news-comuna-q` present"): assert each dist page contains `id="news-filters"`, `id="news-comuna-q"`, `id="news-result-count"`, `id="news-clear-filters"`, one `data-facet="window"|"region"|"family"` fieldset each, at least one `input type="checkbox"` inside each of the region and family fieldsets, and that no UNSUBSTITUTED token survives in the rendered markup (per F-32's mandatory server-side substitution).

      **Token scan — exact form, do not improvise.** One `{n}` is REQUIRED in the dist HTML and is not a defect: Plan 28-02 Task 1 ships the client's re-substitution template in `data-result-count-template` on `#news-result-count`, whose value is `t.news_result_count` = `"{n} incidents shown"` / `"{n} incidentes mostrados"`. The client needs it to rebuild the sentence on every filter change. So strip that one carrier attribute, THEN scan, and separately pin the carrier so it cannot be silently dropped:

      ```js
      const scan = distHtml.replace(/\sdata-result-count-template="[^"]*"/g, '');
      if (scan.includes('{n}') || scan.includes('{date}')) fail(`assertion 18 — ${locale}: unsubstituted token in rendered markup`);
      if ((distHtml.match(/data-result-count-template="/g) || []).length !== 1) fail(`assertion 18 — ${locale}: expected exactly one data-result-count-template`);
      ```

      A whole-HTML scan without the strip is RED against a CORRECT implementation — it was the form originally written here, and it would have fired on the phase's terminal gate. Counterexamples: delete the comuna input → RED; render `news_window_latest` without substituting `{date}` → RED; drop the `data-result-count-template` carrier → RED.
    - **(19) canonical + route count unchanged** (covers "no new indexable URL"): assert each dist news page's `<link rel="canonical">` is exactly `https://ischilesafe.com/news/` / `https://ischilesafe.com/es/noticias/` with no query string, and that neither page emits a `<link rel="alternate">` or internal `<a href>` containing `?family=`, `?region=`, `?window=`, or `?q=`. Counterexample: add a `?family=robos` link → RED.
    - **(20) no `innerHTML` with data-derived strings** (covers NEWSUI-06): source-level scan of `news.astro`, `es/noticias.astro`, and `newsFilterLogic.ts` for `innerHTML`, `outerHTML`, `insertAdjacentHTML`, and `document.write` — assert zero occurrences. `set:html` remains permitted only on the single `#news-facets` node already guarded by assertion 7. **Count the ATTRIBUTE form `/set:html=/g` and require exactly 1 per page — never the bare token `set:html`.** VERIFIED on the current tree: the bare token returns **2** in both pages, because Phase 27 shipped an explanatory `{/* H-2: set:html is unescaped … */}` comment at `news.astro:207` / `es/noticias.astro:206` that Phase 28 does not touch; the attribute form returns exactly 1 in both. A bare-token count is RED before Phase 28 writes a character. Counterexample: replace one `textContent =` with `innerHTML =` → RED.
    - **(21) no `client:` directive** (hardens assertion 15 at source level, since `astro-island` absence is a downstream symptom): assert neither `.astro` source contains `client:load`, `client:idle`, `client:visible`, `client:only`, or `client:media`. Counterexample: add `client:load` to any element → RED.

    After all 11 new assertions (11-21) are green, run the full chained phase-close gate. Append the F-19 freshness branch explicitly: `freshness.mjs` (validator #15) is expected to be RED for this phase — VERIFIED 2026-07-30: `data/incidents/current.json` is 2.9 days old against `MAX_AGE_DAYS = 3` (freshness.mjs:27) and crosses the limit within hours purely from elapsed time, while `data/` is read-only and `git pull` is a banned remediation. Apply the F-19 environmental-exclusion branch verbatim: confirm the `npm run validate` failure output is exactly the age assertion (not missing-file, not unparseable `generated`), confirm `git log -1 --format=%ci origin/master -- data/incidents/current.json` is within 3 days (VERIFIED already true: `2026-07-30 02:02:51 +0000` — the cron is alive, this checkout is merely unpulled), then close this task on a "15/16 validators, freshness excluded" basis if that specific pattern holds, with a STATE.md `## Blockers` entry. If freshness is green (build ran early enough), report 16/16 instead — do not force the exclusion narrative if it isn't needed. **BANNED, each a phase failure:** raising `MAX_AGE_DAYS`, editing/skipping/deleting/unregistering the validator, touching `data/`, or running `git pull`/`rebase`/`push`. If any OTHER validator is red, that is a real regression, not F-19.

    Also actually run the Python test suite as part of the close-out (never claim it unaffected "by inspection of git status" alone): `cd .. && python -m pytest -q`. Expected: 344 passed / 1 skipped / 1 xfailed. The 1 xfail is the deliberate Phase 26 gate quarantine (F-08) and must NOT be "fixed" — if it unexpectedly passes (`XPASS`) or the pass/fail counts differ from 344/1/1, that is a regression to investigate, not something to wave through.
  </action>
  <verify>
    <automated>cd site && npx astro check > .astro-check.log 2>&1; node -e "const s=require('fs').readFileSync('.astro-check.log','utf8').replace(/\[[0-9;]*m/g,''); const e=s.match(/^- (\d+) errors?$/m), w=s.match(/^- (\d+) warnings?$/m); if(!e||!w){ console.error('FAIL: astro check summary not found'); process.exit(1); } if(Number(e[1])!==4||Number(w[1])!==0){ console.error('FAIL: astro check '+e[1]+' errors / '+w[1]+' warnings, baseline 4/0'); process.exit(1); } console.log('astro check 4/0 baseline confirmed');" && rm -f .astro-check.log && npm run build && npm test && npm run validate && cd .. && python -m pytest -q</automated>
  </verify>
  <done>All 21 facets.mjs assertions pass against a real build; assertion 17 concretely ties each dist news page to a script (inline or chunked, whichever Astro actually emitted) containing the `FILTER_LOGIC_MARKER` string literal (F-27 proven by build, not asserted); assertions 18-21 close every remaining ❌ row in 28-VALIDATION.md's Wave 0 table; `npm test` (vitest, including newsFilterLogic.test.ts + newsFacets.test.ts) exits 0; `npm run validate` is 16/16 green, or 15/16 with freshness excluded strictly per F-19 and recorded in STATE.md § Blockers; `npm run check` reports exactly 4 errors / 0 warnings (unchanged baseline); `python -m pytest -q` was actually run and reports 344 passed / 1 skipped / 1 xfailed (or a regression is flagged, not waved through).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time `dist/` artifacts → validator process | Trusted (produced by the same build this validator gates), but the validator must not trust CLAIMS about the build (e.g. "the import was added") without reading the actual bundled output — this is exactly what assertion 17 exists to close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Tampering | A future edit silently reverts `escapeForInlineScript` on the now-larger `facetsProjection` (including `anchorDate`) | mitigate | Already covered by existing assertion 7, unmodified by this plan — `anchorDate` folds into the same guarded object, no new escaping call site was introduced (verified in Plan 28-02); `facetKeys` is deliberately absent (F-29), shrinking the guarded surface rather than growing it |
| T-28-09 | Tampering / Repudiation | A future edit copy-pastes `newsFilterLogic.ts`'s logic inline instead of importing it, silently reintroducing the twin-drift risk F-27 forbids | mitigate | Assertion 17 fails if the shipped page's script (inline or chunked) does not contain the `FILTER_LOGIC_MARKER` string literal — a copy-paste that doesn't import would not produce that literal in the output, catching the regression at build-validation time, not at code review time |
| T-28-10 | Denial of Service (build) | A malformed/huge `dist/_astro/` walk in assertion 17 on a much larger future build | accept | Bounded by explicitly starting the walk at `dist/_astro` (not the general-purpose `SKIP_DIRS`-gated walker used for assertion 10's broader traversal); `_astro/` output is Astro-generated and bounded by the site's own page count (834 pages today, budget-asserted elsewhere) |
| T-28-11 | Repudiation | A future edit adds `innerHTML`/`client:` usage that assertions 15 (dist-level `astro-island` scan) alone would not catch until after a bad build ships | mitigate | Assertions 20/21 add source-level scans (both `.astro` files + `newsFilterLogic.ts`) so the regression is caught before the build artifact is even produced, not just after |
</threat_model>

<verification>
**Gate shape is load-bearing (F-34) — do not "simplify" it.** The single `<automated>` command runs the astro-check assertion FIRST, because `astro check` exits non-zero by design at the 4-error baseline and its status must be discarded by the `;`. Everything after that point is joined by `&&` ONLY, so a failure in `npm run build`, `npm test`, `npm run validate`, or `pytest` propagates to the command's exit status. The earlier draft of this block read `npm run build && npm test && npm run validate; VALIDATE_EXIT=$?; ...` — the `;` terminated the whole preceding `&&` list, so build, test AND validate failures were all swallowed and `VALIDATE_EXIT` was captured but never read. That gate could not go red. Never place a `;` after a command whose failure must fail the phase.

**F-19 / freshness is handled OUT of band, not by weakening this command (F-34).** If `npm run validate` goes red, the gate goes red — full stop. The orchestrator then checks F-19's two preconditions (failure output is exactly the age assertion, and `git log -1 --format=%ci origin/master -- data/incidents/current.json` is within 3 days) and, if both hold, records the exclusion in STATE.md `## Blockers` and closes the phase at 15/16 as an explicit documented decision. BANNED remediations, each a phase failure: raising `MAX_AGE_DAYS`, editing/skipping/deleting/unregistering the validator, touching `data/`, or running `git pull`/`rebase`/`push`.

The single chained gate (ONE command per the OneDrive rule) exits 0 with all of build, vitest, the 16 validators and pytest green. If `npm run validate` goes red the gate goes red; the phase then closes at 15/16 ONLY if the orchestrator confirms F-19's two preconditions and records the exclusion in STATE.md `## Blockers`. `npm run check` reports exactly 4 errors / 0 warnings via the log-file + node-assertion pattern (never a piped `grep` on colorized live output, which cannot match `astro check`'s real summary line format). `python -m pytest -q` is actually executed and its real output (344/1/1) is recorded, not inferred from git status. No `<automated>` block in this plan discards a real failure. Task 2's chain contains exactly ONE `;` — immediately after `npx astro check`, whose non-zero exit at the 4-error baseline is expected and must be discarded — and everything downstream of the astro-check assertion is joined by `&&` only, so build, test, validate and pytest failures each fail the gate. An admissible freshness red is resolved OUT of band by the orchestrator under F-19/F-34, never by letting the command pass.
</verification>

<success_criteria>
- `facets.mjs` (still registered as validator #16 in `all.mjs` — unchanged registration, no new sibling file) passes 21 assertions against the real Phase 28 build.
- Assertion 17 concretely proves the F-27 bundling assumption against real `dist/_astro/` (or inline) chunk contents via the `FILTER_LOGIC_MARKER` string literal, not prose, and accepts whichever emission shape Astro actually produced.
- Assertions 18-21 close every remaining ❌ row in 28-VALIDATION.md's Wave 0 table (filter-control presence, canonical/route-count stability, no-innerHTML, no-client-directive).
- Every new assertion was manually demonstrated RED then restored GREEN (Operating Lesson 11).
- Full phase-close gate: `npm run build && npm test && npm run validate` at 16/16, or 15/16 with freshness excluded strictly under F-19 and recorded in STATE.md § Blockers. `npm run check` at the unchanged 4-error/0-warning baseline.
- Python `pytest` suite actually run and confirmed at 344 passed / 1 skipped / 1 xfailed (the xfail is the deliberate Phase 26 quarantine, F-08 — not to be "fixed").
</success_criteria>

<output>
Create `.planning/phases/28-news-visualizer-ui/28-03-SUMMARY.md` when done
</output>
