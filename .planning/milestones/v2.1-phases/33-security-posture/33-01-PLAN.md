---
phase: 33-security-posture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/deploy-on-code.yml
  - .github/workflows/heartbeat.yml
  - .github/workflows/r2-archive.yml
  - .github/workflows/cead-scraper.yml
  - .github/workflows/news-pipeline.yml
  - .github/scripts/check-sha-pins.sh
  - pipeline/tests/test_workflow_guards.py
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "Every job across all six workflows declares its own job-level permissions: block; no job relies solely on a workflow-level or repo default."
    - "All 13 uses: refs in .github/workflows/*.yml are pinned to a full 40-hex commit SHA with a trailing # vN.N.N comment."
    - "A new check-sha-pins.sh gate fails on any unpinned or comment-less uses: ref and passes on the corrected tree, and asserts it examined at least 13 uses: lines without ever laundering a scan failure into a clean report."
  artifacts:
    - path: ".github/scripts/check-sha-pins.sh"
      provides: "SHA-pin + version-comment + coverage-floor gate, wired into ci.yml's lint-workflows job"
    - path: ".github/workflows/ci.yml"
      provides: "job-level permissions: contents: read on frontend/pipeline/lint-workflows + check-sha-pins.sh step"
  key_links:
    - from: ".github/workflows/ci.yml (lint-workflows job)"
      to: ".github/scripts/check-sha-pins.sh"
      via: "run: bash .github/scripts/check-sha-pins.sh step, appended after the existing lint-workflows.sh step"
      pattern: "check-sha-pins\\.sh"
---

**Fable review: APPROVED 2026-08-05** — after amendment cycle 1. See the notes block immediately below.

<!-- Fable review: -->
<!-- Premortem amendments landed here (F-121): FM-02 (mktemp+rc capture, both the script and
     the F-117 prose below), FM-03 (test_workflow_guards.py literal + files_modified + verify),
     FM-05 (de-laundered verify blocks, all three tasks), FM-08 (EXPECTED_MIN_COUNT floor +
     notice-on-overcount, or self-explaining equality error text). -->

<objective>
Close SEC-01 (job-level `permissions:` on the five under-scoped jobs, per F-106) and SEC-02
(SHA-pin all 13 `uses:` refs with documented version comments, per F-114/F-118, plus a
falsifiable coverage-asserting gate, per F-117/F-120) without touching `lint-workflows.sh`
itself, and without reddening the existing `test_workflow_guards.py` suite (FM-03).

Purpose: least-privilege token scope + immutable action references are the two cheapest,
highest-leverage supply-chain hardenings available, and this repo currently has neither.
Output: five workflows with explicit job-level `permissions:`, 13 SHA-pinned `uses:` refs,
a new `check-sha-pins.sh` composed as an additional step inside the existing
`lint-workflows` job in `ci.yml`, and one existing pytest literal updated so it survives
the pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-security-posture/33-RESEARCH.md
@.planning/phases/33-security-posture/33-PREMORTEM.md
@.planning/STATE.md
@.planning/ROADMAP.md
@pipeline/tests/test_workflow_guards.py

<interfaces>
<!-- Exact SHAs + version comments, fixed by the orchestrator via live GitHub API calls
     (F-118). Do not re-resolve these -- use them verbatim at all 13 sites. -->

Pin table (F-104/F-111/F-118, reconfirmed live):

| Tag | SHA | Comment |
|-----|-----|---------|
| `actions/checkout@v7` | `3d3c42e5aac5ba805825da76410c181273ba90b1` | `# v7.0.1` |
| `actions/setup-python@v7` | `5fda3b95a4ea91299a34e894583c3862153e4b97` | `# v7.0.0` |
| `actions/setup-node@v7` | `820762786026740c76f36085b0efc47a31fe5020` | `# v7.0.0` |

13 edit sites (F-114, corrected count -- 8 checkout + 4 setup-python + 1 setup-node):

```
.github/workflows/ci.yml:17            uses: actions/checkout@v7           (frontend job)
.github/workflows/ci.yml:19            uses: actions/setup-node@v7         (frontend job)
.github/workflows/ci.yml:38            uses: actions/checkout@v7           (pipeline job)
.github/workflows/ci.yml:40            uses: actions/setup-python@v7       (pipeline job)
.github/workflows/ci.yml:55            uses: actions/checkout@v7           (lint-workflows job)
.github/workflows/cead-scraper.yml:44  uses: actions/checkout@v7
.github/workflows/cead-scraper.yml:54  uses: actions/setup-python@v7
.github/workflows/deploy-on-code.yml:51 uses: actions/checkout@v7
.github/workflows/heartbeat.yml:44     uses: actions/checkout@v7
.github/workflows/news-pipeline.yml:32 uses: actions/checkout@v7
.github/workflows/news-pipeline.yml:55 uses: actions/setup-python@v7
.github/workflows/r2-archive.yml:21    uses: actions/checkout@v7
.github/workflows/r2-archive.yml:43    uses: actions/setup-python@v7
```

Job-level permissions gap (F-106, verified this session -- do not re-derive):

| Workflow | Job | Add job-level `permissions:` |
|----------|-----|-------------------------------|
| `ci.yml` | `frontend` | `contents: read` |
| `ci.yml` | `pipeline` | `contents: read` |
| `ci.yml` | `lint-workflows` | `contents: read` |
| `deploy-on-code.yml` | `deploy` | `contents: read` |
| `heartbeat.yml` | `heartbeat` | `actions: read`, `contents: read`, `issues: write` -- carried down VERBATIM from the workflow-level block (do not narrow; F-88 proved all three load-bearing on a real runner) |

Already compliant at job level (no change): `cead-scraper.scrape`, `news-pipeline.scrape`,
`r2-archive.archive` (each already declares its own job-level `permissions:` block -- verify
by reading the files, do not add a duplicate).

Existing gate this plan composes with, unchanged (`.github/scripts/lint-workflows.sh`):
appends actionlint+shellcheck after bootstrapping and running its canary. `check-sha-pins.sh`
is a NEW sibling script; it is picked up automatically by `lint-workflows.sh`'s own
`.github/scripts/*.sh` shellcheck glob (line 38) -- so it must itself be shellcheck-clean, but
do not add a manual shellcheck call for it in this plan.

<!-- FM-02/F-120, BLOCKER, binding correction to F-117's stated mechanism: a process
     substitution's (`done < <(grep ...)`) exit status is NEVER examined by `set -e` and is not
     part of any pipeline, so `pipefail` cannot see a failing grep there either. PROVEN this
     session: a grep against a nonexistent path inside `done < <(grep ...)` prints its own
     stderr error line but the enclosing script still reaches its final `exit 0`. F-117's
     INTENT (rc >= 2 must be a hard failure, not a silent pass) is correct and unchanged; its
     NAMED MECHANISM ("let set -euo pipefail propagate it") does not work and must not be
     written into this script or described that way in prose. The correct mechanism, required
     in check-sha-pins.sh: capture grep's output to a temp file with an explicitly-checked rc,
     OUTSIDE any process substitution or pipeline:

     tmp="$(mktemp)"; trap 'rm -f "$tmp"' EXIT
     set +e; grep -rHn "uses:" .github/workflows/*.yml >"$tmp"; rc=$?; set -e
     [ "$rc" -ge 2 ] && { echo "::error::scan failed (grep rc=$rc) -- refusing to report a clean tree"; exit 1; }
     while IFS=: read -r file line content; do ... done <"$tmp"

     rc 0 (matches) and rc 1 (no matches -- would itself be a coverage failure here since
     uses: lines are expected to exist, handled by the floor/count check below) are legitimate
     data; rc >= 2 (bad path, bad regex, unreadable file) is the hard failure this replaces the
     old `set -euo pipefail`-on-a-process-substitution claim with. -->

<!-- FM-08, MEDIUM: EXPECTED_COUNT=13 as a strict equality makes the first legitimate new
     `uses:` step (e.g. actions/cache) read as a security violation, even though it is
     correctly pinned. The invariant that matters is "every uses: ref found is pinned, and the
     scan saw a non-zero, plausible number of them" -- not "exactly 13 forever". Fix: use
     EXPECTED_MIN_COUNT=13 as a FLOOR (count < 13 is still a hard failure -- an undercounted or
     empty scan must never pass, which is F-117's real point) and emit a non-fatal
     `::notice::` (not an `::error::`, does not fail the job) when count > 13, telling the
     reader this is a coverage assertion they can update, not a pin failure. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add job-level permissions: blocks to the five under-scoped jobs</name>
  <files>.github/workflows/ci.yml, .github/workflows/deploy-on-code.yml, .github/workflows/heartbeat.yml</files>
  <action>
    Per F-106 (SEC-01): add a job-level `permissions:` block directly under each job's
    `runs-on:` line (before `steps:`), keeping the existing workflow-level block in
    `ci.yml`/`heartbeat.yml` untouched (defence in depth for future jobs -- do not remove it).
    In `ci.yml`: add `permissions: {contents: read}` to `frontend`, `pipeline`, and
    `lint-workflows` jobs (three separate blocks, one per job -- YAML block form, not flow
    form, matching this repo's existing style e.g. `cead-scraper.yml:29-31`).
    In `deploy-on-code.yml`: add `permissions: {contents: read}` to the `deploy` job (the
    workflow-level block already reads `contents: read`; this makes the job explicit too,
    satisfying "no job relies on the repo default alone" per success criterion 1's exact
    wording).
    In `heartbeat.yml`: add a job-level block to the `heartbeat` job with all three scopes
    copied verbatim from the workflow-level block: `actions: read`, `contents: read`,
    `issues: write`. Do NOT narrow this to a subset -- F-88 proved (on a real runner, this
    milestone) that `actions: read` is required for the R2 heartbeat check's `gh run list`
    call and `issues: write` for the alert-issue step; a narrowed job-level block would
    silently override the workflow-level grant with a stricter one and reproduce the day-one
    403 F-88 exists to prevent.
    `r2-archive.yml`, `cead-scraper.yml`, `news-pipeline.yml` already have correct job-level
    `permissions:` blocks (`contents: write`/`read` + `issues: write` as applicable) -- do not
    modify these three files in this task.
  </action>
  <verify>
    <automated>test "$(grep -c 'permissions:' .github/workflows/ci.yml)" -eq 4</automated>
  </verify>
  <done>
    `ci.yml` has 4 `permissions:` occurrences (1 workflow-level + 3 job-level: frontend,
    pipeline, lint-workflows). `deploy-on-code.yml` has 2 (workflow-level + job-level, both
    `contents: read`). `heartbeat.yml` has 2 (workflow-level + job-level, identical
    three-scope set). Falsification (FM-05, exit-code gated, no laundering): on the current
    tree `grep -c 'permissions:' ci.yml` is 1, so `test "$(...)" -eq 4` exits 1 (fails) before
    this task and exits 0 only after the job-level blocks are actually added.
  </done>
</task>

<task type="auto">
  <name>Task 2: SHA-pin all 13 uses: refs with version comments; keep test_workflow_guards.py non-vacuous</name>
  <files>.github/workflows/ci.yml, .github/workflows/cead-scraper.yml, .github/workflows/deploy-on-code.yml, .github/workflows/heartbeat.yml, .github/workflows/news-pipeline.yml, .github/workflows/r2-archive.yml, pipeline/tests/test_workflow_guards.py</files>
  <action>
    Per F-104/F-114/F-118 (SEC-02): replace every `uses: actions/checkout@v7` with
    `uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1`, every
    `uses: actions/setup-python@v7` with
    `uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0`, and the
    single `uses: actions/setup-node@v7` with
    `uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0`. Apply at all
    13 sites listed in `<interfaces>` above -- this is a mechanical, identical substitution per
    tag, not a per-file judgment call. Preserve each step's existing `with:` block and
    indentation exactly; only the `uses:` value changes. Do not touch any other line in these
    files (permissions blocks from Task 1 must survive this edit unchanged).

    FM-03 (BLOCKER): `pipeline/tests/test_workflow_guards.py`'s
    `test_guard_is_non_vacuous_against_a_synthetic_regression` (around line 592) does a
    literal substring match `if "uses: actions/setup-python@v7" in line:` to locate and mutate
    away the setup-python step when proving its own H-02 guard is non-vacuous. After this
    task's SHA-pinning, that literal never matches again, the mutation removes nothing, and
    the test fails with a misleading "guard found nothing -- vacuous test" message. Fix it in
    the SAME task (not a follow-up): change that literal from the version-tagged
    `"uses: actions/setup-python@v7"` to the version-agnostic prefix
    `"uses: actions/setup-python@"` (matching the existing pin-agnostic style already used a
    few lines earlier at `stripped.startswith("- uses: actions/setup-python")`). Do not touch
    any other line of this test file. After making both changes, run
    `python -m pytest pipeline/tests/test_workflow_guards.py -q` locally and confirm all tests
    pass, including `test_guard_is_non_vacuous_against_a_synthetic_regression` (which must
    still detect the mutation and fail loudly if the mutation logic itself is ever broken --
    the fix must not neuter what the test proves).
  </action>
  <verify>
    <automated>! grep -rq 'uses: actions/.*@v7$' .github/workflows/*.yml</automated>
  </verify>
  <done>
    Zero remaining bare `@v7` tag references anywhere in `.github/workflows/*.yml`. All 13
    sites now show a 40-hex SHA followed by a `# vN.N.N` comment. Separately,
    `cd pipeline && python -m pytest tests/test_workflow_guards.py -q` exits 0 (all tests
    pass, including the non-vacuity test, which still fails-on-purpose if its own mutation
    logic is broken). Falsification (FM-05, exit-code gated): on the current (pre-edit) tree,
    `grep -rq 'uses: actions/.*@v7$' .github/workflows/*.yml` succeeds (matches exist), so
    `! grep -rq ...` exits 1 (fails) -- it must exit 0 only after every bare tag is replaced.
    Separately, without the FM-03 literal fix, the pytest command above exits non-zero (1
    failed) on the post-pin tree -- both must be true simultaneously for this task to be done.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: check-sha-pins.sh gate -- coverage-floor + pin-format assertion, wired into ci.yml</name>
  <files>.github/scripts/check-sha-pins.sh, .github/workflows/ci.yml</files>
  <behavior>
    - Against the post-Task-2 real tree (all 13 refs pinned correctly): script exits 0, prints
      the count of `uses:` lines examined (must be >= 13, exactly 13 today), no `::error::`
      lines, and no `::notice::` (since count == EXPECTED_MIN_COUNT exactly).
    - Against a fixture/inline test where one `uses:` value is `actions/checkout@v7` (a bare
      tag, unpinned): script exits 1 and emits an `::error::` naming that exact file/line as
      not pinned to a 40-hex SHA.
    - Against a fixture where a `uses:` value is a correctly-pinned 40-hex SHA but has NO
      trailing `# vN.N.N` comment: script exits 1 and emits an `::error::` naming the missing
      version comment specifically (not the same message as the unpinned case).
    - Coverage floor (F-117/FM-08): if the scan returns fewer than EXPECTED_MIN_COUNT (13)
      lines (e.g., a future glob typo that silently narrows the scan), the script exits 1
      with an explicit `::error::` naming the expected minimum versus the actual count -- an
      empty or undercounted scan must never exit 0.
    - Overcount (FM-08): if the scan returns MORE than 13 lines (e.g. a legitimately added
      14th `uses:` step, correctly pinned), the script still exits 0 and prints a `::notice::`
      (not an error) stating this is a coverage assertion the reader can bump, not a pin
      failure -- proving a correctly-pinned new step is never mistaken for a violation.
    - Grep-failure mechanism (FM-02/F-120): against an unreadable/mis-globbed target path, the
      script's explicit `rc=$?` capture (NOT a process-substitution-wrapped `set -euo
      pipefail`, which cannot see this) detects `rc >= 2` and hard-fails with an `::error::`
      naming the scan failure -- demonstrated by temporarily pointing the script at a
      nonexistent path and confirming it exits 1 rather than silently reporting 0 findings.
  </behavior>
  <action>
    Create `.github/scripts/check-sha-pins.sh` using the researched-and-executed script in
    `33-RESEARCH.md` section 1 as the starting shape, but replace its process-substitution
    grep idiom (`done < <(grep ...)`) with the explicit temp-file + rc-capture mechanism
    required by FM-02/F-120 (shown verbatim in `<interfaces>` above): `tmp="$(mktemp)"`, a
    `trap 'rm -f "$tmp"' EXIT`, `set +e; grep -rHn "uses:" .github/workflows/*.yml >"$tmp";
    rc=$?; set -e`, then `[ "$rc" -ge 2 ] && { echo "::error::..."; exit 1; }`, then
    `while IFS=: read -r file line content; do ... done <"$tmp"`. Always pass `-H` to the
    grep (Pitfall 1). After the loop, count the lines in `$tmp` and apply the FM-08 floor
    contract: `EXPECTED_MIN_COUNT=13` (named variable, not a magic number); if the count is
    less than `EXPECTED_MIN_COUNT`, emit an `::error::` naming both numbers and exit 1; if the
    count is greater, emit a `::notice::` (not `::error::`, does not affect the exit code)
    stating "examined N uses: lines; EXPECTED_MIN_COUNT is 13 -- this is a coverage floor, not
    a pin failure; bump it in check-sha-pins.sh if a step was legitimately added" -- and still
    exit 0 if every individual ref found was correctly pinned. Do NOT use
    `2>/dev/null || true` anywhere in this script (F-117's other half -- swallowing grep's own
    error output converts an unreadable-tree failure into a false "zero findings" pass). Keep
    the script POSIX/bash, `set -euo pipefail` for everything except the explicitly-guarded
    grep capture, no interpolation of secrets, and self-contained. Then add one new step to
    `ci.yml`'s existing `lint-workflows` job, immediately after the existing "Lint workflows
    (armed instrument, F-85/H-03)" step: `- name: Check SHA-pins (SEC-02, F-114/F-117/F-120)`
    running `bash .github/scripts/check-sha-pins.sh`. Do not modify `lint-workflows.sh`
    itself -- this is a new, composed sibling step, not an edit to the existing armed
    instrument.
  </action>
  <verify>
    <automated>bash .github/scripts/check-sha-pins.sh</automated>
  </verify>
  <done>
    Running `bash .github/scripts/check-sha-pins.sh` against the real tree (after Task 2)
    exits 0, prints an explicit count line stating >= 13 `uses:` lines were examined (13
    today, no `::notice::` since the count is exactly at the floor), and no `::error::`
    output. Both-direction falsification proven and stated in the plan's summary: (a) a
    planted bare-tag line (temporarily editing a scratch copy of one workflow line to
    `actions/checkout@v7`) makes the script exit 1 naming that file/line; (b) reverting it
    restores exit 0; (c) a scan pointed at a nonexistent path exits 1 via the explicit rc
    check (not via a process-substitution `set -e`, which FM-02 proved cannot see it); (d) a
    fixture with one legitimately-added, correctly-pinned 14th ref exits 0 with a `::notice::`,
    not an `::error::`. `.github/scripts/*.sh` (shellchecked automatically by
    `lint-workflows.sh` line 38) must pass -- confirmed by running
    `bash .github/scripts/lint-workflows.sh` and observing no `check-sha-pins.sh`-attributed
    shellcheck error in its output. Falsification (FM-05, exit-code gated): the verify command
    above is the gate's own exit code with no `; echo` suffix -- a real failure in the script
    must surface as a non-zero exit from this exact command.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub Actions workflow -> third-party/first-party action code | `uses:` refs execute arbitrary code from the referenced tag/SHA inside the job's token scope |
| Job -> repository token scope | `GITHUB_TOKEN`'s effective permissions for that job, gated by `permissions:` blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `actions/checkout@v7` / `setup-python@v7` / `setup-node@v7` mutable tags | mitigate | SHA-pin all 13 refs with `# vN.N.N` comments (Task 2); dependabot (Plan 33-02) keeps pins from rotting |
| T-33-02 | Elevation of Privilege | Jobs inheriting only a workflow-level or repo-default token scope | mitigate | Explicit job-level `permissions:` on all 9 jobs (Task 1 + pre-existing 3) |
| T-33-03 | Tampering | `check-sha-pins.sh` scan silently narrowed (glob typo, future refactor) and passing an undercount as green | mitigate | Explicit rc-capture (FM-02/F-120) + `EXPECTED_MIN_COUNT=13` floor, non-zero exit on any undercount (Task 3) |

**F-119, load-bearing note:** `zizmor` (added in Plan 33-02) does NOT flag `actions/checkout@v7`
or any GitHub-owned `actions/*` tag as `unpinned-uses` at `regular` (or `pedantic`) persona --
measured on fixtures by the orchestrator, both personas accept first-party tags at tag level by
default. This means `check-sha-pins.sh` (Task 3) is the ONLY gate covering SHA-pin regression
in this repo; it is not redundant with zizmor and must not be described as such in any summary.

**F-120, load-bearing note:** F-117's stated mechanism ("let `set -euo pipefail` propagate a
grep failure inside `done < <(grep ...)`") does not work -- a process substitution's exit
status is invisible to both `set -e` and `pipefail`. F-117's intent (rc >= 2 must be a hard
failure) is correct and is satisfied here via an explicit `set +e; ...; rc=$?; set -e` capture
instead. This correction applies identically to Plan 33-03's `check-secret-hygiene.sh`.
</threat_model>

<verification>
1. `! grep -rq 'uses: actions/.*@v7$' .github/workflows/*.yml` -- exits 0 (no bare-tag `uses:` refs remain).
2. `bash .github/scripts/check-sha-pins.sh` -- exits 0, prints a count-of->=13 confirmation line.
3. `bash .github/scripts/lint-workflows.sh` -- exits 0 (existing armed instrument unaffected by the new sibling script).
4. `test "$(grep -c 'permissions:' .github/workflows/ci.yml)" -eq 4` -- exits 0; `.github/workflows/deploy-on-code.yml` and `.github/workflows/heartbeat.yml` each have `grep -c 'permissions:'` equal to 2.
5. `cd pipeline && python -m pytest tests/test_workflow_guards.py -q` -- exits 0 (FM-03 fix confirmed non-regressing).
</verification>

<success_criteria>
All 13 `uses:` refs SHA-pinned with correct version comments (verified by `check-sha-pins.sh`
exiting 0 with a confirmed count floor of 13). All 9 jobs across 6 workflows declare an
explicit job-level `permissions:` block (5 newly added, 3 pre-existing, `ci.yml`'s three jobs
individually scoped). `check-sha-pins.sh` is proven, both directions, to fire on an unpinned
or comment-less ref, to fail loudly on an undercounted scan via an explicit rc capture (not a
process-substitution `set -e`, which cannot see it), and to pass with a non-fatal notice on a
legitimate overcount. `test_workflow_guards.py`'s non-vacuity test still passes and still
fails-on-purpose if its own mutation logic breaks. `lint-workflows.sh` remains byte-unchanged;
the new gate composes as a sibling step in `ci.yml`'s `lint-workflows` job.
</success_criteria>

<output>
Create `.planning/phases/33-security-posture/33-01-SUMMARY.md` when done
</output>
