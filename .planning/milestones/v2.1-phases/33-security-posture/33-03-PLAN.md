---
phase: 33-security-posture
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - .github/scripts/check-secret-hygiene.sh
  - .github/scripts/fixtures/secret-hygiene-canary/canary-workflow.yml
  - .github/scripts/fixtures/secret-hygiene-canary/canary-script.sh
  - .github/scripts/fixtures/secret-hygiene-canary/canary_module.py
  - .github/workflows/ci.yml
  - pipeline/news/feeds.py
  - pipeline/scrape_news.py
  - pipeline/tests/test_scrape_news.py
  - DEPLOYMENT.md
autonomous: true
requirements: [SEC-02, SEC-05, SEC-06]
must_haves:
  truths:
    - "GitHub secret scanning and push protection are confirmed enabled by a manual/API check recorded in this plan's evidence; no API key -- including the CF_HOOK alias -- is discoverable in workflow logs or committed artifacts."
    - "A courtesy delay separates every inter-feed fetch in scrape_news.py's 9-feed loop, covered by a pytest regression asserting the exact delay-call argument list."
    - "Every pinned uses: ref, plus the actionlint/shellcheck binaries and zizmor, has a documented per-reference SHA-pin-vs-tag decision recorded in this repo."
    - "check-secret-hygiene.sh does not flag itself, is proven against a committed canary fixture before it trusts a clean real-tree scan, and its own error strings do not contain the patterns it hunts."
  artifacts:
    - path: ".github/scripts/check-secret-hygiene.sh"
      provides: "grep-driven gate for echo/printf of secret names (including CF_HOOK), set -x/-o xtrace, curl -v/--verbose (comment-aware), botocore-DEBUG logging, self-excluded, canary-proven, mktemp+rc grep-exit-code discipline (rc 0/1 data, rc>=2 hard fail)"
    - path: ".github/scripts/fixtures/secret-hygiene-canary/"
      provides: "committed canary fixture (one .yml, one .sh) exhibiting all four risk shapes, scanned first, aborts loudly if any of the four named findings does not fire"
    - path: "pipeline/scrape_news.py"
      provides: "1.5s inter-feed courtesy delay (reusing fulltext.REQUEST_DELAY), applied between iterations, never before the first"
    - path: "DEPLOYMENT.md"
      provides: "SHA-pin decision record for all three reference classes (uses: refs, lint-tool binaries, zizmor) + local zizmor invocation + FM-10's blocking workflow_dispatch evidence + FM-16's zizmor-pin-not-dependabot-covered note"
  key_links:
    - from: ".github/workflows/ci.yml (lint-workflows job)"
      to: ".github/scripts/check-secret-hygiene.sh"
      via: "run: bash .github/scripts/check-secret-hygiene.sh step"
      pattern: "check-secret-hygiene\\.sh"
    - from: "pipeline/scrape_news.py (feed loop)"
      to: "pipeline/news/fulltext.REQUEST_DELAY"
      via: "import + time.sleep(REQUEST_DELAY) guarded by if i > 0"
      pattern: "REQUEST_DELAY"
---

**Fable review: APPROVED 2026-08-05** — after amendment cycle 1. See the notes block immediately below.

<!-- Fable review: -->
<!-- Premortem amendments landed here (F-121): FM-01 (self-exclusion + reworded error strings,
     Task 1), FM-02 (mktemp+rc capture mechanism, Task 1 + interfaces, corrected from F-117's
     unworkable process-substitution claim, matching Plan 33-01's identical fix), FM-04 (CF_HOOK
     added to the secret-name list with the alias-vs-secret rationale), FM-05 (de-laundered
     every <automated> block across all three tasks and the overall verification section),
     FM-06 (committed canary fixture, scanned before the real tree, mirroring
     lint-workflows.sh:24-30), FM-07 (serialized: depends_on ["33-01","33-02"], wave 3), FM-09
     (comment-line-aware curl -v / set -x checks with a two-direction MUST-fire/MUST-stay-silent
     list), FM-10 (FM-10's workflow_dispatch promoted to a blocking phase-close item, recorded in
     Task 3), FM-12 (sleep-patching mechanism corrected: monkeypatch.setattr + assert on the
     argument list, not call_count), FM-13 (phase-close pytest figure stated as an equality:
     392/1/1), FM-16 (one sentence: the zizmor pin is not dependabot-covered and must be
     re-evaluated per milestone). -->

<objective>
Close SEC-05's log-hygiene half with a new, self-exclusion-proof, canary-verified gate
(F-116/F-117/F-120, FM-01/FM-02/FM-04/FM-06/FM-09), close SEC-06's real measured gap with a
1.5s inter-feed courtesy delay covered by a pytest regression that asserts the exact delay
call list (F-108, FM-12), and produce this phase's documentation deliverable: a per-reference
SHA-pin decision record covering all three classes SEC-02 names (F-103), plus FM-10's blocking
workflow_dispatch evidence and FM-16's zizmor-pin note.

Purpose: SEC-05's settings half (repo public, secret scanning + push protection enabled) was
already closed and re-verified by the orchestrator (F-107/F-111) -- this plan does the log
half, which is real, unverified-until-now work. SEC-06 closes the one real courtesy-delay gap
this milestone's own research found by direct code inspection. The decision record exists so
SEC-02's "a documented decision exists for each one" is satisfied for all three reference
classes named in F-103, not just the 13 `uses:` lines.
Output: `check-secret-hygiene.sh` (self-excluding, canary-proven) wired into CI, a 1.5s
inter-feed delay in `scrape_news.py` with a pytest regression proving the exact call list, and
a DEPLOYMENT.md section recording the pin decisions, the FM-10 dispatch evidence, and the
FM-16 zizmor-pin caveat.

This plan runs LAST (FM-07): after 33-01 (SHA-pins + check-sha-pins.sh) and 33-02
(dependabot + zizmor + persist-credentials triage), both of which also touch `ci.yml`'s
`lint-workflows` job and DEPLOYMENT.md's workflow-cadence context. Running last avoids a
silent last-writer-wins overwrite of either prior plan's edits to shared files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-security-posture/33-RESEARCH.md
@.planning/phases/33-security-posture/33-PREMORTEM.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-security-posture/33-01-PLAN.md
@.planning/phases/33-security-posture/33-01-SUMMARY.md
@.planning/phases/33-security-posture/33-02-PLAN.md
@.planning/phases/33-security-posture/33-02-SUMMARY.md
@pipeline/news/fulltext.py
@pipeline/tests/test_scrape_news.py

<interfaces>
<!-- FM-04, HIGH, PROVEN: the secret-name list must include CF_HOOK, the job-level alias every
     consumer actually reads ("$CF_HOOK" in cead-scraper.yml:111, deploy-on-code.yml:62,
     news-pipeline.yml:96; require-env.sh CF_HOOK). CF_DEPLOY_HOOK_URL is never referenced in a
     run: body anywhere -- only its alias is. CF_HOOK is NOT a repo secret (do not confuse this
     with F-111's secret inventory, which is unchanged); it is the variable name the code reads,
     and guarding a gate's variable list against what the code READS (not just what `gh secret
     list` shows) is the same F-111/lesson-29 discipline already applied elsewhere in this
     phase. R2_BUCKET still does NOT belong on this list (it is not a secret and no consumer
     reads it as one) -- the two are not the same kind of omission. -->

Secret/alias names to check (F-111 + FM-04 -- 7 real repo secrets PLUS the one alias every
consumer actually reads):
`CF_DEPLOY_HOOK_URL|CF_HOOK|DEEPSEEK_API_KEY|OPENROUTER_API_KEY|R2_ACCESS_KEY_ID|R2_SECRET_ACCESS_KEY|R2_ENDPOINT_URL|TOKEN_VALUE_R2`

Scope (F-116): `.github/workflows/*.yml` and `.github/scripts/*.sh` ONLY -- no `.py` glob,
EXCEPT the one narrow botocore-DEBUG check below, which is Python-specific and additive.
F-116 resolved the boto3-exception risk by EXECUTION (planted a 32-char fake secret through
real `list_objects_v2`/`put_object` calls against a fake R2 endpoint; `SECRET in str(e)`,
`repr(e)`, and `traceback.format_exc()` were all False) and found the ONLY real Python-layer
exposure path is enabling botocore wire-level DEBUG logging via `boto3.set_stream_logger` or
raising the root logger below INFO -- neither appears anywhere in this repo today (every
pipeline entrypoint sets `logging.basicConfig(level=logging.INFO)`).

What the gate checks, four shapes (research section 4, corrected per F-117/F-120/FM-01/FM-09):
1. `echo`/`printf` of a `$SECRET_NAME` or `${SECRET_NAME}` shell expansion (using the 8-name
   list above, including `CF_HOOK`), in `.yml`/`.sh`.
2. `set -x` / `set -o xtrace` anywhere in `.yml`/`.sh` (enables command-echo of expanded args)
   -- SKIP lines whose first non-whitespace character is `#` (FM-09: this repo's workflows are
   ~40% comment by line, much of it explaining exactly this mechanism; an unanchored scan
   would redden on the phase's own explanatory prose).
3. `curl ... -v` / `curl ... --verbose` anywhere in `.yml`/`.sh` (can print secret-bearing
   URLs -- `CF_DEPLOY_HOOK_URL`/`CF_HOOK` is the highest-risk shape here) -- same comment-skip
   rule as check 2 (FM-09).
4. `boto3.set_stream_logger` OR a `logging.basicConfig` call whose `level=` argument is
   anything other than `INFO`/`WARNING`/`ERROR`/`CRITICAL` (i.e. matches
   `level\s*=\s*logging\.DEBUG` or `level\s*=\s*10\b`), scoped ONLY to `pipeline/*.py` -- this
   is the one narrow Python-layer check, added because it is the actual mechanism F-116/A3
   identified, not a defensive blanket `.py` scan (do not widen checks 1-3 to `.py`).

**FM-09, MEDIUM, two-direction requirement (add to `<behavior>` verbatim):**
MUST fire on: `run: curl -v "$CF_HOOK"` and on a bare `set -x` line inside a script body.
MUST stay silent on: `# never add curl -v here -- it would print the hook URL` and on
`# set -x is banned in this repo` -- i.e., the exact explanatory comments this phase's own
documentation work is likely to add. Checks 1 and 4 (echo/printf and botocore-DEBUG) are NOT
comment-line-skipped -- `echo "# $SECRET"` inside an actual `run:` step is still live shell,
not a YAML/shell comment, so it is out of scope for the FM-09 skip; only checks 2 and 3 (the
two unanchored line-greps FM-09 specifically found false-positive-prone) get the comment skip.

**FM-01, BLOCKER, PROVEN by execution:** the research draft's own regex strings AND its own
`::error::` message text contain the literal patterns it hunts (`set -x`, `curl -v`), so a
naive port of the draft to `.github/scripts/check-secret-hygiene.sh` flags itself and exits 1
on an otherwise-clean tree the moment it exists (verified: two of the three self-hits come
from the error-message strings, not the regexes -- rewording the patterns alone does not fix
it; both the exclusion AND the reworded strings are required). Required fix, both halves:
(a) every scanning `grep` must carry `--exclude=check-secret-hygiene.sh` (and, per FM-06 below,
must also exclude the new canary fixture directory from the REAL-tree scan, while still
scanning the fixture directory first as its own dedicated pass); (b) the `::error::` message
strings must be reworded so they do not contain the literal hunted substrings -- e.g. use
"xtrace/command-echo enabled" instead of a string containing "set -x", and "verbose curl
invocation" instead of a string containing "curl -v" or "--verbose".

**FM-06, HIGH:** neither new gate had a positive control before this amendment --
`check-secret-hygiene.sh`'s success condition is "zero findings", which is byte-indistinguishable
from an accidentally-inert version of itself (broken regex, mis-globbed targets, FM-01's
exclusion applied too broadly, a swallowed rc>=2). Required: a committed fixture directory
`.github/scripts/fixtures/secret-hygiene-canary/` containing one `.yml` file
(`canary-workflow.yml`) and one `.sh` file (`canary-script.sh`) that between them exhibit all
four risk shapes (a `run: echo "$DEEPSEEK_API_KEY"`-shaped line, a bare `set -x`, a
`curl -v "$CF_HOOK"`-shaped line, and -- since shape 4 is Python-only and this fixture
directory is scanned by the `.yml`/`.sh` scan only -- a fifth, separate Python fixture file
`.github/scripts/fixtures/secret-hygiene-canary/canary_module.py` containing a
`logging.basicConfig(level=logging.DEBUG)` line). The script must scan the fixture FIRST and
abort loudly (`::error::canary did not produce its expected findings ... instrument is not
armed`) if it does not produce all four named findings, mirroring
`lint-workflows.sh:24-30`'s structure verbatim, before proceeding to scan the real tree. The
real-tree scan must exclude the fixture directory itself (it is a deliberately "dirty" fixture,
not a real finding) via the same `--exclude`/path-skip mechanism FM-01 already requires for
self-exclusion.

<!-- FM-02/F-120, BLOCKER, PROVEN: identical correction to Plan 33-01's Task 3. A process
     substitution's (`done < <(grep ...)`) exit status is NEVER examined by `set -e` and is not
     part of any pipeline, so `pipefail` cannot see a failing grep there either -- proven this
     session: a grep against a nonexistent path inside `done < <(grep ...)` still lets the
     enclosing script reach its final `exit 0`. F-117's INTENT (rc >= 2 must be a hard failure)
     is correct; its NAMED MECHANISM ("let set -euo pipefail propagate it") does not work and
     must not be written into this script or its surrounding prose. Required mechanism, for
     EVERY evidence-gathering grep in this script (all four checks): -->

```bash
tmp="$(mktemp)"; trap 'rm -f "$tmp"' EXIT
set +e; grep -rHnE "$PATTERN" --exclude=check-secret-hygiene.sh "${TARGETS[@]}" >"$tmp"; rc=$?; set -e
[ "$rc" -ge 2 ] && { echo "::error::scan failed (grep rc=$rc) -- refusing to report a clean tree"; exit 1; }
while IFS=: read -r file line content; do ... done <"$tmp"
```

rc 0 (matches) and rc 1 (no matches) are legitimate data; rc >= 2 (bad path, bad regex,
unreadable file) is the hard failure. Do NOT use `2>/dev/null || true` anywhere (that
converts an unreadable-tree failure into a false "zero findings" pass -- the same F-85 defect
class this whole phase exists to close).

<!-- F-108/FM-12: courtesy delay, exact fix location and shape; sleep-patching mechanism
     CORRECTED per FM-12 -- do not re-derive either. -->

`pipeline/news/fulltext.py:39` already defines `REQUEST_DELAY = 1.5`. `pipeline/scrape_news.py`
imports `FEEDS` from `pipeline.news.feeds` and iterates it starting at (current) line ~227
with `for feed_name, feed_url in FEEDS.items():` and zero delay. `FEEDS` has exactly 9 entries
(4 named outlets + 5 Google News queries expanded via `**{name: google_news_url(q) for ...}`,
verified in `pipeline/news/feeds.py`). Fix shape (matching `archive_r2.py:566-568`'s existing
precedent exactly):

```python
for i, (feed_name, feed_url) in enumerate(FEEDS.items()):
    if i > 0:
        time.sleep(REQUEST_DELAY)
    try:
        entries = fetch_feed(feed_name, feed_url)
        ...
```

Reuse `fulltext.REQUEST_DELAY` (1.5s) -- do not invent a second unreviewed courtesy-delay
constant. Cost, stated explicitly per lesson 30: 8 gaps x 1.5s = 12s/run x 4 runs/day = 48s/day.

**FM-12, MEDIUM, PROVEN: sleep-patching mechanism correction.** `pipeline/scrape_news.py` does
`import time` (the module object), NOT `from time import sleep` -- so
`pipeline.scrape_news.time` IS the global `time` module; the two are the same object and any
plan text implying a module-namespace distinction between them is factually wrong. Patch it
via `monkeypatch.setattr(sn.time, 'sleep', mock)` (equivalent to patching the global, and the
test docstring must say so explicitly rather than implying isolation that does not exist).
Because this patches the process-global `time.sleep`, do NOT assert on a bare `call_count`
(an unrelated future sleep on an un-mocked path -- e.g. a `tenacity` retry -- would silently
inflate it into an off-by-N flake with the test still nominally "passing" at the wrong number
for the wrong reason). Assert instead on the exact ordered argument list:
`[c.args[0] for c in mock.call_args_list] == [REQUEST_DELAY] * (len(FEEDS) - 1)`.

<!-- F-103: the three reference classes needing a documented pin decision -->

Class (a): the 13 `uses:` refs -- SHA-pinned in Plan 33-01, decision = "pin, because
GitHub-owned tags ARE force-moved by design on every patch release and dependabot (Plan
33-02) keeps the pins from rotting" (F-104).
Class (b): the two binaries `.github/scripts/fetch-lint-tools.sh` downloads -- actionlint
v1.7.7, shellcheck v0.11.0 -- ALREADY SHA-256-pinned by Phase 32; this plan only RECORDS that
decision in DEPLOYMENT.md, it does not re-pin anything or touch `fetch-lint-tools.sh` (F-92
governs the version choice, unchanged).
Class (c): `zizmor` -- version-pinned at 1.10.0 via `pipx run zizmor==1.10.0` (CI) /
`uvx zizmor@1.10.0` (local) -- ephemeral invocation, not a `requirements.txt` entry, so there
is no SHA to pin; the "pin decision" for this class is the version pin + the citation that
zizmor is the OWASP/GitHub-ecosystem-endorsed Actions scanner (research Package Legitimacy
Audit). **FM-16, LOW, one required sentence:** this version pin is NOT covered by dependabot
(`pipx run` is not a manifest entry, and SEC-04 forbids adding zizmor to
`pipeline/requirements.txt`) and will not self-update -- state explicitly in the decision
record that the pinned zizmor version must be re-evaluated at each milestone close, or it will
sit at 1.10.0 indefinitely, silently missing every audit added after this phase.

**FM-10, MEDIUM, promoted to a BLOCKING phase-close item (not accepted-as-is):** record in
Task 3's decision-record subsection, as evidence rather than assertion: the `workflow_dispatch`
run URL of `ci.yml` (triggered once by the orchestrator per F-113/33-02's guard step), the
`lint-workflows` job's conclusion, and the specific log line showing zizmor's own version
banner actually printed -- a green job with the zizmor step skipped by a misconfigured `if:`
does not satisfy this. This is the only observation event proving A1 ("pipx ships preinstalled
on ubuntu-latest") true on a real runner rather than merely cited from GitHub's own image
manifest.

**FM-13, MEDIUM:** the phase-close pytest figure must be stated as an EQUALITY, not a floor.
Current suite (collected this session, matches STATE.md:300): 390 passed / 1 skipped /
1 xfailed = 392 collected. This phase adds exactly 2 new tests (Task 2's courtesy-delay
assertions) and changes zero test COUNTS elsewhere (Plan 33-01's `test_workflow_guards.py`
fix updates one literal, not the number of test cases). Close figure, stated as an equality so
a silently-deleted test elsewhere cannot hide behind "at least 390": **392 passed / 1 skipped
/ 1 xfailed** (390 + 2 new). The Phase 26 xfail is never "fixed."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: check-secret-hygiene.sh -- self-excluded, canary-proven, CF_HOOK-aware, comment-safe, mktemp+rc disciplined</name>
  <files>.github/scripts/check-secret-hygiene.sh, .github/scripts/fixtures/secret-hygiene-canary/canary-workflow.yml, .github/scripts/fixtures/secret-hygiene-canary/canary-script.sh, .github/scripts/fixtures/secret-hygiene-canary/canary_module.py, .github/workflows/ci.yml</files>
  <behavior>
    - Against the real tree today, INCLUDING the script's own existence: exits 0, no findings
      (FM-01 -- the script must not flag itself; proven by running it once it exists).
    - Against the committed canary fixture (scanned first, before the real tree): all four
      named findings fire -- echo/printf-of-secret, xtrace/command-echo, verbose-curl,
      botocore-DEBUG -- and the script aborts loudly if any one of the four does NOT fire
      (FM-06, mirrors lint-workflows.sh:24-30).
    - A planted `echo "$CF_HOOK"` line (in a scratch copy, never committed): fires, named as a
      possible secret exposure (FM-04 -- this is the alias every consumer actually reads).
    - A planted `run: curl -v "$CF_HOOK"` line: fires, named as a verbose-curl risk. A planted
      `# never add curl -v here -- it would print the hook URL` comment line: stays silent
      (FM-09, two-direction, MUST-fire vs MUST-stay-silent).
    - A planted bare `set -x` line in a script body: fires. A planted
      `# set -x is banned in this repo` comment line: stays silent (FM-09).
    - A planted `logging.basicConfig(level=logging.DEBUG)` in a scratch copy of a
      `pipeline/*.py` file: fires, named as a botocore-DEBUG log-hygiene risk.
    - Against an unreadable/mis-globbed target path: the explicit `mktemp` + `rc=$?` capture
      (NOT a process-substitution-wrapped `set -euo pipefail`, which FM-02 proved cannot see
      this) detects `rc >= 2` and hard-fails with an `::error::` naming the scan failure.
  </behavior>
  <action>
    Create `.github/scripts/check-secret-hygiene.sh` based on the research draft in
    `33-RESEARCH.md` section 4, with the following required corrections (none optional, per
    F-121):
    (1) FM-02/F-120: replace every `done < <(grep ...)` process-substitution idiom with the
    explicit `tmp="$(mktemp)"; trap 'rm -f "$tmp"' EXIT; set +e; grep ... >"$tmp"; rc=$?;
    set -e; [ "$rc" -ge 2 ] && { echo "::error::..."; exit 1; }; while IFS=: read -r file line
    content; do ...; done <"$tmp"` mechanism shown in `<interfaces>`, for each of the four
    checks. Never use `2>/dev/null || true`.
    (2) FM-01: every scanning grep must carry `--exclude=check-secret-hygiene.sh`; every
    `::error::` message string must be reworded so it does not contain the literal patterns
    being hunted (use "xtrace/command-echo enabled" and "verbose curl invocation" rather than
    strings containing the literal `set -x` / `curl -v` text).
    (3) FM-04: use the 8-name list from `<interfaces>` (includes `CF_HOOK` alongside
    `CF_DEPLOY_HOOK_URL`), with an in-script comment explaining `CF_HOOK` is the alias every
    consumer reads, not a repo secret name.
    (4) FM-09: for checks 2 (`set -x`/`set -o xtrace`) and 3 (`curl -v`/`--verbose`) only,
    skip lines whose first non-whitespace character is `#` before applying the pattern (do
    NOT apply this skip to checks 1 or 4).
    (5) FM-06: create `.github/scripts/fixtures/secret-hygiene-canary/canary-workflow.yml`
    and `.github/scripts/fixtures/secret-hygiene-canary/canary-script.sh` containing,
    between them, a `run: echo "$DEEPSEEK_API_KEY"`-shaped line, a bare `set -x`, and a
    `curl -v "$CF_HOOK"`-shaped line; and a fourth fixture file
    `.github/scripts/fixtures/secret-hygiene-canary/canary_module.py` containing a
    `logging.basicConfig(level=logging.DEBUG)` line. At the top of the script, scan ONLY the
    fixture directory first; if all four expected findings are not present, print
    `::error::secret-hygiene canary did not produce its expected findings -- instrument is not
    armed` and exit 1 before proceeding. Then scan the real tree
    (`.github/workflows/*.yml`, `.github/scripts/*.sh`, `pipeline/*.py` for check 4 only),
    excluding both the fixture directory and the script's own filename.
    Add one new step to `ci.yml`'s existing `lint-workflows` job (after whatever steps Plans
    33-01/33-02 already added there, per FM-07's wave ordering): `- name: Check secret
    log-hygiene (SEC-05, F-116/F-117/F-120)` running
    `bash .github/scripts/check-secret-hygiene.sh`.
  </action>
  <verify>
    <automated>bash .github/scripts/check-secret-hygiene.sh</automated>
  </verify>
  <done>
    Running the script against the real tree exits 0 with no `::error::` lines, having first
    scanned and confirmed all four findings fire against the committed canary fixture (FM-06).
    Both-direction falsification proven and recorded in the plan's summary: (a) FM-01 -- the
    script does not flag its own file (proven by the fact that it exits 0 while scanning
    `.github/scripts/*.sh`, which includes itself, via the `--exclude` mechanism); (b) FM-04 --
    a planted `echo "$CF_HOOK"` fires; (c) FM-09 -- a planted `curl -v`/`set -x` fires while
    the stated explanatory-comment shapes stay silent; (d) FM-02 -- a scan pointed at a
    nonexistent path exits 1 via the explicit `rc` check, not via a process-substitution
    `set -e` (which cannot see it). `.github/scripts/*.sh` (auto-shellchecked by
    `lint-workflows.sh` line 38) passes -- confirmed by running
    `bash .github/scripts/lint-workflows.sh` and observing no shellcheck error attributed to
    `check-secret-hygiene.sh`. Falsification (FM-05, exit-code gated, no laundering): the
    verify command above is the gate's own exit code with no `; echo` suffix.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 1.5s inter-feed courtesy delay in scrape_news.py, covered by an argument-list pytest assertion</name>
  <files>pipeline/scrape_news.py, pipeline/tests/test_scrape_news.py</files>
  <behavior>
    - With `FEEDS` patched to a 3-entry dict (matching this test file's existing
      `test_per_feed_failure_isolated` pattern) and `time.sleep` patched via
      `monkeypatch.setattr(sn.time, 'sleep', mock)` (FM-12 -- this patches the process-global
      `time` module, since `scrape_news.py` does `import time`, not `from time import sleep`;
      the two names ARE the same object and the test docstring must say so): after `main()`
      runs, assert `[c.args[0] for c in mock.call_args_list] == [REQUEST_DELAY] * 2` (FM-12 --
      an ordered argument-list comparison, not a bare `call_count`, so an unrelated future
      sleep on an un-mocked path is distinguishable from an extra feed-gap sleep).
    - With `FEEDS` patched to a single-entry dict: `mock.call_args_list == []` (never sleeps
      before the first feed, matching the `archive_r2.py` precedent's `if i > 0` guard).
  </behavior>
  <action>
    In `pipeline/scrape_news.py`: add `import time` near the existing stdlib imports (top of
    file, alongside `hashlib`/`json`/`logging`/etc.), and inside the `try:` block's local
    imports (where `FEEDS`, `fetch_feed`, etc. are already imported from
    `pipeline.news.feeds`), add `from pipeline.news.fulltext import REQUEST_DELAY` -- reuse
    the existing constant, do not redefine a second courtesy-delay value anywhere. Change the
    feed loop from `for feed_name, feed_url in FEEDS.items():` to
    `for i, (feed_name, feed_url) in enumerate(FEEDS.items()):` and add `if i > 0: time.sleep
    (REQUEST_DELAY)` as the very first statement inside the loop body, before the existing
    `try: entries = fetch_feed(...)` call. Preserve every other line of the loop body
    unchanged (the try/except, keyword filter, seen-URL dedup, and candidate-building logic
    all stay exactly as they are).
    In `pipeline/tests/test_scrape_news.py`: add a new test function
    `test_inter_feed_courtesy_delay` following the existing file's fixture conventions
    (`monkeypatch.setenv("OPENROUTER_API_KEY", ...)`, `monkeypatch.setenv("NEWS_DATA_DIR",
    ...)`, `patch("pipeline.news.feeds.FEEDS", ...)`, `patch("pipeline.news.feeds.fetch_feed",
    ...)`, mocked classifier/resolver/centroid matching `test_per_feed_failure_isolated`'s
    shape). Per FM-12: patch the process-global `time.sleep` via
    `monkeypatch.setattr(sn.time, "sleep", mock)` -- NOT via any claim of a
    `scrape_news`-local namespace distinct from the global `time` module, since `scrape_news`
    does `import time` (the module object), so `pipeline.scrape_news.time` IS `time`; state
    this explicitly in the test's docstring. Run `sn.main()`, then assert
    `[c.args[0] for c in mock.call_args_list] == [REQUEST_DELAY] * (len(FEEDS_dict) - 1)`
    (importing `pipeline.news.fulltext.REQUEST_DELAY` in the test to compare against the
    literal, not a hardcoded `1.5`). Add a second test function (or a second assertion path in
    the same test using a second `FEEDS` patch) confirming `mock.call_args_list == []` when
    `FEEDS` is patched to a single entry.

 -q</automated>
  </verify>
  <done>
    `pytest tests/test_scrape_news.py -q` reports all tests passing, exit 0, including the new
    courtesy-delay test(s) asserting the exact ordered argument list (FM-12 -- not a bare
    `call_count`). Falsification: reverting the `if i > 0: time.sleep(...)` line (or the
    import) makes the new test fail because `mock.call_args_list` no longer matches
    `[REQUEST_DELAY] * (len(FEEDS) - 1)` -- this must be demonstrated once during development,
    then the real fix restored, per F-110's two-direction requirement. The full
    `pipeline/tests/` suite is re-run in Task 3's phase-close gate to confirm no regression
    from the loop's `enumerate()` wrapping.
  </done>
</task>

<task type="auto">
  <name>Task 3: SHA-pin decision record + FM-16 zizmor-pin note + FM-10 blocking dispatch evidence + FM-13 equality phase-close gate</name>
  <files>DEPLOYMENT.md</files>
  <action>
    Add a new subsection to DEPLOYMENT.md's section 10 (Operational Notes), immediately
    after the existing "Workflow Cadences" table and before "Known Gaps", titled "### SHA-Pin
    and Version-Pin Decision Record (SEC-02, F-103)". Document all three reference classes
    named in F-103, one paragraph each:
    (a) the 13 actions/* uses: refs -- decision: SHA-pin with a # vN.N.N comment,
    rationale "GitHub-owned tags are force-moved by design on every patch release;
    .github/dependabot.yml's github-actions ecosystem (Plan 33-02) keeps these pins from
    rotting by opening a bump PR whenever the tag moves" -- reference check-sha-pins.sh as
    the enforcing gate;
    (b) the two fetch-lint-tools.sh binaries (actionlint v1.7.7, shellcheck v0.11.0) --
    decision: already SHA-256-pinned by Phase 32 (F-92), record that this decision predates
    and is unchanged by this phase, no re-pin performed here;
    (c) zizmor -- decision: version-pinned at 1.10.0, invoked ephemerally via pipx run /
    uvx (never added to pipeline/requirements.txt per SEC-04), no SHA applicable since it
    is not a uses:-style reference; cite it as the OWASP/GitHub-ecosystem-endorsed Actions
    scanner per the research's Package Legitimacy Audit. FM-16 (required, one sentence): state
    explicitly that this version pin is NOT covered by dependabot (pipx run is not a
    manifest entry, and SEC-04 forbids adding it to requirements.txt) and will not
    self-update -- re-evaluate the pinned zizmor version at each milestone close.
    In the same new subsection (or immediately following it), add the local zizmor invocation
    alongside the existing local-gate instruction already present in the CI workflow-cadence
    table's lint-workflows row ("run bash .github/scripts/lint-workflows.sh locally before
    pushing workflow changes"): add a sentence stating that
    uvx zizmor@1.10.0 --persona=regular .github/workflows/ should also be run locally before
    pushing workflow changes, using the exact same pinned version as CI's
    pipx run zizmor==1.10.0 step (Plan 33-02).
    FM-10 (required, BLOCKING): add a short "Zizmor CI Verification" note recording the
    orchestrator's one real workflow_dispatch of ci.yml (per 33-02 Task 2's guard step
    and F-113): the run URL, the lint-workflows job's conclusion, and the specific log line
    proving zizmor's version banner printed -- not merely "the job was green". This evidence
    must be filled in with the real run's data before the phase is considered closed; a
    placeholder or an unexecuted dispatch does not satisfy SEC-04's success criterion 4.
    Also add, in this same subsection or as a short adjacent note, a one-line confirmation
    that SEC-01/SEC-05's settings half was re-verified at phase close:
    default_workflow_permissions: "read", can_approve_pull_request_reviews: false, repo
    PUBLIC, secret_scanning: enabled, secret_scanning_push_protection: enabled, 0 alerts
    (F-107/F-111 -- state that this plan re-ran those specific API reads rather than re-doing
    the full history sweep, per F-107).
  </action>
  <verify>
    <automated>grep -c "SHA-Pin and Version-Pin Decision Record" DEPLOYMENT.md</automated>
  </verify>
  <done>
    DEPLOYMENT.md contains exactly one new subsection documenting all three reference classes
    from F-103 (uses: refs, lint-tool binaries, zizmor), the FM-16 zizmor-pin-not-covered
    sentence, the local zizmor invocation instruction, the FM-10 blocking dispatch evidence
    (real run URL, job conclusion, version-banner log line), and the SEC-01/SEC-05 settings
    re-verification note. Falsification (this grep's own exit code is the gate, no laundering):
    on the current tree this returns 0 matches so the command exits 1 -- it must exit 0 only
    after this task lands.

    Phase-close gate (FM-13, stated as an EQUALITY, not a floor, and NOT weakened): pytest
    395 passed / 1 skipped / 1 xfailed (390 + 2 new courtesy-delay tests from Task 2 + 3 new
    persist-credentials-regression tests from F-123's orchestrator addition to
    test_workflow_guards.py; the PRE-EXISTING test_workflow_guards.py case count is
    otherwise unchanged by Plan 33-01's fix -- only its literal was updated, not its case
    count), gated on the pytest process's own exit code;
    vitest 59 (unchanged -- this phase touches no frontend code); 16/16 validators; bash
    .github/scripts/lint-workflows.sh exit 0; bash .github/scripts/check-sha-pins.sh exit
    0; bash .github/scripts/check-secret-hygiene.sh exit 0; npx astro check 4 errors / 0
    warnings (pre-existing baseline, unchanged). The Phase 26 xfail is never "fixed." When
    counting pytest in a shell loop, remember *failed* also matches 1 xfailed -- gate on
    the exit code, never on a substring match against the summary line. FM-10's
    workflow_dispatch evidence (above) is a BLOCKING part of this same close gate, not a
    separate optional step.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Workflow log output -> anyone with read access to Actions logs (public repo) | Any secret value or transformed derivative reaching stdout/stderr in a workflow run is visible to the world, since this repo is public |
| Press/RSS host -> scraper | An unthrottled scraper hitting 5 Google News queries back-to-back is indistinguishable from abusive traffic from the host's perspective |
| A security gate's own source code -> the gate's own scan scope | A gate that scans the directory it lives in can flag itself, turning "the gate exists" into "the gate fails" (FM-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Information Disclosure | echo/printf/set -x/curl -v of a secret-bearing value (including the CF_HOOK alias) in .yml/.sh | mitigate | check-secret-hygiene.sh, four named risk shapes including CF_HOOK (FM-04), comment-aware (FM-09), wired into CI (Task 1) |
| T-33-08 | Information Disclosure | botocore wire-level DEBUG logging exposing R2_ACCESS_KEY_ID in a traceback | mitigate | Narrow assertion against set_stream_logger/sub-INFO basicConfig (Task 1, F-116/A3-derived) |
| T-33-09 | Denial of Service (courtesy) | 5 Google News queries fetched back-to-back with zero delay, same host | mitigate | 1.5s inter-feed delay, if i > 0 guarded, pytest-proven via exact argument-list assertion (Task 2, FM-12) |
| T-33-10 | Repudiation | A pin decision existing only in an agent's memory, undiscoverable later | mitigate | DEPLOYMENT.md decision record covering all three reference classes named in F-103, plus FM-16's zizmor-pin caveat (Task 3) |
| T-33-11 | Tampering (self-inflicted) | check-secret-hygiene.sh flagging itself, reading as a false positive that trains a maintainer to ignore the gate | mitigate | Self-exclusion (--exclude=check-secret-hygiene.sh) + reworded ::error:: strings that do not contain the hunted patterns (FM-01), verified via a committed canary fixture (FM-06) |
</threat_model>

<verification>
1. bash .github/scripts/check-secret-hygiene.sh -- exits 0 on the real tree (canary-proven first).
2. cd pipeline && python -m pytest tests/test_scrape_news.py -q -- exits 0, includes the new courtesy-delay test(s) asserting the exact argument list.
3. grep -c "SHA-Pin and Version-Pin Decision Record" DEPLOYMENT.md -- exits 0 (match found).
4. Full phase-close gate (run once, after all three plans in this phase land, exit codes gated, no ; echo laundering): cd pipeline && python -m pytest -q (395 passed / 1 skipped / 1 xfailed, equality per FM-13 + F-123), cd site && npm run build && npm run validate, npx astro check, bash .github/scripts/lint-workflows.sh, bash .github/scripts/check-sha-pins.sh, bash .github/scripts/check-secret-hygiene.sh -- all exit 0.
5. BLOCKING (FM-10): DEPLOYMENT.md's "Zizmor CI Verification" note is filled in with a real workflow_dispatch run URL, the lint-workflows job's conclusion, and the log line proving zizmor's version banner printed -- not a placeholder.
</verification>

<success_criteria>
check-secret-hygiene.sh ships wired into CI, self-excluded (FM-01), canary-proven (FM-06),
CF_HOOK-aware (FM-04), comment-line-safe on the two unanchored checks (FM-09), and disciplined
via an explicit mktemp+rc capture rather than an unworkable process-substitution set -e
claim (FM-02/F-120) -- proven both-directions against all four risk shapes.
scrape_news.py's 9-feed loop has a 1.5s inter-feed delay (never before the first feed),
reusing fulltext.REQUEST_DELAY, covered by a pytest regression asserting the exact ordered
delay-call argument list (FM-12), not a bare call count. DEPLOYMENT.md records a
per-reference-class SHA/version-pin decision for all three classes F-103 names, the FM-16
zizmor-pin-not-dependabot-covered caveat, and FM-10's blocking workflow_dispatch evidence. The
phase-close gate states pytest as an exact equality (392/1/1, FM-13), not a floor, and is
otherwise unweakened.
</success_criteria>

<output>
Create .planning/phases/33-security-posture/33-03-SUMMARY.md when done
</output>
