Fable review: APPROVED 2026-07-30 — after an Opus plan-check (9 blockers / 10 warnings, every <automated> gate executed against the real tree) and an Opus premortem (16 ranked failure modes). Both passes were applied INLINE by Fable rather than in a third planner round (F-22/F-26/F-34/F-44 precedent). Every gate edited here was re-verified by execution in BOTH directions — green on a compliant scratch implementation, red on a targeted mutation. Binding decisions for this phase: F-47..F-53 in STATE.md.

---
phase: 30-map-control-shell-rework
plan: 04
type: execute
wave: 3
depends_on: ["30-03"]
files_modified:
  - site/src/components/map/FilterSheet.tsx
  - site/src/components/map/map.css
autonomous: true
requirements: [MAPSH-02, MAPSH-03]
must_haves:
  truths:
    - "Below 480px, the FAB opens the filter sheet via dialog.showModal() (never the open attribute) and dialog.matches(':modal') === true while it is open"
    - "Closing the sheet (via its close button OR Escape/backdrop) returns focus to the FAB and sets aria-expanded=false on the FAB at BOTH the moment of initiation and the close event — never relying on the close event alone (iteration-1 lesson, F-45/spec section 6)"
    - "When a commune is selected at <=640px, the FAB is never occluded by .result-panel's 40vh bottom sheet — it is anchored above it via the existing .map-stage.has-panel hook, CSS-only, zero new JavaScript"
  artifacts:
    - path: "site/src/components/map/FilterSheet.tsx"
      provides: "FAB button data-role=\"filter-entry\" + <dialog id=\"filter-sheet\"> containing the three FilterFields components unwrapped (no details), opened via showModal()"
    - path: "site/src/components/map/map.css"
      provides: ".filter-fab base z-index 800 (set outside the media query too); .map-stage.has-panel .filter-fab bottom offset at <=640px; no z-index on the dialog itself (native top layer)"
  key_links:
    - from: "site/src/components/map/FilterSheet.tsx"
      to: "site/src/components/map/FilterFields.tsx"
      via: "import { YearField, FamilyField, ModeField } — same three components EntryPointsRail wraps in details, rendered flat inside the dialog body here"
      pattern: "from './FilterFields'"
    - from: "site/src/components/map/map.css"
      to: ".map-stage.has-panel (already computed today at MapIsland.tsx:413)"
      via: "@media (max-width:640px) .map-stage.has-panel .filter-fab { bottom: calc(40vh + 16px); }"
      pattern: "has-panel .filter-fab"
---

<objective>
Wave 3 of Phase 30. Builds the mobile (≤480px) filter entry point: a FAB that opens a native `<dialog>` bottom sheet via `showModal()`, containing the same three filter dimensions `EntryPointsRail` exposes at wider widths — but flat, not wrapped in `<details>`, since the dialog itself already provides the disclosure/dismissal semantics (spec §2/§4a).

Also lands the one required, previously-unresolved layout decision from spec §4a: the FAB × `.result-panel` collision. Research (Q4) found the CSS hook this needs — `.map-stage.has-panel` — is **already shipped** (`MapIsland.tsx:413`, added for other styling), so the fix is CSS-only: zero new JavaScript, zero new state, zero prop drilling.

This plan shares `map.css` with Plan 30-03 and therefore must run after it (wave 3, depends_on 30-03) — both plans add rules to the same file and Plan 30-03's `.legend`/`.mode-toggle` edits must land first to avoid a merge collision on the same regions of the stylesheet.

Purpose: complete MAPSH-02's "zero new JS dependencies" mobile requirement using only native `<dialog>`+`showModal()`, and close the one interaction spec §4a explicitly forbids leaving to ad-hoc implementation.

Output: `FilterSheet.tsx` and this wave's `map.css` additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-map-control-shell-rework/30-FABLE-CONSTRAINTS.md
@.planning/phases/30-map-control-shell-rework/30-RESEARCH.md
@.planning/phases/29-map-ux-design-loop/29-DESIGN-SPEC.md
@site/src/components/map/FilterFields.tsx
@site/src/components/map/map.css
@site/src/components/map/MapIsland.tsx
</context>

<interfaces>
From Plan 30-02: YearField/FamilyField/ModeField (site/src/components/map/FilterFields.tsx) — same signatures as used in Plan 30-03's EntryPointsRail.

Existing `.map-stage.has-panel` hook, already computed today (MapIsland.tsx:413, UNCHANGED by this plan):
  className={`map-stage${selected ? ' has-panel' : ''}`}

Existing `.result-panel` mobile geometry (map.css:463-474): fixed bottom sheet, 40vh height, at @media (max-width: 640px).

Sketch reference pattern for the dialog mechanics (iter-2/desktop.html:255-272, port the shape, not a literal copy):
  fab click -> sheet.showModal(); fab.setAttribute('aria-expanded','true')
  closeBtn click -> sheet.close(); fab.setAttribute('aria-expanded','false'); fab.focus()
  sheet 'close' event -> fab.setAttribute('aria-expanded','false')
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: FilterSheet.tsx — FAB + native dialog bottom sheet</name>
  <files>site/src/components/map/FilterSheet.tsx, site/src/components/map/map.css</files>
  <action>
    Create `FilterSheet.tsx` exporting `FilterSheet(props)` forwarding the same filter props as `EntryPointsRail` (`lang`, `year`, `onYearChange`, `crimeFamily`, `onFamilyChange`, `mode`, `onModeChange`). Do not add a `hasPanel` prop — the FAB-collision fix is CSS-only via the already-shipped `.map-stage.has-panel` ancestor class, so no new prop plumbing is needed for it.

    Render a `<button data-role="filter-entry" className="filter-fab" aria-label={map_filters_fab label} aria-haspopup="dialog" aria-expanded={false initially}>` and a `<dialog id="filter-sheet" aria-labelledby="filter-sheet-title">` containing a title heading, a close button, and the three `FilterFields` components rendered flat (no `<details>` wrapper — the dialog already provides disclosure). Use `useRef` for both the FAB button and the dialog element. Wire, in a `useEffect` on mount:
    - FAB click: `dialogRef.current.showModal()` (never set the `open` attribute — spec §5's explicit distinction, load-bearing for focus containment/Escape/backdrop) and immediately `fabRef.current.setAttribute('aria-expanded', 'true')`.
    - Close-button click: `dialogRef.current.close()`, `fabRef.current.setAttribute('aria-expanded', 'false')`, `fabRef.current.focus()`.
    - Dialog `'close'` event (fires on Escape and backdrop-click too, not only the close button): `fabRef.current.setAttribute('aria-expanded', 'false')` — this must be wired independently of the close-button handler, not assumed to be redundant, because relying on the close event alone was iteration 1's exact defect (`closeEventsFired: 0` — it never fired in that build) and setting it only on click-initiated close would repeat that failure in the opposite direction (never clearing `aria-expanded` on an Escape-close). Both paths must set the attribute.
    Clean up all three listeners on unmount.

    Add to `map.css`: `.filter-fab { position: fixed; right: 16px; bottom: 24px; z-index: 800; ... }` as the BASE rule (not only inside `@media (max-width: 480px)` — spec §3's explicit warning, same class of mistake as the news toggle). No z-index rule for `#filter-sheet` itself — a `<dialog>` opened via `showModal()` renders in the browser's top layer and giving it a z-index is a defect per spec §5 ("modal | none | modal `<dialog>` renders in the browser top layer; giving it a z-index is wrong").

    Add the FAB × `.result-panel` collision fix (spec §4a), CSS-only, using the already-shipped `.map-stage.has-panel` hook (`MapIsland.tsx:413`, verified — no JS change needed here):
      @media (max-width: 640px) { .map-stage.has-panel .filter-fab { bottom: calc(40vh + 16px); } }
    This makes the FAB "learn" the panel is open purely via the CSS descendant selector against a class the existing React code already toggles — zero new state, zero `ResizeObserver`/`MutationObserver`, zero prop drilling (Q4's finding).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && node -e "const fs=require('fs'); const tsx=fs.readFileSync('src/components/map/FilterSheet.tsx','utf8'); const css=fs.readFileSync('src/components/map/map.css','utf8'); const checks=[[tsx.includes('showModal('),'showModal() call missing'],[!/<dialog[^>]*\bopen\b/.test(tsx),'dialog must never use the open attribute'],[tsx.includes(\"'close'\")||tsx.includes('\"close\"'),'dialog close event listener missing'],[(tsx.match(/setAttribute\([^)]*aria-expanded/g)||[]).length>=3,'aria-expanded must be set via setAttribute at THREE sites: FAB-click (true), close-button click (false), and the dialog close event (false) — a JSX aria-expanded attribute does not count'],[/\.focus\(\)/.test(tsx),'close handler must return focus to the FAB (spec section 6)'],[tsx.includes('data-role=\"filter-entry\"'),'FAB missing data-role filter-entry'],[/\.filter-fab\s*\{[^}]*z-index:\s*800/.test(css),'filter-fab base z-index 800 missing'],[/has-panel\s+\.filter-fab/.test(css),'FAB collision rule (.has-panel .filter-fab) missing'],[!/#filter-sheet\s*\{[^}]*z-index/.test(css),'dialog must not carry a z-index rule']]; const fails=checks.filter(function(c){return !c[0];}).map(function(c){return c[1];}); if(fails.length){console.error('FAIL: '+fails.join('; ')); process.exit(1);} console.log('PASS FilterSheet contract + CSS present');"</automated>
  </verify>
  <done>`FilterSheet.tsx` opens `#filter-sheet` via `showModal()` (never `open`), sets `aria-expanded` on the FAB from both the click handler and the dialog's `close` event, and `map.css` gives `.filter-fab` an explicit base z-index 800 plus the `.map-stage.has-panel .filter-fab` collision rule with no z-index on the dialog itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| FAB click / dialog close events | Client-side UI state only, no network/data boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-30-04 | Denial of Service (of keyboard/focus usability) | `<dialog>` modal focus containment | accept | Native `showModal()` provides browser-implemented focus containment, Escape-to-close, and backdrop — no custom focus-trap code is written, so there is no custom logic to have a bug in; this is exactly why MAPSH-02 mandates the native primitive instead of a hand-rolled modal |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; verified in Plan 30-06 |
</threat_model>

<verification>
The automated gate asserts `showModal(` is present, the literal `open` attribute is never present on the `<dialog>` tag, `aria-expanded` is set from at least 2 distinct code sites (click handler + close-event handler — a single-site implementation fails this count), the FAB carries `data-role="filter-entry"`, `.filter-fab`'s base rule (outside any media query) has `z-index: 800`, the `.has-panel .filter-fab` collision selector exists, and no `z-index` rule targets `#filter-sheet`. Real interactive proof — `dialog.matches(':modal') === true` while open, focus actually returning to the FAB, the FAB actually clearing `.result-panel` with a commune selected at ≤640px — is settled in Plan 30-06's ORCHESTRATOR-INLINE **step 5b**, which was added specifically because the first draft deferred these three to a step that did not exist. Note for that step: with a commune selected, score the FAB collision with the `elementFromPoint` topmost check ONLY — never with `verdict()`, because `.family-bar-label` (`map.css:538-548`) truncates by design when `ResultPanel` is open and would poison `score.mjs`'s document-wide overflow scan (premortem §2).
</verification>

<success_criteria>
- `FilterSheet.tsx` exists, exports the component, and satisfies every regex assertion above.
- `map.css`'s `.filter-fab` and `.has-panel .filter-fab` rules exist; the dialog itself carries no z-index.
- Not yet wired into `MapTopbar.tsx` (Plan 30-05) — this plan's diff is additive-only.
</success_criteria>

<output>
Create `.planning/phases/30-map-control-shell-rework/30-04-SUMMARY.md` when done.
</output>
