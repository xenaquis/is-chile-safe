---
phase: 14-homicide-as-a-first-class-category
plan: 05
type: execute
wave: 4
depends_on: ["14-04"]
files_modified:
  - site/src/components/map/FiltersRow.tsx
  - site/src/components/map/MapIsland.tsx
autonomous: true
requirements: [HOM-01]
must_haves:
  truths:
    - "An 8th 'homicidios' chip appears in FiltersRow with the featured accent-dot, an honest 'subgroup of vida' tooltip, and familyIndex: null (D-04)"
    - "Selecting the homicide chip recolors the choropleth with the independent homicide scale via buildStyleMapFromHomicide; deselecting/selecting a family restores the family/level scale"
    - "The homicide layer survives a year change (the year effect also branches on the homicide state)"
    - "Chip selection stays exclusive — selecting homicide deactivates any active family chip"
  artifacts:
    - path: "site/src/components/map/FiltersRow.tsx"
      provides: "8th CHIP_DEFS entry { key: 'homicidios', familyIndex: null, featured: true }"
      contains: "homicidios"
    - path: "site/src/components/map/MapIsland.tsx"
      provides: "crimeIsHomicide state + three-way choropleth branch in chip effect AND year effect"
      contains: "crimeIsHomicide"
  key_links:
    - from: "site/src/components/map/FiltersRow.tsx onFamilyChange('homicidios', null)"
      to: "site/src/components/map/MapIsland.tsx handleFamilyChange → setCrimeIsHomicide"
      via: "chip click callback"
      pattern: "crimeIsHomicide"
    - from: "site/src/components/map/MapIsland.tsx chip effect"
      to: "buildStyleMapFromHomicide"
      via: "crimeIsHomicide branch"
      pattern: "buildStyleMapFromHomicide"
---

<objective>
Wire the homicide filter into the live map (HOM-01, D-04): an 8th featured chip in
FiltersRow, and a three-way choropleth branch in MapIsland (homicide → family →
level) in both the chip-recolor effect and the year-change effect, with a new
crimeIsHomicide state and exclusive selection. Consumes the contracts built in
14-04 (buildStyleMapFromHomicide, extended CommunaPayload, homicidios defs).

Purpose: Makes homicide a selectable first-class layer with its own choropleth,
distinct from the aggregated `vida` family — the core HOM-01 user-facing behavior.
Output: A working homicide chip that recolors the map on its own scale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md
@.planning/phases/14-homicide-as-a-first-class-category/14-04-SUMMARY.md

<interfaces>
<!-- From 14-04 (now exists) -->
buildStyleMapFromHomicide(comunas: CommunaPayload[]): Map<string, L.PathOptions>
interface CommunaPayload { id; rate; level; by_family; homicide_rate; homicide_count }
FAMILY_DEFS_EN['homicidios'] / FAMILY_DEFS_ES['homicidios']   # tooltip source

<!-- From site/src/components/map/FiltersRow.tsx (CHIP_DEFS lines 30-39) -->
type ChipDef = { key: string|null; familyIndex: number|null; labelEs: string; labelEn: string; featured: boolean }
# chip click: onFamilyChange(isActive ? null : key, isActive ? null : familyIndex)
# tooltip title lookup at line 80: FAMILY_DEFS_EN[key] / FAMILY_DEFS_ES[key]

<!-- From site/src/components/map/MapIsland.tsx -->
const [crimeFamily, setCrimeFamily] = useState<string|null>(null)        # ~line 95
const [crimeFamilyIndex, setCrimeFamilyIndex] = useState<number|null>(null)
# chip recolor effect lines 277-290 (branches crimeFamilyIndex !== null vs level)
# year effect lines 229-272 (recomputes styleMap on year change, lines 258-263)
# import block from './ChoroplethLayer' lines 24-29
buildStyleMapFromLevel, buildStyleMapFromFamily, applyStyleMap, highlightSelected
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 8th homicide chip to FiltersRow CHIP_DEFS</name>
  <files>site/src/components/map/FiltersRow.tsx</files>
  <read_first>
    - site/src/components/map/FiltersRow.tsx — CHIP_DEFS array (lines 30-39), chip rendering + featured accent-dot (lines 78-98), tooltip title lookup (line 80)
    - .planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md §"FiltersRow.tsx — Add 8th chip" (lines 292-317) — the exact entry + design decisions
    - .planning/phases/14-homicide-as-a-first-class-category/14-RESEARCH.md §"Common Pitfalls" Pitfall 4 (featured dot must not imply 8th sibling family)
  </read_first>
  <action>
    Append one entry to CHIP_DEFS in site/src/components/map/FiltersRow.tsx after the
    `armas` chip: { key: 'homicidios', familyIndex: null, labelEs: 'Homicidios',
    labelEn: 'Homicide', featured: true }. familyIndex: null signals the non-by_family
    homicide code path to MapIsland; featured: true renders the accent-dot consistent
    with vida/propiedad. The existing chip render + onFamilyChange callback handle the
    new entry without change — it passes ('homicidios', null) to MapIsland on click.
    The title tooltip resolves via FAMILY_DEFS_EN['homicidios']/['homicidios'] (added in
    14-04), which honestly frames it as "subgroup 101 within Life Crimes" — do NOT label
    the chip "Vida con homicidios" (Pitfall 4).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('site/src/components/map/FiltersRow.tsx','utf8'); if(!/key:\s*'homicidios'/.test(f)) process.exit(1); console.log('homicide chip present')"</automated>
  </verify>
  <acceptance_criteria>
    - site/src/components/map/FiltersRow.tsx CHIP_DEFS contains `{ key: 'homicidios', familyIndex: null, ... featured: true }`
    - The chip label is 'Homicidios'/'Homicide', not a vida-aggregate label
    - `cd site && npx tsc --noEmit` reports no error in FiltersRow.tsx
  </acceptance_criteria>
  <done>The homicide chip renders as a featured 8th chip with an honest tooltip.</done>
</task>

<task type="auto">
  <name>Task 2: Branch MapIsland choropleth on crimeIsHomicide (chip effect + year effect)</name>
  <files>site/src/components/map/MapIsland.tsx</files>
  <read_first>
    - site/src/components/map/MapIsland.tsx — state declarations (lines 94-103), chip recolor effect (lines 277-290), year effect (lines 229-272, styleMap recompute lines 258-263), ChoroplethLayer import block (lines 24-29), the handleFamilyChange / onFamilyChange handler
    - .planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md §"MapIsland.tsx — Add homicide state + branch" (lines 397-482) — exact state + three-way branch + import + handler
  </read_first>
  <action>
    In site/src/components/map/MapIsland.tsx: add `buildStyleMapFromHomicide` to the
    import from './ChoroplethLayer' (lines 24-29). Add a new state
    `const [crimeIsHomicide, setCrimeIsHomicide] = useState(false)` after
    crimeFamilyIndex. In the handler that FiltersRow's onFamilyChange calls
    (handleFamilyChange), set setCrimeIsHomicide(key === 'homicidios') alongside the
    existing setCrimeFamily/setCrimeFamilyIndex calls — keeping selection exclusive
    (selecting homicide passes index null, deactivating any family). Convert BOTH the
    chip recolor effect (lines 277-290) AND the year effect's styleMap recompute
    (lines 258-263) to a three-way branch: if crimeIsHomicide →
    buildStyleMapFromHomicide(communas); else if crimeFamilyIndex !== null →
    buildStyleMapFromFamily(communas, crimeFamilyIndex); else →
    buildStyleMapFromLevel(communas). Add crimeIsHomicide to the chip effect's
    dependency array. This ensures the homicide layer survives a year change.
  </action>
  <verify>
    <automated>cd site && npx tsc --noEmit 2>&1 | grep -i "MapIsland" && exit 1 || node -e "const f=require('fs').readFileSync('site/src/components/map/MapIsland.tsx','utf8'); if(!/crimeIsHomicide/.test(f)||!/buildStyleMapFromHomicide/.test(f)) process.exit(1); console.log('MapIsland branch wired')"</automated>
  </verify>
  <acceptance_criteria>
    - MapIsland.tsx imports buildStyleMapFromHomicide and declares crimeIsHomicide state
    - Both the chip effect and the year effect use the three-way homicide→family→level branch
    - crimeIsHomicide is in the chip effect dependency array
    - handleFamilyChange sets crimeIsHomicide = (key === 'homicidios'); selection stays exclusive
    - `cd site && npx tsc --noEmit` reports no error in MapIsland.tsx
  </acceptance_criteria>
  <done>Selecting the homicide chip recolors the map on the independent homicide scale and survives year changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chip event → choropleth recolor | Internal client event; payload values pre-clamped at pipeline time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Tampering | MapIsland reading homicide_rate via buildStyleMapFromHomicide | mitigate | buildStyleMapFromHomicide reads `?? 0`; a null/missing homicide field degrades to the lightest bucket, never throws; no untrusted input crosses here |
| T-14-12 | Denial of service (client) | choropleth recompute on chip/year change | accept | Same in-place setStyle path as existing family/level recolor (no re-mount, L.canvas scoped); no new perf surface — 346 polygons within Leaflet envelope |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages — uses existing ChoroplethLayer/colors exports; no install task |
</threat_model>

<verification>
- `cd site && npx tsc --noEmit` passes for FiltersRow.tsx + MapIsland.tsx
- homicidios chip present; crimeIsHomicide + buildStyleMapFromHomicide wired in both effects
</verification>

<success_criteria>
- 8th featured homicide chip with honest tooltip (D-04)
- Independent homicide choropleth on selection, surviving year changes (D-05)
- Exclusive selection preserved
</success_criteria>

<output>
Create `.planning/phases/14-homicide-as-a-first-class-category/14-05-SUMMARY.md` when done.
</output>
