---
phase: 14-homicide-as-a-first-class-category
plan: 03
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - site/scripts/validate/map.mjs
  - site/public/data/cead
autonomous: true
requirements: [HOM-01]
must_haves:
  truths:
    - "The build validator fails if homicide data is missing from the map-payload or from a sampled commune file — the data half is gated before the UI half consumes it"
    - "The synced site/public mirror carries the homicide data so the dev server and build see it"
  artifacts:
    - path: "site/scripts/validate/map.mjs"
      provides: "Check 8 (homicide_rate in map-payload) + Check 9 (Santiago featured_rates.homicidios non-empty)"
      contains: "homicide_rate"
  key_links:
    - from: "site/scripts/validate/map.mjs Check 8"
      to: "data/cead/map-payload.json"
      via: "JSON.parse + sample homicide_rate presence"
      pattern: "homicide_rate"
    - from: "site/scripts/validate/map.mjs Check 9"
      to: "data/cead/comunas/13101.json"
      via: "featured_rates.homicidios non-empty assertion"
      pattern: "featured_rates"
---

<objective>
Gate Half A (data) before Half B (UI) consumes it: add build-validator assertions
that homicide data is present and keyed correctly in the map-payload and in a
sampled commune file, and ensure the data is mirrored into site/public so the dev
server and build see it. If 14-02's scrape did not actually populate homicide data,
these checks must fail loudly here — not silently in the UI.

Purpose: A regression guard so the homicide layer/panel is never built against
empty data. Mirrors the project's existing pattern of validating CEAD data presence
after every scrape (STATE Critical Pitfalls: "CEAD endpoint silently returning wrong
data — validate key presence after every scrape").
Output: Two new validator checks + a synced public data mirror.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md
@.planning/phases/14-homicide-as-a-first-class-category/14-02-SUMMARY.md

<interfaces>
<!-- From site/scripts/validate/map.mjs (analog: Check 1 lines 48-93) -->
pass(label)                # console.log PASS
fail(label, msg)           # console.error FAIL; failures++
REPO_ROOT                  # repo root path constant
existsSync, readFileSync   # node fs; JSON.parse pattern
# existing checks: TopoJSON budget, map page, island markers, aria-labels, zero-React guard, runtime data assets

<!-- Data contracts produced by 14-02 -->
# data/cead/map-payload.json: { comunas: [{ id, rate, level, by_family, homicide_rate, homicide_count? }] }
# data/cead/comunas/13101.json: { featured_rates: { homicidios: {year: rate}, homicidios_count: {year: count} } }

<!-- sync-data.mjs mirrors data/cead -> site/public/data/cead at predev/prebuild (Phase 3 BLOCKER fix) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Check 8 + Check 9 to map.mjs validator</name>
  <files>site/scripts/validate/map.mjs</files>
  <read_first>
    - site/scripts/validate/map.mjs — Check 1 pattern (lines 48-93: existsSync guard, JSON.parse, pass/fail) and the pass/fail helpers (lines 36-43)
    - .planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md §"map.mjs — Add Check 8 + Check 9" (lines 635-678) — the exact check template
    - .planning/phases/14-homicide-as-a-first-class-category/14-02-SUMMARY.md — whether homicide_count made it into the payload (so Check 8 asserts the right keys)
  </read_first>
  <action>
    Append Check 8 and Check 9 to site/scripts/validate/map.mjs after the existing
    checks, mirroring the Check 1 existsSync/JSON.parse/pass/fail pattern. Check 8:
    load data/cead/map-payload.json (path.join(REPO_ROOT,'data','cead','map-payload.json')),
    sample the first 10 comunas, and fail('homicide in payload', ...) if none have a
    `homicide_rate` key (message: "map-payload.json comunas missing homicide_rate —
    subgroup-101 scrape not run"), else pass('homicide_rate in map-payload'). Check 9:
    load data/cead/comunas/13101.json and fail('homicide in 13101.json', ...) if
    featured_rates.homicidios has zero keys (message: "Santiago featured_rates.homicidios
    is empty — subgroup-101 scrape needed"), else pass with the year count. Guard both
    with existsSync so the validator does not crash when a file is absent (it should
    fail with a message, consistent with existing checks). Use `in`/Object.keys length,
    not truthiness, so a confirmed 0 rate still counts as present (Pitfall 5).
  </action>
  <verify>
    <automated>node site/scripts/validate/map.mjs 2>&1 | grep -E "homicide_rate in map-payload|homicide in 13101"</automated>
  </verify>
  <acceptance_criteria>
    - site/scripts/validate/map.mjs contains a Check 8 referencing `homicide_rate` and a Check 9 referencing `featured_rates.homicidios`
    - Running the validator prints PASS for both checks against the 14-02 data
    - Both checks use existsSync guards and Object.keys length (not truthiness)
  </acceptance_criteria>
  <done>The build validator enforces homicide-data presence in both the payload and a commune file.</done>
</task>

<task type="auto">
  <name>Task 2: Sync data to site/public and run the full map validator green</name>
  <files>site/public/data/cead</files>
  <read_first>
    - site/scripts/sync-data.mjs — predev/prebuild mirror of data/cead → site/public/data/cead (Phase 3 BLOCKER fix; STATE Session Continuity)
    - site/package.json — the validate / sync scripts (predev, prebuild, validate commands)
    - .planning/STATE.md — OneDrive dist/ desync: chain build+validate in ONE command
  </read_first>
  <action>
    Run sync-data (`node site/scripts/sync-data.mjs` or `npm run sync-data` from site/)
    so the homicide-populated data/cead JSON is mirrored to site/public/data/cead,
    then run the full map validator (`node site/scripts/validate/map.mjs`) and confirm
    all checks including the new Check 8/9 pass. Because the repo lives in OneDrive,
    chain the sync and validation in a single command invocation so the mirrored files
    do not desync between processes. Do not run a full site build here (that happens in
    Half B) — only the data mirror + validator.
  </action>
  <verify>
    <automated>node site/scripts/validate/map.mjs 2>&1 | grep -E "FAIL" && exit 1 || echo "ALL MAP CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - site/public/data/cead/map-payload.json exists and contains homicide_rate
    - `node site/scripts/validate/map.mjs` exits with zero FAIL lines (all checks pass)
  </acceptance_criteria>
  <done>The homicide data is mirrored to the public dir and passes the full map validator — Half B is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed data/cead JSON → build/frontend | The validator is the integrity gate before the static frontend trusts the payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering (data integrity) | map-payload / commune JSON consumed by the frontend | mitigate | Check 8/9 fail the build if homicide data is missing or empty-keyed — prevents shipping a homicide layer/panel backed by absent data |
| T-14-08 | Information disclosure | site/public mirror | accept | The mirror contains the same public CEAD-derived data as data/cead; no secrets; no new exposure |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages — validator uses node built-in fs/path only; no install task |
</threat_model>

<verification>
- `node site/scripts/validate/map.mjs` passes Check 8 + Check 9 and all prior checks
- site/public/data/cead/map-payload.json mirrors the homicide_rate field
</verification>

<success_criteria>
- Build validator gates homicide-data presence (payload + sampled commune)
- Public data mirror carries homicide data; full map validator green
</success_criteria>

<output>
Create `.planning/phases/14-homicide-as-a-first-class-category/14-03-SUMMARY.md` when done.
</output>
