---
phase: 14-homicide-as-a-first-class-category
plan: 04
type: execute
wave: 3
depends_on: ["14-03"]
files_modified:
  - site/src/components/map/ChoroplethLayer.ts
  - site/src/lib/familyDefs.ts
  - site/src/lib/data.ts
autonomous: true
requirements: [HOM-01]
must_haves:
  truths:
    - "An independent homicide choropleth style function exists that buckets non-zero homicide rates on their own quantile scale (D-05), with zero/null comunas mapped to the lightest bucket and visually distinguished"
    - "The CommunaPayload type carries homicide_rate and homicide_count, so the choropleth and wiring code are type-safe contracts"
    - "Bilingual (EN+ES) homicide label and a sober, CEAD-cited, subgroup-honest definition exist for the chip tooltip"
  artifacts:
    - path: "site/src/components/map/ChoroplethLayer.ts"
      provides: "buildStyleMapFromHomicide(comunas) + CommunaPayload.homicide_rate/homicide_count"
      contains: "buildStyleMapFromHomicide"
    - path: "site/src/lib/familyDefs.ts"
      provides: "homicidios key in FAMILY_LABELS_EN/ES + FAMILY_DEFS_EN/ES"
      contains: "homicidios"
    - path: "site/src/lib/data.ts"
      provides: "CommuneData.featured_rates.homicidios_count type"
      contains: "homicidios_count"
  key_links:
    - from: "site/src/components/map/ChoroplethLayer.ts::buildStyleMapFromHomicide"
      to: "computeQuantileBreaks / levelFromBreaks (./colors)"
      via: "non-zero rate quantile bucketing"
      pattern: "computeQuantileBreaks"
---

<objective>
Build the Half-B contract layer the wiring plan (14-05) consumes (HOM-01, D-05):
an independent homicide choropleth style function on its own quantile scale, the
typed CommunaPayload extension, and the bilingual homicide label/definition. This
is interface-first: 14-05 wires the chip + MapIsland against these exact exports
and types, no exploration needed.

Purpose: Homicide rates are an order of magnitude smaller than family rates (single
digits vs hundreds /100k); reusing the shared 5-bucket scale would collapse every
comuna into the lowest buckets. D-05 mandates an independent scale. Doing the type +
function + defs first prevents the wiring plan from scavenging contracts.
Output: buildStyleMapFromHomicide, extended CommunaPayload, EN/ES homicide strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-homicide-as-a-first-class-category/14-RESEARCH.md
@.planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md

<interfaces>
<!-- From site/src/components/map/ChoroplethLayer.ts (analog: buildStyleMapFromFamily lines 86-105) -->
buildStyleMapFromFamily(comunas: CommunaPayload[], familyIndex: number): Map<string, L.PathOptions>
buildStyleMapFromLevel(comunas: CommunaPayload[]): Map<string, L.PathOptions>
interface CommunaPayload { id: string; rate: number; level: 1|2|3|4|5; by_family: number[] }  // lines 13-18

<!-- From site/src/components/map/colors.ts (already imported in ChoroplethLayer) -->
computeQuantileBreaks(rates: number[], n: number): number[]
levelFromBreaks(rate: number, breaks: number[]): number
INCIDENCE_COLORS: string[]   // 5-level teal→red scale; INCIDENCE_COLORS[0] = lightest

<!-- From site/src/lib/familyDefs.ts -->
FAMILY_LABELS_EN, FAMILY_LABELS_ES, FAMILY_DEFS_EN, FAMILY_DEFS_ES   // Record<string,string>
FAMILY_ORDER   # do NOT add homicidios here (Phase 15 SEO uses it)

<!-- From site/src/lib/data.ts (line 44) -->
CommuneData.featured_rates: { propiedad; homicidios; secuestros }   # add homicidios_count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildStyleMapFromHomicide + extend CommunaPayload</name>
  <files>site/src/components/map/ChoroplethLayer.ts</files>
  <read_first>
    - site/src/components/map/ChoroplethLayer.ts — buildStyleMapFromFamily (lines 86-105) + CommunaPayload interface (lines 13-18) + the existing ./colors imports (line 11)
    - .planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md §"ChoroplethLayer.ts — Add buildStyleMapFromHomicide" (lines 320-394) — the exact structural copy
    - .planning/phases/14-homicide-as-a-first-class-category/14-RESEARCH.md §"Choropleth Bucketing for Homicide Rates (D-05)" — non-zero quantile rationale + zero-comune display treatment
  </read_first>
  <action>
    In site/src/components/map/ChoroplethLayer.ts, extend the CommunaPayload interface
    with `homicide_rate: number | null` and `homicide_count: number | null` (the count
    may be absent if 14-02 dropped it for budget — code that reads it must use `?? null`).
    Add an exported buildStyleMapFromHomicide(comunas: CommunaPayload[]):
    Map<string, L.PathOptions> mirroring buildStyleMapFromFamily but reading
    `c.homicide_rate ?? 0` instead of `c.by_family[familyIndex] ?? 0`. Compute
    breaks via computeQuantileBreaks(positiveRates, 5) where positiveRates filters
    rate > 0 (independent scale, D-05); map each comuna to INCIDENCE_COLORS[level-1]
    via levelFromBreaks; set fillOpacity to 0.25 when rate === 0 (visually distinguish
    zero-homicide comunas) else 0.55; color '#ffffff', weight 1.2. No new imports —
    computeQuantileBreaks and levelFromBreaks are already imported from ./colors.
  </action>
  <verify>
    <automated>cd site && npx tsc --noEmit 2>&1 | grep -i "ChoroplethLayer\|buildStyleMapFromHomicide" && exit 1 || echo "TS OK"</automated>
  </verify>
  <acceptance_criteria>
    - site/src/components/map/ChoroplethLayer.ts exports `buildStyleMapFromHomicide`
    - CommunaPayload interface includes `homicide_rate` and `homicide_count`
    - buildStyleMapFromHomicide filters to positive rates for the quantile breaks (independent scale)
    - Zero-rate comunas get fillOpacity 0.25; `cd site && npx tsc --noEmit` reports no error in this file
  </acceptance_criteria>
  <done>An independent homicide choropleth function and the typed payload contract exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add bilingual homicidios label + definition + data.ts type</name>
  <files>site/src/lib/familyDefs.ts, site/src/lib/data.ts</files>
  <read_first>
    - site/src/lib/familyDefs.ts — FAMILY_LABELS_EN/ES + FAMILY_DEFS_EN/ES (the `vida` entry pattern, lines 18-59) and FAMILY_ORDER (line 7)
    - .planning/phases/14-homicide-as-a-first-class-category/14-PATTERNS.md §"familyDefs.ts — Add homicidios key" (lines 563-599) — the exact strings + the "do NOT add to FAMILY_ORDER" note
    - site/src/lib/data.ts — CommuneData.featured_rates type (lines 44-48)
    - CLAUDE.md — editorial constraint: never label "peligroso/seguro"; cite CEAD; i18n parity EN+ES
  </read_first>
  <action>
    In site/src/lib/familyDefs.ts add a `homicidios` key to all four dicts:
    FAMILY_LABELS_EN['homicidios']='Homicide'; FAMILY_LABELS_ES['homicidios']='Homicidios';
    FAMILY_DEFS_EN['homicidios']='Reported homicide cases disaggregated by commune,
    sourced from CEAD official police statistics (subgroup 101 within the Life Crimes
    family).'; FAMILY_DEFS_ES['homicidios']='Casos de homicidio reportados desagregados
    por comuna, según estadísticas policiales oficiales del CEAD (subgrupo 101 dentro de
    Delitos contra la Vida).'. The definition must be sober, CEAD-cited, and frame
    homicide honestly as a SUBGROUP of vida (not a parallel family) — no "dangerous/safe"
    language. Do NOT add `homicidios` to FAMILY_ORDER (it is for Phase 15 SEO pages; the
    chip tooltip looks up FAMILY_DEFS_*[key] which are open Record<string,string>).
    In site/src/lib/data.ts, extend CommuneData.featured_rates with
    `homicidios_count: Record<string, number>` alongside the existing homicidios key.
  </action>
  <verify>
    <automated>cd site && npx tsc --noEmit 2>&1 | grep -i "familyDefs\|data.ts" && exit 1 || node -e "const f=require('fs').readFileSync('src/lib/familyDefs.ts','utf8'); if(!/homicidios/.test(f)) process.exit(1); console.log('homicidios present')"</automated>
  </verify>
  <acceptance_criteria>
    - site/src/lib/familyDefs.ts contains `homicidios` in FAMILY_LABELS_EN, FAMILY_LABELS_ES, FAMILY_DEFS_EN, FAMILY_DEFS_ES (both locales, sober + CEAD-cited)
    - `homicidios` is NOT added to FAMILY_ORDER
    - site/src/lib/data.ts CommuneData.featured_rates includes `homicidios_count: Record<string, number>`
    - `cd site && npx tsc --noEmit` reports no error in these files
  </acceptance_criteria>
  <done>Bilingual homicide strings and the count type contract exist for the chip + panel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static JSON payload → React island | The island reads payload fields; types enforce the shape but values are pre-clamped at pipeline time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering | buildStyleMapFromHomicide reading homicide_rate | mitigate | Read via `?? 0`/`?? null` so a missing/null field degrades to the lightest bucket rather than throwing; values are integers clamped in the pipeline (T-14-03/04) — no untrusted string reaches styling |
| T-14-10 | Information disclosure | homicide definition strings | accept | Static editorial copy; no user input; CEAD-cited per editorial policy |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages — chroma-js/Leaflet/React already installed (RESEARCH §Standard Stack); no install task |
</threat_model>

<verification>
- `cd site && npx tsc --noEmit` passes for ChoroplethLayer.ts, familyDefs.ts, data.ts
- buildStyleMapFromHomicide exported; CommunaPayload extended; homicidios strings present EN+ES
</verification>

<success_criteria>
- Independent homicide choropleth function on a non-zero quantile scale (D-05)
- Typed CommunaPayload contract for 14-05 to wire against
- Bilingual sober CEAD-cited homicide label/definition
</success_criteria>

<output>
Create `.planning/phases/14-homicide-as-a-first-class-category/14-04-SUMMARY.md` when done.
Note whether homicide_count survived into the payload (affects how 14-05/14-06 read it).
</output>
