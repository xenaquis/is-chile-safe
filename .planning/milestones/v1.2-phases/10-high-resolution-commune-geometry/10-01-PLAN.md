---
phase: 10-high-resolution-commune-geometry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-topojson.mjs
  - site/scripts/validate/map.mjs
autonomous: true
requirements: [GEO-01, GEO-02]

must_haves:
  truths:
    - "Build script rejects any output that is missing, orphan, or duplicate CUTs vs index.json (not just a count + Santiago check)"
    - "Build script and validator both accept the new size budget (≤420 KB raw AND ≤140 KB gzip) and reject anything over it"
    - "The stale 100 KB raw gate no longer blocks recognizable geometry"
  artifacts:
    - path: "scripts/build-topojson.mjs"
      provides: "Extended Step-5 integrity assertion (missing/orphan/dup/Santiago) + dual raw+gzip budget assertion"
      contains: "missing"
    - path: "site/scripts/validate/map.mjs"
      provides: "Raised TOPO_MAX_BYTES + gzip-size assertion"
      contains: "gzip"
  key_links:
    - from: "scripts/build-topojson.mjs"
      to: "data/cead/meta/index.json"
      via: "Set of c.cut compared against feature properties.id"
      pattern: "index"
    - from: "site/scripts/validate/map.mjs"
      to: "data/cead/geo/communes.topo.json"
      via: "raw statSync size + zlib.gzipSync length assertions"
      pattern: "gzipSync"
---

<objective>
Harden the build + validator gates BEFORE the source swap lands, so a new high-fidelity asset cannot ship un-gated. Two changes: (1) extend the in-script Step-5 integrity assertion in `scripts/build-topojson.mjs` to explicitly check missing + orphan + duplicate CUTs against `data/cead/meta/index.json` (today it only checks feature count + Santiago presence), and (2) replace the stale `<100 KB raw` budget — the documented bug that hard-fails any recognizable geometry — with a dual budget (≤420 KB raw AND ≤140 KB gzip) in BOTH `scripts/build-topojson.mjs` and `site/scripts/validate/map.mjs`, adding a gzip-size assertion to the validator.

Purpose: This is the Wave-0 safety net. The source swap in Plan 02 regenerates the asset; these gates must already enforce join integrity and the new budget so a bad geometry (mis-joined CUTs, over-budget file) fails fast at build time. Per LOCKED CONTEXT decisions: budget is ≤140 KB gzip AND ≤420 KB raw; the 100 KB gate is "the bug".
Output: Updated `scripts/build-topojson.mjs` (assertion + budget logic only — source load stays GADM this plan), updated `site/scripts/validate/map.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md
@.planning/phases/10-high-resolution-commune-geometry/10-RESEARCH.md

<interfaces>
<!-- index.json entry shape (verified): each entry has `cut` (unpadded raw digit string), `name`, `slug`, `region_id`, `population`, `low_population`. The build join uses c.cut. -->
<!-- build-topojson.mjs current Step-5 (lines 172-189): asserts features.length === 346 and Santiago 13101 present. Output TopoJSON object key is the first key of topo.objects; per-geometry properties = { id, name } where id IS the CUT string. -->
<!-- Current budget constant: TMP loop uses `< 102400` (build script lines 202-236); validator uses `TOPO_MAX_BYTES = 102400` (map.mjs line 47). -->
<!-- Reference code examples are in 10-RESEARCH.md "Code Examples" (join-integrity assertion, gzip-size assertion). Use them as the pattern — do not invent a different structure. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend build-script integrity assertion to missing + orphan + duplicate CUTs</name>
  <read_first>
    - scripts/build-topojson.mjs (the file being modified — Step-5 block, lines 171-189)
    - data/cead/meta/index.json (346-CUT source of truth; entries keyed by `cut`)
    - .planning/phases/10-high-resolution-commune-geometry/10-RESEARCH.md ("Code Examples" → join-integrity assertion; "Pitfall 2" zero-pad)
    - .planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md ("CUT Join Integrity (LOCKED)")
  </read_first>
  <action>
    Replace the Step-5 pre-commit assertion block in scripts/build-topojson.mjs (currently only `features.length !== 346` + Santiago `13101` lookup) with an explicit four-way integrity check against index.json. Build `wantCuts = new Set(indexEntries.map(c => c.cut))`; `gotCuts = features.map(f => f.properties.id)`; compute `missing` (wantCuts not in gotCuts), `orphan` (gotCuts not in wantCuts), `dup` (gotCuts appearing more than once). Fail with `process.exit(1)` and log `{ count, missing, orphan, dup }` if `features.length !== 346 || missing.length || orphan.length || dup.length`. Keep the Santiago `13101` presence check (assert it is in gotCuts). On success, print a single line `346 features, 0 missing, 0 orphan, 0 dup` so downstream tooling and the Plan-02 regeneration can grep it. Do NOT change the source-load (GADM) or the simplify step in this task — assertion logic only. Mirror the same four-way structure in the post-mapshaper Step-8 validation block (currently only checks geomCount === 346 + Santiago) so the encoded TopoJSON is verified too: extract `geometries[].properties.id` and assert 0 missing/orphan/dup against wantCuts there as well.
  </action>
  <verify>
    <automated>node scripts/build-topojson.mjs prints a line matching "346 features, 0 missing, 0 orphan, 0 dup" (against current GADM source it must still pass — this task does not change geometry)</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-topojson.mjs` exits 0 AND stdout contains the literal "346 features, 0 missing, 0 orphan, 0 dup"
    - The Step-5 block references `missing`, `orphan`, and `dup` (grep `scripts/build-topojson.mjs` for all three identifiers, each present)
    - The post-mapshaper Step-8 block also asserts 0 missing/orphan/dup (not just geomCount === 346)
    - Santiago `13101` presence still asserted in both blocks
  </acceptance_criteria>
  <done>Build script hard-fails on any missing/orphan/duplicate CUT, not just a wrong count; passes clean against the current asset.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the 100 KB gate with a dual raw+gzip budget (build script + validator)</name>
  <read_first>
    - scripts/build-topojson.mjs (the file being modified — SIMPLIFY_CONFIGS loop + `< 102400` logic, lines 198-236; header comment lines 18-21)
    - site/scripts/validate/map.mjs (the file being modified — `TOPO_MAX_BYTES = 102400` line 47, Check 1 block lines 43-82)
    - .planning/phases/10-high-resolution-commune-geometry/10-RESEARCH.md ("Pitfall 1"; "Code Examples" → gzip-size assertion; measured matrix)
    - .planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md ("Size / Performance Budget (LOCKED)")
  </read_first>
  <action>
    In site/scripts/validate/map.mjs: raise `TOPO_MAX_BYTES` from 102400 to 430080 (420 KB raw ceiling — locked budget ≤420 KB raw). Add `import { gzipSync } from 'node:zlib';` at the top. In Check 1, after the raw-size assertion, read the topo file bytes once and add a gzip assertion: compute `gzipSync(readFileSync(TOPO_PATH)).length`; `fail('TopoJSON gzip budget', ...)` if it exceeds 143360 (140 KB gzip), else `pass('TopoJSON gzip budget (<140 KB)')`. Update the raw-size pass/fail labels to say the new ceiling (e.g. "< 420 KB raw") instead of "< 100 KB". Do NOT loosen any other check (346 feature count, Santiago join, dist/ runtime-data guards stay exactly as-is). In scripts/build-topojson.mjs: replace the three-config over-aggressive `SIMPLIFY_CONFIGS` fallback loop and its `< 102400` break/fatal logic. For THIS plan keep the existing GADM source so the script still runs green, but rewire the budget guard to the dual ceiling: assert final raw size `< 430080` AND `gzipSync(readFileSync(TMP_TOPO)).length < 143360`, failing with `process.exit(1)` and printing both numbers if either is exceeded. Remove the "Over 100 KB — trying more aggressive simplification" retry path entirely (Plan 02 sets the single correct simplify config; silent re-over-simplification is forbidden per CONTEXT). Update the build-script header comment block (lines 18-21) so the documented budget/config text no longer says "< 100 KB" / "0.05% q=500" — state the new budget (≤420 KB raw AND ≤140 KB gzip).
  </action>
  <verify>
    <automated>node scripts/build-topojson.mjs exits 0 (current GADM asset is ~87 KB raw / ~21 KB gzip — well under the new dual ceiling) AND cd site && node scripts/sync-data.mjs && node scripts/validate/map.mjs exits 0 with a PASS line containing "gzip budget"</automated>
  </verify>
  <acceptance_criteria>
    - `site/scripts/validate/map.mjs` defines `TOPO_MAX_BYTES = 430080` and imports `gzipSync` from `node:zlib`
    - Running `cd site && node scripts/sync-data.mjs && node scripts/validate/map.mjs` exits 0 and prints a PASS line matching "gzip budget"
    - `scripts/build-topojson.mjs` no longer contains the literal "102400" nor the string "trying more aggressive simplification"
    - `scripts/build-topojson.mjs` asserts both raw `< 430080` and gzip `< 143360` before writing the final asset
    - Build-script header comment no longer states "< 100 KB" as the budget
  </acceptance_criteria>
  <done>Both the build script and the validator enforce ≤420 KB raw AND ≤140 KB gzip; the stale 100 KB blocker and the silent re-simplify fallback are gone; the current asset still passes (chained command exits 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source dataset → build script | Offline GeoJSON parsed and re-keyed at build time; not user input, but a tampered/corrupt source would otherwise ship silently |
| committed asset → browser (runtime) | `communes.topo.json` fetched by the map island; malformed JSON could break the map |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | scripts/build-topojson.mjs integrity assertion | mitigate | Extended Step-5 + Step-8 assertions (missing/orphan/dup/Santiago vs index.json) reject mis-joined or corrupt geometry before the asset is written — Task 1 |
| T-10-02 | Tampering | site/scripts/validate/map.mjs budget gate | mitigate | Dual raw+gzip budget assertion rejects an over-budget (potentially malformed/bloated) asset in CI — Task 2 |
| T-10-DoS | Denial of Service (client) | MapIsland.tsx fetch/decode | accept | Existing `safeFetch` + `loadError` graceful path already handles malformed/missing TopoJSON without throwing; this plan does NOT touch the render path, so the mitigation is preserved by non-regression |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (mapshaper + topojson-client already vetted in Phase 3 per RESEARCH Package Legitimacy Audit); no install task exists |
</threat_model>

<verification>
- `node scripts/build-topojson.mjs` exits 0 and prints "346 features, 0 missing, 0 orphan, 0 dup" against the current asset.
- `cd site && node scripts/sync-data.mjs && node scripts/validate/map.mjs` exits 0 with PASS lines for raw budget, gzip budget, 346 features, and Santiago CUT join (chained per OneDrive desync constraint).
- No regression to the render path, sync-data.mjs, or any non-budget validator check.
</verification>

<success_criteria>
- Build script enforces missing/orphan/duplicate/Santiago integrity against index.json (GEO-01 join gate).
- Build script and validator both enforce ≤420 KB raw AND ≤140 KB gzip; the 100 KB blocker is removed (GEO-02 budget gate).
- Current GADM-sourced asset still passes both — gates are correct and non-destructive ahead of the Plan-02 swap.
</success_criteria>

<output>
Create `.planning/phases/10-high-resolution-commune-geometry/10-01-SUMMARY.md` when done.
</output>
