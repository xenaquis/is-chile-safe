---
phase: 10-high-resolution-commune-geometry
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - data/cead/geo/communes-chilemapas.geojson
  - data/cead/geo/communes.topo.json
  - scripts/build-topojson.mjs
  - .gitignore
autonomous: true
requirements: [GEO-01, GEO-02]

must_haves:
  truths:
    - "communes.topo.json is regenerated from the chilemapas source (GPL-3, INE/SUBDERE/BCN), not GADM"
    - "All 346 CEAD CUTs join correctly after zero-pad normalization (0 missing, 0 orphan, 0 dup)"
    - "The asset is recognizable-fidelity (visvalingam ~10% + quantization 10000) and within ≤420 KB raw / ≤140 KB gzip"
    - "The raw chilemapas source is committed so the build re-runs offline with no network"
  artifacts:
    - path: "data/cead/geo/communes-chilemapas.geojson"
      provides: "Committed raw chilemapas source for reproducible offline rebuild"
    - path: "data/cead/geo/communes.topo.json"
      provides: "Regenerated high-fidelity 346-commune TopoJSON keyed by CUT"
      contains: "Topology"
    - path: "scripts/build-topojson.mjs"
      provides: "chilemapas source load + srcCodeToCut zero-pad normalization + retuned single simplify config + source attribution header"
      contains: "srcCodeToCut"
    - path: ".gitignore"
      provides: "communes-chilemapas.geojson un-ignored so the raw source is committed"
  key_links:
    - from: "scripts/build-topojson.mjs"
      to: "data/cead/geo/communes-chilemapas.geojson"
      via: "readFileSync of chilemapas GeoJSON, codigo_comuna re-keyed via srcCodeToCut"
      pattern: "chilemapas"
    - from: "scripts/build-topojson.mjs"
      to: "mapshaper CLI"
      via: "visvalingam percentage=10% keep-shapes + quantization=10000"
      pattern: "quantization"
---

<objective>
Swap the boundary source from license-incompatible GADM to chilemapas (the LOCKED choice — GPL-3, INE/SUBDERE/BCN-authoritative, ships a real `codigo_comuna` CUT), commit the raw source for offline reproducibility, add the zero-pad CUT normalization that the chilemapas code-join requires, retune the simplification to recognizable fidelity, and regenerate + sync + validate the asset in a single chained command.

Purpose: This is the core of GEO-01 (recognizable comuna shapes, correct CUT join, no comuna dropped) and GEO-02 (within the documented size budget, reproducible regeneration). The gates from Plan 01 now enforce both. Per LOCKED CONTEXT: source = chilemapas `data_geojson`; normalize source code → CUT with `String(parseInt(code, 10))`; simplify config = `visvalingam percentage=10% keep-shapes` + `quantization=10000` (measured ~367 KB raw / ~107 KB gzip, integrity-clean); commit the raw source.
Output: `data/cead/geo/communes-chilemapas.geojson` (committed), regenerated `data/cead/geo/communes.topo.json`, edited `scripts/build-topojson.mjs`, updated `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md
@.planning/phases/10-high-resolution-commune-geometry/10-RESEARCH.md
@.planning/phases/10-high-resolution-commune-geometry/10-01-SUMMARY.md

<interfaces>
<!-- chilemapas data_geojson comunas file ships per-feature properties including `codigo_comuna` (5-char zero-padded CUT string, e.g. "02101"). Source repo: github.com/pachadotdev/chilemapas (GPL-3). CRS EPSG:4326. ~346 comunas. -->
<!-- index.json entries use `cut` = unpadded raw digits ("2101", "13101"). Normalization helper (from RESEARCH Code Examples): srcCodeToCut(code) => String(parseInt(code,10)). "02101"->"2101", "13101"->"13101". -->
<!-- ASSUMPTION A1/A2 (RESEARCH): verify the actual property name + padding on the downloaded file before locking the join — if the property is not `codigo_comuna` or codes are already unpadded, adapt the re-key (parseInt is a harmless no-op if already unpadded). -->
<!-- The Plan-01 four-way integrity assertion (missing/orphan/dup/Santiago) is the gate that proves the join is correct. The retuned mapshaper invocation (from RESEARCH Code Examples): `-clean -simplify visvalingam percentage=10% keep-shapes -o format=topojson quantization=10000`. -->
<!-- Antártica 12202 + oceanic islands: if chilemapas lacks them, reuse the existing placeholder-polygon approach (Step 4) for ONLY those CUTs; keep keep-shapes so tiny comunas survive. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire + commit the chilemapas raw source; un-ignore it</name>
  <read_first>
    - .gitignore (the file being modified — line 26 currently ignores communes-chilemapas.geojson)
    - .planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md ("Reproducibility (LOCKED)" + "Boundary Source (LOCKED)")
    - .planning/phases/10-high-resolution-commune-geometry/10-RESEARCH.md ("Boundary Source" table; "Commit-source note"; Sources → chilemapas repo URL)
  </read_first>
  <action>
    Download the chilemapas comunas GeoJSON from the chilemapas `data_geojson` source (github.com/pachadotdev/chilemapas — the comunas-level file). Save it to `data/cead/geo/communes-chilemapas.geojson`. Verify it parses as well-formed GeoJSON (FeatureCollection) and inspect ONE feature's `properties` to confirm the CUT property name (expected `codigo_comuna`, 5-char zero-padded) and feature count (~346) — record the actual property name and padding in the SUMMARY; if it differs from `codigo_comuna`/zero-padded, note the adaptation needed for Task 2 (ASSUMPTION A1/A2). Remove the `data/cead/geo/communes-chilemapas.geojson` line from `.gitignore` (line 26) so the raw source is committed for offline reproducibility (LOCKED Success-Criterion-5 decision). Leave the `communes-raw.geojson` (GADM) and `communes-fcorrea.geojson` ignore lines untouched — GADM is kept for diff/QA per the deferred-items note. If a network download is unavailable in the execution environment, STOP and surface a blocking note: the raw source must be obtained once at dev time (it cannot be synthesized).
  </action>
  <verify>
    <automated>node -e "const g=require('./data/cead/geo/communes-chilemapas.geojson'); if(g.type!=='FeatureCollection'||!Array.isArray(g.features)||g.features.length<340){process.exit(1)}; console.log(g.features.length+' features; props='+Object.keys(g.features[0].properties).join(','))" exits 0 and prints a feature count near 346</automated>
  </verify>
  <acceptance_criteria>
    - `data/cead/geo/communes-chilemapas.geojson` exists, parses as a FeatureCollection with ≥340 features
    - The CUT property name and padding are recorded in the SUMMARY (e.g. `codigo_comuna`, zero-padded)
    - `.gitignore` no longer contains `data/cead/geo/communes-chilemapas.geojson` (grep returns nothing); the GADM and fcorrea ignore lines remain
    - `git status` shows `communes-chilemapas.geojson` as a tracked/stageable file (not ignored)
  </acceptance_criteria>
  <done>The chilemapas raw source is on disk, committed (un-ignored), and its CUT-property scheme is confirmed for the re-key step.</done>
</task>

<task type="auto">
  <name>Task 2: Swap source load + add srcCodeToCut zero-pad normalization in build script</name>
  <read_first>
    - scripts/build-topojson.mjs (the file being modified — Step 1 source load lines 84-98, Step 2/3 re-key lines 100-146, Step 4 Antártica placeholder lines 148-169, header lines 1-32)
    - data/cead/geo/communes-chilemapas.geojson (the new source — confirm the CUT property name from Task 1)
    - data/cead/meta/index.json (346-CUT truth; entries keyed by `cut`)
    - .planning/phases/10-high-resolution-commune-geometry/10-RESEARCH.md ("Code Examples" → srcCodeToCut + join-integrity; "Pitfall 2"; "Pitfall 4")
    - .planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md ("CUT Join Integrity (LOCKED)")
  </read_first>
  <action>
    Edit scripts/build-topojson.mjs so it loads `data/cead/geo/communes-chilemapas.geojson` instead of the GADM `communes-raw.geojson` (change the `RAW_GEOJSON` constant + the not-found error message + the "Loading GADM" log). Add a `srcCodeToCut(code) => String(parseInt(code, 10))` helper. Replace the GADM name-match re-key (Step 3: `norm(NAME_3)` lookups + `MANUAL_FIXES` + `indexByNorm`) with a code-join: for each chilemapas feature, read its CUT property (the name confirmed in Task 1, expected `codigo_comuna`), normalize via `srcCodeToCut`, look it up in a `Map` of `index.json` keyed by `c.cut`, and re-key the feature to `properties = { id: cut, name: ie.name }` dropping all other props. Retire the `MANUAL_FIXES`, `norm`, and `indexByNorm` name-matching machinery (chilemapas ships a real code — prefer code-join per CONTEXT, "retires the fragile name-matching path"). Keep the Step-4 Antártica `12202` placeholder block AND the `keep-shapes` flag, but make the placeholder conditional: only inject 12202 (and any other oceanic-territory CUT) if chilemapas actually lacks it after the code-join (Pitfall 4 — if chilemapas includes Antártica, drop the placeholder hack). The Plan-01 four-way integrity assertion (Step 5) is unchanged and is the gate that proves the join — it must print "346 features, 0 missing, 0 orphan, 0 dup". Do NOT touch the simplify config in this task (Task 3 does that). Do NOT touch the render path, ChoroplethLayer, LowZoomDotLayer, or sync-data.mjs.
  </action>
  <verify>
    <automated>node scripts/build-topojson.mjs runs the re-key and the Plan-01 Step-5 assertion; stdout contains "346 features, 0 missing, 0 orphan, 0 dup" (proving every CEAD CUT code-joined after zero-pad normalization)</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build-topojson.mjs` reads `communes-chilemapas.geojson` (grep for "chilemapas"); no longer reads `communes-raw.geojson` as the ship source
    - The script defines `srcCodeToCut` and joins on the chilemapas CUT property via `String(parseInt(...))`
    - `node scripts/build-topojson.mjs` exits 0 and stdout contains "346 features, 0 missing, 0 orphan, 0 dup" and confirms Santiago 13101
    - The name-match `MANUAL_FIXES`/`indexByNorm` path is removed (grep for "MANUAL_FIXES" returns nothing, or only a comment noting its retirement)
    - Antártica 12202 placeholder injected only if chilemapas lacks it (conditional preserved)
  </acceptance_criteria>
  <done>The build re-keys chilemapas geometry to all 346 CEAD CUTs via zero-pad-normalized code-join with zero missing/orphan/dup; name-matching is retired.</done>
</task>

<task type="auto">
  <name>Task 3: Retune simplification to recognizable fidelity, regenerate, sync, validate</name>
  <read_first>
    - scripts/build-topojson.mjs (the file being modified — Step 7 mapshaper invocation; header comment)
    - site/scripts/validate/map.mjs (read-only — the Plan-01 dual budget gate the new asset must pass)
    - .planning/phases/10-high-resolution-commune-geometry/10-RESEARCH.md ("Code Examples" → re-tuned mapshaper invocation; "Pattern 1" quantization; measured matrix)
    - .planning/phases/10-high-resolution-commune-geometry/10-CONTEXT.md ("Size / Performance Budget (LOCKED)" + "Specific Ideas" mapshaper line)
  </read_first>
  <action>
    In scripts/build-topojson.mjs, replace the single mapshaper invocation with the LOCKED config: `-clean -simplify visvalingam percentage=10% keep-shapes -o format=topojson quantization=10000` (measured ~367 KB raw / ~107 KB gzip, ~0.4 km grid, recognizable coastline, integrity-clean). There is now exactly ONE simplify config — no fallback loop (Plan 01 already removed the retry path; do not reintroduce it). The Plan-01 dual budget assertion (raw < 430080 AND gzip < 143360) gates the output. Update the build-script header (Step-7 description + the "Source dataset" line at the top) to document chilemapas as the source with its download URL/version/date and the GPL-3 attribution line, and to state the new simplify config + measured sizes (replace the GADM/`0.05% q=500` references). Then regenerate + mirror + validate in a SINGLE chained command (OneDrive desync mitigation, LOCKED): `node scripts/build-topojson.mjs && cd site && node scripts/sync-data.mjs && node scripts/validate/map.mjs`. The regenerated `data/cead/geo/communes.topo.json` is the committed output. Do NOT chase fidelity past 10% (CONTEXT: "10% is plenty; don't go to 20%").
  </action>
  <verify>
    <automated>node scripts/build-topojson.mjs && cd site && node scripts/sync-data.mjs && node scripts/validate/map.mjs — chained command exits 0; build prints "346 features, 0 missing, 0 orphan, 0 dup"; validator prints PASS for raw budget, gzip budget, 346 features, and Santiago CUT join. The committed communes.topo.json gzip size lands between ~80 KB and 140 KB (recognizable-fidelity range, not the old ~21 KB blocky asset).</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build-topojson.mjs` mapshaper call uses `visvalingam`, `percentage=10%`, `keep-shapes`, and `quantization=10000` (grep all four)
    - The chained command `node scripts/build-topojson.mjs && cd site && node scripts/sync-data.mjs && node scripts/validate/map.mjs` exits 0
    - `data/cead/geo/communes.topo.json` raw size is between ~280 KB and 420 KB AND gzip size is between ~80 KB and 140 KB (proves real fidelity, within budget) — confirm via `node -e "const z=require('node:zlib'),f=require('node:fs');const b=f.readFileSync('data/cead/geo/communes.topo.json');console.log('raw',b.length,'gzip',z.gzipSync(b).length)"`
    - Build-script header documents chilemapas source URL + GPL-3 attribution + the new simplify config (no remaining "0.05%"/"q=500"/GADM-as-ship-source text)
  </acceptance_criteria>
  <done>The shipped asset is regenerated from chilemapas at recognizable fidelity, passes the dual budget + integrity gates, and is mirrored/validated via the single chained command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downloaded chilemapas source → repo | A tampered/corrupt downloaded GeoJSON would otherwise be committed and shipped |
| source dataset → build script | Re-keyed offline; integrity asserted before write |
| committed asset → browser (runtime) | Regenerated TopoJSON fetched by the map island |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | data/cead/geo/communes-chilemapas.geojson | mitigate | Committing the raw source (un-ignored) pins exactly-this-bytes for reproducible offline rebuild; Task 1 parses + schema-checks it before use; the Plan-01 346/0/0/0 integrity assertion rejects geometry that does not map cleanly to CEAD CUTs |
| T-10-04 | Tampering | build re-key (srcCodeToCut code-join) | mitigate | Zero-pad normalization + four-way integrity assertion (missing/orphan/dup/Santiago) guarantees no comuna dropped or mis-joined before the asset is written — Task 2 |
| T-10-LICENSE | (Legal, not STRIDE) | shipped geometry license | mitigate | Swap GADM (non-commercial/no-redistribution) → chilemapas (GPL-3, redistribution OK with attribution); attribution added to script header here + site colophon in Plan 03 |
| T-10-DoS | Denial of Service (client) | MapIsland.tsx fetch/decode | accept | Existing `safeFetch` + `loadError` graceful path unchanged (render path untouched); higher vertex count does not change the fetch/decode failure handling |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs (mapshaper global + topojson-client already vetted Phase 3); no install task |
</threat_model>

<verification>
- Single chained command `node scripts/build-topojson.mjs && cd site && node scripts/sync-data.mjs && node scripts/validate/map.mjs` exits 0.
- Build prints "346 features, 0 missing, 0 orphan, 0 dup" and Santiago 13101 confirmed.
- Validator prints PASS for raw budget (< 420 KB), gzip budget (< 140 KB), 346 features, and Santiago CUT join.
- Committed `communes.topo.json` is recognizable-fidelity (raw ~280–420 KB / gzip ~80–140 KB), not the old ~21 KB blocky asset.
- Raw chilemapas source is committed (un-ignored).
</verification>

<success_criteria>
- Asset regenerated from chilemapas (authoritative, openly-licensed) covering every CEAD comuna, keyed by CUT, 0 missing/orphan/dup (GEO-01).
- Recognizable fidelity (visvalingam 10% + quantization 10000) within ≤420 KB raw / ≤140 KB gzip (GEO-02 size budget).
- Reproducible: raw source committed + single-command regeneration documented in the script header (GEO-02 reproducibility).
- Render architecture, sync-data.mjs, and join key untouched.
</success_criteria>

<output>
Create `.planning/phases/10-high-resolution-commune-geometry/10-02-SUMMARY.md` when done. Record the confirmed chilemapas CUT property name + padding, the measured raw/gzip sizes of the shipped asset, and whether the Antártica 12202 placeholder was needed.
</output>
