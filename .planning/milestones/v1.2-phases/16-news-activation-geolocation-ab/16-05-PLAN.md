---
phase: 16-news-activation-geolocation-ab
plan: 05
type: execute
wave: 5
depends_on: ["16-01", "16-02", "16-03", "16-04"]
files_modified:
  - .planning/phases/16-news-activation-geolocation-ab/16-VERIFICATION.md
autonomous: false
requirements: [NEWS-01, NEWS-02, NEWS-03, NEWS-04]
must_haves:
  truths:
    - "The full pipeline test suite is green"
    - "A chained build+validate (OneDrive-safe) passes"
    - "BrowserOS confirms the news page + pins render with both link types and no console errors in EN and ES"
    - "16-VERIFICATION.md records per-requirement evidence + the A/B winner"
  artifacts:
    - path: ".planning/phases/16-news-activation-geolocation-ab/16-VERIFICATION.md"
      provides: "Per-requirement verification log + A/B winner + BrowserOS notes"
      contains: "NEWS-01"
  key_links:
    - from: "16-VERIFICATION.md"
      to: "results/ab_*.json"
      via: "winner table cites the A/B metrics"
      pattern: "accuracy"
---

<objective>
Final phase gate: run the full pipeline test suite + a chained `npm run build && npm run validate` (OneDrive-safe), then use BrowserOS to visually confirm the news page + incident pins render with BOTH source and comuna links and no console errors in EN and ES. Consolidate all evidence (including the A/B winner from Plan 02) into `16-VERIFICATION.md`. Covers NEWS-01..04 end-to-end.

Purpose: Prove the dark layer is genuinely on — accurate geolocation, evidence-based provider, real incidents surfaced, both link types, bilingual page — before declaring the phase done.

Output: 16-VERIFICATION.md with per-requirement pass/notes + BrowserOS observations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-news-activation-geolocation-ab/16-VALIDATION.md
@.planning/phases/16-news-activation-geolocation-ab/16-01-SUMMARY.md
@.planning/phases/16-news-activation-geolocation-ab/16-02-SUMMARY.md
@.planning/phases/16-news-activation-geolocation-ab/16-03-SUMMARY.md
@.planning/phases/16-news-activation-geolocation-ab/16-04-SUMMARY.md

<interfaces>
<!-- BrowserOS MCP at http://127.0.0.1:9200/mcp (project memory). Dev server: cd site && npm run dev. -->
<!-- save_screenshot needs a no-spaces path; emulate mobile via 375px iframe (project memory: browseros-review-gotchas). -->
<!-- Run browser-review inline (gsd-executor lacks browseros). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + chained build+validate</name>
  <files>.planning/phases/16-news-activation-geolocation-ab/16-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/16-news-activation-geolocation-ab/16-VALIDATION.md (§ Per-Requirement Verification Map + Sampling Rate — the exact commands)
    - .planning/phases/16-news-activation-geolocation-ab/16-02-SUMMARY.md (A/B winner + metrics, if the run completed)
    - $HOME memory note "OneDrive build artifacts desync" (chain build+validate)
  </read_first>
  <action>
    Run the full pipeline suite `cd pipeline && python -m pytest tests/ -q` and capture the pass count. Then run the chained, OneDrive-safe frontend gate from the site dir: `npm run build && npm run validate` in ONE command (predev/prebuild sync-data.mjs copies data/incidents/ → site/public/). Capture the validator pass count and confirm dist/news/index.html + dist/es/noticias/index.html exist. Begin `16-VERIFICATION.md` (create if Plan 02 did not, else append): a per-requirement table (NEWS-01..04) with the automated signal + result for each — NEWS-01 resolver tests + golden-set accuracy vs baseline; NEWS-02 A/B winner table + the metrics (or "A/B pending keys, deepseek default"); NEWS-03 current.json validation result (real or empty-valid fallback) + every-incident-has-cut+slug; NEWS-04 build+validate green + both news pages built. Record the actual commands run and their exit status.
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/ -q && cd ../site && npm run build && npm run validate 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` exits 0 (full pipeline suite green).
    - `npm run build && npm run validate` exits 0 (chained, single command).
    - dist/news/index.html + dist/es/noticias/index.html exist.
    - 16-VERIFICATION.md has a NEWS-01..04 table with each row's signal + result.
  </acceptance_criteria>
  <done>Full pytest + chained build+validate green; per-requirement evidence written to 16-VERIFICATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BrowserOS visual verify (EN + ES) + finalize verification</name>
  <action>BrowserOS checkpoint (port 9200, run inline). With `npm run dev` running, visually verify EN /news/ + ES /es/noticias/ and the map incident pins render with BOTH source and comuna links and no console errors, confirm the News/Noticias nav link, then finalize 16-VERIFICATION.md with observations + overall status + A/B winner. See how-to-verify for exact steps.</action>
  <what-built>The full test suite and chained build+validate pass. This checkpoint visually confirms — in a real browser via BrowserOS (port 9200) — that the news page and map incident pins render correctly with BOTH link types in EN and ES, with no console errors.</what-built>
  <how-to-verify>
    PREREQ: BrowserOS MCP connected (http://127.0.0.1:9200/mcp); `cd site && npm run dev` running (predev syncs incidents). Run browser-review inline (gsd-executor lacks browseros). save_screenshot to a NO-SPACES path.

    1. Navigate to the dev URL for `/news/` (EN). Confirm: H1 + freshness indicator (or empty-state if current.json is empty-valid), and — IF incidents are present — each incident row shows a working Source link AND a "View commune" link. Check the browser console: NO errors.
    2. Navigate to `/es/noticias/` (ES). Confirm the ES twin: "Actualizado …" (or empty state), "Fuente" + "Ver comuna" links, no console errors.
    3. Navigate to `/map/` (EN) and `/es/mapa/` (ES). If incidents exist, click an incident pin: the popup must show the source link AND a "View commune"/"Ver comuna" link pointing at /commune/{slug}/ resp. /es/comuna/{slug}/. No console errors.
    4. Confirm the News/Noticias nav link appears in the header in both locales and routes correctly.
    5. KEY-ABSENT NOTE: if current.json is empty-valid (live run pended on keys), verify the empty-state renders cleanly (no crash, no console error) and record that pins/incident-row link checks are "pending real incidents" — the page + nav + build evidence still pass.
    6. Finalize `16-VERIFICATION.md`: add a BrowserOS section with the observations (+ screenshot paths), set the overall status (passed / passed-with-pending-keys), and confirm the A/B winner is recorded.
  </how-to-verify>
  <acceptance_criteria>
    - EN /news/ and ES /es/noticias/ render without console errors (real incidents OR clean empty-state).
    - When incidents are present: each incident row + map pin popup shows BOTH a source link and a comuna link with the correct locale prefix.
    - News/Noticias nav link present and routing in both locales.
    - 16-VERIFICATION.md finalized with BrowserOS observations + overall status + A/B winner.
  </acceptance_criteria>
  <resume-signal>Type "approved" once BrowserOS checks pass and 16-VERIFICATION.md is finalized, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| current.json → browser render | Verifies escHtml/safeUrl mitigations hold at runtime (no XSS, no broken links) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-14 | Tampering (XSS) | rendered incident strings | mitigate | BrowserOS console check confirms no script execution / no errors from incident titles; escHtml (Plan 04) verified live |
| T-16-SC | Tampering | npm installs | accept | verification plan installs nothing; audit table not required |
</threat_model>

<verification>
- `cd pipeline && python -m pytest tests/ -q` green
- `cd site && npm run build && npm run validate` exits 0 (chained)
- BrowserOS: EN + ES news pages + map pins render with both link types, no console errors
- 16-VERIFICATION.md finalized (NEWS-01..04 evidence + A/B winner + status)
</verification>

<success_criteria>
- Full pipeline suite + chained build+validate green
- BrowserOS confirms news page + pins + both link types in EN and ES, no console errors
- 16-VERIFICATION.md records per-requirement evidence, the A/B winner, and overall status
</success_criteria>

<output>
Create `.planning/phases/16-news-activation-geolocation-ab/16-05-SUMMARY.md` when done
</output>
