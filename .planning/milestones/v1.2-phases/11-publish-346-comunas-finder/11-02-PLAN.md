---
phase: 11-publish-346-comunas-finder
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - site/package.json
  - site/src/pages/region/[slug].astro
  - site/src/pages/es/region/[slug].astro
  - site/src/pages/crime/[family].astro
  - site/src/pages/es/delito/[family].astro
autonomous: true
requirements: [COMU-01, COMU-03]
must_haves:
  truths:
    - "A committed `npm run build` (fresh checkout, no machine env var) emits all 346 EN + 346 ES comuna pages — 346 is the build-default."
    - "The sitemap includes all 346×2 comuna URLs (the ROLLOUT_ALL branch already returns true for every URL)."
    - "Region and crime-type ranking tables (EN+ES) link EVERY comuna; the 'Showing N of M'/'Mostrando N de M' note no longer appears anywhere in dist."
  artifacts:
    - path: "site/package.json"
      provides: "Committed ROLLOUT_ALL=true build default via cross-env"
      contains: "cross-env ROLLOUT_ALL=true astro build"
    - path: "site/src/pages/region/[slug].astro"
      provides: "Region ranking table linking all comunas (rollout row-filter removed)"
      contains: "rankingRows"
    - path: "site/src/pages/crime/[family].astro"
      provides: "Crime ranking table linking all comunas (rollout row-filter removed)"
      contains: "rankingRows"
  key_links:
    - from: "site/package.json build script"
      to: "loadRolloutCuts() ROLLOUT_ALL branch + astro.config sitemap filter"
      via: "process.env.ROLLOUT_ALL === 'true'"
      pattern: "ROLLOUT_ALL=true"
    - from: "region/crime templates"
      to: "CommuneRankingTable / national ranking <tbody>"
      via: "pass full rankingRows (not displayedRows)"
      pattern: "rows=\\{rankingRows\\}"
---

<objective>
Flip the rollout to all 346 comunas as the committed build default, and un-gate the four ranking-table templates so every comuna is linked.

1. Set `ROLLOUT_ALL=true` as the committed default in `package.json`'s build script via `cross-env` (verified available, v10.1.0) — the smallest, already-supported, fully-reversible flip per 11-RESEARCH.md Pattern 1. This makes `loadRolloutCuts()` (data.ts:217) and the `astro.config.mjs` sitemap filter emit all 346×2 comuna pages + URLs.
2. Remove the `loadRolloutCuts()` row-filter + "Showing N of M" / "Mostrando N de M" note from the four ranking templates (region EN/ES, crime EN/ES) so all comunas link (Pattern 3). `CommuneRankingTable.astro` needs no change — it links every row it receives.

Purpose: Kills the "material perdido" orphan problem — map clicks and ranking rows for all 334 previously-gated comunas now reach real pages. Depends on Plan 01's memoization to keep the 346 build under the 20-min CI/Cloudflare timeout.
Output: Committed 346 build default + four un-gated ranking templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-publish-346-comunas-finder/11-CONTEXT.md
@.planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md
@site/package.json
@site/src/pages/region/[slug].astro
@site/src/pages/crime/[family].astro

<interfaces>
<!-- The ROLLOUT_ALL machinery already exists — the flip is purely setting the env at build time. -->
From site/src/lib/data.ts:
```typescript
export function loadRolloutCuts(): string[]; // returns loadIndex().map(c=>c.cut) (all 346) when process.env.ROLLOUT_ALL === 'true'
```
From site/astro.config.mjs:
```javascript
const ROLLOUT_ALL = process.env.ROLLOUT_ALL === 'true';
function sitemapFilter(url) { if (ROLLOUT_ALL) return true; /* …staged… */ } // already returns all URLs under ROLLOUT_ALL
```
Ranking-table line references (from 11-RESEARCH.md Pattern 3, verified):
- region/[slug].astro: rolloutCuts/displayedRows/hiddenCount at L91-96; rollout-note at L214-216; table at L213 uses displayedRows.
- es/region/[slug].astro: same logic L84-89; note L202-203; table L201 uses displayedRows.
- crime/[family].astro: L100-104; tbody maps displayedRows L216; note L242-244.
- es/delito/[family].astro: L94-96; tbody maps displayedRows L206; note L232-233.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Commit ROLLOUT_ALL=true as the build default via cross-env</name>
  <read_first>
    - site/package.json (the file being modified)
    - .planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md (Pattern 1 + Assumption A1 + Environment Availability: cross-env verified)
  </read_first>
  <files>site/package.json</files>
  <behavior>
    - `npm run build` on a fresh checkout (no ROLLOUT_ALL set in the shell/CI) emits all 346 EN + 346 ES comuna pages — the value lives in package.json, not a dev machine.
    - The build is portable: works on the Windows dev machine (PowerShell) and Linux CI/Cloudflare identically.
    - `prebuild` (sync-data.mjs) still runs before the build (npm lifecycle preserved).
  </behavior>
  <action>
    Add `cross-env` (verified v10.1.0 via `npm view cross-env version`, an established widely-used package — legitimacy confirmed, see threat T-11-SC) to `devDependencies` and change the `build` script from `"astro build"` to `"cross-env ROLLOUT_ALL=true astro build"`. Use `cross-env` (NOT bare `ROLLOUT_ALL=true astro build`) because the dev machine is Windows/PowerShell which ignores `VAR=value cmd` syntax (11-RESEARCH.md A1; project env is win32). Run the install so package-lock.json updates (`npm install --save-dev cross-env` from site/). Do NOT edit astro.config.mjs (its ROLLOUT_ALL branch is already correct) and do NOT expand rollout.json's enabled[] (keep it vestigial per Open Question 2). Leave the `validate` script as-is.
  </action>
  <verify>
    <automated>cd site && node -e "const p=require('./package.json'); if(!/cross-env ROLLOUT_ALL=true astro build/.test(p.scripts.build)) throw new Error('build script not set'); if(!p.devDependencies['cross-env']) throw new Error('cross-env not in devDeps'); console.log('build default OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `scripts.build` === `cross-env ROLLOUT_ALL=true astro build`.
    - `cross-env` present in devDependencies and resolvable (`npm ls cross-env` from site/ shows it installed).
    - astro.config.mjs and rollout.json unchanged.
  </acceptance_criteria>
  <done>The committed build default is ROLLOUT_ALL=true via cross-env; a fresh `npm run build` will emit all 346×2 comuna pages.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Un-gate the EN + ES region ranking tables</name>
  <read_first>
    - site/src/pages/region/[slug].astro (full read)
    - site/src/pages/es/region/[slug].astro (full read)
    - site/src/components/CommuneRankingTable.astro (confirm it links every row — needs NO change)
    - .planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md (Pattern 3, region line refs)
  </read_first>
  <files>site/src/pages/region/[slug].astro, site/src/pages/es/region/[slug].astro</files>
  <behavior>
    - The region ranking table renders ALL communes in the region (full rankingRows), each linked to its comuna page.
    - No "Showing N of M — more coming soon" (EN) or "Mostrando N de M — más próximamente" (ES) note renders.
    - The existing rate-descending sort and low-pop marking are preserved.
  </behavior>
  <action>
    In BOTH region files: delete the `rolloutCuts`/`displayedRows`/`hiddenCount` block (EN L91-96, ES L84-89) and the `loadRolloutCuts` import from the data.ts import list. Change the `<CommuneRankingTable rows={displayedRows} …>` prop to `rows={rankingRows}` (EN L213, ES L201). Delete the `{hiddenCount > 0 && (<p class="rollout-note">…</p>)}` block (EN L214-216, ES L202-203). Leave the `.rollout-note` CSS rule in `<style>` (harmless dead CSS) OR remove it — either is acceptable; if removed, remove from both files. Do NOT touch CommuneRankingTable.astro, the sort, the JSON-LD, or any other section.
  </action>
  <verify>
    <automated>cd site && grep -rl "displayedRows\|hiddenCount\|loadRolloutCuts" src/pages/region/ src/pages/es/region/ && echo "FAIL: gating remnants found" && exit 1 || echo "region tables un-gated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "displayedRows\|hiddenCount\|loadRolloutCuts" src/pages/region/[slug].astro src/pages/es/region/[slug].astro` returns nothing.
    - Both region templates pass `rows={rankingRows}` to CommuneRankingTable.
    - No "Showing"/"Mostrando … más próximamente" string remains in either region file.
  </acceptance_criteria>
  <done>Both region ranking tables link every comuna; the rollout row-filter and gating note are gone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Un-gate the EN + ES crime-type ranking tables</name>
  <read_first>
    - site/src/pages/crime/[family].astro (full read)
    - site/src/pages/es/delito/[family].astro (full read)
    - .planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md (Pattern 3, crime line refs)
  </read_first>
  <files>site/src/pages/crime/[family].astro, site/src/pages/es/delito/[family].astro</files>
  <behavior>
    - The national ranking <tbody> maps over the full rankingRows (all comunas, eligible + low-pop), each comuna linked to /commune/{slug}/ (EN) resp. /es/comuna/{slug}/ (ES).
    - No "Showing N of M" (EN) / "Mostrando N de M" (ES) note renders.
    - The existing national-rank numbering (eligible ranked, low-pop "—"), low-pop footnote, and region-breakdown bars are preserved.
  </behavior>
  <action>
    In BOTH crime files: delete the `rolloutCuts`/`displayedRows`/`hiddenCount` block (EN L100-104, ES L94-96) and the `loadRolloutCuts` import. Change the `{displayedRows.map((row) => …)}` in the `<tbody>` to `{rankingRows.map((row) => …)}` (EN L216, ES L206). Change the low-pop footnote guard `displayedRows.some(...)` to `rankingRows.some(...)` (EN L237). Delete the `{hiddenCount > 0 && (<p class="rollout-note">…</p>)}` block (EN L242-244, ES L232-233). Optionally remove the now-dead `.rollout-note` CSS from both `<style>` blocks. Do NOT touch the rank-assignment logic (`rankingRows` already carries `nationalRank` for eligible and `null` for low-pop), the region-breakdown, or JSON-LD.
  </action>
  <verify>
    <automated>cd site && grep -rln "displayedRows\|hiddenCount\|loadRolloutCuts" src/pages/crime/ src/pages/es/delito/ && echo "FAIL: gating remnants found" && exit 1 || echo "crime tables un-gated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "displayedRows\|hiddenCount\|loadRolloutCuts" src/pages/crime/[family].astro src/pages/es/delito/[family].astro` returns nothing.
    - Both crime `<tbody>` blocks map over `rankingRows`; the low-pop footnote guard references `rankingRows`.
    - No "Showing"/"Mostrando … próximamente" string remains in either crime file.
  </acceptance_criteria>
  <done>Both crime-type ranking tables link every comuna; the rollout row-filter and gating note are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDeps | cross-env install pulls a third-party package into the build toolchain. |
| repo data → static HTML | Comuna names/slugs in tables come from trusted CEAD index; no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | crime/region templates emitting comuna links | accept | Slugs/names are trusted build data rendered as static `<a href>`; canonical /commune/{slug}/ paths only — no injection surface. |
| T-11-04 | Information Disclosure | newly-published 334 comuna pages | accept | All content is already-public CEAD statistics; publishing is the intended goal. No PII. |
| T-11-SC | Tampering | cross-env devDependency install | mitigate | cross-env legitimacy verified via `npm view cross-env version` → v10.1.0 (established, ubiquitous build-tooling package, MIT). NOT [ASSUMED]/[SUS] → no blocking-human checkpoint required. Install pinned via package-lock. |
</threat_model>

<verification>
- `node -e` build-script assertion passes (Task 1 verify).
- `grep` for `displayedRows`/`hiddenCount`/`loadRolloutCuts` across all four templates returns nothing.
- Full-build confirmation (346×2 pages emitted, sitemap 770 locs, build under 20 min, zero "Showing"/"Mostrando" in dist) is performed in Plan 04 — this plan stops at code edits + per-file greps to keep within context budget.
</verification>

<success_criteria>
- ROLLOUT_ALL=true is the committed build default via cross-env; rollout.json/astro.config untouched.
- All four ranking templates (region EN/ES, crime EN/ES) pass full rankingRows and emit no gating note.
- CommuneRankingTable.astro unchanged.
</success_criteria>

<output>
Create `.planning/phases/11-publish-346-comunas-finder/11-02-SUMMARY.md` when done.
</output>
