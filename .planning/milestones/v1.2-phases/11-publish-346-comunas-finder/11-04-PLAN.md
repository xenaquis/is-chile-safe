---
phase: 11-publish-346-comunas-finder
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - site/scripts/validate/all.mjs
autonomous: false
requirements: [COMU-01, COMU-02, COMU-03]
must_haves:
  truths:
    - "coverage.mjs is registered in the validator suite and runs as part of `node scripts/validate/all.mjs`."
    - "A full committed `npm run build` emits 346 EN + 346 ES comuna pages + the directory pages, completes under the 20-min CI/Cloudflare timeout, and the whole validator suite (incl. coverage.mjs + rollout.mjs) passes green."
    - "The sitemap includes all 346×2 comuna URLs + the directory URLs; ZERO internal links point to an ungenerated comuna slug (no orphans); no 'Showing N of M'/'Mostrando N de M' string remains in dist."
    - "Manual: in both locales a previously-orphaned deep/small comuna is reachable in ≤2 interactions via the directory, and a map polygon + a non-rollout ranking row both reach a real page (no 404)."
  artifacts:
    - path: "site/scripts/validate/all.mjs"
      provides: "coverage.mjs registered in the VALIDATORS array"
      contains: "coverage.mjs"
  key_links:
    - from: "site/scripts/validate/all.mjs"
      to: "coverage.mjs"
      via: "VALIDATORS array entry"
      pattern: "coverage\\.mjs"
---

<objective>
Wire and run the full Phase-11 verification, then gate on a human reachability check.

1. Register `coverage.mjs` (built in Plan 01) in `all.mjs`'s VALIDATORS array so the COMU-01/02/03 coverage assertions run as part of the suite.
2. Run the full committed `npm run build` (now ROLLOUT_ALL=true) + the validator suite, asserting: 346×2 comuna pages emitted, directory pages emitted, build under the 20-min timeout, sitemap coverage (346×2 + directory), zero orphan internal links, no gating note in dist — all validators green.
3. Human checkpoint: confirm directory reachability (≤2 interactions, both locales) and that previously-orphaned map/ranking clicks reach real pages.

Purpose: Prove the phase's three Success Criteria end-to-end. This is the phase gate. Depends on all prior plans (memoization, rollout flip, un-gated tables, directory).
Output: Registered coverage validator + a green full build + signed-off reachability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-publish-346-comunas-finder/11-CONTEXT.md
@.planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md
@.planning/phases/11-publish-346-comunas-finder/11-VALIDATION.md
@site/scripts/validate/all.mjs

<!-- OneDrive build-artifact desync (project memory): chain build + validate in ONE command so dist/ does not vanish between processes. -->
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register coverage.mjs in the validator suite</name>
  <read_first>
    - site/scripts/validate/all.mjs (the VALIDATORS array)
    - site/scripts/validate/coverage.mjs (confirm it exists from Plan 01)
  </read_first>
  <files>site/scripts/validate/all.mjs</files>
  <behavior>
    - `node scripts/validate/all.mjs` runs coverage.mjs as one of the validators; a non-zero exit from it fails the suite.
    - The suite summary lists coverage in its PASS/FAIL table.
  </behavior>
  <action>
    Add `'coverage.mjs'` to the `VALIDATORS` array in `all.mjs` (place it after `'rollout.mjs'` so route-count + coverage run adjacently). Update the header comment's numbered validator list to include coverage (route-count + no-orphan-link + sitemap-coverage + directory-completeness). Do NOT change the spawn/summary logic.
  </action>
  <verify>
    <automated>cd site && node -e "const t=require('fs').readFileSync('scripts/validate/all.mjs','utf8'); if(!/'coverage\.mjs'/.test(t)) throw new Error('coverage.mjs not registered'); console.log('coverage.mjs registered')"</automated>
  </verify>
  <acceptance_criteria>
    - `'coverage.mjs'` appears in the VALIDATORS array in all.mjs.
    - The spawnSync loop and summary table logic are unchanged.
  </acceptance_criteria>
  <done>coverage.mjs is part of the validator suite run by all.mjs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full build, then isolate coverage.mjs, then the full validator suite (timed, chained)</name>
  <read_first>
    - .planning/phases/11-publish-346-comunas-finder/11-VALIDATION.md (full suite command + sign-off)
    - .planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md (Pitfall 2 build-time budget; verified ~9.4 min with the memoization fix expected lower)
  </read_first>
  <files>(none — verification task; no source edits)</files>
  <behavior>
    - A single chained command runs the committed `npm run build` (ROLLOUT_ALL=true via cross-env) immediately followed by the validators, so dist/ does not desync (OneDrive memory). NO extra env (NO EXPECT_ALL) is exported — this is the EXACT committed CI command, and rollout.mjs (per Plan 01 Fix) must assert 346 by default.
    - After the build, coverage.mjs is run ALONE first to confirm its own 4 per-check PASS lines (isolating a coverage.mjs validator bug from a real data/orphan defect), THEN the full all.mjs suite runs.
    - The build emits 346 EN + 346 ES comuna dirs + dist/communes/index.html + dist/es/comunas/index.html + the 346×2 sitemap locs + directory URLs.
    - All validators pass (structure, commune, rollout, region, crime, hreflang, schema, map, forbidden-language, coverage).
    - Wall-clock build time is recorded and is comfortably under the 20-min CI/Cloudflare limit (memoization headroom).
  </behavior>
  <action>
    From `site/`, run the build, then the isolated coverage check, then the full suite in ONE chained command (per the OneDrive desync memory — never split across processes, so dist/ survives): `npm run build && node scripts/validate/coverage.mjs && node scripts/validate/all.mjs`. Do NOT export EXPECT_ALL or any extra env — the committed `npm run build` sets ROLLOUT_ALL via cross-env and rollout.mjs now defaults to 346 (Plan 01 Fix); exporting EXPECT_ALL would mask whether the committed default truly asserts 346. The isolated `coverage.mjs` run first MUST print its four per-check PASS lines (route-count, no-orphan-link, sitemap-coverage, directory-completeness) — if coverage.mjs itself errors (e.g. bad path resolution) vs. reports a real data defect, this first run disambiguates which. Then `all.mjs` re-runs coverage inside the full suite (idempotent — both run against the same dist). Capture and record the build wall-clock time. If the build approaches/exceeds the 20-min budget, STOP and report — do not paper over it (the memoization in Plan 01 is the mitigation; a regression here is a real finding). If coverage.mjs reports orphan links or a count mismatch, treat it as a real defect to fix (likely a stray /commune/ link to a non-existent slug, or a missing directory page) rather than relaxing the assertion or exporting EXPECT_ALL. Record the final validator summary (X/10 passed) and the build time in the SUMMARY.
  </action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/coverage.mjs && node scripts/validate/all.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && node scripts/validate/all.mjs` (the exact committed CI command, NO extra env) exits 0 (all validators pass).
    - rollout.mjs passes asserting 346 with NO EXPECT_ALL exported (proves Plan 01's committed-default fix holds end-to-end).
    - coverage.mjs run ALONE first (after the build) prints its 4 per-check PASS lines before the full all.mjs runs — isolating a coverage.mjs validator bug from a real data/orphan defect.
    - dist/commune and dist/es/comuna each contain 346 directories; dist/communes/index.html and dist/es/comunas/index.html exist.
    - coverage.mjs passes: route-count 346/locale, ZERO orphan links, sitemap 346×2 + directory URLs present, directory HTML contains ≥346 comuna hrefs each.
    - No "Showing"/"Mostrando" gating string in dist (coverage / grep confirms).
    - Recorded build wall-clock is under 20 min (target well under, with memoization).
  </acceptance_criteria>
  <done>Committed full build (no extra env) emits 346×2 + directory pages under the timeout; rollout.mjs asserts 346 with no EXPECT_ALL; coverage.mjs's isolated run is green; and the entire validator suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human reachability + no-dead-link verification (both locales)</name>
  <what-built>
    The rollout is flipped to all 346 comunas, the four ranking tables link every comuna, and a searchable directory (A–Z + by-region, accent-insensitive) exists at /communes/ and /es/comunas/, linked from nav + home. The full validator suite (incl. the new coverage.mjs no-orphan-link assertion) is green and the build is under the 20-min budget.
  </what-built>
  <how-to-verify>
    With the built site served locally (e.g. `cd site && npm run preview`, or open the dev server):
    1. Open `/communes/` (EN) and `/es/comunas/` (ES). Confirm the full A–Z list renders, the count reads "… of 346 / de 346", and the by-region toggle shows 16 region sections.
    2. In the search box, type an accent test: "vina" should surface "Viña del Mar"; "nunoa" should surface "Ñuñoa". Confirm the match highlights.
    3. Pick a DEEP / small comuna (e.g. a low-pop southern comuna tagged "baja pob. / low pop."), reach it in ≤2 interactions (1 filter + 1 click, or 1 region toggle + 1 click), and confirm its page renders (not a 404).
    4. Disable JavaScript and reload `/communes/` — confirm the full A–Z list of 346 links is still visible and clickable (graceful degradation).
    5. On the map (`/map/` and `/es/mapa/`), click a previously-orphaned comuna polygon → confirm it reaches a real comuna page. Open a region page and click a previously-non-rollout ranking row → confirm it reaches a real page (no 404). Confirm no "Showing N of M / Mostrando N de M" note appears on region/crime pages.
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass in both locales, or describe any 404 / unreachable comuna / broken filter / missing no-JS list.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → dist/ → CI gate | The validator suite is the trust gate proving 0 dead-links + full coverage before the site is publishable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Spoofing/Integrity | orphan internal links (dead clicks) | mitigate | coverage.mjs no-orphan-link assertion fails the build if any /commune/ or /es/comuna/ href targets an ungenerated slug — the exact inverse of the original "material perdido" bug. |
| T-11-02 | Denial of Service | CI/Cloudflare 20-min build budget | mitigate | Task 2 records build wall-clock and halts/reports if it nears the limit; memoization (Plan 01) is the structural mitigation. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; slopcheck N/A. |
</threat_model>

<verification>
- `npm run build && node scripts/validate/all.mjs` (no extra env) exits 0 with coverage.mjs registered and passing, and rollout.mjs asserting 346 by default.
- coverage.mjs run alone (after the build, before all.mjs) prints its 4 per-check PASS lines.
- Human checkpoint approved: directory reachability ≤2 interactions in both locales, no-JS full list visible, map/ranking clicks reach real pages, no gating note.
</verification>

<success_criteria>
- coverage.mjs registered and green within the suite.
- Full committed build (no extra env) emits 346×2 comuna pages + directory pages under the 20-min timeout; rollout.mjs asserts 346 with no EXPECT_ALL; sitemap + no-orphan + thin-content guards all pass.
- Human-verified: any comuna reachable in ≤2 interactions; previously-orphaned map/ranking clicks no longer 404.
</success_criteria>

<output>
Create `.planning/phases/11-publish-346-comunas-finder/11-04-SUMMARY.md` when done (record build wall-clock time + validator summary + checkpoint sign-off).
</output>
</content>
