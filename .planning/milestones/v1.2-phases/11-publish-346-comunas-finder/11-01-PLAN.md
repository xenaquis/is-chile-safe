---
phase: 11-publish-346-comunas-finder
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - site/src/lib/data.ts
  - site/scripts/validate/coverage.mjs
  - site/scripts/validate/rollout.mjs
autonomous: true
requirements: [COMU-01, COMU-03]
must_haves:
  truths:
    - "A full 346×2 build completes under the 20-min CI/Cloudflare timeout (data.ts memoization removes the O(n²) re-reads)."
    - "coverage.mjs exists and exposes route-count + no-orphan-link + sitemap-coverage + directory-completeness assertions (it may legitimately report gaps until later waves land)."
    - "rollout.mjs no longer hard-asserts enabled.length === 12 and treats 346/locale as the all-rollout expectation."
  artifacts:
    - path: "site/src/lib/data.ts"
      provides: "Build-time memoization for loadIndex, loadCommune, loadNationalAverage, loadRegionalAverage"
      contains: "_indexCache"
    - path: "site/scripts/validate/coverage.mjs"
      provides: "Coverage validator: route-count, no-orphan-link, sitemap-coverage, directory-completeness"
      min_lines: 60
    - path: "site/scripts/validate/rollout.mjs"
      provides: "Updated rollout assertion (346 default expectation, relaxed === 12 config check)"
      contains: "enabled.length"
  key_links:
    - from: "site/src/lib/data.ts"
      to: "loadCommune cache Map"
      via: "module-level Map keyed by cut"
      pattern: "_communeCache"
---

<objective>
Lay the Wave-0 foundation that must land BEFORE the rollout flip (Plan 02):

1. Add build-time memoization to `data.ts` so the 346×2 build (692 commune pages each re-reading ~700–1000 JSON files) stays inside the 20-min CI/Cloudflare timeout. This is the single highest-leverage task in the phase — without it the rollout flip risks a CI build timeout.
2. Scaffold the new `coverage.mjs` validator (route-count + no-orphan-link + sitemap-coverage + directory-completeness) that proves COMU-01/02/03 against `dist/`.
3. Update `rollout.mjs` so 346/locale is the all-rollout expectation and the `=== 12` config assertion no longer fails once the build emits all comunas.

Purpose: De-risk the build-timeout (the phase's only material risk per 11-RESEARCH.md Pitfall 2) and put the COMU-03 coverage validator in place so subsequent waves can be verified.
Output: Memoized `data.ts`, new `coverage.mjs`, updated `rollout.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-publish-346-comunas-finder/11-CONTEXT.md
@.planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md
@.planning/phases/11-publish-346-comunas-finder/11-VALIDATION.md

<interfaces>
<!-- Existing exports the executor builds on (from site/src/lib/data.ts). Use directly — no exploration needed. -->
From site/src/lib/data.ts:
```typescript
export interface CommuneMeta { cut: string; name: string; slug: string; region_id: string; population: number; low_population: boolean; }
export interface CommuneData extends CommuneMeta { /* …enriched: latestCompleteYear, regionName, series, national_rank, trend… */ }
export function loadIndex(): CommuneMeta[];                 // reads meta/index.json each call
export function loadCommune(cut: string): CommuneData;      // reads comunas/{cut}.json + loadRegion each call
export function loadNationalAverage(): number;             // re-reads ALL eligible communes each call
export function loadRegionalAverage(regionId: string): number; // re-reads region's communes each call
export function regionFileId(regionId: string): string;
export function latestCompleteYearRate(commune: CommuneData): number;
```
Note: `nearestComparable()` also calls `loadIndex()`/`loadCommune()` repeatedly — it benefits from the cache automatically once `loadCommune`/`loadIndex` are memoized; do NOT rewrite its logic.
</interfaces>

<!-- Validator conventions to mirror -->
@site/scripts/validate/rollout.mjs
@site/scripts/validate/all.mjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add build-time memoization to data.ts loaders</name>
  <read_first>
    - site/src/lib/data.ts (the file being modified — full read)
    - .planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md (Pitfall 2 + "Memoization for build-time loaders" code example, lines ~251-303)
  </read_first>
  <files>site/src/lib/data.ts</files>
  <behavior>
    - loadIndex() returns the identical array on repeated calls (read once, cached in a module-level variable).
    - loadCommune(cut) returns the identical enriched object on repeated calls for the same cut (cached in a module-level Map keyed by cut).
    - loadNationalAverage() computes once and returns the cached number on subsequent calls.
    - loadRegionalAverage(regionId) computes once per regionId and returns the cached number on subsequent calls.
    - All returned VALUES are unchanged vs. the pre-memoization implementation (memoization is transparent — no behavior change, only fewer disk reads).
  </behavior>
  <action>
    Add module-level memoization exactly as 11-RESEARCH.md prescribes (data is immutable within a single Astro build — Assumption A2). Introduce: a `let _indexCache: CommuneMeta[] | null` guard inside `loadIndex()`; a `const _communeCache = new Map<string, CommuneData>()` checked/populated inside `loadCommune()` (cache the fully-enriched object that includes `cut`, `latestCompleteYear`, `regionName`); a `let _natAvg: number | null` guard inside `loadNationalAverage()`; and a `const _regAvg = new Map<string, number>()` inside `loadRegionalAverage()` keyed by regionId. Also memoize `loadRegion()` per regionId with a Map (it is called once per commune via loadCommune and is pure). Preserve every existing computation, comment, and the `cut`-from-argument enrichment in loadCommune (the CR-fix at lines 152-158 must stay). Do NOT touch `nearestComparable()` — it inherits the speedup through the cached loaders. Do NOT add any cache-invalidation or TTL logic (build is single-pass).
  </action>
  <verify>
    <automated>cd site && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | grep -i "data.ts" || echo "data.ts typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `data.ts` contains `_indexCache`, `_communeCache` (a Map), `_natAvg`, and a regional-average Map.
    - `astro check` / `tsc --noEmit` reports zero new type errors in data.ts.
    - No exported function signature changed (loadIndex/loadCommune/loadNationalAverage/loadRegionalAverage keep their current return types).
  </acceptance_criteria>
  <done>data.ts memoizes index, per-cut commune, national average, regional average, and region loads at module scope; values identical to before; no signature or behavior change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold coverage.mjs validator (route-count + no-orphan-link + sitemap-coverage + directory-completeness)</name>
  <read_first>
    - site/scripts/validate/rollout.mjs (mirror its dist-inspection + assert/exit-code style)
    - .planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md ("New validator: coverage.mjs" spec, lines ~385-392 + the Phase Requirements→Test Map)
    - site/astro.config.mjs (sitemap emits dist/sitemap-0.xml; comuna URL shapes /commune/{slug}/ and /es/comuna/{slug}/)
  </read_first>
  <files>site/scripts/validate/coverage.mjs</files>
  <behavior>
    - Run against an existing dist/ (does NOT rebuild). Exits non-zero on any failed assertion, zero when all pass; prints a per-check PASS/FAIL line.
    - Reads the expected count from loadIndex().length (the canonical 346), not a hardcoded literal, so it tracks the real index.
    - ASSERTION A (route-count): count of dist/commune/* directories === count of dist/es/comuna/* directories === index length.
    - ASSERTION B (no-orphan-link): scan every dist/**/*.html, extract every href="/commune/{slug}/" and href="/es/comuna/{slug}/", assert each {slug} has a generated page (dist/commune/{slug}/index.html resp. dist/es/comuna/{slug}/index.html exists). Fail listing any orphan slug. This proves "0 dead-links".
    - ASSERTION C (sitemap-coverage): parse dist/sitemap-0.xml; assert it contains 346 .../commune/ <loc> and 346 .../es/comuna/ <loc> entries (=== index length), and that the directory URLs (/communes/ and /es/comunas/) are present.
    - ASSERTION D (directory-completeness): assert dist/communes/index.html contains >=346 distinct /commune/ hrefs and dist/es/comunas/index.html contains >=346 distinct /es/comuna/ hrefs (proves the full list is in server HTML — SEO-safe).
  </behavior>
  <action>
    Create `site/scripts/validate/coverage.mjs` following the structure of `rollout.mjs`: resolve `SITE_ROOT`/`DIST_DIR`, import `assert`, read `data/cead/meta/index.json` for the expected count (resolve via `path.resolve(SITE_ROOT, '..', 'data', 'cead', 'meta', 'index.json')` to avoid importing the TS loader from an .mjs). Implement assertions A–D as named, individually-reported checks (accumulate a `failures` counter like rollout.mjs, print each PASS/FAIL, `process.exit(1)` if any fail). For the HTML scan in B/D, read files recursively under dist and regex-match the href patterns; dedupe slugs with a Set. For C, regex `<loc>([^<]+)</loc>` over sitemap-0.xml. IMPORTANT: this scaffold is allowed to REPORT FAILURES at this wave (no directory pages or 346 rollout exist yet) — it is wired into the suite and made green in Plan 04 after the rollout + directory land. Do NOT register it in all.mjs yet (that is Plan 04's job). Add a header comment documenting each assertion and the usage `node scripts/validate/coverage.mjs`.
  </action>
  <verify>
    <automated>cd site && node -c scripts/validate/coverage.mjs && echo "coverage.mjs parses"</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/validate/coverage.mjs` exits 0 (valid JS, no syntax error).
    - File contains all four assertion blocks (route-count, no-orphan-link, sitemap-coverage, directory-completeness) and derives the expected count from index.json (not a hardcoded 346 literal as the source of truth).
    - The expected count read from `data/cead/meta/index.json` is the SAME source rollout.mjs's default branch now uses (single source of truth for 346 across both validators).
    - File is NOT yet listed in all.mjs's VALIDATORS array (deferred to Plan 04).
  </acceptance_criteria>
  <done>coverage.mjs exists, parses, implements A–D against dist/, derives 346 from index.json, and is ready to be wired in Plan 04.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update rollout.mjs so 346/locale is the default (no-EXPECT_ALL) all-rollout expectation</name>
  <read_first>
    - site/scripts/validate/rollout.mjs (the file being modified — full read)
    - .planning/phases/11-publish-346-comunas-finder/11-RESEARCH.md (Pitfall 1: the === 12 hard assertion)
  </read_first>
  <files>site/scripts/validate/rollout.mjs</files>
  <behavior>
    - After the flip, a default `node scripts/validate/rollout.mjs` run (NO EXPECT_ALL env) against a 346×2 dist passes: EN count === ES count === 346, all slugs reciprocal.
    - The config assertion no longer hard-fails when rollout.json still lists 12 cuts (rollout.json is now vestigial under ROLLOUT_ALL).
    - The Santiago/Maipú presence checks on rollout.json remain (config sanity).
  </behavior>
  <action>
    Make 346 the COMMITTED-DEFAULT expected dist count. Per 11-RESEARCH.md Pitfall 1: at line 65-66 change `expectedCount` so the DEFAULT branch (when `EXPECT_ALL` is NOT set) resolves to the full index length by reading `data/cead/meta/index.json` and using its array length (=346) — the SAME source coverage.mjs uses (`path.resolve(SITE_ROOT, '..', 'data', 'cead', 'meta', 'index.json')`). The default branch must NO LONGER read `rollout.enabled.length`. Keep `EXPECT_ALL=true` as an accepted ALIAS that also yields the index length (it is no longer REQUIRED to reach 346 — the whole point of this fix is that the committed CI command `npm run build && node scripts/validate/all.mjs`, which sets ROLLOUT_ALL but NOT EXPECT_ALL, asserts 346 by default and does not red-fail at 12). Relax the config assertion at line 34 from `assert.strictEqual(rollout.enabled.length, 12, …)` to a range check (`>= 1 && <= 346`) so the vestigial 12-entry rollout.json does not fail the validator. Keep the `includes('13101')` / `includes('13119')` sanity checks and ALL reciprocity/parity assertions (b) unchanged. Update the header comment + the trailing "Default rollout mode" log line to reflect that 346 (the index length) is now the default expectation regardless of EXPECT_ALL.
  </action>
  <verify>
    <automated>cd site && node -c scripts/validate/rollout.mjs && echo "rollout.mjs parses"</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/validate/rollout.mjs` exits 0.
    - `grep -n "strictEqual(rollout.enabled.length, 12" site/scripts/validate/rollout.mjs` returns nothing (the hard === 12 assertion is gone).
    - With NO EXPECT_ALL env set, rollout.mjs's `expectedCount` resolves to 346 (the index length); grep confirms the default branch at ~L66 no longer reads `rollout.enabled.length`.
    - `node scripts/validate/rollout.mjs` after a ROLLOUT_ALL build exits 0 WITHOUT EXPECT_ALL exported.
    - The default `expectedCount` derives from the index length (346), not from `rollout.enabled.length`.
    - Reciprocity (EN↔ES slug parity) assertions are unchanged.
  </acceptance_criteria>
  <done>rollout.mjs expects 346/locale by default (no EXPECT_ALL needed), the === 12 config assertion is relaxed to a range, and reciprocity checks are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo data → build | Only trusted, version-controlled CEAD JSON feeds the loaders. No external/user input in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | data.ts memoization caches | accept | Caches hold only build-time-derived values from trusted repo JSON; single-pass build, no concurrency, no invalidation surface. No mitigation needed. |
| T-11-02 | Denial of Service | CI/Cloudflare 20-min build budget | mitigate | Memoization removes the O(n²) re-reads that pushed the build toward the timeout (Pitfall 2); coverage/rollout validators are dist-only (seconds). |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (zero new deps); slopcheck N/A. cross-env install is in Plan 02. |
</threat_model>

<verification>
- `cd site && npx tsc --noEmit` (or `npm run check`) reports no new errors in data.ts.
- `node -c scripts/validate/coverage.mjs` and `node -c scripts/validate/rollout.mjs` both parse.
- Memoization is transparent: a pre-flip `npm run build` (still 12 comunas) must still succeed and produce identical page output (no behavior regression). Full 346 timing is measured in Plan 02/04.
</verification>

<success_criteria>
- data.ts memoizes index, per-cut commune, region, national average, and regional average at module scope with no behavior change.
- coverage.mjs scaffolded with all four assertions, derived count from index.json, not yet wired into all.mjs.
- rollout.mjs default expectation is 346/locale (no EXPECT_ALL required); the === 12 config assertion is relaxed; reciprocity preserved.
</success_criteria>

<output>
Create `.planning/phases/11-publish-346-comunas-finder/11-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
