---
phase: 17-robust-crime-index
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/snapshots/__init__.py
  - pipeline/snapshots/fetch_spd_homicide.py
  - pipeline/snapshots/fetch_sii_exposure.py
  - pipeline/snapshots/fetch_fiscalia_secuestro.py
  - data/snapshots/spd_homicide.json
  - data/snapshots/sii_exposure.json
  - data/snapshots/fiscalia_secuestro.json
  - data/snapshots/ATTRIBUTION.md
autonomous: true
requirements: [DQ-04]
user_setup: []
must_haves:
  truths:
    - "Three reproducible fetch/normalize scripts exist under pipeline/snapshots/, each with a CACHE_ONLY=1 offline path"
    - "data/snapshots/spd_homicide.json has >200 records keyed by cut/year/homicides"
    - "data/snapshots/sii_exposure.json has >200 records keyed by cut/year/empresas/trabajadores"
    - "data/snapshots/fiscalia_secuestro.json exists with a regional secuestro series"
    - "No raw xlsx/xlsb is committed; only normalized JSON under data/snapshots/"
  artifacts:
    - path: "pipeline/snapshots/fetch_spd_homicide.py"
      provides: "SPD VHC homicide-per-comuna/year normalizer with CACHE_ONLY support"
      min_lines: 50
    - path: "pipeline/snapshots/fetch_sii_exposure.py"
      provides: "SII PUB_COMU.xlsb empresas+trabajadores-per-comuna/year normalizer"
      min_lines: 50
    - path: "pipeline/snapshots/fetch_fiscalia_secuestro.py"
      provides: "Fiscalia regional secuestro series normalizer"
      min_lines: 30
    - path: "data/snapshots/spd_homicide.json"
      provides: "Normalized SPD homicide snapshot"
      contains: "records"
    - path: "data/snapshots/sii_exposure.json"
      provides: "Normalized SII exposure snapshot"
      contains: "records"
    - path: "data/snapshots/fiscalia_secuestro.json"
      provides: "Normalized Fiscalia secuestro snapshot"
      contains: "records"
    - path: "data/snapshots/ATTRIBUTION.md"
      provides: "Per-snapshot source/URL/licence/vintage attribution"
      contains: "SPD"
  key_links:
    - from: "pipeline/snapshots/fetch_spd_homicide.py"
      to: "data/cead/meta/catalog.json"
      via: "name->CUT join using make_slug normalization"
      pattern: "make_slug"
    - from: "pipeline/snapshots/fetch_spd_homicide.py"
      to: "data/snapshots/spd_homicide.json"
      via: "atomic_write_json output"
      pattern: "spd_homicide.json"
    - from: "pipeline/snapshots/fetch_sii_exposure.py"
      to: "data/snapshots/sii_exposure.json"
      via: "pyxlsb parse -> normalized JSON"
      pattern: "sii_exposure.json"
---

<objective>
Create three reproducible fetch/normalize snapshot scripts (SPD homicide per comuna/year, SII empresas+trabajadores per comuna/year, Fiscalia secuestro per region/year) and commit their small normalized JSON outputs under `data/snapshots/`, plus an attribution file.

Purpose: Make the newly-confirmed authoritative sources (SPD, SII, Fiscalia) reproducible and committed as reference snapshots for the future Phase 18 composite index — WITHOUT wiring any of them to a displayed number and WITHOUT touching the CEAD schema.
Output: `pipeline/snapshots/*.py` (3 scripts + `__init__.py`) and `data/snapshots/*.json` (3 snapshots + `ATTRIBUTION.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-robust-crime-index/17-CONTEXT.md
@.planning/phases/17-robust-crime-index/17-RESEARCH.md
@.planning/phases/17-robust-crime-index/17-WAVE0-RESEARCH-B.md

<interfaces>
<!-- Reuse existing helpers and the Wave 0 probe scripts as analogs. Confirmed present. -->

From pipeline/cead/normalizer.py:
- make_slug(name: str) -> str   # diacritic-safe slug; use for SPD COMUN_AGR -> CUT join

From pipeline/shared/atomic_write.py:
- atomic_write_json(path: pathlib.Path, data, compact: bool = False) -> None  # crash-safe write

Wave 0 analog scripts (read for the confirmed reading patterns):
- pipeline/experiments/probe_spd_homicide.py   # SPD xlsx columns: COMUN_AGR, ID_ANO; count rows per (COMUN_AGR, ID_ANO)
- pipeline/experiments/probe_sii_comu.py        # SII .xlsb: engine=pyxlsb, header=row index 4

Cache files already present (pipeline/cache/, git-ignored — DO NOT commit):
- pipeline/cache/spd_vhc.xlsx     (~547KB, sheet "Base de Datos", 8669 rows, 2018-2025)
- pipeline/cache/sii_pub_comu.xlsb (~1.2MB, sheet "Datos", header idx 4, 2005-2024, 347 comunas)

CUT join catalog:
- data/cead/meta/catalog.json   (CUT <-> commune name)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPD homicide + SII exposure snapshot scripts (xlsx/xlsb)</name>
  <read_first>
    - pipeline/experiments/probe_spd_homicide.py (confirmed SPD column names + row-count aggregation)
    - pipeline/experiments/probe_sii_comu.py (confirmed pyxlsb engine + header=4 + column names)
    - pipeline/cead/normalizer.py (make_slug — line 74)
    - pipeline/shared/atomic_write.py (atomic_write_json — line 13)
    - data/cead/meta/catalog.json (CUT<->name map for the join)
  </read_first>
  <files>pipeline/snapshots/__init__.py, pipeline/snapshots/fetch_spd_homicide.py, pipeline/snapshots/fetch_sii_exposure.py</files>
  <action>
    Create `pipeline/snapshots/__init__.py` (empty package marker). Create `fetch_spd_homicide.py` and `fetch_sii_exposure.py` following existing `pipeline/` script conventions (REPO_ROOT from `__file__`, module docstring with Source/URL/Vintage/Licence header, `main()` + `if __name__ == "__main__"`).

    Both scripts support `CACHE_ONLY=1` (env var): when set, parse from `pipeline/cache/` and skip the network download; otherwise download to the cache path first (use the SPD URL `https://prevenciondehomicidios.cl/wp-content/uploads/2026/03/Base_de_Datos_VHC_2018_2025.xlsx` and the SII URL `https://www.sii.cl/sobre_el_sii/empresas/PUB_COMU.xlsb`, with a descriptive User-Agent and `time.sleep(1)` courtesy delay; reuse the tenacity retry pattern from `pipeline/cead/client.py`).

    fetch_spd_homicide.py: read the cached `spd_vhc.xlsx` (sheet "Base de Datos") with pandas + openpyxl engine; aggregate `COUNT` of rows per `(COMUN_AGR, ID_ANO)` -> homicides; join `COMUN_AGR` name to CUT using `make_slug(COMUN_AGR) == make_slug(catalog_name)`; LOG unmatched names (do not silently drop). Output `data/snapshots/spd_homicide.json` with shape `{source, source_url, vintage:"2018-2025", fetched, caveat (2025 partial; HQ/name-join note), records:[{cut, name, year, homicides}]}`. Communes with zero homicides 2018-2025 are simply absent (represent missing != zero).

    fetch_sii_exposure.py: read cached `sii_pub_comu.xlsb` (sheet "Datos") with `engine="pyxlsb"` and `header=4`; columns of interest `Comuna del domicilio o casa matriz`, `Numero de empresas`, `Numero de trabajadores dependientes informados`, `Trabajadores ponderados por meses trabajados`; DROP the `Sin Informacion` bucket row; join name->CUT via make_slug. Output `data/snapshots/sii_exposure.json` with `{source, source_url, vintage:"2005-2024", fetched, caveat (HQ/domicile artifact), records:[{cut, name, year, empresas, trabajadores}]}`.

    SECURITY (ASVS L1, per 17-RESEARCH § Security Domain): wrap each `pd.read_excel()` in try/except; on parse failure raise a clear error and write NO output. Before writing, validate the record count is > 200 (SPD) / > 200 (SII) and raise if below threshold (guards against a silently-truncated parse). Use `atomic_write_json` for all writes. Do NOT commit the raw xlsx/xlsb — write only to `data/snapshots/`.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" ; $env:CACHE_ONLY="1"; python pipeline/snapshots/fetch_spd_homicide.py ; python pipeline/snapshots/fetch_sii_exposure.py ; python -c "import json,pathlib; a=json.loads(pathlib.Path('data/snapshots/spd_homicide.json').read_text(encoding='utf-8')); b=json.loads(pathlib.Path('data/snapshots/sii_exposure.json').read_text(encoding='utf-8')); assert len(a['records'])>200 and len(b['records'])>200; print('OK', len(a['records']), len(b['records']))"</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/snapshots/__init__.py` exists.
    - `CACHE_ONLY=1 python pipeline/snapshots/fetch_spd_homicide.py` runs offline and writes `data/snapshots/spd_homicide.json` with `len(records) > 200` and each record having `cut`, `name`, `year`, `homicides`.
    - `CACHE_ONLY=1 python pipeline/snapshots/fetch_sii_exposure.py` runs offline and writes `data/snapshots/sii_exposure.json` with `len(records) > 200` and each record having `cut`, `name`, `year`, `empresas`, `trabajadores`.
    - Both scripts contain a try/except around `read_excel` and a record-count guard before writing (grep for `try` + `except` near the read; grep for `> 200` or equivalent threshold).
    - Neither script commits a raw file: no `.xlsx`/`.xlsb` path appears under `data/` in `git status`.
    - The `Sin Informacion` bucket is dropped (grep the SII script).
  </acceptance_criteria>
  <done>Both xlsx/xlsb snapshots reproduce offline, pass the count guard, and write only normalized JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Fiscalia secuestro snapshot + attribution file</name>
  <read_first>
    - .planning/phases/17-robust-crime-index/17-WAVE0-RESEARCH-B.md (S5 — Fiscalia regional figures, 868 in 2024, region caveat)
    - pipeline/snapshots/fetch_spd_homicide.py (the analog written in Task 1 — match its docstring/output shape)
    - pipeline/shared/atomic_write.py (atomic_write_json)
  </read_first>
  <files>pipeline/snapshots/fetch_fiscalia_secuestro.py, data/snapshots/fiscalia_secuestro.json, data/snapshots/ATTRIBUTION.md</files>
  <action>
    Create `fetch_fiscalia_secuestro.py`. No downloadable dataset exists (Fiscalia publishes PDFs only), so the script embeds the known regional secuestro series from Wave 0 (17-WAVE0-RESEARCH-B.md S5: 868 nationally in 2024, top regions RM Sur / RM Centro Norte / Valparaiso) as a static, source-attributed dict and writes `data/snapshots/fiscalia_secuestro.json` with shape `{source:"Fiscalia de Chile - Boletin Estadistico", source_url:"https://www.fiscaliadechile.cl/persecucion-penal/estadisticas", vintage, fetched, caveat:"Regional granularity only; fiscalia regions != administrative regions (RM split); comuna-level not available (S5b deferred)", records:[{region, year, secuestros}]}`. Use `atomic_write_json`. Cite the Fiscalia URL as the source-of-truth; do not assert any number not traceable to Wave 0.

    Then create `data/snapshots/ATTRIBUTION.md` with one entry per snapshot file (SPD homicide, SII exposure, Fiscalia secuestro): source full name, exact URL, licence (Datos Abiertos Gobierno de Chile where applicable), vintage/coverage, and a one-line "reference-only — NOT wired to any displayed metric" note.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" ; python pipeline/snapshots/fetch_fiscalia_secuestro.py ; python -c "import json,pathlib; d=json.loads(pathlib.Path('data/snapshots/fiscalia_secuestro.json').read_text(encoding='utf-8')); assert len(d['records'])>=1 and 'region' in d['records'][0]; assert pathlib.Path('data/snapshots/ATTRIBUTION.md').exists(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python pipeline/snapshots/fetch_fiscalia_secuestro.py` writes `data/snapshots/fiscalia_secuestro.json` with a non-empty `records` array of `{region, year, secuestros}`.
    - JSON contains the regional caveat string and the Fiscalia source URL.
    - `data/snapshots/ATTRIBUTION.md` exists and contains entries naming SPD, SII, and Fiscalia with their URLs and a "reference-only / not wired" note.
  </acceptance_criteria>
  <done>Fiscalia regional snapshot + attribution file written; all three snapshots present and reference-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gov host -> snapshot script | Untrusted xlsx/xlsb bytes from SPD/SII enter `pd.read_excel()` |
| snapshot script -> repo | Normalized JSON written into committed `data/snapshots/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `pd.read_excel()` on spd_vhc.xlsx / sii_pub_comu.xlsb | mitigate | Wrap read in try/except; on failure raise and write NO output. Validate record count (> 200) before writing; raise on truncated/empty parse. |
| T-17-02 | Information Disclosure | raw downloaded xlsx/xlsb in repo | mitigate | Never commit raw files; they stay in git-ignored `pipeline/cache/`. Only normalized JSON under `data/snapshots/` is committed. |
| T-17-03 | Denial of Service | gov host download hang | accept | `CACHE_ONLY=1` path runs fully offline from Wave 0 cache; network path uses timeout + tenacity retry + `time.sleep(1)` courtesy delay. Low-value target, manual/cron-only invocation. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (`pandas`, `pyxlsb`, `openpyxl`, `requests`, `tenacity` already in requirements.txt — no install task). |
</threat_model>

<verification>
- All three scripts run (SPD/SII via `CACHE_ONLY=1`, Fiscalia directly) with no traceback.
- `data/snapshots/` contains 3 JSON files (each with non-empty `records`) + `ATTRIBUTION.md`.
- `git status` shows NO `.xlsx` or `.xlsb` staged.
- No file under `data/cead/` or `site/` was modified (snapshots are reference-only, not wired to UI).
</verification>

<success_criteria>
- DQ-04 satisfied: 3 reproducible scripts + 3 normalized snapshots + attribution, all reference-only.
- SPD/SII snapshots reproduce offline via `CACHE_ONLY=1`.
- No raw binary committed; no displayed metric or schema touched.
</success_criteria>

<output>
Create `.planning/phases/17-robust-crime-index/17-02-SUMMARY.md` when done.
(Note: `17-WAVE0-RESEARCH-A.md` / `17-WAVE0-RESEARCH-B.md` are Wave 0 research — do NOT overwrite. Use the `-SUMMARY.md` suffix.)
</output>
