---
phase: 17-robust-crime-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/scripts/verify_data_quality.py
  - .planning/phases/17-robust-crime-index/17-DATA-QUALITY.md
autonomous: true
requirements: [DQ-01]
must_haves:
  truths:
    - "17-DATA-QUALITY.md exists and records PASS/FAIL/DOCUMENTED per check with evidence"
    - "All 346 commune JSONs carry partial=True on the 2026 series entry"
    - "No 2025 series entry is marked partial=True (design behavior documented)"
    - "The 4 anomalies (Providencia, Lo Barnechea, San Joaquin, Recoleta) are re-derived from the offline CEAD cache and explained"
    - "tipoVal=1,2 additivity and drug scope (Ley 20.000) are confirmed in the report"
  artifacts:
    - path: "pipeline/scripts/verify_data_quality.py"
      provides: "Offline data-quality cross-check harness re-parsing pipeline/cache against data/cead/comunas"
      min_lines: 80
    - path: ".planning/phases/17-robust-crime-index/17-DATA-QUALITY.md"
      provides: "Evidence report with per-check PASS/FAIL/DOCUMENTED status"
      contains: "## Check 1"
  key_links:
    - from: "pipeline/scripts/verify_data_quality.py"
      to: "pipeline.cead.parser.parse_cead_table"
      via: "import + re-parse of cached HTML"
      pattern: "parse_cead_table"
    - from: "pipeline/scripts/verify_data_quality.py"
      to: "data/cead/comunas/*.json"
      via: "read committed JSON for cross-check"
      pattern: "data/cead/comunas"
---

<objective>
Build an offline data-quality verification harness that re-parses the CEAD cache and cross-checks every surfaced metric against its source, then emits a traceable evidence report `17-DATA-QUALITY.md`.

Purpose: Establish the evidence base proving the *current* displayed figures are correct (or documenting where they carry known, by-design artifacts) before the methodology is rewritten to describe them. No displayed data changes — this is read-only verification.
Output: `pipeline/scripts/verify_data_quality.py` (offline harness) and `.planning/phases/17-robust-crime-index/17-DATA-QUALITY.md` (evidence report).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-robust-crime-index/17-CONTEXT.md
@.planning/phases/17-robust-crime-index/17-RESEARCH.md
@.planning/phases/17-robust-crime-index/17-VALIDATION.md

<interfaces>
<!-- Battle-tested helpers the harness MUST reuse (do not hand-roll). Confirmed present in repo. -->

From pipeline/cead/parser.py:
- parse_cead_table(html: str) -> list[dict]   # rows: [{"name": str, "values": {year_str: "NNN,0000000000"}}]
- parse_cead_float(value: str) -> float | None  # CEAD latin-1 float format -> float

From pipeline/cead/client.py:
- load_cached(filename: str) -> str | None   # reads pipeline/cache/<filename>; None if absent

From pipeline/scrape_cead.py:
- mark_partial_year(...)  (line 67)  # reference logic for partial-year design; DO NOT modify it
- _match_cut(name, catalog) -> str | None  (line 598)  # name -> CUT join

Cache naming (pipeline/cache/, git-ignored):
- cead_15_{familia_id}_{year}.html   # all 346 communes per familia/year (region "15" is a dummy)

Anomaly communes (data/cead/comunas/<cut>.json):
- Providencia 13123 · Lo Barnechea 13115 · San Joaquin 13129 · Recoleta 13127
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build offline data-quality verification harness</name>
  <read_first>
    - pipeline/cead/parser.py (parse_cead_table, parse_cead_float — the only sanctioned CEAD parsers)
    - pipeline/cead/client.py (load_cached — line 196)
    - pipeline/scrape_cead.py (mark_partial_year line 67, _match_cut line 598 — reference only, do not modify)
    - data/cead/comunas/13123.json, 13115.json, 13129.json, 13127.json (the 4 anomaly communes)
    - pipeline/scripts/fetch_ine_population.py (existing script — match its import/path/CLI conventions)
  </read_first>
  <files>pipeline/scripts/verify_data_quality.py</files>
  <action>
    Create `pipeline/scripts/verify_data_quality.py` as a standalone, offline, read-only harness. It imports `parse_cead_table` and `parse_cead_float` from `pipeline.cead.parser` and `load_cached` from `pipeline.cead.client` — never a custom HTML parser. Resolve `REPO_ROOT` from `__file__` the same way existing `pipeline/scripts/` modules do.

    The harness runs these checks and collects a structured result (check name, status one of PASS/FAIL/DOCUMENTED, evidence string) for each:
    1. CEAD host string: assert the canonical host used by the scraper is `cead.minsegpublica.gob.cl` (read from `pipeline/cead/client.py`), and record the wrong host `cead.ministeriointerior.gob.cl` as the value to be corrected downstream.
    2. tipoVal semantics: assert additivity from Wave 0 facts (denuncias `1` + detenciones `2` == casos policiales `1,2`); confirm the pipeline uses `tipoVal="1,2"`.
    3. Partial-year guard coverage: load every `data/cead/comunas/*.json`; assert every commune that has a 2026 series entry carries `partial=True` on it, and assert no 2025 series entry carries `partial=True`. Record counts. Status DOCUMENTED with the design note that "past years are treated as complete; 2025 may be unconsolidated".
    4. Drug scope: assert `by_family.drogas` corresponds to Ley 20.000 (grupo 401), referencing the prior `quick-260616-klv` audit; DOCUMENTED.
    5-8. The 4 anomalies: for each anomaly commune, re-parse the relevant `cead_15_{familia}_{year}.html` from cache via `parse_cead_table` + `parse_cead_float`, and cross-check the re-derived value against the committed JSON figure (Providencia propiedad 2024 ~4423.5/100k; Lo Barnechea 2024 total rate ~2544.8; San Joaquin homicidios 2024=11, 2025=1; Recoleta homicidios 2024=34 -> ~17.3/100k). Each gets PASS (value reproduces) plus the explanation (floating-population denominator / partial-year / mathematically-correct).
    9. National/regional aggregation = population-weighted mean (reference `quick-260616-klv`); DOCUMENTED.
    10. Range/sanity: assert all surfaced `rate_per_100k` values across communes fall within [0, 100000]; FAIL with the offending CUTs listed if any are out of range.

    If a required cache file is absent, mark that specific check DOCUMENTED with evidence "cache file <name> not present; check skipped" rather than crashing — the harness must complete and write the report under all cache states. Provide a `main()` and `if __name__ == "__main__"` guard. After running checks, write the report to `.planning/phases/17-robust-crime-index/17-DATA-QUALITY.md`. Do NOT modify any file under `data/` or `pipeline/cead/` — read-only.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" ; python -c "import ast,pathlib; ast.parse(pathlib.Path('pipeline/scripts/verify_data_quality.py').read_text(encoding='utf-8')); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/scripts/verify_data_quality.py` parses as valid Python (ast.parse OK).
    - Source contains the strings `parse_cead_table`, `parse_cead_float`, `load_cached` (imports the sanctioned helpers — each grep returns >= 1).
    - Source references `data/cead/comunas` and the 4 anomaly CUTs `13123`, `13115`, `13129`, `13127`.
    - Source does NOT write to any path under `data/cead/` or `pipeline/cead/` (no `.write_text`/`.write_bytes`/`atomic_write` targeting those trees).
    - A `main()` function and `if __name__ == "__main__":` guard are present.
  </acceptance_criteria>
  <done>Harness exists, imports only sanctioned CEAD parsers, runs all 10 checks offline, and is read-only with respect to committed data.</done>
</task>

<task type="auto">
  <name>Task 2: Run harness and emit 17-DATA-QUALITY.md evidence report</name>
  <read_first>
    - pipeline/scripts/verify_data_quality.py (the harness from Task 1)
    - .planning/phases/17-robust-crime-index/17-RESEARCH.md (DQ-01 section — exact report section list, lines ~152-172)
  </read_first>
  <files>.planning/phases/17-robust-crime-index/17-DATA-QUALITY.md</files>
  <action>
    Execute `python pipeline/scripts/verify_data_quality.py` from the repo root so the script writes `.planning/phases/17-robust-crime-index/17-DATA-QUALITY.md`. The report MUST contain a `## Summary: N checks, N passed, N flags` line and one `## Check {n}: ...` heading per check with an explicit `[PASS]` / `[FAIL]` / `[DOCUMENTED]` tag and an evidence line. Use the exact 10-check structure from 17-RESEARCH.md DQ-01 (host, tipoVal, partial-year coverage, drug scope, Providencia, Lo Barnechea, San Joaquin, Recoleta, aggregation, range/sanity). If any check is FAIL, do NOT silently pass — surface the FAIL in the report and stop to investigate the underlying data before continuing the phase (a genuine FAIL here means a surfaced figure is wrong, which blocks DQ-03). Partial-year (San Joaquin 2025=1) and floating-population (Providencia) are expected DOCUMENTED-by-design outcomes, not failures.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" ; python -c "import pathlib; t=pathlib.Path('.planning/phases/17-robust-crime-index/17-DATA-QUALITY.md').read_text(encoding='utf-8'); assert '## Check 1' in t and '## Summary' in t and ('PASS' in t or 'DOCUMENTED' in t); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-robust-crime-index/17-DATA-QUALITY.md` exists.
    - Contains a `## Summary:` line with check counts.
    - Contains 10 `## Check N:` headings, each tagged `[PASS]`, `[FAIL]`, or `[DOCUMENTED]`.
    - Re-derived anomaly values for 13123/13115/13129/13127 are present with evidence lines.
    - No check is left FAIL without an explicit investigation note; design-by artifacts (San Joaquin partial-year, Providencia denominator) are tagged DOCUMENTED, not FAIL.
  </acceptance_criteria>
  <done>Evidence report written with all 10 checks resolved; any genuine FAIL is flagged for investigation, design artifacts are documented.</done>
</task>

</tasks>

<threat_model>
No new attack surface. This plan is offline, read-only verification: it re-parses already-cached HTML and reads committed JSON; it writes only a Markdown report under `.planning/`. No network calls, no untrusted binary parsing, no displayed-data mutation.
</threat_model>

<verification>
- `python pipeline/scripts/verify_data_quality.py` runs to completion with no traceback.
- `17-DATA-QUALITY.md` exists with the 10-check structure and a summary line.
- No file under `data/` or `pipeline/cead/` was modified (`git status` shows only the new script + report).
</verification>

<success_criteria>
- DQ-01 evidence report exists and is traceable (each check cites its evidence).
- The 4 anomalies are re-derived from cache and explained.
- Partial-year coverage (2026 partial, 2025 complete-by-design) is verified across all commune JSONs.
- Zero changes to displayed data or schema.
</success_criteria>

<output>
Create `.planning/phases/17-robust-crime-index/17-01-SUMMARY.md` when done.
(Note: `17-WAVE0-RESEARCH-A.md` and `17-WAVE0-RESEARCH-B.md` are Wave 0 research artifacts — do NOT overwrite them. Use the `-SUMMARY.md` suffix for plan execution summaries.)
</output>
