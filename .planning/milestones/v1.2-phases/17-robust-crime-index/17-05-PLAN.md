---
phase: 17-robust-crime-index
plan: 05
type: execute
wave: 3
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - .planning/phases/17-robust-crime-index/17-VERIFICATION.md
autonomous: false
requirements: [DQ-01, DQ-02, DQ-03, DQ-04]
must_haves:
  truths:
    - "Chained build+validate passes (OneDrive-safe, single command) with all validators green"
    - "The wrong host ministeriointerior.gob.cl is absent from built site/dist/ HTML"
    - "Forbidden-language validator #9 passes on the rewritten methodology pages"
    - "BrowserOS confirms both methodology pages render with correct host + working clickable source links"
    - "17-VERIFICATION.md records the phase gate result per requirement"
  artifacts:
    - path: ".planning/phases/17-robust-crime-index/17-VERIFICATION.md"
      provides: "Phase 17 verification gate record (per-requirement PASS/FAIL + evidence)"
      contains: "DQ-01"
  key_links:
    - from: "site/dist/"
      to: "host-string gate"
      via: "grep for ministeriointerior after build"
      pattern: "ministeriointerior"
---

<objective>
Run the phase verification gate: one chained OneDrive-safe `build && validate`, the dist-level wrong-host grep gate, the snapshot file/schema checks, a BrowserOS visual check of both methodology pages, and write `17-VERIFICATION.md`.

Purpose: Prove the whole focused phase is correct end-to-end — figures verified, host fixed everywhere (incl. built HTML), methodology rewritten and rendering with working links, snapshots present — without any displayed-metric or schema change.
Output: `.planning/phases/17-robust-crime-index/17-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-robust-crime-index/17-VALIDATION.md
@.planning/phases/17-robust-crime-index/17-DATA-QUALITY.md
@data/SOURCES.md

<interfaces>
<!-- OneDrive build-desync: build + validate MUST be ONE chained command (dist/ is swept between separate shells). -->
Chained gate:   cd site ; npm run build ; npm run validate   (PowerShell: chain with ';' in one Bash call)
Forbidden-language validator = #9 in site/scripts/validate/all.mjs
Methodology routes: /methodology/  and  /es/metodologia/
BrowserOS MCP: http://127.0.0.1:9200/mcp (memory: browseros-mcp-port). save_screenshot needs a no-spaces path. Run inline (gsd-executor lacks browseros).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chained build+validate + dist host gate + snapshot checks</name>
  <read_first>
    - .planning/phases/17-robust-crime-index/17-VALIDATION.md (per-requirement automated signals)
    - site/scripts/validate/all.mjs (validator list; #9 = forbidden-language)
    - data/snapshots/spd_homicide.json, sii_exposure.json, fiscalia_secuestro.json (Plan 02 outputs)
  </read_first>
  <files>.planning/phases/17-robust-crime-index/17-VERIFICATION.md</files>
  <action>
    Run the OneDrive-safe chained gate as a SINGLE Bash invocation: `cd site && npm run build && npm run validate` (do NOT split build and validate across separate tool calls — `dist/` is swept between processes). All validators (including #9 forbidden-language) must pass.

    Then run the dist-level host gate: grep `site/dist/` recursively for `ministeriointerior.gob.cl` — it MUST be absent (CLEAN). Confirm `minsegpublica.gob.cl` is present in the built methodology HTML.

    Then run the snapshot file/schema checks: assert `data/snapshots/spd_homicide.json` and `sii_exposure.json` each have `len(records) > 200`, and `data/snapshots/fiscalia_secuestro.json` exists with a non-empty regional `records` array.

    Record every result (command, expected, actual, PASS/FAIL) into a draft of `17-VERIFICATION.md`. If the chained gate or the host gate fails, STOP and report — do not write a PASS verification.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe\site" ; npm run build ; npm run validate ; cd .. ; python -c "import pathlib,glob; d=''.join(pathlib.Path(p).read_text(encoding='utf-8',errors='ignore') for p in glob.glob('site/dist/**/*.html',recursive=True)); assert 'ministeriointerior' not in d, 'wrong host still in dist'; assert 'minsegpublica.gob.cl' in d; import json; assert len(json.loads(pathlib.Path('data/snapshots/spd_homicide.json').read_text(encoding='utf-8'))['records'])>200; assert len(json.loads(pathlib.Path('data/snapshots/sii_exposure.json').read_text(encoding='utf-8'))['records'])>200; assert pathlib.Path('data/snapshots/fiscalia_secuestro.json').exists(); print('GATE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && npm run validate` exits 0 with all validators (incl. #9) green.
    - `ministeriointerior.gob.cl` is ABSENT from all `site/dist/**/*.html`.
    - `minsegpublica.gob.cl` is PRESENT in built methodology HTML.
    - `spd_homicide.json` and `sii_exposure.json` each have > 200 records; `fiscalia_secuestro.json` exists with non-empty records.
    - Results recorded in a draft `17-VERIFICATION.md`.
  </acceptance_criteria>
  <done>Build green, dist host clean, snapshots present — automated gate passes and is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BrowserOS visual check of EN+ES methodology pages</name>
  <read_first>
    - .planning/phases/17-robust-crime-index/17-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <files>(verification only — no files modified; renders the built methodology pages)</files>
  <action>
    Serve the built site (preview `site/dist/` or run the local dev server) and drive BrowserOS inline (the gsd-executor lacks browseros; run at http://127.0.0.1:9200/mcp). Follow the how-to-verify steps below: navigate `/methodology/` and `/es/metodologia/`, take_snapshot, confirm the corrected host + four clickable source links + all caveats, click each link, and spot-check 2-3 commune pages for source+year attribution. save_screenshot to a no-spaces path. This is verification only — modify no files.
  </action>
  <what-built>
    Rewritten EN (`/methodology/`) and ES (`/es/metodologia/`) methodology pages with the corrected CEAD host and clickable links to CEAD, SPD, SII, and Fiscalia, plus all data-quality caveats. The build+validate gate (Task 1) already passed.
  </what-built>
  <how-to-verify>
    Ensure BrowserOS is running at http://127.0.0.1:9200/mcp. Run BrowserOS inline (not via gsd-executor). For the served build (preview the dist or local dev server):
    1. Navigate to `/methodology/`. take_snapshot. Confirm the page shows host `cead.minsegpublica.gob.cl` (NOT `ministeriointerior`) and visible clickable links to CEAD, SPD (prevenciondehomicidios.cl), SII (sii.cl), and Fiscalia (fiscaliadechile.cl). save_screenshot to a no-spaces path.
    2. Click each source link; confirm each resolves to the correct host (or opens the right external URL).
    3. Confirm the caveats are visible: casos-policiales semantics, partial-year note, drug scope, floating-population denominator.
    4. Navigate to `/es/metodologia/`. Repeat 1-3; confirm ES parity (same sections, same links).
    5. Open 2-3 commune pages; confirm sampled figures still cite source + year.
  </how-to-verify>
  <acceptance_criteria>
    - Both pages render with `cead.minsegpublica.gob.cl` and no `ministeriointerior`.
    - All four source links are present, clickable, and resolve to the correct host on both pages.
    - Caveats (casos policiales, partial-year, drug scope, floating-population) are visible on both pages.
    - Sampled commune/ranking figures still show source + year.
  </acceptance_criteria>
  <verify>
    <human-check>BrowserOS confirms both methodology pages (EN + ES) render with host cead.minsegpublica.gob.cl, four working clickable source links, and all caveats visible; sampled commune pages still cite source + year.</human-check>
  </verify>
  <done>Both methodology pages visually verified in a real browser via BrowserOS; reviewer approved.</done>
  <resume-signal>Type "approved" if both pages render correctly with working links, or describe the issue (e.g. broken link, missing caveat, ES drift).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize 17-VERIFICATION.md phase gate record</name>
  <read_first>
    - .planning/phases/17-robust-crime-index/17-VERIFICATION.md (Task 1 draft)
    - .planning/phases/17-robust-crime-index/17-VALIDATION.md (the per-requirement signals to attest)
  </read_first>
  <files>.planning/phases/17-robust-crime-index/17-VERIFICATION.md</files>
  <action>
    Finalize `17-VERIFICATION.md` with a per-requirement row for DQ-01, DQ-02, DQ-03, DQ-04 — each citing the concrete evidence (DQ-01: 17-DATA-QUALITY.md check counts; DQ-02: SOURCES.md exists + dist host CLEAN; DQ-03: build green + validator #9 pass + BrowserOS approval; DQ-04: 3 snapshot files with record counts). Record the BrowserOS result from Task 2 (approved / issues). State explicitly that NO displayed metric, choropleth, or schema changed (scope guardrail attestation). If any requirement is not fully met, mark it FAIL with the gap, do not write an all-PASS gate.
  </action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" ; python -c "import pathlib; t=pathlib.Path('.planning/phases/17-robust-crime-index/17-VERIFICATION.md').read_text(encoding='utf-8'); assert all(r in t for r in ['DQ-01','DQ-02','DQ-03','DQ-04']); assert 'schema' in t.lower(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `17-VERIFICATION.md` exists with a per-requirement row for DQ-01..DQ-04, each with cited evidence and PASS/FAIL.
    - The BrowserOS result from Task 2 is recorded.
    - An explicit attestation that no displayed metric / choropleth / schema changed is present.
  </acceptance_criteria>
  <done>Phase 17 verification gate recorded; all four requirements attested with evidence.</done>
</task>

</tasks>

<threat_model>
No new attack surface. This plan runs the build/validate toolchain, greps built HTML, reads committed JSON, and drives a local BrowserOS instance against the local build. No new code, no external input parsing. (The snapshot-script attack surface is mitigated in Plan 02's threat model.)
</threat_model>

<verification>
- Chained `build && validate` exits 0; all validators (incl. #9) green.
- `ministeriointerior.gob.cl` absent from `site/dist/`; `minsegpublica.gob.cl` present.
- 3 snapshots present with expected record counts.
- BrowserOS visual check approved for both methodology pages.
- `17-VERIFICATION.md` records the per-requirement gate + no-schema-change attestation.
</verification>

<success_criteria>
- All of DQ-01..DQ-04 attested with concrete evidence.
- Site builds green, host fixed in built HTML, methodology renders with working links.
- Scope guardrail honored: zero displayed-metric / choropleth / schema change.
</success_criteria>

<output>
Create `.planning/phases/17-robust-crime-index/17-05-SUMMARY.md` when done.
(Use the `-SUMMARY.md` suffix — do NOT overwrite Wave 0 `17-0N-SUMMARY.md`.)
</output>
