---
phase: 12-home-ia-redesign-comuna-page-hub-and-spoke
plan: 06
type: execute
wave: 3
depends_on: ["12-03", "12-04", "12-05"]
files_modified:
  - site/scripts/validate/spine.mjs
  - site/scripts/validate/all.mjs
autonomous: false
requirements: [IA-01, IA-02, IA-03]
must_haves:
  truths:
    - "A new spine.mjs validator asserts: map-spoke targets exist, /rankings/ + /es/rankings/ exist in dist and sitemap, every commune page links back to /communes/, the home has exactly one H1, and the Rankings nav link is present site-wide"
    - "spine.mjs is registered in all.mjs and runs in the full suite"
    - "The full validator suite passes against a fresh ROLLOUT_ALL build with all Phase-12 changes landed"
    - "Manual smoke confirms /map/?cut=<cut> focuses the comuna"
  artifacts:
    - path: "site/scripts/validate/spine.mjs"
      provides: "IA-01/02/03 spine assertions over dist/"
      contains: "rankings"
    - path: "site/scripts/validate/all.mjs"
      provides: "spine.mjs registered in the validator list"
      contains: "spine.mjs"
  key_links:
    - from: "site/scripts/validate/all.mjs"
      to: "spine.mjs"
      via: "VALIDATORS array entry"
      pattern: "spine.mjs"
    - from: "site/scripts/validate/spine.mjs"
      to: "dist/ HTML + sitemap"
      via: "fs reads + regex assertions (coverage.mjs pattern)"
      pattern: "DIST_DIR"
---

<objective>
Close the phase with an extended link/structure validator (D-13) and a full-suite gate. Create spine.mjs (mirroring coverage.mjs) asserting the new cross-links and structure are real and reciprocal, register it in all.mjs, run the full ROLLOUT_ALL build + validator suite green, and run a human smoke check of the map focus param. This is the goal-backward gate for IA-01/02/03.

Purpose: D-13 requires the validator be extended to enforce reciprocity + zero 404 across the new cross-links. Without it, the spine can silently rot.
Output: 1 new validator + 1 suite registration + a passing full build/validate + human map-focus checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-CONTEXT.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md

<interfaces>
<!-- Contracts the executor uses directly. -->

site/scripts/validate/coverage.mjs (the analog to copy — confirmed structure):
  - imports readFileSync, existsSync, readdirSync from node:fs; fileURLToPath; path
  - SITE_ROOT = resolve(__dirname,'../..'); DIST_DIR = join(SITE_ROOT,'dist')
  - collectHtmlFiles(dir) recursive helper (L107-120)
  - sitemap read at DIST_DIR/sitemap-0.xml with LOC_RE = /<loc>([^<]+)<\/loc>/g (L173-198)
  - exit pattern: if (failures>0) process.exit(1) else process.exit(0)

site/scripts/validate/all.mjs (the file being modified):
  - VALIDATORS array L30-41 currently ends with 'coverage.mjs'
  - each validator spawned with env ROLLOUT_ALL: 'true' (L59)

Build command: cd site && npm run build  (this sets cross-env ROLLOUT_ALL=true automatically)
Full suite: cd site && npm run validate  (runs all.mjs)

Sitemap note: under ROLLOUT_ALL=true the astro.config sitemapFilter returns true for ALL urls, so /rankings/ + /es/rankings/ are auto-included — no astro.config change needed; spine.mjs only ASSERTS their presence.

OneDrive gotcha (project memory): dist/ artifacts desync between separate processes — ALWAYS chain build + validate in ONE command.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spine.mjs with IA-01/02/03 assertions</name>
  <read_first>
    - site/scripts/validate/coverage.mjs (the analog — copy imports, SITE_ROOT/DIST_DIR, collectHtmlFiles, sitemap LOC_RE, the failures counter + exit pattern)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md Finding 6 (assertions F-I) + Validation Architecture
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md spine.mjs section
  </read_first>
  <action>
    Create site/scripts/validate/spine.mjs mirroring coverage.mjs (same imports, SITE_ROOT/DIST_DIR resolution, collectHtmlFiles helper, failures counter, process.exit pattern). Implement assertions, each logging PASS/FAIL and incrementing failures:
    - F (map-spoke targets exist): dist/map/index.html AND dist/es/mapa/index.html exist.
    - G (rankings pages exist): dist/rankings/index.html AND dist/es/rankings/index.html exist.
    - H (rankings in sitemap): dist/sitemap-0.xml <loc> entries include a /rankings/ URL AND a /es/rankings/ URL (use the LOC_RE regex from coverage.mjs).
    - I (breadcrumb reciprocity): every dist/commune/*/index.html contains href="/communes/" AND every dist/es/comuna/*/index.html contains href="/es/comunas/" (sample is fine: assert on ALL collected commune html, fail listing up to 5 offenders).
    - J (map-spoke present on ficha): every commune page HTML contains the substring map/?cut= (EN) and /es/mapa/?cut= (ES respectively).
    - K (home single H1): dist/index.html and dist/es/index.html each contain exactly one <h1 occurrence (count /<h1[\s>]/g matches === 1).
    - L (rankings nav site-wide): a sample of representative dist pages (home, a commune, the map page) contains href="/rankings/" (EN pages) / href="/es/rankings/" (ES pages) in the nav.
    Use grep-safe counting (filter is over HTML, not source comments). Exit 0 only if failures === 0.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3 && node scripts/validate/spine.mjs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - site/scripts/validate/spine.mjs exists and references DIST_DIR + sitemap-0.xml
    - Running spine.mjs against a fresh build prints PASS for assertions F,G,H,I,J,K,L
    - node scripts/validate/spine.mjs exits 0 after a full build
  </acceptance_criteria>
  <done>spine.mjs created with assertions F-L; passes against a fresh ROLLOUT_ALL build.</done>
</task>

<task type="auto">
  <name>Task 2: Register spine.mjs in the validator suite + run full gate</name>
  <read_first>
    - site/scripts/validate/all.mjs (VALIDATORS array L30-41; spawn env L59)
  </read_first>
  <action>
    In site/scripts/validate/all.mjs, add 'spine.mjs' to the VALIDATORS array (after 'coverage.mjs') and add a one-line comment entry (#11 spine.mjs — IA spine cross-link reciprocity) in the header doc block. Then run the full chained build + validate gate in a SINGLE command (OneDrive desync gotcha): cd site && npm run build && npm run validate. All validators including spine.mjs must pass. If any pre-existing validator regresses due to Phase-12 changes, fix the offending Phase-12 output (not the validator) — except spine.mjs itself which you authored.
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - all.mjs VALIDATORS array contains 'spine.mjs'
    - cd site && npm run build && npm run validate exits 0
    - The suite summary shows spine PASS and all other validators PASS
  </acceptance_criteria>
  <done>spine.mjs registered; full suite green against fresh ROLLOUT_ALL build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke check — map focus param + home/ficha visual</name>
  <what-built>
    The map island now reads ?cut= (12-02); the home is rebuilt on Hybrid C+B (12-03); each ficha is a hub with a map-spoke link (12-04); nav + footer spine + /rankings/ added (12-05). All automated validators pass.
  </what-built>
  <how-to-verify>
    1. cd site && npm run dev  (predev syncs data; incidents layer empty is expected).
    2. Visit http://localhost:4321/map/?cut=13101 — the map should fly to and select Santiago (commune highlighted, panel/flyToBounds engaged). Visit http://localhost:4321/map/ — unchanged default view. Visit http://localhost:4321/map/?cut=zzz — no crash, default view.
    3. Visit http://localhost:4321/ and /es/ — confirm Hybrid C+B above the fold: search hero, two lead ranking tables (lowest + highest reported rate), map CTA, guide cards; condensed prose below; single H1; Rankings in nav.
    4. Visit a commune page (e.g. /commune/santiago/) — confirm breadcrumb Home > Communes > Region > Santiago, a map-spoke link, similar-comunas table, crime-type links, regional-rank "#N of M"; click the map-spoke and confirm it lands focused.
    5. Confirm the footer spine row (map / communes / rankings / editorial) appears on every page in both locales.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "map does not fly to commune", "double H1 on /es/").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validator inputs | spine.mjs reads only build-produced dist/ files (trusted local artifacts) — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | spine.mjs reads dist/ | accept | Validator reads only local build output; no network, no user input. Severity: none. |
| T-12-SC | Tampering | npm installs | accept | No new packages (RESEARCH audit: none). |
</threat_model>

<verification>
- cd site && npm run build && npm run validate exits 0 with spine.mjs PASS and all prior validators PASS.
- Human smoke confirms map focus param + Hybrid C+B home + ficha hub + footer spine.
</verification>

<success_criteria>
- spine.mjs enforces IA-01/02/03 cross-link reciprocity + structure (assertions F-L); registered in all.mjs.
- Full suite green against a fresh ROLLOUT_ALL build.
- Human-verified: /map/?cut= focuses the comuna; home + ficha + spine render as designed.
</success_criteria>

<output>
Create `.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-06-SUMMARY.md` when done.
</output>
