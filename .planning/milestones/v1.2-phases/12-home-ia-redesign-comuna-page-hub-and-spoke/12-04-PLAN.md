---
phase: 12-home-ia-redesign-comuna-page-hub-and-spoke
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - site/src/pages/commune/[slug].astro
  - site/src/pages/es/comuna/[slug].astro
autonomous: true
requirements: [IA-02]
must_haves:
  truths:
    - "Every comuna page shows a breadcrumb Home > Communes > Region > Commune (Inicio > Comunas > Region > Comuna in ES), with the Communes level linking the directory and the commune name as the terminal aria-current item"
    - "Every comuna page links to the map centered on itself via /map/?cut=<cut> (/es/mapa/?cut=<cut> in ES), replacing the old MapPlaceholderSlot"
    - "Every comuna page shows a similar-comunas-in-this-region spoke (multiple communes, nearest rate, same region)"
    - "Every comuna page shows per-crime-type ranking spoke links to existing /crime/[family] (/es/delito/[family]) pages"
    - "Every comuna page shows the regional-rank device #N of M with M = count of communes in the region"
  artifacts:
    - path: "site/src/pages/commune/[slug].astro"
      provides: "upgraded breadcrumb + map spoke + similar-comunas spoke + crime-type spoke + regional-rank device (EN)"
      contains: "/communes/"
    - path: "site/src/pages/es/comuna/[slug].astro"
      provides: "ES mirror of all spokes + breadcrumb"
      contains: "/es/comunas/"
  key_links:
    - from: "site/src/pages/commune/[slug].astro map spoke"
      to: "/map/?cut=<cut>"
      via: "static anchor using data.cut"
      pattern: "map/\\?cut="
    - from: "site/src/pages/commune/[slug].astro similar spoke"
      to: "nearestComparables(cut, n)"
      via: "data helper from 12-01 + CommuneRankingTable"
      pattern: "nearestComparables"
    - from: "site/src/pages/commune/[slug].astro breadcrumb"
      to: "/communes/"
      via: "static anchor"
      pattern: "href=.\\/communes\\/"
---

<objective>
Turn each comuna page into a hub (spike 003): upgrade the breadcrumb to Inicio > Comunas > Region > Comuna (D-09), replace the MapPlaceholderSlot with a static map-spoke CTA /map/?cut=<cut> (D-08, consumes the focus param from 12-02), add a similar-comunas-in-this-region spoke using nearestComparables from 12-01 (D-07), add per-crime-type ranking spoke links to existing /crime/[family] pages (D-10), and add the regional-rank device #N of M (the spike-003 engagement hook). EN + ES parity (IA-02).

Phase-16 boundary: the news spoke is DEFERRED — do NOT add any news section here.

Purpose: The audit found rankings and comuna pages that do not cross-link; this makes every ficha a connected hub.
Output: targeted additions to both commune page templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-CONTEXT.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md
@.planning/spikes/003-comuna-page-spokes/comuna-page.html
@.planning/spikes/003-comuna-page-spokes/README.md

<interfaces>
<!-- Contracts the executor uses directly. -->

site/src/pages/commune/[slug].astro (the file being modified — confirmed line refs):
  - breadcrumb at L125-129: currently Home > region only (no Communes level, no terminal commune name)
  - MapPlaceholderSlot import L22, usage L191 (REPLACE with map spoke — Pitfall 7)
  - data available: data.cut, data.name, data.slug, data.regionName, data.region_id, data.regional_rank, data.national_rank
  - byFamily already computed at L68 (Record of family keys present that year)
  - regionSlug already computed at L75 via loadRegion(regionFileId(data.region_id)).slug
  - loadIndex already imported (L33) — use it for region commune count
  - regional_rank StatCard at L150-153

From site/src/lib/data.ts (nearestComparables added in 12-01):
  nearestComparables(targetCut: string, n: number): RankingRow[]   // same-region, nearest rate
  loadIndex(): CommuneMeta[]   // CommuneMeta.region_id is the grouping field

From site/src/config/i18n.ts:
  FAMILY_SLUGS at L354: { vida:{en:'homicide',es:'homicidios'}, propiedad:{en:'property',es:'propiedad'}, ... }

From site/src/components/CommuneRankingTable.astro:
  Props { rows: RankingRow[]; locale }  // reuse for the similar-comunas spoke

ES mirror: site/src/pages/es/comuna/[slug].astro breadcrumb at L127-131 (Inicio > region), MapPlaceholderSlot at L193.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade breadcrumb + add regional-rank device on EN commune page</name>
  <read_first>
    - site/src/pages/commune/[slug].astro (breadcrumb L125-129, regional_rank StatCard L150-153, frontmatter data block L50-95)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md Pattern 3 + Pattern 7 + Pitfall 3 + Pitfall 8
  </read_first>
  <action>
    In site/src/pages/commune/[slug].astro: (1) Replace the breadcrumb (L125-129) with four levels: Home (href="/") then Communes (href="/communes/") then data.regionName (href region/regionSlug) then a terminal data.name rendered as a span with aria-current="page" (NOT an anchor) per Pitfall 8. Keep the chevron separators with aria-hidden. (2) In frontmatter, after the existing data block, add regionCommuneCount = loadIndex().filter where c.region_id === data.region_id, then .length; add a comment noting BUGFIX-999.1 (Tarapaca region_id collision may skew this count — accept, do not fix here). (3) Update the existing regional-rank StatCard value (L150-153) to show #regional_rank of regionCommuneCount. Hardcode all EN hrefs.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - dist/commune/santiago/index.html breadcrumb contains href="/communes/" AND aria-current="page"
    - dist/commune/santiago/index.html shows the regional-rank "of" count device
    - [slug].astro frontmatter computes regionCommuneCount via loadIndex filter on region_id
    - cd site && npm run build exits 0
  </acceptance_criteria>
  <done>EN breadcrumb has Communes level + terminal aria-current commune; regional-rank device shows #N of M.</done>
</task>

<task type="auto">
  <name>Task 2: Replace MapPlaceholderSlot with map / similar / crime-type spokes on EN commune page</name>
  <read_first>
    - site/src/pages/commune/[slug].astro (MapPlaceholderSlot import L22 + usage L191; byFamily L68; imports block L24-37)
    - site/src/components/CommuneRankingTable.astro (reuse for similar-comunas spoke)
    - site/src/config/i18n.ts FAMILY_SLUGS L354-362
    - .planning/spikes/003-comuna-page-spokes/comuna-page.html (spoke layout reference)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md Pattern 6 + Pattern 8 + Pitfall 7
  </read_first>
  <action>
    In site/src/pages/commune/[slug].astro: (1) Remove the MapPlaceholderSlot import (L22) and replace its usage (L191) with a map-spoke section: an H2 plus a static anchor href map/?cut= using data.cut (the focus param is read by the island per 12-02), labelled e.g. "See {data.name} on the interactive Chile crime map". Do NOT embed Leaflet — link only (zero client:*). (2) Add a similar-comunas-in-this-region spoke: in frontmatter call nearestComparables(cut, 5) from data.ts and render the result via CommuneRankingTable with locale="en" under an H2 framed as same-region reported incidence (Editorial Tone Guard — no absolute safe/dangerous labels). (3) Add a per-crime-type ranking spoke: import FAMILY_SLUGS from config/i18n.ts and render a list of anchors href crime/<en-slug>/ for each key in byFamily using FAMILY_SLUGS[key]?.en (fallback to key), under an H2 "Rankings by crime type". Leave BaseLayout's mountType prop unchanged (do not break the existing island mount contract); only the placeholder component is removed. Hardcode all EN hrefs.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3 && node scripts/validate/commune.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dist/commune/santiago/index.html contains the substring map/?cut=
    - dist/commune/santiago/index.html contains at least one href to /crime/
    - dist/commune/santiago/index.html contains a commune-ranking-table (the similar-comunas spoke)
    - dist/commune/santiago/index.html contains NO MapPlaceholderSlot dashed-box markup (placeholder removed)
    - commune.mjs validator passes; cd site && npm run build exits 0
  </acceptance_criteria>
  <done>EN ficha: map spoke link + similar-comunas spoke + crime-type spoke; MapPlaceholderSlot removed; commune validator passes.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror all spokes + breadcrumb on ES commune page</name>
  <read_first>
    - site/src/pages/es/comuna/[slug].astro (breadcrumb L127-131, MapPlaceholderSlot L193, frontmatter mirrors EN)
    - site/src/pages/commune/[slug].astro (the EN version after Tasks 1-2 — mirror it ES-localized)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md es/comuna section + Hardcoded Per-Locale Slug Pattern
  </read_first>
  <action>
    In site/src/pages/es/comuna/[slug].astro, mirror the EN changes from Tasks 1-2, ES-localized: (1) breadcrumb Inicio (href="/es/") then Comunas (href="/es/comunas/") then data.regionName (href /es/region/regionSlug/) then terminal data.name as span aria-current="page". (2) regionCommuneCount + regional-rank StatCard #N de M (same frontmatter computation; same BUGFIX-999.1 comment). (3) map spoke anchor href /es/mapa/?cut= using data.cut (hardcoded ES slug). (4) similar-comunas spoke via nearestComparables(cut, 5) rendered with CommuneRankingTable locale="es". (5) crime-type spoke anchors href /es/delito/<es-slug>/ using FAMILY_SLUGS[key]?.es. Remove the MapPlaceholderSlot import + usage. ES editorial tone (reported CEAD incidence, never absolute labels). Every ES slug hardcoded per-locale; never getRelativeLocaleUrl for localized slugs.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3 && node scripts/validate/commune.mjs 2>&1 | tail -5 && node scripts/validate/hreflang.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dist/es/comuna/santiago/index.html breadcrumb contains href="/es/comunas/" AND aria-current="page"
    - dist/es/comuna/santiago/index.html contains the substring /es/mapa/?cut=
    - dist/es/comuna/santiago/index.html contains at least one href to /es/delito/
    - dist/es/comuna/santiago/index.html contains a commune-ranking-table (similar-comunas spoke)
    - commune.mjs + hreflang.mjs validators pass; cd site && npm run build exits 0
  </acceptance_criteria>
  <done>ES ficha mirrors EN at full parity with ES slugs hardcoded; commune + hreflang validators pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commune page hrefs | All spoke hrefs are computed at build time from trusted data (data.cut, FAMILY_SLUGS, slugs from index) — no user input crosses into these pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | static spoke hrefs (map/?cut=, crime, communes) | accept | All hrefs are built from trusted build-time data (CUT from index.json, FAMILY_SLUGS constants, region slug). No user input, no client JS on the ficha. The /map/?cut= value originates from data.cut, not from a request. Severity: none (static, no untrusted input). |
| T-12-SC | Tampering | npm installs | accept | No new packages (RESEARCH audit: none). |
</threat_model>

<verification>
- cd site && npm run build exits 0.
- commune.mjs + hreflang.mjs validators pass.
- Both EN + ES ficha pages: 4-level breadcrumb with directory link + aria-current terminal; map spoke (map/?cut= and /es/mapa/?cut=); similar-comunas table; crime-type links; regional-rank #N of M.
- MapPlaceholderSlot no longer referenced on the ficha.
</verification>

<success_criteria>
- Each comuna page is a hub: breadcrumb with Communes level, map-spoke link to focused map, similar-comunas spoke, crime-type spoke, regional-rank device.
- EN/ES parity; ES slugs hardcoded; zero client:*; no news section (deferred to Phase 16).
- Reuses nearestComparables (12-01) + CommuneRankingTable + FAMILY_SLUGS; consumes the ?cut= focus param (12-02).
</success_criteria>

<output>
Create `.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-04-SUMMARY.md` when done.
</output>
