---
phase: 12-home-ia-redesign-comuna-page-hub-and-spoke
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/map/MapIsland.tsx
autonomous: true
requirements: [IA-02]
must_haves:
  truths:
    - "Opening /map/?cut=<cut> flies the map to and selects that commune once the choropleth layer is mounted"
    - "Opening /map/ with no ?cut= behaves exactly as before (no regression)"
    - "An unknown/garbage ?cut= value is a no-op (no crash, no DOM injection)"
  artifacts:
    - path: "site/src/components/map/MapIsland.tsx"
      provides: "?cut= URL focus param reader inside loadData() after layer mount"
      contains: "URLSearchParams"
  key_links:
    - from: "site/src/components/map/MapIsland.tsx loadData()"
      to: "selectCommune(focusCut)"
      via: "URLSearchParams(window.location.search).get('cut') after mountChoroplethLayer"
      pattern: "selectCommune\\(focusCut\\)"
---

<objective>
Add a `?cut=<cut>` focus-parameter reader to the Leaflet map island so the ficha map-spoke (built in 12-04) can deep-link `/map/?cut=13101` and open the map centered on that commune (D-08). This is the only real code change to the map island; everything else this phase is static links.

Purpose: D-08 requires the map to open focused on a comuna without embedding Leaflet on the 346 ficha pages. The island does NOT read any URL param today; this adds the minimal capability.
Output: ~6 lines inside the existing `loadData()` async function in MapIsland.tsx. No new state, no new useEffect, no new component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-CONTEXT.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md

<interfaces>
<!-- Verified contracts inside MapIsland.tsx — executor uses these directly. -->

site/src/components/map/MapIsland.tsx (confirmed line refs):
  - polyIdxRef = useRef<Map<string, L.Layer>>(new Map())   // L85 — indexed by CUT string
  - mapReady state                                          // L102
  - useEffect([mapReady]) runs async loadData()             // L148-218
  - INSIDE loadData, the guard `if (!cancelled && mapRef.current) { ... }`  // L195-210
      layerRef.current = mountChoroplethLayer(mapRef.current, ..., polyIdxRef, (cut)=>{...})  // L196-206
      ^ polyIdxRef is populated by mountChoroplethLayer — empty until this call returns
  - selectCommune = useCallback((cut: string) => { ... polyIdxRef.current.get(cut); map.flyToBounds(...) }, [])  // L352-371
      ^ in scope inside loadData; no-ops safely when cut not found (get returns undefined → no flyToBounds)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read ?cut= and select the commune at the end of loadData()</name>
  <read_first>
    - site/src/components/map/MapIsland.tsx (the file being modified — read L80-220 for loadData + the mount guard, and L350-371 for selectCommune)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md "Pattern 2: Map Focus Parameter" + "Pitfall 2"
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md MapIsland.tsx section (insertion point L195-210)
  </read_first>
  <action>
    In site/src/components/map/MapIsland.tsx, INSIDE the existing `if (!cancelled && mapRef.current) { ... }` block in `loadData()` (currently L195-210), immediately AFTER the `layerRef.current = mountChoroplethLayer(...)` assignment completes, add: read `const focusCut = new URLSearchParams(window.location.search).get('cut');` and if `focusCut` is truthy, call `setTimeout(() => selectCommune(focusCut), 100);` (the small timeout lets Leaflet finish rendering polygons so getBounds is valid — Pitfall 2). Do NOT add a separate useEffect keyed on [mapReady] — `polyIdxRef` is empty until mountChoroplethLayer returns, so a separate effect would fire too early and silently fail. Treat `focusCut` as an opaque string: it is only passed to `selectCommune`, which does `polyIdxRef.current.get(cut)` (a Map lookup) and `flyToBounds` — no innerHTML, no eval, and an unknown CUT returns undefined → safe no-op. Add a one-line comment referencing D-08.
  </action>
  <verify>
    <automated>cd site && npm run check 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - MapIsland.tsx contains `URLSearchParams(window.location.search).get('cut')` inside loadData (NOT in a new useEffect)
    - MapIsland.tsx contains `selectCommune(focusCut)`
    - `cd site && npm run check` exits 0
    - `cd site && npm run build` exits 0 (map.mjs validator's zero-React-regression guard still passes — change is inside the island, not a content page)
  </acceptance_criteria>
  <done>Map island reads ?cut= after layer mount and focuses the commune; no new effect/state; build + check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?cut=` → map island | User-controlled query string read client-side by the React island |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering / Injection | `?cut=` read in MapIsland.loadData | mitigate | Treat `?cut=` as an opaque string: pass only to `selectCommune`, which does a Map lookup (`polyIdxRef.current.get(cut)`) + `flyToBounds`. No innerHTML, no eval, no server round-trip. Unknown CUT → `get` returns undefined → no flight (safe no-op). Severity: low. |
| T-12-SC | Tampering | npm installs | accept | No new packages (RESEARCH audit: none). |
</threat_model>

<verification>
- `cd site && npm run check` exits 0.
- `cd site && npm run build` exits 0; the map.mjs validator still passes (change is internal to the island).
- Manual smoke (deferred to 12-06 phase gate): visit `/map/?cut=13101` → map flies to Santiago; visit `/map/` → unchanged; visit `/map/?cut=zzz` → no crash.
</verification>

<success_criteria>
- `/map/?cut=<cut>` focuses the commune once the choropleth layer is ready; `/map/` is unchanged; garbage CUT is a no-op.
- Change is a ~6-line addition inside loadData() — no new useEffect, state, or component.
</success_criteria>

<output>
Create `.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-02-SUMMARY.md` when done.
</output>
