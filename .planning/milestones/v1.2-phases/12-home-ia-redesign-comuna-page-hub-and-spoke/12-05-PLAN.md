---
phase: 12-home-ia-redesign-comuna-page-hub-and-spoke
plan: 05
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - site/src/components/PageHeader.astro
  - site/src/components/PageFooter.astro
  - site/src/pages/rankings.astro
  - site/src/pages/es/rankings.astro
autonomous: true
requirements: [IA-03]
must_haves:
  truths:
    - "The primary nav includes a Rankings link in both locales (/rankings/ EN, /es/rankings/ ES) alongside the existing Communes link"
    - "A common footer spine row links map / communes / rankings / key editorial in both locales on every page"
    - "/rankings/ and /es/rankings/ are real static pages listing/linking the existing ranking pages, each with a single H1"
    - "All new links are reciprocal and 404-free; ES slugs hardcoded per-locale"
  artifacts:
    - path: "site/src/components/PageHeader.astro"
      provides: "Rankings nav link (hardcoded per-locale)"
      contains: "rankingsHref"
    - path: "site/src/components/PageFooter.astro"
      provides: "footer-spine nav row (map/communes/rankings/editorial)"
      contains: "footer-spine"
    - path: "site/src/pages/rankings.astro"
      provides: "EN rankings index page"
      contains: "<h1"
    - path: "site/src/pages/es/rankings.astro"
      provides: "ES rankings index page"
      contains: "<h1"
  key_links:
    - from: "site/src/components/PageHeader.astro nav"
      to: "/rankings/ and /es/rankings/"
      via: "rankingsHref = locale==='en' ? '/rankings/' : '/es/rankings/'"
      pattern: "rankingsHref"
    - from: "site/src/components/PageFooter.astro"
      to: "map/communes/rankings/editorial"
      via: "footer-spine nav, hardcoded per-locale"
      pattern: "footer-spine"
---

<objective>
Build the navigation spine (IA-03): add a Rankings entry to the primary nav (D-12), extend the existing PageFooter with a footer-spine nav row for cheap global interconnection (D-13), and create new static /rankings/ + /es/rankings/ index pages that list and link the existing ranking pages (no new ranking data). Consumes the nav_rankings + footer-spine i18n keys from 12-01.

Purpose: The audit found no coherent spine connecting map <-> comuna <-> ranking <-> region. The Rankings node + footer spine + index page close the IA-03 gap (the Comunas nav node already exists from Phase 11; the Noticias node is deferred to Phase 16).
Output: 1 modified header, 1 extended footer, 2 new pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-CONTEXT.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md

<interfaces>
<!-- Contracts the executor uses directly. -->

site/src/components/PageHeader.astro (the file being modified — confirmed line refs):
  - hardcoded per-locale slug pattern at L24-27: mapHref, methodHref, communesHref = locale==='en' ? '/communes/' : '/es/comunas/'
  - nav links rendered at L60-63 (homeHref, mapHref, communesHref, methodHref)
  - comment L22-23 documents: getRelativeLocaleUrl does NOT translate ES slugs — hardcode per-locale

site/src/components/PageFooter.astro (the file being EXTENDED — already exists, already rendered by BaseLayout):
  - Props { locale:'en'|'es'; enPath; esPath }
  - existing footer-legal nav row at L35-51 uses the locale==='en' ? (...) : (...) hardcoded pattern — MIRROR it for the new footer-spine row
  - t = locale==='en' ? EN_STRINGS : ES_STRINGS

From site/src/config/i18n.ts (keys added in 12-01):
  nav_rankings ('Rankings' both locales)
  footer_spine_map / footer_spine_communes / footer_spine_rankings / footer_spine_guide

From site/src/layouts/EditorialLayout.astro (use for the rankings index pages — they are text/link pages, NOT the wide HomeLayout):
  Props { lang; title; description; enPath; esPath; jsonLd? }

Existing ranking-bearing pages to LINK from the index (all already exist): /safest-cities-in-chile/, /chile-crime-map/, /crime/<family>/ (and /es/delito/<family>/), /region/<slug>/ (and /es/region/<slug>/). ES editorial slugs are hardcoded (e.g. /es/metodologia/, /es/mapa/, /es/comunas/).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rankings nav link to PageHeader (both locales)</name>
  <read_first>
    - site/src/components/PageHeader.astro (hardcoded-slug block L24-27, nav links L60-63, the getRelativeLocaleUrl warning comment L22-23)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md Pattern 4 + Pitfall 1
  </read_first>
  <action>
    In site/src/components/PageHeader.astro: add a hardcoded per-locale href after communesHref (L27): rankingsHref = locale === 'en' ? '/rankings/' : '/es/rankings/'. Render a new nav anchor href={rankingsHref} class="nav-link" with label {t.nav_rankings}, placed after the Communes nav link (L62) so nav order is Home, Map, Communes, Rankings, Methodology. NEVER use getRelativeLocaleUrl for the rankings slug (Pitfall 1 — it does not translate ES slugs).
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - PageHeader.astro contains rankingsHref = locale === 'en' ? '/rankings/' : '/es/rankings/'
    - dist/index.html nav contains href="/rankings/"
    - dist/es/index.html nav contains href="/es/rankings/"
    - cd site && npm run build exits 0
  </acceptance_criteria>
  <done>Rankings nav link present in both locales, hardcoded per-locale, in correct nav order.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PageFooter with a footer-spine nav row (both locales)</name>
  <read_first>
    - site/src/components/PageFooter.astro (the file being extended — mirror the existing footer-legal nav row L35-51 and its hardcoded locale===en ternary)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md "PageFooter — Existing Component" section (footer-spine extension snippet)
  </read_first>
  <action>
    In site/src/components/PageFooter.astro, add a new nav row with class="footer-spine page-content" (mirroring the existing footer-legal nav at L35-51) ABOVE or beside the legal row, using the locale===en ? (...) : (...) hardcoded pattern. EN links: /map/ (t.footer_spine_map), /communes/ (t.footer_spine_communes), /rankings/ (t.footer_spine_rankings), /is-chile-safe/ (t.footer_spine_guide). ES links: /es/mapa/, /es/comunas/, /es/rankings/, /es/es-seguro-chile/ (use the ES editorial slug that already exists in the site; if /es/es-seguro-chile/ is not confirmed in dist, link to a confirmed ES editorial page such as /es/metodologia/). Add minimal styling mirroring .footer-legal. Zero client:*. Every ES slug hardcoded per-locale.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - PageFooter.astro contains a nav with class footer-spine
    - dist/index.html footer contains href="/rankings/" AND href="/communes/" AND href="/map/" within the footer-spine
    - dist/es/index.html footer contains href="/es/rankings/" AND href="/es/comunas/" AND href="/es/mapa/"
    - cd site && npm run build exits 0
  </acceptance_criteria>
  <done>Footer spine row links map/communes/rankings/editorial on every page in both locales, hardcoded per-locale.</done>
</task>

<task type="auto">
  <name>Task 3: Create /rankings/ + /es/rankings/ static index pages</name>
  <read_first>
    - site/src/layouts/EditorialLayout.astro (the layout to use — text/link page, not HomeLayout)
    - site/src/pages/index.astro guide-link list (pattern for a curated link list)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md rankings.astro section
  </read_first>
  <action>
    Create site/src/pages/rankings.astro using EditorialLayout (lang="en", title/description SEO-appropriate for crime rankings, enPath="/rankings/", esPath="/es/rankings/"). Content: ONE h1 (e.g. "Crime Incidence Rankings"), a short intro framed as reported CEAD incidence (Editorial Tone Guard — no absolute safe/dangerous labels), then a curated list of anchors linking EXISTING ranking pages only: /safest-cities-in-chile/, the seven /crime/<family>/ pages (use FAMILY_SLUGS .en values from i18n.ts), and /communes/ (browse all by region). No new ranking data; no ranking tables built here. Create site/src/pages/es/rankings.astro as the ES mirror: lang="es", enPath="/rankings/", esPath="/es/rankings/", ONE Spanish h1, links to /es/delito/<family>/ (FAMILY_SLUGS .es) and /es/comunas/ and the ES safest-communes editorial page. Hardcode all ES slugs per-locale; never getRelativeLocaleUrl for localized slugs.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3 && node scripts/validate/hreflang.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dist/rankings/index.html exists with exactly one h1 and at least 3 links to existing ranking/crime pages
    - dist/es/rankings/index.html exists with exactly one h1
    - dist/rankings/index.html contains href="/es/rankings/" in its hreflang alternate (reciprocal)
    - hreflang.mjs validator passes; cd site && npm run build exits 0
  </acceptance_criteria>
  <done>/rankings/ + /es/rankings/ exist as static link-index pages, single H1 each, reciprocal hreflang, link existing pages only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav/footer/rankings hrefs | All hrefs are build-time constants (hardcoded per-locale slugs + FAMILY_SLUGS) — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | nav/footer/rankings links | accept | All links are static build-time constants; no user input, no client JS. Severity: none. |
| T-12-SC | Tampering | npm installs | accept | No new packages (RESEARCH audit: none). |
</threat_model>

<verification>
- cd site && npm run build exits 0.
- hreflang.mjs validator passes (rankings pages have reciprocal alternates).
- Nav contains Rankings in both locales; footer spine contains map/communes/rankings/editorial in both locales; /rankings/ and /es/rankings/ exist with single H1 each.
</verification>

<success_criteria>
- Rankings nav node added (D-12); footer spine added (D-13); /rankings/ + /es/rankings/ index pages created.
- All new links hardcoded per-locale, reciprocal, link existing pages only (no new ranking data).
- No Noticias node (deferred to Phase 16); zero client:*.
</success_criteria>

<output>
Create `.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-05-SUMMARY.md` when done.
</output>
