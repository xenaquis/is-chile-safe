---
phase: 12-home-ia-redesign-comuna-page-hub-and-spoke
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/lib/data.ts
  - site/src/config/i18n.ts
  - site/src/pages/communes/index.astro
  - site/src/pages/es/comunas/index.astro
autonomous: true
requirements: [IA-02, IA-03]
must_haves:
  truths:
    - "A build-time helper returns the N most similar communes in the same region ordered by nearest rate"
    - "The i18n string map exposes nav_rankings and footer-spine labels in both locales"
    - "The comuna directory pre-filters its list from a ?q= URL parameter on first load (no JS required to navigate, but the inline script honors ?q=)"
  artifacts:
    - path: "site/src/lib/data.ts"
      provides: "nearestComparables(targetCut, n) plural helper returning RankingRow[]"
      contains: "export function nearestComparables"
    - path: "site/src/config/i18n.ts"
      provides: "nav_rankings + footer spine i18n keys (EN + ES)"
      contains: "nav_rankings"
    - path: "site/src/pages/communes/index.astro"
      provides: "?q= URL read on load in the inline finder script"
      contains: "URLSearchParams"
  key_links:
    - from: "site/src/pages/communes/index.astro"
      to: "?q= URL param"
      via: "URLSearchParams(window.location.search).get('q') in inline script"
      pattern: "URLSearchParams"
    - from: "site/src/lib/data.ts nearestComparables"
      to: "loadIndex region filter + rate sort"
      via: "same-region filter + abs(rate-targetRate) sort"
      pattern: "nearestComparables"
---

<objective>
Lay the shared foundations every Wave-2 page depends on: (1) a new plural `nearestComparables(targetCut, n)` data helper for the "similar comunas" spoke (D-07), (2) `nav_rankings` + footer-spine i18n strings (D-12/D-13), and (3) make the Phase-11 comuna directory honor a `?q=` URL parameter so the home search hero (D-05/D-06) actually lands the user on a pre-filtered directory.

Purpose: These three changes are the contracts consumed by the home rebuild (12-03), the ficha hub (12-04), and the nav/footer/rankings work (12-05). Building them first prevents downstream scavenger hunts.
Output: One new exported function, ~5 new i18n keys per locale, and a ~6-line addition to each directory finder script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-CONTEXT.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md
@.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor uses directly — no exploration needed. -->

From site/src/lib/data.ts (existing — DO NOT change signatures):
  export interface CommuneMeta { cut, name, slug, region_id, population, low_population }
  export function loadIndex(): CommuneMeta[]
  export function loadCommune(cut: string): CommuneData   // CommuneData has national_rank, regional_rank, trend
  export function latestCompleteYearRate(commune: CommuneData): number
  export function nearestComparable(targetCut: string): ComparableResult  // SINGULAR — the analog to copy

From site/src/components/CommuneRankingTable.astro — the RankingRow shape the new helper MUST return:
  interface RankingRow { name: string; slug: string; rate: number; national_rank: number; trend: 'up'|'down'|'stable'; low_population: boolean }

From site/src/config/i18n.ts:
  export interface I18nStrings { ... nav_communes: string; ... }   // nav_communes at L107
  export const EN_STRINGS: I18nStrings   // nav_communes: 'Communes' at L220
  export const ES_STRINGS: I18nStrings   // nav_communes: 'Comunas' at L336
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nearestComparables(targetCut, n) plural helper to data.ts</name>
  <read_first>
    - site/src/lib/data.ts (the file being modified — read nearestComparable at L266-302 as the analog to copy)
    - site/src/components/CommuneRankingTable.astro L16-23 (the RankingRow shape the return value MUST match)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md "No Analog Found" section
  </read_first>
  <behavior>
    - Given a valid targetCut, returns an array of length min(n, same-region-pool size) of RankingRow objects.
    - Excludes the target commune itself and low_population communes from results.
    - Same-region only (filter by the target's region_id), ordered by ascending absolute rate distance to the target's latest-complete-year rate.
    - Each returned row has name, slug, rate, national_rank, trend, low_population fields populated from loadCommune.
    - Throws if targetCut is not in loadIndex() (mirror nearestComparable's existing throw).
  </behavior>
  <action>
    Add a new exported function `nearestComparables(targetCut: string, n: number)` to site/src/lib/data.ts, returning an array of objects matching the `RankingRow` shape from CommuneRankingTable.astro (name, slug, rate, national_rank, trend, low_population). Copy the filter/sort skeleton from the existing singular `nearestComparable` (L266): resolve target via loadIndex().find, compute targetRate via latestCompleteYearRate, build the same-region pool by filtering `c.region_id === target.region_id && !c.low_population && c.cut !== targetCut`, sort ascending by `Math.abs(rate - targetRate)`, slice to n. Unlike the singular version, do NOT add a national fallback — same-region only per D-07. Reuse loadCommune + latestCompleteYearRate per row. Add a code comment noting BUGFIX-999.1 (Tarapacá region_id collision) may skew same-region grouping — accept, do not fix here.
  </action>
  <verify>
    <automated>cd site && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "data.ts" ; node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - site/src/lib/data.ts contains `export function nearestComparables`
    - `cd site && npm run check` reports no new type errors referencing data.ts
    - The function returns at most n rows and excludes the target cut (verified during 12-04 build)
  </acceptance_criteria>
  <done>nearestComparables(cut, n) exported, type-checks, returns same-region RankingRow[] ordered by nearest rate.</done>
</task>

<task type="auto">
  <name>Task 2: Add nav_rankings + footer-spine i18n keys (EN + ES)</name>
  <read_first>
    - site/src/config/i18n.ts (the file being modified — read the I18nStrings interface L7-116, EN_STRINGS nav block L118-123 + nav_communes L220, ES_STRINGS nav_communes L336)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-PATTERNS.md "i18n String Addition Pattern"
  </read_first>
  <action>
    In site/src/config/i18n.ts: (1) add `nav_rankings: string;` to the `I18nStrings` interface immediately after the existing `nav_communes: string;` at L107. (2) Add `nav_rankings: 'Rankings',` to EN_STRINGS (after nav_communes: 'Communes'). (3) Add `nav_rankings: 'Rankings',` to ES_STRINGS (after nav_communes: 'Comunas'; same word both locales per RESEARCH.md). Also add footer-spine link labels used by the footer spine in 12-05: add `footer_spine_map`, `footer_spine_communes`, `footer_spine_rankings`, `footer_spine_guide` to the interface and to both EN_STRINGS and ES_STRINGS. EN values: 'Map', 'Communes', 'Rankings', 'Is Chile Safe?'. ES values: 'Mapa', 'Comunas', 'Rankings', '¿Es seguro Chile?'. Do NOT use getRelativeLocaleUrl anywhere — these are labels only; hrefs are hardcoded per-locale in the consuming components.
  </action>
  <verify>
    <automated>cd site && npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - i18n.ts contains `nav_rankings` in the interface and in BOTH EN_STRINGS and ES_STRINGS
    - i18n.ts contains `footer_spine_rankings` in the interface and both string maps
    - `cd site && npm run check` exits 0 (no missing-property errors on I18nStrings)
  </acceptance_criteria>
  <done>nav_rankings + 4 footer-spine keys present in interface + EN_STRINGS + ES_STRINGS; astro check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Make the comuna directory honor ?q= on load (EN + ES finder)</name>
  <read_first>
    - site/src/pages/communes/index.astro (the file being modified — read the inline `<script>` block L170-300, especially the `q` variable L177, the `input` element binding L186, and `render()` L224)
    - site/src/pages/es/comunas/index.astro (the ES mirror — same inline script structure)
    - .planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-RESEARCH.md Open Question #1 + Assumption A1
  </read_first>
  <action>
    In the inline IIFE script of site/src/pages/communes/index.astro, after the `input` element is grabbed (around L186) and before the final `render()` call (L298), read the URL query param: `const initialQ = new URLSearchParams(window.location.search).get('q');` and if present and non-empty, assign `q = initialQ;` and set `input.value = initialQ;` so the search box reflects it. The existing `render()` already filters on `q` and the existing `highlight()`/`render()` slice TRUSTED data-name (T-11-05) — do NOT change that; the `?q=` value is only used as a filter substring against pre-rendered data-norm and assigned to input.value (a form field value, not innerHTML), so there is no injection vector. Apply the identical change to site/src/pages/es/comunas/index.astro. Keep the value read as an opaque string; never inject it into innerHTML.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -3 && node -e "const fs=require('fs');const f=fs.readFileSync('dist/communes/index.html','utf8');process.exit(f.includes('URLSearchParams')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Both communes/index.astro and es/comunas/index.astro inline scripts contain `URLSearchParams(window.location.search).get('q')`
    - dist/communes/index.html and dist/es/comunas/index.html contain the URLSearchParams read (script is inlined/bundled into the page)
    - `cd site && npm run build` exits 0
    - The ?q= value is assigned to input.value (form field), never to innerHTML
  </acceptance_criteria>
  <done>Both directory finders pre-populate the search box and pre-filter the list from ?q= on load; build passes; no innerHTML injection of the query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?q=` → directory page | User-controlled query string read by client-side inline script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering / XSS | `?q=` read in communes finder | mitigate | Assign `?q=` only to `input.value` (form field) and use it as a filter substring against pre-rendered `data-norm`; NEVER inject into innerHTML. Highlight continues to slice the TRUSTED `data-name` (existing T-11-05 control), not the raw query. Severity: low (static site, client-only, no echo into markup). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `cd site && npm run check` exits 0 (types intact after data.ts + i18n.ts changes).
- `cd site && npm run build` exits 0 (directory finder change compiles + bundles).
- `dist/communes/index.html` and `dist/es/comunas/index.html` contain the `URLSearchParams` read.
</verification>

<success_criteria>
- `nearestComparables(cut, n)` exported from data.ts, returns same-region RankingRow[] ordered by nearest rate, excludes target + low-pop.
- `nav_rankings` + 4 footer-spine keys present in I18nStrings interface and both EN/ES string maps.
- Both comuna directory finders read `?q=` on load and pre-filter; no XSS vector introduced.
</success_criteria>

<output>
Create `.planning/phases/12-home-ia-redesign-comuna-page-hub-and-spoke/12-01-SUMMARY.md` when done.
</output>
