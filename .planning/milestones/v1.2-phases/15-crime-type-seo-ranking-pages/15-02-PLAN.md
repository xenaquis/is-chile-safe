---
phase: 15-crime-type-seo-ranking-pages
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - site/src/pages/crime-ranking/[crime].astro
  - site/src/pages/es/ranking-delito/[crime].astro
  - site/src/pages/commune/[slug].astro
  - site/src/pages/es/comuna/[slug].astro
autonomous: true
requirements: [CSEO-01, CSEO-02]

must_haves:
  truths:
    - "A reader can open /crime-ranking/homicide/ and /es/ranking-delito/homicidios/ and see Chilean communes ranked by reported homicide rate, with each commune linking to its own page"
    - "Each of the 8 crime types (homicidios + 7 families) has an EN page and an ES page generated at build time"
    - "Every ranking page has a single keyword-focused H1, unique title/meta/canonical, reciprocal hreflang (en/es/x-default), an ItemList JSON-LD, and appears in the sitemap"
    - "The homicide page ranks by featured_rates.homicidios (non-zero, non-uniform rates) — NOT by_family['homicidios'] (which would be 0 for all communes)"
    - "Comuna pages link to the homicide ranking page (reciprocal IA link), in addition to the existing 7 family-ranking spokes"
    - "Sober tone preserved: no forbidden-language term; ItemList name passes assertSafeName"
  artifacts:
    - path: "site/src/pages/crime-ranking/[crime].astro"
      provides: "EN crime-type ranking pages for 8 crimes"
      contains: "featured_rates"
      min_lines: 200
    - path: "site/src/pages/es/ranking-delito/[crime].astro"
      provides: "ES crime-type ranking pages for 8 crimes"
      contains: "featured_rates"
      min_lines: 200
    - path: "site/src/pages/commune/[slug].astro"
      provides: "Homicide-ranking reciprocal spoke link"
      contains: "crime-ranking/homicide"
    - path: "site/src/pages/es/comuna/[slug].astro"
      provides: "ES homicide-ranking reciprocal spoke link"
      contains: "ranking-delito/homicidios"
  key_links:
    - from: "site/src/pages/crime-ranking/[crime].astro"
      to: "commune.featured_rates.homicidios[latestCompleteYear]"
      via: "dataSource==='featured_rates' branch in the rank loop"
      pattern: "featured_rates\\?\\.homicidios"
    - from: "site/src/pages/crime-ranking/[crime].astro"
      to: "/commune/{slug}/"
      via: "ranking table row anchor"
      pattern: "/commune/\\$\\{row.slug\\}/"
    - from: "site/src/pages/es/ranking-delito/[crime].astro"
      to: "/es/comuna/{slug}/"
      via: "ranking table row anchor"
      pattern: "/es/comuna/\\$\\{row.slug\\}/"
    - from: "site/src/pages/crime-ranking/[crime].astro"
      to: "buildItemList / buildBreadcrumb"
      via: "jsonLd array passed to BaseLayout"
      pattern: "buildItemList\\("
---

<objective>
Create the bilingual crime-type ranking pages and wire the reciprocal comuna-page link. Two new Astro SSG routes — `/crime-ranking/[crime]/` (EN) and `/es/ranking-delito/[crime]/` (ES) — generate one page per crime type for the 8 in-scope crimes: `homicide/homicidios` (homicide subgroup) plus the 7 CEAD families (violent-robbery, drug-crimes, property, domestic-violence, weapons, disorder, life-crimes). Each page ranks all non-low-population communes by the crime's rate per 100k, links each row to that commune's page, emits ItemList + BreadcrumbList + Dataset JSON-LD, and gets reciprocal hreflang + canonical + OG from BaseLayout. The comuna hub pages gain a homicide-ranking spoke so the new namespace is reciprocally linked (RESEARCH A3 / Pitfall 5).

Purpose: Delivers CSEO-01 (programmatic bilingual ranking pages, homicide first, internal-linked, hreflang, sitemap) and CSEO-02 (single keyword H1, unique title/meta/canonical, ItemList JSON-LD, sober tone). Copy the existing `crime/[family].astro` + `es/delito/[family].astro` templates verbatim — only the getStaticPaths list, the rate-extraction branch, the URL constants, the breadcrumb labels, and the H1 framing change.

Output: 2 new page files + 2 modified comuna page files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-CONTEXT.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-RESEARCH.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-PATTERNS.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-VALIDATION.md
@.planning/phases/15-crime-type-seo-ranking-pages/15-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly; no exploration needed. -->

From site/src/lib/jsonld.ts (Phase 13):
  export function buildItemList(rows, locale, opts): object;
    // rows need { name, slug }; urlMode defaults to 'commune' -> /commune/{slug}/ (EN), ES variant by locale
    // opts.name MUST NOT contain 'safest' | 'most dangerous' | 'safe' | 'dangerous' (assertSafeName throws at build)
    // rate is deliberately EXCLUDED from the ItemList -- never pass it in opts.name
  export function buildBreadcrumb(items): object;  // items: {name, url?}[]; current page item has no url

From site/src/lib/data.ts:
  loadIndex(): meta[]  // each meta: { name, slug, cut, region_id, low_population }
  loadCommune(cut): CommuneData  // enriched with latestCompleteYear, regionName, trend
  loadCatalog(): { family_keys, families }
  regionFileId(region_id): string
  // CommuneData.series: { year, partial, by_family: Record<familyKey, rate> }[]
  // CommuneData.featured_rates?.homicidios?: Record<yearStr, number>   <- homicide rate lives HERE

From site/src/lib/familyDefs.ts (Plan 01 added the methodology records):
  export const FAMILY_LABELS_EN / FAMILY_LABELS_ES: Record<string,string>
  export const FAMILY_DEFS_EN / FAMILY_DEFS_ES: Record<string,string>
  export const RANKING_METHODOLOGY_EN / RANKING_METHODOLOGY_ES: Record<string,string>  <- methodology <p>

From site/src/layouts/BaseLayout.astro:
  props: lang, title, description, enPath, esPath, jsonLd, mountType, pageType
  // injects hreflang en/es/x-default + canonical + OG from enPath/esPath; pageType="crime" -> /og/crime.png
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create EN crime-ranking page /crime-ranking/[crime].astro</name>
  <files>site/src/pages/crime-ranking/[crime].astro</files>
  <read_first>
    - site/src/pages/crime/[family].astro (FULL — verbatim template: imports, RankRow type, rank loop lines 64-128, JSON-LD lines 146-178, BaseLayout lines 181-190, visual breadcrumb + H1 + table + region breakdown + the entire <style> block)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-PATTERNS.md (section "site/src/pages/crime-ranking/[crime].astro" — CRIME_RANKING_DEFS list, ES_CRIME_SLUGS map, the dataSource branch, enPath/esPath wiring)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-RESEARCH.md (Pitfall 1 homicide field; Pitfall 2 URL collision; Pitfall 4 ItemList name; section 4 slug/H1 table)
    - site/src/lib/jsonld.ts (buildItemList/buildBreadcrumb signatures + assertSafeName FORBIDDEN list)
    - site/src/lib/familyDefs.ts (RANKING_METHODOLOGY_EN, FAMILY_LABELS_EN — added/confirmed in Plan 01)
  </read_first>
  <action>
    Create site/src/pages/crime-ranking/[crime].astro by copying site/src/pages/crime/[family].astro and applying these specific changes. Imports: identical relative depth (the new file is inside crime-ranking/, same depth as crime/, so the ../../layouts/... paths are unchanged); add RANKING_METHODOLOGY_EN to the familyDefs import. Replace getStaticPaths: define a module-level CRIME_RANKING_DEFS array of 8 entries shaped param/familyKey/dataSource per 15-PATTERNS.md — homicide maps to familyKey homicidios with dataSource featured_rates, plus the 7 families with dataSource by_family (violent-robbery to robos_violentos, drug-crimes to drogas, property to propiedad, domestic-violence to vif, weapons to armas, disorder to incivilidades, life-crimes to vida); also define an ES_CRIME_SLUGS Record map (15-PATTERNS.md has the exact pairs). getStaticPaths returns params crime=def.param with props=def. In the rank loop, BRANCH on dataSource: when featured_rates read commune.featured_rates?.homicidios?.[commune.latestCompleteYear] ?? 0 (THE HOMICIDE DATA DEVIATION — using by_family here returns 0 for all 346 communes and silently breaks the ranking); else keep the existing entry?.by_family?.[familyKey] ?? 0 path. Keep the latestYear Math.max capture, eligible/lowPop sort, rank assignment, national mean, and region breakdown verbatim. URL constants: enPath = /crime-ranking/{crime}/ and esPath = /es/ranking-delito/{ES_CRIME_SLUGS[crime]}/ — hardcoded ES slug, never getRelativeLocaleUrl (i18n-localized-slug-pitfall). Labels: familyLabel = FAMILY_LABELS_EN[familyKey]; methodology familyDef = RANKING_METHODOLOGY_EN[familyKey]. title/description: keyword-focused per 15-RESEARCH.md section 4 (e.g. "Communes with the Highest Reported Homicide Incidence in Chile — Chile Safety Map"). JSON-LD: copy the Dataset block (substitute familyLabel + enPath); breadcrumb Home then Crime Rankings; buildItemList(eligibleRankingRows,'en',{ name:`Communes by reported ${familyLabel.toLowerCase()} incidence (descending)`, order:'Descending' }) — NEVER put rate or 'safe'/'dangerous' in the name. BaseLayout: pageType="crime", mountType="crime-type-map", enPath/esPath/jsonLd as above. Visual breadcrumb span reads "Crime Rankings"; H1 = "Communes with the Highest Reported {familyLabel} Incidence in Chile"; render a methodology paragraph element with class crime-def showing familyDef. Table rows link to /commune/{row.slug}/. Copy the entire <style> block verbatim. The existing template renders a Featured badge from catalog.families[familyKey].featured; homicidios is not a family key, so guard that lookup with ?? false or drop the badge to avoid an undefined access.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -20 && node -e "const fs=require('fs');const p='dist/crime-ranking/homicide/index.html';if(!fs.existsSync(p)){console.error('FAIL: homicide EN page not built');process.exit(1)}const h=fs.readFileSync(p,'utf8');if(!/ItemList/.test(h)){console.error('FAIL: no ItemList JSON-LD');process.exit(1)}const h1=(h.match(/<h1[^>]*>/g)||[]);if(h1.length!==1){console.error('FAIL: expected exactly 1 H1, got '+h1.length);process.exit(1)}if(!fs.existsSync('dist/crime-ranking/property/index.html')){console.error('FAIL: family page property not built');process.exit(1)}console.log('PASS build + homicide page + ItemList + single H1')"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build succeeds (Astro errors if getStaticPaths is malformed or assertSafeName throws).
    - dist/crime-ranking/homicide/index.html AND dist/crime-ranking/property/index.html (and the other 6) exist.
    - The homicide page's ranking is non-uniform: the rendered ranked rates are NOT all 0 (proves the featured_rates branch is used, not by_family). Spot-checkable in the built HTML table.
    - Exactly one <h1> per page; H1 is the keyword-focused sober phrasing.
    - Page HTML contains an ItemList JSON-LD block and a BreadcrumbList.
    - Each table row links to /commune/{slug}/.
    - hreflang en/es/x-default + canonical present (injected by BaseLayout from enPath/esPath).
    - No build-time throw from jsonld.ts assertSafeName.
  </acceptance_criteria>
  <done>8 EN ranking pages build; homicide ranks by featured_rates (non-zero); single H1; ItemList + Breadcrumb JSON-LD; comuna links present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ES crime-ranking page /es/ranking-delito/[crime].astro</name>
  <files>site/src/pages/es/ranking-delito/[crime].astro</files>
  <read_first>
    - site/src/pages/es/delito/[family].astro (FULL — verbatim ES template: import depth ../../../, ES breadcrumb labels, ES H1 framing, ES commune link /es/comuna/{slug}/, ES <style> block)
    - site/src/pages/crime-ranking/[crime].astro (the EN page created in Task 1 — mirror its CRIME_RANKING_DEFS, ES_CRIME_SLUGS, and dataSource branch exactly)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-PATTERNS.md (section "site/src/pages/es/ranking-delito/[crime].astro" — ES getStaticPaths uses the ES slug as the crime param; enPath/esPath wiring; ES breadcrumb + ItemList strings)
    - site/src/lib/familyDefs.ts (RANKING_METHODOLOGY_ES, FAMILY_LABELS_ES)
  </read_first>
  <action>
    Create site/src/pages/es/ranking-delito/[crime].astro as the ES mirror of the EN page. Import depth is ../../../ (3 levels, same as es/delito/[family].astro) — import FAMILY_LABELS_ES, RANKING_METHODOLOGY_ES from familyDefs. Reuse the SAME CRIME_RANKING_DEFS + ES_CRIME_SLUGS definitions as the EN page (duplicate them inline — these are tiny static lists; do not over-engineer a shared module). getStaticPaths: params crime = ES_CRIME_SLUGS[def.param] (the ES slug is the route param) and pass props that carry BOTH the EN param and the def fields so the page can build both paths. In the page body resolve esCrime from Astro.params.crime and the EN param from props; enPath = /crime-ranking/{enParam}/ and esPath = /es/ranking-delito/{esCrime}/. Rate extraction: identical dataSource branch — homicidios reads commune.featured_rates?.homicidios?.[commune.latestCompleteYear] ?? 0. Labels: FAMILY_LABELS_ES + RANKING_METHODOLOGY_ES. Breadcrumb Inicio then Ranking de Delitos (Inicio url = https://ischilesafe.com/es/). buildItemList with locale 'es' and name `Comunas por incidencia reportada de ${familyLabel.toLowerCase()} (descendente)` — no rate, no 'segura'/'peligrosa'. BaseLayout lang="es", pageType="crime", mountType="crime-type-map". H1 = "Comunas con mayor incidencia de {familyLabel.toLowerCase()} reportados en Chile" (sober, passes forbidden-language). Methodology paragraph with class crime-def showing RANKING_METHODOLOGY_ES[familyKey]. Table rows link to /es/comuna/{row.slug}/. Copy the ES <style> block verbatim. Apply the same Featured-badge guard as the EN page.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -20 && node -e "const fs=require('fs');const p='dist/es/ranking-delito/homicidios/index.html';if(!fs.existsSync(p)){console.error('FAIL: ES homicide page not built');process.exit(1)}const h=fs.readFileSync(p,'utf8');if(!/ItemList/.test(h)){console.error('FAIL: no ItemList');process.exit(1)}const h1=(h.match(/<h1[^>]*>/g)||[]);if(h1.length!==1){console.error('FAIL: expected 1 H1, got '+h1.length);process.exit(1)}if(!/\/es\/comuna\//.test(h)){console.error('FAIL: no ES comuna links');process.exit(1)}if(!fs.existsSync('dist/es/ranking-delito/robos-violentos/index.html')){console.error('FAIL: ES family page robos-violentos not built');process.exit(1)}console.log('PASS ES pages')"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build succeeds; dist/es/ranking-delito/homicidios/index.html and the 7 ES family slugs (robos-violentos, drogas, propiedad, violencia-intrafamiliar, armas, incivilidades, delitos-contra-la-vida) all exist.
    - Exactly one <h1>; ES sober keyword phrasing.
    - ItemList + BreadcrumbList JSON-LD present; ItemList name in Spanish, no rate, no forbidden term.
    - Table rows link to /es/comuna/{slug}/.
    - esPath/enPath reciprocal: ES page's hreflang en -> /crime-ranking/{enParam}/, es -> /es/ranking-delito/{esCrime}/.
  </acceptance_criteria>
  <done>8 ES ranking pages build with correct ES slugs, ES sober H1, ES ItemList, ES comuna links, reciprocal hreflang.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add reciprocal homicide-ranking spoke to comuna pages + chained build/validate gate</name>
  <files>site/src/pages/commune/[slug].astro, site/src/pages/es/comuna/[slug].astro</files>
  <read_first>
    - site/src/pages/commune/[slug].astro (lines 236-246 — the existing "Rankings by Crime Type" spoke that iterates byFamily, 7 keys, no homicide)
    - site/src/pages/es/comuna/[slug].astro (the ES mirror of that spoke section)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-RESEARCH.md (Pitfall 5 + A3 — comuna pages miss the homicide ranking because homicidios is not a byFamily key)
    - .planning/phases/15-crime-type-seo-ranking-pages/15-VALIDATION.md (full suite command + OneDrive chained-build note)
  </read_first>
  <action>
    In site/src/pages/commune/[slug].astro, inside the existing crime-type-spoke list (the ul.crime-type-list around lines 239-246), add one explicit list item linking to the homicide ranking: an anchor to /crime-ranking/homicide/ with a label consistent with the surrounding "{name} ranking" style (e.g. "homicide ranking"). Place it alongside the 7 family links rendered from Object.entries(byFamily); because homicidios is NOT a byFamily key it must be hardcoded, not derived from the loop. Do the same in site/src/pages/es/comuna/[slug].astro pointing to /es/ranking-delito/homicidios/ with an ES label (e.g. "ranking de homicidios"). Do NOT change the existing 7-family loop. Match the surrounding markup/class names (crime-type-list li a). This closes the reciprocal-link requirement so the new namespace is reachable from comuna hubs. After editing, run the chained build + full validator suite in ONE command (OneDrive dist/ desync — never split build and validate across processes).
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate 2>&1 | tail -40 && node -e "const fs=require('fs');const dirs=fs.readdirSync('dist/commune').filter(d=>fs.existsSync('dist/commune/'+d+'/index.html'));const h=fs.readFileSync('dist/commune/'+dirs[0]+'/index.html','utf8');if(!/crime-ranking\/homicide/.test(h)){console.error('FAIL: EN comuna page missing homicide-ranking spoke');process.exit(1)}const edirs=fs.readdirSync('dist/es/comuna').filter(d=>fs.existsSync('dist/es/comuna/'+d+'/index.html'));const eh=fs.readFileSync('dist/es/comuna/'+edirs[0]+'/index.html','utf8');if(!/ranking-delito\/homicidios/.test(eh)){console.error('FAIL: ES comuna page missing homicide-ranking spoke');process.exit(1)}console.log('PASS reciprocal spokes present + full validate suite green')"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build succeeds and npm run validate (all.mjs, the 13-validator suite) passes fully — including seo.mjs (now sampling crime-ranking pages from Plan 01), hreflang.mjs (reciprocity on the new pages), forbidden-language.mjs (methodology + H1 prose), spine.mjs (single-H1), coverage.mjs (sitemap + no-orphan internal links).
    - A built EN comuna page contains a link to /crime-ranking/homicide/.
    - A built ES comuna page contains a link to /es/ranking-delito/homicidios/.
    - No regression: existing crime/[family] pages and all prior validators still pass.
  </acceptance_criteria>
  <done>Comuna hubs reciprocally link the homicide ranking; full build + 13-validator suite green; CSEO-01/CSEO-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CEAD JSON -> Astro build-time read | Only data crossing into the build; pipeline-validated. No user input, no runtime network, no auth. |
| rendered dist/ HTML -> public/SEO crawlers | Output is static HTML; editorial-tone guard is the relevant integrity boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | ItemList name / H1 / methodology prose (editorial-guard bypass) | mitigate | buildItemList assertSafeName throws at build on forbidden name (Task 1/2); forbidden-language.mjs scans all dist/ HTML in Task 3 full-suite gate; sober H1/copy taken from validator-checked RESEARCH/PATTERNS strings. |
| T-15-04 | Tampering | Homicide rate field (wrong-source data integrity) | mitigate | dataSource branch reads featured_rates.homicidios (Pitfall 1); Task 1 acceptance asserts ranked rates are non-uniform, catching the silent by_family=0 failure. |
| T-15-05 | Information disclosure | Internal links / sitemap | accept | All links target already-public static comuna/region pages; coverage.mjs asserts no orphan/ungenerated-slug links. No secrets/PII. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none). No install tasks -> no slopcheck checkpoint. |

No HIGH threats: static-page generation from committed local JSON, no user input, no auth, no runtime network. Build-time editorial-guard integrity (T-15-03) and homicide data-source correctness (T-15-04) are the relevant low-severity items, both covered by existing/added build assertions.
</threat_model>

<verification>
- `cd site && npm run build && npm run validate` (chained, ONE command — OneDrive dist/ desync) is fully green.
- 16 new pages built (8 crimes x 2 locales): homicide/homicidios + 7 families in both namespaces.
- Homicide page ranked rates are non-uniform (featured_rates branch, not by_family).
- Single H1 per page; ItemList + BreadcrumbList + Dataset JSON-LD on every page.
- hreflang en/es/x-default reciprocal; canonical unique per page; OG injected (pageType=crime).
- Sitemap includes the 16 new URLs (auto via @astrojs/sitemap + getStaticPaths); coverage.mjs no-orphan-link passes.
- Comuna pages reciprocally link the homicide ranking.
- forbidden-language.mjs clean on all new prose.

Manual-only (per 15-VALIDATION.md — judgment checks, not blocking automated gates):
- Browser: confirm hreflang resolves cross-locale (load /crime-ranking/homicide/, click locale switch -> /es/ranking-delito/homicidios/).
- Editorial: methodology note reads accurately (lists included delitos, cites CEAD, reported-incidence framing).
</verification>

<success_criteria>
- CSEO-01: bilingual ranking pages exist for the 8 in-scope crime types (homicide first), internal-linked to comuna pages, reciprocal hreflang, sitemap entries.
- CSEO-02: each page static/indexable, single keyword H1, unique title/meta/canonical, ItemList JSON-LD, sober reported-incidence tone, CEAD cited.
- Full 13-validator suite + build green; no regression to existing pages.
</success_criteria>

<output>
Create `.planning/phases/15-crime-type-seo-ranking-pages/15-02-SUMMARY.md` when done.
</output>
