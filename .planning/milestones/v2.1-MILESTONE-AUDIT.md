# MILESTONE AUDIT — v2.1 News Intelligence, Map UX & Ops Hardening

**Status:** `passed` (audited as `gaps_found`; the single archival blocker I-01 was applied before archiving — see §2)
**Audited:** 2026-08-07 · Opus integration checker + orchestrator verification
**Scope:** Phases 26–33 · 54 requirements (CLUS / FACET / NEWSUI / MAPUX / MAPSH / DOCS / CRON / SEC)

## 1. Verdict

**Zero broken cross-phase connections, zero broken E2E flows, zero v2.0 regressions.** Every
connection traced end-to-end resolves to WIRED against the code, not against the summaries.
The wiring was genuinely clean; the paperwork was one edit behind it.

## 2. Findings

### I-01 — BLOCKER (documentation) — **RESOLVED before archiving**

`.planning/REQUIREMENTS.md` still carried `- [ ]` for **MAPUX-01..05 and MAPSH-01..07**, and
twelve `| Phase 29 | Pending |` / `| Phase 30 | Pending |` traceability rows, while the same
repo's `STATE.md` recorded both phases 100% COMPLETE and the code shipped and live.

This mattered because it is **the record about to be frozen**: archiving would have
immortalised a document claiming a third of the milestone never happened. Verified
independently by the orchestrator (`sed -n '72,86p'` and `'163,174p'`), then fixed: 12
checkboxes ticked, 12 rows flipped. `grep -c "| Pending |"` → **0**; `grep -c "^- \[ \]"` → **0**.

### I-02 — WARNING — MAPSH-06's geolocation regression item never discharged (carried)

`30-VERIFICATION.md:4` closed Phase 30 at `gaps_found` with one PARTIAL: the locate control is
reached through a prop chain Phase 30 rewrote and no runtime regression pass covers it. The
static trace is clean end to end — `MapIsland.tsx:37` (`import { locate }`), `:454-457` (handler),
`MapTopbar.tsx:22,37,48` (prop forwarded), `SearchBox.tsx:77,79` (`locate-btn-visible`,
`onClick={onLocate}`), and `locate-btn-visible` present in the shipped
`dist/_astro/MapIsland.CFisI_-J.js`. Nothing is disconnected; what is missing is a runtime
assertion, so **nothing would go red if it regressed**. Carried as a note, not a blocker.

### I-03 — WARNING — `fetch-lint-tools.sh` has no direct workflow caller (informational)

Reached transitively: `lint-workflows.sh:6` invokes it, and `ci.yml:87` invokes that. Not
orphaned. Flagged only so a future reader grepping the workflows for it is not misled.

## 3. Cross-phase wiring — all WIRED

| # | Connection | Evidence |
|---|---|---|
| W-1 | 26 → 27/28: the clustering NO-GO is real in code | `pipeline/scrape_news.py` never imports `pipeline.news.clustering`; repo-wide the module is referenced only by `build_golden_set.py:71`, `run_clustering_spike.py:41` and tests. No `cluster_id`/`is_primary` anywhere in `pipeline/` outside those. Gate quarantined at `test_clustering.py:976` (`xfail(strict=True)`). |
| W-2 | 27 → 28: `computeNewsFacets()` feeds the filter UI | `news.astro:20,135` and `es/noticias.astro:19,132` import and call it; output drives rendered counts and is serialized at `news.astro:323` via `escapeForInlineScript()`. |
| W-3 | 27 → 28: the client consumes that payload | `news.astro:343-346` reads `#news-facets` and `JSON.parse`s it, then runs `computeFacetCounts` from `newsFilterLogic`. |
| W-4 | 27 → validator suite | `site/scripts/validate/all.mjs:20,55` registers `facets.mjs` as validator #16. |
| W-5 | 30 preserved 28's news toggle | `NewsToggle.tsx:16` ← `MapTopbar.tsx:8,50` ← `MapIsland.tsx:42,438,451-452`; `news-toggle` present in the built bundle. |
| W-6 | 30 preserved the `?cut=` deep link | `MapIsland.tsx:227-236` retains the D-08 handler and the 4–5-digit pre-fetch guard; `get("cut")` in the built bundle. |
| W-7 | 30 added `?region=` (MAPSH-04) | `site/src/lib/regionParam.ts` (+ its test) → `MapIsland.tsx:46,235`. |
| W-8 | 30 did NOT touch the protected Leaflet layers | Last commit per file predates v2.1 (`ChoroplethLayer.ts` 2026-06-19, `IncidentPinLayer.ts` 2026-07-02, `LowZoomDotLayer.ts` 2026-06-13). No declarative react-leaflet layer component introduced. |
| W-9 | MAPSH-07: shared vocabulary, no code dependency | Map imports `familyDefs`; the news pages import label maps; neither news page references `components/map` or `MapIsland`. |
| W-10 | 32 → 33: shared scripts still called by the workflows 33 modified | `require-env.sh` ← 5 workflows; `push-with-rebase.sh` ← the 2 pushing crons; `check-heartbeat.sh` ← `heartbeat.yml:62,68,74`; `check-sha-pins.sh` ← `ci.yml:90`; `check-secret-hygiene.sh` ← `ci.yml:101`; `lint-workflows.sh` ← `ci.yml:87`. |
| W-11 | 33's SHA pinning did not break 32's calls | Every `bash .github/scripts/*.sh` line still resolves; all four gates exit 0. |
| W-12 | CRON-03 single retry policy | Byte-identical curl at `cead-scraper.yml:113`, `deploy-manual.yml:53`, `deploy-on-code.yml:66`, `news-pipeline.yml:98`. |
| W-13 | CRON-04 per-pipeline labels | `pipeline-failure-{cead,heartbeat,news,r2}`, each paired with the generic label. |

## 4. E2E flows

**Flow A — news faceting: COMPLETE.** Superset pre-rendered (NEWSUI-02): 1,489 `news-card`
nodes in `dist` per locale, 1,499 live (the delta is the news cron running after the build),
each carrying `data-family`/`data-region-id`/`data-date`. One `#news-facets` payload per page;
four facet groups both locales. `GET /news/?family=robos&window=7d` → **200**, emitting
`<link rel="canonical" href="https://ischilesafe.com/news/">` plus hreflang — the query-param
design adds no indexable URL. `#news-empty` ships `hidden`. **EN and ES render identical card
counts from an identical computation**, so FACET-01's "two locales cannot drift" holds
observationally, not merely by shared import.

**Flow B — map + deep links: COMPLETE.** `/map/` 200; the **production bundle hash equals the
local `dist` hash**, so every `dist`-level assertion transfers to production. `/map/?cut=13101`
200, `/map/?region=13` 200, `/es/mapa/` 200.

**Flow C — v2.0 surfaces: no regression.** `compare`, `commune/santiago`, `rankings`,
`is-santiago-safe`, `404.html` all present and 200 in production; the three protected Leaflet
files byte-untouched since v2.0 — the surface v2.1 could most plausibly have broken.

## 5. Per-requirement coverage — 54/54 MET

- **CLUS-01..09** — MET via the documented NO-GO (fp=11, tp=22, precision 0.667 over 86
  hand-labeled pairs against a 100 %/zero-false-merge gate), verified in code rather than on
  the summary's word (W-1). A measured negative result is a met requirement, not a gap.
- **FACET-01..07** — MET. CEAD's `FAMILY_KEYS` stays exactly **7** (`pipeline/shared/schema.py:22-30`)
  while the news vocabulary carries the news-only `sexuales` — the separation held.
- **NEWSUI-01..07** — MET. NEWSUI-02, which originally shipped with *no gate at all*, now holds
  observationally at 1,489/1,499 pre-rendered cards per locale. The `</script>` XSS closed in
  review stays closed via `escapeForInlineScript()`.
- **MAPUX-01..05 / MAPSH-01..07** — MET in code; the record was corrected by I-01. MAPUX-05
  discharged as Fable-proxy acceptance under F-46 and reviewed by the user afterwards.
  MAPSH-03: zero map-owned touch-target violations at every width.
- **DOCS-01..06** — MET. `methodology.astro:337-338` states the rank direction correctly
  ("Rank 1 = highest reported rate"), the inversion this phase existed to fix.
- **CRON-01..07** — MET, and CRON-01 is stronger than its wording: the old
  `if: env.CF_HOOK != ''` gate was **deleted** (F-76), so an empty hook fails loudly instead of
  skipping green.
- **SEC-01..06** — MET. zizmor runs from `ci.yml:98` and is **absent from
  `pipeline/requirements.txt`**, honouring SEC-04's explicit prohibition.

## 6. Measurements

Cited from the same day, not re-run: pytest **395 passed / 1 skipped / 1 xfailed** · vitest
**59** · **16/16 validators** · `astro check` **0 errors / 0 warnings** (F-132) · the four
workflow-security gates exit 0 · production 200 across seven paths.

Added by this audit: `/news/?family=robos&window=7d` 200 · `/map/?cut=13101` 200 ·
`/map/?region=13` 200 · `/es/mapa/` 200 · production `MapIsland` bundle hash == local `dist` hash.

## 7. Carried items (not v2.1 gaps)

- v2.0 GL-04 / Phase 22-03 GSC submission — operator task per **F-133**.
- **12 open Dependabot security alerts** in `site/package-lock.json` (7 high) — pre-existing
  transitive dependencies, made visible by this milestone rather than caused by it (**F-130/F-131**).
- I-02's one browser click (geolocation regression pass).
- Map legend numeric bands do not vary by selected family (`MapIsland.tsx:201,290`) — pre-existing,
  flagged in `30-CLOSE.md §7`.
- `comparator_table.json`: 106 KB shipped to the CDN with zero runtime consumers (**F-135**).

## 8. Archival recommendation

**Archive.** I-01 was applied before archiving, so the milestone's 54/54 claim is now true of
the document as well as of the code. I-02 and I-03 travel forward as notes; neither describes a
broken connection, and both rest on a verified static trace.
