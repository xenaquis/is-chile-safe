---
phase: 21-commune-comparator-a-vs-b-seo
plan: 02
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - site/src/islands/ComparatorIsland.tsx
  - site/src/pages/compare/index.astro
  - site/src/pages/es/comparar/index.astro
  - site/src/components/PageHeader.astro
autonomous: false
requirements: [CMP-01, CMP-02]
must_haves:
  truths:
    - "A user at /compare/ and /es/comparar/ sees a pre-rendered static HTML shell (h1, lead, methodology caveat, noscript) that Google can index"
    - "The comparator island hydrates client-side and lets a user select 2–3 communes via accent-insensitive autocomplete across all 346 communes"
    - "The side-by-side panel shows composite-index headline, per-family breakdown, per-family trend indicator, a homicide-per-100k row, and a Chile/regional average column"
    - "A 'Compare' nav link appears in the site header for both locales pointing to /compare/ and /es/comparar/"
  artifacts:
    - path: "site/src/islands/ComparatorIsland.tsx"
      provides: "client:only react comparator island with autocomplete + side-by-side columns + homicide row"
      min_lines: 120
    - path: "site/src/pages/compare/index.astro"
      provides: "EN comparator landing static shell + island mount"
      contains: "comparator-root"
    - path: "site/src/pages/es/comparar/index.astro"
      provides: "ES comparator landing static shell + island mount"
      contains: "comparator-root"
    - path: "site/src/components/PageHeader.astro"
      provides: "Compare nav link (EN+ES hardcoded slugs)"
      contains: "comparar"
  key_links:
    - from: "site/src/pages/compare/index.astro"
      to: "site/src/islands/ComparatorIsland.tsx"
      via: "client:only='react' mount with communes prop"
      pattern: "client:only"
    - from: "site/src/islands/ComparatorIsland.tsx"
      to: "/data/cead/comunas/{cut}.json"
      via: "safeFetch on commune selection"
      pattern: "comunas/"
---

<objective>
Build the interactive comparator: a React island (mirroring the existing map island hydration pattern) that lets a user pick 2–3 communes with accent-insensitive autocomplete and renders them side by side with composite index, per-family breakdown, trend indicators, a homicide-per-100k row (added scope), and a Chile/regional average column — all on a pre-rendered, Google-indexable static shell at /compare/ and /es/comparar/.

Purpose: CMP-01 + CMP-02. The comparator is the phase's interactive centerpiece. The static shell guarantees SEO indexability; the island delivers the interaction without any new heavy dependency.

Output: ComparatorIsland.tsx, EN + ES landing pages, and the header nav link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-commune-comparator-a-vs-b-seo/21-RESEARCH.md
@.planning/phases/21-commune-comparator-a-vs-b-seo/21-PATTERNS.md
@.planning/phases/21-commune-comparator-a-vs-b-seo/21-UI-SPEC.md

<interfaces>
<!-- Source-of-truth contracts. Use directly. -->

Island mount pattern (from site/src/pages/map.astro line 40):
  <MapIsland client:only="react" lang="en" nationalAvg={nationalAvg} />

Shell data prop (from site/src/lib/data.ts):
  export function loadIndex(): CommuneMeta[]   // CommuneMeta = { cut; name; slug; region_id; population; low_population }
  // Pass communes={loadIndex().map(c => ({ id: c.cut, name: c.name, region_id: c.region_id }))} to the island.

Per-commune JSON fetched at runtime (served by sync-data at /data/cead/comunas/{cut}.json):
  { id; name; slug; region_id; national_rank; regional_rank; trend: 'up'|'down'|'stable';
    featured_rates: { propiedad; homicidios; homicidios_count; secuestros };  // each Record<string,number>
    series: [{ year; rate_per_100k; by_family: {vida,robos_violentos,vif,drogas,armas,propiedad,incivilidades}; partial? }];
    composite_index: { "2024": { score; rank; regional_rank; level; available_metrics } } }

Composite table fetched once at runtime (/data/cead/comparator_table.json): array of 346 entries with composite_index.
National/regional per-family averages: /data/cead/national.json and /data/cead/regions/{region_id}.json (series[latestYear].by_family).

safeFetch helper (copy verbatim from site/src/components/map/MapIsland.tsx lines 64-74):
  async function safeFetch<T>(url): Promise<T|null>  // try/catch, returns null on !ok

Homicide access (HOM-02 from ResultPanel.tsx — use !== undefined, 0 is valid):
  const homRate = data.featured_rates?.homicidios?.[String(year)];
  const homCount = data.featured_rates?.homicidios_count?.[String(year)];
  const hasHom = homRate !== undefined && homCount !== undefined;

Accent-insensitive autocomplete (port of pipeline/news/resolver.py _norm):
  const normalizeForSearch = s => s.normalize('NFD').replace(/\p{Diacritic}/gu,'').toLowerCase();

latestCompleteYear client-side (Pitfall 3): Math.max(...series.filter(s=>!s.partial).map(s=>s.year))

i18n keys (added in Plan 01): cmp_page_h1, cmp_page_lead, cmp_input_placeholder, cmp_empty_heading,
  cmp_add_prompt, cmp_no_match, cmp_max_reached, cmp_avg_column, cmp_remove_aria, cmp_data_error,
  cmp_homicide_row, cmp_homicide_no_data, nav_compare. Pass the relevant strings into the island as props
  (the island is client:only and cannot import EN_STRINGS at SSR-safe time — pass a strings object prop, matching how MapIsland receives lang).

ES slug rule (memory i18n-localized-slug-pitfall): hardcode esPath="/es/comparar/"; NEVER getRelativeLocaleUrl.
Astro <script> rule (memory astro-script-no-expr-interpolation): do not interpolate {expr} in <script>; pass via props/data-* — N/A here since island is React, but keep shell free of inline-script interpolation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComparatorIsland.tsx — autocomplete + side-by-side columns + homicide row</name>
  <files>site/src/islands/ComparatorIsland.tsx</files>
  <read_first>
    - site/src/components/map/MapIsland.tsx (hydration pattern, Props interface, safeFetch helper, useState/useRef/useEffect usage)
    - site/src/components/map/ResultPanel.tsx (HOM-02 homicide block: featured_rates.homicidios / homicidios_count, !== undefined, fallback copy)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-UI-SPEC.md (Component Inventory #1: ComparatorSearch/SelectedCommuneTag/ComparatorColumn/FamilyComparisonTable/AverageColumn/CompositeIndexBand; Interaction States table; Accessibility Contract; Color/Typography tokens)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-PATTERNS.md (ComparatorIsland.tsx section: Props with communes[], searchIndex, homicide sub-row, latestCompleteYear)
    - site/src/config/i18n.ts (TREND_ARROW symbols / trend labels if present; family labels)
  </read_first>
  <action>
    Create site/src/islands/ComparatorIsland.tsx as a default-export React component. Props: { lang: 'en'|'es'; communes: Array<{id:string;name:string;region_id:string}>; strings: Record<string,string> }. Build the accent-insensitive search index at init from the communes prop using normalizeForSearch; expose a searchCommunes(query) filter returning ≤20 matches. Manage selected communes in useState (max 3; min behavior per Interaction States). On selection, safeFetch /data/cead/comunas/{cut}.json (copy safeFetch from MapIsland) and also safeFetch /data/cead/comparator_table.json once (memoize) plus /data/cead/national.json and the relevant /data/cead/regions/{region_id}.json for the AverageColumn. Compute latestCompleteYear client-side via the !partial filter. Render: ComparatorSearch (role=combobox, role=listbox, role=option, aria-expanded, arrow/Enter/Escape keyboard nav, 44px min touch targets, already-selected items aria-disabled), SelectedCommuneTag pills (× button uses cmp_remove_aria interpolated with name), one ComparatorColumn per commune (name h2, composite score at --text-display, band label, national rank), a FamilyComparisonTable (7 families + a distinct Homicide sub-row under Life crimes using HOM-02 logic — !== undefined, cmp_homicide_row label with {year}, cmp_homicide_no_data fallback; per-family trend arrow ↑↓→ with --trend-up/down/stable), and a rightmost AverageColumn ("Chile avg." header, --bg tint to distinguish). Implement all 8 interaction states from the UI-SPEC (empty / 1 / 2-3 / autocomplete open / no-match / loading skeleton / data error / max reached). Use existing CSS tokens only; no new design system; no Leaflet. Interpolate strings from the strings prop (do NOT import EN_STRINGS at module top — island is client:only). Editorial invariant: never render an absolute safe/dangerous verdict.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx astro check 2>&1 | grep -v "^#" | grep -iE "ComparatorIsland|error" | head; npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "ComparatorIsland" | head; echo "check-complete"</automated>
  </verify>
  <acceptance_criteria>
    - File exports a default React component with Props { lang; communes; strings }
    - Homicide sub-row uses `!== undefined` (not truthiness) — source assertion; grep `homicidios_count` present
    - normalizeForSearch uses NFD + \p{Diacritic} strip (source assertion)
    - `npx astro check` reports no type errors referencing ComparatorIsland
    - No top-level import of EN_STRINGS/ES_STRINGS in the island (client:only safety)
  </acceptance_criteria>
  <done>Island compiles type-clean; autocomplete is accent-insensitive; columns render composite + per-family + homicide row + average column; all 8 states handled.</done>
</task>

<task type="auto">
  <name>Task 2: EN + ES comparator landing shells + Compare nav link</name>
  <files>site/src/pages/compare/index.astro, site/src/pages/es/comparar/index.astro, site/src/components/PageHeader.astro</files>
  <read_first>
    - site/src/pages/map.astro (EXACT static-shell + island-mount + BaseLayout enPath/esPath pattern)
    - site/src/components/PageHeader.astro (nav-link block lines 21-31, 64-70 — hardcoded per-locale slug pattern)
    - site/src/components/MethodologyCaveat.astro (mandatory caveat component signature)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-UI-SPEC.md (Static Shell Contract: h1, lead, MethodologyCaveat, noscript, #comparator-root)
    - .planning/phases/21-commune-comparator-a-vs-b-seo/21-PATTERNS.md (compare/index.astro + es/comparar/index.astro sections)
  </read_first>
  <action>
    Create site/src/pages/compare/index.astro mirroring map.astro: import BaseLayout, ComparatorIsland (from ../../islands/ComparatorIsland), MethodologyCaveat, loadIndex, EN_STRINGS. Compute communes = loadIndex().map(c => ({ id: c.cut, name: c.name, region_id: c.region_id })). Build a strings object from EN_STRINGS (the cmp_* keys the island needs) and pass it as the strings prop. Render BaseLayout with lang="en", enPath="/compare/", esPath="/es/comparar/" (HARDCODE the ES slug per memory note), a unique title + description. In the body render the Static Shell Contract: <h1>{EN_STRINGS.cmp_page_h1}</h1>, <p>{EN_STRINGS.cmp_page_lead}</p>, <MethodologyCaveat locale="en" />, a <noscript> EN fallback, and <div id="comparator-root"><ComparatorIsland client:only="react" lang="en" communes={communes} strings={strings} /></div>. Create site/src/pages/es/comparar/index.astro as the ES mirror (lang="es", ES_STRINGS, three-level import paths ../../../, ES noscript text, esPath/enPath swapped accordingly). Extend PageHeader.astro: add a compareHref = locale === 'en' ? '/compare/' : '/es/comparar/' (hardcoded per-locale, matching mapHref) and insert <a href={compareHref} class="nav-link">{t.nav_compare}</a> into the nav (place it after nav_map, before nav_communes).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npm run build 2>&1 | tail -5 && node -e "const fs=require('fs'); const en='dist/compare/index.html', es='dist/es/comparar/index.html'; for(const f of [en,es]){ if(!fs.existsSync(f)){console.error('missing',f);process.exit(1)} const h=fs.readFileSync(f,'utf8'); if(!h.includes('comparator-root')){console.error('no root',f);process.exit(1)} if(!/<h1[^>]*>/.test(h)){console.error('no h1',f);process.exit(1)} } const enh=fs.readFileSync(en,'utf8'); if(!enh.includes('/es/comparar/')){console.error('no es hreflang');process.exit(1)} console.log('OK shells built + indexable + hreflang')"</automated>
  </verify>
  <acceptance_criteria>
    - dist/compare/index.html and dist/es/comparar/index.html exist after build
    - Each shell contains an <h1>, the lead paragraph, MethodologyCaveat, a noscript fallback, and #comparator-root (pre-rendered, indexable — CMP-02)
    - EN shell links to /es/comparar/ (hardcoded ES slug, hreflang reciprocity)
    - PageHeader contains a Compare nav link for both locales (grep "comparar")
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Both landing shells pre-render indexable content + mount the island; Compare nav link present in both locales; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The interactive comparator island on /compare/ and /es/comparar/: accent-insensitive autocomplete over 346 communes, 2–3 side-by-side columns with composite index, per-family breakdown, trend arrows, a homicide-per-100k row, and a Chile-average column. Pre-rendered static shell for SEO.</what-built>
  <how-to-verify>
    1. Run `cd site && npm run build` then `npx astro preview --port 4321 --host` (no preview npm script — use astro preview per memory note serve-build-for-e2e-review).
    2. Visit http://localhost:4321/compare/ — confirm h1 + lead + methodology caveat render before JS (view-source shows them).
    3. Type "nunoa" (no tilde) in the search — confirm "Ñuñoa" appears in the dropdown (accent-insensitive).
    4. Select 2–3 communes — confirm side-by-side columns show composite index headline, per-family rates with trend arrows, a distinct Homicide (per 100k, {year}) row, and a "Chile avg." column.
    5. Confirm a commune with 0 homicides shows the count (0 is valid data, not "no data").
    6. Visit http://localhost:4321/es/comparar/ — confirm ES copy parity (Comparar Comunas… / Prom. Chile / Homicidios).
    7. Mobile: open at 375px (iframe per memory note) — columns stack vertically, no horizontal scroll.
    8. Confirm no absolute "safe/dangerous/seguro/peligroso" verdict anywhere.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. autocomplete miss, homicide row wrong, ES parity gap)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → autocomplete input | User types a free-text query; matched against the closed 346-name set via normalized includes(). No server, no injection target. |
| browser → static JSON fetch | Island fetches /data/cead/*.json (same-origin static files). No credentials, no mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering (XSS) | Commune name rendered in island DOM + autocomplete | mitigate | React auto-escapes all rendered children; commune names come from the closed CEAD catalog (loadIndex), never raw user input. No dangerouslySetInnerHTML. |
| T-21-05 | Information disclosure | Static JSON fetches | accept | All data is public CEAD/SPD statistics already published on commune pages; no PII, no secrets. |
| T-21-06 | Repudiation | Island-rendered comparison | mitigate | No absolute-verdict language; reuses sober per-family/composite framing from ResultPanel; human-verify checkpoint confirms. |
| T-21-SC | Tampering | npm installs | mitigate | Zero new runtime dependencies (React + Astro already present). No install task. |
</threat_model>

<verification>
- `npx astro check` type-clean for ComparatorIsland.
- `npm run build` emits dist/compare/index.html + dist/es/comparar/index.html with indexable shell content.
- Human-verify checkpoint confirms autocomplete, columns, homicide row, ES parity, mobile stacking, no forbidden verdict.
</verification>

<success_criteria>
- Comparator island hydrates over a pre-rendered indexable shell at both routes (CMP-02).
- 2–3 communes compare side by side with composite + per-family + trend + homicide row + average column (CMP-01).
- Accent-insensitive autocomplete across all 346 communes.
- Compare nav link in both locales.
</success_criteria>

<output>
Create `.planning/phases/21-commune-comparator-a-vs-b-seo/21-02-SUMMARY.md` when done
</output>
