---
phase: 24-rankings-ux-dynamic-sortable-tables-and-visual-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01-rankings-ux-dynamic-sortable-tables-and-visual-polish
# Verification-only; no source edits. May produce screenshot artifacts under .planning/.
files_modified: []
# autonomous: false — BrowserOS review runs INLINE by the orchestrator; gsd-executor subagents lack BrowserOS.
autonomous: false
# Phase 24 has no formal requirement IDs (ROADMAP: TBD).
requirements: []

must_haves:
  truths:
    - "On every reviewed ranking surface (EN+ES, desktop 1280px + mobile 375px), a teal sort arrow (▲ or ▼) is visible on the rate column header within 500ms of load."
    - "The year <select> appears above each ranking table and clicking a rate/name header reorders the rows."
    - "On mobile (375px), the ranking table scrolls horizontally via overflow-x without breaking page layout."
    - "Keyboard Tab reveals a visible teal focus ring on the sort header buttons."
    - "safest-cities (EN) and comunas-mas-seguras (ES) load ascending (▲, lowest first); home and crime-family load descending (▼)."
  artifacts:
    - path: ".planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-02-SUMMARY.md"
      provides: "BrowserOS E2E review record: per-surface pass/fail across desktop+mobile, EN+ES, with screenshot references"
      contains: "BrowserOS"
  key_links:
    - from: "BrowserOS review"
      to: "24-UI-SPEC.md BrowserOS E2E Review Contract"
      via: "each table row in the contract checked at both viewports"
      pattern: "sort arrow"
---

<objective>
Run the BrowserOS in-detail E2E visual review that closes Phase 24, verifying the sort affordance, year selector, mobile overflow, and focus ring across all ranking surfaces (EN+ES, desktop + mobile 375px) per the 24-UI-SPEC.md BrowserOS E2E Review Contract.

Purpose: Confirm the headline UX requirement (sort arrows obvious on load, sortable tables) holds visually on real rendered pages — automated validators cannot see arrows, focus rings, or mobile overflow.

Output: A pass/fail review record (24-02-SUMMARY.md) covering every contract row at both viewports, with screenshots; any failure becomes a gap for follow-up.

NOTE TO ORCHESTRATOR: This plan is non-autonomous. gsd-executor subagents do NOT have BrowserOS — run this review INLINE. save_screenshot needs a path with NO spaces; emulate mobile via a 375px viewport. Serve the production build locally first (`cd site && npm run preview`) and review the served URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-UI-SPEC.md
@.planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: BrowserOS E2E visual review of all ranking surfaces (EN+ES, desktop + mobile)</name>
  <files>.planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-UI-SPEC.md (BrowserOS E2E Review Contract table + Sortability Visual Contract + pass criteria)
    - .planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-01-SUMMARY.md (what was wired in Plan 01)
  </read_first>
  <action>
    Run the BrowserOS review INLINE (subagents lack BrowserOS). Build + serve in ONE command (OneDrive desync guard): `cd site && npm run build && npm run preview`; note the preview URL. At each viewport, navigate to each surface, screenshot to a NO-SPACES path, and record PASS/FAIL against the UI-SPEC pass criteria. Write results into 24-02-SUMMARY.md as a surface × viewport matrix. This is a verification task only — do not edit source files; if a check fails, document it as a discrete gap rather than fixing inline.
  </action>
  <what-built>
    Plan 01 fixed RankingTableEnhancer to show the sort affordance on load (honoring data-default-sort-dir)
    and wired /safest-cities-in-chile/ + /es/comunas-mas-seguras-chile/ into the enhancer.
    This review confirms it renders correctly on real pages — arrows, year selector, mobile overflow, focus ring.
  </what-built>
  <how-to-verify>
    1. Build + serve locally in ONE command (OneDrive desync guard): `cd site && npm run build && npm run preview`. Note the local preview URL (e.g. http://localhost:4321).
    2. With BrowserOS, at DESKTOP viewport 1280px, navigate to and screenshot each surface; confirm a teal (#0f766e) sort arrow is visible on the rate column header within ~500ms of load, a year <select> sits above the table, and clicking the rate header reorders rows:
       - / (EN home) — expect descending (▼)
       - /es/ (ES home) — expect descending (▼)
       - one /crime-ranking/<crime>/ page (EN) — expect descending (▼), year selector functional
       - the matching /es/ranking-delito/<crime>/ page (ES) — expect descending (▼)
       - /safest-cities-in-chile/ (EN) — expect ASCENDING (▲), lowest-rate first
       - /es/comunas-mas-seguras-chile/ (ES) — expect ASCENDING (▲), lowest-rate first
    3. Keyboard: Tab to a sort header button on one desktop page; confirm a visible 2px teal focus ring (offset 2px).
    4. Switch to MOBILE viewport 375px and re-check each of the six surfaces above: sort arrow still visible on load; table scrolls horizontally (overflow-x) without breaking page layout.
    5. Save screenshots to a NO-SPACES path (e.g. C:/tmp/p24-review/<surface>-<viewport>.png) and reference them in the SUMMARY.
    6. Record each contract row as PASS/FAIL. Pass criteria (per UI-SPEC): sort arrow visible in teal on the active rate header within 500ms; year selector present above table; table reorders correctly on header click.
  </how-to-verify>
  <verify>
    <human-check>All six surfaces confirmed at both 1280px and 375px; safest-cities EN+ES ascending (▲), home + crime-family descending (▼); mobile overflow + keyboard focus ring confirmed; results recorded in 24-02-SUMMARY.md with screenshot references.</human-check>
  </verify>
  <acceptance_criteria>
    - All six surfaces checked at BOTH 1280px and 375px; each result recorded PASS/FAIL with a screenshot reference.
    - safest-cities (EN) and comunas-mas-seguras (ES) confirmed ascending (▲) on load; home + crime-family confirmed descending (▼).
    - Mobile (375px) horizontal overflow confirmed present on safest-cities tables without layout breakage.
    - Focus ring (2px teal, offset 2px) confirmed on a sort header button via keyboard Tab.
    - Any FAIL is written up as a discrete gap (surface, viewport, expected vs actual) for gap-closure planning.
  </acceptance_criteria>
  <done>24-02-SUMMARY.md records a complete PASS/FAIL matrix (6 surfaces × 2 viewports + focus ring + mobile overflow) with screenshot references; all checks pass or failures are captured as actionable gaps.</done>
  <resume-signal>Type "approved" if all surfaces pass at both viewports, or list the failing surface/viewport pairs.</resume-signal>
</task>

</tasks>

<verification>
- 24-02-SUMMARY.md records a PASS/FAIL matrix covering all six surfaces × 2 viewports, plus the focus-ring and mobile-overflow checks, with screenshot paths.
- No source files modified by this plan (verification-only).
</verification>

<success_criteria>
- Every ranking surface shows the sort affordance on load at both desktop and mobile, with the correct default direction (▼ home/crime-family, ▲ safest-cities EN+ES).
- Year selector and click-to-sort confirmed functional; mobile overflow and keyboard focus ring confirmed.
- Review record committed; any failures captured as actionable gaps.
</success_criteria>

<output>
Create `.planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-02-SUMMARY.md` when done.
</output>
