---
phase: 24-rankings-ux-dynamic-sortable-tables-and-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/RankingTableEnhancer.astro
  - site/src/pages/safest-cities-in-chile.astro
  - site/src/pages/es/comunas-mas-seguras-chile.astro
autonomous: true
requirements: []   # Phase 24 has no formal requirement IDs (ROADMAP: "TBD"). must_haves derived from phase goal + 24-UI-SPEC.md.

must_haves:
  truths:
    - "On the EN home and crime-family ranking pages, a sort arrow (▼) is visible on the rate column header on page load without any user interaction (descending default preserved)."
    - "On /safest-cities-in-chile/ and /es/comunas-mas-seguras-chile/, a sort arrow (▲) is visible on the rate column header on page load (ascending default, editorial intent preserved)."
    - "Clicking any wired ranking table's rate or commune-name header re-sorts the rows and updates the visible arrow + aria-sort."
    - "A year <select> appears above each safest-cities ranking table and re-renders rates for the chosen year."
    - "With JavaScript disabled, both safest-cities pages still render their static server-ordered table (ascending by rate) with no JS errors."
  artifacts:
    - path: "site/src/components/RankingTableEnhancer.astro"
      provides: "Auto-initialized sort on load (arrow + aria-sort visible without interaction) honoring per-table data-default-sort-dir"
      contains: "defaultSortDir"
    - path: "site/src/pages/safest-cities-in-chile.astro"
      provides: "Wired sortable ranking table (data-ranking-table, per-row data-rates, enhancer mounted, asc override)"
      contains: "data-ranking-table"
    - path: "site/src/pages/es/comunas-mas-seguras-chile.astro"
      provides: "ES wired sortable ranking table (mirror of EN, data-locale=es)"
      contains: "data-ranking-table"
  key_links:
    - from: "site/src/pages/safest-cities-in-chile.astro"
      to: "site/src/components/RankingTableEnhancer.astro"
      via: "import + <RankingTableEnhancer locale=\"en\" /> mounted after the table wrapper"
      pattern: "RankingTableEnhancer"
    - from: "site/src/pages/safest-cities-in-chile.astro <tr>"
      to: "RankingTableEnhancer renderSort()"
      via: "data-rates JSON built from data.series.filter(s => !s.partial) using s.rate_per_100k"
      pattern: "data-rates"
    - from: "RankingTableEnhancer init"
      to: "<table data-default-sort-dir>"
      via: "table.dataset.defaultSortDir read drives initial sortDir; absent => 'desc'"
      pattern: "defaultSortDir"
---

<objective>
Wire the two remaining unwired ranking surfaces (`/safest-cities-in-chile/` and `/es/comunas-mas-seguras-chile/`) into the existing `RankingTableEnhancer`, and fix the enhancer so the sort affordance (arrow + aria-sort) is visible on page load across ALL wired surfaces — the headline requirement of 24-UI-SPEC.md that is currently unmet everywhere.

Purpose: Make ranking pages — the project's most important SEO products — feel like first-class sortable data tables, with sortability obvious on load and an editorial ascending override on the "lowest-rate" pages.

Output: Modified enhancer with auto-initialized sort honoring `data-default-sort-dir`; both safest-cities pages wired with `data-ranking-table`, per-row `data-rates`, and a mounted enhancer; verified build + validators.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-UI-SPEC.md
@.planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — do NOT re-explore. -->

RankingTableEnhancer.astro table-level data-* API (from its docblock, lines 12–21):
  data-ranking-table   — presence flag; tables without it are ignored
  data-rate-col        — 0-based <td> index of the rate cell
  data-name-col        — 0-based <td> index of the commune-name cell
  data-rate-format     — "round" | "1dp" (must match server-side format)
  data-locale          — "en" | "es"
  data-default-sort-dir — NEW this plan: "asc" | "desc"; absent => "desc"

RankingTableEnhancer.astro row-level data-* API:
  data-rates  — JSON map of completeYear(number) -> rate(number)
  data-low-pop — "1" for low-pop rows (NOT needed here; safest-cities rows are all non-low-pop)

Current enhancer init state (RankingTableEnhancer.astro lines 104–106):
  let sortKey: 'rate' | 'name' | null = null;   // null => NO arrow renders on load (the bug)
  let sortDir: 'asc' | 'desc' = 'desc';
  updateAriaSort() (lines 171–191) renders the arrow span; renderSort() (lines 194–242) reorders rows.
  Neither is called on init today — only on header click (line 155) / year change (line 245).

CommuneRankingTable.astro — reference for a CORRECTLY wired table (already shipped):
  <table ... data-ranking-table data-rate-col="1" data-name-col="0" data-rate-format="round" data-locale={locale}> (line 49)
  <tr ... data-rates={JSON.stringify(row.ratesByYear)} data-low-pop={...}> (line 60)
  <RankingTableEnhancer locale={locale} /> mounted after the table (line 85)
  .table-wrapper has `-webkit-overflow-scrolling: touch;` (line 90)

Data shape (from site/src/lib/data.ts loadCommune): commune `data.series` is an array of
  { year:number, partial:boolean, rate_per_100k:number, by_family:{...} }.
  ratesByYear for these pages = total rate => use s.rate_per_100k (NOT by_family).

i18n VERIFIED present — no i18n.ts edit needed:
  EN_STRINGS (i18n.ts lines 291–295) and ES_STRINGS (lines 462–466) already define
  sort_label, sort_ascending, sort_descending, year_select_label, year_select_aria.
  The "2 new caption strings" in UI-SPEC are the inline <caption> sort-hint text on the
  two .astro pages (already partially present), NOT new i18n keys.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make RankingTableEnhancer auto-show the sort affordance on load and honor data-default-sort-dir</name>
  <files>site/src/components/RankingTableEnhancer.astro</files>
  <read_first>
    - site/src/components/RankingTableEnhancer.astro (the file being modified — read the full client `<script>` block, lines 43–262)
    - site/src/components/CommuneRankingTable.astro (already-wired analog — confirms desc default must keep working on home/region tables)
    - .planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-UI-SPEC.md (Sortability — Visual Contract; "sort arrow visible immediately without user interaction")
  </read_first>
  <behavior>
    - On page load (JS enabled) with a table that has data-rates rows, the rate column header shows an arrow glyph (▼ for desc, ▲ for asc) and the <th> has aria-sort set — WITHOUT any click.
    - A table carrying data-default-sort-dir="asc" loads with ascending order (▲) and lowest rate first for the selected (most recent complete) year.
    - A table with no data-default-sort-dir (home, region, crime-family) loads descending (▼) — NO regression of existing surfaces.
    - Header click still toggles direction exactly as before; year <select> still re-renders.
    - A table with no data-rates rows still silently exits (existing guard at lines 85–87 unchanged).
  </behavior>
  <action>
    In the per-table loop, after the existing config reads (the `rateCol`/`nameCol`/`rateFmt`/`loc` block around lines 74–77), add a read of the new attribute: `defaultSortDir` = `table.dataset.defaultSortDir === 'asc' ? 'asc' : 'desc'`.

    Change the initial sort state so the affordance renders on load: set `sortKey` to `'rate'` (instead of `null`) and set `sortDir` to `defaultSortDir` (replacing the hardcoded `'desc'` at line 106). Keep the `sortKey: 'rate' | 'name' | null` type — only the initial value changes.

    At the END of the per-table loop, after the existing initial-rate-hydration block (the `allTrList.forEach` at lines 251–259 that seeds `dataset.numericRate`), invoke `updateAriaSort()` then `renderSort()` once so the arrow, aria-sort, and initial row order all reflect the default sort on load. Do NOT duplicate the hydration loop — `renderSort()` already rewrites rate cells and seeds numericRate via its own pass, but the pre-existing seeding loop must remain so the first `renderSort()` has numeric values to sort by; call order is: existing seed loop, then `updateAriaSort()`, then `renderSort()`.

    Do not touch the `.rte-controls`, `.rte-sort-btn`, or `.rte-sort-arrow` styles, the i18n blob, or the year-select wiring. This is the ONLY component change in the phase; it must not regress the descending surfaces.
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - RankingTableEnhancer.astro client script reads `table.dataset.defaultSortDir` and derives `defaultSortDir` with fallback `'desc'` (grep: `defaultSortDir` appears at least twice — declaration + usage).
    - Initial `sortKey` is `'rate'` (not `null`) and initial `sortDir` is `defaultSortDir`.
    - `updateAriaSort()` and `renderSort()` are both invoked once during the per-table init (outside any click/change handler).
    - The pre-existing no-data-rates guard (`if (!hasRatesData) return;`) is unchanged.
    - `cd site && npm run build && npm run validate` exits 0; @astrojs/check passes (run as part of validate).
  </acceptance_criteria>
  <done>Enhancer renders the correct sort arrow + aria-sort on load for every wired table, honoring data-default-sort-dir; build and all validators pass; no regression to home/region/crime-family descending default.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire /safest-cities-in-chile/ (EN) and /es/comunas-mas-seguras-chile/ (ES) into the enhancer</name>
  <files>site/src/pages/safest-cities-in-chile.astro, site/src/pages/es/comunas-mas-seguras-chile.astro</files>
  <read_first>
    - site/src/pages/safest-cities-in-chile.astro (EN target — frontmatter communeRates map lines 31–44; table lines 117–145; .ranking-table-wrapper style lines 263–269)
    - site/src/pages/es/comunas-mas-seguras-chile.astro (ES target — mirror; communeRates lines 28–41; table lines 110–139; wrapper style lines 261–267)
    - site/src/components/CommuneRankingTable.astro (correctly-wired analog: table attrs line 49, data-rates row line 60, enhancer mount line 85, webkit-overflow line 90)
    - site/src/components/RankingTableEnhancer.astro (data-* contract docblock lines 12–21)
    - .planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-PATTERNS.md (7-step wiring recipe, Steps 1–6, + ES locale-difference table)
  </read_first>
  <action>
    Apply the identical wiring to BOTH pages (EN first, then ES mirror).

    Frontmatter: in the `communeRates` map, build a `ratesByYear: Record<number, number>` for each commune from `data.series.filter((s) => !s.partial)`, assigning `ratesByYear[s.year] = s.rate_per_100k` (total rate — NOT by_family). Add `ratesByYear` to the returned object. Do not change the existing ascending `communeRates.sort` or `TOP_N` slice — server order stays lowest-first.

    Imports: add `import RankingTableEnhancer from '../components/RankingTableEnhancer.astro';` to the EN page and `from '../../components/RankingTableEnhancer.astro';` to the ES page (ES file is one directory deeper).

    Table element: add attributes to the `<table class="ranking-table">` so it reads `data-ranking-table data-rate-col="3" data-name-col="1" data-rate-format="round" data-locale="en" data-default-sort-dir="asc"` (use `data-locale="es"` on the ES page). Column map is fixed: col0=#, col1=Commune/Comuna name, col2=Region/Región, col3=Rate/Tasa, col4=National rank. The `asc` override preserves the editorial lowest-first intent.

    Rows: add `data-rates={JSON.stringify(c.ratesByYear)}` to each `<tr>` in the `<tbody>` map. These rows are all non-low-population (filtered upstream) so no `data-low-pop` / low-pop class is added.

    Caption: append the sort hint to the existing `<caption>` text — EN: "Click column headers to sort." ; ES: "Haga clic en los encabezados de columna para ordenar." (inline text in the .astro file; these are the UI-SPEC "2 new caption strings" — NOT i18n keys).

    Mount: add `<RankingTableEnhancer locale="en" />` (ES: `locale="es"`) immediately AFTER the closing `</div>` of `.ranking-table-wrapper`, before the `.table-note` paragraph.

    Style: in each page's `.ranking-table-wrapper` rule, add `-webkit-overflow-scrolling: touch;` alongside the existing `overflow-x: auto;` to match the CommuneRankingTable analog (mobile momentum scroll).

    Do not modify i18n.ts (strings verified present). Do not touch the other sections, JSON-LD, or DataCallout.
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - safest-cities-in-chile.astro contains `data-ranking-table`, `data-rate-col="3"`, `data-name-col="1"`, `data-rate-format="round"`, `data-locale="en"`, and `data-default-sort-dir="asc"` on the ranking `<table>`.
    - comunas-mas-seguras-chile.astro contains the same attributes with `data-locale="es"`.
    - Each `<tr>` in the `<tbody>` of both pages emits a `data-rates={JSON.stringify(...)}` attribute built from `data.series.filter((s) => !s.partial)` using `s.rate_per_100k`.
    - Both pages import and mount `<RankingTableEnhancer .../>` after the `.ranking-table-wrapper` div (grep: `RankingTableEnhancer` present in both files).
    - Each page's `<caption>` includes the sort hint ("Click column headers to sort." EN / "Haga clic en los encabezados de columna para ordenar." ES).
    - Both `.ranking-table-wrapper` style rules include `-webkit-overflow-scrolling: touch;`.
    - No edit to site/src/config/i18n.ts.
    - `cd site && npm run build && npm run validate` exits 0; @astrojs/check passes.
  </acceptance_criteria>
  <done>Both safest-cities pages render a sortable ranking table that, on load, shows an ascending (▲) sort arrow and lowest-rate-first order, exposes a year selector, and reorders on header click — with the static no-JS table preserved. Build + all validators green.</done>
</task>

</tasks>

<verification>
- `cd site && npm run build && npm run validate` exits 0 (chained in ONE command — OneDrive desync guard).
- @astrojs/check (run inside validate) reports no type errors on the three modified files.
- grep confirms `data-ranking-table` and `data-default-sort-dir="asc"` on both safest-cities tables.
- grep confirms `defaultSortDir` read + the init `updateAriaSort()/renderSort()` calls in RankingTableEnhancer.astro.
- No new entry in site/src/config/i18n.ts; no new npm dependency added (package.json unchanged).
</verification>

<success_criteria>
- The headline UI-SPEC requirement is met: sort arrow visible on the rate column header on load for every wired surface (▼ on home/crime-family, ▲ on safest-cities).
- The two previously-unwired safest-cities pages are fully wired (table attrs + per-row data-rates + mounted enhancer + webkit overflow) with the ascending editorial override.
- Descending surfaces (home EN/ES, region pages, crime-family EN/ES) are NOT regressed — they keep their desc default via the absent-attribute fallback.
- Zero new dependencies; i18n.ts untouched.
- Build and all validators pass.
</success_criteria>

<output>
Create `.planning/phases/24-rankings-ux-dynamic-sortable-tables-and-visual-polish/24-01-SUMMARY.md` when done.
</output>
