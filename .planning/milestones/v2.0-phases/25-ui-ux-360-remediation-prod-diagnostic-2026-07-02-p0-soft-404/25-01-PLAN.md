---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/pages/404.astro
  - site/public/favicon.svg
  - site/public/favicon.ico
  - site/public/apple-touch-icon.png
  - site/src/layouts/BaseLayout.astro
  - site/src/components/PageHeader.astro
  - site/src/config/i18n.ts
  - site/src/pages/index.astro
autonomous: true
requirements: [P0-1, P0-2, P0-3]
must_haves:
  truths:
    - "An unknown URL served by the build produces dist/404.html (real 404 status in prod), not a 200 homepage clone"
    - "Every page's <head> declares a favicon (svg + ico + apple-touch-icon)"
    - "At tablet widths (~640-1099px) the header nav is collapsed to a hamburger and the home lead tables stack, with no horizontal overflow"
  artifacts:
    - path: "site/src/pages/404.astro"
      provides: "Real 404 page (EN primary + inline ES), CTA links to home/map/rankings"
      contains: "Page not found"
    - path: "site/public/favicon.svg"
      provides: "Brand pin-glyph favicon"
      contains: "#0f766e"
    - path: "site/public/favicon.ico"
      provides: "ICO fallback favicon"
    - path: "site/public/apple-touch-icon.png"
      provides: "180x180 apple touch icon"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro"
      to: "/favicon.svg + /favicon.ico + /apple-touch-icon.png"
      via: "<link rel=icon> tags in <head>"
      pattern: "rel=\"icon\""
    - from: "site/src/components/PageHeader.astro"
      to: "hamburger nav"
      via: "@media (min-width: 1100px)"
      pattern: "min-width:\\s*1100px"
---

<objective>
Deliver the three P0 critical fixes from the 2026-07-02 production audit — each as its own independent atomic commit, first, before any polish. These are the highest-SEO/branding-return fixes and must land ahead of all P1/P2 work.

Purpose: Kill the soft-404 (arbitrary URLs currently return a 200 homepage clone, wasting crawl budget and creating indexable duplicates), add a real favicon (Google shows favicons in mobile results), and fix the broken tablet breakpoint (~305px horizontal overflow at 772px viewport breaks the header on every page).

Output: `site/src/pages/404.astro`, favicon assets, favicon `<link>` tags in BaseLayout, hamburger breakpoint raised to 1100px, home lead-table stacking at ≤900px, shortened "Glossary" nav label.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-RESEARCH.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-PATTERNS.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

Note: `site/public/_redirects` does NOT exist (confirmed) — there is no catch-all to remove. Cloudflare Pages serves the root index.html for unmatched paths only because `dist/404.html` is absent. Creating `src/pages/404.astro` makes Astro emit `dist/404.html`, which CF serves with a real 404 status.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real 404 page (P0-1) — atomic commit</name>
  <files>site/src/pages/404.astro</files>
  <read_first>
    - site/src/pages/is-santiago-safe.astro (EditorialLayout invocation pattern, lines 11-14 imports, 66-73 layout props)
    - site/src/layouts/BaseLayout.astro (pageType="editorial" handling; how head/robots are set)
    - .planning/phases/25-.../25-UI-SPEC.md "### 404 Page (P0-1)" and Copywriting Contract rows (exact EN+ES copy)
  </read_first>
  <action>
    Create `site/src/pages/404.astro`. Use `BaseLayout` (or `EditorialLayout` which wraps it) with `pageType="editorial"`, `lang="en"`, `title="Page not found — Chile Safety Map"`, `description="The page you were looking for doesn't exist or has moved."`. Content: h1 "Page not found", body "The page you were looking for doesn't exist or has moved.", then three CTA links using the existing `.panel-cta` class: "Go to home" → `/`, "Explore the map" → `/map/`, "View rankings" → `/rankings/`. Below an `<hr>`, add an inline ES block: h1-equivalent "Página no encontrada", body "La página que buscas no existe o fue movida.", links "Ir al inicio" → `/es/`, "Explorar el mapa" → `/es/mapa/`, "Ver rankings" → `/es/rankings/` (hardcode ES slugs — getRelativeLocaleUrl does not translate them). Do NOT add `<meta name="robots" content="noindex">` (per UI-SPEC: CF serves real 404 status; noindex would signal the old 200-soft-404 pattern). Do NOT create or edit `public/_redirects`.
    Then commit ONLY this file: `git add site/src/pages/404.astro && git commit -m "feat(25): add real 404 page to kill soft-404 catch-all (P0-1)"`.
  </action>
  <verify>
    <automated>cd site && npm run build && test -f dist/404.html && grep -q "Page not found" dist/404.html && echo P0-1-OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/dist/404.html` exists after build.
    - `dist/404.html` contains the string "Page not found" AND "Página no encontrada".
    - `dist/404.html` does NOT contain `name="robots" content="noindex"`.
    - `site/public/_redirects` still does not exist.
    - CTA links present: href="/", href="/map/", href="/rankings/", href="/es/mapa/".
    - This change is its own commit (only 404.astro staged).
  </acceptance_criteria>
  <done>Unknown URLs will serve dist/404.html with real 404 status in production; no indexable homepage clones.</done>
</task>

<task type="auto">
  <name>Task 2: Favicon assets + head links (P0-3) — atomic commit</name>
  <files>site/public/favicon.svg, site/public/favicon.ico, site/public/apple-touch-icon.png, site/src/layouts/BaseLayout.astro</files>
  <read_first>
    - site/src/components/PageHeader.astro lines 43-47 (source PinGlyph SVG: circle cx=11 cy=9 r=5, inner ellipse, tail path)
    - site/src/layouts/BaseLayout.astro lines 63-113 (head block; manifest link at line 95 is the insertion anchor)
    - .planning/phases/25-.../25-UI-SPEC.md "### Favicon (P0-3)"
  </read_first>
  <action>
    Create `site/public/favicon.svg` from the PageHeader PinGlyph motif: `viewBox="0 0 22 22"`, root `fill="none"`, transparent background, with `<circle cx="11" cy="9" r="5" fill="#0f766e" />`, `<ellipse cx="11" cy="9" rx="2" ry="2" fill="#ffffff" />`, `<path d="M11 14 Q7 18 11 22 Q15 18 11 14Z" fill="#0f766e" />`. Use the literal hex `#0f766e` (CSS custom properties do not resolve in standalone SVG).
    Generate `site/public/apple-touch-icon.png` (180×180, same glyph rendered on a `#f5f8f8` opaque background) and `site/public/favicon.ico` (32×32) WITHOUT adding any npm dependency: write a one-off Node script that uses the already-installed `sharp` (in site/node_modules) to rasterize the SVG to a 180×180 PNG (apple-touch-icon) and a 32×32 PNG, then wrap the 32×32 PNG bytes in a minimal ICO container (6-byte ICONDIR header + 16-byte ICONDIRENTRY pointing at the embedded PNG) to produce a valid PNG-based favicon.ico. Do NOT install `png-to-ico` or any new package. Delete the one-off script after generating, or keep it under `site/scripts/` if it follows existing conventions.
    In `site/src/layouts/BaseLayout.astro`, after the manifest `<link>` (line ~95), add: `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`, `<link rel="icon" type="image/x-icon" href="/favicon.ico" />`, `<link rel="apple-touch-icon" href="/apple-touch-icon.png" />`.
    Then commit ONLY these files: `git commit -m "feat(25): add brand favicon (svg+ico+apple-touch) and head links (P0-3)"`.
  </action>
  <verify>
    <automated>cd site && npm run build && test -f dist/favicon.svg && test -f dist/favicon.ico && test -f dist/apple-touch-icon.png && [ "$(grep -c 'rel="icon"' dist/index.html)" -ge 2 ] && echo P0-3-OK</automated>
  </verify>
  <acceptance_criteria>
    - `dist/favicon.svg`, `dist/favicon.ico`, `dist/apple-touch-icon.png` all exist after build.
    - `dist/favicon.svg` contains `#0f766e`.
    - `dist/index.html` contains at least 2 `rel="icon"` link tags AND one `rel="apple-touch-icon"`.
    - `favicon.ico` is a valid ICO (first 4 bytes `00 00 01 00`).
    - No new entry added to `site/package.json` dependencies.
    - This change is its own commit.
  </acceptance_criteria>
  <done>All pages declare a brand favicon; the /favicon.ico 200-text/html symptom resolves once the real 404 (Task 1) plus this asset are in place.</done>
</task>

<task type="auto">
  <name>Task 3: Tablet breakpoint fix (P0-2) — atomic commit</name>
  <files>site/src/components/PageHeader.astro, site/src/config/i18n.ts, site/src/pages/index.astro</files>
  <read_first>
    - site/src/components/PageHeader.astro (the four `@media (min-width: 640px)` blocks at lines ~130-134, 204-206, 227-229, 255-263 — ALL must change together)
    - site/src/config/i18n.ts line 296 (`glossary_link: 'Crime type glossary'`) and line 495 (ES `'Glosario de tipos de delito'`)
    - site/src/pages/index.astro lines 109 and 115 (`<div class="lead-table-col">` blocks) and their parent flex container + `<style>` block
    - .planning/phases/25-.../25-UI-SPEC.md "### Breakpoints" and "### Nav Copy Fix"
  </read_first>
  <action>
    In `site/src/components/PageHeader.astro`, change ALL FOUR `@media (min-width: 640px)` blocks to `@media (min-width: 1100px)` (Pitfall 1: missing any one leaves the nav in a broken state — the hamburger toggle, nav display, and checked-dropdown positioning must all shift together). Hamburger is now visible up to 1099px; full nav at ≥1100px.
    In `site/src/config/i18n.ts`, change `glossary_link` EN value from `'Crime type glossary'` to `'Glossary'` (line ~296) and ES value from `'Glosario de tipos de delito'` to `'Glosario'` (line ~495).
    In `site/src/pages/index.astro`, ensure the parent of the two `.lead-table-col` blocks has `flex-wrap: wrap`, and add to that page's `<style>`: `@media (max-width: 900px) { .lead-table-col { width: 100%; } }` so the two 504px columns stack to a single column at ≤900px. Use only existing spacing tokens; do not introduce new breakpoint values other than 900px and 1100px (the only two allowed by UI-SPEC).
    Then commit ONLY these files: `git commit -m "fix(25): collapse nav to hamburger <=1100px + stack home lead tables <=900px + shorten Glossary label (P0-2)"`.
  </action>
  <verify>
    <automated>cd site && grep -c "min-width: 1100px" src/components/PageHeader.astro | grep -qx 4 && grep -q "'Glossary'" src/config/i18n.ts && grep -q "max-width: 900px" src/pages/index.astro && ! grep -q "min-width: 640px" src/components/PageHeader.astro && npm run build && echo P0-2-OK</automated>
  </verify>
  <acceptance_criteria>
    - `PageHeader.astro` contains exactly 4 occurrences of `min-width: 1100px` and ZERO occurrences of `min-width: 640px`.
    - `i18n.ts` glossary_link EN === `'Glossary'`, ES === `'Glosario'`.
    - `index.astro` `<style>` contains `@media (max-width: 900px)` targeting `.lead-table-col { width: 100% }` and the parent has `flex-wrap: wrap`.
    - Build succeeds (`npm run build`).
    - This change is its own commit.
    - Visual tablet-overflow confirmation is deferred to the final BrowserOS review (Plan 09) — orchestrator runs it inline.
  </acceptance_criteria>
  <done>Header collapses to hamburger below 1100px and home tables stack below 900px, eliminating the ~305px tablet horizontal overflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external crawler → static HTML | Search bots consume 404/head output; soft-404 leaks crawl budget |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-SEO | Information Disclosure | soft-404 catch-all | mitigate | Real dist/404.html emitted; no `_redirects` catch-all; no noindex on 404 |
| T-25-01-XSS | Tampering/XSS | 404.astro static copy | accept | 404 content is static literal copy, no user input rendered |
| T-25-SC | Tampering (supply chain) | npm installs | mitigate | No new npm/pip/cargo packages installed this plan; favicon.ico generated with already-present `sharp` + pure-Node ICO wrapper. Package Legitimacy Gate N/A (zero installs). |
</threat_model>

<verification>
Chained (OneDrive requirement): `cd site && npm run build && npm run validate`.
Confirm: `dist/404.html` exists with "Page not found"; `dist/favicon.{svg,ico}` + `dist/apple-touch-icon.png` exist; `dist/index.html` has ≥2 `rel="icon"` links; PageHeader has 4× `min-width: 1100px` and 0× `640px`; i18n glossary shortened; index.astro has the 900px stacking rule. Full validator suite exits 0.
</verification>

<success_criteria>
- Three independent atomic commits landed (404, favicon, breakpoints).
- `dist/404.html` present; favicon assets present + linked; nav collapses at 1100px; home tables stack at 900px; "Glossary" label shortened.
- `npm run build && npm run validate` exits 0.
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-01-SUMMARY.md` when done.
</output>
