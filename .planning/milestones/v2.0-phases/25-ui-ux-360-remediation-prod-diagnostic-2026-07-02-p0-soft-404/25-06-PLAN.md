---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 06
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - site/src/islands/ComparatorIsland.tsx
  - site/src/config/i18n.ts
autonomous: true
requirements: [P1-2, P1-1, P2-3]
must_haves:
  truths:
    - "The big compare number is labeled 'Composite Crime Index' with a '(what is this?)' link to methodology — the bare 'Very High' no longer floats unattributed"
    - "Selecting communes syncs ?a=&b= into the URL (history.replaceState) so comparisons are shareable"
    - "The empty state offers popular-comparison chips (Santiago vs Las Condes, Providencia vs Ñuñoa, Viña del Mar vs Valparaíso)"
    - "Compare rates use the shared formatNumber helper (thousands separators, 1 decimal incl. homicide); the misleading per-row '→' is not repeated on every family row"
    - "The Chile-average card shows an index value (not blank)"
  artifacts:
    - path: "site/src/islands/ComparatorIsland.tsx"
      provides: "Labeled index, URL sync, popular chips, consistent numbers"
      contains: "Composite Crime Index"
  key_links:
    - from: "ComparatorIsland.tsx"
      to: "?a=&b= URL"
      via: "history.replaceState on selection"
      pattern: "replaceState"
    - from: "ComparatorIsland.tsx"
      to: "site/src/lib/formatNumber.ts"
      via: "import formatDecimal"
      pattern: "formatDecimal"
---

<objective>
Fix P1-2 (compare island: unlabeled big number risks the editorial rule, selection not in URL, bare empty state, all trends show "→") and finish P1-1 (ComparatorIsland's number formatting) plus P2-3 (homicide decimals, plural) on this single file.

Purpose: The comparator shows "39.0 Very High" without naming the Composite Crime Index (an editorial-attribution risk), comparisons aren't shareable/bookmarkable, the empty state is bare, every family row shows the same "→" (misleading), rates lack thousands separators ("2871.5") and homicide uses 2 decimals. Fixing all of these on ComparatorIsland.tsx keeps single-file ownership.

Output: "Composite Crime Index" label + methodology link, ?a=&b= URL sync (replaceState + mount read), popular-comparison chips, formatDecimal wiring, per-row trend arrow removed (shown once in the header), Chile-avg index value, plural fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-RESEARCH.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-PATTERNS.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

<interfaces>
- formatDecimal(value, locale, decimals=1) from `site/src/lib/formatNumber.ts` (created in Plan 25-02) — import via `../lib/formatNumber`.
- ComparatorIsland.tsx anchors: score render lines 579-583; empty state lines 458-468; per-row trend `{TREND_ARROW[data.trend] ?? '→'}` line 630; rate `rate.toFixed(1)` line 620; homicide `homRate!.toFixed(2)` line 644; commune objects have `id` (CUT).
- URL sync resolution (RESEARCH open Q2): use CUT codes for `?a=&b=` (simpler round-trip); the pre-generated `/compare/x-vs-y/` slug pages are a separate SEO surface.
- Per-family trend fact (RESEARCH): the commune JSON has ONE overall `trend`; there is no per-family trend. Show it ONCE in the header card, remove the repeated per-row arrow.
</interfaces>

Editorial constraint: the band label (e.g. "Very High"/"Muy Alto") must appear ONLY in context of the "Composite Crime Index" label — never standalone.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composite Crime Index label + methodology link + Chile-avg value + trend cleanup + numbers/plural</name>
  <files>site/src/islands/ComparatorIsland.tsx, site/src/config/i18n.ts</files>
  <read_first>
    - site/src/islands/ComparatorIsland.tsx lines 458-468 (empty state), 579-583 (score+band), 620/630/644 (rate/trend/homicide), the `s.cmp_*` i18n string usage, the Chile-avg card render
    - site/src/config/i18n.ts (the `cmp_*` key block, EN + ES) — add new keys here
    - site/src/lib/formatNumber.ts (formatDecimal signature)
    - .planning/phases/25-.../25-UI-SPEC.md "### Compare Island — Composite Crime Index Label", Copywriting Contract rows (composite label, "(what is this?)", trend tooltip, empty-state copy) and "### Plural Fix"
  </read_first>
  <action>
    Add a label above the big score in ComparatorIsland.tsx (lines 579-583): "Composite Crime Index" (EN) / "Índice Compuesto de Criminalidad" (ES) at label size / `--muted`, followed by a methodology link `(what is this?)` → `/methodology/` (EN) / `(¿qué es esto?)` → `/es/metodologia/` (ES) — hardcode ES slug. Ensure the band chip ("Very High"/"Muy Alto") renders only inside this labeled context. Make the Chile-average card show its index value (or "—" if unavailable, never blank).
    Wire numbers to the shared helper: line 620 `rate.toFixed(1)` → `formatDecimal(rate, lang)`; line 644 `homRate!.toFixed(2)` → `formatDecimal(homRate!, lang)` (homicide now 1 decimal, consistent). Apply the plural fix for "(n cases)": EN `n===1?'case':'cases'`, ES `n===1?'caso':'casos'`.
    Trend cleanup: remove the per-family-row trend arrow (line 630) — since the JSON only has one overall commune `trend`, show a single trend indicator in the header card. Where trend is unavailable, use the tooltip copy "Trend data unavailable for this commune." / "Datos de tendencia no disponibles para esta comuna."
    Add the new `cmp_*` strings to `i18n.ts` (composite label, "(what is this?)", empty-state heading/body, chip pairs labels if needed, trend-unavailable) in both EN and ES blocks.
  </action>
  <verify>
    <automated>cd site && grep -q "Composite Crime Index" src/islands/ComparatorIsland.tsx && grep -q "formatDecimal" src/islands/ComparatorIsland.tsx && ! grep -q "toFixed(2)" src/islands/ComparatorIsland.tsx && npm run build && npm run validate && echo P1-2-A-OK</automated>
  </verify>
  <acceptance_criteria>
    - ComparatorIsland renders the "Composite Crime Index" label + methodology link near the score; band chip never appears without the label.
    - `formatDecimal` used for rate + homicide; no `toFixed(2)` remains.
    - Plural branch present (case/cases, caso/casos).
    - Per-family repeated trend arrow removed; a single header trend (or unavailable tooltip) shown.
    - Chile-avg card shows an index value or "—".
    - `npm run build && npm run validate` exits 0 (forbidden-language validator green).
  </acceptance_criteria>
  <done>The compare headline is attributed to the named index, numbers are consistent, and the misleading per-row arrows are gone.</done>
</task>

<task type="auto">
  <name>Task 2: URL sync (?a=&b=) + popular-comparison chips in empty state</name>
  <files>site/src/islands/ComparatorIsland.tsx</files>
  <read_first>
    - site/src/islands/ComparatorIsland.tsx (selection state `selected`, `selectCommune`, existing useEffect patterns, mount logic, empty state region lines 458-468)
    - .planning/phases/25-.../25-UI-SPEC.md "### Compare Island — URL Sync", "### Compare Island — Popular Comparison Chips" (.compare-suggestions/.compare-chip CSS + pairs)
  </read_first>
  <action>
    Add a `useEffect` watching `selected` that calls `window.history.replaceState(null, '', '?a=' + (selected[0]?.id ?? '') + '&b=' + (selected[1]?.id ?? ''))` using CUT codes. On mount, read `?a=` and `?b=` params, validate each with `/^\d{5}$/`, and pre-select the matching communes from the index (ignore invalid params — do not fetch on an unvalidated value). 
    In the empty state, below the two search inputs, add a `.compare-suggestions` row of `.compare-chip` buttons for: Santiago vs Las Condes, Providencia vs Ñuñoa, Viña del Mar vs Valparaíso — clicking a chip selects both communes. Add the `.compare-suggestions`/`.compare-chip` CSS from UI-SPEC (36px height chips, token-based colors, 44px min touch target respected). Use the empty-state heading/body copy from the Copywriting Contract.
  </action>
  <verify>
    <automated>cd site && grep -q "replaceState" src/islands/ComparatorIsland.tsx && grep -q "compare-chip" src/islands/ComparatorIsland.tsx && grep -Eq "\\\\d\\{5\\}" src/islands/ComparatorIsland.tsx && npm run build && npm run validate && echo P1-2-B-OK</automated>
  </verify>
  <acceptance_criteria>
    - Selecting communes updates the URL to `?a={cut}&b={cut}` via replaceState.
    - On mount, valid `?a=`/`?b=` (5-digit) pre-select; invalid params are ignored without a fetch.
    - Empty state shows `.compare-chip` buttons for the three named pairs; clicking selects both.
    - `.compare-suggestions`/`.compare-chip` use existing tokens only.
    - `npm run build && npm run validate` exits 0.
    - URL-sync + chips confirmed visually in Plan 09.
  </acceptance_criteria>
  <done>Comparisons are shareable via URL and the empty state guides users into popular comparisons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL ?a=&b= params → commune select/fetch | Untrusted params used to preselect communes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-URL | Tampering | ComparatorIsland ?a=&b= | mitigate | `/^\d{5}$/` validation on both params before preselect/fetch |
| T-25-06-EDIT | Information Disclosure (editorial) | bare band label | mitigate | Band chip only rendered with "Composite Crime Index" label + methodology link; forbidden-language validator in verify |
| T-25-SC | Tampering (supply chain) | npm installs | mitigate | No new packages; formatDecimal is existing helper |
</threat_model>

<verification>
`cd site && npm run build && npm run validate && npm run check`. Confirm: "Composite Crime Index" label + methodology link, replaceState URL sync with 5-digit guard, compare-chip empty state, formatDecimal (no toFixed(2)), plural fix; forbidden-language validator green.
</verification>

<success_criteria>
- Labeled index + methodology link; ?a=&b= URL sync; popular chips; consistent numbers; single trend indicator.
- Build+validate+check exit 0.
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-06-SUMMARY.md` when done.
</output>
