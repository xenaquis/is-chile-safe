---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 09
type: execute
wave: 5
depends_on: [25-01, 25-02, 25-03, 25-04, 25-05, 25-06, 25-07, 25-08]
files_modified: []
autonomous: false
requirements: [P0-1, P0-2, P0-3, P1-1, P1-2, P1-3, P1-4, P1-5, P1-6, P2-1, P2-2, P2-3]
must_haves:
  truths:
    - "Full build + validate + check + pytest are green on the integrated phase"
    - "A BrowserOS visual review (desktop + 375px mobile, EN + ES) confirms every P0/P1/P2 fix renders correctly on the local preview"
    - "The unknown-URL path serves the 404 (not a 200 homepage) in the local preview"
  artifacts: []
  key_links: []
---

<objective>
Final integrated verification of Phase 25. Run the full automated suite, then perform the BrowserOS visual E2E review INLINE (the orchestrator runs this — gsd-executor subagents lack BrowserOS tools). This plan modifies no source; it confirms the whole remediation renders correctly before `/gsd:verify-work`.

Purpose: The P0-2 tablet overflow, map framing/pins/legend, compare URL-sync/labels, editorial visuals, news headings, and a11y focus all require visual judgment that only a rendered preview can confirm. This is the phase's acceptance gate.

Output: a pass/fail record across all fixes; a checkpoint for the human to approve or list issues.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-VALIDATION.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

Verification env (MEMORY): no `npm run preview` script — use `npx astro preview --port 4321 --host`. OneDrive: chain build+validate in ONE command. BrowserOS lacks viewport resize — emulate mobile via a 375px iframe; save screenshots to a no-spaces path (`C:\Users\Carlo\bos-shots\`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated suite (build + validate + check + pytest)</name>
  <files>none</files>
  <read_first>
    - .planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-VALIDATION.md (Per-Task Verification Map — the build-grep assertions)
  </read_first>
  <action>
    Run the full suite as single chained commands (OneDrive requirement):
    `cd site && npm run build && npm run validate && npm run check` then `cd pipeline && python -m pytest -q`.
    Then assert the phase build-grep contract from 25-VALIDATION.md against `site/dist/`: `test -f dist/404.html`; `grep -c 'rel="icon"' dist/index.html` ≥ 2; PageHeader has 4× `min-width: 1100px`; `grep -c es-CL src/components/map/ResultPanel.tsx` = 0; MapIsland has fitBounds/maxBounds + 5-digit guard; `grep -c '<table' dist/is-santiago-safe/index.html` > 0 (and is-chile-safe, safest-cities); `grep -c '<h2' dist/news/index.html` > 0; `grep WebSite dist/index.html`; `grep -c '/region/' dist/rankings/index.html` ≥ 16; `grep skip-link dist/index.html`. Record each result.
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate && npm run check && cd ../pipeline && python -m pytest -q && echo SUITE-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run validate`, `npm run check` all exit 0.
    - `python -m pytest -q` (pipeline) exits 0 including the traffic-accident test.
    - Every build-grep assertion from 25-VALIDATION.md passes; failures are itemized for gap-closure.
  </acceptance_criteria>
  <done>Automated evidence that all 12 requirement fixes are present and the full suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BrowserOS inline visual E2E review (all P0/P1/P2)</name>
  <files>none</files>
  <read_first>
    - .planning/UI-360-DIAGNOSTIC-260702.md (the authoritative defect list to confirm each fix against)
    - .planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md (expected rendered result per fix)
  </read_first>
  <action>
    Orchestrator runs this INLINE (BrowserOS is unavailable to subagents). Start the preview with a single command `cd site && npm run build && npx astro preview --port 4321 --host`, then drive the BrowserOS checklist in how-to-verify, saving screenshots to `C:\Users\Carlo\bos-shots\`. Pause for the human resume signal.
  </action>
  <what-built>All Phase 25 P0/P1/P2 fixes are integrated and the automated suite is green. The orchestrator now drives a BrowserOS visual review on the local preview.</what-built>
  <how-to-verify>
    Running BrowserOS INLINE (not a subagent), review and screenshot:
    1. P0-1: navigate to `http://localhost:4321/404-test-nonexistent/` → the 404 page renders (astro preview returns 404 for unknown paths when dist/404.html exists), not a homepage clone.
    2. P0-3: browser tab shows the brand favicon.
    3. P0-2: at ~800px width (and a 375px iframe) the header is a hamburger with NO horizontal overflow; home lead tables stacked; nav reads "Glossary".
    4. P1-1/P1-4: open `/map/`, click a commune — EN panel shows "4,984" (comma), no "~", year badges (2024 composite / 2025 rate) + "?" tooltip; ES `/es/comuna/nunoa/` "Similar Communes" shows period-grouped numbers.
    5. P1-3: `/map/` opens centered on Chile (minimal ocean); incident pins are purple with white border + legend pin row; 375px iframe: legend collapsible, Index/By-crime pills on their own row.
    6. P1-5: `/is-santiago-safe/`, `/is-chile-safe/`, `/safest-cities-in-chile/` each show a rate table + sparkline.
    7. P1-6: `/news/` has date-group headings, h3 titles, type + named-comuna chips.
    8. P1-2: `/compare/` select Santiago vs Las Condes → URL shows `?a=13101&b=13109` (or the real CUTs); card shows "Composite Crime Index" + methodology link; empty state shows popular-comparison chips.
    9. P2-1: keyboard Tab from page top reveals the skip link; link focus shows a visible outline; incident markers expose an accessible name.
    10. P2-2/P2-3: `/rankings/` lists 16 region links; a region page shows "#n of 16 (1 = highest reported)", the 2005–2026* chart footnote, and the map-placeholder CTA card.
    Confirm no forbidden absolute safe/dangerous verdicts appear anywhere (all band labels attributed to the Composite Crime Index).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 25, or list the specific items that fail so gap-closure plans can be created.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification-only | No source changes; validates integrated output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09-VER | Repudiation | unverified visual regressions | mitigate | Explicit BrowserOS checklist across all P0/P1/P2 fixes + forbidden-language re-check |
| T-25-SC | Tampering (supply chain) | npm installs | mitigate | No new packages; verification only |
</threat_model>

<verification>
Automated: `cd site && npm run build && npm run validate && npm run check`; `cd pipeline && python -m pytest -q`. Manual/inline: BrowserOS checklist above on `npx astro preview`.
</verification>

<success_criteria>
- Full automated suite green (frontend validators + pipeline pytest).
- BrowserOS visual review passes all 10 checklist groups (EN + ES, desktop + 375px).
- Human approves; any failures itemized for gap-closure.
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-09-SUMMARY.md` when done.
</output>
