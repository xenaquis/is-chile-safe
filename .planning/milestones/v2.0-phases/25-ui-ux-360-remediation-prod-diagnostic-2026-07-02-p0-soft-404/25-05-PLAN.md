---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 05
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - site/src/pages/is-santiago-safe.astro
  - site/src/pages/is-chile-safe.astro
  - site/src/pages/safest-cities-in-chile.astro
autonomous: true
requirements: [P1-5]
must_haves:
  truths:
    - "/is-santiago-safe/ shows a static top/bottom commune rate table and a sparkline (was 916 words, zero visuals)"
    - "/is-chile-safe/ and /safest-cities-in-chile/ each gain a static rate table + sparkline"
    - "All visuals are pre-rendered HTML in dist (generated at build time from public/data JSON — nothing client-only)"
    - "Rate tables attribute CEAD and never issue an absolute safe/dangerous verdict"
  artifacts:
    - path: "site/src/pages/is-santiago-safe.astro"
      provides: "Gran Santiago top/bottom-10 rate table + Santiago sparkline"
      contains: "rate-table"
  key_links:
    - from: "is-santiago-safe.astro"
      to: "loadIndex() filtered to region_id 13"
      via: "build-time table rows"
      pattern: "loadIndex"
    - from: "editorial pages"
      to: "CommuneRankingTable.astro + Sparkline.astro"
      via: "static component import"
      pattern: "Sparkline"
---

<objective>
Fix P1-5: the highest-volume SEO pages — `/is-santiago-safe/`, `/is-chile-safe/`, `/safest-cities-in-chile/` — currently have zero visual elements (916 words of text + 2 KPIs on the Santiago page). Competitors show comparative tables.

Purpose: Add build-time static visuals (top/bottom commune rate tables + trend sparklines) generated from existing `public/data` JSON, so they land in the pre-rendered HTML (SEO-indexable, nothing client-rendered).

Output: rate tables + sparklines added to the three editorial pages, sourced from `loadIndex()`/`loadCommune()` at build time, using existing `CommuneRankingTable.astro` + `Sparkline.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-RESEARCH.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-PATTERNS.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

<interfaces>
- `CommuneRankingTable.astro` Props: `{ rows: RankingRow[]; locale: 'en'|'es'; sortable?; defaultSortDir? }` — you MUST map loadIndex() commune rows into the RankingRow shape it expects (read the component to get RankingRow's fields; do not pass raw commune objects).
- `Sparkline.astro` takes a numeric `series`/`values` array and renders inline SVG, zero JS (used on commune pages). Editorial dimensions: 180×40, stroke `--primary`.
- `loadIndex()` returns commune index rows with `region_id`, `slug`, `featured_rates`, `low_population`, `national_rank`. `loadCommune(cut)` returns full series. Import from `../lib/data.ts`.
- Constraint: rates are already per-100k — never rescale. `FAMILY_SLUGS.vida.en === 'homicide'` — filter `vida` in any byFamily loop.
</interfaces>

Editorial constraint: tables/captions must attribute CEAD ("Reported cases per 100,000 inhabitants") and never label a commune safe/dangerous in absolute terms. Only the three EN pages are in scope (their ES counterparts are separate surfaces; do not change hreflang/esPath).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static rate table + sparkline on is-santiago-safe.astro</name>
  <files>site/src/pages/is-santiago-safe.astro</files>
  <read_first>
    - site/src/pages/is-santiago-safe.astro (full — data load lines 16-20, EditorialLayout usage, FAQ/DataCallout placement, `<style>` block)
    - site/src/components/CommuneRankingTable.astro (Props interface + RankingRow field names)
    - site/src/components/Sparkline.astro (prop name + expected series shape)
    - site/src/lib/data.ts (loadIndex, loadCommune, latestCompleteYearRate signatures)
    - .planning/phases/25-.../25-UI-SPEC.md "### Editorial Visuals — Static Tables (P1-5)" (exact table structure, `.rate-table-figure` CSS, source-attribution copy) and "### P1-5" in RESEARCH.md
  </read_first>
  <action>
    In `site/src/pages/is-santiago-safe.astro` frontmatter, import `loadIndex` and `CommuneRankingTable`/`Sparkline`. Build `rmCommunes = loadIndex().filter(c => c.region_id === '13' && !c.low_population)` sorted descending by the year's total rate; take `top10` and `bottom10` (mapped into the RankingRow shape the table expects). After the intro `<p>` (before the FAQ block), render a `CommuneRankingTable` for the Gran Santiago top/bottom-10 with a caption "Highest reported crime rates in Región Metropolitana (CEAD {year})" and a source line "Source: CEAD – Ministry of Interior, {year}. Reported cases per 100,000 inhabitants." Render a `Sparkline` of Santiago's per-year rate series (180×40, stroke `--primary`). Copy the `.rate-table-figure` CSS block from UI-SPEC into the page `<style>`. Everything computed in frontmatter → static HTML. Never rescale rates. Do not add an absolute safe/dangerous verdict.
  </action>
  <verify>
    <automated>cd site && npm run build && [ "$(grep -c '<table' dist/is-santiago-safe/index.html)" -gt 0 ] && grep -q "CEAD" dist/is-santiago-safe/index.html && grep -Eq "svg|sparkline" dist/is-santiago-safe/index.html && echo P1-5-STGO-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<table" dist/is-santiago-safe/index.html` > 0.
    - A sparkline SVG is present in the rendered HTML.
    - Table caption/source attributes CEAD with "per 100,000 inhabitants".
    - No client-only rendering: the table/sparkline exist in the static HTML output.
    - `npm run build && npm run validate` exits 0 (forbidden-language validator green).
  </acceptance_criteria>
  <done>The Santiago SEO page has a static comparative rate table + trend sparkline.</done>
</task>

<task type="auto">
  <name>Task 2: Static rate table + sparkline on is-chile-safe.astro and safest-cities-in-chile.astro</name>
  <files>site/src/pages/is-chile-safe.astro, site/src/pages/safest-cities-in-chile.astro</files>
  <read_first>
    - site/src/pages/is-chile-safe.astro (current data hooks + layout + `<style>`)
    - site/src/pages/safest-cities-in-chile.astro (current data hooks + layout + `<style>`)
    - site/src/components/CommuneRankingTable.astro (RankingRow shape) and Sparkline.astro
    - The just-completed is-santiago-safe.astro (Task 1) as the in-repo pattern
  </read_first>
  <action>
    Apply the same static-visual pattern to both pages. `is-chile-safe.astro`: a national top/bottom-10 commune rate table (from `loadIndex()` across all regions, excluding low_population, filter `vida` where relevant) + a national-trend sparkline. `safest-cities-in-chile.astro`: a table of the lowest-reported-rate communes (bottom-of-ranking, the page's theme) with the same CEAD attribution + a sparkline. Reuse `CommuneRankingTable` + `Sparkline` and the `.rate-table-figure` CSS. All build-time static. Never rescale rates; keep captions attributed to CEAD; no absolute verdicts. Where the "safest" framing appears, ensure existing copy remains attributed to "reported" incidence (do not introduce an unqualified "safest").
  </action>
  <verify>
    <automated>cd site && npm run build && [ "$(grep -c '<table' dist/is-chile-safe/index.html)" -gt 0 ] && [ "$(grep -c '<table' dist/safest-cities-in-chile/index.html)" -gt 0 ] && npm run validate && echo P1-5-REST-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<table" dist/is-chile-safe/index.html` > 0 AND `grep -c "<table" dist/safest-cities-in-chile/index.html` > 0.
    - Each page renders a sparkline SVG in static HTML.
    - CEAD attribution present on each table.
    - `npm run build && npm run validate` exits 0 (forbidden-language validator green — no unqualified "safest").
  </acceptance_criteria>
  <done>All three priority editorial pages now carry static comparative visuals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time JSON → static table | Trusted local data; no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-EDIT | Information Disclosure (editorial) | editorial table framing | mitigate | Captions attribute CEAD + "reported"; no absolute safe/dangerous verdict; forbidden-language validator in verify |
| T-25-05-DATA | Tampering | rate values | accept | Values rendered as-is (already per-100k); never rescaled |
| T-25-SC | Tampering (supply chain) | npm installs | mitigate | No new packages; reuses existing Astro components |
</threat_model>

<verification>
`cd site && npm run build && npm run validate`. Confirm each of the three dist pages has `<table>` + sparkline SVG + CEAD attribution; forbidden-language validator green.
</verification>

<success_criteria>
- All three editorial pages render static rate tables + sparklines in dist HTML.
- CEAD-attributed, per-100k, no absolute verdicts.
- Build+validate exit 0.
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-05-SUMMARY.md` when done.
</output>
