---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 03
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - site/src/pages/news.astro
  - pipeline/news/classifier.py
  - pipeline/tests/test_classifier_traffic.py
autonomous: true
requirements: [P1-6]
must_haves:
  truths:
    - "News card titles are real headings (h2 date groups, h3 per article); the page is no longer 0-heading"
    - "Each news card shows a crime-type chip and a comuna chip naming the actual commune (not a bare 'View commune')"
    - "Incidents are grouped by month at build time (static HTML, no client JS)"
    - "The classifier rejects traffic accidents / road collisions as non-crime incidents"
  artifacts:
    - path: "site/src/pages/news.astro"
      provides: "Semantic, chipped, date-grouped news list"
      contains: "news-title"
    - path: "pipeline/tests/test_classifier_traffic.py"
      provides: "Traffic-accident rejection regression test"
      contains: "test_traffic_accident_rejected"
  key_links:
    - from: "site/src/pages/news.astro"
      to: "commune name lookup"
      via: "loadIndex() cut→name dict"
      pattern: "news-commune-chip"
    - from: "pipeline/news/classifier.py SYSTEM_PROMPT"
      to: "traffic-accident exclusion rule"
      via: "confidence 0.0 for traffic accidents"
      pattern: "[Tt]raffic|colisión|accidente de tránsito"
---

<objective>
Fix P1-6: the /news/ page renders 129 items in a flat `<div>` list with ZERO headings (poor semantics/SEO/a11y), no crime-type or comuna chips, and no date grouping; and the Python classifier publishes traffic accidents (e.g. "Collision leaves at least one dead… in Carahue") as crime incidents.

Purpose: Make the news surface semantically correct and scannable (build-time date grouping + heading structure + chips), and stop non-crime traffic accidents from entering the incident feed at the pipeline level.

Output: `news.astro` restructured with `<h2>` month groups / `<h3>` article titles + type/comuna chips; `classifier.py` SYSTEM_PROMPT hardened; new `test_classifier_traffic.py` regression test (Wave 0 gap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-RESEARCH.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-PATTERNS.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

Data source: `site/public/data/incidents/current.json` read at build time via `path.resolve(process.cwd(), 'public/data/incidents/current.json')` (per news.astro line 19 — DO NOT switch to import.meta.url; see MEMORY "News page build path drift"). Each incident has `title_es`, `title_en`, `date`, `outlet`, `url`, optional `slug`, `cut`, `family`.
The classifier rejection gate already exists: `confidence < CONFIDENCE_THRESHOLD` in classifier.py (~line 164). The traffic fix makes the LLM emit `confidence: 0.0` for traffic accidents.
</context>

<tasks>

<task type="auto">
  <name>Task 1: News page — semantic headings, chips, build-time date grouping (P1-6 frontend)</name>
  <files>site/src/pages/news.astro</files>
  <read_first>
    - site/src/pages/news.astro (full file — data load lines 19-58, the incident type destructure at line ~22, the current `<div class="incident-title">` at line ~115, the "View commune" link, existing `safeUrl`/escaping helpers)
    - site/src/lib/data.ts (loadIndex to build a cut→commune-name lookup at build time)
    - .planning/phases/25-.../25-UI-SPEC.md "### News Page — Semantic Headings (P1-6)" (exact chip CSS, heading structure, "View {CommuneName}" copy, news h1 EN/ES)
  </read_first>
  <action>
    In `site/src/pages/news.astro`: add `family?: string` to the destructured incident type. Build a `cut → commune name` lookup from `loadIndex()` in frontmatter. Group the sorted incidents by `date.slice(0,7)` (YYYY-MM) into a Map at build time (zero JS). Render each month group as `<section><h2 class="news-date-group">{Month YYYY}</h2> … </section>`, and each article as `<article class="news-card"><h3 class="news-title"><a href={safeUrl(url)}>{locale title}</a></h3><p class="news-meta">…</p></article>`. In `.news-meta` render `<span class="news-type-chip">{family}</span>` when family present and `<span class="news-commune-chip"><a href="/{locale-prefixed}/commune|comuna/{slug}/">{commune name}</a></span>` when slug/cut present — the chip must NAME the commune (replace any bare "View commune" with "View {CommuneName}" / "Ver {NombreComuna}"). Ensure the page has an `<h1>` "Recent Crime Incidents in Chile" (ES page: "Incidentes delictivos recientes en Chile"). Add the `.news-type-chip`/`.news-commune-chip` CSS from UI-SPEC (12px font is a named compact-badge exception) to the page `<style>`. All chip/title text goes through Astro's default `{expr}` HTML-escaping — never `set:html` on incident titles (they originate from external RSS). Hardcode ES commune slugs (`/es/comuna/...`) — getRelativeLocaleUrl does not translate them.
  </action>
  <verify>
    <automated>cd site && npm run build && [ "$(grep -c '<h2' dist/news/index.html)" -gt 0 ] && grep -q "news-title" dist/news/index.html && grep -q "news-commune-chip" dist/news/index.html && echo P1-6-FRONT-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<h2" dist/news/index.html` > 0 AND `<h3 class="news-title"` present.
    - `dist/news/index.html` contains `news-type-chip` and `news-commune-chip`.
    - At least one comuna chip renders an actual commune name (not the literal string "View commune").
    - Incident titles are rendered via `{expr}` (auto-escaped), not `set:html`.
    - `npm run build && npm run validate` exits 0.
  </acceptance_criteria>
  <done>The news page has a proper heading outline, per-card type + comuna chips, and month-grouped sections — all static HTML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classifier traffic-accident exclusion + regression test (P1-6 pipeline)</name>
  <files>pipeline/news/classifier.py, pipeline/tests/test_classifier_traffic.py</files>
  <read_first>
    - pipeline/news/classifier.py lines 105-110 (SYSTEM_PROMPT Rules block) and ~line 164 (CONFIDENCE_THRESHOLD rejection gate)
    - pipeline/tests/test_classifier.py lines 1-50 (module structure, _make_mock_response factory, patch("pipeline.news.classifier.client") pattern)
    - .planning/phases/25-.../25-PATTERNS.md "### pipeline/tests/test_classifier_traffic.py" and "### pipeline/news/classifier.py"
  </read_first>
  <behavior>
    - When the mocked LLM returns confidence 0.0 for a traffic-collision headline, classify(...) returns None (rejected by the existing threshold gate).
    - The SYSTEM_PROMPT contains an explicit rule that traffic accidents / road collisions / vehicle crashes are NOT crime incidents even if fatal.
  </behavior>
  <action>
    In `pipeline/news/classifier.py`, add to the SYSTEM_PROMPT Rules block (after the "not about a crime incident" rule, ~line 108): a rule stating that traffic accidents, road collisions, and vehicle crashes are NOT crime incidents even if they result in fatalities, and that for articles primarily about a traffic accident (colisión, accidente de tránsito, choque, atropello sin culpa) the model MUST set confidence to 0.0. Keep closed-list classification at temperature 0.0. Use only `deepseek-v4-flash`/`deepseek-v4-pro` model IDs (never `deepseek-chat`).
    Create `pipeline/tests/test_classifier_traffic.py` following test_classifier.py's structure: `test_traffic_accident_rejected` (mock returns a `vida`-classified collision with confidence 0.0 → assert `classify(...)` is None) and `test_zero_confidence_traffic_rejected`. Mock DeepSeek via `patch("pipeline.news.classifier.client")` — never call the API live. Optionally also assert the SYSTEM_PROMPT string contains the traffic-exclusion phrase.
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest -q -k traffic && echo P1-6-PIPE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/news/classifier.py` SYSTEM_PROMPT contains the traffic-accident exclusion rule (grep matches "traffic"/"tránsito"/"colisión").
    - `pipeline/tests/test_classifier_traffic.py` exists with `test_traffic_accident_rejected`.
    - `python -m pytest -q -k traffic` passes; no live DeepSeek call (client is patched).
    - Model IDs remain `deepseek-v4-flash`/`deepseek-v4-pro`.
  </acceptance_criteria>
  <done>Fatal traffic collisions no longer enter the incident feed; a regression test guards it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external RSS → incident title → HTML | Untrusted news titles rendered as headings/chips/links |
| external RSS → DeepSeek classifier | Untrusted article text classified into closed family list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-XSS | Injection (XSS) | news.astro incident titles | mitigate | Render titles via Astro `{expr}` auto-escaping; never `set:html`; commune slug used only in href built from validated slug |
| T-25-03-CLS | Tampering (data quality) | classifier miscategorization | mitigate | SYSTEM_PROMPT traffic-exclusion rule + confidence-threshold gate + regression test |
| T-25-SC | Tampering (supply chain) | pip/npm installs | mitigate | No new packages; test uses existing pytest + unittest.mock |
</threat_model>

<verification>
`cd site && npm run build && npm run validate` (news headings/chips in dist) then `cd pipeline && python -m pytest -q -k traffic`. Both green.
</verification>

<success_criteria>
- news/index.html has ≥1 `<h2>`, `<h3 class="news-title">`, type + named-comuna chips, month sections.
- Classifier rejects traffic accidents; regression test passes.
- Incident titles auto-escaped (no set:html).
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-03-SUMMARY.md` when done.
</output>
