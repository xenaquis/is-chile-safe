---
phase: 25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404
plan: 08
type: execute
wave: 4
depends_on: [25-01, 25-06]
files_modified:
  - site/src/lib/jsonld.ts
  - site/src/pages/index.astro
  - site/src/pages/rankings.astro
  - site/src/pages/region/[slug].astro
  - site/src/config/i18n.ts
autonomous: true
requirements: [P2-2, P2-3]
must_haves:
  truths:
    - "The home page emits WebSite (+SearchAction) and Organization JSON-LD"
    - "The /rankings/ 'By region' section links all 16 region pages (not 'use the map')"
    - "The region KPI '#13 of 16' carries a direction note '(1 = highest reported)'"
    - "The region evolution chart title reflects the partial 2026 bar (2005–2026*) with a footnote"
    - "The region map placeholder reads as an intentional CTA card, not a failed map load"
  artifacts:
    - path: "site/src/lib/jsonld.ts"
      provides: "buildWebSite + buildOrganization"
      exports: ["buildWebSite", "buildOrganization"]
    - path: "site/src/pages/rankings.astro"
      provides: "16 region links"
      contains: "/region/"
  key_links:
    - from: "site/src/pages/index.astro"
      to: "buildWebSite/buildOrganization"
      via: "jsonLd prop to BaseLayout"
      pattern: "buildWebSite"
    - from: "site/src/pages/rankings.astro"
      to: "16 region pages"
      via: "region-links-list"
      pattern: "region-links-list"
---

<objective>
Deliver P2-2 (missing home JSON-LD WebSite+Organization; /rankings/ "By region" links no regions) and P2-3 remaining copy polish (region KPI direction label, region evolution chart title/footnote for the partial 2026 bar, and the ambiguous gray map placeholder styled as an intentional CTA card).

Purpose: Close the technical-SEO gaps (structured data + regional interlinking/anchor text) and the region-page copy ambiguities flagged in the audit. Excludes the home-title/cannibalization re-targeting (deferred per CONTEXT).

Output: `buildWebSite`/`buildOrganization` in jsonld.ts injected on home; 16 region links in rankings; region KPI direction; chart title "(2005–2026*)" + footnote; map-placeholder CTA card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/UI-360-DIAGNOSTIC-260702.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-RESEARCH.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-PATTERNS.md
@.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-UI-SPEC.md

<interfaces>
- jsonld.ts already exports `buildBreadcrumb`/`buildItemList` and has a `BASE_URL` const — follow that shape (return Record<string, unknown>).
- BaseLayout injects `jsonLd` (object | object[]) via the existing loop (lines 107-112) — pass `jsonLd={[buildWebSite(), buildOrganization()]}` from index.astro; escapes `</` already.
- region/[slug].astro: getStaticPaths iterates region files 1..16 with `region.slug`; there is a loop (~line 96) iterating regions 1..16 with slug — reuse that region list for the rankings 16-link list too (or `data/cead/regions/`). KPI "#13 of 16" at ~line 197; evolution title at ~line 207 (`latestCompleteYear`).
- Pitfall: `FAMILY_SLUGS.vida.en === 'homicide'` — the existing rankings byFamily loop at line ~25 already filters; the new region-links section is separate, no vida involvement.
- Hardcode ES region slugs for `/es/region/...` links.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home JSON-LD (WebSite + Organization) + /rankings/ 16 region links (P2-2)</name>
  <files>site/src/lib/jsonld.ts, site/src/pages/index.astro, site/src/pages/rankings.astro</files>
  <read_first>
    - site/src/lib/jsonld.ts (buildBreadcrumb pattern, BASE_URL const)
    - site/src/pages/index.astro (frontmatter jsonLd prop passed to BaseLayout; existing lead-table area)
    - site/src/pages/rankings.astro lines 78-83 ("By region" prose section) + the region list source (region JSON / REGION_META)
    - .planning/phases/25-.../25-UI-SPEC.md "### JSON-LD Additions" + "### Rankings Page — Region Links", "### buildWebSite + buildOrganization" in PATTERNS.md
  </read_first>
  <action>
    In `site/src/lib/jsonld.ts` add `buildWebSite()` (returns `@type WebSite`, name "Chile Safety Map", url BASE_URL, `potentialAction` SearchAction with target `${BASE_URL}/map/?search={search_term_string}` + `query-input: required name=search_term_string`) and `buildOrganization()` (`@type Organization`, name "Chile Safety Map", url BASE_URL, description "Interactive crime data map of Chile using official CEAD statistics."). Follow the existing builder shape.
    In `site/src/pages/index.astro` frontmatter, import both and set `const jsonLd = [buildWebSite(), buildOrganization()]`; pass `jsonLd={jsonLd}` to BaseLayout (append to any existing jsonLd array — do not drop existing home structured data). Never use a raw `<script>` tag (Astro `{expr}` interpolation pitfall) — rely on BaseLayout's injection loop.
    In `site/src/pages/rankings.astro`, replace the "By region" prose ("use the map") with `<ul class="region-links-list">` of all 16 `<a href="/{locale}region/{slug}/">{region name}</a>` links (EN `/region/...`, ES `/es/region/...` hardcoded), sourced from the same region list region/[slug].astro uses. Add `.region-links-list { list-style:none; padding:0; columns:2; }` to the page `<style>`.
  </action>
  <verify>
    <automated>cd site && npm run build && grep -q "WebSite" dist/index.html && grep -q "Organization" dist/index.html && [ "$(grep -c '/region/' dist/rankings/index.html)" -ge 16 ] && echo P2-2-OK</automated>
  </verify>
  <acceptance_criteria>
    - `dist/index.html` contains `"@type":"WebSite"` with a SearchAction and `"@type":"Organization"`.
    - Existing home JSON-LD (if any) is preserved.
    - `grep -c "/region/" dist/rankings/index.html` ≥ 16 (all 16 regions linked).
    - `npm run build && npm run validate` exits 0.
  </acceptance_criteria>
  <done>Home carries WebSite+Organization structured data; rankings interlinks all 16 regions with regional anchor text.</done>
</task>

<task type="auto">
  <name>Task 2: Region page copy polish — KPI direction, chart title/footnote, placeholder CTA (P2-3)</name>
  <files>site/src/pages/region/[slug].astro, site/src/config/i18n.ts</files>
  <read_first>
    - site/src/pages/region/[slug].astro line ~197 (KPI "#{nationalPosition} of 16"), ~207 (evolution `<h2>(2005–{latestCompleteYear})`), the gray map-placeholder slot markup
    - site/src/config/i18n.ts (region-related keys, EN + ES)
    - .planning/phases/25-.../25-UI-SPEC.md "### Region KPI Directional Label", "### Region Evolution Chart Title", "### Placeholder Map Slot", Copywriting Contract (direction, footnote, placeholder CTA copy)
    - MEMORY: national_rank direction — #1 = MOST reported crime (descending); direction label must be attributed
  </read_first>
  <action>
    KPI direction: next to the region "#{n} of 16" KPI add the sub-label "(1 = highest reported)" (EN) / "(1 = mayor incidencia reportada)" (ES) at label size / `--muted`. This matches the commune-page directional pattern (rank #1 = most reported crime).
    Chart title: change the evolution title from "(2005–{latestCompleteYear})" to "(2005–2026*)" (reflecting the partial 2026 bar the chart renders) and add a footnote below the chart: "* 2026: partial year data (Jan–Jun)" (EN) / "* 2026: año parcial (ene–jun)" (ES). Alternatively, if simpler and correct, render the title from the actual last year present in the series with the asterisk — but it MUST match the bars shown.
    Placeholder card: replace the ambiguous gray box + pin with `<a href="/map/?region={slug}" class="map-placeholder-card">` labeled "View interactive map →" / "Ver mapa interactivo →" (hardcode ES slug `/es/mapa/?region=...`). Add `.map-placeholder-card` CSS from UI-SPEC (dashed border, hover → primary).
    Add any new copy strings to i18n.ts (EN + ES) as needed.
  </action>
  <verify>
    <automated>cd site && npm run build && npm run validate && grep -rEq "1 = highest reported|mayor incidencia reportada" dist/region/ && echo P2-3-OK</automated>
  </verify>
  <acceptance_criteria>
    - A region page in dist shows the KPI direction note "(1 = highest reported)" / ES equivalent.
    - The evolution chart title reflects the partial 2026 bar (2005–2026*) with a footnote.
    - The map placeholder renders as an `.map-placeholder-card` link to `/map/?region=...` (ES hardcoded), not a bare gray box.
    - `npm run build && npm run validate` exits 0 (forbidden-language validator green — direction label is attributed, no absolute verdict).
  </acceptance_criteria>
  <done>Region pages disambiguate rank direction and the partial-year chart, and the placeholder reads as an intentional CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time static → JSON-LD/head | Trusted static data serialized into structured data |
| region ?region= link → map | Slug used to build a map deep-link href |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08-LD | Injection | JSON-LD serialization | mitigate | Static literal strings; BaseLayout injection loop escapes `</`; no user input |
| T-25-08-EDIT | Information Disclosure (editorial) | region rank direction | mitigate | Directional label attributed ("highest reported"); forbidden-language validator in verify |
| T-25-SC | Tampering (supply chain) | npm installs | mitigate | No new packages |
</threat_model>

<verification>
`cd site && npm run build && npm run validate`. Confirm WebSite+Organization in dist/index.html; ≥16 region links in rankings; region KPI direction + chart footnote + placeholder CTA in dist/region/. Forbidden-language validator green.
</verification>

<success_criteria>
- Home WebSite+Organization JSON-LD; 16 region links; region KPI direction; chart title/footnote; placeholder CTA card.
- Build+validate exit 0.
</success_criteria>

<output>
Create `.planning/phases/25-ui-ux-360-remediation-prod-diagnostic-2026-07-02-p0-soft-404/25-08-SUMMARY.md` when done.
</output>
