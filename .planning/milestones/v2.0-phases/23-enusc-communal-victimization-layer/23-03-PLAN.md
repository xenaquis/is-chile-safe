---
phase: 23-enusc-communal-victimization-layer
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - site/src/lib/data.ts
  - site/src/config/i18n.ts
  - site/src/pages/commune/[slug].astro
  - site/src/pages/es/comuna/[slug].astro
  - site/scripts/validate/commune.mjs
autonomous: true
requirements: [VL-03, VL-04]
must_haves:
  truths:
    - "A covered comuna page (EN and ES) shows the VHDV figure as a household percentage with reference year 2024 and a bilingual experimental/SAE caveat"
    - "An uncovered comuna page (EN and ES) shows an explicit 'no ENUSC estimate (coverage: 136 comunas)' note with no broken layout and no implied value"
    - "The VHDV value renders as a percentage of households (rate * 100, one decimal, %), never per-100k and never the raw proportion"
    - "The forbidden-language validator exits 0 over the built pages"
    - "commune.mjs asserts every commune page has either enusc-vhdv-section or enusc-no-estimate"
  artifacts:
    - path: "site/src/lib/data.ts"
      provides: "enusc_vhdv optional field on CommuneData"
      contains: "enusc_vhdv"
    - path: "site/src/config/i18n.ts"
      provides: "6 new ENUSC i18n keys in I18nStrings + EN_STRINGS + ES_STRINGS"
      contains: "enusc_section_heading"
    - path: "site/src/pages/commune/[slug].astro"
      provides: "section 6c EN VHDV module + no-estimate fallback"
      contains: "enusc-vhdv-section"
    - path: "site/src/pages/es/comuna/[slug].astro"
      provides: "section 6c ES VHDV module + no-estimate fallback"
      contains: "enusc-vhdv-section"
    - path: "site/scripts/validate/commune.mjs"
      provides: "VL-03/VL-04 presence assertion over dist/ commune pages"
      contains: "enusc-no-estimate"
  key_links:
    - from: "site/src/pages/commune/[slug].astro"
      to: "data.enusc_vhdv"
      via: "IIFE null-guard rendering section 6c"
      pattern: "data.enusc_vhdv"
    - from: "site/src/pages/commune/[slug].astro"
      to: "site/src/config/i18n.ts"
      via: "t.enusc_* keys"
      pattern: "t\\.enusc_"
---

<objective>
Render the ENUSC VHDV victimization module on the 136 covered commune pages (EN + ES) and an explicit bilingual "no estimate" note on the 210 uncovered pages, with the mandatory experimental/SAE caveat. Add the TypeScript optional field, the six bilingual i18n strings, and extend the commune validator to assert the module's presence.

Purpose: VL-03 (covered pages show value + year + bilingual SAE/experimental caveat; forbidden-language exits 0) and VL-04 (uncovered pages degrade gracefully).
Output: data.ts type, i18n.ts strings, section 6c in both commune templates, commune.mjs assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-enusc-communal-victimization-layer/23-RESEARCH.md
@.planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md
@.planning/phases/23-enusc-communal-victimization-layer/23-02-SUMMARY.md

<interfaces>
<!-- enusc_vhdv field on each covered commune JSON (written by plan 23-02):
     { vhdv_rate: number(0-1), ci_lower, ci_upper, tipo_estimacion: string, cv: number, vintage: 2024 } -->

<!-- data.ts composite_index additive pattern (lines ~66-68):
       composite_index?: Record<string, CompositeIndexEntry>;
     Add enusc_vhdv? immediately after, per PATTERNS.md data.ts block. loadCommune() needs NO change. -->

<!-- i18n.ts Phase 18-04 CI keys block (lines ~151-159) is the registration analog.
     6 new keys: enusc_section_heading, enusc_section_aria, enusc_ci_label,
       enusc_caveat_text, enusc_no_estimate_text, enusc_no_estimate_aria.
     EN/ES draft values are in PATTERNS.md (copy verbatim). -->

<!-- Astro section 6b (composite index IIFE null-guard, EN lines ~219-241, ES ~225) is the exact analog.
     Insert section 6c immediately after 6b in BOTH templates.
     Display rule: (vhdv.vhdv_rate * 100).toFixed(1) + "%". NEVER per-100k, never raw proportion. -->

<!-- commune.mjs default mode reads dist/ HTML and asserts structural requirements (lines 1-60+). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enusc_vhdv type + 6 bilingual i18n keys</name>
  <read_first>
    - site/src/lib/data.ts (CommuneData interface; composite_index? additive field lines ~66-68)
    - site/src/config/i18n.ts (I18nStrings interface + EN_STRINGS + ES_STRINGS; Phase 18-04 CI keys block ~151-159)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (sections "site/src/lib/data.ts" and "site/src/config/i18n.ts" — exact key list + EN/ES values)
  </read_first>
  <action>In site/src/lib/data.ts add an optional enusc_vhdv field to CommuneData immediately after composite_index?, typed { vhdv_rate: number; ci_lower: number; ci_upper: number; tipo_estimacion: string; cv: number; vintage: number } with the "Phase 23 additive field" comment; do not change loadCommune(). In site/src/config/i18n.ts add six keys to the I18nStrings interface under a "// Phase 23 — ENUSC SAE VHDV" header: enusc_section_heading, enusc_section_aria, enusc_ci_label, enusc_caveat_text, enusc_no_estimate_text, enusc_no_estimate_aria; add the EN values to EN_STRINGS and the ES values to ES_STRINGS copied verbatim from PATTERNS.md. The caveat strings must label the figure INE experimental / SAE-modeled, say "proportion of households" / "proporción de hogares", state coverage 136 of 346, and distinguish it from CEAD reported-crime — and must contain NO forbidden absolute-verdict term (no safe/dangerous/seguro/peligroso/safest/más segura/más peligrosa).</action>
  <verify>
    <automated>cd site && npx astro check 2>&1 | grep -E "enusc_vhdv|enusc_section|0 errors|error" | head</automated>
  </verify>
  <done>data.ts has the enusc_vhdv optional field; i18n.ts has all six keys present in I18nStrings, EN_STRINGS, and ES_STRINGS; astro check reports no new type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add section 6c (VHDV module + no-estimate fallback) to EN + ES commune templates</name>
  <read_first>
    - site/src/pages/commune/[slug].astro (section 6b composite-index IIFE, lines ~219-241)
    - site/src/pages/es/comuna/[slug].astro (section 6b ES analog, ~line 225)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section "[slug].astro ... section 6c to insert" — exact markup)
  </read_first>
  <action>Insert section 6c immediately after section 6b in BOTH site/src/pages/commune/[slug].astro and site/src/pages/es/comuna/[slug].astro, copy-adapting the section-6b IIFE null-guard. The guard reads const vhdv = data.enusc_vhdv. If falsy (VL-04): render a non-empty section.enusc-no-estimate containing a paragraph with t.enusc_no_estimate_text and aria-label t.enusc_no_estimate_aria — never an empty section (Pitfall 6). If present (VL-03): render section.panel-section.enusc-vhdv-section with h2 t.enusc_section_heading, the rate as (vhdv.vhdv_rate * 100).toFixed(1) + "%" followed by "(2024)", a CI line using t.enusc_ci_label with ci_lower/ci_upper each *100 toFixed(1) + "%", and a div.enusc-caveat with t.enusc_caveat_text. NO client:* directive (pure static markup, zero JS). Do NOT use a <script> tag (Astro {expr} does not interpolate inside <script>). Do NOT render per-100k or the raw proportion. Add minimal CSS so .enusc-no-estimate is a subtle muted informational note (no broken layout).</action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -5 && node -e "const fs=require('fs'),path=require('path');function walk(d,a){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p,a);else if(e.name==='index.html'&&(p.includes(path.sep+'commune'+path.sep)||p.includes(path.sep+'comuna'+path.sep)))a.push(p);}return a;}const files=walk('dist',[]);let cov=0,no=0;for(const f of files){const h=fs.readFileSync(f,'utf8');if(h.includes('enusc-vhdv-section'))cov++;else if(h.includes('enusc-no-estimate'))no++;}console.log('covered',cov,'noEstimate',no);if(cov===0||no===0)process.exit(1);"</automated>
  </verify>
  <done>Both templates render section 6c; built dist/ has commune pages with enusc-vhdv-section (covered) and pages with enusc-no-estimate (uncovered), counts both > 0; values display as household % with (2024); no empty sections.</done>
</task>

<task type="auto">
  <name>Task 3: Extend commune.mjs to assert the VHDV module; run forbidden-language + commune validators</name>
  <read_first>
    - site/scripts/validate/commune.mjs (dist/ HTML inspection loop, lines 1-60+; existing word-count + forbidden-term checks)
    - site/scripts/validate/forbidden-language.mjs (term list + allow-list mechanics — confirm caveat text is clean)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section "site/scripts/validate/commune.mjs" — extension snippet)
  </read_first>
  <action>In site/scripts/validate/commune.mjs, after the existing per-page checks, add an assertion that every inspected commune page contains EITHER 'enusc-vhdv-section' OR 'enusc-no-estimate'; if neither, console.error the file and process.exit(1) (VL-03/VL-04). Then verify the full bilingual surface: build once and run both validators in a single chained command (OneDrive desync — never separate build from validate). Confirm forbidden-language.mjs exits 0 over the new caveat text (no absolute-verdict term).</action>
  <verify>
    <automated>cd site && npm run build && node scripts/validate/commune.mjs && node scripts/validate/forbidden-language.mjs && echo VALIDATORS_OK</automated>
  </verify>
  <done>commune.mjs enforces the enusc presence rule; commune.mjs and forbidden-language.mjs both exit 0 against the freshly built dist/ (VALIDATORS_OK printed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commune JSON (enusc_vhdv cell content) -> rendered HTML | Build-time interpolation of parsed government data into static pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Tampering | enusc_vhdv values injected into HTML | mitigate | Astro auto-escapes {expr} output; numeric values pass through toFixed(); caveat/label text comes from i18n.ts constants, not from cell content — no set:html of cell values |
| T-23-08 | Repudiation | unattributed / verdict-implying figure | mitigate | mandatory bilingual caveat labels it INE experimental/SAE, distinct from reported crime; forbidden-language validator gate exits 0; no absolute safe/dangerous wording |
| T-23-09 | Information disclosure | rendered statistic | accept | public government figure, no PII/secret |

No high-severity threats remain after mitigation: no user input, no dynamic routes, no API keys; the only injected content is auto-escaped numeric values plus constant i18n strings, and the verdict-implication risk is gated by the forbidden-language validator.
</threat_model>

<verification>
- `cd site && npm run build && node scripts/validate/commune.mjs && node scripts/validate/forbidden-language.mjs` all exit 0
- dist/ covered pages contain enusc-vhdv-section with a household % and (2024); uncovered pages contain enusc-no-estimate text
- `npx astro check` reports no new type errors
</verification>

<success_criteria>
- VL-03: EN + ES covered pages show value (household %) + 2024 + bilingual experimental/SAE caveat; forbidden-language exits 0.
- VL-04: EN + ES uncovered pages show explicit bilingual "no estimate (coverage: 136 comunas)" note, no broken layout, no implied value.
</success_criteria>

<output>
Create `.planning/phases/23-enusc-communal-victimization-layer/23-03-SUMMARY.md` when done.
</output>
