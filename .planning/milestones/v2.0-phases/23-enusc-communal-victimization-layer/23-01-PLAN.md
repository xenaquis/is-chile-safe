---
phase: 23-enusc-communal-victimization-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/snapshots/fetch_enusc_vhdv.py
  - pipeline/tests/test_fetch_enusc_vhdv.py
  - data/snapshots/enusc_vhdv.json
  - data/snapshots/ATTRIBUTION.md
autonomous: true
requirements: [VL-01, VL-02]
must_haves:
  truths:
    - "Running fetch_enusc_vhdv.py with CACHE_ONLY=1 against the verified cache file writes data/snapshots/enusc_vhdv.json with 136 records"
    - "The fetcher refuses to process a cache file whose sha256 does not match the known checksum (fails loudly)"
    - "Each output record carries cut, name, vhdv_rate (proportion 0-1), ci_lower, ci_upper, tipo_estimacion, cv"
    - "Every parsed CUT (col A raw digits) exists in data/cead/meta/index.json; the name->CUT resolver is a redundant cross-check only"
  artifacts:
    - path: "pipeline/snapshots/fetch_enusc_vhdv.py"
      provides: "ENUSC VHDV XLSX ingestion with sha256 guard + direct CUT join"
      contains: "KNOWN_SHA256"
    - path: "pipeline/tests/test_fetch_enusc_vhdv.py"
      provides: "pytest coverage for CACHE_ONLY parse, sha256 guard, column detection, record-count guard"
    - path: "data/snapshots/enusc_vhdv.json"
      provides: "committed versioned snapshot with provenance (source_url, vintage, fetched, caveat, records)"
      contains: "records"
    - path: "data/snapshots/ATTRIBUTION.md"
      provides: "attribution row for enusc_vhdv.json noting it is WIRED (not reference-only)"
  key_links:
    - from: "pipeline/snapshots/fetch_enusc_vhdv.py"
      to: "pipeline/cache/enusc_vhdv.xlsx"
      via: "sha256 assertion before pandas parse"
      pattern: "hashlib.sha256"
    - from: "pipeline/snapshots/fetch_enusc_vhdv.py"
      to: "data/cead/meta/index.json"
      via: "make_slug cross-check of parsed CUTs"
      pattern: "make_slug"
---

<objective>
Build the isolated ENUSC SAE VHDV ingestion fetcher and its committed versioned snapshot. This is the data source-of-truth task: it parses the manually-deposited INE XLSX (sha256-verified), joins directly on CUT raw digits, and writes data/snapshots/enusc_vhdv.json with full provenance — mirroring the SPD/SII snapshot convention exactly.

Purpose: VL-01 (versioned snapshot with provenance, sha256-guarded, no brittle cron download) and the source half of VL-02 (every row resolves to a valid CUT).
Output: pipeline/snapshots/fetch_enusc_vhdv.py, its pytest, data/snapshots/enusc_vhdv.json, ATTRIBUTION.md row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-enusc-communal-victimization-layer/23-RESEARCH.md
@.planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md
@.planning/research/SEED-002-ENUSC-DATA-PROFILE.md

<interfaces>
<!-- Verified schema (SEED-002-ENUSC-DATA-PROFILE.md). Do not re-derive. -->
ENUSC XLSX: single sheet "Hoja1", range A1:G139. Title row 1, blank row 2, HEADER row 3
(pandas header=2), data rows 4-139 = 136 comunas.
Columns (trimmed): A "Código comuna" (CUT raw digits), B "Nombre comuna",
  C "Porcentaje de hogares víctimas de delitos violentos 2024" (proportion 0-1),
  D "Límite inferior", E "Límite superior", F "Tipo de estimación", G "Coeficiente de variación logarítmico".
sha256 = ad9503826fd21590eca7cf37629872df09f3115ad0c6d80dfe5cedcb63c3d4ba
Cache file (git-ignored): pipeline/cache/enusc_vhdv.xlsx

Reusable utilities (do not hand-roll):
- pipeline.shared.atomic_write.atomic_write_json(path, data) — cross-platform atomic write
- pipeline.cead.normalizer.make_slug(name) -> str — NFD/accent/apostrophe-safe slug
- data/cead/meta/index.json — canonical CUT authority (list of {cut, name, ...})
- tenacity.retry — already in pipeline/requirements.txt (download path only)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing pytest for the ENUSC fetcher (Wave 0 scaffold, RED)</name>
  <read_first>
    - pipeline/tests/test_composite.py (fixture/tmp_path idiom, REPO_ROOT sys.path insert)
    - pipeline/tests/conftest.py (shared fixtures available)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section "pipeline/tests/test_fetch_enusc_vhdv.py" — required test cases)
  </read_first>
  <behavior>
    - test_cache_only_mode_exits_zero_with_valid_cache: build a minimal 136-row openpyxl XLSX fixture (or copy the real cache if present), monkeypatch KNOWN_SHA256 and paths to the fixture, set CACHE_ONLY=1, run main(); assert OUTPUT_FILE written and parses to >=136 records.
    - test_sha256_mismatch_raises: write a cache file with wrong bytes; assert RuntimeError whose message contains "sha256 mismatch".
    - test_missing_cache_raises_file_not_found: CACHE_ONLY=1 with absent cache file; assert FileNotFoundError.
    - test_column_detection: parse a minimal fixture; assert a record contains keys vhdv_rate, ci_lower, ci_upper, cut, name, tipo_estimacion, cv.
    - test_record_count_guard: truncated sheet (<136 rows) raises RuntimeError containing "Record count guard".
  </behavior>
  <action>Create pipeline/tests/test_fetch_enusc_vhdv.py mirroring the structure of test_composite.py (from __future__ import annotations, REPO_ROOT sys.path insert, pytest, tmp_path). Use openpyxl to write tiny XLSX fixtures into tmp_path so tests are hermetic and never depend on the git-ignored real cache. Monkeypatch the module-level CACHE_FILE / OUTPUT_FILE / KNOWN_SHA256 constants (use monkeypatch.setattr on the imported module) to point at fixtures. Cover the five behavior cases above. Tests MUST fail now because fetch_enusc_vhdv.py does not yet exist — import will error; that is the expected RED state.</action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_fetch_enusc_vhdv.py -x -q 2>&1 | grep -E "error|Error|no tests ran|ModuleNotFoundError" </automated>
  </verify>
  <done>test_fetch_enusc_vhdv.py exists with the five named test cases; running it fails with an import/module error (RED) — proving the tests run and target the not-yet-written fetcher.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fetch_enusc_vhdv.py (sha256 guard + CACHE_ONLY parse + direct CUT join) (GREEN)</name>
  <read_first>
    - pipeline/snapshots/fetch_spd_homicide.py (canonical fetcher: imports, path constants, _download retry, CACHE_ONLY guard, _build_cut_map, payload write)
    - pipeline/shared/atomic_write.py (atomic_write_json signature)
    - pipeline/cead/normalizer.py (make_slug)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section "fetch_enusc_vhdv.py" — verbatim pattern blocks)
    - data/cead/meta/index.json (CUT authority shape)
  </read_first>
  <action>Create pipeline/snapshots/fetch_enusc_vhdv.py copy-adapting fetch_spd_homicide.py. Module constants: CACHE_FILE = pipeline/cache/enusc_vhdv.xlsx, OUTPUT_FILE = data/snapshots/enusc_vhdv.json, KNOWN_SHA256 = ad9503826fd21590eca7cf37629872df09f3115ad0c6d80dfe5cedcb63c3d4ba, ENUSC_SHEET = "Hoja1", ENUSC_HEADER_ROW = 2, MIN_RECORDS = 136, plus RESOLVED_URL and LANDING_PAGE from ATTRIBUTION/SEED profile. Add new function _assert_sha256(path) using hashlib.sha256 that raises RuntimeError("sha256 mismatch ...") on mismatch. main(): read CACHE_ONLY env (1/true/yes); skip _download in CACHE_ONLY mode; raise FileNotFoundError if cache absent (message instructs manual deposit); call _assert_sha256 before parse. Parse with pd.read_excel(sheet_name=ENUSC_SHEET, header=ENUSC_HEADER_ROW, engine="openpyxl"); strip whitespace from df.columns; guard required columns set (exact trimmed Spanish headers from interfaces) and raise listing missing+found. Direct CUT join: cut = str(row["Código comuna"]).strip().split(".")[0]; build records with vhdv_rate/ci_lower/ci_upper as float, tipo_estimacion/cv. Build _build_cut_map() from index.json via make_slug as a redundant cross-check; log.warning on any col-A vs slug-resolved CUT mismatch (never fail). Enforce MIN_RECORDS guard (RuntimeError "Record count guard ..."). Write payload (source, source_url=RESOLVED_URL, landing_page, vintage="2024", fetched ISO-Z, caveat, records sorted by cut) via atomic_write_json. Do NOT add a per-100k rescale anywhere — vhdv_rate stays the proportion 0-1.</action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_fetch_enusc_vhdv.py -x -q</automated>
  </verify>
  <done>All five tests in test_fetch_enusc_vhdv.py pass (GREEN). The fetcher asserts sha256 before parse, joins directly on col-A CUT, cross-checks via make_slug without failing, and writes a sorted records payload.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the committed snapshot + ATTRIBUTION row from the real cache file</name>
  <read_first>
    - data/snapshots/ATTRIBUTION.md (existing table format for spd_homicide.json / sii_exposure.json; the REFERENCE-ONLY preamble)
    - data/snapshots/spd_homicide.json (canonical snapshot shape)
    - .planning/phases/23-enusc-communal-victimization-layer/23-PATTERNS.md (section "data/snapshots/enusc_vhdv.json" + ATTRIBUTION entry)
    - .planning/research/SEED-002-ENUSC-DATA-PROFILE.md (provenance fields, resolved URL, sha256)
  </read_first>
  <action>Run the fetcher against the manually-deposited cache to produce the committed artifact: ensure pipeline/cache/enusc_vhdv.xlsx is present (sha256 must match KNOWN_SHA256; if absent, this task is blocked and must surface a checkpoint to the operator to drill the INE widget and deposit the file). Execute `cd pipeline && set CACHE_ONLY=1 && python snapshots/fetch_enusc_vhdv.py` (PowerShell: `$env:CACHE_ONLY='1'`) to write data/snapshots/enusc_vhdv.json. Then add a `## enusc_vhdv.json` row block to data/snapshots/ATTRIBUTION.md mirroring the existing block format, explicitly noting in the Note field that this snapshot is WIRED to commune pages (section 6c) — a departure from the REFERENCE-ONLY convention of the other snapshots — and that vhdv_rate is a proportion 0-1 (NOT per-100k). Include the sha256 and the resolved sfvrsn URL. Do NOT commit pipeline/cache/enusc_vhdv.xlsx (it is git-ignored per .gitignore pipeline/cache/*).</action>
  <verify>
    <automated>cd pipeline && python -c "import json,pathlib; p=json.loads(pathlib.Path('../data/snapshots/enusc_vhdv.json').read_text(encoding='utf-8')); rs=p['records']; assert len(rs)==136, len(rs); assert all(0<r['vhdv_rate']<1 for r in rs); assert {'cut','name','vhdv_rate','ci_lower','ci_upper','tipo_estimacion','cv'} <= set(rs[0]); print('OK', len(rs))"</automated>
  </verify>
  <done>data/snapshots/enusc_vhdv.json exists with exactly 136 records, each vhdv_rate in (0,1), full provenance header; ATTRIBUTION.md has a WIRED-noted enusc_vhdv.json block; raw XLSX is not staged in git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INE XLSX file -> pipeline | Externally-sourced binary, manually deposited, no stable URL — untrusted until checksum-verified |
| pipeline -> data/snapshots JSON | Parsed cell content serialized into a committed artifact consumed downstream by the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | manually-deposited enusc_vhdv.xlsx | mitigate | _assert_sha256 against KNOWN_SHA256 before any parse; mismatch raises RuntimeError and (in CI) is absorbed by continue-on-error so CEAD is unaffected |
| T-23-02 | Spoofing | INE sfvrsn URL rotation / silent republish | mitigate | CACHE_ONLY=1 in CI never downloads; acquisition is manual/assisted; sha256 pins the exact bytes; URL recorded in ATTRIBUTION + SOURCES |
| T-23-03 | Information disclosure | snapshot JSON content | accept | Public government statistic, no PII, no secrets; safe to commit |
| T-23-SC | Tampering | pip installs (openpyxl/pandas/tenacity) | accept | All deps already pinned in pipeline/requirements.txt and present; no new package introduced this plan |

No high-severity threats remain after mitigation: the only externally-controlled input is the XLSX, fully pinned by sha256; no auth, no user input, no secrets in scope.
</threat_model>

<verification>
- `cd pipeline && python -m pytest tests/test_fetch_enusc_vhdv.py -x -q` green
- data/snapshots/enusc_vhdv.json: 136 records, vhdv_rate in (0,1), required keys present
- `git status` shows enusc_vhdv.json + ATTRIBUTION.md staged, pipeline/cache/enusc_vhdv.xlsx NOT tracked
</verification>

<success_criteria>
- VL-01 (source half): isolated fetcher writes a provenance-bearing versioned snapshot from a sha256-verified cache; never downloads in CI.
- VL-02 (source half): all 136 parsed rows carry CUT raw digits that exist in index.json; make_slug cross-check logs (never fails).
- No Phase-18 file touched.
</success_criteria>

<output>
Create `.planning/phases/23-enusc-communal-victimization-layer/23-01-SUMMARY.md` when done.
</output>
