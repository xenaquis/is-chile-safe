---
phase: 18-composite-crime-index
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - site/src/components/map/ChoroplethLayer.ts
  - site/src/components/map/MapIsland.tsx
  - site/src/components/map/map.css
autonomous: true
requirements: [CI-07]
user_setup: []

must_haves:
  truths:
    - "The map choropleth defaults to composite index mode on page load"
    - "A toggle in the map topbar switches between index mode and per-family rate mode"
    - "In index mode each commune polygon is colored by its ci level (1-5) using the existing 5-level palette"
    - "Switching modes restyles the existing Leaflet layer via applyStyleMap without re-mounting"
    - "The legend title updates to Crime Index / Indice Delictivo in index mode"
    - "MapIsland passes its current toggle mode ('composite' | 'family') down to ResultPanel as a prop so the popup can gate the composite score block on index mode (D-06); Plan 04 consumes this prop and never edits MapIsland"
  artifacts:
    - path: "site/src/components/map/ChoroplethLayer.ts"
      provides: "ci field on CommunaPayload; buildStyleMapFromCompositeIndex()"
      contains: "buildStyleMapFromCompositeIndex"
    - path: "site/src/components/map/MapIsland.tsx"
      provides: "mode toggle state defaulting to composite; mode passed to ResultPanel via prop"
      contains: "composite"
  key_links:
    - from: "site/src/components/map/MapIsland.tsx"
      to: "site/src/components/map/ChoroplethLayer.ts"
      via: "calls buildStyleMapFromCompositeIndex + applyStyleMap on toggle"
      pattern: "buildStyleMapFromCompositeIndex"
    - from: "site/src/components/map/MapIsland.tsx"
      to: "site/src/components/map/ResultPanel.tsx"
      via: "passes mode ('composite' | 'family') as a prop so the popup gates the composite block on index mode (D-06)"
      pattern: "mode="
---

<objective>
Add the composite-index choropleth mode to the Leaflet map: extend CommunaPayload with the ci field, add buildStyleMapFromCompositeIndex(), wire a topbar toggle in MapIsland that defaults to index mode (D-05) and swaps styles via the existing applyStyleMap(), update the legend title, AND pass the current toggle mode down to ResultPanel as a prop so the popup can show the composite score only when in index mode (D-06). No client-side quantile recompute — the level is pre-computed in the pipeline.

Purpose: CI-07 — the choropleth must offer index mode (default) vs per-family rate mode using the embedded ci field from Plan 02's map-payload. This is the headline v2.0 map view. MapIsland is the single owner of the toggle mode state; it exposes that state to ResultPanel (Plan 04) so the popup's composite block can be mode-aware (D-06) without Plan 04 reaching into MapIsland.
Output: extended ChoroplethLayer.ts; toggle in MapIsland.tsx; mode prop wired to ResultPanel; new CSS classes in map.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-composite-crime-index/18-PATTERNS.md
@.planning/phases/18-composite-crime-index/18-UI-SPEC.md
@.planning/phases/18-composite-crime-index/18-02-SUMMARY.md

<interfaces>
ChoroplethLayer.ts (analog buildStyleMapFromHomicide lines 120-138):
  - CommunaPayload interface lines 13-22; add after homicide_count: ci: number | null (composite level 1-5; null pre-composite)
  - INCIDENCE_COLORS array (5 hex, from colors.ts) reused unchanged
  - applyStyleMap() lines 144-151 restyles the existing L.geoJSON layer — NEVER re-mount
  - buildStyleMapFromCompositeIndex(comunas): level = c.ci ?? 3; fillColor INCIDENCE_COLORS[level-1];
      fillOpacity c.ci === null ? 0.25 : 0.55; color white; weight 1.2. No client-side quantile recompute.

MapIsland.tsx: add toggle state, DEFAULT to composite (D-05). On toggle call the matching builder + applyStyleMap; update legend title.
  Pass the current mode value ('composite' | 'family') to <ResultPanel mode={mode} ... /> as a prop (D-06). MapIsland OWNS this state;
  Plan 04 (ResultPanel) only READS the prop and gates its composite block on mode === 'composite'. Plan 04 must NOT edit MapIsland.

UI-SPEC (locked):
  - Toggle: two .chip buttons in .map-topbar wrapped in .mode-toggle; active = background var(--primary) color #fff; 44px height.
    Labels EN Index / By crime; ES Indice / Por delito.
  - Legend title index mode: EN Crime Index / ES Indice Delictivo.
  - Null/no-data communes: fillColor #cccccc, fillOpacity 0.25 (existing DEFAULT_STYLE).
  - New CSS classes in map.css only: .mode-toggle (display:flex; gap:4px). No new CSS files.

Project rules: native L.geoJSON() (never react-leaflet GeoJSON component); chroma-js palette reused.
Astro <script> no expr interpolation (memory) — pass server data via data-* / props, not braces in <script>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ci to CommunaPayload + buildStyleMapFromCompositeIndex()</name>
  <files>site/src/components/map/ChoroplethLayer.ts</files>
  <read_first>
    - site/src/components/map/ChoroplethLayer.ts (CommunaPayload lines 13-22; buildStyleMapFromHomicide lines 120-138; applyStyleMap lines 144-151; INCIDENCE_COLORS import)
    - .planning/phases/18-composite-crime-index/18-PATTERNS.md (ChoroplethLayer.ts section lines 170-197)
    - .planning/phases/18-composite-crime-index/18-RESEARCH.md (Pattern 4 lines 287-313)
  </read_first>
  <behavior>
    - buildStyleMapFromCompositeIndex returns a Map with one entry per commune (346)
    - A commune with ci=4 gets fillColor INCIDENCE_COLORS[3] and fillOpacity 0.55
    - A commune with ci=null gets fillOpacity 0.25 and a defined fallback fill
  </behavior>
  <action>
    Extend the CommunaPayload interface: add ci: number | null after homicide_count. Add exported buildStyleMapFromCompositeIndex(comunas) returning Map of id to L.PathOptions, copying the loop shape from buildStyleMapFromHomicide (lines 127-136) but reading c.ci directly: level = c.ci ?? 3; fillColor INCIDENCE_COLORS[level-1]; fillOpacity c.ci === null ? 0.25 : 0.55; color white; weight 1.2. Do NOT compute quantile breaks (pipeline pre-computed the level). ci is required on CommunaPayload (map-payload always carries it, null when absent) to keep astro check clean.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - site/src/components/map/ChoroplethLayer.ts contains buildStyleMapFromCompositeIndex
    - CommunaPayload declares ci: number | null
    - npx astro check passes (0 errors)
    - buildStyleMapFromCompositeIndex reads c.ci only (no quantile recompute)
  </acceptance_criteria>
  <done>ci field added to CommunaPayload; buildStyleMapFromCompositeIndex returns 346 styled entries using the precomputed level; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: MapIsland toggle (default composite) + mode prop to ResultPanel + legend title + map.css</name>
  <files>site/src/components/map/MapIsland.tsx, site/src/components/map/map.css</files>
  <read_first>
    - site/src/components/map/MapIsland.tsx (existing mode/filter state; legend title render; applyStyleMap usage; how ResultPanel is rendered + its current props)
    - site/src/components/map/ChoroplethLayer.ts (buildStyleMapFromCompositeIndex from Task 1; applyStyleMap)
    - site/src/components/map/ResultPanel.tsx (current prop signature — confirm the prop name space so adding mode is additive; Plan 04 reads this prop)
    - .planning/phases/18-composite-crime-index/18-UI-SPEC.md (Choropleth Mode Toggle; Legend Update sections)
    - site/src/components/map/map.css (existing .chip, .legend, .map-topbar classes)
  </read_first>
  <action>
    Add a mode state to MapIsland.tsx with values composite (DEFAULT, D-05) and family. Render two .chip toggle buttons inside .map-topbar wrapped in a .mode-toggle div: labels EN Index / By crime, ES Indice / Por delito; active chip gets .chip.active (background var(--primary), color #fff); 44px height. On toggle, compute the style map via buildStyleMapFromCompositeIndex (composite) or the existing family builder, then call applyStyleMap() — never re-mount the layer. When mode is composite set the legend title to EN Crime Index / ES Indice Delictivo (lang-aware); family mode keeps existing legend behavior. Use the existing lang prop for bilingual labels.
    Pass the current mode value to ResultPanel as a prop: render the existing <ResultPanel ... /> with an added mode={mode} prop (D-06). MapIsland is the SOLE owner of this state; ResultPanel (Plan 04) only reads the prop to gate its composite block on mode === 'composite' and must not mutate MapIsland. Do not change ResultPanel's other props. The prop type is 'composite' | 'family'.
    Add .mode-toggle { display: flex; gap: 4px; } to map.css (no new CSS file; reuse .chip styles).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site" && npx astro check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - MapIsland.tsx default mode is composite (D-05) — assert initial state literal composite
    - Two toggle chips render in .map-topbar; .mode-toggle class present in map.css
    - Legend title becomes Crime Index / Indice Delictivo in composite mode
    - Toggle calls applyStyleMap (no layer re-mount)
    - MapIsland renders ResultPanel with a mode prop carrying the current toggle value ('composite' | 'family') — grep for `mode=` on the ResultPanel render
    - npx astro check passes; npm run build succeeds
  </acceptance_criteria>
  <done>Map opens in composite index mode; toggle swaps to per-family rate mode and back via applyStyleMap; legend title updates; MapIsland passes mode to ResultPanel as a prop (D-06 wiring); build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map-payload.json -> client map island | Pre-rendered static data; ci is a bounded integer 1-5 or null |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Spoofing (editorial false precision via color) | composite choropleth implies a verdict through red-to-green ramp | mitigate | Reuses the neutral 5-level incidence palette (not a red=bad green=good ramp); caveat block (Plan 04) accompanies every score; legend titled Crime Index not Safety |
| T-18-09 | Tampering | ci out of range corrupts color index | mitigate | level = c.ci ?? 3 fallback; null -> dimmed 0.25 opacity; INCIDENCE_COLORS index bounded 0-4 |
</threat_model>

<verification>
- npx astro check passes (CI-07 TS types)
- npm run build succeeds with toggle in place
- map defaults to composite index mode; toggle swaps to family mode and back
- MapIsland passes mode to ResultPanel (D-06 wiring seam for Plan 04)
</verification>

<success_criteria>
- Choropleth offers index mode (default) vs per-family rate mode (CI-07)
- ci level drives polygon color via the precomputed pipeline field
- applyStyleMap restyle (no re-mount); legend title lang-aware
- mode state owned by MapIsland and exposed to ResultPanel via prop (D-06 enablement)
</success_criteria>

<output>
Create `.planning/phases/18-composite-crime-index/18-03-SUMMARY.md` when done.
</output>
