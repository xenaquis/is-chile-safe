---
phase: 20-methodology-sources-hardening
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
  - site/src/pages/crime-ranking/[crime].astro
  - site/src/pages/es/ranking-delito/[crime].astro
  - site/src/components/FamilyBreakdownBars.astro
autonomous: true
requirements: [MTH-01, MTH-02, MTH-03]
must_haves:
  truths:
    - "The crime-ranking hero per-family national mean and the regional breakdown mean each carry an inline 'unweighted commune mean' caveat (EN+ES)"
    - "The methodology Comparison-criteria H2 explains per-family means, per-region breakdown means, and the 1-5 LevelChip tier classification (EN+ES) as sub-sections preserving the 7-H2 parity"
    - "The methodology distinguishes the unweighted national aggregate (F2) from the population-weighted national/regional time-series (F5)"
    - "The drogas scope note (Ley 20.000 grupo 401 only) renders inline on the commune FamilyBreakdownBars where the drogas bar appears (EN+ES)"
    - "The #trend-formula anchor exists on the methodology Trend-formula H2 (EN+ES)"
  artifacts:
    - path: "site/src/pages/methodology.astro"
      provides: "Comparison-criteria sub-sections for per-family/per-region means + LevelChip tiers + F2/F5 weighting distinction + #trend-formula anchor"
      contains: "trend-formula"
    - path: "site/src/components/FamilyBreakdownBars.astro"
      provides: "Inline drogas scope note rendered with the drogas bar"
      contains: "drogas"
  key_links:
    - from: "site/src/components/FamilyBreakdownBars.astro"
      to: "site/src/lib/familyDefs.ts FAMILY_SCOPE_NOTE_EN/ES"
      via: "import + render of drogas scope note"
      pattern: "FAMILY_SCOPE_NOTE"
    - from: "site/src/pages/crime-ranking/[crime].astro"
      to: "methodology Comparison-criteria"
      via: "inline caveat near National mean hero-stat"
      pattern: "unweighted"
---

<objective>
Close the methodology-completeness orphans on the figure registry: add inline "unweighted commune mean" caveats to the displayed per-family national mean (F3) and per-region breakdown mean (F4); add methodology sub-sections explaining per-family means, per-region breakdowns, and the 1-5 LevelChip tier classification (F6) while preserving the 7-H2 EN/ES parity; make the unweighted-national (F2) vs population-weighted-series (F5) distinction explicit; render the drogas scope note inline on the commune FamilyBreakdownBars (F10/MC-09); and add the #trend-formula anchor (F7/MC-06).

Purpose: Every aggregate figure a user sees on a ranking hero or commune hero must be self-explaining at the point of display AND traceable to a methodology explanation. This is MTH-01, MTH-02, and the MTH-03 drogas-note + anchor halves.

Output: Updated methodology pages (EN+ES) at structural parity, ranking pages with inline caveats, and a FamilyBreakdownBars component that surfaces the drogas scope note.

VERIFIED PRE-STATE (do NOT redo): F2/TD-01 unweighted-mean wording is already FIXED in Phase 19 (methodology.astro:302 says "unweighted mean"). The #trend-formula anchor does NOT yet exist (Phase 19 did not land it) and FamilyBreakdownBars does NOT render the drogas note — both are in scope here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-METHODOLOGY-SPEC.md
@site/src/pages/methodology.astro
@site/src/pages/es/metodologia.astro
@site/src/pages/crime-ranking/[crime].astro
@site/src/pages/es/ranking-delito/[crime].astro
@site/src/components/FamilyBreakdownBars.astro
@site/src/lib/familyDefs.ts

<interfaces>
From site/src/lib/familyDefs.ts (drogas scope notes already authored — reuse, do NOT rewrite):
  export const FAMILY_SCOPE_NOTE_EN: Record<string, string>  // FAMILY_SCOPE_NOTE_EN['drogas'] = "Note: this is CEAD's Drug Crimes family ..."
  export const FAMILY_SCOPE_NOTE_ES: Record<string, string>  // FAMILY_SCOPE_NOTE_ES['drogas'] = "Nota: corresponde a la familia Drogas del CEAD ..."

FamilyBreakdownBars.astro current Props:
  interface Props { byFamily: ByFamily; locale: 'en' | 'es'; level: 1|2|3|4|5; }
  FAMILY_ORDER includes 'drogas'; rows are mapped; component has zero client:* directives (keep static).

methodology.astro current H2 order (7): Data source: CEAD / Map boundaries: chilemapas / How the rate is calculated / Underreporting (cifra negra) / Trend formula / Comparison criteria / What this site does NOT say.
es/metodologia.astro mirror (7): Fuente de datos: CEAD / Geometria del mapa / Como se calcula la tasa / Subregistro (cifra negra) / Formula de tendencia / Criterios de comparacion / Lo que este sitio NO dice.

crime-ranking hero per-family mean: methodology.astro EN ranking shows hero-stat "National mean: {familyNationalMean} per 100k" at [crime].astro:239; ES "Media nacional: ..." at es/ranking-delito/[crime].astro:245. Regional breakdown section at ~:291/:297.
</interfaces>

PARITY RULE (locked, MTH-04): methodology.astro and es/metodologia.astro MUST keep exactly 7 H2 sections each. Add the new per-family/per-region/LevelChip explanations as SUB-sections (h3 or list items) UNDER the existing "Comparison criteria" / "Criterios de comparacion" H2 — do NOT add an 8th H2. The F2/F5 weighting distinction goes under "Trend formula" / "How the rate is calculated" as a sub-paragraph. Crime rates are ALREADY per-100k — never rescale, no recompute.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Methodology sub-sections — per-family mean, per-region breakdown, LevelChip tiers, F2/F5 distinction, #trend-formula anchor (MTH-01, MTH-02; F3/F4/F5/F6/F7)</name>
  <files>site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro</files>
  <action>Edit both methodology pages in lockstep to preserve 7-H2 parity.

  (1) Under the existing "Comparison criteria" / "Criterios de comparacion" H2, add three sub-sections (use h3 or labeled list items, NOT new h2):
    - Per-family national mean (F3, MTH-01): explain the per-family "National mean" figure shown on crime-ranking heroes is the UNWEIGHTED mean of eligible commune rates for that family — same typical-commune basis as the overall national aggregate, not population-weighted, excludes <10k communes.
    - Per-region breakdown mean (F4, MTH-01): explain the per-region breakdown figure shown on crime-ranking pages is the unweighted mean of eligible commune rates within that region, excludes <10k communes.
    - Incidence level classification (F6, MTH-02): explain the 1-5 LevelChip on every commune hero is a RELATIVE 5-tier classification derived at build time from the distribution of non-low-population commune rates — a relative ranking, NOT an absolute threshold and NOT a safety verdict.

  (2) Make the F2 vs F5 weighting distinction explicit: add a sentence (under the rate-calculation or trend section) noting the national/regional TIME-SERIES rates use population-weighted means (correct for trends), whereas the national/per-family/per-region AGGREGATE figures are unweighted typical-commune means — flagged to avoid confusion.

  (3) F7/MC-06: add id="trend-formula" to the Trend-formula H2 (EN) and id="trend-formula" to the Formula-de-tendencia H2 (ES) so /methodology/#trend-formula and /es/metodologia/#trend-formula deep-link. Note params (5%, 3-yr) are editorial (already explained on page; keep prose, just add the anchor + reference that these are documented as editorial in SOURCES.md).

  Keep legal-safe tone (no absolute safe/dangerous verdicts). EN and ES edits must be content-equivalent. Verify with grep that both files still have exactly 7 h2.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && test "$(grep -c '<h2' site/src/pages/methodology.astro)" = "7" && test "$(grep -c '<h2' site/src/pages/es/metodologia.astro)" = "7" && grep -q 'id="trend-formula"' site/src/pages/methodology.astro && grep -q 'id="trend-formula"' site/src/pages/es/metodologia.astro && echo PASS</automated>
  </verify>
  <done>Both methodology pages keep exactly 7 H2; Comparison-criteria H2 in each has sub-sections explaining per-family mean, per-region breakdown, and LevelChip tiers; the F2/F5 weighting distinction is explicit; the Trend-formula H2 carries id="trend-formula" in both locales. Tone stays legal-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Inline ranking caveats + commune drogas scope note (MTH-01 inline; MTH-03 drogas)</name>
  <files>site/src/pages/crime-ranking/[crime].astro, site/src/pages/es/ranking-delito/[crime].astro, site/src/components/FamilyBreakdownBars.astro</files>
  <action>(1) F3/F4 inline caveat (MTH-01): on the crime-ranking hero near the "National mean" / "Media nacional" hero-stat (EN [crime].astro:239, ES es/ranking-delito/[crime].astro:245) and on the regional breakdown section (~:291 EN / ~:297 ES), add a short inline note that the displayed mean is an unweighted commune mean (excludes <10k communes), linking to the methodology Comparison-criteria explanation. Keep it terse — a caption or small note, not a paragraph. EN and ES content-equivalent.

  (2) F10/MC-09 drogas scope note (MTH-03): make FamilyBreakdownBars render the drogas scope note inline. Import FAMILY_SCOPE_NOTE_EN / FAMILY_SCOPE_NOTE_ES from ../lib/familyDefs (do NOT rewrite the note text — reuse the existing strings). When the rendered FAMILY_ORDER includes 'drogas' (always does), render the locale-appropriate scope note as a small footnote/caption beneath the bars (or as an inline note on the drogas row). Keep the component static (zero client:* directives). The note must be visible in rendered HTML on commune/[slug] and es/comuna/[slug] which both mount FamilyBreakdownBars.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && grep -q "FAMILY_SCOPE_NOTE" site/src/components/FamilyBreakdownBars.astro && grep -iq "unweighted" site/src/pages/crime-ranking/[crime].astro && echo PASS</automated>
  </verify>
  <done>The crime-ranking hero + regional breakdown carry an inline unweighted-commune-mean caveat in both locales; FamilyBreakdownBars imports and renders FAMILY_SCOPE_NOTE for drogas so the Ley 20.000 grupo 401 scope note appears inline on commune pages in both locales; component stays static.</done>
</task>

<task type="auto">
  <name>Task 3: Build + validate gate (OneDrive-safe chained)</name>
  <files>site/src/pages/methodology.astro</files>
  <action>Run the full chained build+validate in ONE command (OneDrive desync guard — dist/ vanishes if build and validate run as separate processes). Confirm the dist HTML renders the new drogas scope note on a commune page (EN+ES), the #trend-formula anchor in the built methodology HTML, and the inline ranking caveat. Fix any astro/TypeScript errors surfaced by the build before declaring done. Do NOT recompute any data; this is copy/structure only.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe/site" && npm run build && npm run validate</automated>
  </verify>
  <done>npm run build succeeds and npm run validate reports all validators pass (12/12) in a single chained invocation; the built dist methodology HTML contains the trend-formula anchor and the commune HTML contains the drogas scope note in both locales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editorial copy -> reader | Methodology/ranking copy is read as factual provenance; an unqualified absolute verdict would violate the legal-safe constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Repudiation | methodology copy edits | mitigate | Keep all three sources (methodology, FAQ, data.ts) consistent; do not reintroduce "population-weighted" for the aggregate |
| T-20-04 | Information disclosure | new ranking caveats | accept | Caveats add clarity only; no new data exposed; forbidden-language validator re-runs in build |
</threat_model>

<verification>
Full chained `cd site && npm run build && npm run validate` is green (12/12). Both methodology pages keep exactly 7 H2. Drogas scope note present in built commune HTML (EN+ES). #trend-formula anchor present in built methodology HTML (EN+ES). No data files changed; no rate rescaled.
</verification>

<success_criteria>
- MTH-01: per-family national mean (F3) and per-region breakdown mean (F4) carry inline unweighted-commune caveats AND are explained in the methodology (EN+ES).
- MTH-02: LevelChip 1-5 (F6) explained as a relative distribution-derived tier scale (EN+ES) as a sub-section preserving 7-H2 parity.
- MTH-03 (this plan's halves): drogas scope note (Ley 20.000 grupo 401) renders inline on FamilyBreakdownBars; #trend-formula anchor exists in both locales.
- F2/F5 weighting distinction explicit.
- Build + all 12 validators pass (chained). No data recompute, no rate rescale.
</success_criteria>

<output>
Create `.planning/phases/20-methodology-sources-hardening/20-02-SUMMARY.md` when done.
</output>
