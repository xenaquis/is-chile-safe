---
phase: 20-methodology-sources-hardening
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - site/src/pages/methodology.astro
  - site/src/pages/es/metodologia.astro
  - site/src/components/MethodologyCaveat.astro
autonomous: true
requirements: [MTH-04, SRC-02]
must_haves:
  truths:
    - "The methodology underreporting/cifra-negra claims carry an inline clickable citation link to the ENUSC source (EN+ES)"
    - "Each methodology figure explanation links to its named source (CEAD, INE, ENUSC, chilemapas) so no on-page claim is unattributed"
    - "Trend/sparkline references deep-link to /methodology/#trend-formula (EN) and /es/metodologia/#trend-formula (ES)"
    - "methodology.astro and es/metodologia.astro are at structural H2 parity (7 H2 each, matching attribution/table-note blocks)"
  artifacts:
    - path: "site/src/pages/methodology.astro"
      provides: "Inline ENUSC + per-figure source citation links"
      contains: "ENUSC"
    - path: "site/src/components/MethodologyCaveat.astro"
      provides: "Trend deep-link to #trend-formula"
      contains: "trend-formula"
  key_links:
    - from: "site/src/pages/methodology.astro underreporting H2"
      to: "ENUSC source landing page"
      via: "inline clickable citation link"
      pattern: "href=.*(ine\\.gob\\.cl/enusc|seguridadpublica)"
    - from: "site/src/components/MethodologyCaveat.astro"
      to: "/methodology/#trend-formula"
      via: "deep-link href"
      pattern: "#trend-formula"
---

<objective>
Wire the inline source-citation links that make every methodology claim attributable: add an inline clickable ENUSC citation on the underreporting/cifra-negra claims (SRC-02, F11), ensure each figure explanation links to its named source (CEAD / INE / ENUSC / chilemapas), deep-link trend/sparkline references to the #trend-formula anchor created in plan 20-02, and assert EN/ES structural H2 parity (MTH-04).

Purpose: SRC-02 requires the ENUSC claims carry an inline citation link, and MTH-04 requires the two methodology pages be at structural parity with matching attribution blocks. This plan turns the SOURCES.md entries from plan 20-01 into clickable on-page citations and confirms the bilingual structure holds.

Output: methodology pages with inline source links + ENUSC citation, a MethodologyCaveat that deep-links to #trend-formula, and verified 7-H2 EN/ES parity.

VERIFIED PRE-STATE: ENUSC is currently cited by name at methodology.astro:214 / es/metodologia.astro:217 with NO link and is absent from SOURCES.md (plan 20-01 adds the SOURCES.md entry; this plan adds the on-page link). MethodologyCaveat currently links generically to /methodology/ with no anchor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-METHODOLOGY-SPEC.md
@data/SOURCES.md
@site/src/pages/methodology.astro
@site/src/pages/es/metodologia.astro
@site/src/components/MethodologyCaveat.astro

<interfaces>
ENUSC is cited (no link) at methodology.astro:214 and es/metodologia.astro:217 inside the Underreporting/Subregistro H2.
MethodologyCaveat.astro builds methodHref = locale==='en' ? '/methodology/' : '/es/metodologia/' and renders it as an <a>. To deep-link trends, append the #trend-formula fragment where the caveat references trends/sparklines.
The #trend-formula anchor was added to both methodology pages in plan 20-02 (this plan depends on 20-02).
ENUSC official URL: use the [verify URL]-marked URL committed in data/SOURCES.md by plan 20-01 (https://www.ine.gob.cl/enusc — keep the [verify URL] discipline: link it but it may be confirmed later).
</interfaces>

PARITY RULE (MTH-04): keep exactly 7 H2 in each page; only adding inline <a> links and an anchor fragment, no structural change. Crime rates already per-100k — no recompute. Legal-safe tone preserved.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline ENUSC + per-figure source citation links (SRC-02)</name>
  <files>site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro</files>
  <action>(1) SRC-02 ENUSC link: in the Underreporting/Subregistro H2 of both pages, wrap the ENUSC mention (methodology.astro:214, es/metodologia.astro:217) in an inline clickable <a> to the ENUSC landing URL committed in data/SOURCES.md by plan 20-01. Honor the [verify URL] discipline — link the best-known official URL; do not invent a new one.

  (2) Per-figure attribution links: ensure each figure explanation in the methodology names AND links its source so no on-page quantitative claim is unattributed — CEAD (rate calc, F1), INE (population denominator, F1/F9 — link the new INE SOURCES.md landing), chilemapas (map boundaries, F12 — already attributed, confirm link present). Reuse the official URLs already committed in SOURCES.md. EN and ES content-equivalent.

  Keep exactly 7 H2 per page; this is additive linking only.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && grep -Eq 'href="[^"]*(ine\.gob\.cl/enusc|seguridadpublica)' site/src/pages/methodology.astro && grep -Eq 'href="[^"]*(ine\.gob\.cl/enusc|seguridadpublica)' site/src/pages/es/metodologia.astro && test "$(grep -c '<h2' site/src/pages/methodology.astro)" = "7" && echo PASS</automated>
  </verify>
  <done>Both methodology pages carry an inline clickable ENUSC citation link in the underreporting H2 and link each figure to its named source (CEAD/INE/chilemapas); 7-H2 parity preserved; no invented URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Trend deep-link to #trend-formula + ES structural parity assertion (MTH-04, MC-06)</name>
  <files>site/src/components/MethodologyCaveat.astro, site/src/pages/methodology.astro, site/src/pages/es/metodologia.astro</files>
  <action>(1) MC-06: in MethodologyCaveat.astro, where the caveat references the trend/sparkline (or as a dedicated trend link), deep-link to the locale-correct anchor: '/methodology/#trend-formula' (EN) and '/es/metodologia/#trend-formula' (ES). The plain methodology link may stay; the trend-specific reference should target the anchor created in plan 20-02. Keep the component static (zero client:* directives).

  (2) MTH-04 ES parity: confirm metodologia.astro mirrors methodology.astro structurally — same 7 H2, same attribution/table-note blocks, same set of inline source links (ENUSC, CEAD, INE, chilemapas). If any attribution block exists on EN but not ES (or vice versa), add the missing block to reach parity. Do NOT touch the forbidden-language validator here — it already exists (validator #9) and is verified/hardened in plan 20-04.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && grep -q "#trend-formula" site/src/components/MethodologyCaveat.astro && test "$(grep -c '<h2' site/src/pages/methodology.astro)" = "$(grep -c '<h2' site/src/pages/es/metodologia.astro)" && echo PASS</automated>
  </verify>
  <done>MethodologyCaveat deep-links the trend reference to #trend-formula in both locales; methodology.astro and es/metodologia.astro have equal H2 counts (7 each) and matching attribution/source-link blocks; component stays static.</done>
</task>

<task type="auto">
  <name>Task 3: Build + validate gate (OneDrive-safe chained)</name>
  <files>site/src/pages/methodology.astro</files>
  <action>Run the full chained build+validate in ONE command (OneDrive desync guard). Confirm the built dist methodology HTML contains the ENUSC citation link and the #trend-formula deep-link resolves (anchor target present from plan 20-02). The forbidden-language validator (#9) must pass after the copy edits — re-run is automatic within npm run validate. Fix any astro/TS errors before declaring done. No data recompute.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe/site" && npm run build && npm run validate</automated>
  </verify>
  <done>npm run build succeeds and npm run validate reports all validators pass (12/12) chained; built dist methodology HTML contains the ENUSC citation link and the #trend-formula anchor; forbidden-language validator passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline citation link -> external official source | A wrong/invented citation URL misattributes authoritative evidence |
| methodology copy -> reader | Forbidden-language constraint must hold after copy edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Spoofing | ENUSC citation URL | mitigate | Link only the SOURCES.md-committed URL with [verify URL] discipline; never fabricate |
| T-20-06 | Tampering | methodology copy | mitigate | forbidden-language validator (#9) re-runs in build and gates absolute safe/dangerous verdicts |
</threat_model>

<verification>
Chained `cd site && npm run build && npm run validate` green (12/12). Built dist methodology HTML contains the inline ENUSC citation link and the #trend-formula anchor target. EN/ES H2 counts equal (7 each). MethodologyCaveat deep-links trend references to #trend-formula. No data changed.
</verification>

<success_criteria>
- SRC-02: underreporting/cifra-negra claims carry an inline clickable ENUSC citation link (EN+ES); each figure links its named source.
- MTH-04 (this plan's halves): EN/ES structural parity confirmed (7 H2, matching attribution blocks); trend references deep-link to #trend-formula.
- Build + all 12 validators pass (chained, OneDrive-safe). Forbidden-language validator passes after edits. No data recompute.
</success_criteria>

<output>
Create `.planning/phases/20-methodology-sources-hardening/20-03-SUMMARY.md` when done.
</output>
