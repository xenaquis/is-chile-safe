---
phase: 20-methodology-sources-hardening
plan: 04
type: execute
wave: 4
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - pipeline/tests/test_ine_population_spotcheck.py
  - site/scripts/validate/figure-registry.mjs
  - site/scripts/validate/all.mjs
  - site/scripts/validate/forbidden-language.mjs
autonomous: true
requirements: [SRC-01, SRC-05, MTH-04]
must_haves:
  truths:
    - "A pipeline pytest spot-checks >=5 commune populations in data/ine/poblacion_comunal.json against known official INE projection values and fails on drift"
    - "A figure-registry completeness validator asserts every figure F1-F12,F15 maps to a SOURCES.md entry or a labeled editorial-parameter note (zero orphans)"
    - "The figure-registry validator is registered in all.mjs and runs as part of npm run validate"
    - "The forbidden-language validator is confirmed gated and registered; it fails the build on a non-attributed absolute safe/dangerous/seguro/peligroso verdict outside the allowlist"
  artifacts:
    - path: "pipeline/tests/test_ine_population_spotcheck.py"
      provides: "INE population spot-check against official values (>=5 communes)"
      contains: "population_2024"
    - path: "site/scripts/validate/figure-registry.mjs"
      provides: "Figure-registry zero-orphan completeness check over data/SOURCES.md"
      exports: []
  key_links:
    - from: "site/scripts/validate/figure-registry.mjs"
      to: "data/SOURCES.md"
      via: "reads SOURCES.md and asserts each registry figure maps to an entry"
      pattern: "SOURCES\\.md"
    - from: "site/scripts/validate/all.mjs"
      to: "figure-registry.mjs"
      via: "registered in VALIDATORS array"
      pattern: "figure-registry"
---

<objective>
Add the CI guards that keep the methodology/sources contract from regressing: an INE population spot-check (SRC-01) comparing >=5 commune populations against official INE values; a figure-registry completeness validator (SRC-05) that fails if any figure F1-F12,F15 lacks a SOURCES.md entry or labeled editorial-parameter note; and verification/hardening of the already-existing forbidden-language validator (MTH-04 validator half).

Purpose: SRC-01 requires a CI spot-check of commune populations vs official INE; SRC-05 requires zero orphan figures with an automated check to keep it that way; MTH-04 requires the forbidden-language CI guard. The forbidden-language validator already exists (validator #9, gated, registered) — this plan confirms and hardens it rather than recreating it.

Output: a new pytest, a new figure-registry.mjs validator registered as the 13th validator, and a verified-hardened forbidden-language.mjs.

NOTE: This plan owns only validator/test files and is file-disjoint from plans 20-01/02/03, so it runs in Wave 1 parallel with plan 20-01. The figure-registry check reads the SOURCES.md entries authored in plan 20-01 — if run before 20-01 lands, it will (correctly) report orphans; sequence the figure-registry COMPLETENESS confirmation after 20-01 + 20-03 land, but the validator code itself can be authored in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-METHODOLOGY-SPEC.md
@data/SOURCES.md
@site/scripts/validate/all.mjs
@site/scripts/validate/forbidden-language.mjs
@pipeline/tests/test_population.py
@pipeline/scripts/fetch_ine_population.py

<interfaces>
data/ine/poblacion_comunal.json shape: array of { "cut": "1101", "name": "Iquique", "population_2024": 231962 }.
pipeline.shared.population.load_population() returns dict keyed by str cut -> int population (see pipeline/tests/test_population.py).
CI (.github/workflows/ci.yml) runs `pytest pipeline/tests/ -q` (auto-discovers new test files) and `node scripts/validate/all.mjs`. No CI edit needed — new pytest + registration in all.mjs are picked up automatically.
all.mjs VALIDATORS is a string array of script filenames run in sequence; any non-zero child exit fails the suite; summary prints N/N passed. forbidden-language.mjs is already entry #9.
Figure registry source of truth: v1.3-METHODOLOGY-SPEC.md Part 1 table (F1-F15). In-scope figures requiring a SOURCES.md mapping: F1-F12 + F15. F13/F14 are deferred (Phase 18) and excluded.
Forbidden-language validator already: guards dist/ existence (exit 1), scans visible text, allowlists CEAD-attributed superlatives, exits non-zero on match. It is gated and registered.
</interfaces>

OneDrive: when running the JS validator standalone after a build, chain build+validate in one command. Do NOT recompute INE data — the spot-check reads the committed JSON only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: INE population spot-check pytest (SRC-01, SC-02)</name>
  <files>pipeline/tests/test_ine_population_spotcheck.py</files>
  <behavior>
    - Loads data/ine/poblacion_comunal.json (the committed static asset; no network).
    - Asserts >=5 named communes match known official INE 2024 projection values within a tight tolerance (exact match or +/- a tiny rounding epsilon). Use communes with stable, well-known projections (e.g. a mix of large + small CUTs already present, such as Iquique 1101, Santiago RM communes, and at least one <10k commune from the file).
    - Hardcode the expected official values as the ground-truth constant in the test (sourced from the official INE projection table — the test IS the spot-check; mirror drift will flip it red).
    - Test fails loudly if any spot-checked commune deviates beyond tolerance (catches silent mirror re-methodologization, SC-02 risk).
  </behavior>
  <action>Create pipeline/tests/test_ine_population_spotcheck.py. Read the committed data/ine/poblacion_comunal.json directly (pathlib, no mocking — mirror the existing test_population.py loader pattern). Define a dict of >=5 {cut: expected_official_population_2024} ground-truth values and assert each matches the committed file within tolerance. Document in a module docstring that these expected values are the official INE 2024 commune projection figures and that a failure indicates the third-party mirror (bastianolea/censo_proyecciones_poblacion) has drifted from official INE — the SC-02 supply-chain guard. Pick communes already present in the file; if you cannot independently confirm an official value for a commune, do NOT fabricate one — choose communes whose official projection you can cite, and note the source in the docstring. CI runs pytest pipeline/tests/ so no workflow edit is needed.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe" && pytest pipeline/tests/test_ine_population_spotcheck.py -q</automated>
  </verify>
  <done>pytest pipeline/tests/test_ine_population_spotcheck.py passes against the committed JSON, spot-checking >=5 communes vs hardcoded official INE values; the test fails if the committed populations drift from the official ground truth.</done>
</task>

<task type="auto">
  <name>Task 2: Figure-registry completeness validator (SRC-05) + register in all.mjs</name>
  <files>site/scripts/validate/figure-registry.mjs, site/scripts/validate/all.mjs</files>
  <action>Create site/scripts/validate/figure-registry.mjs: an offline validator (no dist/ needed) that reads data/SOURCES.md and asserts every in-scope figure from v1.3-METHODOLOGY-SPEC.md Part 1 (F1-F12 + F15) maps to a SOURCES.md entry or a labeled editorial-parameter note — zero orphans. Implement as an explicit mapping table in the validator: for each figure id, list the required SOURCES.md anchor token(s) that must be present (e.g. F1->CEAD+INE, F2->Methodology parameters editorial note, F3/F4->INE/CEAD+Methodology parameters, F6/F8->derived note, F7/F9->Methodology parameters editorial note, F11->ENUSC, F12->chilemapas, F15->news/RSS). The check passes only if every required token group is found in SOURCES.md (grep-style substring/heading presence). Exit non-zero listing any figure whose required source token is missing. Exclude F13/F14 (deferred Phase 18) explicitly with a comment. Then register 'figure-registry.mjs' in the VALIDATORS array in all.mjs (append after seo.mjs, making it the 13th) and update the header comment block listing validators.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe/site" && node scripts/validate/figure-registry.mjs && grep -q "figure-registry.mjs" scripts/validate/all.mjs && echo PASS</automated>
  </verify>
  <done>figure-registry.mjs reads data/SOURCES.md, asserts F1-F12+F15 each map to a SOURCES.md entry or editorial note (excludes F13/F14), exits non-zero on any orphan, and is registered as the 13th validator in all.mjs.</done>
</task>

<task type="auto">
  <name>Task 3: Verify + harden forbidden-language validator; full suite green (MTH-04)</name>
  <files>site/scripts/validate/forbidden-language.mjs</files>
  <action>Confirm forbidden-language.mjs is gated (exits 1 when dist/ missing — already present), registered in all.mjs (already #9), and fails on a non-attributed absolute verdict. Harden ONLY if a gap is found: ensure the FORBIDDEN_TERMS list covers unqualified absolute "safe/dangerous/seguro/peligroso" verdict patterns described in v1.3-METHODOLOGY-SPEC Part 3 (the existing list covers "dangerous zone/area/commune/neighborhood", "the safest", "100% safe/seguro", "zona/comuna/barrio peligroso", etc.). Do NOT over-broaden into rhetorical titles, explicit negations, or disclaimed quotes — those are permitted by the guardrail and protected by the ALLOW_LIST. If the current coverage already matches the spec's intent, make no code change and document the verification in the SUMMARY. Then run the full chained build+validate (OneDrive-safe) to confirm 13/13 validators pass (now including figure-registry) AND the pipeline pytest is green.</action>
  <verify>
    <automated>cd "C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Is Chile Safe/site" && npm run build && npm run validate</automated>
  </verify>
  <done>forbidden-language.mjs confirmed gated + registered + correctly failing on non-attributed absolute verdicts (hardened only if a gap existed); the full chained build+validate reports 13/13 validators pass (including the new figure-registry check); pipeline pytest passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party mirror -> committed INE JSON | Silent drift from official INE values must be caught in CI |
| editorial copy -> dist HTML | Absolute safe/dangerous verdicts must be blocked at build time |
| SOURCES.md -> figure registry | An orphaned figure (number without a source) must fail CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | INE mirror -> population JSON | mitigate | Spot-check pytest fails on drift from hardcoded official ground truth (>=5 communes) |
| T-20-08 | Tampering | dist HTML editorial copy | mitigate | forbidden-language validator (#9) gates non-attributed absolute verdicts |
| T-20-09 | Repudiation | unsourced displayed figure | mitigate | figure-registry validator (#13) fails on any orphan figure |
</threat_model>

<verification>
Chained `cd site && npm run build && npm run validate` reports 13/13 validators pass (structure, commune, rollout, region, crime, hreflang, schema, map, forbidden-language, coverage, spine, seo, figure-registry). `pytest pipeline/tests/ -q` is green including the new INE spot-check. figure-registry.mjs reports zero orphan figures (after plans 20-01 + 20-03 land their SOURCES.md entries + inline links).
</verification>

<success_criteria>
- SRC-01: INE population spot-check pytest exists, checks >=5 communes vs official INE values, registered (auto-discovered by CI), passes against committed JSON, fails on drift.
- SRC-05: figure-registry.mjs asserts zero orphan figures (F1-F12+F15), registered as 13th validator.
- MTH-04 (validator half): forbidden-language validator confirmed gated + registered + correctly blocking non-attributed absolute verdicts.
- Full chained build+validate green (13/13); pipeline pytest green. OneDrive-safe single command.
</success_criteria>

<output>
Create `.planning/phases/20-methodology-sources-hardening/20-04-SUMMARY.md` when done.
</output>
