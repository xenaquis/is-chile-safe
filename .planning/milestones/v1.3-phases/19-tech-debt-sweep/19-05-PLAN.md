---
phase: 19-tech-debt-sweep
plan: 05
type: execute
wave: 4
depends_on: ["19-04"]
files_modified:
  - site/src/config/i18n.ts
  - site/src/components/MapPlaceholderSlot.astro
  - site/src/pages/region/[slug].astro
  - site/src/pages/es/region/[slug].astro
  - site/src/pages/crime/[family].astro
  - site/src/pages/es/delito/[family].astro
  - site/src/pages/crime-ranking/[crime].astro
  - site/src/pages/es/ranking-delito/[crime].astro
  - site/src/components/map/FiltersRow.tsx
  - pipeline/shared/schema.py
  - pipeline/tests/test_centroids.py
  - pipeline/tests/test_classifier.py
  - pipeline/tests/test_dedup.py
  - pipeline/tests/test_feeds.py
  - pipeline/tests/test_store.py
  - .planning/milestones/v1.0-phases/04-editorial-pages-adsense/04-VALIDATION.md
  - .planning/milestones/v1.0-phases/05-rss-news-pipeline/05-VALIDATION.md
  - .planning/milestones/v1.0-phases/06-cicd-cloudflare-deployment/06-VALIDATION.md
autonomous: true
requirements: [TD-02, TD-07]
must_haves:
  truths:
    - "No coming soon / disponible proximamente placeholder text renders anywhere on the site; the live map is the only map surface"
    - "The map_placeholder_text i18n key and the coming soon aria-label are gone"
    - "A CI assertion fails the build if FAMILY_KEYS order changes unexpectedly"
    - "by_family is typed (not loose dict) and AVAILABLE_YEARS is derived dynamically rather than hardcoded"
    - "No stale skipif/xfail guards referencing not yet implemented remain in the pipeline test suite; tests run unconditionally"
    - "nyquist_compliant is reconciled (true) for archived phases 4-6 VALIDATION.md"
  artifacts:
    - path: "site/src/components/MapPlaceholderSlot.astro"
      provides: "Neutral live-map link (no coming soon text), or retired"
    - path: "pipeline/shared/schema.py"
      provides: "FAMILY_KEYS order CI assertion + typed by_family"
  key_links:
    - from: "region/crime/crime-ranking pages"
      to: "/map/"
      via: "static link to the live map instead of dead placeholder slot"
      pattern: "/map/"
    - from: "site/src/components/map/FiltersRow.tsx"
      to: "AVAILABLE_YEARS"
      via: "dynamically derived year list"
      pattern: "AVAILABLE_YEARS"
---

<objective>
Remove the stale map placeholder (TD-02) and close residual code tech-debt from PROJECT.md (TD-07).

The map is live at /map/, but six editorial templates (region, crime, crime-ranking — EN+ES) still render MapPlaceholderSlot whose i18n text is "Interactive map — coming in next update" / "Mapa interactivo — disponible proximamente" and whose aria-label says "coming soon" (MapPlaceholderSlot.astro:36, i18n.ts:184,317). TD-07 closes the PROJECT.md:73 tech-debt line: FAMILY_KEYS order CI assertion, typed by_family, dynamic AVAILABLE_YEARS (FiltersRow.tsx:46 hardcodes 2025-i), reconcile nyquist_compliant for archived phases 4-6, and remove stale skipif/xfail guards (pipeline/tests/* say "not yet implemented (Wave 1)" though modules are live).

Purpose: No misleading placeholder; data-shape invariants guarded; tests run honestly; archived audit flags reconciled.
Output: Neutral live-map link replacing the placeholder; FAMILY_KEYS guard; typed by_family; dynamic AVAILABLE_YEARS; skipif cleanup; nyquist flags reconciled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-tech-debt-sweep/19-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
MapPlaceholderSlot.astro renders t.map_placeholder_text + a static aria-label "Interactive map — coming soon"/"Mapa interactivo — proximamente". It is imported + rendered on: region/[slug].astro:220, es/region/[slug].astro:213, crime/[family].astro:279, es/delito/[family].astro:270, crime-ranking/[crime].astro:312, es/ranking-delito/[crime].astro:318. No React island mounts into these slots on these pages (the live map lives only at /map/ + /es/mapa/).
i18n.ts: map_placeholder_text declared in interface (line 56) and defined at EN_STRINGS:184 + ES_STRINGS:317.
FiltersRow.tsx:46 AVAILABLE_YEARS = Array.from({length:21},(_,i)=>2025-i) — hardcoded.
pipeline/shared/schema.py:22 FAMILY_KEYS list; by_family typed loosely (dict[str, float | None]).
pipeline/tests with skipif/_MODULE_AVAILABLE: test_centroids.py, test_classifier.py, test_dedup.py, test_feeds.py, test_store.py (reasons say "not yet implemented (Wave 1)" but modules are live).
Archived VALIDATION.md with nyquist_compliant:false: v1.0-phases/04,05,06 (DECISIONS a = close/reconcile).
NOTE: 19-06 (Wave 1) added i18n keys and edited crime/crime-ranking pages; 19-04 (Wave 2) added vida filter + redirect. This plan is Wave 3 to edit those same files after them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale map placeholder; link to live map instead (TD-02)</name>
  <files>site/src/components/MapPlaceholderSlot.astro, site/src/config/i18n.ts, site/src/pages/region/[slug].astro, site/src/pages/es/region/[slug].astro, site/src/pages/crime/[family].astro, site/src/pages/es/delito/[family].astro, site/src/pages/crime-ranking/[crime].astro, site/src/pages/es/ranking-delito/[crime].astro</files>
  <action>
Replace the dead placeholder with a static link to the live map. Preferred approach: convert MapPlaceholderSlot.astro into a neutral "view on the live map" call-to-action card (no "coming soon" wording, no dashed-border placeholder styling that implies absence): render a link to /map/ (EN) or /es/mapa/ (ES) with neutral label "View the interactive map" / "Ver el mapa interactivo". Remove the "coming soon"/"proximamente" aria-label (use a neutral aria-label or rely on the link text). Remove the map_placeholder_text key from the i18n.ts interface, EN_STRINGS, and ES_STRINGS, and add a neutral label key if you introduce one (e.g. map_cta_label) used by the new card. Update the six callers only if the prop contract changes; if you keep the same component API they need no edit. Ensure the map.mjs validator (zero-React content-page regression guard) still passes — this stays a static link, zero client:* directives. Do NOT add a Leaflet island to these editorial pages.
  </action>
  <verify>
    <automated>cd site; npm run build; npm run validate</automated>
    <grep>grep -c "map_placeholder_text" site/src/config/i18n.ts returns 0; grep -ri "coming soon\|disponible proximamente\|coming in next update" site/dist returns 0; grep -c "/map/\|/es/mapa/" site/src/components/MapPlaceholderSlot.astro returns >=1</grep>
  </verify>
  <done>No coming-soon placeholder text in dist; map_placeholder_text key removed; the six pages show a neutral live-map link; map.mjs (zero-React guard) + all validators green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FAMILY_KEYS order assertion + typed by_family + skipif cleanup (TD-07 pipeline)</name>
  <files>pipeline/shared/schema.py, pipeline/tests/test_centroids.py, pipeline/tests/test_classifier.py, pipeline/tests/test_dedup.py, pipeline/tests/test_feeds.py, pipeline/tests/test_store.py</files>
  <behavior>
    - A test asserts FAMILY_KEYS equals its expected canonical ordered list; reordering FAMILY_KEYS fails CI.
    - by_family is declared with an explicit typed structure (TypedDict or per-key model) rather than a bare dict.
    - Every pipeline test runs unconditionally; no skipif/_MODULE_AVAILABLE guard with a "not yet implemented" reason remains.
  </behavior>
  <action>
In schema.py, add an explicit canonical FAMILY_KEYS order assertion: either a module-level expected tuple compared in a dedicated test, or strengthen the typing of by_family from dict[str, float | None] to a TypedDict keyed by the known family keys (and add a test asserting key set + order matches FAMILY_KEYS). Add/locate a test (e.g. pipeline/tests/test_schema.py or test_output.py) that fails if FAMILY_KEYS order changes — this is the CI assertion. In the five test files, remove the skipif decorators and _MODULE_AVAILABLE guards whose reasons reference "not yet implemented (Wave 1)" (the modules are live, confirmed by 158/158 passing); let the tests run unconditionally. Do NOT change real test logic — only drop the obsolete guards. Keep any genuinely-needed skips (none expected per the finding).
  </action>
  <verify>
    <automated>python -m pytest pipeline/tests/ -q -rs</automated>
  </verify>
  <done>FAMILY_KEYS order is CI-guarded; by_family is typed; no "not yet implemented" skipif remains; pytest passes with zero unexpected skips.</done>
</task>

<task type="auto">
  <name>Task 3: Dynamic AVAILABLE_YEARS + reconcile nyquist flags (TD-07 frontend + docs)</name>
  <files>site/src/components/map/FiltersRow.tsx, .planning/milestones/v1.0-phases/04-editorial-pages-adsense/04-VALIDATION.md, .planning/milestones/v1.0-phases/05-rss-news-pipeline/05-VALIDATION.md, .planning/milestones/v1.0-phases/06-cicd-cloudflare-deployment/06-VALIDATION.md</files>
  <action>
In FiltersRow.tsx replace the hardcoded AVAILABLE_YEARS = Array.from({length:21},(_,i)=>2025-i) with a dynamically-derived list: derive the upper bound from the available data/catalog (e.g. the map payload year or catalog years passed as a prop) rather than literal 2025; if the component cannot easily receive data, at minimum derive the end year from new Date().getFullYear() bounded by the data's known earliest year (2005) so it stays correct as new CEAD years land. Keep the dropdown behavior identical. Then reconcile the archived audit debt: in the three VALIDATION.md files (phases 04, 05, 06) flip nyquist_compliant: false -> true (per DECISIONS a — these milestones shipped and were superseded by the v1.2 audit + v1.3 review with 0 blockers); add a one-line note in each frontmatter or body that the flag was reconciled at v1.3 P19 (TD-07). Do NOT alter the substantive validation content.
  </action>
  <verify>
    <automated>cd site; npm run build; npm run validate</automated>
    <grep>grep -c "2025-i\|length: 21" site/src/components/map/FiltersRow.tsx returns 0; grep -rc "nyquist_compliant: false" .planning/milestones/v1.0-phases/0[456]-* returns 0</grep>
  </verify>
  <done>AVAILABLE_YEARS is derived dynamically (no hardcoded 2025 length-21 literal); phases 4-6 VALIDATION.md show nyquist_compliant: true with a reconciliation note; build + 12/12 validators green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline -> schema | FAMILY_KEYS order is a cross-cutting invariant (map-payload compact list relies on it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Tampering | FAMILY_KEYS order drift | mitigate | CI assertion fails build if order changes (compact by_family list depends on it) |
| T-19-11 | Repudiation | misleading "coming soon" UI | mitigate | placeholder replaced with truthful live-map link; dist grep gate |
</threat_model>

<verification>
- cd site; npm run build; npm run validate -> build green + 12/12 validators (OneDrive-safe chained).
- python -m pytest pipeline/tests/ -q -rs -> green, zero unexpected skips.
- grep gates: no coming-soon text in dist; map_placeholder_text removed; no hardcoded 2025 length-21; no nyquist false in phases 4-6.
</verification>

<success_criteria>
- Stale placeholder gone; live map is the only map surface; neutral live-map link on the six pages.
- FAMILY_KEYS order CI-guarded; by_family typed; AVAILABLE_YEARS dynamic; skipif cruft removed; nyquist 4-6 reconciled.
- Build + all validators + pipeline pytest green.
</success_criteria>

<output>
Create `.planning/phases/19-tech-debt-sweep/19-05-SUMMARY.md` when done
</output>
