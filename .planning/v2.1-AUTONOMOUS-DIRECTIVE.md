# v2.1 AUTONOMOUS RUN DIRECTIVE

**Authorized by user: 2026-07-29.** The user is away. The orchestrator session takes ALL decisions, maximizing quality and granularity. This file is the contract for every session that continues the run. Read it in full before doing anything.

## Standing order

Execute milestone v2.1 (Phases 26–33 in `.planning/ROADMAP.md`) end to end, autonomously, phase by phase, honoring the per-phase protocol and the locked decisions in `.planning/REQUIREMENTS.md`. Do not stop to ask the user anything — decide, record the decision, and continue. Only a genuine safety/irreversibility issue justifies halting.

## Model routing (user directive: "Sonnet codea, Opus valida, Fable solo decisiones difíciles")

| Step | Agent | Model |
|---|---|---|
| Phase research | gsd-phase-researcher | `sonnet` |
| Plan | gsd-planner | `sonnet` |
| Plan check | gsd-plan-checker | `opus` |
| **Premortem** (see below) | general-purpose agent | `opus` |
| **Plan review / approval** | orchestrator inline (session model is Fable) or Agent `model="fable"` | `fable` |
| Execution / coding | gsd-executor | `sonnet` |
| Code review | gsd-code-reviewer | `opus` |
| Verification (gsd-verifier) | gsd-verifier | `opus` |
| GO/NO-GO verdicts, design acceptance, blocker triage, anything ambiguous | orchestrator inline | `fable` |

Config note: `.planning/config.json` stays `model_profile: balanced` — enforce the routing above by passing explicit `model=` overrides when spawning agents. `mode: yolo` and `skip_discuss: true` are already set.

## Per-phase protocol (mandatory, phases 26–33)

1. `/gsd:plan-phase N` (researcher + planner = Sonnet; plan-checker = Opus).
2. **Premortem**: spawn an Opus agent → writes `NN-PREMORTEM.md` in the phase dir ("this phase failed — why?"; concrete failure modes, each with a plan amendment or acceptance). Planner (Sonnet) incorporates amendments into PLAN.md.
3. **Fable plan review**: orchestrator reviews plan + premortem against the phase's success criteria and the locked decisions. Verdict recorded at the top of PLAN.md (`Fable review: APPROVED [date]` + notes). If rejected → back to planner, max 2 cycles, then decide inline and move on.
4. `/gsd:execute-phase N` (executors = Sonnet).
5. **Opus code review** (`/gsd:code-review` or gsd-code-reviewer agent): HIGH findings → Sonnet fixes → re-review. Max 2 fix cycles; unresolved HIGH → record in STATE.md blockers, do not merge that plan's claim of completion.
6. GSD validation: gsd-verifier (Opus) → VERIFICATION.md. `nyquist_validation` is on.
7. Update STATE.md, commit everything (GSD atomic commits). Move to next phase.

## Execution order & decision tree

**26 ✅ DONE (NO-GO) → 27 → 28 → 29 → 30 → 31 → 32 → 33**, with these rules:

- ~~27 does NOT wait for 26's verdict~~ — moot, 26 is closed.
- **28: NEWSUI-05 degrades to faceting-only. Phase 26 returned NO-GO** (fp=11, precision 0.667 against a 100%/zero-false-merge gate). There is no clustering layer to visualize. Do not reopen; see STATE.md § Phase 26 Outcome for the three measured failure modes and the cheap re-entry path.
- **29 (BrowserOS)**: MUST run inline in the orchestrator session — gsd-executor has no BrowserOS tools. If BrowserOS MCP is unreachable when 29 starts: retry once after re-checking `http://127.0.0.1:9200/mcp`; still down → mark 29 BLOCKED in STATE.md, skip 30 (hard-gated), continue with 31/32/33, and leave 29/30 as the only remaining work.
- **30** starts only after 29's acceptance gate (amended, see below).
- 31 documents whatever actually shipped; if 29/30 blocked, the map-redesign section is written as "pending" and flagged.
- 32/33 close the milestone.

## Gate amendments for unattended mode (decided by Fable, 2026-07-29)

1. **MAPUX-05 human acceptance → Fable-proxy acceptance.** The design loop still runs ≥2 full iterations with per-iteration scoring; acceptance is issued inline by the Fable orchestrator against MAPUX-03's concrete criteria, with an evidence pack (all screenshots + scoring table) saved in the phase dir. **Human review is deferred, not waived**: recorded as a pending item for the user's return. Phase 30 may proceed on Fable acceptance.
2. **CRON-01 "deliberately-blanked-secret dry run" → local logic verification.** No live GitHub secret is touched while unattended. The guard steps are verified by local simulation (env-var-blanked shell test of the guard script/step logic) + actionlint. The live dry run goes on the deferred-live list.
3. **SEC-01 repo-level default token setting and SEC-05 settings verification** are GitHub-Settings-UI actions → deferred-live list. The code-side halves (per-job `permissions:` blocks, zizmor in CI, dependabot.yml, SHA-pinning, log hygiene) proceed normally.

## Hard safety rules (non-negotiable)

- **NO `git push` during the autonomous run.** All commits stay local. Production and the live crons remain untouched. Exception: if CI feedback is truly needed, pushing a branch named `v2.1-autonomous` is allowed — **never master**.
- **No mutation of `data/`** beyond what the pipeline tests write to fixtures. The clustering spike reads `current.json`/archive read-only. Never run repair scripts against production data unattended.
- **No GitHub settings/secrets changes. No workflow_dispatch triggers.**
- **LLM budget for Phase 26: ≤ 2,000 OpenRouter calls total** (golden set ~100 pairs × few runs ≈ well under). If exceeded, stop the spike and write NO-GO-by-cost.
- OneDrive gotcha: always chain `npm run build && npm run validate` (and any dist/ consumer) in ONE command.
- Every phase leaves validators (14) + pytest (~179+) green before being marked complete.
- If anything fails twice, record it in STATE.md `## Blockers`, move to the next independent phase. Never delete or revert completed work to "unblock".

## Pre-verified facts (do NOT re-verify; done 2026-07-29 before handoff)

- `data/cead/meta/index.json` entries carry `region_id` (e.g. `{"cut":"10101","region_id":"10"}`) → **FACET-03 open question CLOSED**; CUT→region derivation can also cross-check against it.
- Archive schema fields: `cut, date, family, id, lat, lng, outlet, slug, title_en, title_es, url` — sufficient for the golden set. `current.json` = 1,215 incidents; **257 same-(cut,date) buckets with >1 article** — golden-set material is abundant.
- `.env` (repo root) has live `OPENROUTER_API_KEY` + `DEEPSEEK_API_KEY`.
- Clustering model wiring: `ibm-granite/granite-4.1-8b` via `https://openrouter.ai/api/v1` (`pipeline/news/classifier.py:77-79`).
- **Spike ping PASS 3/3** (see `.planning/phases/26-event-clustering-spike/26-00-SPIKE-PING.md`): Granite at temp 0.0 returned clean structured-JSON verdicts — positive pair (Lo Prado homicide, 2 outlets) merged; clean negative (Iquique droga vs asalto) and adversarial negative (Antofagasta condena vs operativo, same day+comuna) both correctly refused. Real adversarial bucket found in live data: comuna 2101 on 2026-07-01 has 9 articles covering 3 distinct events — use it in the golden set.

## Phase 26 planner decisions already taken (don't reopen)

- GO gate: **100% pairwise precision, 0 false merges** (locked at kickoff).
- Verdict schema: the spike's JSON shape (`same_event/confidence/facts/rationale`, doubt → false) is the validated starting point.
- Call pattern: pairwise within `(cut, date)` buckets (±1 day optional) after rapidfuzz pre-filter — matches the pre-verified 257-bucket structure.
- Golden set: build from `current.json` + `archive/*.json`; MUST include the comuna-2101/2026-07-01 nine-article bucket and the Tongoy 4102/2026-07-01 bucket (8 articles, probable single event with noisy sentence figures — good hard positive).

## Operating lessons from Phase 26 (added 2026-07-29 — apply to every remaining phase)

These were learned the hard way in Phase 26. Do not rediscover them.

1. **`gsd-executor` has NO Agent tool** (its registry grants Read/Write/Edit/Bash/Grep/Glob only). Any plan step that requires spawning an independent reviewer, a blind second opinion, or a parallel sub-analysis **must be executed inline by the orchestrator**, not delegated to an executor. Leaving such a step inside a plan produces either a silently-skipped gate or a self-review by the same agent that produced the work. Split the plan dispatch into orchestrator-managed steps instead (Phase 26 Wave 2 was dispatched in three: executor draft -> orchestrator blind pass -> executor finalize). Same applies to Phase 29: BrowserOS work runs inline.
2. **Audit BOTH error directions before issuing a verdict.** A gate that guards one direction (e.g. confirming `merge` labels) leaves the opposite direction (a wrong `no_merge` fabricating a false positive) unguarded. Before signing any GO/NO-GO, verify the labels/assumptions that *drive* the verdict, not just the ones the protocol happens to name.
3. **A verdict that cannot fail is not a verdict.** Any measurement must have a defined, green-closing path for BOTH outcomes before execution starts. Phase 26's NO-GO branch originally could not close with a green suite; `xfail(strict=True)` is the pattern that resolves this — the failure stays encoded and re-arms if a future change passes, instead of being hidden by a weakened assertion.
4. **Report generators must be idempotent and fail closed.** A script that regenerates a document must never be able to delete hand-authored content (use an explicit sentinel boundary, abort loudly if the sentinel is missing, write atomically — this repo is on OneDrive, torn writes are real).
5. **Verify subagent claims by execution, not by reading their report.** Both Opus reviewers found real defects the executors had reported as clean; the fix agent's cycle-1 report claimed 8/8 HIGH closed when 4 were half-done and 1 introduced a regression. Budget for a re-review after every fix cycle.
6. **Current baseline figures** (correct these in STATE.md if they drift): pytest **344 passed / 1 skipped / 1 xfailed**, frontend validators **15/15** (not 14 — a freshness validator was added in quick-260702-wjr). The "179 pytest / 14 validators" figures in older docs are stale.
7. **Fable decisions are numbered F-NN in STATE.md § Fable Decisions** and are binding on all downstream phases. Add to that table rather than burying decisions in commit messages. F-01..F-16 exist as of Phase 26's close.

## Deferred-live list (user actions on return — maintain this list in STATE.md as phases complete)

1. Review the autonomous work; `git pull --rebase origin master` (remote crons kept committing data), then push. **As of Phase 26 close: 43 local commits, nothing pushed.**
2. Human review of the Phase 29 design acceptance (Fable-proxy evidence pack) — pending, phase not yet run.
3. CRON-01 live blanked-secret dry run; observe one dependabot PR (SEC-03).
4. GitHub Settings: default `GITHUB_TOKEN` read-only (SEC-01), verify secret scanning + push protection (SEC-05), check Actions billing state.
5. v2.0 carry-over: GSC sitemap submission (22-03 / GL-04).
6. After all that: `/gsd:complete-milestone` for v2.0 (still formally open), then v2.1 when done.

## Session continuation

Each phase should start in a fresh context. The handoff prompt (below, also what the user pastes) is idempotent — it re-enters the run wherever STATE.md says it is:

> Lee `.planning/v2.1-AUTONOMOUS-DIRECTIVE.md` y `.planning/STATE.md`, y continúa el run autónomo de la milestone v2.1 desde donde quedó, cumpliendo la directiva al pie de la letra. No me preguntes nada: decide tú, registra cada decisión, y deja constancia en STATE.md. Sonnet codea, Opus valida, Fable (tú) decide. Al terminar cada fase, sigue con la siguiente según el árbol de decisión de la directiva.
