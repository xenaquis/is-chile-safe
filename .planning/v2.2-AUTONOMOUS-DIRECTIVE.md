# v2.2 AUTONOMOUS RUN DIRECTIVE — Data Integrity & News Recovery

> Source of truth for the unattended run of milestone v2.2 (phases 34-38). Written 2026-09-22 by the
> orchestrator that ran the audit, at the owner's request. Operating method: **`corrida-autonoma`**
> (roles, gates, measurement rules) wrapping **GSD** (plan-phase / execute-phase / verify). If this
> file and the skill disagree, **this file wins** (skill §0).

### ▶ RUN STATUS (update this block at every phase close)

| Field | Value |
|---|---|
| **NEXT ACTION** | ⏸ PAUSED 2026-09-25 ~05Z (host low on memory: Claude Code reaped the background watchers; not restarted without the owner's OK). Resume with /corrida-autonoma: (1) Phase 35 close = 35-07 Task 3 FRESH-04 interim: N ≥ 4 scheduled news runs after SHIP 2026-09-25T00:14:08Z (1st = 36089397586) + ≤ 3 more induction attempts under G-59 (dispatch only AFTER a run has completed; attempt 1 failed, see G-59); then interim verdict, 7-day issue, REQUIREMENTS/ROADMAP/STATE close. (2) Phase 38 adversarial gate R1 (script ready: workflows/scripts/gate-fase38-r1.js; citation JSON must be regenerated — the run was reaped). (3) Execute Phase 36 (gated GO) → 37 (gated GO) → 38 in order. |
| **Phases** | 34 ☑ · 35 ▶ (shipped + prod-verified; FRESH-04 live proof pending) · 36 ◐ (planned, premortem, gate GO) · 37 ◐ (planned, premortem, gate GO) · 38 ◐ (planned, premortem; gate R1 pending) |
| **Git** | master at `396eef9` (Phase 35 code pushed 2026-09-25T00:14:08Z; deploy verified). |
| **LLM spend (external API)** | USD ≈0.54 of the USD 10.00 run cap (Phase 34: 0.53; Phase 35 induction attempt 1 re-classified 19 items ≈ 0.005). Live cron classification = production operations. |
| **Phase-36 push policy (R-23)** | 36-09/36-10 pushes under G-34/G-07(b); backfill publication under owner default + G-18(2); revert = git revert <data commit> |
| **Decisions** | see § Decision log |
| **Baseline (2026-09-22, HEAD e565ce8)** | pytest 395 passed / 1 skipped / 1 xfailed (exit 0) · vitest 86/86 (9 files) · validators 16/16 (freshness PASS on `generated`) · astro check 0 errors / 0 warnings / 47 hints · lint-workflows exit 0 · build 834 pages |
| **DeepSeek backup** | smoke call OK 2026-09-22 (deepseek-v4-flash direct, 34 tokens); balance USD 23.24 → pause point 3 not triggered |

## Standing order

Execute phases **34 → 35 → 36 → 37 → 38**, one at a time, each one fully closed (verification PASSED, STATE and ROADMAP updated, committed, pushed, deploy verified on prod when it touches the served surface) before the next begins. Run unattended. Stop only at the **pause points** listed below. Every non-trivial decision is recorded as **G-NN** in this file (append to § Decision log) *before* the subagent that needs it runs.

## Required reading at start (in this order)

1. This file.
2. `.planning/research/v2.2-AUDIT-260922.md`: the arbitrated audit. Finding IDs V-01..V-29 with evidence. **REFUTED findings stay refuted; do not re-chase them** (V-20 BioBio mojibake, V-21/V-22 CEAD stale/monitor, V-23 data-only deploy, V-24).
3. `.planning/REQUIREMENTS.md` (33 reqs + owner decisions) and `.planning/ROADMAP.md` § v2.2.
4. `.planning/STATE.md` § Current Position.
5. Memory index (auto-loaded). The relevant entries are v2.2 milestone, granite-default-classifier, OneDrive build desync, sexuales news-only, national_rank direction, CEAD scraper local, i18n localized-slug pitfall, Astro scoped style defeats [hidden], and news page build path drift.

## Roles (corrida-autonoma §2; applies to every phase)

| Step | Who |
|---|---|
| Research (codebase, measurements) | sonnet (haiku for censuses/log triage) |
| Plan (PLAN.md with file:line evidence, every gate threshold numeric BEFORE execution) | opus, effort medium |
| Premortem (≥3 independent perspectives: technical, methodological, integration) | Workflow; synthesis by a **fresh** opus arbiter |
| Adversarial gate on the plan vs real code | Workflow per `~/.claude/skills/corrida-autonoma/references/gate-adversarial.md` (haiku×2 → opus lenses×3 → fresh opus arbiter, xhigh) → `GATE-N.md` beside the PLAN |
| Execution | sonnet (routine); opus for contract, pipeline-kernel or security changes; orchestrator for long jobs (evals, backfill, builds, deploy polling) |
| Validation | opus, against the **served route** (curl prod after deploy), not only the harness |
| Arbiter for hard calls | fresh opus, xhigh. Use `fable` only when the doubt is about model judgement rather than a repo fact (declare why). |

Max 2 gate→reformulation cycles. If NO-GO persists, **pause** (see below). PLAUSIBLE never signs.

## Phase notes (what the audit already settled; don't re-derive)

### Phase 34 — CRITICAL PATH (deadline 2026-10-05)
- **Root cause (pre-verified):** `pipeline/news/classifier.py:72-79` hard-codes `ibm-granite/granite-4.1-8b`. OpenRouter delisted it between 2026-09-04 15:40Z and 20:12Z. The API-error → None → `classifier_none` path is `classifier.py:162-168,231-247`; the seen-before-classify path is `scrape_news.py:303`. The heartbeat and `freshness.mjs` read `current.json.generated`, which `store.py:194-209` rewrites every run.
- **Order inside the phase:** (1) model-parameterized eval runner + A/B (≥3 candidates from NREC-01) → pick by pre-declared metrics → **G-NN**; (2) config-driven model id, preflight, DeepSeek-direct backup, typed outcomes, retry/backoff, circuit breaker; (3) alarm (post-commit health gate + issue) and the newest-incident heartbeat; (4) push + verify live runs are classifying; (5) **backfill** of the 2,073 items; (6) prod verification before 2026-10-05.
- **Fast-path rule:** if by **2026-09-30** the full protocol has not produced a green classifier on prod, ship a minimal hotfix (env-configurable model id = the A/B winner or `deepseek/deepseek-v4-flash`, with the existing DeepSeek path as fallback) through `/gsd:quick` with a single opus validation. Then continue the rest of Phase 34 normally. Record it as G-NN.
- Check that the DeepSeek account is funded with ONE tiny smoke call. If it is not funded, **pause** (owner must top up), but continue with the OpenRouter primary meanwhile.
- Backfill: dry-run first (counts only, zero writes), then a real run on a branch-free working tree, with a backup copy of `current.json`/`rejected/2026-09.json` in the scratchpad. Report the accepted/rejected split as measured numbers.

### Phase 35-38
Follow ROADMAP success criteria. Phase 36 FID-05 dedup is **deterministic only** (0.82 rule); LLM clustering stays NO-GO (v2.1 locked). HYG-06 (local CEAD scrape) only runs **on/after 2026-10-01**. It runs on this local machine, never in Actions (403). If the date has not arrived when Phase 38 is reached, do everything else in the phase, leave HYG-06 open, and add it to the deferred-live list.

## Owner-decision defaults (use these unless the owner has written otherwise; record each as G-NN when applied)

| Decision | Default | Reason |
|---|---|---|
| Replacement model | Winner of the NREC-01 A/B by pre-declared thresholds. Tie-break: more live endpoints, then lower cost. | Owner: "decidir por métricas" |
| Backup provider | DeepSeek direct API (`DEEPSEEK_API_KEY`) | Owner decision |
| Backfilled incidents | Keep their original publication dates. Add one bilingual line on the methodology page about the 2026-09-04..N reclassification. | Honest, non-destructive |
| Re-classify Granite-era published incidents (07-27..09-04) | **NO** in this milestone. Fix forward only. List it in deferred-live. | Rewrites public history; owner call |
| FID-03 family-agreement threshold | ≥85% on the 50-item hand audit, pre-declared in the plan | Needs a number before execution |
| EN headline disclosure | Yes: "Machine-translated headline" label on EN cards | Owner: verbatim policy |
| Public ledgers (`seen.json`, `rejected/`) | Stop serving them on prod (keep them in the repo and R2) | Third-party snippets; reversible |
| `/map/` vs `/chile-crime-map/` | Differentiate titles/H1 by intent. **No noindex and no redirect** without owner OK. | Least destructive |
| Dependabot | Weekly manual triage policy documented. No auto-merge. Enable CodeQL (free on public repos). | Conservative |
| GSC sitemap (HYG-07) | Leave it for the operator (deferred-live) | Needs the owner's Google account |

## Hard safety rules

- **Push is authorized** for this run, only after the phase-close gate is green, never mid-phase, never `--force`, and always after `git pull --rebase`. The news cron commits to `data/incidents/` ~4x/day, so expect rebases.
- **Deploy:** dispatching `deploy-manual.yml` is authorized after a green gate when the change touches served data/pages. After every deploy, curl prod and verify.
- **`data/` mutation is allowed ONLY for:** the NREC-09 backfill (dry-run + backup first), the pipeline's own normal runs, and HYG-06 CEAD output. Any other edit to `data/` → pause.
- **External LLM spend cap: USD 10 for the whole run** (estimate: A/B ~0.2, backfill ~0.3-1.4, FID-02 eval ~0.2). Track it in RUN STATUS. Over USD 5 in one phase, or over the run cap → pause.
- **Never:** git-history rewrite/purge, deleting branches or tags, changing repo secrets or settings, closing Dependabot PRs without a written reason, touching the locked decisions (CEAD FAMILY_KEYS=7, national_rank #1 = most reported, no heat maps or severity scores, no LLM clustering).
- Never write "seguro/peligroso" in absolute terms; always attribute CEAD and the outlet.
- Build hygiene: the repo lives in OneDrive, so chain build + validate (+ vitest) in ONE command. `npm run validate` does not run vitest. Count pytest results by exit code: a `*failed*` grep also matches `xfailed`.
- **Close gate per phase:** measure the baseline at the start of Phase 34 (pytest / vitest / validators / `astro check` / `bash .github/scripts/lint-workflows.sh`) and record it here. A phase closes only at ≥ baseline plus its own new tests. `freshness` red is now a **real** signal; do not exclude it.

## Pause points (stop, write the reason in RUN STATUS, and end with a clear question for the owner)

1. NO-GO after 2 gate cycles.
2. No A/B candidate meets the thresholds.
3. The DeepSeek backup account is unfunded AND the primary is failing.
4. The spend cap would be exceeded.
5. An irreversible or public-facing action outside the defaults above.
6. Prod is still not classifying on 2026-10-03, even after the fast path. **Say so loudly**: pages empty on 10-05.

## Decision log

_(append G-01, G-02, … — each: date, decision, evidence, who decided [arbiter/orchestrator/default])_

- **G-01** (2026-09-22, orchestrator) — Phase-34 artifacts live in `.planning/phases/34-news-classification-restore/` (phases/ was empty after the v2.1 archive). Close-gate baseline recorded in RUN STATUS above; a phase closes only at ≥ that baseline plus its own new tests.
- **G-02** (2026-09-22, default "decidir por métricas" + orchestrator) — NREC-01 selection rule, pre-declared before any A/B call: population = the 44 labelled items of `pipeline/tests/fixtures/golden_set.json` (+3 null items for the null guard); a candidate QUALIFIES iff commune accuracy ≥ 95.45% AND parse-fail ≤ 2% AND family accuracy ≥ 65.91% AND ≥ 2 live OpenRouter endpoints at switch time AND the run's `reasoning_tokens` are 0 or the answer is never truncated (empty-content count = 0). Among qualifiers: highest family accuracy wins; ties (within 1 item) → more live endpoints, then lower blended price. Reasoning is disabled per request for every candidate (the exact flag is measured in the runner, not assumed). Candidates: deepseek/deepseek-v4-flash, deepseek/deepseek-v4.1-flash, ibm-granite/granite-4.2-8b, qwen/qwen3.5-9b. Zero qualifiers → pause point 2.
- **G-03** (2026-09-22, orchestrator) — Failure knobs: circuit breaker trips after **5 consecutive** API_ERROR outcomes on the primary → failover to DeepSeek direct for the rest of the run; the backup has its own breaker at 5 consecutive → remaining items go to the retry queue ("backup exhausted"). Retry: 429/5xx/connection/timeout → tenacity exponential backoff, max 3 attempts per provider, base 2s, cap 20s; 401/403/404/400 → no retry. Retry queue: `data/incidents/pending.json`, max 5 attempts and max age 14 days per item, after which the item is recorded in rejected/ with stage `api_error_expired` (never `classifier_none`).
- **G-04** (2026-09-22, orchestrator) — Health gate (NREC-07), evaluated post-commit on the run summary: "responded" = items for which a provider returned a parseable model answer (OK or genuine rejection). The job FAILS when (a) attempted > 0 and responded == 0, or (b) attempted ≥ 10 and api_errors/attempted ≥ 0.5, or (c) backup exhausted. Failover to the backup alone does NOT fail the job but is logged and counted.
- **G-05** (2026-09-22, orchestrator) — Freshness instrument (NREC-08): new field `last_new_incident_at` (UTC ISO, set only when the incident set gains ≥1 new id) in `current.json`; heartbeat and `freshness.mjs` fail when now − `last_new_incident_at` > 48h, falling back to max(incident.date)+1 day when the field is absent. Fixture: generated=now, last_new_incident_at/max(date)=now−3d → both fail.
- **G-06** (2026-09-22, orchestrator, accepting planner knob objections 1-4, 6, 8) — G-02 thresholds are compared as counts on 44 labelled items: commune ≥ 42/44, family ≥ 29/44, parse-fail 0/47. The NREC-09 population is selected by parsed datetime (not string compare): 2,073 rows as of the audit, growing until the fix ships; the check is that all audit-measured rows are included. The "no literal granite-4.1-8b" rule applies to `pipeline/**/*.py`; the NREC-03 fixture and `clustering_golden_set.json` provenance may keep it. G-05 fixtures use a noon as-of time plus exact-boundary tests (48h pass, 48h+1s fail). Fail-closed additions accepted: health gate fails on missing/malformed summary; heartbeat fails on no evidence. `data/SOURCES.md` wording and `pending.json` public exposure are deferred to Phase 38 (HYG-01/HYG-05).
- **G-07** (2026-09-22, orchestrator, planner knob objection 5 + 7) — (a) The backfill must NOT bump `last_new_incident_at`; only live pipeline runs do, so a backfill cannot mask a live outage. (b) Phase 34 pushes three times (34-04 ship, 34-05 backfill data, 34-06 close), each behind the full close gate + `git pull --rebase`, never `--force`. The directive's own step 4 ("push + verify live runs") requires a mid-phase push; this is read as authorized, not a violation.
- **G-08** (2026-09-22, orchestrator, premortem R-01 P0) — Deadline-first handling of pause point 2. If the A/B yields NO_QUALIFIER before 2026-10-03, the run does NOT stop there: the question is written to RUN STATUS/deferred-live and a deterministic fallback applies. FALLBACK_WINNER = candidate with ≥2 endpoints, parse_errors ≤1/47 (after the prod-identical empty-content re-call) and commune ≥41/44, ordered by parse asc, commune desc, family desc (on golden_set_v2, G-10), endpoints desc. If none qualifies, primary = DeepSeek direct (`NEWS_PROVIDER=deepseek`, the directive's pre-authorized fast-path model family). Pause point 1 (NO-GO after 2 gate cycles) stays a real pause, but if prod is not classifying at that moment the directive's fast-path is shipped first (it is `/gsd:quick` + single opus validation, not gated), then the run pauses. Reason: the directive's fast-path already pre-authorizes `deepseek/deepseek-v4-flash`, and an absent owner must not turn a metrics tie into an empty /news/ on 10-05.
- **G-09** (2026-09-22, orchestrator, premortem R-03/R-04, amends G-03/G-04) — Empty content (after one re-call) and `finish_reason=="length"` are transient → API_ERROR (pending, never seen). Primary breaker trip re-dispatches the tripping streak to the backup in the same run. Health gate adds: (d) attempted ≥10 and accepted == 0 → fail (NREC-07 literal); (e) attempted ≥10 and parse_errors/attempted ≥ 0.20 → fail; (f) expired > 0 → fail. Run time budget `NEWS_RUN_BUDGET_S`=1200 s; exhausted → items queued without attempt increment, warning not fail. A run counts toward NREC-10 only with failovers == 0 (or a diagnosed reason).
- **G-10** (2026-09-22, orchestrator, premortem R-05) — The golden set has zero `robos_violentos` labels, so the family metric rewards the V-07 catch-all. A fresh opus reviewer relabels, offline and item by item with a reason, the violent-robbery candidates into a NEW `pipeline/tests/fixtures/golden_set_v2.json` (golden_set.json stays as Phase-16 provenance). The G-02/G-06 family threshold becomes the Phase-16 DeepSeek baseline RE-SCORED on v2 from the stored per-item predictions (`pipeline/results/ab_deepseek.json`), as a count; commune and parse thresholds unchanged. This anticipates part of FID-02 (Phase 36) but does not close it.
- **G-11** (2026-09-22, orchestrator, premortem R-06/R-07) — Backfill quality gate before apply: stratified audit of 50 OK items (≥20 Google-News title-only, ≥10 with non-Chile location cues, rest random) by a fresh opus reviewer; pass ≥ 43/50 (86%, aligned with the FID-03 ≥85% default). A failing stratum is withheld (stays classifier_none, not published) and 30 more items of the rest are re-audited. Backfill spend rule: typical = N × measured per-item cost from 34-01; stop only if typical > (5.00 − phase spend so far); on hitting the cap, apply the partial cache ordered by date so 2026-09-05..09-22 is covered first.
- **G-12** (2026-09-22, orchestrator, gate R1 BF-06) — There is NO "immediate fast-path". A G-08 fallback outcome (FALLBACK_WINNER or DEEPSEEK_DIRECT) simply flows through the normal 34-02→34-04 path with that model. The directive's fast-path fires only on its own trigger: prod not classifying on 2026-09-30 (or at pause point 1). If it fires, it is built from a branch based on `origin/master` (so unpushed, ungated commits are not dragged to prod), and it uses `NEWS_PROVIDER`/`NEWS_MODEL` via `model_config` if 34-02 has landed, else the directive's literal minimal hotfix.
- **G-13** (2026-09-22, orchestrator, gate R1 BF-05, amends G-02/G-08 to match G-09) — In the A/B runner, `finish_reason=="length"` and empty content after the single re-call are counted as `transient` (a subset of api_errors) and are NOT parsed. Any candidate with finish_length_count > 0 or empty_content_count > 0 is disqualified in BOTH tiers (strict G-02 and G-08 fallback), whatever reasoning_tokens reports, because in prod those items would expire (G-09 f). `parse_errors` = items whose non-empty, non-truncated content fails `_parse_content`; the G-06/G-08 parse limits (0/47 strict, ≤1/47 fallback) count only these.
- **G-14** (2026-09-22, orchestrator, gate R2 BF2-02; refines G-11 without tightening it) — (i) A stratum "fails" when its agreement count < ceil(0.86·n_s); full publication therefore needs every stratum to pass AND the overall ≥ 43/50 (in practice ≈ ≥45/50). (ii) A failing stratum — including `random` — is withheld (stays classifier_none, not published) and 30 more items drawn from the NOT-withheld remainder are re-audited. (iii) The 30-item re-audit passes at ≥ 26/30 overall (no per-stratum threshold). If the re-audit fails, nothing more is published from that remainder: it is withheld and reported, and the owner question goes to deferred-live. If everything ends up withheld, 34-05 Task 3 is skipped (no apply, no data commit, no push, no R2), NREC-09 residual = selected, and NREC-10 is reported Partial with the uncovered days.
- **G-15** (2026-09-22, orchestrator; G-10 executed) — Blind relabel by a fresh opus reviewer (taxonomy from `pipeline/cache/cead_page.html` CEAD group catalog: 201/20102/202 → robos_violentos; 6xx → propiedad; 50102 explosives → armas) changed 11 families in `pipeline/tests/fixtures/golden_set_v2.json`: gs-001, 002, 003, 026, 033 vida→robos_violentos; gs-012, 013, 021, 029, 036 propiedad→robos_violentos; gs-046 propiedad→armas (all 9 pre-listed candidates + 2 found blind). Re-score of stored per-item predictions over the 44 labelled ids (instrument: python over `pipeline/results/ab_deepseek.json` and spike-008 `results_granite_golden.json`, family_correct = status ok ∧ predicted == v2 label): **DeepSeek v4-flash Phase-16 = 40/44 on v2** (29 on v1), commune 42/44 (consistency check passed); Granite 4.1 = 38/44 (informational), commune 44. ⇒ **FAMILY_MIN_V2 = 40/44**. The strict tier sits on DeepSeek's own mark (knife edge by design); the G-08 fallback is an expected route, not an anomaly. Reviewer flag for the owner: gs-032 (UNESCO statement on Valparaíso fires) is arguably a non-crime item; kept as propiedad because nulling it would change commune fields (out of scope; FID-02 in Phase 36).
- **G-16** (2026-09-23 02:45Z, mechanical `select_winner` under G-02/G-06/G-10/G-13; NREC-01) — A/B on golden_set_v2 (44 labelled + 3 null), instrument `pipeline/experiments/eval_classifier.py`, results `.planning/phases/34-news-classification-restore/34-AB-RESULTS.{json,md}` + `ab/`. Reasoning variant measured per model: `{"reasoning":{"enabled":false}}` for all 4 OpenRouter candidates, `{"thinking":{"type":"disabled"}}` for DeepSeek direct; reasoning_tokens_total = 0, empty = 0, finish_length = 0, api_errors = 0 on every run. Table (commune/family/parse, endpoints): **deepseek/deepseek-v4.1-flash 44/44 · 42/44 · 0/47 · 24 ep → WINNER (strict tier, sole max family)**; deepseek/deepseek-v4-flash 42 · 39 · 0 · 15 (fails family ≥40); granite-4.2-8b 42 · 36 · 1 · 2 (fails family, parse); qwen3.5-9b 44 · 40 · 3 · 6 (fails parse); DeepSeek direct backup (informational) 44 · 42 · 0, non-transient api_errors 0 → NREC-04 funded/reachable evidence. Served-by for the winner: CoreWeave, DeepInfra, Sail Research, Together. Measured cost: winner $0.010017 / 47 items ≈ $0.000213 per item, mean latency 2.48 s. **A/B spend $0.111128** (cap 0.50; direct row ceiling-priced). No tie.
- **G-17** (2026-09-23, orchestrator) — 34-05 Task 2 steps 1-7b (backup, dry-run, classify into the scratchpad cache, review, G-11 audit) start while 34-04 Task 2 waits for a scheduled run with classified ≥ 1. Rationale: the classify phase writes nothing under data/ (tool refuses cache paths inside data/), so it cannot race the cron or publish anything, and it takes ~90 min; the population is already frozen (post-push rejections carry real stages — measured: the 49 classifier_none rows of 2026-09-23 all have first_seen 03:05:13Z, from the pre-push run on e565ce8; rows after 03:20Z are low_confidence). The APPLY (34-05 Task 3) stays blocked until 34-04 Task 2 is green. Dry-run 2026-09-23: selected 2,122 (frozen_subset 2,073 ✓), would_archive 1.
- **G-18** (2026-09-23, orchestrator applying G-11/G-14 mechanically; outcome record, not a new rule) — Backfill classify phase: 2,122 rows → ok 1,162 · not_crime 953 · parse_error 5 · api_error 2; spend USD 0.41. G-11 audit (fresh opus, seed 3405): gnews_title_only 17/20 (needs 18), non_chile_cue 7/10 (needs 9), random 14/20 (needs 18), overall 38/50 (needs 43); q1 "crime in Chile" alone 40/50. All strata fail → all withheld → **no-publication; 34-05 Task 3 skipped** (no apply/commit/push/R2/disclosure). NREC-09 = Partial (residual 2,122, still `classifier_none` in `rejected/2026-09.json`). Evidence: `.planning/phases/34-news-classification-restore/34-BACKFILL-AUDIT.md`. Consequences: (1) the same non-crime acceptance (~20%, accidents/natural deaths/fires labelled vida) is live in production since 8095928 — pre-existing V-07 defect, scope of Phase 36 FID-02/FID-03; (2) deferred-live + Phase 36 hand-off: after FID-02 tightens the non-crime rule, re-run `backfill_classifier_outage.py --classify` on a fresh cache (≈USD 0.4) and re-audit under G-14 to publish the rows still inside the 30-day window. /news/ does not empty on 2026-10-05 because live classification is restored (34-04/34-06).
- **G-19** (2026-09-23, orchestrator) — Phase 34's remaining close evidence is wall-clock bound (34-04 Task 2 needs a scheduled run with classified ≥ 1; 34-06 needs 3 consecutive scheduled runs ≈ 18-24 h). During that wait, Phase 35 research, planning, premortem and gate (documents only) proceed. No Phase 35 code is executed, committed or pushed until Phase 34 is closed (verification passed, STATE/ROADMAP updated). Reading of "one phase at a time": execution and close are serialized; paperwork is not.
- **G-20** (2026-09-23, orchestrator accepting the Phase-35 planner's knobs; plans 35-01..35-07) — (1) The "Updated <build date>" stamp is REMOVED from /news/ and /es/noticias/ and replaced by the data-derived "Latest incident: <newest incident date>" line. (2) Stale notice when the shared G-05 evidence rule (one helper `site/src/lib/newsEvidence.mjs`, also imported by freshness.mjs) is > 48h at build time; now = build clock (`data-build-now`). (3) Commune caveat at > 168h using that commune's newest listed item (date + 1 day) vs the build clock. (4) Histogram starts at the earliest valid incident date in current.json; 30-day cap kept. (5) store.py skips the write when incidents, window_days and last_new_incident_at are unchanged (no unknown keys); archive months skip when no new id. (6) Deploy only when the staged diff touches `data/incidents/current.json` or `data/incidents/archive/`; seen/rejected/pending still commit but do not deploy. (7) heartbeat.yml calls the existing deploy hook when the news heartbeat check fails (≤1/day, only while stale) so stale notices reach prod during an outage; reported separately (H), not counted in FRESH-04's inequality. (8) FRESH-04 measured D ≤ C and G = 0 over 4 scheduled runs at close; the 7-day check is scheduled at SHIP+7d and FRESH-04 stays unchecked until it passes. (9) Freshness red at close holds the phase (never excluded); >24h red → Phase-34 gap plan. (10) Partial-year label claims no month cutoff: "only what CEAD had published by <last_updated>". (11) Frozen fixture = 40 newest incidents of commit f5761ea (newest 2026-09-04, no last_new_incident_at). (12) Validators 16 → 18 (cead-vintage #17, news-freshness #18); the two-build harness runs only at the close gate.
- **G-21** (2026-09-23, orchestrator accepting Phase-35 premortem knobs) — (1) Phase 35 plans execute one at a time (only 35-01 ∥ 35-02 in wave 1; 35-02 never builds site/) — shared site/public/data and site/dist on OneDrive. (2) COVERAGE_GAPS = [{from 2026-09-05, to 2026-09-22, source "G-18 classifier outage"}]; a day is a gap only inside that range with 0 incidents; the entry is removed only if the withheld backfill is published. (3) Gap copy: bar label "no data collected" / "sin datos recolectados"; caption "No incidents were collected from {from} to {to} because of a classification outage in our pipeline; those days are not zero-incident days." / "No se recolectaron incidentes entre el {from} y el {to} por una falla de clasificación en nuestro pipeline; esos días no son días sin incidentes." (4) Visitor notices: news + home on verdict stale|none; commune caveat only on stale; "future" never shows a notice. (5) Home pulse reuses `news_stale_notice`, rendered statically. (6) deploy-on-code also triggers on human pushes touching data/incidents/current.json, data/incidents/archive/**, data/cead/** (bot pushes use GITHUB_TOKEN → no loop).
- **G-22** (2026-09-23, orchestrator, Phase-35 gate R1 conditions 4, 6, 7; amends G-21(3) and G-20(8)) — (a) Gap caption dates {from}/{to} come from each contiguous span of buckets with gap=true (one caption per span), never from the declared COVERAGE_GAPS range; the validator asserts every captioned date is a gap bucket. (b) NewsStrip on /map/ and /es/mapa/ is IN scope (option a): clipped to max(anchor−(windowDays−1), earliest unfiltered incident date), gap days marked, pure helper + vitest — so "every recency signal" stays true. (c) FRESH-04 live no-op proof: up to K = 4 induced dispatches (queued while a scheduled run is running, or ≤ 15 min after it ends, preferring 06-12 UTC). If ≥ 1 is a no-op → proven live. If none → interim PASS with residual "no-op path proven by pytest only (test_store byte-identity + test_news_deploy_gate)", and the 7-day check carries the same induction procedure. D ≤ C and G = 0 are declared non-discriminating under healthy flow (116/116 bot commits added ids, measured 2026-08-05..09-04); the deploy gate counts as live-proven only if an observed no-op committed seen/rejected and its deploy step was skipped.
- **G-23** (2026-09-23, orchestrator, Phase-35 gate R1 non-blocker A-R1-08; amends G-21(2)) — COVERAGE_GAPS ranges are never removed: a day inside a declared range that has ≥ 1 incident is not a gap by construction, so the declaration stays true even if the backfill is later published.
- **G-24** (2026-09-23, orchestrator; extends G-19) — While Phase 34 waits on scheduled-run evidence, Phase 36 RESEARCH (read-only, documents only) may run. Phase 36 planning waits until Phase 35 code has executed, because both touch the news pages (35-04 vs FID-01 headline display) and a plan's file:line evidence would go stale.
- **G-25** (2026-09-23, orchestrator; outcome record) — 34-04 closed: push 8095928 (+ CR-01 fix); dispatch 35814941600 (responded 2/2, preflight ok, 0×404); dispatch 35826563848 (U=7, attempted 7, accepted 4, genuine rejects 3, api_errors 0, failovers 0, 0×404, health gate success; prod served 746 incidents newest 2026-09-23 on /news/ and /es/noticias/; freshness PASS on last_new_incident_at 2026-09-23T06:25:01Z); CI ci.yml 35826777865 3/3 green; forced-backup dispatch 35835020397 (failover_reason forced, responded 2/2, api_errors 0, backup_exhausted false, served by DeepSeek direct → Actions DEEPSEEK_API_KEY funded and reachable, NREC-04 live). Phase 37 research (read-only) also proceeds under G-24's logic.
- **G-26** (2026-09-23, orchestrator; outcome record + pause) — 34-06 Task 1 PASSED (3 consecutive scheduled runs). Task 2 step 3: fresh Opus reviewer over 123 pattern hits → 103 OK / 1 V06_KINSHIP (63fb0c3ef0adca19, verified vs source) / 0 V06_SUICIDE / 19 OTHER_DEFECT; verdicts in phases/34-news-classification-restore/34-06-HITS-VERDICTS.json. Per plan 'any hit → STOP' and data/ rule → pause point 5; Phase 34 close-out (REQUIREMENTS/STATE/ROADMAP) held until the owner answers.
- **G-27** (2026-09-24, owner-delegated → orchestrator; answers pause point 5) — Owner: "continúa con lo que consideres más inteligente". Options weighed: (A) drop 63fb0c3ef0adca19; (B) retitle it — rejected, an editorial rewrite contradicts the verbatim-headline owner decision; (C) defer to Phase 36 FID-07 — rejected, leaves a false factual claim about a minor live for days. Chosen A: 5 correct sibling cards (4902e5aa, d79d809e, 16373e92, 81731f4c, df5a205c) cover the same event, so zero information loss; reversible via git. seen.json/rejected untouched (URL present in seen.json → never re-added); archive/*.json had no copy. Mirrors quick-260922-t59. current.json 799→798; build + validators 16/16 green.
- **G-28** (2026-09-25, orchestrator accepting the Phase-36 planner's knobs; FID-01 contract) — IncidentRecord gains optional `title_src` and `via_url`. `title_src` = outlet headline, entity-decoded, whitespace-collapsed, one trailing " - <outlet>" removed (present on 2,212/2,212 Google-News rows in rejected/). New rows set `title_es = title_src` (one field for the 6 site consumers, dedup and R2 citations); ClassifierOutput drops `title_es`; the classifier input stays the raw feed title. `title_en` passes a deterministic kinship guard; on mismatch it becomes `title_src`, shown in EN as "Original Spanish headline (not translated)". EN cards always carry "Machine-translated headline"; legacy ES cards without `title_src` carry "Titular generado automáticamente" until they age out (≤30 days). Golden sets: v2 frozen (Phase-34 provenance, DEPS-03 re-run), v3 = Phase-36 gate set, growth → v4.
- **G-29** (2026-09-25, orchestrator) — Editorial filter: a new incident whose `title_src` or `title_en` contains a term from `forbidden-language.mjs` is not published; it goes to rejected/ with stage `editorial_filter`. The Python list is pinned to the JS list by a test. Measured frequency 2/4,427 raw titles.
- **G-30** (2026-09-25, orchestrator) — `merge_and_write` applies the existing 0.82 (cut, date) rule over existing current.json rows + new ones, first-seen wins; archive months only block new additions and are never pruned. Pruning existing rows ships only if a fresh-opus review of the drop list finds 0 false merges and 514872e1d06db7f2 is not dropped; otherwise `PRUNE_EXISTING=False` and FID-05 is Partial.
- **G-31** (2026-09-25, orchestrator) — After a successful Google-News decode, `url` = publisher URL and id = sha256(publisher URL); `via_url` keeps the Google link; seen records both; pending carries via_url; queued items are never re-decoded; seen/pending checks run on the Google link before any network call. Backfill URLs are not decoded (NREC-09 "without refetching").
- **G-32** (2026-09-25, orchestrator) — FID-02 gate on golden_set_v3 with deepseek/deepseek-v4.1-flash: non-crime rejection ≥ 0.80; commune ≥ 42/44 and family ≥ 40/44 on the 44 v2 items; parse/empty/truncation 0; v2 null items 3/3; boundary family ≥ the 36-02 baseline; set composition ≥20 non-crime (accident ≥6, suicide ≥3, non-crime death ≥3, fire/emergency ≥2, institutional ≥6) + ≥8 boundary; a pytest forbids 5-word overlap prompt↔golden; max 3 prompt iterations, else ship the headline change only and pause. FID-03: q1∧q3 agreement ≥ 43/50 (ceil(0.85·50), directive default); "vida not a catch-all" = ≥ ceil(0.85·n) of predicted-vida items truly vida; vida share reported vs 51% (V-07) / 51.4% (G-18), not gated; seed 3608; population = offline replay of the 14-day stored stream (rejected/) minus golden-set sources. FID-04: decode rate on n=50 (seed 3607); news.google.com share among live-added incidents ≤ 43% with n ≥ 20 within 8 runs; decode budget 60 items / 180 s per run, 1.5 s delay, 10 s timeout. Spend caps per plan 0.10 / 0.25 / 0.60 / 1.00 USD (expected ≈ USD 1.0 total).
- **G-33** (reserved for 36-08) — records the FID-03 outcome; owner default applied: no Granite-era re-classification this milestone.
- **G-34** (2026-09-25, orchestrator; G-07(b) reading) — Phase 36 pushes up to three times (ship 36-09, backfill data 36-10 only if its audit passes, close 36-11), each behind the full gate + `git pull --rebase`, never --force. FID-01/04/05 can only be shown on live runs.
- **G-35** (2026-09-25, orchestrator; answers the planner's open questions under the owner's delegation of 2026-09-24) — (1) FID-03 by offline replay accepted (a live audit would push the close ≥14 days). (2) Pruning the ~64 published near-duplicate cards is allowed ONLY through the G-30 review gate (0 false merges) — reversible via git, removes duplicates rather than facts. (3) Editorial filter G-29 accepted over exempting quoted text (the project rule "never absolute seguro/peligroso" applies to what we publish, verbatim or not). (4) aef6f3680c904f4c (workplace accident shown under vida) is NOT hand-removed: unlike G-27 it makes no false claim about a person, and it ages out ~2026-10-23; Phase-36 classifier fixes the class forward. (5) ES label "Titular generado automáticamente" accepted as honest disclosure. (6) FID-04 Partial allowed if Google blocks decoding from runners, with the measured rate recorded.
- **G-36** (2026-09-25, orchestrator under the owner's 2026-09-24 delegation; premortem R-01, amends G-28 with G-35(5) logic) — "Machine-translated headline" applies ONLY to rows with a source headline (`title_src`). Legacy EN cards without `title_src` carry "Automatically generated headline" (their title_en translates the classifier's own Spanish headline, classifier.py:176-177, not the outlet's); they age out in ≤30 days. EN cards where the kinship guard fell back carry "Original Spanish headline (not translated)". Every EN card carries exactly one disclosure label. Checks: 36-03 (dist all-legacy: AG == cards, MT == 0); 36-09/36-11 (EN cards == MT + OS + AG; AG == served rows without title_src). Reversible by copy only.
- **G-37** (2026-09-25, orchestrator; premortem R-03 + R-02, amends G-32 FID-04) — FID-04_live PASS needs BOTH (a) live decode rate = Σdecoded/Σ(decoded+failed) from run-summary `gnews_decode`, Σ attempts ≥ 20, ≥ `live_decode_floor` = min(0.70, residential success_rate of 36-GNEWS-DECODE − 0.20), computed mechanically in 36-07 Task 1; and (b) news.google.com share of `url` among live-added incidents ≤ 43% with n ≥ 20. Live-added = ids absent from S0 (36-S0-IDS.txt) and not in 36-BACKFILL-IDS.txt; the backfill's Google share is reported ungated (G-31). If (a) fails because Google blocks runner decoding → FID-04 PARTIAL per G-35(6) with the rate recorded. Reason: new-model share is already 48/104 = 46.2%, so (b) alone passes with zero decoding P ≈ 0.31-0.37.
- **G-38** (2026-09-25, orchestrator; premortem R-05, amends G-32 FID-02) — contested_v2 = {gs-030, gs-032, gs-038} (34-GOLDEN-V2-RELABEL.md:57-60, "arguably non-crime"). The FID-02 v2 gate runs on the 41 uncontested labelled v2 ids: commune ≥ 39/41 and family ≥ 37/41 (same allowed misses as 42/44 G-06 and 40/44 G-15). The 3 contested items are reported per id, never gated. Pin: G-16 run scores 41/41 and 39/41 (measured). golden_set_v2.json untouched; FAMILY_MIN_V2 = 40 unchanged.
- **G-39** (2026-09-25, orchestrator; premortem R-06, amends G-32 composition) — Per-cue availability is counted and recorded before labelling. A not_crime category that cannot reach its nominal quota from stored inputs (measured: suicide cue 6 rows, ≤2 non-crime suicides, none in Chile) is filled with not_crime items of other categories (death_no_crime first), keeping total not_crime ≥ 20; shortfalls and effective quotas recorded in 36-GOLDEN-V3.md; per-category rates reported only when n ≥ 3. STOP (pause question) only if total not_crime < 20 or boundary < 8. Text is never synthesized.
- **G-40** (2026-09-25, orchestrator; premortem R-17, precedent G-17) — 36-10 Tasks 1-2 (dry-run, fresh classify, review, G-11/G-14 audit; zero writes under data/, cache in scratchpad) start as soon as 36-LIVE-EVIDENCE.md records a ship_sha on origin/master, concurrently with 36-09 Task 3's live wait. 36-10 Task 3 (apply/push) waits for 36-09's `first_post_ship_run` line with 0 drops (full, or forward-only under PRUNE_EXISTING=False) and #19 registered or its False-branch line recorded. Reason: each day after ~10-04 sends 87-148 backfill rows straight to archive.
- **G-41** (2026-09-25, orchestrator; premortem R-18) — DEPS-03 (Phase 37) baseline = `.planning/phases/36-classification-fidelity-attribution/36-DEPS03-BASELINE.json`: v2-subset metrics (44 labelled + 3 null: commune, family, parse, null_correct, served_by_counts) of the winning 36-06 iteration = prompt at the 36-09 ship SHA. Not 34-AB-RESULTS.json, because the Phase-36 prompt changes v2 results by design.
- **G-42** (2026-09-25, orchestrator; premortem R-09/R-10, narrows G-32 FID-03) — FID-03 population: as-of A = now, stepping back one day at a time while fewer than 50 items would publish; event siblings (headline ratio ≥ 0.60 vs any v3 golden headline) excluded; `pool_by_epoch` and `as_of_final` recorded. Audit is two-pass (q3 from pass 1 before the reviewer sees the prediction), each verdict carries a `basis`, agreement reported per basis. 36-08 decodes Google URLs with the existing `gnews_decoder.decode_gnews_url` (http(s), host ≠ news.google.com) because 36-07's resolver is in the same wave.
- **G-43** (2026-09-25, orchestrator; gate R1 BF-03, amends G-42) — 36-08 decodes Google URLs with the existing `gnews_decoder.decode_gnews_url` WITH a requests.Session (new-format needs the network): User-Agent = fulltext.USER_AGENT, timeout 10 s, 1.5 s between calls; accepted only if http(s) and host ≠ news.google.com; `publisher_url_decoded: d/n_google` recorded. In 36-10 Task 2 the decoded URL is always given to the G-11 auditor and agreement is reported per basis; the G-11/G-14 gate is unchanged. Reason: without a session 0/2,212 Google URLs in rejected/ and 0/426 in current.json decode (measured by the gate arbiter).
- **G-44** (2026-09-25, orchestrator; gate R1 NB-05, closes R-03, amends G-37) — If the residential decode rate X/50 in 36-GNEWS-DECODE is < 0.50 → `live_decode_floor: n/a (decoder ineffective)` and FID-04 is PARTIAL "decoder ineffective" with the rates recorded; otherwise live_decode_floor = min(0.70, X/50 − 0.20) (≥ 0.30).
- **G-45** (2026-09-25, orchestrator; gate R1 BF-07 + NB-02, amends G-32 spend caps and G-41) — 36-08 plan cap USD 0.60 → 0.90. A failed FID-03 audit's extra iteration re-classifies the pool into a NEW cache (`fid03-cache-iter{k}.jsonl`), records the classifier.py blob (`git hash-object`) and asserts it equals the prompt that ships (≈ USD 0.48 + back-off); phase cap USD 5 unchanged. If that iteration passes, 36-DEPS03-BASELINE.json is re-extracted from it (key `classifier_blob`) and 36-09 asserts `classifier_blob` == classifier.py at the ship HEAD.
- **G-46** (2026-09-25, orchestrator accepting the Phase-37 planner; owner default "Enable CodeQL" vs hard rule "never change repo settings") — CodeQL via advanced setup only: committed `.github/workflows/codeql.yml` (push/PR to master + weekly schedule; javascript-typescript + python; build-mode none; top-level `permissions: {}`; job `contents: read` + `security-events: write`; `persist-credentials: false`; SHA-pinned; clean under lint-workflows, check-sha-pins, zizmor and secret-hygiene). The Settings toggle and default-setup API are never used. If the owner prefers default setup later, codeql.yml is deleted in the same change.
- **G-47** (2026-09-25, orchestrator; merge authority) — "No auto-merge" = no `--auto`, no auto-merge workflow, no bots. An agent merges a Dependabot PR only via 37-merge-pr.sh after triage: author Dependabot, OPEN + MERGEABLE, every check SUCCESS on the head, then `gh pr merge N --squash --match-head-commit <head>` (no `--admin`, no `--delete-branch`); after each merge `git pull --rebase`, full local gate, push (mid-phase pushes as G-07(b)/G-34). Rollback = `git revert`, never force-push. Closing a PR always posts a written reason first.
- **G-48** (2026-09-25, orchestrator; DEPS-03, extends G-41) — Run B = master's classifier.py with PR #37's openai version in a scratch venv, on golden_set_v2 with the baseline's provider/model/reasoning variant. MERGE iff the four counts (commune, family, parse, null) equal 36-DEPS03-BASELINE.json exactly, error counts 0, pytest passes under 3.x and PR #37's CI is green; otherwise one control run A (openai 2.53.0) and decide per G-50. `classifier_blob` must equal classifier.py at HEAD before any spend; served_by reported, never compared. Cap USD 0.05.
- **G-49** (2026-09-25, orchestrator; DEPS-01/02 thresholds) — astro ≥ 7.2.8 (GHSA-26w7-cxv4-gfx2: `astro < 7.2.8`, RCE via libheif when Astro optimizes an UNTRUSTED AVIF; this static site only optimizes its own repo images at build, so live exposure is nil — the phase order 34→38 is kept, no expedite), 0 critical in `npm audit --omit=dev`, alert #22 fixed; ≤3 open Dependabot PRs none older than 14 days at 37-07 close; PRs opened during the phase triaged under the same rules, max 2 passes, else FAIL + gap plan; leftover advisories only via `npm audit fix` without `--force` and only if the diff is lockfile-only; major-only fixes → deferred-live.
- **G-50** (2026-09-25, orchestrator; answers the Phase-37 planner's open question 1) — If run B mismatches the baseline AND control A (openai 2.53.0, same day, same provider) ALSO mismatches the baseline, the drift is the provider's, not openai's: DEPS-03 then compares B to A and MERGEs iff B == A on the four counts with errors 0 (both runs recorded). If A matches the baseline and B does not → CLOSE #37 with the numbers. At most 2 runs of each (cap USD 0.05 unchanged).
- **G-51** (2026-09-25, orchestrator; Phase-37 premortem R-01 P0 + owner-question (c)) — `37-served-routes.sh SHA` resolves D = newest non-`[skip ci]` commit ≤ SHA touching the deploy-on-code paths; requires the deploy-on-code run for D = success AND `data-build-now` on /news/ and /es/noticias/ ≥ that run's createdAt; keeps the 8 routes, 404, MapIsland and "Latest incident". Exit: 1 route fail, 10 stale build, 11 run missing/failed, 12 pending; polling cap 30 min. At the cap with exit 10/11: NO revert → pause point 5 asking the owner to read the Cloudflare build log (the agent cannot). Revert only on a fresh fingerprint + route failure.
- **G-52** (2026-09-25, orchestrator; R-06, amends G-49 tooling) — Leftover advisories: `npm audit fix --dry-run` first; if it crashes (measured: rc=1 "edgesOut"), `npm update <pkg> --package-lock-only`, then a within-range `npm install <pkg>@<patched> --package-lock-only` for transitives; diff must be lockfile-only; gate green + G-51 before a residual counts as fixed; else log `residual <pkg> <GHSA> fixable-but-tooling-crash` → deferred-live.
- **G-53** (2026-09-25, orchestrator; R-02/R-03/R-09, amends G-47) — 37-merge-pr.sh exits: 3 conflicting, 4 failed conclusion, 5 pending/UNKNOWN, 6 stale base (→ `@dependabot rebase`), 7 major, 8 dirty tree, 9 not eligible. Exit 4 is classified before any close; a PR is never closed for being pending. Pulls use `--rebase --autostash`.
- **G-54** (2026-09-25, orchestrator; R-05) — Revert only when a BUILD/VITEST/ASTRO/PYTEST or structural-validator stage that was green turns red. INSTALL red (e.g. OneDrive EPERM): retry once, then pause. freshness/news-freshness red: stop the merge queue and record a news incident — no revert, no PR close.
- **G-55** (2026-09-25, orchestrator under the owner's 2026-09-24 delegation; owner-question (a)) — Within the run only patch/minor and security-fix Dependabot PRs are merged. Major-version PRs (TypeScript 7, @astrojs/react 7, vitest 5, …) are closed with "major upgrade reserved for owner review; reopen or @dependabot recreate when ready" and listed in deferred-live; no `@dependabot ignore`. openai 3.x only through DEPS-03 (`--allow-major`). DEPS-02 is measured after this triage.
- **G-56** (2026-09-25, orchestrator; owner-question (b)) — After the run the repo owner performs the weekly Dependabot triage, starting the first Monday after max(37-06 execution date, 2026-10-01); 37-06 creates the issue "Dependabot weekly triage — first check <date>" with the policy checklist; the final report retitles it if the run ends later. No repo settings touched.
- **G-57** (2026-09-25, orchestrator; R-04/R-07/R-11, amends G-48) — The DEPS-03 PR is found by title "openai from 2.53.0" (#37 or its successor), logged as `deps03_pr:`. The venv installs from a copy of requirements.txt with only the openai line changed; `pip check` must be 0; an install failure is a STOP, never a CLOSE; the CLOSE reason names the measured cause (e.g. httpx → httpx2 in openai 3.7.0). The `classifier_blob` check runs right before spend.
- **G-58** (2026-09-25, orchestrator; R-08, amends G-49 timing) — DEPS-02 is measured only after the first Saturday Dependabot run (≥ 15:30Z) following the last 37-02..37-05 merge, with that wave triaged under G-55.
- **G-59** (2026-09-25, orchestrator; outcome of FRESH-04 induction attempt 1, amends G-22(c)) — Attempt 1 (dispatch 36089456308 queued behind scheduled 36089397586) FAILED: a queued run checks out the trigger-time SHA (8a7eff6, before the scheduled run's data commit 9b2eb62), so it re-classified the same 19 items with a stale seen.json (≈ USD 0.005) and the push rebase conflicted → run aborted before committing (no data lost; issue #44 opened and closed with the diagnosis). The "queued while a scheduled run is running" branch of G-22(c) is withdrawn: remaining attempts (≤3) dispatch only AFTER a news run has completed (≤15 min after it ends, preferring 06-12 UTC), never while one is queued/running. Attempt 1 counts toward K=4. Latent defect (any two chained news runs — e.g. a delayed schedule queued behind a dispatch — conflict the same way because checkout defaults to GITHUB_SHA) → deferred-live / Phase 38: checkout `ref: master` (or re-fetch before classify) in news-pipeline.yml, with a test.
- **G-60** (2026-09-25, orchestrator; Phase-37 gate R1 BF-04, amends G-52) — "Diff must be lockfile-only" means `git diff --name-only -- site/` prints exactly `site/package-lock.json`; files outside site/ (e.g. the triage log under .planning/) may be modified and are outside the rule. Any other site/ file changed → `git checkout -- site/` and log `<tool> changed <files>; skipped`. A `residual <pkg> <GHSA> fixable-but-tooling-crash` line is valid only after an `audit_fix_dry_run rc=<non-zero>` line.
- **G-61** (2026-09-25, orchestrator; Phase-37 gate R1 BF-02/BF-05/BF-06, amends G-47 and G-54, applies the hard rule "after every deploy, curl prod and verify") — (a) Before the first Phase-37 merge, 37-gate.sh runs on master as the BASELINE gate: GATE rc=0 required, red → STOP (pre-existing red, merge nothing); logged under `## Baseline gate`; it is G-54's "was green" reference for the first merge. 37-gate.sh parses VALIDATE lines only after "VALIDATION SUITE SUMMARY" and requires exactly 18 (else `STAGE VALIDATE:parse rc=1` = instrument error → STOP, never revert); adds `STAGE VALIDATE rc=<npm run validate exit>` to the GATE conjunction; prints `TOOLCHAIN node= npm= python=` before GATE; the local gate is never evidence of Linux or prod. (b) After EVERY merge touching site/ with a green gate, `37-served-routes.sh <merge sha>` must PASS before the next PR is touched (G-51 polling/cap/rule (d)), logged `served_routes: PASS deployable=<D> built_at=<ts> (#N)`; a rule-(d) revert names that merge; the end-of-batch check stays. (c) The merge-candidate list goes into RUN STATUS before 37-01's first merge; the final report asks the owner to ratify G-47 with that list and the PRs actually merged.
- **G-62** (2026-09-25, orchestrator accepting the Phase-38 planner) — Phase-38 order: every plan starts only after Phase 37 is ticked in ROADMAP, except 38-08 (HYG-06). Plans that build the site run one at a time. 38-08 runs from a worktree of origin/master and commits only `data/cead/**`. Pushes only in 38-08 (data) and 38-09 (close), after `pull --rebase`, never `--force`.
- **G-63** (2026-09-25, orchestrator; HYG-01 owner default) — `data/incidents` is copied to public/ by allowlist (current.json, archive/, centroids.json, cut_list.json). seen.json, rejected/ and pending.json stay in the repo and R2 but are not served; `public-data.mjs` fails on any other file.
- **G-64** (2026-09-25, orchestrator; HYG-02 owner default) — `/map/` title "Interactive Map — Reported Crime by Commune in Chile", `/es/mapa/` "Mapa interactivo — Delitos reportados por comuna en Chile", each with a visually hidden static H1 (full-screen map layout). The two editorial pages are unchanged. No noindex, no redirect.
- **G-65** (2026-09-25, orchestrator; HYG-03) — A seen.json value is the UTC date the URL was first seen, written once. Pruned when max(value, cutover) < run date − 60 days; cutover = the 38-03 commit date. Old entries are never hand-edited; the normal pipeline run applies the rule. (Research correction: feeds.py:236-253 already prunes by publication date, re-classifying >60-day-old items every run.)
- **G-66** (2026-09-25, orchestrator; HYG-04, overrides D-11) — A failing CEAD builder fails the job before the commit step; exactly 1 `continue-on-error: true` remains in cead-scraper.yml (the alert-label step).
- **G-67** (2026-09-25, orchestrator; closes G-59's deferred defect) — `ref: master` on the news-pipeline checkout, pinned by a workflow-guard test.
- **G-68** (2026-09-25, orchestrator) — Editing `data/SOURCES.md` is HYG-05 documentation work (deferred here by G-06), not a data/ mutation. Memory files are edited by the orchestrator.
- **G-69** (2026-09-25, orchestrator; research correction) — The CEAD client cache has no expiry (client.py:196-199), so the 2026-08-06 "byte-identical, no new data" result is withdrawn as evidence. CEAD scraping runs only from a fresh worktree with an empty CEAD cache (≈198 requests, 2.5 s apart); 38-08 commits only if ≥1 commune/region/national figure changed.
- **G-70** (2026-09-25, orchestrator; HYG-07 owner default) — GSC sitemap submission stays with the operator (`https://ischilesafe.com/sitemap-index.xml`); DEPLOYMENT.md gets the exact steps.
- **G-71** (2026-09-25, orchestrator; closes deferred items) — The /news/ histogram is window_days + 1 wide (every stored date has a bar); news-freshness judges against the page's `data-build-now`; the ResultPanel year tooltip and region chart headings derive from the data series.
- **G-72** (2026-09-25, orchestrator under the owner's 2026-09-24 delegation; answers the Phase-38 planner's open questions) — (1) A visually hidden H1 on /map/ is accepted (SEO/a11y served, layout untouched). (2) centroids.json and cut_list.json stay public (CEAD-derived, no third-party text). (3) HYG-06 deploys via deploy-on-code (fires on data/cead/** since 35-02); deploy-manual only as fallback if no deploy-on-code run within 10 min — the requirement's intent (deployed + verified) is met. (4) If the live CEAD scrape is identical, keep the heartbeat's quarterly-boundary check and record the verified-identical scrape in RUN STATUS; a "last live-verified scrape" marker is deferred-live. (5) seen.json growth for ~60 days after cutover is accepted.
- **G-73** (2026-09-25, orchestrator under the owner's 2026-09-24 delegation; answers the owner questions in 38-PREMORTEM) — (1) The G-62 carve-out is WITHDRAWN: 38-08 (HYG-06) runs in normal phase order — only after Phase 37 is ticked and `37-served-routes.sh` exists, on/after 2026-10-01, with no other plan running; 38-09 depends on 38-08. (2) From 2026-10-01 the CEAD heartbeat red (until HYG-06 lands new data, or while CEAD has not published) is an accepted expected signal; 38-08 writes `heartbeat_cead_expected_red:` in RUN STATUS; the "last live-verified scrape" marker stays deferred-live (G-72(4)). (3) Ledgers stay in the public repo (owner default); deferred-live owner item: "seen.json/rejected/ with third-party headlines remain readable on GitHub; move to private storage? (history cannot be purged without a rewrite, which is forbidden)". (4) HYG-02 assigned without GSC data; a 28-day GSC check of "chile crime map" per URL → deferred-live, tied to HYG-07. (5) ABORTED_SUSPECT → no publish; one retry ≥ 24 h later; still suspect → deferred-live owner question with the evidence. (6) 38-07 documents the quarterly local CEAD procedure in DEPLOYMENT.md (fresh worktree, empty CEAD cache, ~198 requests, Cloudflare-log check on a stale build).
- **G-74** (2026-09-25, orchestrator; refines G-66, premortem R-08) — cead-scraper.yml's "Build ENUSC victimization snapshot" step runs only when `hashFiles('pipeline/cache/enusc_vhdv.xlsx') != ''` (absent on Actions → the tracked data/snapshots/enusc_vhdv.json is used). HYG-04 is CI-only; the local path is covered by the DEPLOYMENT.md runbook (builders chained with `&&`). Exactly 1 `continue-on-error` remains.
- **G-75** (2026-09-25, orchestrator; corrects G-63, premortem R-10) — The ledgers stay in the repo (public on GitHub). Only `rejected/` is archived to R2 (archive_r2.py:172-197); seen.json and pending.json are not. G-63's "stay in the repo and R2" is read that way.
- **G-76** (2026-09-25, orchestrator; Phase-38 gate R1 NB-15) — When Phase 38 closes, the orchestrator runs `/gsd:audit-milestone` for v2.2 (read-only audit against the original intent) and includes its verdict in the final report. `/gsd:complete-milestone` (archiving v2.2) is left to the owner.

## Deferred-live list (owner actions on return; keep it current)

- **Backfill not published (G-18):** 2,122 outage items stay unpublished because the G-11 audit failed (38/50). Owner question: accept re-running it after Phase 36's non-crime fix (planned), or publish anyway? Withhold evidence: `34-BACKFILL-AUDIT.md`.
- **Live precision (G-18):** until Phase 36 ships, ~20% of live cards may be non-crimes (accidents, natural deaths, fires) labelled Delitos contra la Vida — same class as the pre-v2.2 Granite era.

- GSC sitemap submission (HYG-07).
- Granite-era re-classification decision.
- Three editorial pages still say "2005–2025" while CEAD data carries a partial 2026 (Phase-38 gate NB-12) → sweep in a later copy pass.
- news-pipeline.yml stale checkout on chained runs (G-59): a run queued behind another checks out GITHUB_SHA from trigger time → duplicate classification + rebase-conflict failure. Fix in Phase 38: `ref: master` on actions/checkout (or re-fetch before classify) + workflow-guard test.
- ResultPanel yearMismatchTip hardcodes '(2025)' as latest complete CEAD year (Phase-35 review F3) → Phase 38 docs/drift; derive from series.
- sync-data copies all of data/incidents (seen.json, rejected/) to public/ and those copies now go stale between deploys (Phase-35 review F4) → Phase 38 HYG-01 public ledgers.
- /news/ histogram 30-day cap (G-20(4)) hides the oldest store day (store keeps window_days+1 dates; 71 cards on 08-25 had no bar at check time) → Phase 36/38 decide: widen cap to window_days+1 or trim store window.
- Region chart title 'Annual evolution (2005–2025)' while a faded 2026 bar is drawn (validator F3) → Phase 38 copy.
- news-freshness validator judges staleness vs its own clock, not the page's data-build-now (validator F2) → Phase 38 hardening.
- data/SOURCES.md:417 model-name drift (Granite named) → Phase 38 HYG docs cleanup.
- 19 OTHER_DEFECT live items (accidents, non-incidents, wrong family/cut) from 34-06-HITS-VERDICTS.json → Phase 36 FID-02/03/07 input.
- `/map/` noindex/redirect decision, if the differentiated titles are not enough.

## When the run finishes (or pauses)

Write a final report. It must cover: what shipped per phase; every G-NN; measured metrics (A/B table, backfill split, prod checks); spend; the deferred-live list; and the exact commands the owner must run, if any. Update this file's RUN STATUS, `STATE.md` and memory (`v2.2-milestone-news-recovery`).
