---
id: 260810-l3e
title: Instalar rediseño Rankings v2 (propuesta 3b) desde paquete install-rankings
type: quick
must_haves:
  truths:
    - "national_rank #1 = MÁS delito reportado (descendente); nunca calificar 'seguro/peligroso'; toda fuente atribuida (CEAD/medio)."
    - "latestYear se deriva del dato (max de claves ratesByYear), nunca de new Date() — B-01 BLOCKER."
    - "Rank fuera del último año completo = '—' (decisión locked de Fable, revierte 3b.a de INSTALL.md §5a), con nota que explica ocultación, no cálculo."
    - "safest-cities-in-chile.astro y es/comunas-mas-seguras-chile.astro siguen usando RankingTableEnhancer sin bifurcación de la regla '—'."
    - "Hub ES instala la lista de 16 regiones (decisión locked del usuario) con nombres gramaticales vía regionNameEs()/'{name} Region'."
    - "No se borra RankingTableEnhancer.astro ni TrendChip.astro; no se toca i18n.ts, data.ts, familyDefs.ts, jsonld.ts."
  artifacts:
    - "site/src/components/RankingBoard.astro"
    - "site/src/components/RankingHub.astro"
    - "site/src/config/rankingStrings.ts"
    - "site/src/pages/crime-ranking/[crime].astro"
    - "site/src/pages/es/ranking-delito/[crime].astro"
    - "site/src/pages/rankings.astro"
    - "site/src/pages/es/rankings.astro"
  key_links:
    - "site/scripts/validate/all.mjs"
    - "site/scripts/validate/seo.mjs"
    - "site/scripts/validate/spine.mjs"
    - "site/scripts/validate/figure-registry.mjs (DOCS-01)"
---

# Plan: instalar Rankings v2 desde paquete

## Contexto

Paquete origen: `C:/Users/Carlo/Downloads/Optimizar diseño de comunas (2)/install-rankings`
(INSTALL.md leído íntegro — es la autoridad sobre qué se copia y qué NO se toca).
Repo destino: `C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site`
(dentro de OneDrive: encadenar build+validate en UN comando; citar TODAS las rutas con espacios/paréntesis).

Premortem con 9 hallazgos (B-01 HIGH, B-02..B-06 MEDIUM, B-07..B-09 LOW). Se aplican en Tarea 1
todos los BLOCKER/HIGH y MEDIUM baratos; LOW se dejan documentados como follow-up opcional si el
tiempo lo permite pero no bloquean el gate.

## Tarea 1 — Copiar paquete + aplicar fixes de premortem + decisión de rank

**files:**
- Copiar (rutas absolutas citadas, SIN escapes de corchete tipo bash — Windows/Git Bash no los necesita y B-06 documenta por qué romperían):
  - `"C:/Users/Carlo/Downloads/Optimizar diseño de comunas (2)/install-rankings/site/src/components/RankingBoard.astro"` → `"C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Is Chile Safe/site/src/components/RankingBoard.astro"`
  - `".../install-rankings/site/src/components/RankingHub.astro"` → `".../site/src/components/RankingHub.astro"`
  - `".../install-rankings/site/src/config/rankingStrings.ts"` → `".../site/src/config/rankingStrings.ts"`
  - `".../install-rankings/site/src/pages/crime-ranking/[crime].astro"` → `".../site/src/pages/crime-ranking/[crime].astro"`
  - `".../install-rankings/site/src/pages/es/ranking-delito/[crime].astro"` → `".../site/src/pages/es/ranking-delito/[crime].astro"`
  - `".../install-rankings/site/src/pages/rankings.astro"` → `".../site/src/pages/rankings.astro"`
  - `".../install-rankings/site/src/pages/es/rankings.astro"` → `".../site/src/pages/es/rankings.astro"`
- Editar tras copiar: `site/src/pages/crime-ranking/[crime].astro`, `site/src/pages/es/ranking-delito/[crime].astro`, `site/src/components/RankingBoard.astro`, `site/src/components/RankingHub.astro`

**action:**
1. Ejecutar las 7 copias con `cp` (rutas absolutas citadas entre comillas dobles, tal como arriba). Verificar con `ls` que los 7 destinos existen y no quedó ningún archivo llamado literalmente `[crime].astro` corrupto.
2. **Fix B-01 (HIGH — timebomb latestYear)** en ambas páginas `[crime].astro` (EN y ES):
   - Sustituir `let latestYear = new Date().getFullYear() - 1;` (y el `Math.max` posterior) por: `let latestYear = 0;` seguido de derivarlo de los datos reales antes de construir `scriptData`, p.ej. `latestYear = Math.max(...allRows.flatMap(r => Object.keys(r.ratesByYear).map(Number)));` (usar el nombre real de la variable de filas del paquete — verificar en el archivo copiado).
   - Exportar `years` en `scriptData` (array de años completos disponibles, ya existe en el paquete — confirmar antes de duplicar).
3. **Defensa en profundidad B-01** dentro de `RankingBoard.astro`, bloque de arranque del script:
   - `state.year = D.years.includes(D.latestYear) ? D.latestYear : Math.max(...D.years);`
   - Forzar `yearSel.value = String(state.year);` inmediatamente después de fijar `state.year`, antes del primer `render()`.
4. **Decisión de rank = '—' fuera del último año** (revierte INSTALL.md §5a, override explícito de Fable sobre el comportamiento por defecto del paquete):
   - En `RankingBoard.astro`, dentro de `render()`, localizar la línea que asigna `r.rank.textContent = ...` y añadir la condición `|| state.year !== D.latestYear` a la rama que produce `'—'` (cambio de una línea, tal como documenta INSTALL.md §5a como alternativa).
   - Reescribir el string `rk_rank_year_note` en `rankingStrings.ts` (EN y ES) para que explique **ocultación**, no cálculo. EN: algo equivalente a "Rank is published only for {year} (CEAD, latest complete year); for other years compare the rate per 100,000." ES: "La posición se publica solo para {year} (CEAD, último año completo); para otros años compara la tasa por 100.000." — sin calificar territorios, pasando forbidden-language.mjs y DOCS-01.
   - **No tocar** `rk_source_note` — debe conservar intacta la dirección "Posición 1 = mayor incidencia reportada" (o su equivalente EN), que ya cumple la convención `national_rank` #1 = MÁS delito.
5. **Fix B-02 (denominador count)**: en `RankingBoard.astro`, alinear el `.replace('{total}', ...)` del cliente (dentro de `render()`) para que use siempre `D.total` (346), igual que el string renderizado en servidor — eliminar la bifurcación `state.hideLowpop ? D.eligible : D.total`.
6. **Fix B-03 (vida→homicide redirect)**: en `RankingHub.astro`, la construcción de `familyHref` debe excluir también `familyKey === 'vida'` (no solo el caso sin slug), dejando `familyHref: null` para esa tarjeta, o apuntándola a `/methodology/#...` si existe anchor — usar `null` si no hay anchor confirmado, para no inventar una URL nueva.
7. **Fix B-04 (asterisco huérfano)**: en `RankingBoard.astro`, el chip de baja población debe imprimir el marcador `*` que la nota al pie referencia: `<span class="lowpop">*{t.dir_lowpop_tag}</span>` (no editar `i18n.ts` — lo consumen otras páginas).
8. **Fix B-05 (gramática regiones ES + hub)**: en `RankingHub.astro`, importar `regionNameEs` desde `site/src/config/i18n.ts` y renderizar la lista de 16 regiones como `{lang === 'es' ? regionNameEs(x.name) : `${x.name} Region`}` — instalando así la lista de 16 regiones en el hub ES (decisión locked del usuario) con nombres correctos. Aplicar el mismo criterio al panel regional de `RankingBoard.astro` si hay espacio; si se deja el nombre corto por las 250px de ancho, dejar un comentario in-code explicando la omisión deliberada (no silenciosa).
9. Fixes LOW (B-07, B-08, B-09) — aplicar solo si no cuestan gate adicional:
   - B-07: cambiar `mark { ... }` por `mark { ... }` con selector `:global(mark)` en el `<style>` de `RankingBoard.astro`.
   - B-08: en `paint()`, usar `norm(q).length` en vez de `q.length` para los `slice()`, o el guard `r.norm.length === r.name.length` antes de resaltar.
   - B-09: opcional — reservar altura en `.rk-table tbody` u ocultar `.rk-more-wrap` junto al botón "Ver más"; añadir pista condicional al estado vacío cuando `hideLowpop` activo. Si no alcanza el tiempo, documentar como deuda conocida en el commit, no bloquea.

**verify:**
- `grep -n "getFullYear" "site/src/pages/crime-ranking/[crime].astro" "site/src/pages/es/ranking-delito/[crime].astro"` → sin resultados (0 matches).
- `grep -n "state.year !== D.latestYear" site/src/components/RankingBoard.astro` → al menos 1 match, en la rama de rank.
- `grep -n "D.total" site/src/components/RankingBoard.astro` → el `.replace('{total}'` del cliente usa `D.total` sin condicional ternario de `hideLowpop`.
- `grep -n "vida" site/src/components/RankingHub.astro` → la exclusión de `familyHref` cubre `vida` explícitamente.
- `grep -n "regionNameEs" site/src/components/RankingHub.astro` → importado y usado.
- Ningún cambio toca `site/src/config/i18n.ts`, `site/src/lib/data.ts`, `site/src/lib/familyDefs.ts`, `site/src/lib/jsonld.ts` (`git diff --stat` no debe listarlos).
- `RankingTableEnhancer.astro` y `TrendChip.astro` siguen existiendo sin cambios (`git status` no los marca).

**done:** los 7 archivos copiados + los 6 fixes BLOCKER/MEDIUM aplicados y verificables por grep; commit atómico aún no requerido hasta pasar Tarea 2.

## Tarea 2 — Cadena de gates + verificaciones manuales de INSTALL.md §6

**files:** ninguno nuevo — solo ejecución desde `site/`.

**action:**
1. Ejecutar en UN comando encadenado (evita el desync de `dist/` en OneDrive — memoria del proyecto):
   `npm run check` — si pasa, encadenar en el mismo comando: `npm test && npm run build && npm run validate`.
2. Si `npm run check` reporta errores nuevos: atribuirlos al paquete instalado (baseline documentado = 0 errores) y corregir en los archivos copiados/editados de Tarea 1 antes de continuar — no suprimir con `// @ts-ignore` sin justificar.
3. Verificaciones manuales automatizables de INSTALL.md §6 sobre el build servido (`npx astro preview --port 4321 --host` en un comando separado, o inspección directa de `dist/`):
   - `grep -c 'href="/es/comuna/' "dist/es/ranking-delito/robos-violentos/index.html"` → **346**
   - `grep -c 'href="/commune/' "dist/crime-ranking/violent-robbery/index.html"` (o el slug EN equivalente confirmado en `rankingStrings.ts`) → **346**
   - Confirmar en `dist/es/rankings/index.html` (o `dist/rankings/index.html` si el hub vive ahí) que aparecen las 16 URLs de región y que la reciprocidad hreflang en/es se mantiene (spot-check 2-3 regiones).
   - Confirmar que `safest-cities-in-chile` y `es/comunas-mas-seguras-chile` en `dist/` siguen mostrando `—` para comunas de baja población, sin bifurcación.
4. Si `npm run validate` falla en `seo.mjs` (Assertion Q/R sobre JSON-LD del primer subdir de `crime-ranking/` o `ranking-delito/`) o en `spine.mjs` (checks G/H/L sobre `/rankings/`), volver a Tarea 1 y corregir el archivo correspondiente — no parchear el validador.

**verify:**
- Salida de la cadena `npm run check && npm test && npm run build && npm run validate` termina en verde, sin errores nuevos de `astro check` (baseline 0), 18/18 validadores OK.
- Los dos `grep -c` de INSTALL.md §6 devuelven exactamente `346`.
- `figure-registry.mjs` (DOCS-01) no marca ninguna prosa nueva de `rk_rank_year_note` ni de las tarjetas del hub como dirección invertida.

**done:** gates verdes + los dos conteos de 346 confirmados + verificación visual mínima de las 16 regiones ES + confirmación de que safest-cities/comunas-más-seguras no cambiaron su comportamiento `—`.

<invariants>
- crime.mjs exige exactamente 7 subdirs en `dist/crime/` y `dist/es/delito/` con nombres = FAMILY_SLUGS — este paquete NO debe tocar esas rutas (son namespace distinto de `crime-ranking/`/`ranking-delito/`).
- seo.mjs toma automáticamente el PRIMER subdir (orden `readdirSync`) de `dist/crime-ranking/` y `dist/es/ranking-delito/` como muestra: exige JSON-LD parseable (Assertion Q) siempre; si hay `og:title`, exige el set completo de OG_KEYS + `twitter:card=summary_large_image` (Assertion N), `og:url` coherente con el locale (O), `og:image` con prefijo absoluto `https://ischilesafe.com/og/` (P); si hay bloque `ItemList`, cada item necesita `position` numérico y `url` string, sin huecos (R); si hay `BreadcrumbList`, cada crumb necesita `position`+`name`, todos menos el último necesitan `item` (S).
- spine.mjs check G exige que existan `dist/rankings/index.html` y `dist/es/rankings/index.html` (ruta `/rankings/`, namespace distinto de `/crime-ranking/`).
- spine.mjs check H exige al menos una URL `/rankings/` (sin `/es/`) y una `/es/rankings/` en `dist/sitemap-0.xml`.
- spine.mjs check L exige `href="/rankings/"` (EN) / `href="/es/rankings/"` (ES) literal en home, map/mapa, y la primera commune/comuna page de cada locale.
- figure-registry.mjs (DOCS-01) barre `.astro/.ts/.tsx` bajo `src/` buscando prosa que invierta la dirección de `national_rank` (ej. "rank 1 = lowest"); cualquier copy nuevo sobre dirección del ranking debe seguir "posición 1 = mayor incidencia/tasa reportada".
- Regla site-wide: `national_rank` #1 = MÁS delito reportado (descendente); nunca calificar territorios de "seguros/peligrosos"; toda estadística/incidente cita y enlaza su fuente (CEAD o medio).
- Todo contenido crítico debe quedar pre-renderizado en el HTML servido (346 filas visibles en servidor, JS solo enriquece/oculta) — sin render-only en cliente.
</invariants>

<locked_decisions>
- Instalar la lista de 16 regiones en el hub ES tal como la trae el paquete, con nombres corregidos vía `regionNameEs()` (EN: `{name} Region`) — decisión del usuario, no reabrir.
- Rank fuera del último año completo = `'—'` con nota que explica ocultación (no recálculo) — decisión de Fable tras el premortem, revierte el comportamiento por defecto del paquete descrito en INSTALL.md §5a. Razones: honestidad editorial ante empates masivos en 0.0, modo de fallo visible vs. silencioso, consistencia con safest-cities/comunas-más-seguras que ya blanquean a `'—'`.
- `rk_source_note` permanece sin cambios: dirección "posición 1 = mayor incidencia" siempre visible.
- No bifurcar la regla `'—'`: una sola autoridad site-wide, servida en el último año, citada por el `ItemList` JSON-LD.
</locked_decisions>

<rollback>
1. Restaurar los 4 archivos reemplazados desde git:
   `git checkout -- "site/src/pages/crime-ranking/[crime].astro" "site/src/pages/es/ranking-delito/[crime].astro" "site/src/pages/rankings.astro" "site/src/pages/es/rankings.astro"`
2. Borrar los 3 archivos nuevos:
   `rm "site/src/components/RankingBoard.astro" "site/src/components/RankingHub.astro" "site/src/config/rankingStrings.ts"`
3. Confirmar que `RankingTableEnhancer.astro` y `TrendChip.astro` no fueron tocados (no requieren rollback).
4. Re-ejecutar `npm run check && npm test && npm run build && npm run validate` desde `site/` para confirmar retorno al baseline verde (0 errores astro check, 18/18 validadores).
5. Si ya hubo commit, usar `git revert` del commit de instalación en vez de `checkout`/`rm` directos sobre working tree sucio.
</rollback>
