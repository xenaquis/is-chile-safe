---
quick_id: 260922-t59
type: hotfix
owner_approved: 2026-09-22 (milestone v2.2 owner decision "Hotfix inmediato")
requirements: [pre-Phase-34 hotfix; re-verified later by FID-07]
must_haves:
  truths:
    - "Exactly one card for the Carahue arrest story remains; its title_es/title_en make NO kinship claim about Rosamel Fierro or any named person"
    - "Prison-suicide items efa0fae52180e3e2 and c53a579a6c972705 are absent from current.json and every archive file"
    - "The 6 duplicate Fierro cards are absent from current.json and every archive file"
    - "Their URLs stay in seen.json (so the pipeline never re-adds them)"
    - "All validators green; build succeeds"
  artifacts:
    - data/incidents/current.json
    - data/incidents/archive/*.json (only if they contain the target ids)
    - pipeline/news/hotfix_overrides.py or equivalent one-shot script (optional, committed for auditability)
---

# Quick 260922-t59: Hotfix live erroneous news cards

## Why
Audit V-06/V-07 (`.planning/research/v2.2-AUDIT-260922.md`). Prod shows 7 cards for one arrest in Carahue (2026-09-03). The LLM-rewritten headlines call the arrested man Rosamel Fierro's "grandfather", "in-law", "father-in-law" and "nephew-in-law". The press itself disagrees: the BioBio URL slug says *yerno* while other outlets' headlines say *suegro*. That is defamation/editorial risk about a named real person. Separately, two prison-suicide items are published under family `vida` ("Delitos contra la Vida"). A suicide is not a crime.

## Measured targets (data/incidents/current.json @ 7c32fd7)

Fierro cluster, all dated 2026-09-03 and family robos_violentos:
- KEEP `514872e1d06db7f2`: cut 9102 Carahue, BioBio publisher URL (not a Google News redirect).
- DROP `c3db293fd1316ad1`, `d468d43eeda0316d`, `f9031873f12b9479` (cut 8207 Tirúa), `8f18cd275b4a6692`, `836e500e4e4b1f30`, `60fa7e16887af782`. All six are news.google.com redirects.

Prison suicides, family vida, cut 5101:
- DROP `efa0fae52180e3e2` (2026-08-25, "Imputado se quitó la vida en cárcel de Valparaíso…")
- DROP `c53a579a6c972705` (2026-08-26, "Interno provisional se quita la vida en cárcel de Valparaíso…")

## Tasks

### Task 1 — Apply the data correction
- files: data/incidents/current.json, data/incidents/archive/*.json (grep each target id first; edit only files that contain one)
- action:
  1. `git pull --ff-only` first. The news cron commits to data/incidents/ about 4x/day.
  2. Remove the 8 DROP ids from `incidents`.
  3. On `514872e1d06db7f2`, set:
     - `title_es` = "Detienen a prófugo por violento robo a trabajadores forestales en Carahue"
     - `title_en` = "Fugitive arrested over violent robbery of forestry workers in Carahue"

     These are neutral and factual, with no named person and no kinship. Leave every other field untouched.
  4. Preserve the JSON formatting and encoding of the existing file: UTF-8 with `ensure_ascii` matching the current file, and the same indent. Keep the top-level keys (`generated`, `window_days`, `incidents`) unchanged; do not bump `generated`.
  5. Do NOT touch seen.json. The URLs must stay seen so the pipeline never re-adds them.
  6. Check `pipeline/news/store.py` `merge_and_write`/`_merge_by_id` to confirm that the next cron run merges existing current.json + new items and does not rebuild from any other source that would resurrect the dropped ids. Also grep for other copies of the incidents used by the site build: site/public/data, any R2-sync or facet snapshot committed in the repo. Apply the same edit to any committed copy that feeds prod pages.
- verify: a python one-liner asserting that none of the 8 ids appear anywhere under data/ (except rejected/ and seen.json by URL); that `514872e1d06db7f2` has the new titles; and that the incident count dropped by exactly 8.
- done: assertions pass.

### Task 2 — Build + validate (one chained command; the repo is in OneDrive)
- action: from `site/`, run `npm run build && npm run validate` in ONE command. Report the validator count (expect 16/16) and grep the built `dist/` for "Rosamel" (expect 0 hits), for "se quit" (expect 0 hits in news pages), and for the new Carahue title (expect ≥1).
- done: build green, 16/16 validators, grep expectations met.

### Task 3 — Commit (do NOT push, do NOT dispatch deploy; the orchestrator does that)
- One atomic commit: `fix(news): hotfix erroneous live cards — Fierro kinship cluster + prison suicides (quick-260922-t59)`, with a body listing the 8 dropped ids and the retitled id, and the attribution line `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.
- Write `260922-t59-SUMMARY.md` next to this plan (frontmatter `status: complete`), stating the measured before/after counts.
