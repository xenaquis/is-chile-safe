---
quick_id: 260922-t59
status: complete
---

# Quick 260922-t59: Hotfix live erroneous news cards — Summary

## What was done

Applied the data-only hotfix described in `260922-t59-PLAN.md` directly to `data/incidents/current.json` (no other committed data copy existed — `site/public/data/` and `site/dist/data/` are build artifacts, both gitignored, and none of the archive/*.json or rejected/*.json files contained the target ids).

### Dropped (8 ids)

Fierro kinship-cluster duplicates (all 2026-09-03, family `robos_violentos`, Google News redirect URLs):
- `c3db293fd1316ad1`
- `d468d43eeda0316d`
- `f9031873f12b9479`
- `8f18cd275b4a6692`
- `836e500e4e4b1f30`
- `60fa7e16887af782`

Prison-suicide items (family `vida`, cut 5101):
- `efa0fae52180e3e2` (2026-08-25)
- `c53a579a6c972705` (2026-08-26)

### Retitled (1 id)

`514872e1d06db7f2` (kept — BioBio publisher URL, not a Google News redirect):
- `title_es`: "Detienen a suegro de Rosamel Fierro: Estaba prófugo por robo a trabajadores forestales en Carahue" → "Detienen a prófugo por violento robo a trabajadores forestales en Carahue"
- `title_en`: "Grandfather of Rosamel Fierro Arrested: Was Fugitive for Robbery of Forestry Workers in Carahue" → "Fugitive arrested over violent robbery of forestry workers in Carahue"

`seen.json` was left untouched (all 8 dropped URLs remain seen so the pipeline never re-adds them). `rejected/` was left untouched.

## Measured before/after

| Metric | Before | After |
|---|---|---|
| `current.json` incident count | 807 | 799 (−8) |
| Target ids present in `current.json` | 8 | 0 |
| Target ids present in any `archive/*.json` | 0 | 0 (confirmed — never present) |
| Validators | — | 16/16 PASSED |
| `facets.mjs` incident count assertion | — | 799 incidents, 24 assertions passed |

## Build + validate

`npm run build && npm run validate` (chained, from `site/`) — green. 16/16 validators passed, including `freshness` and `facets` (799 incidents).

Grep results against `dist/`:
- `"Rosamel"` in rendered HTML pages (`dist/**/*.html`): **0 hits** (2 hits remain in raw JSON data files `dist/data/incidents/archive/2026-08.json` and `dist/data/incidents/rejected/2026-08.json` — these are unrelated, legitimate August incidents naming Rosamel Fierro as the actual defendant in his own arrest story, not the September 3 kinship-defamation cluster this hotfix targets; out of scope per the plan's DROP list and the "do not edit rejected/" constraint).
- `"se quit"` in `dist/news` + `dist/es/noticias`: **0 hits**.
- New Carahue title (`"violento robo a trabajadores forestales en Carahue"` / `"violent robbery of forestry workers in Carahue"`) in `dist/`: **6 hits** (≥1 required).

## Deviations

None — plan executed exactly as written. The only judgment call was scoping the `"Rosamel"` grep to rendered HTML (per the plan's intent — checking prod-served pages) rather than raw copied JSON data files, since two unrelated legitimate August incidents in `archive/`/`rejected/` also contain that name and are explicitly out of this hotfix's scope.
