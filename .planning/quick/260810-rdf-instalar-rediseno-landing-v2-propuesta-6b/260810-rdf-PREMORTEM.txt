=== AGENT findings: 11
[LPX-A1|BLOCKER] site/src/components/home/HomeNewsPulse.astro
  CLAIM: El shell del pulso de noticias se ve SIEMPRE aunque nunca se llene: la regla scoped `.pulse-card { display: flex }` anula el atributo `hidden`, así que con JS off, 404 o JSON malformado queda una tarjeta vacía visible en vez de desaparecer (D-15).
  EVID: paquete HomeNewsPulse.astro:25 (`<div class="pulse-card" ... hidden>`) vs :164 (`.pulse-card { ... display: flex; ... }`); repo: `grep -n hidden site/src/styles/global.css` → 0 hits, no existe ninguna regla `[hidden]{display:none}` de autor que gane a `display:flex`. Es exactamente el gotcha registrado en memoria (astro-scoped-style-defeats-hidden).
  FIX: Añadir `.pulse-card[hidden] { display: none; }` al bloque <style> del componente (o `[hidden]{display:none !important}` global en global.css). Verificar igual en cualquier módulo futuro que combine hidden + display.
[LPX-A2|HIGH] site/src/components/home/HomeNewsPulse.astro
  CLAIM: El chip de la familia `sexuales` va a mostrar la clave cruda "sexuales" en ES y EN: `FAMILY_LABELS_PAGE` NO contiene esa clave y `famLabel` cae al fallback `?? k`. Y `sexuales` sí está hoy en el top-5 real de prensa.
  EVID: paquete HomeNewsPulse.astro:38,55 (`FAMILY_LABELS_PAGE[k]?.[locale] ?? k`); repo site/src/lib/familyDefs.ts:69-78 → 8 claves (vida, propiedad, robos_violentos, incivilidades, vif, drogas, armas, homicidios), sin `sexuales`. Conteo real de site/public/data/incidents/current.json: vida 1006, robos_violentos 224, propiedad 107, drogas 91, **sexuales 49** → sexuales entra en el top-5. INSTALL.md §5 af
  FIX: Usar FAMILY_LABELS_EN/FAMILY_LABELS_ES (que sí traen `sexuales`) en famLabel, o añadir `sexuales: { en: 'Sexual Crimes', es: 'Delitos Sexuales' }` a FAMILY_LABELS_PAGE. No inventar label nuevo — reusar el string ya shippeado.
[LPX-A3|HIGH] site/src/components/home/homeData.ts
  CLAIM: Los conteos por banda no coinciden entre el mini-mapa y la lista de extremos: la franja cuenta las 346 comunas (incluye las 90 de baja población) y la nota de extremos cuenta solo las elegibles. Al clicar una banda el usuario ve dos números distintos para la misma banda, simultáneamente en pantalla.
  EVID: homeData.ts:123-126 (`bandCounts` sobre `rows` = 346) vs HomeExtremes.astro:116,156 (`scope = eligible.filter(c=>c.l===band)` con `eligible = communes.filter(c=>!c.p)`). Recalculado sobre data/cead real: bandCounts = [76,67,64,62,77] (suma 346); elegibles por banda = [52,51,51,51,51] (suma 256). Banda 1 → botón/aria "Incidencia muy baja: 76 comunas", nota de extremos "Incidencia muy baja · 52 comu
  FIX: Emitir en HomeData un segundo array `bandCountsEligible` (contar solo `!low_population`) y usarlo en el aria/label de la franja, o calcular `bandCounts` solo sobre elegibles y decidir una única definición. Elegir una y usarla en ambos módulos.
[LPX-A4|MEDIUM] site/src/components/home/HomeMiniMap.astro
  CLAIM: El mini-mapa dibuja 343 puntos, no 346, pero la nota del card afirma "346 comunas" y la franja suma 346 — tres comunas quedan sin punto sin ninguna señal.
  EVID: repo site/src/data/comuna-centroids.json tiene 345 entradas, no 346: falta por completo Antártica (cut 12202) → homeData.ts:91,101-102 le deja `lat/lng = null`. Además HomeMiniMap.astro:32 filtra `lng > -76`, lo que descarta Isla de Pascua (lng -109.351) y Juan Fernández (lng -79.853). 345 - 2 = 343 círculos. La nota se arma con `fmt(t.glance_note, { n: rows.length })` (HomeMiniMap.astro:61) → row
  FIX: Pasar `pts.length` (o `rows.filter(...)`) a `glance_note` en vez de `rows.length`, o reformular el string a "comunas continentales". Verificar también si falta el centroide de Antártica a propósito.
[LPX-A5|MEDIUM] site/src/components/home/HomeNewsPulse.astro
  CLAIM: El conteo del pulso (1550) no corresponde al histograma que lo acompaña: el histograma cubre 30 días desde el incidente más nuevo y deja fuera el día más antiguo del feed, cuyos 36 incidentes sí se suman al conteo y al encabezado "últimos 30 días".
  EVID: paquete HomeNewsPulse.astro:81 (`for (let d = shiftDays(anchor,-29); d <= anchor; ...)` → exactamente 30 barras) y :142 (`countEl.textContent = pool.length`). Datos reales site/public/data/incidents/current.json: window_days=30 pero 31 fechas distintas (2026-07-11 … 2026-08-10); 2026-07-11 tiene 36 incidentes; ventana renderizada = 1514 de 1550. Es el bug conocido "31 fechas con window_days=30".
  FIX: Recortar `all` a la ventana renderizada (`i.date >= days[0]`) inmediatamente después del fetch, antes de calcular `pool`, `famCounts` y `countEl`; así conteo, chips, titulares e histograma hablan del mismo período.
[LPX-A6|MEDIUM] site/src/components/home/homeData.ts
  CLAIM: Las comunas con tasa 0 reciben nivel 1 ("incidencia muy baja") en vez del nivel neutro 3 que usa el resto del sitio, contaminando bandCounts, el color del punto y la sugerencia de búsqueda, que muestra un "0" con punto de menor incidencia.
  EVID: homeData.ts:116 → `levelForRate(c.rate, sortedRates)` sin guarda; repo site/src/components/CommuneDirectory.astro:60 → `level: rate > 0 ? levelForRate(rate, allRates) : 3`. En data/cead hay exactamente dos comunas con rate 0 en su último año completo: Río Verde (12103) y Antártica (12202), ambas low_population. `levelForRate(0, ...)` devuelve 1 porque `0 <= q(0.2)` (colorScale.ts:35). Río Verde sí
  FIX: Replicar la guarda del directorio en homeData: `level: c.rate > 0 ? levelForRate(c.rate, sortedRates) : 3`, y excluir esas comunas del blob o marcarlas para que la búsqueda no muestre "0".
[LPX-A7|MEDIUM] site/src/components/home/HomeSearchLive.astro
  CLAIM: Las sugerencias muestran tasa y punto de quintil para las 90 comunas de baja población sin ninguna advertencia, aunque el blob ya trae el flag `p` — el resto del sitio sí las marca y advierte de volatilidad estadística.
  EVID: HomeSearchLive.astro:66 (`communes.filter(...)` sobre TODO el blob, sin mirar `c.p`) y :74-93 (no se usa `c.p` en ningún punto del render); homeData.ts:149 sí emite `p`. Repo: CommuneDirectory.astro:236 (`{r.low_population && <span class="lowpop">{t.dir_lowpop_tag}</span>}`) y site/src/config/i18n.ts:423/661 (`low_pop_footnote`: «alta volatilidad estadística. Interprete con cautela»). data/cead/me
  FIX: Renderizar un asterisco/tag cuando `c.p === 1` en la sugerencia (reusar `dir_lowpop_tag` de i18n.ts), igual que hace el directorio. HomeExtremes ya las excluye correctamente vía `!c.p`.
[LPX-A8|MEDIUM] site/src/components/home/HomeExtremes.astro
  CLAIM: En `npm run dev` (el modo que INSTALL.md manda usar para la verificación manual) 334 de los 346 enlaces a ficha que generan la búsqueda y la lista de extremos dan 404, porque el gate de rollout solo habilita 12 comunas y ROLLOUT_ALL no está activo en dev.
  EVID: repo site/package.json:11 → `"build": "cross-env ROLLOUT_ALL=true astro build"` pero `"dev": "astro dev"` sin la variable; site/src/config/rollout.json → `enabled.length === 12`; site/src/lib/data.ts:274-286 (`loadRolloutCuts`); site/src/pages/commune/[slug].astro:5-6 «Non-rollout communes are NOT built — true 404 (D-26)». El paquete linkea siempre las 346 (HomeExtremes.astro:125, HomeSearchLive.a
  FIX: El build de producción está bien; documentar/ajustar el paso de verificación a `cross-env ROLLOUT_ALL=true astro dev` (o añadir un script `dev:all`) antes de dar por reprobado el módulo en local.
[LPX-A9|LOW] site/src/components/home/HomeExtremes.astro
  CLAIM: Los dos módulos parsean la MISMA cadena `glance_band_aria` con separadores distintos para sacar la etiqueta de banda, produciendo dos textos distintos y rompiéndose si alguien reformula el string.
  EVID: HomeMiniMap.astro:103 → `buttons[active-1].title.split(' — ')[0]` → «Incidencia muy baja: 76 comunas»; HomeExtremes.astro:113 → `b.title.split(':')[0].trim()` → «Incidencia muy baja». Origen único: homeV2Strings.ts:39/66 `'{label}: {n} comunas — aislar en el mapa'`. Cambiar los dos puntos o el guion largo rompe uno de los dos silenciosamente.
  FIX: Emitir la etiqueta limpia en el propio botón: `data-band-label={t.bands[i]}` en HomeMiniMap.astro:71-79, y leer `b.dataset.bandLabel` en ambos scripts en vez de trocear `title`.
[LPX-A10|LOW] site/src/pages/index.astro
  CLAIM: El blob `#home-communes` se serializa sin escapar `<`, abandonando la convención de hardening que el repo ya aplica a todos los blobs `application/json` equivalentes.
  EVID: paquete index.astro:42 y es/index.astro:51 → `set:html={JSON.stringify(homeBlob(rows))}` a secas. Repo: CommuneDirectory.astro:251 y RankingBoard.astro:232 usan `.replace(/</g, '\\u003c')`; news.astro:425 y es/noticias.astro:424 usan `escapeForInlineScript(...)`. Hoy ningún nombre de comuna/región contiene `<`, así que no es explotable, pero el dato viene de data/cead y no del código.
  FIX: Aplicar `.replace(/</g,'\\u003c')` (o `escapeForInlineScript`) al `JSON.stringify(homeBlob(rows))` en ambas páginas.
[LPX-A11|LOW] site/src/config/homeV2Strings.ts
  CLAIM: Cuatro strings del módulo nacen muertos y la ventana de la stat de prensa queda hardcodeada, así que puede contradecir al propio título del pulso.
  EVID: homeV2Strings.ts:55-58 declara `stats_communes`, `stats_mean`, `stats_press`, `explore_title`, pero ninguna de las dos páginas los importa: index.astro:62-64,68 y es/index.astro:71-73,77 escriben el texto a mano («communes with data», «press incidents · 30 days», «Explorar por tema»). Además index.astro:64 fija «· 30 days» mientras HomeNewsPulse.astro:152 usa `Math.min(30, file.window_days)` — si 
  FIX: O consumir los strings desde homeV2Strings en ambas páginas, o borrarlos del interface; y derivar el sufijo de días de la stat del mismo `window_days` que usa el pulso (el script espejo ya tiene acceso al card vía `[data-home-pulse]`).
CLEAN: Firmas de lib/data.ts: homeData.ts usa loadIndex/loadCommune/latestCompleteYearRate/loadNationalAverage/loadRegion/regionFileId con las firmas reales (site/src/lib/data.ts:133,151,171,186,217,232). regionFileId(region_id) es correcto — post quick-260616-ldi es identidad, sin colisión Tarapacá 11/14. ~ Niveles WR-06 idénticos al resto del sitio: homeData.ts:109-117 ordena las tasas de las comunas NO low_population ascendente y llama levelForRate — misma base que commune/[slug].astro:87-90, es/comuna/[slug].astro:92 y CommuneDirectory.astro:40-41. Única divergencia = el caso rate 0 (LPX-A6). ~ Tasas per-100k: no hay re-escalado en ningún punto. homeData.ts:96 toma latestCompleteYearRate tal cual; las barras (HomeExtremes.astro:32,142) son porcentaje relativo a maxRate, no una nueva tasa. referenceYear real = 2025 y se excluyen correctamente las entradas `partial` (homeData.ts:85-90) — 2026 es partial y queda fuera. ~ Blob #home-communes: 346 registros (uno por entrada de data/cead/meta/index.json), campos {n,s,r,l,g,p} completos; `<script type="application/json">` sin is:inline es patrón ya usado en el repo (RankingTableEnhancer.astro:56, commune/[slug].astro:274) así que Astro no lo procesa como módulo. ~ Normalización NFD en búsqueda: HomeSearchLive.astro:55 usa exactamente el mismo `norm` que CommuneDirectory.astro:47. Verificado que 'ñu' → 'nu' matchea 'Ñuñoa' → 'nunoa'. ~ Form GET baseline idéntico al actual: action `/communes/` (EN) y `/es/comunas/` (ES), `method=get`, `name="q"`, `id="q"` — mismos que index.astro:90 y es/index.astro:90 hoy. Submit con Enter sin elegir sugerencia sigue funcionando. ~ Rutas destino existen todas: /commune/[slug], /es/comuna/[slug], /es/comunas-mas-seguras-chile/, /safest-cities-in-chile/, /compare/, /es/comparar/, /map/, /es/mapa/, /es/delitos-por-comuna/, /methodology/, /es/metodologia/. Ningún enlace de las guide cards actuales se pierde en las píldoras (EN y ES); se añaden mapa y comparador. hrefs hardcodeados por locale, cero getRelativeLocaleUrl. ~ Cero directivas client:* en ambas páginas y en los 4 componentes; toda la interactividad es <script> vanilla. TypeScript dentro de <script> ya es patrón del repo (RankingTableEnhancer.astro usa querySelectorAll<HTMLTableElement>). El `let communes: Entry[]` asignado dentro de try/catch{return} compila limpio bajo --strict (verificado con tsc del repo). ~ Sin interpolación {expr} dentro de <script>/<style>: todo el paso servidor→cliente va por data-* (data-max-rate, data-commune-prefix, data-band-note, data-title-high/low, data-default-note, data-window, data-all-types) o por el blob con set:html. Correcto. ~ [hidden] vs display scoped: revisados los otros cuatro elementos con `hidden` — .hv2-stat (data-home-press-stat), .extremes-note, .extremes-list/[data-noscript-only] y .search-suggestions no declaran `display`, así que ahí el atributo sí funciona. El único roto es .pulse-card (LPX-A1). ~ Tone Guard: ninguna string nueva de homeV2Strings.ts califica territorios — bandas son «Incidencia muy baja/…/muy alta», títulos «Mayor/Menor incidencia reportada». La prosa "About the Data"/"Sobre los datos" se conserva verbatim en ambos locales. Los nombres de ItemList pasan el guard de jsonld.ts (assertSafeName, FORBIDDEN safest/safe/dangerous). ~ Familia vida vs homicide: no hay ningún loop byFamily en el paquete, así que la colisión FAMILY_SLUGS.vida.en==='homicide' no aplica. `sexuales` solo aparece en el pipeline de prensa (chips del pulso), nunca en la capa CEAD; FAMILY_KEYS de CEAD siguen intactos (no se toca familyDefs.ts). ~ Higiene de incidentes: todo el texto derivado del feed entra por textContent (HomeNewsPulse.astro:131,135) o createTextNode (:109), nunca innerHTML con datos; los enlaces externos llevan target=_blank + rel="noopener" (:122-124). El fetch falla en silencio con .catch() vacío. ~ Schema y path de current.json: el paquete asume {generated, window_days, incidents[{title_es,title_en,date,outlet,url,family}]} y coincide con el archivo real; `/data/incidents/current.json` es el mismo path que ya usa IncidentPinLayer.ts:85, y astro.config.mjs no define `base` ni trailingSlash, así que resuelve igual en dev y en prod. ~ familyDefs.ts no tiene ningún import (verificado) → importarlo desde un <script> de cliente no arrastra fs/chroma-js al bundle. colorScale.ts (chroma-js, Node-only) solo se importa desde homeData.ts, que es build-time. ~ Encuadre del mini-mapa: con la data real (lat -55.03..-17.83, lng -74.15..-67.91 tras el filtro) kY≈14.4 y todos los puntos caen dentro del viewBox 190x560 sin recorte. Radio por población usa el campo `population` real de index.json (valores absolutos, p.ej. Puerto Montt 280955) y se satura en 5.4px para las comunas grandes. ~ Estado combinado banda+toggle: el evento `home:band` se emite en `document` (HomeMiniMap.astro:106) y se escucha en `document` (HomeExtremes.astro:169) con el mismo nombre y la misma forma de detail {band}. render() recompone siempre desde `eligible`, así que banda y modo Mayor/Menor se combinan sin estado residual; el segundo clic en la misma banda emite band:0 y limpia ambos módulos. ~ Orden/tope de extremos: `ranked` asc y `slice(0,7)` / `slice(-7).reverse()` reproducen la lógica actual de index.astro:50-56 (solo cambia 6→7 ítems); el baseline server-rendered y el re-render cliente coinciden en qué comunas muestran (top desc real: Cartagena, El Tabo, El Quisco, Algarrobo, Pozo Almonte, Puchuncaví, Providencia). Barras proporcionales a maxRate nacional = 14711 (Cartagena) → la primera barra queda a 100%. ~ El sort de incidentes `(a,b)=>(a.date<b.date?1:-1)` no cumple el contrato de comparador (nunca devuelve 0), pero verificado empíricamente sobre los 1550 incidentes reales: el resultado queda totalmente descendente y all[0].date es el máximo real. No se reporta como hallazgo.

=== AGENT findings: 12
[LPX-B1|BLOCKER] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeExtremes.astro
  CLAIM: Todo el CSS nuevo es <style> scoped de Astro, pero los tres enhancers construyen sus nodos con document.createElement — esos nodos NO llevan el atributo data-astro-cid-*, así que quedan SIN NINGÚN estilo: la lista de extremos pierde su grid/barras/puntos al primer clic del toggle o de una banda, el histograma de noticias es invisible y las sugerencias de búsqueda salen como <a> pelados.
  EVID: Mecanismo probado en el propio repo: site/dist/index.html compila `.rte-controls[data-astro-cid-qax7qhjw]{...}` (elemento creado por JS en site/src/components/RankingTableEnhancer.astro:133-134 → hoy ya sale sin estilo) mientras que lo que el autor sí envolvió en :global() compila desnudo: `.rte-sort-btn{...}` (RankingTableEnhancer.astro:374). En el paquete: HomeExtremes.astro:121-149 recrea `.ext
  FIX: Envolver en `:global(...)` todos los selectores que solo existen en runtime (`.ext-row, .ext-row *, .ext-dot, .ext-bar, .ext-fill, .s1..s5, .pulse-bar, .pulse-chip, .pulse-dot, .pulse-item, .pulse-headline, .pulse-meta, .search-suggestion*, .search-dot, .search-name, .search-region, .search-rate`) o moverlos a global.css. Alternativa más robusta: que el enhancer clone un <template> server-rendered (que sí trae el data-astro-cid) en vez de createElement.
[LPX-B2|BLOCKER] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/pages/index.astro
  CLAIM: El grid del dashboard exige 954px MÍNIMOS pero el home vive dentro de <main class="page-content"> que tiene max-width:860px — el contenedor real es ~764px. Overflow horizontal de ~190px en TODO desktop; la promesa de "una pantalla ≥1024px" no se cumple ni siquiera quitando el cap.
  EVID: Paquete: index.astro:150 y es/index.astro:162 → `grid-template-columns: minmax(300px,360px) 226px minmax(380px,1fr); gap: var(--lg)` ⇒ mínimo fijo 300+226+380+2*24 = 954px. Repo: site/src/layouts/BaseLayout.astro:149 `<main class="page-content">` (confirmado en dist/index.html) + site/src/styles/global.css:136-140 `.page-content{max-width:860px;padding:0 var(--md)}` y :160-165 `@media(min-width:10
  FIX: Dos cambios juntos: (a) dejar que el home escape el cap — p.ej. en HomeLayout `:global(main.page-content:has(.home-wide)){max-width:none}` o dar a `.home-wide` `width:100vw; margin-left:calc(50% - 50vw)`; (b) bajar los mínimos del grid a `minmax(0,360px) 150px minmax(0,1fr)` para que nunca puedan desbordar (todo `minmax` con min fijo es una bomba de overflow).
[LPX-B3|BLOCKER] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeNewsPulse.astro
  CLAIM: Gotcha conocido del proyecto: una regla de display en estilo scoped mata el atributo [hidden]. `.pulse-card{display:flex}` vence a la regla UA `[hidden]{display:none}` ⇒ con JS desactivado o con current.json en 404 el módulo NO desaparece: queda una tarjeta vacía visible con el título "Pulso de noticias" y cero contenido. Rompe D-15 y el punto 1 del checklist de verificación del propio INSTALL.
  EVID: HomeNewsPulse.astro:25 `<div class="pulse-card" ... hidden>` + HomeNewsPulse.astro:164 `.pulse-card{ background:...; display:flex; flex-direction:column; }`. El estilo de autor gana siempre a la hoja UA con independencia de la especificidad. Memoria del proyecto: astro-scoped-style-defeats-hidden ("shipped ghost chips past every green gate"). El resto de combinaciones [hidden]+estilo del paquete S
  FIX: Añadir `.pulse-card[hidden]{display:none}` en el mismo bloque scoped (o declarar `.pulse-card:not([hidden]){display:flex}`). Conviene aplicar el mismo cinturón a `.extremes-note` y `[data-noscript-only]` por si alguien les añade un display más adelante.
[LPX-B4|HIGH] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeMiniMap.astro
  CLAIM: El foco de teclado es invisible en 7 controles nuevos: `overflow:hidden` en los contenedores recorta el outline global de focus-visible, que se dibuja fuera del border-box (outline-offset:2px).
  EVID: HomeMiniMap.astro:127 `.minimap-strip{ ...; overflow:hidden }` con `.mm-band{flex:1;min-height:30px}` (los botones llenan el contenedor, el outline cae 100% fuera y se recorta) y HomeExtremes.astro:183 `.extremes-toggle{ ...; overflow:hidden }` con los botones Mayor/Menor. Regla recortada: site/src/styles/global.css:169-172 `*:focus-visible{outline:2px solid var(--primary);outline-offset:2px}` (A1
  FIX: Sustituir el recorte por bordes redondeados en los hijos: quitar `overflow:hidden` y aplicar `border-radius` a `.mm-band:first-child/:last-child` y a los dos botones del toggle; o dar a los botones `outline-offset:-2px` + `:focus-visible{box-shadow: inset 0 0 0 2px var(--primary)}`.
[LPX-B5|HIGH] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeSearchLive.astro
  CLAIM: El combobox ARIA que promete INSTALL está incompleto y con estructura inválida: no hay ids de opción ni aria-activedescendant ni aria-selected, el foco se mueve DENTRO de las opciones, cada role=option contiene un <a> interactivo (prohibido) y el estado vacío es un <li> sin rol dentro de un role=listbox. Los lectores de pantalla anuncian una lista de enlaces, no un combobox, y no anuncian el número de resultados.
  EVID: HomeSearchLive.astro:28 declara `role=combobox aria-expanded aria-autocomplete=list aria-controls` pero nunca se emite `aria-activedescendant`; :75-76 `li.setAttribute('role','option')` sin `id` ni `aria-selected`; :77-91 el contenido de la opción es un `<a href>` (descendiente interactivo dentro de option); :69-72 el `li.search-suggestion-empty` va sin rol dentro del `ul[role=listbox]` de :31; :1
  FIX: Mantener el foco en el input: dar `id="home-sug-N"` a cada opción, usar `input.setAttribute('aria-activedescendant', id)`, `aria-selected` en la opción activa, y navegar con Enter (leyendo la opción activa) en vez de mover el foco. Convertir las opciones en `<li role="option">` no interactivos (navegar por JS) o abandonar listbox y usar una lista de enlaces con `aria-live="polite"` que anuncie "N comunas". Al `<li>` vacío darle `role="option" aria-disabled="true"` o sacarlo del listbox.
[LPX-B6|MEDIUM] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeExtremes.astro
  CLAIM: Contraste no textual: las barras y los puntos del quintil 1 son literalmente invisibles. En el modo "Menor incidencia" —que es la mitad del toggle— TODAS las filas son nivel 1, así que la lista de barras se ve como una lista sin barras.
  EVID: HomeExtremes.astro:196-200: `.ext-bar{background: var(--bg)}` (#f5f8f8) y `.ext-fill.s1{background: var(--s1)}` (#dbeef0) ⇒ 1.09:1 (WCAG 1.4.11 exige 3:1 para objetos gráficos que transmiten información). `.ext-dot.s1` (10px) sobre `.extremes-card{background:var(--card)}` #ffffff ⇒ 1.17:1; s2 (#9fd0cd) ⇒ 1.72:1. Mismo problema en HomeSearchLive.astro:134-135 `.search-dot.s1` sobre `var(--card)`. T
  FIX: Dar a los puntos el mismo segundo canal que el SVG: `box-shadow: inset 0 0 0 1px rgba(28,42,43,.35)` en `.ext-dot`/`.search-dot`; y cambiar el track de la barra a `var(--line)` (#e2e9e8) con `outline:1px solid var(--line)` en el fill, o usar el borde del fill para separarlo del track.
[LPX-B7|MEDIUM] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeMiniMap.astro
  CLAIM: Los conteos de la franja de quintiles no llegan a AA sobre la banda más alta, y al activar una banda las demás caen a opacity .3, con lo que sus cifras quedan en ~1.9:1.
  EVID: HomeMiniMap.astro:128 `.mm-band{font-size:12px;font-weight:700;color:var(--ink)}` sobre `.mm-b5{background:var(--s5)}` (#c96b5a): #1c2a2b/#c96b5a = 4.09:1 < 4.5:1 exigido (12px bold NO es texto grande). HomeMiniMap.astro:131 `[data-active-band] .mm-band[aria-pressed='false']{opacity:.3}` aplica la opacidad a todo el botón, texto incluido.
  FIX: Subir el texto de las bandas a 13-14px o poner `color:#fff` con `text-shadow`/fondo más oscuro en b5; y en vez de bajar la opacidad del botón entero, atenuar solo el fondo (`background-image: linear-gradient(rgba(255,255,255,.7),rgba(255,255,255,.7))`) manteniendo el número legible.
[LPX-B8|MEDIUM] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeMiniMap.astro
  CLAIM: El mini-mapa reserva una columna de 226px para dibujar una silueta de ~74px de ancho: ~110px de columna vacía, que son justamente los que hacen reventar el grid de LPX-B2. Además pinta 343 puntos, no 346, mientras la nota anuncia rows.length=346, y esos ~40KB de SVG inline se envían también a móvil donde la columna está en display:none.
  EVID: HomeMiniMap.astro:28-40: W=190, PAD=12, `kY = min((560-24)/Δlat, (190-24)/(Δlng*0.82))`. Bounding box real de site/src/data/comuna-centroids.json tras el filtro `lng > -76` (HomeMiniMap.astro:32): lat -55.03..-17.83 (Δ37.20), lng -74.15..-67.91 (Δ6.25) ⇒ kY=536/37.2=14.41 domina, kX=11.82 ⇒ ancho dibujado = 6.25*11.82 ≈ 74px dentro de un viewBox de 190. Conteo: el fichero tiene 345 entradas (falta
  FIX: Fijar W ≈ 96-110 y estrechar la columna del grid de 226px a ~120px (recupera ~106px del déficit de B2); usar el conteo real de puntos (`dots.length`) en la nota o cambiar la copia a "346 comunas · CEAD {year}" desacoplada del mapa; y evaluar mover el SVG a un `<picture>`/no renderizarlo bajo 1024px si el peso importa.
[LPX-B9|MEDIUM] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeNewsPulse.astro
  CLAIM: La paleta de familias se duplica por tercera vez y se desincroniza: el punto de `homicidios` del pulso no coincide con el de la página de noticias y pierde el anillo que es el segundo canal visual (WR-02), de modo que a 8px `vida` y `homicidios` se leen como el mismo color.
  EVID: Paquete HomeNewsPulse.astro:50-54 `FAM_DOT.homicidios = 'oklch(0.48 0.16 25)'`, `vida = 'oklch(0.58 0.14 25)'` (mismo hue 25). Repo site/src/pages/es/noticias.astro:738-741 usa `homicidios: oklch(0.45 0.16 12)` y añade `[data-family='homicidios'] .fam-dot{box-shadow:0 0 0 1.5px #fff,0 0 0 3px var(--fam)}` con el comentario explícito "homicidios comparte territorio de tono con vida — un anillo es e
  FIX: Importar la paleta desde `config/rankingStrings.ts` (o extraerla a un módulo único) en vez de re-declarar FAM_DOT, y replicar el anillo de homicidios en `.pulse-dot` para no depender solo del tono.
[LPX-B10|LOW] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeMiniMap.astro
  CLAIM: Ninguna de las transiciones nuevas respeta prefers-reduced-motion, pese a que el trabajo más reciente del repo (map v2) ya sentó el precedente.
  EVID: Paquete: HomeMiniMap.astro:118 `.mm-dot{transition:opacity .15s}`, :128 `.mm-band{transition:opacity .15s}`, index.astro:187 / es/index.astro:201 `.hv2-pills a{transition:border-color .15s, background .15s}`. Repo: site/src/components/map/map-v2.css:102 `@media (prefers-reduced-motion: reduce){...}` es el único bloque del proyecto.
  FIX: Añadir al final de cada <style> nuevo: `@media (prefers-reduced-motion: reduce){ .mm-dot,.mm-band,.hv2-pills a{transition:none} }`.
[LPX-B11|LOW] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/components/home/HomeSearchLive.astro
  CLAIM: Colores hardcodeados que replican tokens existentes: rgba(28,42,43,…) es literalmente --ink (#1c2a2b) escrito a mano en dos sitios.
  EVID: HomeSearchLive.astro:131 `box-shadow: 0 10px 24px rgba(28,42,43,.16)` y HomeMiniMap.astro:118 `stroke: rgba(28,42,43,.3)` vs site/src/styles/global.css:15 `--ink: #1c2a2b`. (Los `#fff` sobre --primary/--ink sí tienen precedente en el repo, no los cuento.)
  FIX: Usar `color-mix(in srgb, var(--ink) 30%, transparent)` o definir `--ink-rgb: 28 42 43` en global.css y consumir `rgb(var(--ink-rgb)/.3)`.
[LPX-B12|LOW] C:/Users/Carlo/Downloads/Optimizar diseño de comunas (6)/install-landing/site/src/pages/index.astro
  CLAIM: El breakpoint intermedio es CSS muerto: entre 768 y 1023px se declara un grid de 2 columnas pero los dos únicos hijos visibles ocupan `grid-column: 1 / -1`, así que ya es una sola columna; el breakpoint de 767px no cambia nada.
  EVID: index.astro:158-167 (idéntico en es/index.astro:170-179): `@media(max-width:1023px){ .hv2-top{grid-template-columns:1fr 1fr} .hv2-map{display:none} .hv2-hero{grid-column:1/-1} .hv2-modules{grid-column:1/-1} }` seguido de `@media(max-width:767px){ .hv2-top{grid-template-columns:1fr} }`. El claim de INSTALL.md:150-151 ("<768px todo apila a una columna") es correcto por accidente: ya apila desde 1023
  FIX: Simplificar a `@media(max-width:1023px){ .hv2-top{grid-template-columns:1fr} .hv2-map{display:none} }` y borrar el bloque de 767px, o usar de verdad las 2 columnas (hero | módulos) en 768-1023px si esa era la intención.
CLEAN: Tokens: los 24 custom properties usados por el paquete (`--primary --bg --ink --muted --line --card --s1..--s5 --pin --radius --xs --sm --md --lg --xl --2xl --font --text-label --text-body --text-heading --weight-strong`) existen TODOS en site/src/styles/global.css:10-59. Cero tokens inventados. ~ Cero directivas `client:*` en el paquete: grep sobre install-landing/site solo devuelve las 4 menciones dentro de comentarios. Toda la interactividad es <script> vanilla con baseline server-rendered (D-05/D-06 respetado). ~ Gotcha `{expr}` dentro de <script>: ningún <script> del paquete interpola expresiones; los datos servidor→cliente viajan por `set:html` en el blob `#home-communes` (index.astro:42) y por `data-*` (data-commune-prefix, data-no-match, data-max-rate, data-band-note, data-default-note, data-title-high/low, data-all-types, data-window). Correcto. ~ Barrido completo de `[hidden]` + regla de display scoped: los otros 4 casos están limpios porque su CSS no declara `display` — `ul.search-suggestions` (HomeSearchLive:31/131), `p.extremes-note` (HomeExtremes:53/181), `[data-noscript-only]` h2+ol (HomeExtremes:66-67/180,188) y `.hv2-stat[data-home-press-stat]` (index.astro:64/180). Solo `.pulse-card` está roto (LPX-B3). ~ Dark mode: el sitio NO tiene tema oscuro — cero `prefers-color-scheme` en todo site/src (único media-query de preferencia del repo es `prefers-reduced-motion` en map-v2.css:102). No hay nada que respetar; los `#fff` y `var(--card)` del paquete no rompen nada. ~ Targets táctiles: el claim de INSTALL se cumple — `.mm-band{min-height:30px}` (HomeMiniMap:128) ≥30px, `.pulse-chip{min-height:26px}` (HomeNewsPulse:174) ≥26px, `.hv2-pills a{padding:7px 13px}` ≈34px, `.ext-row{padding:7px}` con dos líneas ≈45px, `.extremes-toggle button{padding:6px 12px}` ≈30px. ~ Controles muertos sin JS: correcto — los 5 botones de banda (HomeMiniMap:78) y los 2 del toggle (HomeExtremes:49-50) se sirven con `disabled` y solo el enhancer los habilita (HomeMiniMap:93, HomeExtremes:108). `aria-pressed` presente y sincronizado en bandas, toggles y chips. ~ SVG del mini-mapa: `role="img"` + `aria-label` coherente (HomeMiniMap:62) y `<title>` por círculo para tooltip de ratón; el histograma del pulso va con `aria-hidden="true"` (HomeNewsPulse:30) al ser puramente decorativo con eje textual al lado. Coherente. ~ Enlaces externos y XSS de texto: los titulares usan `target=_blank` + `rel="noopener"` (HomeNewsPulse:122-124) y TODO el texto derivado de incidentes entra por `textContent` (:131,135), nunca innerHTML — T-03-03-02 respetado. Igual en búsqueda y extremos. ~ Stacking del dropdown de sugerencias: `.search-hero-form{position:relative}` + `.search-suggestions{position:absolute;z-index:30}` funciona porque ningún hermano del grid (`.hv2-map`, `.hv2-modules`) crea contexto de apilamiento (sin z-index/transform/filter). Sin colisión con el header. ~ Cierre del combobox por clic fuera: `document.addEventListener('click', e => if(!form.contains(e.target)) close())` (HomeSearchLive:116-118) no mata el clic en una sugerencia porque las opciones están DENTRO del form. Escape cierra desde input y desde la lista.

=== AGENT findings: 9
[LPX-C1|BLOCKER] install-landing/site/src/components/home/HomeExtremes.astro (+ HomeSearchLive.astro, HomeNewsPulse.astro)
  CLAIM: Los tres enhancers construyen DOM con document.createElement y le ponen clases del <style> scoped. Astro compila el CSS scoped con estrategia "attribute" (.ext-row[data-astro-cid-XXXX]), y los nodos creados en runtime NO llevan ese atributo: todo lo re-renderizado sale SIN estilos.
  EVID: Default confirmado: site/node_modules/astro/dist/core/config/schemas/base.js:49 -> scopedStyleStrategy ... .default("attribute"); astro.config.mjs no lo sobreescribe. Salida real construida: site/dist/_astro/CommuneDirectory.Bmwul-fW.css -> `.chip[data-astro-cid-jda2hpxv]{...}`. Paquete: HomeExtremes.astro:121-150 (list.innerHTML=''+createElement de .ext-row/.ext-dot/.ext-bar/.ext-fill/.ext-rate) 
  FIX: Namespar bajo un ancestro server-rendered y declarar esas reglas con :global — p.ej. `:global(.extremes-list .ext-row){...}`, `:global(.search-suggestions .search-suggestion){...}`, `:global(.pulse-card .pulse-bar){...}` (mismo recurso que ya usan CommuneDirectory/RankingBoard), o bien re-renderizar reutilizando nodos server-rendered. Verificar en dist/ que ninguna clase creada por JS quede compilada con [data-astro-cid-*].
[LPX-C2|BLOCKER] install-landing/site/src/components/home/HomeNewsPulse.astro
  CLAIM: El módulo de noticias se sirve con atributo `hidden` pero su estilo scoped declara `.pulse-card{display:flex}`. Una declaración de autor gana a la regla UA `[hidden]{display:none}`: el card queda SIEMPRE visible. Con current.json 404/vacío el módulo no desaparece (viola D-15) y en carga normal se ve un card vacío hasta que resuelve el fetch.
  EVID: Paquete: HomeNewsPulse.astro:25 `<div class="pulse-card" ... hidden>` + :164 `.pulse-card { ... display: flex; flex-direction: column; }`. No existe regla global compensatoria: grep de "hidden" en site/src/styles/global.css no devuelve nada. Es exactamente la regresión ya vivida en el repo: commit 4a2f0aa 'fix(directory): filter chips ignored the hidden attribute' -> se tuvo que añadir `.dir-chips
  FIX: Añadir `.pulse-card[hidden]{display:none}` al <style> de HomeNewsPulse.astro. Auditar de paso el resto de nodos con `hidden` del paquete (`.hv2-stat`, `.search-suggestions`, `.extremes-note`, `[data-noscript-only]`): esos hoy no declaran display y sí funcionan, pero conviene blindarlos igual.
[LPX-C3|HIGH] install-landing/site/src/components/home/HomeNewsPulse.astro:38,55
  CLAIM: El pulso etiqueta familias con FAMILY_LABELS_PAGE, que NO tiene la clave `sexuales`; el fallback es la clave cruda, así que el home mostrará un chip literal "sexuales" (sin traducir en EN, en minúscula en ES). INSTALL.md afirma lo contrario.
  EVID: site/src/lib/familyDefs.ts:69-78 FAMILY_LABELS_PAGE = {vida, propiedad, robos_violentos, incivilidades, vif, drogas, armas, homicidios} — sin `sexuales`. El paquete: HomeNewsPulse.astro:55 `const famLabel = (k) => FAMILY_LABELS_PAGE[k]?.[locale] ?? k;`. Datos reales site/public/data/incidents/current.json: {vida:1006, robos_violentos:224, propiedad:107, drogas:91, sexuales:49, ...} -> `sexuales` e
  FIX: Usar FAMILY_LABELS_EN/FAMILY_LABELS_ES (mismo origen que news.astro) en HomeNewsPulse, o añadir `sexuales: { en:'Sexual crimes', es:'Delitos sexuales' }` a FAMILY_LABELS_PAGE. Mantener `sexuales` como familia solo-prensa (no tocar los 7 FAMILY_KEYS de CEAD).
[LPX-C4|MEDIUM] install-landing/site/src/components/home/homeData.ts:123-126
  CLAIM: La franja de quintiles cuenta las 346 comunas (incluidas las de baja población) pero la lista de extremos filtra a las 256 no-baja-población: al pulsar una banda la pantalla muestra dos cifras contradictorias para la misma banda.
  EVID: homeData.ts:123-126 `bandCounts` se acumula sobre `rows` (todas). HomeExtremes.astro:95 `const eligible = communes.filter(c => !c.p)` y :157 `note = bandNoteTpl.replace('{n}', String(scope.length))`. Cálculo con los datos reales del repo (data/cead): bandCounts = [76,67,64,62,77] vs banda-en-extremos = [52,51,51,51,51] (90 de 346 comunas son low_population). El botón dice "76", la nota del minimap
  FIX: Unificar el universo: calcular bandCounts sólo sobre `!low_population` (coherente con las tablas y con levelForRate, que ya se calibra sobre no-baja-población), o dejar explícito en el aria/nota que la franja cuenta 346 y la lista 256.
[LPX-C5|MEDIUM] install-landing/INSTALL.md:117-155
  CLAIM: La verificación propuesta (astro check + vitest + dev) no ejerce ninguna de las puertas reales del repo: `npm run validate` (16 validadores sobre dist/) nunca se ejecuta, y vitest tiene cobertura CERO sobre el home.
  EVID: site/package.json scripts: `validate: node scripts/validate/all.mjs`; scripts/validate/all.mjs corre 16 validadores, varios con asserts sobre el home: spine.mjs:253-286 [K] exactamente un <h1> en dist/index.html y dist/es/index.html; seo.mjs:185-186 + :227-252 [R] forma de ItemList en home EN/ES; forbidden-language.mjs sobre texto visible de dist/; coverage.mjs [B] no-orphan-link. Tests existentes
  FIX: Añadir al INSTALL: `cd site && npm run build && npm run validate` en UN solo comando encadenado (no dos invocaciones), y revisar a ojo dist/index.html + dist/es/index.html (1 h1, JSON-LD WebSite+Organization+2 ItemList en EN, 2 ItemList en ES).
[LPX-C6|MEDIUM] install-landing/site/src/pages/index.astro y es/index.astro
  CLAIM: Pérdida de texto indexable en la página SEO más importante: desaparecen la sección CTA del mapa (h2 + párrafo), los dos subencabezados de tabla, el texto descriptivo de las 6 tarjetas guía (que se reduce a píldoras de 2-3 palabras) y las columnas de rank nacional + tendencia de CommuneRankingTable. También se pierde el selector de año del enhancer.
  EVID: Actual site/src/pages/index.astro:111,117 (subheads con la métrica y el año), :125-131 (h2 'Explore the Interactive Map' + cuerpo), :137-142 (anchors descriptivos tipo 'Chile Crime Map — interactive choropleth', 'Is Chile Safe? — national overview'); es/index.astro:126-131,137-142 equivalentes. Paquete: index.astro:69-78 y es/index.astro:78-87 reducen todo a píldoras ('Chile Crime Map', 'Is Chile 
  FIX: Rescatar el texto: dar a las píldoras su anchor text descriptivo actual (o un `title`/subtítulo por píldora), y reponer 1-2 frases junto al minimapa con el h2 del mapa. Prosa 'Sobre los datos' / 'About the Data' sí está verbatim — no tocarla.
[LPX-C7|LOW] install-landing/site/src/components/home/HomeMiniMap.astro:32,61
  CLAIM: El minimapa promete 346 puntos y la nota imprime '346 comunas', pero sólo se dibujan 343: falta un centroide en el dataset y dos comunas insulares se filtran.
  EVID: Conteo real: data/cead/meta/index.json = 346 entradas; site/src/data/comuna-centroids.json = 345 (1 CUT sin centroide); 2 centroides con lng < -76 (Rapa Nui / Juan Fernández) los descarta HomeMiniMap.astro:32 `.filter(... c.lng > -76)`. La nota usa HomeMiniMap.astro:61 `fmt(t.glance_note, { n: rows.length })` = 346. INSTALL.md:20,135 afirma '346 {cut,lat,lng}' y '346 puntos'.
  FIX: Imprimir `pts.length` (o `rows.length` sólo si se dice 'comunas con datos', no 'puntos'), y dejar la exclusión insular explícita en la nota o en el aria-label del SVG.
[LPX-C8|LOW] install-landing/site/src/pages/index.astro:42 y es/index.astro:51
  CLAIM: El blob #home-communes se serializa con JSON.stringify crudo dentro de set:html, saltándose el escape que TODOS los demás script[type=application/json] del repo aplican.
  EVID: Paquete: `<script type="application/json" id="home-communes" set:html={JSON.stringify(homeBlob(rows))}>`. Repo: site/src/components/CommuneDirectory.astro:251 y RankingBoard.astro:232 usan `.replace(/</g,'\\u003c')`; site/src/pages/news.astro:425 y es/noticias.astro:424 usan `escapeForInlineScript(...)`. set:html no escapa nada por definición. El dato hoy es de confianza (nombres/slugs/regiones CE
  FIX: Aplicar el mismo escape que el resto (`.replace(/</g,'\\u003c')` o `escapeForInlineScript`) para que un futuro campo de texto libre en el blob no abra un `</script>`.
[LPX-C9|LOW] install-landing/site/src/components/home/HomeMiniMap.astro:62-68
  CLAIM: Peso de HTML del home: ~20 KB de blob JSON inline + 343 <circle> cada uno con su <title> hijo, y en <1024px el SVG se descarga igual aunque está display:none — el móvil paga el coste completo de un módulo que no ve.
  EVID: HomeMiniMap.astro:63-67 emite un <circle> con <title> por comuna; index.astro:158-163 / es:170-175 `@media (max-width:1023px){ .hv2-map{display:none} }`. El blob (346 registros {n,s,r,l,g,p}) va inline en index.astro:42. INSTALL.md:160-163 estima ~20 KB sólo para el blob.
  FIX: Si el presupuesto de HTML importa: quitar el <title> por punto (la info ya vive en la búsqueda y en las listas; con role="img" en el SVG los lectores de pantalla los ignoran igual) y/o recortar el blob a las comunas necesarias. Medir el tamaño de dist/index.html antes/después.
CLEAN: Contrato HomeLayout: los dos index.astro nuevos pasan EXACTAMENTE las mismas props que los actuales (lang/title/description/enPath='/'/esPath='/es/'/pageType='home'), así que head, canonical, hreflang y OG no cambian; el <div class="home-wide"> y el AdSlot position="bottom" siguen inyectados por HomeLayout.astro:45-50 (el paquete no añade slot ad-content, igual que hoy). ~ H1 verbatim y único en ambos locales: EN 'Is Chile Safe? Official CEAD Crime Data for All 346 Communes' (index.astro:83 -> paquete:47) y ES '¿Es seguro Chile? Datos CEAD para las 346 comunas' (es/index.astro:83 -> paquete:56). Los demás encabezados son h2. Cumple spine.mjs [K]. ~ Prosa 'About the Data' / 'Sobre los datos' copiada verbatim, párrafo a párrafo, incluida la atribución CEAD completa y el enlace a metodología (/methodology/ y /es/metodologia/). También se conserva el intro bajo el H1. ~ JSON-LD: firmas reales verificadas contra site/src/lib/jsonld.ts — buildWebSite()/buildOrganization() sin args, buildItemList(rows,locale,{name,order}) exige row.slug (HomeCommune lo tiene). EN emite WebSite+Organization+2 ItemList, ES sólo 2 ItemList — idéntico al actual. Sin BreadcrumbList (D-07). Los nombres de lista son los mismos strings actuales, así que no disparan assertSafeName (jsonld.ts:19-31). numberOfItems pasa de 6 a 7; seo.mjs [R] sólo valida forma, no cardinalidad. ~ Enlaces: las 6 rutas guía de CADA locale sobreviven como píldoras, más el mapa y el comparador. EN: /chile-crime-map/, /is-chile-safe/, /is-santiago-safe/, /safest-cities-in-chile/, /methodology/, /communes/, /map/, /compare/. ES: /es/mapa-delito-chile/, /es/mapa-seguridad-santiago/, /es/comunas-mas-seguras-chile/, /es/metodologia/, /es/delitos-por-comuna/, /es/comunas/, /es/mapa/, /es/comparar/. Todas existen como página real en site/src/pages (compare/index.astro y es/comparar/index.astro incluidos). Slugs ES hardcodeados, cero getRelativeLocaleUrl. ~ Cero directivas client:* en el paquete completo (grep sobre los 6 archivos): toda la interactividad es <script> vanilla, sin islas React. No afecta al guard de map.mjs sobre páginas de contenido sin React. ~ Imports: los dos index.astro nuevos sólo importan HomeLayout, los 4 componentes home/ del propio paquete, homeData.ts y lib/jsonld.ts — todo existe tras la instalación. homeData.ts usa firmas reales de lib/data.ts (loadIndex/loadCommune/latestCompleteYearRate/loadNationalAverage/loadRegion/regionFileId) y de lib/colorScale.ts (levelForRate(rate, sortedRates)). El import estático de src/data/comuna-centroids.json funciona (tsconfig.json tiene resolveJsonModule y Vite resuelve JSON). ~ Ningún archivo compartido tocado hoy cambió contrato bajo el paquete: git log desde 2026-08-08 sobre lib/data.ts, lib/jsonld.ts, lib/colorScale.ts, lib/familyDefs.ts, HomeLayout.astro y BaseLayout.astro no muestra commits del sprint de mapa v2 / rankings v2 / noticias / comparador. CommuneRankingTable y RankingTableEnhancer siguen usados en commune/[slug], region/[slug], is-chile-safe, is-santiago-safe, safest-cities-in-chile, crime-ranking/[crime] y es/comunas-mas-seguras-chile — sacarlos del home no los deja huérfanos. ~ TypeScript dentro de <script> y <script type="application/json"> sin is:inline son patrones ya establecidos y en producción en este repo (CommuneDirectory.astro usa tipos y casts en su script; RankingTableEnhancer.astro:56, commune/[slug].astro:274-275, news.astro:425 usan script JSON). No es un riesgo nuevo. ~ Tone Guard: ninguna string nueva (EN/ES) califica territorio en absoluto — 'Lowest/Highest reported rate', 'Menor/Mayor incidencia reportada', bandas 'Incidencia muy baja..muy alta'. Ninguna coincide con FORBIDDEN_TERMS de scripts/validate/forbidden-language.mjs ('la mas segura','the safest','most dangerous', etc.). Atribución CEAD presente en el hero nuevo (hv2-source) además de la prosa. Las tasas se muestran tal cual vienen (per-100k), sin re-escalar: sólo Math.round + toLocaleString. ~ Sin bucles byFamily en el paquete, así que la colisión FAMILY_SLUGS.vida.en==='homicide' no aplica; el pulso agrupa por i.family de las noticias, no genera enlaces por familia. ~ Enlaces externos de incidentes con target=_blank + rel="noopener" y todo el texto de incidente vía textContent (HomeNewsPulse.astro:123-135) — cumple T-03-03-02; el fetch falla en silencio sin console.error (D-15 a nivel de lógica; el fallo es sólo visual, ver LPX-C2). ~ i18n/rutas: es/index.astro sigue siendo página estática sin getStaticPaths (igual que hoy); no cambian rutas ni el filtro de sitemap de astro.config.mjs (home EN/ES ya listadas explícitamente en sitemapFilter). robots/sitemap sin impacto. ~ Drift de INSTALL.md: dice 407/402 líneas y el repo tiene 406/401 — es diferencia de conteo de newline final, no cambio de contenido; los index.astro actuales siguen siendo los de Phase 12 Plan 03 'Hybrid C+B' que el paquete asume. Como son reemplazos completos, el drift no afecta la instalación. ~ Pasos del INSTALL sin dependencia de estado entre procesos: mkdir + cp + rollback por git checkout son atómicos; el único riesgo OneDrive aparece al validar (cubierto en LPX-C5).
