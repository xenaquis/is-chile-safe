---
id: 260810-pz5
title: Eliminar deuda técnica documentada de los installs v2 (mapa, comparador, rankings, noticias)
type: quick
executed_via: dynamic-workflow (sonnet ejecutores secuenciales, opus verificador, fable estratega/validador)
strategy_inputs: pase visual BrowserOS del estratega (elimina VERIF-UI y produce las medidas reales de --map-topbar-h); SUMMARYs de 260810-pbj/ocs/l3e/kcj; REVIEW.md de l3e y kcj
must_haves:
  truths:
    - "Ninguna regresión: los 4 gates (check/test/build/validate) verdes tras CADA commit de área."
    - "El badge de año parcial nunca se solapa con la topbar: --map-topbar-h ≥ altura real medida en cada tramo."
    - "cmp_homicide_no_data deja de afirmar 'Sin casos reportados' ante dato AUSENTE (en island Y en [pair].astro)."
    - "Rankings: ctrl/cmd-click en el panel regional vuelve a abrir en pestaña; th recupera scope=col; guardia [hidden]{display:none} presente (pitfall Astro scoped-style conocido)."
    - "Nunca calificar territorios de seguros/peligrosos en ningún string tocado."
  artifacts:
    - "site/src/components/map/map-v2.css (tiers medidos de --map-topbar-h)"
    - "site/src/config/i18n.ts (claves huérfanas fuera + reword cmp_homicide_no_data)"
    - "site/src/components/RankingBoard.astro + RankingHub.astro + crime-ranking/[crime].astro + es/ranking-delito/[crime].astro"
    - "site/src/pages/news.astro + es/noticias.astro (WR-02)"
  key_links:
    - ".planning/quick/260810-l3e-instalar-rediseno-rankings-v2-desde-paqu/260810-l3e-REVIEW.md (spec exacta de F-01..F-16)"
---

# Plan: barrido de deuda técnica de los 4 installs v2 de hoy

## Deuda YA eliminada por el estratega (pase visual BrowserOS, sin código)

- **VERIF-UI (pbj)**: verificado en vivo sobre dist/ — rail envuelve sin overflow (0px en 481–1280), swatches 14×14, band-btns 44px, leyenda 208px, capa Homicidios con fila "Sin homicidios reportados 163" (conteos suman 346), aislamiento "Aislado: Muy alta ×" + clear OK, franja: 31 barras (día más antiguo 11-jul presente = fix C2 vivo), barras 44px de alto con aria-label, hint sr-only invisible (79bb23c vivo), scope "Vida y convivencia · 1006" con capa Homicidios (fix C3 vivo). EN y ES.
- **Medidas reales de topbar** (EN ≡ ES, iframe sobre dist/):
  | ancho | .map-topbar real | var actual | veredicto |
  |---|---|---|---|
  | ≤480 | 111px (rail oculto) | 143px | sin solape — NO tocar |
  | 481–559 | 343px | 200px | SOLAPE |
  | 560–719 | 293px | 200px | SOLAPE |
  | 720–1023 | 243px | 200px | SOLAPE |
  | ≥1024 | 217px | 117px | SOLAPE |

## Ejecutor 1 (Sonnet) — mapa + comparador (dueño único de i18n.ts)

1. **MAP-01 — `--map-topbar-h` con valores medidos.** En map-v2.css, reemplazar el bloque único `@media (min-width:481px) and (max-width:1023px) { --map-topbar-h:200px }` por tiers progresivos sobre `.map-stage` (map-v2.css carga después de map.css, min-width asciende):
   `@media (min-width:481px){ 351px }` · `@media (min-width:560px){ 301px }` · `@media (min-width:720px){ 251px }` · `@media (min-width:1024px){ 225px }` (real medido + 8px slack). Actualizar el comentario MPX-B3/C8 con la tabla de medidas. Antes: `grep -rn "map-topbar-h" site/src` para confirmar que el único consumidor es `.partial-year-badge` (si hay otro, revisar que un var mayor no lo rompa).
2. **MAP-02 — claves i18n huérfanas.** Eliminar `map_entry_family` y `map_entry_mode` de i18n.ts (interfaz línea ~238-239 + EN ~476-477 + ES ~717-718). Verificado: cero usos fuera de i18n.ts.
3. **CMP-01 (V-03) — prop muerta.** Quitar `cmp_avg_column` de los objetos strings de `compare/index.astro:50` y `es/comparar/index.astro:43`; si la interfaz de strings del ComparatorIsland la declara, quitarla también. NO tocar la clave en i18n.ts si otra página la usa (grep primero; si nadie la usa, eliminarla también de i18n.ts).
4. **CMP-02 (CX-C9) — reword dato ausente.** i18n.ts: `cmp_homicide_no_data` EN `'No reported cases — CEAD {year}'` → `'No homicide data — CEAD {year}'`; ES `'Sin casos reportados — CEAD {year}'` → `'Sin datos de homicidios — CEAD {year}'`. Antes de editar, confirmar que las 5 usos (island :731 y [pair].astro ×4) disparan ante dato ausente (no ante cero real); si alguno significa cero-real, NO reword global — crear clave nueva para el caso ausente y documentar.
5. **CMP-03 (V-02) — cosmética natMissing.** En ComparatorIsland: con `national.json` caído, no renderizar el span de año vacío (los guiones de la sr-table se quedan — son honestos). Cambio mínimo.

Gates + commit atómico: `fix(deuda): map topbar-h measured tiers + orphan i18n keys + comparator wording (MAP-01/02, CMP-01/02/03)`.

## Ejecutor 2 (Sonnet) — rankings (spec = secciones F-* de 260810-l3e-REVIEW.md; leerlas ANTES de editar)

6. **RKD-01 (F-01 MEDIUM)** — `/crime/homicide/` y `/es/delito/homicidios/` recuperan ≥2 enlaces entrantes (el hub dejó de enlazar la familia homicidios; aplicar el fix recomendado en la sección F-01 del REVIEW).
7. **RKD-02 (F-02 MEDIUM)** — las 16 páginas de ranking recuperan un `<h2>` visible (fix recomendado en F-02).
8. **RKD-03 (F-03 MEDIUM)** — panel regional: `preventDefault()` solo cuando `!e.ctrlKey && !e.metaKey && !e.shiftKey && !e.altKey && e.button === 0`; exponer estado activo con `aria-pressed`/`aria-current` además de la clase CSS.
9. **RKD-04 (F-04)** — al auto-ocultarse el botón activo, mover el foco a un control vivo (no dejarlo caer al body).
10. **RKD-05 (F-05)** — restaurar `scope="col"` en los `<th>`.
11. **RKD-06 (F-06)** — `dirty` incluye `state.dir` (línea exacta en REVIEW:255).
12. **RKD-07 (F-07)** — el H2 sr-only no fija "(2025)": actualizarlo al cambiar de año o quitar el año del heading.
13. **RKD-08 (F-08)** — añadir guardia `[hidden]{display:none}` al scoped style de RankingBoard (pitfall conocido: display de autor mata el atributo hidden — ya mordió en el directorio).
14. **RKD-09 (F-09)** — retro-portar B-07/B-08 a CommuneDirectory (ver qué son en el REVIEW y aplicar igual).
15. **RKD-10 (F-10)** — no envolver en `<mark>` cuando la query normaliza a `''`.
16. **RKD-11 (F-15)** — los 6 enlaces con texto idéntico ganan aria-label diferenciado por destino.
17. **RKD-12 (F-16)** — hub ES recupera el enlace a `/es/mapa/`; ambos hubs recuperan la línea "Fuente: CEAD" del pie. Atribución obligatoria (regla editorial).
18. **RKD-13 (B-09 LOW, SUMMARY l3e)** — estado vacío con `hideLowpop` activo: pista condicional (p.ej. "N comunas de baja población ocultas — desactiva el filtro").

Gates + commit atómico: `fix(deuda): rankings review follow-ups F-01..F-10, F-15, F-16, B-09`.

## Ejecutor 3 (Sonnet) — noticias (spec = REVIEW/SUMMARY de 260810-kcj)

19. **NWS-01 (WR-02)** — el color de familia deja de ser color-only: cada fila/chip con punto de familia gana nombre accesible (aria-label o texto visible) y `vida` vs `homicidios` se distinguen por algo más que el tono (borde/anillo o glifo), en news.astro Y es/noticias.astro. Leer la sección WR-02 del REVIEW de kcj para el fix recomendado.
20. **NWS-02 (stretch)** — des-duplicar el script (~258 líneas) y el stylesheet (~528 líneas) gemelos byte-idénticos entre ambas páginas: extraer a un componente/archivo compartido importado por ambas. CUIDADO pitfall Astro: `{expr}` dentro de `<script>` se renderiza literal — si los gemelos existen porque son estáticos, la extracción es segura; verificar byte-igualdad ANTES (`diff` de los bloques). Si algo interpola, abortar este ítem y documentar el porqué en el reporte.
21. **NWS-03 (stretch, con NWS-02)** — paleta de familias única: si NWS-02 procede, mover la paleta duplicada al recurso compartido; si además coincide con la de los pins del mapa, dejar UN origen (CSS vars o módulo) sin cambiar ningún color visible.

Gates + commit atómico: `fix(deuda): news family color accessibility + dedup shared assets (WR-02, stretch)`. Si los stretch se abortan, commit solo con NWS-01.

## Explícitamente FUERA de alcance (decisión del estratega, queda documentado)

- Barras-día <44px de ancho (pbj): tradeoff de diseño ya mitigado y comentado en CSS — no es eliminable sin rediseñar la franja.
- F-11/F-12/F-13 (INFO, l3e): controles inertes sin JS (patrón del sitio), columnas perdidas en móvil, 220KB data-rates por página — decisiones de diseño/perf que merecen fase propia, no un barrido.
- Topbar de 343px en 481–559px: es el coste del wrap de 10 chips (criterio 2 manda); si molesta, iteración de diseño aparte (chips compactos).

## Verificación (Opus, post-ejecución)

- Los 3 commits tocan solo sus áreas; diff acumulado sin tocar: map.css, ChoroplethLayer.ts, colors.ts, mapFilterDefs.ts, newsFacets.ts, newsFilterLogic.test.ts, [pair].astro (salvo si CMP-01 exige su interfaz), data/.
- Greps por ítem (18-21 aplicados o abortados con razón escrita).
- i18n.ts: sin map_entry_family/map_entry_mode; cmp_homicide_no_data nuevo texto EN/ES; ninguna otra clave alterada.
- Re-correr los 4 gates en UN comando desde site/ y reportar números.
- Sin "seguro/peligroso"/"safe/dangerous" calificativo en strings tocados; "Fuente: CEAD" presente en ambos hubs de rankings.
- mapFilterDefs.test.ts y newsFilterLogic.test.ts sin diff.

## Rollback

`git revert` por commit de área (3 commits independientes).
